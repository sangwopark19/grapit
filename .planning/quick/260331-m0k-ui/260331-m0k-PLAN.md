---
phase: quick
plan: 260331-m0k
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/app/globals.css]
autonomous: true
requirements: []
must_haves:
  truths:
    - "필터 탭 전환 시 컨테이너가 좌우로 밀리지 않는다"
    - "스크롤바 유무와 관계없이 레이아웃 너비가 일정하다"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "scrollbar-gutter: stable on html element"
      contains: "scrollbar-gutter"
  key_links: []
---

<objective>
장르 목록 페이지에서 필터 탭 전환 시 발생하는 레이아웃 시프트 버그 수정

Purpose: 콘텐츠 양에 따라 스크롤바가 나타나거나 사라지면서 mx-auto 컨테이너가 좌우로 밀리는 현상 제거
Output: globals.css에 html { scrollbar-gutter: stable } 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: html에 scrollbar-gutter: stable 추가</name>
  <files>apps/web/app/globals.css</files>
  <action>
    globals.css의 body 규칙 직전(line 96 앞)에 html 규칙을 추가한다:

    ```css
    html {
      scrollbar-gutter: stable;
    }
    ```

    이 CSS 속성은 콘텐츠가 overflow되지 않아도 스크롤바 트랙 공간을 미리 확보하여,
    스크롤바 출현/소멸에 따른 뷰포트 너비 변동(mx-auto 컨테이너 좌우 시프트)을 방지한다.

    주의사항:
    - `overflow-y: scroll` 방식은 사용하지 않는다 (스크롤바가 항상 보여서 시각적으로 지저분함)
    - `scrollbar-gutter: stable both-edges`는 사용하지 않는다 (양쪽에 공간이 생겨 불필요)
    - 기존 body 규칙이나 @theme 블록은 변경하지 않는다
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && grep -n "scrollbar-gutter" apps/web/app/globals.css</automated>
  </verify>
  <done>globals.css의 html 규칙에 scrollbar-gutter: stable이 존재하며, 장르 페이지 탭 전환 시 레이아웃 시프트가 발생하지 않는다</done>
</task>

</tasks>

<verification>
- globals.css에 `html { scrollbar-gutter: stable; }` 규칙이 존재한다
- 기존 body 규칙, @theme 블록이 변경되지 않았다
- dev 서버 실행 시 컴파일 에러가 없다
</verification>

<success_criteria>
장르 목록 페이지(/genre/[genre])에서 필터 탭("전체", "요즘HOT" 등) 전환 시 mx-auto 컨테이너가 좌우로 밀리지 않는다
</success_criteria>

<output>
After completion, create `.planning/quick/260331-m0k-ui/260331-m0k-SUMMARY.md`
</output>
