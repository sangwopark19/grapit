---
quick_id: 260420-ci-toss-secrets-restore
date: 2026-04-20
status: in-progress
---

# Quick Task — CI Toss test secrets 복구

## Task
PR #17 CI 실패 복구. `Verify Toss test secrets present` hard-gate 에서 `TOSS_CLIENT_KEY_TEST` / `TOSS_SECRET_KEY_TEST` 부재로 exit 1.

## Root Cause
직전 quick task `260420-cd7` 에서 두 secret을 "`deploy.yml` 어디서도 참조되지 않는 고아 시크릿"으로 오판하여 제거. 그러나 Phase 09-03 D-13 격리 설계상 이 secret들은 **의도적으로 `deploy.yml`에 주입 금지**이며 `ci.yml` 전용으로 존재해야 함 (E2E Toss 결제 테스트 + non-fork hard-gate).

즉, orphan 검사를 `deploy.yml` 단독 기준으로 돌리면 D-13 격리 시크릿이 반드시 false positive로 잡힘.

## Changes
1. `gh secret set TOSS_CLIENT_KEY_TEST` = `test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm`
2. `gh secret set TOSS_SECRET_KEY_TEST` = `test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6`
   - 값은 Toss 공식 docs 공개 테스트 키 (09-03-PLAN.md §Installation 기준)
   - 동일 값이 이미 `.env.example`, `NEXT_PUBLIC_TOSS_CLIENT_KEY` GitHub secret 에 등록되어 있으므로 추가 노출 리스크 없음
3. `260420-cd7-SUMMARY.md` 에 보정 메모 추가 (재발 방지)
4. `gh run rerun <failed-run-id>` 로 PR #17 CI 재실행 → 통과 확인

## Out of Scope
- `.github/workflows/ci.yml` 변경 없음 (gate 로직은 설계 의도대로 동작)
- `deploy.yml` 변경 없음 (D-13 격리 유지)
