---
phase: quick
plan: 260413-jw1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/database/seed.mjs
autonomous: true
must_haves:
  truths:
    - "seed.mjs 실행 후 admin@grapit.test 유저가 DB에 존재한다"
    - "해당 유저의 role이 'admin'이다"
    - "해당 유저의 password_hash가 argon2id로 해싱되어 있다"
    - "admin@grapit.test / TestAdmin2026! 로 로그인 후 /admin 페이지에 접근할 수 있다"
  artifacts:
    - path: "apps/api/src/database/seed.mjs"
      provides: "어드민 유저 시드 INSERT"
      contains: "admin@grapit.test"
  key_links:
    - from: "apps/api/src/database/seed.mjs"
      to: "users table"
      via: "INSERT INTO users with role='admin'"
      pattern: "INSERT INTO users"
---

<objective>
seed.mjs에 어드민 유저(admin@grapit.test)를 추가하여 어드민 페이지 접속이 가능하도록 수정한다.

Purpose: 현재 seed 스크립트가 유저를 전혀 생성하지 않아 admin@grapit.test 계정이 DB에 없음. 회원가입 API는 role을 항상 'user'로 고정하므로, seed에서 직접 admin role 유저를 INSERT해야 한다.
Output: 수정된 seed.mjs -- 실행 시 어드민 유저가 생성됨
</objective>

<context>
@apps/api/src/database/seed.mjs
@apps/api/src/database/schema/users.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: seed.mjs에 어드민 유저 INSERT 추가</name>
  <files>apps/api/src/database/seed.mjs</files>
  <action>
seed.mjs 파일 상단에 argon2 import를 추가한다:
```js
import argon2 from 'argon2';
```

기존 clean 섹션(DELETE 문들)에 users 관련 테이블 삭제를 FK 순서에 맞게 추가한다. 시드 어드민 유저만 삭제하도록 조건부 DELETE를 사용한다:
```js
await client.query("DELETE FROM refresh_tokens WHERE user_id IN (SELECT id FROM users WHERE email = 'admin@grapit.test')");
await client.query("DELETE FROM social_accounts WHERE user_id IN (SELECT id FROM users WHERE email = 'admin@grapit.test')");
await client.query("DELETE FROM terms_agreements WHERE user_id IN (SELECT id FROM users WHERE email = 'admin@grapit.test')");
await client.query("DELETE FROM users WHERE email = 'admin@grapit.test'");
```

이 DELETE 문들은 기존 DELETE 문들(banners, castings 등) 바로 앞에 배치한다. 시드 어드민만 삭제하므로 실제 유저 데이터에는 영향 없다.

Banners 삭제 문 바로 뒤(즉 모든 기존 DELETE 직후), `console.log('Cleared existing data')` 바로 앞에 어드민 유저 생성 로직을 추가한다:

```js
// Admin user
const adminPasswordHash = await argon2.hash('TestAdmin2026!', {
  type: argon2.argon2id,
  memoryCost: 19456,
  timeCost: 2,
  parallelism: 1,
});

await client.query(`
  INSERT INTO users (id, email, password_hash, name, phone, gender, country, birth_date, role, is_phone_verified, is_email_verified)
  VALUES (gen_random_uuid(), 'admin@grapit.test', $1, '관리자', '010-0000-0000', 'unspecified', 'KR', '1990-01-01', 'admin', true, true)
`, [adminPasswordHash]);
console.log('Inserted admin user: admin@grapit.test');
```

argon2 설정은 CLAUDE.md에 명시된 값 그대로 사용: memory 19 MiB (19456 KiB), iterations 2, parallelism 1.
is_phone_verified와 is_email_verified를 true로 설정하여 어드민이 인증 관련 제약 없이 동작하도록 한다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && node -e "import('argon2').then(a => a.hash('test', {type: 2, memoryCost: 19456, timeCost: 2, parallelism: 1}).then(h => console.log('argon2 OK:', h.startsWith('\$argon2id'))))" 2>/dev/null || echo "argon2 check skipped"</automated>
    grep -q "admin@grapit.test" apps/api/src/database/seed.mjs && echo "PASS: admin email found in seed" || echo "FAIL: admin email not found"
    grep -q "import argon2" apps/api/src/database/seed.mjs && echo "PASS: argon2 import found" || echo "FAIL: argon2 import not found"
    grep -q "role.*admin" apps/api/src/database/seed.mjs && echo "PASS: admin role found" || echo "FAIL: admin role not found"
  </verify>
  <done>
    - seed.mjs에 argon2 import가 존재한다
    - seed.mjs에 admin@grapit.test INSERT 문이 존재한다
    - role='admin'으로 설정되어 있다
    - password는 argon2id로 해싱된다
    - 기존 시드 데이터(venues, performances 등) 로직은 변경 없다
    - 재실행 시 기존 시드 어드민만 삭제 후 재생성 (일반 유저 데이터 보존)
  </done>
</task>

<task type="auto">
  <name>Task 2: seed 실행하여 어드민 유저 생성 확인</name>
  <files>apps/api/src/database/seed.mjs</files>
  <action>
seed 스크립트를 실행하여 어드민 유저가 정상 생성되는지 확인한다:
```bash
cd /Users/sangwopark19/icons/grapit
source .env 2>/dev/null || true
node apps/api/src/database/seed.mjs
```

실행 로그에 "Inserted admin user: admin@grapit.test"가 출력되어야 한다.
seed 완료 후 DB에서 어드민 유저를 직접 조회하여 확인한다:
```bash
node -e "
import pg from 'pg';
const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL });
const res = await pool.query(\"SELECT email, role, name, password_hash IS NOT NULL as has_password FROM users WHERE email = 'admin@grapit.test'\");
console.log(res.rows[0]);
await pool.end();
"
```

결과가 `{ email: 'admin@grapit.test', role: 'admin', name: '관리자', has_password: true }`와 일치해야 한다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && node -e "import pg from 'pg'; const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL }); const res = await pool.query(\"SELECT email, role FROM users WHERE email = 'admin@grapit.test'\"); console.log(res.rows[0]?.role === 'admin' ? 'PASS' : 'FAIL'); await pool.end();"</automated>
  </verify>
  <done>
    - seed 스크립트가 에러 없이 완료된다
    - DB에 admin@grapit.test 유저가 존재한다
    - role이 'admin'이다
    - password_hash가 null이 아니다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script -> DB | 시드 데이터가 DB에 직접 INSERT됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I (Information Disclosure) | seed.mjs 내 평문 비밀번호 | accept | 개발용 테스트 계정. 프로덕션에서는 seed를 실행하지 않으며, .env의 DATABASE_URL이 프로덕션 DB를 가리키지 않도록 관리. 비밀번호는 argon2id로 해싱 후 저장. |
| T-quick-02 | E (Elevation of Privilege) | admin role 직접 INSERT | accept | seed 스크립트는 개발/스테이징 전용. 프로덕션 어드민은 DB 직접 조작 또는 별도 관리 스크립트로 생성. |
</threat_model>

<verification>
1. seed.mjs 실행이 에러 없이 완료
2. DB에서 admin@grapit.test 조회 시 role='admin' 확인
3. admin@grapit.test / TestAdmin2026! 로 로그인 후 /admin 접속 가능
</verification>

<success_criteria>
- admin@grapit.test 계정이 seed 실행 후 DB에 존재
- 해당 계정으로 로그인 시 어드민 페이지(/admin) 정상 접근 가능
- 기존 시드 데이터(공연, 장소, 배너 등) 정상 동작
- seed 재실행 시 중복 에러 없이 정상 동작 (DELETE 후 재INSERT)
</success_criteria>

<output>
완료 후 `.planning/quick/260413-jw1-admin-grapit-test/260413-jw1-SUMMARY.md` 생성
</output>
