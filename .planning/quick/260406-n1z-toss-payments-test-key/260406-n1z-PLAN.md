---
phase: quick
plan: 260406-n1z
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - .env.example
autonomous: true
must_haves:
  truths:
    - "TOSS_SECRET_KEY is available to NestJS ConfigService at runtime"
    - "NEXT_PUBLIC_TOSS_CLIENT_KEY is available to Next.js at build/runtime"
    - ".env.example documents all Toss keys for other developers"
    - ".env contains working test keys for local development"
  artifacts:
    - path: ".env"
      provides: "Toss Payments test keys for local development"
      contains: "TOSS_SECRET_KEY"
    - path: ".env.example"
      provides: "Template showing required Toss env vars"
      contains: "TOSS_SECRET_KEY"
  key_links:
    - from: "apps/api/src/modules/payment/toss-payments.client.ts"
      to: ".env"
      via: "ConfigService.get('TOSS_SECRET_KEY')"
      pattern: "TOSS_SECRET_KEY"
    - from: "apps/web/components/booking/toss-payment-widget.tsx"
      to: ".env"
      via: "process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY"
      pattern: "NEXT_PUBLIC_TOSS_CLIENT_KEY"
---

<objective>
Toss Payments 문서용 테스트 키를 환경변수로 설정하여 프론트엔드/백엔드 결제 플로우가 로컬에서 동작하도록 한다.

Purpose: Phase 04에서 구현된 결제 위젯(TossPaymentWidget)과 결제 승인 클라이언트(TossPaymentsClient)가 현재 환경변수 미설정으로 동작하지 않음. 테스트 키 적용으로 로컬 개발 환경에서 결제 플로우를 검증할 수 있게 한다.
Output: .env (gitignored, 실제 키), .env.example (커밋, 템플릿)
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md

Existing env var consumers:
- apps/api/src/modules/payment/toss-payments.client.ts (line 34): `this.configService.get<string>('TOSS_SECRET_KEY', '')`
- apps/web/components/booking/toss-payment-widget.tsx (line 66): `process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY`

Convention: Single .env at monorepo root (/grapit/.env). NestJS ConfigModule reads from cwd. Next.js reads NEXT_PUBLIC_* automatically.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env with Toss test keys and .env.example template</name>
  <files>.env, .env.example</files>
  <action>
1. Create `.env` at monorepo root with ALL existing env vars from the project (DATABASE_URL, JWT_SECRET, JWT_REFRESH_SECRET, etc.) plus the two new Toss keys. Read the existing codebase for all env var references to ensure completeness. The Toss test keys to add:

```
# Toss Payments (test keys from docs)
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm
TOSS_SECRET_KEY=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
```

Key naming matches exactly what the code already reads:
- `NEXT_PUBLIC_TOSS_CLIENT_KEY` -- used by Next.js frontend (process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY in toss-payment-widget.tsx)
- `TOSS_SECRET_KEY` -- used by NestJS backend (configService.get('TOSS_SECRET_KEY') in toss-payments.client.ts)

2. Create `.env.example` at monorepo root as a committed template. For the Toss keys specifically, include the actual test_gck_docs/test_gsk_docs values since these are PUBLIC documentation keys (not real secrets). For all other env vars (DATABASE_URL, JWT_SECRET, etc.), use placeholder values like `your_value_here`.

3. .env is already in .gitignore (confirmed). .env.example is NOT in .gitignore (safe to commit).

IMPORTANT: Scan the codebase for ALL process.env.* and configService.get() references to include every env var the project uses. Group them by category (DB, Auth, Toss, Twilio, OAuth, etc.) with comments.
  </action>
  <verify>
    <automated>grep -q "NEXT_PUBLIC_TOSS_CLIENT_KEY=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm" /Users/sangwopark19/icons/grapit/.env && grep -q "TOSS_SECRET_KEY=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6" /Users/sangwopark19/icons/grapit/.env && grep -q "NEXT_PUBLIC_TOSS_CLIENT_KEY" /Users/sangwopark19/icons/grapit/.env.example && grep -q "TOSS_SECRET_KEY" /Users/sangwopark19/icons/grapit/.env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - .env exists at monorepo root with NEXT_PUBLIC_TOSS_CLIENT_KEY and TOSS_SECRET_KEY set to Toss docs test keys
    - .env.example exists with all env vars documented, Toss test keys included as-is (public docs keys)
    - All other existing env vars (DB, Auth, OAuth, Twilio) are present in both files
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify env vars are loaded by both apps</name>
  <files></files>
  <action>
Verify the env vars are correctly picked up without code changes:

1. For NestJS (apps/api): Check that ConfigModule.forRoot() in app.module.ts loads from cwd (monorepo root when running `pnpm --filter @grapit/api dev`). The TossPaymentsClient already reads `TOSS_SECRET_KEY` via ConfigService -- no code change needed.

2. For Next.js (apps/web): NEXT_PUBLIC_* vars are automatically inlined at build time. The TossPaymentWidget already reads `process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY` -- no code change needed.

3. Run a quick typecheck on both apps to confirm nothing is broken:
   - `pnpm --filter @grapit/api exec tsc --noEmit` (or the project's typecheck script)
   - `pnpm --filter @grapit/web exec tsc --noEmit` (or the project's typecheck script)

If typecheck scripts are not available, just verify the .env file is parseable and the key names match exactly what the code references.
  </action>
  <verify>
    <automated>grep -c "TOSS" /Users/sangwopark19/icons/grapit/.env | grep -q "2" && echo "PASS: Both TOSS keys present" || echo "FAIL"</automated>
  </verify>
  <done>
    - Confirmed TOSS_SECRET_KEY matches configService.get('TOSS_SECRET_KEY') in toss-payments.client.ts
    - Confirmed NEXT_PUBLIC_TOSS_CLIENT_KEY matches process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY in toss-payment-widget.tsx
    - No code changes required -- env vars align with existing code
  </done>
</task>

</tasks>

<verification>
- .env exists at monorepo root with both Toss test keys
- .env.example exists and is safe to commit (documents all env vars)
- Key names match exactly: NEXT_PUBLIC_TOSS_CLIENT_KEY and TOSS_SECRET_KEY
- No code changes needed -- existing code already reads these env var names
</verification>

<success_criteria>
- `grep TOSS .env` returns both NEXT_PUBLIC_TOSS_CLIENT_KEY and TOSS_SECRET_KEY with test values
- `.env.example` committed to git with Toss docs test keys included
- Frontend TossPaymentWidget can initialize SDK with the client key
- Backend TossPaymentsClient can make API calls with the secret key
</success_criteria>

<output>
After completion, update .planning/STATE.md quick tasks table.
</output>
