---
phase: quick-260420-fi4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/auth/phone-verification.tsx
autonomous: true
requirements:
  - UI-REVIEW-FIX-01
  - UI-REVIEW-FIX-02
  - UI-REVIEW-FIX-03
must_haves:
  truths:
    - "재발송 API 호출 중(isSending=true && codeSent=true) 전송/재발송 버튼이 default variant로 렌더링된다"
    - "인증 완료 텍스트가 디자인 시스템 토큰인 text-caption으로 통일된다 (에러·타이머와 동일 계층)"
    - "만료 타이머가 시간 만료 상태로 전환될 때 screen reader가 폴라이트 알림으로 상태를 통지받는다"
  artifacts:
    - path: "apps/web/components/auth/phone-verification.tsx"
      provides: "세 가지 UI-REVIEW 이슈 수정 (버튼 variant, typography 토큰, aria-live)"
      contains: "variant={isSending ? 'default' : codeSent ? 'outline' : 'default'}"
  key_links:
    - from: "phone-verification.tsx send button"
      to: "shadcn Button variant"
      via: "variant prop 3항 분기"
      pattern: "variant=\\{isSending \\? 'default'"
    - from: "phone-verification.tsx 인증 완료 span"
      to: "globals.css --text-caption 토큰"
      via: "text-caption utility class"
      pattern: "text-caption text-success"
    - from: "phone-verification.tsx 타이머 container"
      to: "screen reader announcement"
      via: "conditional aria-live on 시간 만료"
      pattern: "aria-live=\"polite\""
---

<objective>
phone-verification 컴포넌트에서 10-UI-REVIEW.md가 지적한 3건의 계약 위반을 한 커밋으로 정정한다.

Purpose: UI-SPEC 계약과 실구현 간 drift (버튼 variant, typography 토큰, aria-live)를 제거해 감사 점수(20/24)를 완성시키고, 스크린리더 사용자의 시간 만료 인지 접근성 계약 위반을 해결한다.

Output: 단일 파일 `apps/web/components/auth/phone-verification.tsx` 3곳 수정. 동작 로직은 그대로이며 외형/접근성만 계약과 동기화된다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-sms/10-UI-REVIEW.md
@.planning/phases/10-sms/10-UI-SPEC.md
@apps/web/components/auth/phone-verification.tsx

<interfaces>
<!-- 수정 대상 코드의 현재 상태 (L185-L207, L247-L255, L270-L275) -->

File: `apps/web/components/auth/phone-verification.tsx`

**Fix 1 — L185-L206: 전송/재발송 Button variant**
Current:
```tsx
<Button
  type="button"
  variant={codeSent ? 'outline' : 'default'}
  size="lg"
  onClick={handleSendCode}
  disabled={sendButtonDisabled}
  aria-label={sendButtonAriaLabel}
  className="shrink-0"
>
  {isSending ? (
    <>
      <Loader2 className="h-4 w-4 animate-spin" />
      발송 중...
    </>
  ) : codeSent && resendCooldown > 0 ? (
    `재발송 (${resendCooldown}s)`
  ) : codeSent ? (
    '재발송'
  ) : (
    '인증번호 발송'
  )}
</Button>
```

UI-SPEC §Button Labels 계약: sending 상태는 항상 `default` variant. 현재 `codeSent=true && isSending=true`(재발송 클릭 순간) 조합에서 outline이 찍힘.

**Fix 2 — L273: 인증 완료 span typography 토큰**
Current:
```tsx
<span className="text-sm text-success">인증 완료</span>
```
픽셀 값은 동일(14px)이지만 UI-SPEC §Typography는 Caption 역할에 `text-caption` 토큰을 요구. 에러(L211, L263)·타이머(L249, L253)는 이미 `text-caption` 사용 중.

**Fix 3 — L247-L255: 만료 타이머 aria-live 누락**
Current:
```tsx
<div className="flex items-center gap-2">
  {timeLeft > 0 ? (
    <span className="text-caption text-error">
      {formatTime(timeLeft)}
    </span>
  ) : (
    <span className="text-caption text-error">시간 만료</span>
  )}
</div>
```
UI-SPEC §Accessibility Contract:
- 타이머 span은 기본 `aria-live="off"` (1초 갱신 소음 방지)
- "시간 만료" 진입 시에만 `aria-live="polite"` + `role="status"`로 알림
- 접근성 계약 위반 상태 → 스크린리더 사용자는 만료를 전혀 인지 못 함
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phone-verification 3개 UI-REVIEW 이슈 한 번에 정정</name>
  <files>apps/web/components/auth/phone-verification.tsx</files>
  <action>
위 `<interfaces>` 블록에 명시된 3곳을 정확히 수정한다. 다른 라인은 건드리지 않는다.

**Fix 1 — send/resend Button variant (L187 근처)**
현재:
```tsx
variant={codeSent ? 'outline' : 'default'}
```
다음으로 교체:
```tsx
variant={isSending ? 'default' : codeSent ? 'outline' : 'default'}
```
근거: UI-REVIEW Top 3 Fixes #1. sending 상태는 항상 default variant (UI-SPEC §Button Labels). 재발송 클릭 중 isSending=true && codeSent=true 조합에서 outline으로 렌더되던 버그 제거. disabled 로직(`sendButtonDisabled`)은 이미 `isSending`을 포함하므로 클릭 불가 상태는 유지된다.

**Fix 2 — 인증 완료 span 토큰 통일 (L273)**
현재:
```tsx
<span className="text-sm text-success">인증 완료</span>
```
다음으로 교체:
```tsx
<span className="text-caption text-success">인증 완료</span>
```
근거: UI-REVIEW Top 3 Fixes #2. 픽셀 렌더(14px)는 동일하지만 에러/타이머와 토큰 일관성 확보. globals.css `--text-caption` 토큰 체계 준수.

**Fix 3 — 만료 타이머 container에 조건부 aria-live + role="status" (L247-L255)**
현재:
```tsx
<div className="flex items-center gap-2">
  {timeLeft > 0 ? (
    <span className="text-caption text-error">
      {formatTime(timeLeft)}
    </span>
  ) : (
    <span className="text-caption text-error">시간 만료</span>
  )}
</div>
```
다음으로 교체:
```tsx
<div
  className="flex items-center gap-2"
  aria-live={timeLeft === 0 ? 'polite' : 'off'}
  role={timeLeft === 0 ? 'status' : undefined}
>
  {timeLeft > 0 ? (
    <span className="text-caption text-error">
      {formatTime(timeLeft)}
    </span>
  ) : (
    <span className="text-caption text-error">시간 만료</span>
  )}
</div>
```
근거: UI-REVIEW Top 3 Fixes #3 + UI-SPEC §Accessibility Contract "1초마다 시끄러우니 aria-live='off' 로 설정하고 '시간 만료' 상태 진입 시에만 aria-live='polite' 로 전환". 컨테이너에 조건부 aria-live를 두어 카운트다운 소음을 피하고, timeLeft === 0 진입 시에만 polite 알림. role="status"는 실시간 알림 영역을 조용히 알리는 ARIA 랜드마크 역할.

**수정 범위 제약 (엄수):**
- 파일 단 하나: `apps/web/components/auth/phone-verification.tsx`
- 위 3곳 외 라인은 건드리지 않는다 (import, 타이머 로직, state, 에러 블록 등 손대지 않음)
- 새 hook이나 새 state 추가 금지
- 새 dependency 금지
  </action>
  <verify>
    <automated>pnpm --filter @grapit/web typecheck &amp;&amp; pnpm --filter @grapit/web lint apps/web/components/auth/phone-verification.tsx</automated>
  </verify>
  <done>
- `apps/web/components/auth/phone-verification.tsx`가 위 3개 diff만 포함 (git diff로 확인)
- `pnpm --filter @grapit/web typecheck` 통과
- `pnpm --filter @grapit/web lint` 통과 (phone-verification.tsx 관련 오류 0)
- 변경 후 L187 근처 grep `variant=\{isSending \? 'default'` 1회 매치
- 변경 후 L273 근처 grep `text-caption text-success">인증 완료` 1회 매치
- 변경 후 L247-L255 구간에 `aria-live={timeLeft === 0 ? 'polite' : 'off'}` 1회 매치
- `rg "text-sm text-success" apps/web/components/auth/phone-verification.tsx` 0 매치 (완전 제거 확인)
  </done>
</task>

</tasks>

<verification>
- diff가 위 3곳 외 변경을 포함하지 않는지 (`git diff --stat apps/web/components/auth/phone-verification.tsx` → 1 file changed, 작은 라인 수)
- `pnpm --filter @grapit/web typecheck` 통과
- `pnpm --filter @grapit/web lint` 통과 (수정 파일에 신규 경고 없음)
- 기능 회귀 없음 확인: 발송 클릭 → sending → 30s cooldown → 재발송 활성 → 검증 성공 → 인증 완료 플로우 각 단계에서 variant/토큰/aria 속성이 의도대로 나오는지 수동 스팟체크 가능 (필수 아님 — 자동 typecheck+lint 통과 시 머지 가능)
</verification>

<success_criteria>
- 3개 diff 모두 적용되어 파일에 존재
- typecheck, lint 통과
- 기존 테스트 실행 시 회귀 없음 (phone-verification 단위 테스트가 있다면 통과)
- UI-REVIEW.md Top 3 Fixes 항목이 모두 해소되었다고 말할 수 있는 상태
</success_criteria>

<output>
After completion, create `.planning/quick/260420-fi4-phase-10-ui-review-3-phone-verification-/260420-fi4-SUMMARY.md` with:
- 3 diffs applied (variant, typography, aria-live)
- typecheck/lint PASS
- commit hash
- UI-REVIEW.md 반영 상태 note
</output>
