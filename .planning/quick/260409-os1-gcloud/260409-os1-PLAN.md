---
phase: quick-260409-os1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/auth/auth.controller.ts
  - apps/api/src/modules/auth/auth.controller.spec.ts
autonomous: true
must_haves:
  truths:
    - "setRefreshTokenCookie sets sameSite to 'none' for cross-site Cloud Run domains"
    - "setRefreshTokenCookie sets secure to true unconditionally (required by SameSite=None)"
    - "clearCookie in logout includes sameSite:'none' and secure:true to properly clear the cookie"
    - "All existing auth controller tests pass with updated expectations"
  artifacts:
    - path: "apps/api/src/modules/auth/auth.controller.ts"
      provides: "Fixed cookie settings for cross-site production deployment"
      contains: "sameSite: 'none'"
    - path: "apps/api/src/modules/auth/auth.controller.spec.ts"
      provides: "Updated tests verifying SameSite=None cookie behavior"
      contains: "sameSite.*none"
  key_links:
    - from: "apps/api/src/modules/auth/auth.controller.ts"
      to: "browser cookie jar"
      via: "res.cookie with SameSite=None; Secure"
      pattern: "sameSite:\\s*'none'"
---

<objective>
Fix production social login cookie bug: cross-site POST requests from grapit-web to grapit-api
on Cloud Run fail because SameSite=Lax cookies are not sent. Change to SameSite=None + Secure=true.

Purpose: Unblock social login on production (AUTH-01 blocker in STATE.md)
Output: Fixed auth.controller.ts + updated tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/src/modules/auth/auth.controller.ts
@apps/api/src/modules/auth/auth.controller.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests to expect SameSite=None and fix cookie settings</name>
  <files>apps/api/src/modules/auth/auth.controller.spec.ts, apps/api/src/modules/auth/auth.controller.ts</files>
  <behavior>
    - Test: setRefreshTokenCookie sets sameSite to 'none' (not 'lax') in production
    - Test: setRefreshTokenCookie sets sameSite to 'none' in development too (unconditional)
    - Test: setRefreshTokenCookie sets secure to true unconditionally (SameSite=None requires Secure)
    - Test: logout clearCookie includes sameSite:'none', secure:true, path:'/' options
  </behavior>
  <action>
RED phase - Update existing tests in auth.controller.spec.ts:

1. In "setRefreshTokenCookie via socialKakaoCallback" describe block:
   - Line 76: Change `expect(cookieOptions['sameSite']).toBe('lax')` to `expect(cookieOptions['sameSite']).toBe('none')`
   - Line 96: Change `expect(cookieOptions['sameSite']).toBe('lax')` to `expect(cookieOptions['sameSite']).toBe('none')`
   - Add a new test: "secure가 환경변수와 무관하게 항상 true로 설정된다" that checks `cookieOptions['secure']` is `true` regardless of NODE_ENV

2. Add a new describe block "logout clearCookie options" with:
   - Add `clearCookie` to mockResponse (vi.fn())
   - Add `revokeRefreshToken` to mockAuthService (vi.fn())
   - Test: "logout 시 clearCookie에 sameSite:'none', secure:true가 포함된다"
     - Set mockRequest.cookies = { grapit_refresh_token: 'test-token' }
     - Call controller.logout(req, res)
     - Assert clearCookie called with AUTH_COOKIE_NAME and options including `{ sameSite: 'none', secure: true, path: '/' }`

Run tests — they MUST fail (red) because source still has sameSite: 'lax'.

GREEN phase - Fix apps/api/src/modules/auth/auth.controller.ts:

1. `setRefreshTokenCookie` method (line 249-258): Replace entire method body with:
   - Remove `const isProduction = ...` line (no longer needed)
   - Set `secure: true` (unconditional, not tied to isProduction)
   - Set `sameSite: 'none'` (instead of 'lax')
   - Keep httpOnly: true, maxAge, path unchanged

2. `logout` method (line 112): Change `res.clearCookie(AUTH_COOKIE_NAME, { path: '/' })` to:
   `res.clearCookie(AUTH_COOKIE_NAME, { path: '/', sameSite: 'none', secure: true })`
   This is required because browsers match cookie attributes when clearing — mismatched sameSite/secure means the cookie won't be cleared.

Run tests — they MUST pass (green).
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec vitest run src/modules/auth/auth.controller.spec.ts</automated>
  </verify>
  <done>
    - setRefreshTokenCookie uses sameSite:'none' and secure:true unconditionally
    - clearCookie in logout uses matching sameSite:'none' and secure:true
    - All auth.controller.spec.ts tests pass (including updated + new tests)
    - No isProduction variable in setRefreshTokenCookie (eliminated dead code)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full API test suite and typecheck</name>
  <files></files>
  <action>
Run the full API test suite to confirm no regressions from the cookie change.
Then run typecheck to ensure type safety.

If any tests fail that are unrelated to this change, note them but do not fix (out of scope).
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec vitest run && pnpm --filter @grapit/api typecheck</automated>
  </verify>
  <done>
    - All API tests pass (no regressions)
    - TypeScript compilation succeeds with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web -> api (cross-site) | Browser sends httpOnly cookie from grapit-web to grapit-api on different Cloud Run subdomains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Spoofing | SameSite=None cookie | mitigate | Secure=true enforces HTTPS-only; httpOnly prevents JS access; cookie is still bound to API domain by browser |
| T-quick-02 | Tampering | Cross-site cookie attachment | accept | SameSite=None is intentional for legitimate cross-site flow between two Cloud Run services under same project; CSRF risk is mitigated by the fact that refresh endpoint only returns tokens (no state mutation beyond token rotation) |
</threat_model>

<verification>
1. `pnpm --filter @grapit/api exec vitest run` passes all tests
2. `pnpm --filter @grapit/api typecheck` passes
3. grep confirms: `sameSite: 'none'` in auth.controller.ts (no remaining 'lax')
4. grep confirms: `secure: true` without conditional in setRefreshTokenCookie
</verification>

<success_criteria>
- SameSite cookie attribute changed from 'lax' to 'none' in setRefreshTokenCookie
- secure attribute is unconditionally true
- clearCookie in logout matches with sameSite:'none' and secure:true
- All tests pass with updated expectations
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260409-os1-gcloud/260409-os1-SUMMARY.md`
</output>
