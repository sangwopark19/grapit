---
phase: quick
plan: 260331-mq2
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/proxy.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Next.js 16 deprecation warning for middleware file convention no longer appears in dev server"
    - "Admin routes (/admin/*) still redirect unauthenticated users to /auth"
  artifacts:
    - path: "apps/web/proxy.ts"
      provides: "Next.js 16 proxy file (replaces deprecated middleware.ts)"
      contains: "export function proxy"
  key_links:
    - from: "apps/web/proxy.ts"
      to: "/admin/:path*"
      via: "config.matcher"
      pattern: "matcher.*admin"
---

<objective>
Next.js 16에서 middleware.ts가 proxy.ts로 rename된 convention 변경에 맞춰 파일명과 export 함수명을 업데이트한다.

Purpose: dev 서버 deprecation warning 제거 및 Next.js 16 convention 준수
Output: apps/web/proxy.ts (middleware.ts 삭제)
</objective>

<context>
@apps/web/middleware.ts (현재 파일 -- 삭제 대상)
</context>

<tasks>

<task type="auto">
  <name>Task 1: middleware.ts를 proxy.ts로 rename하고 export 함수명 변경</name>
  <files>apps/web/proxy.ts</files>
  <action>
    1. `apps/web/middleware.ts`를 `apps/web/proxy.ts`로 git mv 한다
    2. export 함수명을 `middleware`에서 `proxy`로 변경한다
    3. 나머지 로직(refreshToken 쿠키 체크, /auth redirect, config matcher)은 그대로 유지한다
    4. `apps/web/middleware.ts`가 완전히 삭제되었는지 확인한다

    변경 전:
    ```typescript
    export function middleware(request: NextRequest) {
    ```

    변경 후:
    ```typescript
    export function proxy(request: NextRequest) {
    ```

    import문(NextResponse, NextRequest)과 config export는 변경 없음.
  </action>
  <verify>
    <automated>ls apps/web/proxy.ts && ! ls apps/web/middleware.ts 2>/dev/null && grep -q "export function proxy" apps/web/proxy.ts && grep -q "matcher.*admin" apps/web/proxy.ts</automated>
  </verify>
  <done>
    - apps/web/proxy.ts 존재하며 `export function proxy` 포함
    - apps/web/middleware.ts 삭제됨
    - config.matcher가 ['/admin/:path*']로 유지됨
    - dev 서버 실행 시 middleware deprecation warning 없음
  </done>
</task>

</tasks>

<verification>
1. `apps/web/proxy.ts` 파일이 존재하고 올바른 export function proxy를 포함
2. `apps/web/middleware.ts`가 삭제됨
3. admin route protection 로직이 동일하게 유지됨 (refreshToken 쿠키 체크 -> /auth redirect)
</verification>

<success_criteria>
- Next.js dev 서버에서 "middleware file convention is deprecated" 경고가 사라짐
- /admin 경로 접근 시 인증 체크 동작이 기존과 동일
</success_criteria>

<output>
After completion, create `.planning/quick/260331-mq2-rename-middleware-ts-to-proxy-ts-for-nex/260331-mq2-SUMMARY.md`
</output>
