---
phase: 07-valkey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/booking/providers/redis.provider.ts
  - apps/api/src/config/redis.config.ts
  - apps/api/src/modules/booking/booking.module.ts
  - apps/api/src/modules/booking/booking.service.ts
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/package.json
autonomous: true
requirements:
  - VALK-01
  - VALK-03
  - VALK-04

must_haves:
  truths:
    - "@upstash/redis 패키지가 apps/api/package.json에서 완전히 제거되어 있다"
    - "UPSTASH_REDIS Symbol이 REDIS_CLIENT Symbol로 교체되어 단일 ioredis 클라이언트만 존재한다"
    - "BookingService의 모든 eval() 호출이 ioredis 플랫 시그니처(script, numKeys, ...keys, ...args)를 사용한다"
    - "InMemoryRedis mock의 eval()이 ioredis 시그니처(script, numKeys, ...keysAndArgs)로 동작한다"
    - "Socket.IO Redis adapter가 동일 ioredis 클라이언트(REDIS_CLIENT)를 사용한다"
    - "기존 booking.service.spec.ts 전체 테스트가 통과한다"
  artifacts:
    - path: "apps/api/src/modules/booking/providers/redis.provider.ts"
      provides: "REDIS_CLIENT 단일 ioredis provider + InMemoryRedis mock (ioredis eval 시그니처)"
      exports: ["REDIS_CLIENT", "redisProvider"]
    - path: "apps/api/src/config/redis.config.ts"
      provides: "redis.url 단일 키만 제공 (upstashUrl/upstashToken 제거)"
      contains: "url: process.env"
    - path: "apps/api/src/modules/booking/booking.service.ts"
      provides: "eval(script, numKeys, ...keys, ...args) 시그니처 사용"
      contains: "REDIS_CLIENT"
    - path: "apps/api/src/modules/booking/booking.module.ts"
      provides: "redisProvider 단일 등록"
      contains: "redisProvider"
  key_links:
    - from: "apps/api/src/modules/booking/booking.service.ts"
      to: "apps/api/src/modules/booking/providers/redis.provider.ts"
      via: "@Inject(REDIS_CLIENT)"
      pattern: "Inject\\(REDIS_CLIENT\\)"
    - from: "apps/api/src/modules/booking/booking.module.ts"
      to: "apps/api/src/modules/booking/providers/redis.provider.ts"
      via: "providers array"
      pattern: "redisProvider"
---

<objective>
@upstash/redis를 제거하고 ioredis 단일 클라이언트(REDIS_CLIENT)로 통합한다. Lua 스크립트 3개의 eval() 시그니처를 Upstash 패턴에서 ioredis 플랫 패턴으로 변환하고, InMemoryRedis mock과 테스트를 동시 업데이트한다. Socket.IO adapter는 코드 변경 없이 IOREDIS_CLIENT -> REDIS_CLIENT 심볼 변경만 반영한다.

Purpose: Upstash HTTP + ioredis TCP 이원화 구조를 ioredis TCP 단일 클라이언트로 단순화 (per D-01, D-02)
Output: @upstash/redis 제거, REDIS_CLIENT 통합 provider, ioredis eval() 시그니처 변환 완료, 전체 테스트 통과
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-valkey/07-CONTEXT.md
@.planning/phases/07-valkey/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/api/src/modules/booking/providers/redis.provider.ts (CURRENT - to be replaced):
```typescript
export const UPSTASH_REDIS = Symbol('UPSTASH_REDIS');
export const IOREDIS_CLIENT = Symbol('IOREDIS_CLIENT');

// InMemoryRedis.eval current signature (Upstash pattern):
async eval(_script: string, keys: string[], args: string[]): Promise<unknown>

// upstashRedisProvider: provides UPSTASH_REDIS
// ioredisClientProvider: provides IOREDIS_CLIENT
```

From apps/api/src/modules/booking/booking.service.ts (CURRENT - eval calls to convert):
```typescript
import { UPSTASH_REDIS } from './providers/redis.provider.js';
import type { Redis } from '@upstash/redis';

// lockSeat eval (3 keys, 5 args):
const result = await this.redis.eval<[string, string, string, string, string], [number, string, string?]>(
  LOCK_SEAT_LUA,
  [userSeatsKey, lockKey, lockedSeatsKey],
  [userId, String(LOCK_TTL), String(MAX_SEATS), seatId, keyPrefix],
);

// unlockSeat eval (3 keys, 2 args):
const result = await this.redis.eval<[string, string], number>(
  UNLOCK_SEAT_LUA,
  [lockKey, userSeatsKey, lockedSeatsKey],
  [userId, seatId],
);

// getSeatStatus eval (1 key, 1 arg):
const lockedSeats = await this.redis.eval<[string], string[]>(
  GET_VALID_LOCKED_SEATS_LUA,
  [lockedSeatsKey],
  [keyPrefix],
);
```

From apps/api/src/modules/booking/booking.module.ts (CURRENT):
```typescript
import { upstashRedisProvider, ioredisClientProvider } from './providers/redis.provider.js';
providers: [BookingService, BookingGateway, upstashRedisProvider, ioredisClientProvider],
```

From apps/api/src/config/redis.config.ts (CURRENT):
```typescript
export const redisConfig = registerAs('redis', () => ({
  upstashUrl: process.env['UPSTASH_REDIS_REST_URL'] ?? '',
  upstashToken: process.env['UPSTASH_REDIS_REST_TOKEN'] ?? '',
  ioredisUrl: process.env['REDIS_URL'] ?? 'redis://localhost:6379',
}));
```

From apps/api/src/modules/booking/providers/redis-io.adapter.ts (NO CHANGE NEEDED):
```typescript
// Uses IORedis type directly - no symbol import needed
export function createSocketIoRedisAdapter(ioredisClient: IORedis): ReturnType<typeof createAdapter>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redis provider 통합 + config 정리 + InMemoryRedis eval 시그니처 변환</name>
  <files>
    apps/api/src/modules/booking/providers/redis.provider.ts
    apps/api/src/config/redis.config.ts
    apps/api/src/modules/booking/booking.module.ts
    apps/api/package.json
  </files>
  <read_first>
    apps/api/src/modules/booking/providers/redis.provider.ts
    apps/api/src/config/redis.config.ts
    apps/api/src/modules/booking/booking.module.ts
    apps/api/src/modules/booking/providers/redis-io.adapter.ts
    apps/api/package.json
  </read_first>
  <action>
**Per D-01, D-05, D-06:**

1. **redis.provider.ts 전면 재작성:**
   - `UPSTASH_REDIS` Symbol 삭제, `IOREDIS_CLIENT` Symbol 삭제
   - 새 Symbol: `export const REDIS_CLIENT = Symbol('REDIS_CLIENT');`
   - `import { Redis } from '@upstash/redis';` 삭제
   - `import IORedis from 'ioredis';` 유지

   InMemoryRedis class의 `eval()` 시그니처를 ioredis 패턴으로 변환:
   ```typescript
   async eval(
     _script: string,
     numKeys: number,
     ...keysAndArgs: (string | number)[]
   ): Promise<unknown> {
     const keys = keysAndArgs.slice(0, numKeys).map(String);
     const args = keysAndArgs.slice(numKeys).map(String);
     // 기존 dispatch 로직 유지 (keys.length + args.length 매칭)
     if (keys.length === 3 && args.length === 5) return this.evalLockSeat(keys, args);
     if (keys.length === 3 && args.length === 2) return this.evalUnlockSeat(keys, args);
     if (keys.length === 1 && args.length === 1) return this.evalGetValidLockedSeats(keys, args);
     throw new Error('InMemoryRedis: unknown Lua script pattern');
   }
   ```

   InMemoryRedis의 `set()` 메서드 시그니처는 ioredis 패턴으로도 동작하도록 유지 (opts 기반 현재 구현은 내부 전용이므로 그대로).

   기존 두 provider(`upstashRedisProvider`, `ioredisClientProvider`)를 단일 provider로 교체:
   ```typescript
   export const redisProvider: Provider = {
     provide: REDIS_CLIENT,
     inject: [ConfigService],
     useFactory: (config: ConfigService): IORedis | InMemoryRedis => {
       const url = config.get<string>('redis.url', '');

       if (!url) {
         console.warn('[redis] No REDIS_URL -- using in-memory mock.');
         return new InMemoryRedis() as unknown as IORedis;
       }

       const client = new IORedis(url, {
         maxRetriesPerRequest: 3,
         lazyConnect: true,
         retryStrategy: (times: number) => {
           if (times > 5) return null;
           return Math.min(times * 500, 5000);
         },
       });

       client.on('error', (err: Error) => {
         console.error('[redis] Error:', err.message);
       });

       client.connect().catch(() => {});
       return client;
     },
   };
   ```

2. **redis.config.ts 정리:**
   ```typescript
   import { registerAs } from '@nestjs/config';

   export const redisConfig = registerAs('redis', () => ({
     url: process.env['REDIS_URL'] ?? '',
   }));
   ```
   `upstashUrl`, `upstashToken` 키 완전 삭제. `ioredisUrl` -> `url`로 단순화. 빈 문자열일 때 InMemoryRedis fallback.

3. **booking.module.ts 업데이트:**
   ```typescript
   import { redisProvider } from './providers/redis.provider.js';
   // upstashRedisProvider, ioredisClientProvider 임포트 제거

   providers: [BookingService, BookingGateway, redisProvider],
   exports: [BookingService, BookingGateway, redisProvider],
   ```
   `redisProvider`를 exports에 추가하여 다른 모듈(PerformanceModule)에서 REDIS_CLIENT를 사용할 수 있게 한다.

4. **@upstash/redis 패키지 제거:**
   ```bash
   pnpm --filter @grapit/api remove @upstash/redis
   ```
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - redis.provider.ts에 `REDIS_CLIENT` Symbol이 export되어 있다
    - redis.provider.ts에 `UPSTASH_REDIS`, `IOREDIS_CLIENT` Symbol이 없다
    - redis.provider.ts에 `@upstash/redis` import가 없다
    - redis.provider.ts에 `redisProvider` (단일)가 export된다
    - redis.provider.ts InMemoryRedis의 eval 시그니처가 `(script, numKeys, ...keysAndArgs)` 형태이다
    - redis.config.ts에 `upstashUrl`, `upstashToken` 키가 없다
    - redis.config.ts에 `url: process.env['REDIS_URL']`만 있다
    - booking.module.ts에 `redisProvider` 하나만 providers에 등록되어 있다
    - booking.module.ts에 `upstashRedisProvider`, `ioredisClientProvider`가 없다
    - apps/api/package.json에 `@upstash/redis`가 없다
    - TypeScript 컴파일 에러 없음 (tsc --noEmit 통과)
  </acceptance_criteria>
  <done>REDIS_CLIENT 단일 provider 완성, @upstash/redis 제거, InMemoryRedis eval 시그니처 변환, TypeScript 컴파일 통과</done>
</task>

<task type="auto">
  <name>Task 2: BookingService eval() 시그니처 변환 + 테스트 업데이트</name>
  <files>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
    apps/api/src/modules/booking/providers/redis.provider.ts
  </read_first>
  <action>
**Per D-01, VALK-03:**

1. **booking.service.ts import 변경:**
   - `import type { Redis } from '@upstash/redis';` 삭제
   - `import type IORedis from 'ioredis';` 추가
   - `import { UPSTASH_REDIS } from './providers/redis.provider.js';` -> `import { REDIS_CLIENT } from './providers/redis.provider.js';`
   - constructor: `@Inject(UPSTASH_REDIS) private readonly redis: Redis` -> `@Inject(REDIS_CLIENT) private readonly redis: IORedis`

2. **lockSeat() eval 변환 (3 keys, 5 args):**
   ```typescript
   // 변경 전:
   const result = await this.redis.eval<[string, string, string, string, string], [number, string, string?]>(
     LOCK_SEAT_LUA,
     [userSeatsKey, lockKey, lockedSeatsKey],
     [userId, String(LOCK_TTL), String(MAX_SEATS), seatId, keyPrefix],
   );

   // 변경 후:
   const result = await this.redis.eval(
     LOCK_SEAT_LUA,
     3,
     userSeatsKey, lockKey, lockedSeatsKey,
     userId, String(LOCK_TTL), String(MAX_SEATS), seatId, keyPrefix,
   ) as [number, string, string?];
   ```

3. **unlockSeat() eval 변환 (3 keys, 2 args):**
   ```typescript
   // 변경 전:
   const result = await this.redis.eval<[string, string], number>(
     UNLOCK_SEAT_LUA,
     [lockKey, userSeatsKey, lockedSeatsKey],
     [userId, seatId],
   );

   // 변경 후:
   const result = await this.redis.eval(
     UNLOCK_SEAT_LUA,
     3,
     lockKey, userSeatsKey, lockedSeatsKey,
     userId, seatId,
   ) as number;
   ```

4. **getSeatStatus() eval 변환 (1 key, 1 arg):**
   ```typescript
   // 변경 전:
   const lockedSeats = await this.redis.eval<[string], string[]>(
     GET_VALID_LOCKED_SEATS_LUA,
     [lockedSeatsKey],
     [keyPrefix],
   );

   // 변경 후:
   const lockedSeats = await this.redis.eval(
     GET_VALID_LOCKED_SEATS_LUA,
     1,
     lockedSeatsKey,
     keyPrefix,
   ) as string[];
   ```

5. **unlockAllSeats()와 getMyLocks():** 이 메서드들은 `eval()`을 사용하지 않고 `smembers`, `get`, `del`, `srem`, `ttl`만 사용한다. ioredis도 동일 메서드명을 지원하므로 코드 변경 불필요. 단, `this.redis.smembers(...)` 반환 타입이 ioredis에서는 `string[]`이므로 `as string[]` 캐스트를 제거할 수 있다.

6. **booking.service.spec.ts 테스트 업데이트:**
   - mock eval 호출 검증에서 `[script, keys, args]` destructuring을 ioredis 시그니처로 변경:
   ```typescript
   // lockSeat test (line 81):
   // 변경 전: const [script, keys, args] = mockRedis.eval.mock.calls[0] as [string, string[], unknown[]];
   // 변경 후:
   const callArgs = mockRedis.eval.mock.calls[0] as unknown[];
   const script = callArgs[0] as string;
   const numKeys = callArgs[1] as number;
   const keysAndArgs = callArgs.slice(2) as string[];
   expect(script).toContain('SMEMBERS');
   expect(script).toContain('EXISTS');
   expect(numKeys).toBe(3);
   expect(keysAndArgs[0]).toBe(`user-seats:${showtimeId}:${userId}`);
   expect(keysAndArgs[1]).toBe(`seat:${showtimeId}:${seatId}`);
   expect(keysAndArgs[2]).toBe(`locked-seats:${showtimeId}`);
   ```

   - unlockSeat test (line 191):
   ```typescript
   // 변경 전: const [script, keys, args] = mockRedis.eval.mock.calls[0] as [string, string[], string[]];
   // 변경 후:
   const callArgs = mockRedis.eval.mock.calls[0] as unknown[];
   const script = callArgs[0] as string;
   const numKeys = callArgs[1] as number;
   const flatKeys = callArgs.slice(2, 2 + numKeys) as string[];
   const flatArgs = callArgs.slice(2 + numKeys) as string[];
   expect(script).toContain('GET');
   expect(script).toContain('DEL');
   expect(script).toContain('SREM');
   expect(numKeys).toBe(3);
   expect(flatKeys).toEqual([
     `seat:${showtimeId}:${seatId}`,
     `user-seats:${showtimeId}:${userId}`,
     `locked-seats:${showtimeId}`,
   ]);
   expect(flatArgs).toEqual([userId, seatId]);
   ```

   - getSeatStatus test (line 321):
   ```typescript
   // 변경 전: const [script, keys, args] = mockRedis.eval.mock.calls[0] as [string, string[], string[]];
   // 변경 후:
   const callArgs = mockRedis.eval.mock.calls[0] as unknown[];
   const script = callArgs[0] as string;
   const numKeys = callArgs[1] as number;
   const flatKeys = callArgs.slice(2, 2 + numKeys) as string[];
   const flatArgs = callArgs.slice(2 + numKeys) as string[];
   expect(script).toContain('SMEMBERS');
   expect(script).toContain('EXISTS');
   expect(numKeys).toBe(1);
   expect(flatKeys).toEqual([`locked-seats:${showtimeId}`]);
   expect(flatArgs).toEqual([`seat:${showtimeId}:`]);
   ```

   mock의 `ttl` 메서드도 추가 (getMyLocks에서 사용):
   ```typescript
   function createMockRedis() {
     return {
       set: vi.fn(), get: vi.fn(), del: vi.fn(),
       smembers: vi.fn(), sadd: vi.fn(), srem: vi.fn(),
       scard: vi.fn(), expire: vi.fn(), eval: vi.fn(),
       ttl: vi.fn(),
     };
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec vitest run src/modules/booking/__tests__/booking.service.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - booking.service.ts에 `@upstash/redis` import가 없다
    - booking.service.ts에 `REDIS_CLIENT`가 import되어 있다
    - booking.service.ts constructor에 `@Inject(REDIS_CLIENT)` 사용
    - booking.service.ts의 redis 타입이 `IORedis`이다
    - lockSeat의 eval 호출이 `eval(LOCK_SEAT_LUA, 3, userSeatsKey, lockKey, lockedSeatsKey, ...)` 형태
    - unlockSeat의 eval 호출이 `eval(UNLOCK_SEAT_LUA, 3, lockKey, userSeatsKey, lockedSeatsKey, ...)` 형태
    - getSeatStatus의 eval 호출이 `eval(GET_VALID_LOCKED_SEATS_LUA, 1, lockedSeatsKey, keyPrefix)` 형태
    - booking.service.spec.ts의 모든 테스트 통과 (vitest exit code 0)
    - booking.service.spec.ts에서 eval mock 검증이 numKeys 인자를 포함
  </acceptance_criteria>
  <done>BookingService의 모든 eval() 호출이 ioredis 시그니처로 변환되고, 테스트 전체 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BookingService -> Redis | 내부 서비스에서 Redis 명령어 실행. Lua 스크립트는 하드코딩, 사용자 입력은 ARGV로만 전달 |
| Client -> BookingController | 클라이언트가 좌석 잠금/해제 요청. JwtAuthGuard로 인증 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | redis.provider.ts eval() | mitigate | Lua 스크립트 하드코딩 유지, 사용자 입력은 키/인자로만 전달 (기존 패턴 유지) |
| T-07-02 | Denial of Service | InMemoryRedis fallback | accept | REDIS_URL 미설정 시 InMemoryRedis 사용은 로컬 개발 전용. 프로덕션에서는 REDIS_URL 필수 (deploy.yml secrets로 보장) |
| T-07-03 | Information Disclosure | redis.config.ts | mitigate | REDIS_URL은 환경변수로만 주입, .env는 .gitignore에 포함. 프로덕션은 GCP Secret Manager |
</threat_model>

<verification>
1. `pnpm --filter @grapit/api exec vitest run src/modules/booking/__tests__/booking.service.spec.ts` - 전체 테스트 통과
2. `pnpm --filter @grapit/api exec tsc --noEmit` - TypeScript 컴파일 에러 없음
3. `grep -r '@upstash/redis' apps/api/src/` - 결과 없음 (완전 제거 확인)
4. `grep -r 'UPSTASH_REDIS' apps/api/src/` - 결과 없음 (심볼 완전 교체 확인)
</verification>

<success_criteria>
- @upstash/redis 패키지 완전 제거 (package.json + 모든 import)
- REDIS_CLIENT 단일 Symbol로 provider 통합
- eval() 시그니처 3곳 모두 ioredis 플랫 패턴으로 변환
- InMemoryRedis mock eval() 시그니처 동기화
- 기존 테스트 전체 통과
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/07-valkey/07-01-SUMMARY.md`
</output>
