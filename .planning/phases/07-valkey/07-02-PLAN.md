---
phase: 07-valkey
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/api/src/modules/performance/cache.service.ts
  - apps/api/src/modules/performance/performance.module.ts
  - apps/api/src/modules/performance/performance.service.ts
  - apps/api/src/modules/performance/performance.controller.ts
  - apps/api/src/modules/admin/admin.service.ts
  - apps/api/src/modules/admin/admin-performance.controller.ts
  - apps/api/src/modules/admin/admin-banner.controller.ts
  - apps/api/src/modules/admin/admin.module.ts
  - apps/api/src/modules/performance/__tests__/cache.service.spec.ts
autonomous: true
requirements:
  - VALK-06

must_haves:
  truths:
    - "공연 목록 API가 첫 요청 시 DB 조회 후 캐시에 저장하고, 두 번째 요청부터 캐시에서 서빙한다"
    - "공연 상세 API가 캐시에서 서빙된다 (단, viewCount 증가는 항상 DB에 실행)"
    - "홈 배너/인기/신규 API가 캐시에서 서빙된다"
    - "admin에서 공연 생성/수정/삭제 시 관련 캐시가 즉시 무효화된다"
    - "admin에서 배너 생성/수정/삭제/재정렬 시 배너 캐시가 즉시 무효화된다"
    - "캐시 TTL이 5분(300초)이다"
    - "CacheService 유닛 테스트가 통과한다"
  artifacts:
    - path: "apps/api/src/modules/performance/cache.service.ts"
      provides: "CacheService: get, set, invalidate, invalidatePattern"
      exports: ["CacheService"]
    - path: "apps/api/src/modules/performance/__tests__/cache.service.spec.ts"
      provides: "CacheService 유닛 테스트 (hit/miss/invalidation)"
  key_links:
    - from: "apps/api/src/modules/performance/performance.service.ts"
      to: "apps/api/src/modules/performance/cache.service.ts"
      via: "DI injection"
      pattern: "this\\.cacheService\\."
    - from: "apps/api/src/modules/admin/admin.service.ts"
      to: "apps/api/src/modules/performance/cache.service.ts"
      via: "DI injection"
      pattern: "this\\.cacheService\\.invalidate"
---

<objective>
공연 카탈로그 캐시 레이어를 구현한다. CacheService를 생성하여 공연 목록/상세/홈 API에 read-through 캐시를 적용하고, admin CRUD 시 캐시를 즉시 무효화한다.

Purpose: DB 직접 조회 대비 응답 시간 단축, 읽기 비율 높은 엔드포인트 최적화 (per D-07, D-08)
Output: CacheService + PerformanceService/AdminService 캐시 통합 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-valkey/07-CONTEXT.md
@.planning/phases/07-valkey/07-RESEARCH.md
@.planning/phases/07-valkey/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output - REDIS_CLIENT provider -->

From apps/api/src/modules/booking/providers/redis.provider.ts (after Plan 01):
```typescript
export const REDIS_CLIENT = Symbol('REDIS_CLIENT');
export const redisProvider: Provider;
// redisProvider is exported from BookingModule
```

From apps/api/src/modules/booking/booking.module.ts (after Plan 01):
```typescript
exports: [BookingService, BookingGateway, redisProvider],
```

From apps/api/src/modules/performance/performance.service.ts (current):
```typescript
@Injectable()
export class PerformanceService {
  constructor(@Inject(DRIZZLE) private readonly db: DrizzleDB) {}
  async findByGenre(genre: string, query: PerformanceQuery): Promise<PerformanceListResponse>
  async findById(id: string): Promise<PerformanceWithDetails | null>
  async getHomeBanners(): Promise<Banner[]>
  async getHotPerformances(): Promise<PerformanceCardData[]>
  async getNewPerformances(): Promise<PerformanceCardData[]>
}
```

From apps/api/src/modules/admin/admin.service.ts (current):
```typescript
@Injectable()
export class AdminService {
  constructor(@Inject(DRIZZLE) private readonly db: DrizzleDB) {}
  async createPerformance(input: CreatePerformanceInput): Promise<PerformanceWithDetails>
  async updatePerformance(id: string, input: UpdatePerformanceInput): Promise<PerformanceWithDetails>
  async deletePerformance(id: string): Promise<void>
  async createBanner(input: CreateBannerInput): Promise<Banner>
  async updateBanner(id: string, input: Partial<CreateBannerInput>): Promise<Banner>
  async deleteBanner(id: string): Promise<void>
  async reorderBanners(orderedIds: string[]): Promise<void>
}
```

From apps/api/src/modules/performance/performance.module.ts (current):
```typescript
@Module({
  controllers: [PerformanceController],
  providers: [PerformanceService],
  exports: [PerformanceService],
})
export class PerformanceModule {}
```

From apps/api/src/modules/admin/admin.module.ts:
// AdminModule imports PerformanceModule and uses PerformanceService
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CacheService 생성 + 유닛 테스트 + PerformanceModule 연결</name>
  <files>
    apps/api/src/modules/performance/cache.service.ts
    apps/api/src/modules/performance/__tests__/cache.service.spec.ts
    apps/api/src/modules/performance/performance.module.ts
  </files>
  <read_first>
    apps/api/src/modules/booking/providers/redis.provider.ts
    apps/api/src/modules/performance/performance.module.ts
    apps/api/src/modules/booking/booking.module.ts
    apps/api/src/modules/performance/performance.service.ts
  </read_first>
  <behavior>
    - Test 1: get() - 키가 없을 때 null 반환
    - Test 2: set() + get() - 데이터 저장 후 조회 시 파싱된 객체 반환
    - Test 3: set() - redis.set이 'EX', 300 (TTL 5분)으로 호출됨
    - Test 4: invalidate() - 지정 키들에 대해 redis.del 호출
    - Test 5: invalidatePattern() - redis.keys(pattern) + redis.del(...keys) 호출
    - Test 6: invalidatePattern() - keys 결과가 빈 배열이면 del 호출하지 않음
    - Test 7: get() - redis 에러 시 null 반환 (graceful degradation)
    - Test 8: set() - redis 에러 시 예외 전파하지 않음 (graceful degradation)
  </behavior>
  <action>
**Per D-07, D-08:**

1. **cache.service.spec.ts 먼저 작성 (RED):**
   ```typescript
   import { describe, it, expect, beforeEach, vi } from 'vitest';
   import { CacheService } from '../cache.service.js';

   function createMockRedis() {
     return {
       get: vi.fn(),
       set: vi.fn(),
       del: vi.fn(),
       keys: vi.fn(),
     };
   }

   describe('CacheService', () => {
     let service: CacheService;
     let mockRedis: ReturnType<typeof createMockRedis>;

     beforeEach(() => {
       mockRedis = createMockRedis();
       service = new CacheService(mockRedis as any);
     });

     // ... tests per behavior block
   });
   ```

2. **cache.service.ts 구현 (GREEN):**
   ```typescript
   import { Injectable, Inject } from '@nestjs/common';
   import type IORedis from 'ioredis';
   import { REDIS_CLIENT } from '../booking/providers/redis.provider.js';

   const DEFAULT_TTL = 300; // 5분 (per D-08)

   @Injectable()
   export class CacheService {
     constructor(
       @Inject(REDIS_CLIENT) private readonly redis: IORedis,
     ) {}

     async get<T>(key: string): Promise<T | null> {
       try {
         const data = await this.redis.get(key);
         if (!data) return null;
         return JSON.parse(data) as T;
       } catch {
         return null;
       }
     }

     async set(key: string, data: unknown, ttlSeconds: number = DEFAULT_TTL): Promise<void> {
       try {
         await this.redis.set(key, JSON.stringify(data), 'EX', ttlSeconds);
       } catch {
         // Graceful degradation: 캐시 저장 실패 시 무시
       }
     }

     async invalidate(...keys: string[]): Promise<void> {
       if (keys.length > 0) {
         await this.redis.del(...keys);
       }
     }

     async invalidatePattern(pattern: string): Promise<void> {
       const keys = await this.redis.keys(pattern);
       if (keys.length > 0) {
         await this.redis.del(...keys);
       }
     }
   }
   ```

3. **performance.module.ts 업데이트:**
   ```typescript
   import { Module } from '@nestjs/common';
   import { BookingModule } from '../booking/booking.module.js';
   import { PerformanceController } from './performance.controller.js';
   import { PerformanceService } from './performance.service.js';
   import { CacheService } from './cache.service.js';

   @Module({
     imports: [BookingModule],  // REDIS_CLIENT provider 접근을 위해 import
     controllers: [PerformanceController],
     providers: [PerformanceService, CacheService],
     exports: [PerformanceService, CacheService],
   })
   export class PerformanceModule {}
   ```
   BookingModule을 import하여 REDIS_CLIENT를 DI로 주입받는다. CacheService를 exports에 포함하여 AdminModule에서도 사용 가능하게 한다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec vitest run src/modules/performance/__tests__/cache.service.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cache.service.ts 파일이 존재하고 CacheService 클래스가 export된다
    - cache.service.ts에 get, set, invalidate, invalidatePattern 4개 메서드가 있다
    - cache.service.ts에 DEFAULT_TTL이 300으로 정의되어 있다
    - cache.service.ts에 `@Inject(REDIS_CLIENT)` 사용
    - cache.service.ts get()과 set()에 try/catch가 있다 (graceful degradation)
    - cache.service.spec.ts의 모든 테스트 통과 (vitest exit code 0)
    - performance.module.ts에 BookingModule이 imports에 포함되어 있다
    - performance.module.ts에 CacheService가 providers와 exports에 포함되어 있다
  </acceptance_criteria>
  <done>CacheService 생성, 유닛 테스트 통과, PerformanceModule에 DI 연결 완료</done>
</task>

<task type="auto">
  <name>Task 2: PerformanceService 캐시 적용 + AdminService 캐시 무효화</name>
  <files>
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/performance/performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin-banner.controller.ts
    apps/api/src/modules/admin/admin.module.ts
  </files>
  <read_first>
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/performance/performance.controller.ts
    apps/api/src/modules/performance/cache.service.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin-banner.controller.ts
    apps/api/src/modules/admin/admin.module.ts
  </read_first>
  <action>
**Per D-07, D-08, D-09:**

캐시 키 네이밍 컨벤션:
```
cache:performances:list:{genre}:{page}:{limit}:{sort}:{ended}:{sub|none}
cache:performances:detail:{id}
cache:home:banners
cache:home:hot
cache:home:new
```

1. **performance.service.ts에 CacheService 주입 + read-through 캐시 적용:**

   constructor에 CacheService 추가:
   ```typescript
   constructor(
     @Inject(DRIZZLE) private readonly db: DrizzleDB,
     private readonly cacheService: CacheService,
   ) {}
   ```

   `findByGenre()` - 캐시 read-through:
   ```typescript
   async findByGenre(genre: string, query: PerformanceQuery): Promise<PerformanceListResponse> {
     const { page = 1, limit = 20, sort = 'latest', ended = false, sub } = query;
     const cacheKey = `cache:performances:list:${genre}:${page}:${limit}:${sort}:${ended}:${sub ?? 'none'}`;

     const cached = await this.cacheService.get<PerformanceListResponse>(cacheKey);
     if (cached) return cached;

     // ... 기존 DB 조회 로직 그대로 유지 ...

     const result = { data: ..., total, page, limit, totalPages: ... };
     await this.cacheService.set(cacheKey, result);
     return result;
   }
   ```

   `findById()` - 캐시 read-through (viewCount 증가는 항상 실행):
   ```typescript
   async findById(id: string): Promise<PerformanceWithDetails | null> {
     // viewCount 증가는 캐시 여부와 관계없이 항상 실행
     await this.db
       .update(performances)
       .set({ viewCount: sql`${performances.viewCount} + 1` })
       .where(eq(performances.id, id));

     const cacheKey = `cache:performances:detail:${id}`;
     const cached = await this.cacheService.get<PerformanceWithDetails>(cacheKey);
     if (cached) return cached;

     // ... 기존 DB 조회 로직 (viewCount increment 이후 부분) 그대로 유지 ...

     if (!performanceRow) return null;

     const result = { ... };
     await this.cacheService.set(cacheKey, result);
     return result;
   }
   ```

   `getHomeBanners()` - 캐시:
   ```typescript
   async getHomeBanners(): Promise<Banner[]> {
     const cacheKey = 'cache:home:banners';
     const cached = await this.cacheService.get<Banner[]>(cacheKey);
     if (cached) return cached;

     // ... 기존 DB 조회 ...
     const result = rows.map(...);
     await this.cacheService.set(cacheKey, result);
     return result;
   }
   ```

   `getHotPerformances()` - 캐시 (`cache:home:hot`):
   동일 패턴. cacheKey = `'cache:home:hot'`.

   `getNewPerformances()` - 캐시 (`cache:home:new`):
   동일 패턴. cacheKey = `'cache:home:new'`.

   CacheService import 추가:
   ```typescript
   import { CacheService } from './cache.service.js';
   ```

2. **admin.service.ts에 CacheService 주입 + 캐시 무효화:**

   constructor에 CacheService 추가:
   ```typescript
   constructor(
     @Inject(DRIZZLE) private readonly db: DrizzleDB,
     private readonly cacheService: CacheService,
   ) {}
   ```

   `createPerformance()` 끝에 캐시 무효화 추가:
   ```typescript
   // return 직전에 추가
   await this.cacheService.invalidatePattern('cache:performances:list:*');
   await this.cacheService.invalidatePattern('cache:home:*');
   ```

   `updatePerformance()` 끝에 캐시 무효화 추가:
   ```typescript
   // return 직전에 추가
   await this.cacheService.invalidate(`cache:performances:detail:${id}`);
   await this.cacheService.invalidatePattern('cache:performances:list:*');
   await this.cacheService.invalidatePattern('cache:home:*');
   ```

   `deletePerformance()` 끝에 캐시 무효화 추가:
   ```typescript
   async deletePerformance(id: string): Promise<void> {
     await this.db.delete(performances).where(eq(performances.id, id));
     await this.cacheService.invalidate(`cache:performances:detail:${id}`);
     await this.cacheService.invalidatePattern('cache:performances:list:*');
     await this.cacheService.invalidatePattern('cache:home:*');
   }
   ```

   배너 CRUD에도 캐시 무효화 추가:
   - `createBanner()` 끝: `await this.cacheService.invalidate('cache:home:banners');`
   - `updateBanner()` 끝: `await this.cacheService.invalidate('cache:home:banners');`
   - `deleteBanner()` 끝: `await this.cacheService.invalidate('cache:home:banners');`
   - `reorderBanners()` 끝: `await this.cacheService.invalidate('cache:home:banners');`

   CacheService import 추가:
   ```typescript
   import { CacheService } from '../performance/cache.service.js';
   ```

3. **admin.module.ts 업데이트:**
   AdminModule에서 CacheService를 사용하므로 PerformanceModule이 이미 imports에 있는지 확인. 없으면 추가하되, 이미 PerformanceService를 import하고 있다면 PerformanceModule import로 전환하여 CacheService도 함께 사용 가능하게 한다.

   admin.module.ts를 읽어서 현재 상태를 확인한 후:
   - PerformanceModule이 imports에 없으면 추가
   - CacheService가 PerformanceModule에서 export되므로 별도 등록 불필요

4. **admin-performance.controller.ts와 admin-banner.controller.ts:**
   이 컨트롤러들은 AdminService를 통해 간접적으로 캐시를 무효화하므로 변경 불필요. 단, admin.module.ts의 DI 설정만 올바르면 된다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec tsc --noEmit 2>&1 | head -30 && pnpm --filter @grapit/api exec vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - performance.service.ts constructor에 CacheService가 주입되어 있다
    - performance.service.ts의 findByGenre에 `cache:performances:list:` 캐시 키 사용
    - performance.service.ts의 findById에 `cache:performances:detail:` 캐시 키 사용
    - performance.service.ts의 findById에서 viewCount 증가가 캐시 확인 전에 실행된다
    - performance.service.ts의 getHomeBanners에 `cache:home:banners` 캐시 키 사용
    - performance.service.ts의 getHotPerformances에 `cache:home:hot` 캐시 키 사용
    - performance.service.ts의 getNewPerformances에 `cache:home:new` 캐시 키 사용
    - admin.service.ts constructor에 CacheService가 주입되어 있다
    - admin.service.ts createPerformance에 `invalidatePattern('cache:performances:list:*')` 호출 있음
    - admin.service.ts updatePerformance에 `invalidate('cache:performances:detail:${id}')` 호출 있음
    - admin.service.ts deletePerformance에 캐시 무효화 호출 있음
    - admin.service.ts의 배너 CRUD 4곳에 `invalidate('cache:home:banners')` 호출 있음
    - TypeScript 컴파일 에러 없음 (tsc --noEmit 통과)
    - vitest 전체 테스트 통과
  </acceptance_criteria>
  <done>PerformanceService 5개 엔드포인트에 read-through 캐시 적용, AdminService CRUD 7곳에 캐시 무효화 구현, 전체 테스트 + 타입체크 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CacheService -> Redis | 캐시 데이터 직렬화/역직렬화. 캐시 키는 서버 측 생성, 사용자 입력 불포함 |
| Client -> PerformanceController | Public 엔드포인트. 캐시된 데이터를 읽기 전용으로 반환 |
| Admin -> AdminController | RolesGuard로 보호. admin만 캐시 무효화 트리거 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | cache.service.ts 캐시 poisoning | mitigate | 캐시 키를 서버 측에서만 생성 (genre, page, id 등은 서버 파라미터). 사용자 입력을 키에 직접 사용하지 않음. 쿼리 파라미터는 Zod 검증 후 전달 |
| T-07-05 | Denial of Service | KEYS 명령어 | accept | invalidatePattern에서 KEYS 사용. 현재 캐시 키 수가 100개 미만으로 안전. 트래픽 증가 시 SCAN으로 교체 필요 (Out of Scope for this phase) |
| T-07-06 | Information Disclosure | 캐시 데이터 | accept | 캐시 대상이 모두 Public 엔드포인트 (performances, home). 인증 필요 없는 데이터만 캐시. 사용자별 데이터 미캐시 |
</threat_model>

<verification>
1. `pnpm --filter @grapit/api exec vitest run` - 전체 테스트 통과
2. `pnpm --filter @grapit/api exec tsc --noEmit` - TypeScript 컴파일 에러 없음
3. `grep -r 'cacheService' apps/api/src/modules/performance/performance.service.ts` - 캐시 사용 확인
4. `grep -r 'invalidate' apps/api/src/modules/admin/admin.service.ts` - 무효화 확인
</verification>

<success_criteria>
- CacheService가 get/set/invalidate/invalidatePattern 제공
- PerformanceService 5개 메서드에 read-through 캐시 적용
- AdminService CRUD 7곳에 캐시 무효화 적용
- TTL 300초 (5분)
- 전체 테스트 통과 + TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/07-valkey/07-02-SUMMARY.md`
</output>
