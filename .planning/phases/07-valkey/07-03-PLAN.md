---
phase: 07-valkey
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .github/workflows/deploy.yml
  - scripts/provision-valkey.sh
autonomous: false
requirements:
  - VALK-02
  - VALK-05

must_haves:
  truths:
    - "Memorystore for Valkey 인스턴스 프로비저닝 스크립트가 존재한다"
    - "Cloud Run deploy.yml에 Direct VPC Egress 플래그가 추가되어 있다"
    - "deploy.yml secrets에 REDIS_URL이 추가되어 있다"
    - "deploy.yml env_vars에서 UPSTASH 관련 변수가 제거되어 있다"
  artifacts:
    - path: "scripts/provision-valkey.sh"
      provides: "GCP Memorystore for Valkey 프로비저닝 스크립트"
      contains: "gcloud memorystore instances create"
    - path: ".github/workflows/deploy.yml"
      provides: "Cloud Run Direct VPC Egress + REDIS_URL Secret 설정"
      contains: "vpc-egress=private-ranges-only"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GCP Secret Manager"
      via: "secrets mapping"
      pattern: "REDIS_URL=redis-url:latest"

user_setup:
  - service: gcp-memorystore
    why: "Valkey 인스턴스 프로비저닝 + Secret Manager에 REDIS_URL 등록"
    env_vars:
      - name: REDIS_URL
        source: "gcloud memorystore instances describe 출력의 discoveryEndpoints[0].address + port로 구성: redis://<ip>:<port>"
    dashboard_config:
      - task: "scripts/provision-valkey.sh 실행 후 REDIS_URL을 GCP Secret Manager에 등록"
        location: "GCP Console > Secret Manager 또는 gcloud secrets create redis-url"
---

<objective>
Google Memorystore for Valkey 프로비저닝 스크립트를 생성하고, Cloud Run deploy.yml에 Direct VPC Egress + REDIS_URL Secret을 추가한다. 인프라 프로비저닝은 사용자가 gcloud CLI로 직접 실행해야 하므로 checkpoint를 포함한다.

Purpose: Cloud Run에서 Valkey로의 VPC 연결을 안정적으로 유지 (per D-10, D-11)
Output: 프로비저닝 스크립트 + deploy.yml 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-valkey/07-CONTEXT.md
@.planning/phases/07-valkey/07-RESEARCH.md
@.planning/phases/07-valkey/07-01-SUMMARY.md

<interfaces>
<!-- deploy.yml current state -->

From .github/workflows/deploy.yml (current API deploy flags):
```yaml
flags: >-
  --service-account=grapit-cloudrun@${{ env.GCP_PROJECT_ID }}.iam.gserviceaccount.com
  --add-cloudsql-instances=${{ secrets.CLOUD_SQL_CONNECTION_NAME }}
  --min-instances=0
  --max-instances=5
  --memory=512Mi
  --cpu=1
  --port=8080
  --no-cpu-throttling
  --session-affinity
  --allow-unauthenticated
secrets: |
  DATABASE_URL=database-url:latest
  JWT_SECRET=jwt-secret:latest
  JWT_REFRESH_SECRET=jwt-refresh-secret:latest
  KAKAO_CLIENT_ID=kakao-client-id:latest
  KAKAO_CLIENT_SECRET=kakao-client-secret:latest
  NAVER_CLIENT_ID=naver-client-id:latest
  NAVER_CLIENT_SECRET=naver-client-secret:latest
  GOOGLE_CLIENT_ID=google-client-id:latest
  GOOGLE_CLIENT_SECRET=google-client-secret:latest
```

From 07-RESEARCH.md (GCP provisioning commands):
```bash
# PSC 서비스 연결 정책 생성
gcloud network-connectivity service-connection-policies create grapit-valkey-policy \
  --network=default --region=asia-northeast3 --service-class=gcp-memorystore \
  --subnets=https://www.googleapis.com/compute/v1/projects/PROJECT_ID/regions/asia-northeast3/subnetworks/default

# Memorystore 인스턴스 생성
gcloud memorystore instances create grapit-valkey \
  --location=asia-northeast3 --node-type=shared-core-nano \
  --shard-count=1 --replica-count=0 --engine-version=VALKEY_8_0 \
  --endpoints='[{"connections": [...]}]'

# Cloud Run Direct VPC Egress 플래그
--network=default --subnet=default --vpc-egress=private-ranges-only
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로비저닝 스크립트 생성 + deploy.yml 업데이트</name>
  <files>
    scripts/provision-valkey.sh
    .github/workflows/deploy.yml
  </files>
  <read_first>
    .github/workflows/deploy.yml
  </read_first>
  <action>
**Per D-10, D-11, VALK-02, VALK-05:**

1. **scripts/provision-valkey.sh 생성:**
   ```bash
   #!/usr/bin/env bash
   set -euo pipefail

   # Grapit - Google Memorystore for Valkey provisioning script
   # Usage: ./scripts/provision-valkey.sh <GCP_PROJECT_ID>
   #
   # Prerequisites:
   #   - gcloud CLI installed and authenticated
   #   - Billing enabled on the GCP project
   #
   # After running:
   #   1. Get the endpoint: gcloud memorystore instances describe grapit-valkey --location=asia-northeast3 --format="value(discoveryEndpoints[0].address,discoveryEndpoints[0].port)"
   #   2. Create GCP Secret: echo -n "redis://<IP>:<PORT>" | gcloud secrets create redis-url --data-file=-
   #   3. Grant Cloud Run service account access: gcloud secrets add-iam-policy-binding redis-url --member="serviceAccount:grapit-cloudrun@<PROJECT_ID>.iam.gserviceaccount.com" --role="roles/secretmanager.secretAccessor"

   PROJECT_ID="${1:?Usage: $0 <GCP_PROJECT_ID>}"
   REGION="asia-northeast3"
   INSTANCE_NAME="grapit-valkey"
   NETWORK="default"

   echo "=== Step 1: Enable required APIs ==="
   gcloud services enable \
     networkconnectivity.googleapis.com \
     compute.googleapis.com \
     serviceconsumermanagement.googleapis.com \
     memorystore.googleapis.com \
     --project="$PROJECT_ID"

   echo "=== Step 2: Create PSC service connection policy ==="
   gcloud network-connectivity service-connection-policies create grapit-valkey-policy \
     --network="$NETWORK" \
     --project="$PROJECT_ID" \
     --region="$REGION" \
     --service-class=gcp-memorystore \
     --subnets="https://www.googleapis.com/compute/v1/projects/$PROJECT_ID/regions/$REGION/subnetworks/$NETWORK" \
     2>/dev/null || echo "Policy already exists, skipping."

   echo "=== Step 3: Create Memorystore for Valkey instance ==="
   gcloud memorystore instances create "$INSTANCE_NAME" \
     --project="$PROJECT_ID" \
     --location="$REGION" \
     --node-type=shared-core-nano \
     --shard-count=1 \
     --replica-count=0 \
     --engine-version=VALKEY_8_0 \
     --endpoints="[{\"connections\": [{\"pscAutoConnection\": {\"network\": \"projects/$PROJECT_ID/global/networks/$NETWORK\", \"projectId\": \"$PROJECT_ID\"}}]}]"

   echo "=== Step 4: Get endpoint ==="
   echo "Waiting for instance to be ready..."
   gcloud memorystore instances describe "$INSTANCE_NAME" \
     --location="$REGION" \
     --project="$PROJECT_ID" \
     --format="yaml(discoveryEndpoints)"

   echo ""
   echo "=== Done ==="
   echo "Next steps:"
   echo "  1. Note the discoveryEndpoints address and port above"
   echo "  2. Create secret: echo -n 'redis://<IP>:<PORT>' | gcloud secrets create redis-url --data-file=- --project=$PROJECT_ID"
   echo "  3. Grant access: gcloud secrets add-iam-policy-binding redis-url --member='serviceAccount:grapit-cloudrun@$PROJECT_ID.iam.gserviceaccount.com' --role='roles/secretmanager.secretAccessor' --project=$PROJECT_ID"
   ```

   파일에 실행 권한 부여: `chmod +x scripts/provision-valkey.sh`

2. **deploy.yml API deploy 단계 업데이트:**

   `flags` 섹션에 Direct VPC Egress 플래그 3개 추가:
   ```yaml
   flags: >-
     --service-account=grapit-cloudrun@${{ env.GCP_PROJECT_ID }}.iam.gserviceaccount.com
     --add-cloudsql-instances=${{ secrets.CLOUD_SQL_CONNECTION_NAME }}
     --min-instances=0
     --max-instances=5
     --memory=512Mi
     --cpu=1
     --port=8080
     --no-cpu-throttling
     --session-affinity
     --allow-unauthenticated
     --network=default
     --subnet=default
     --vpc-egress=private-ranges-only
   ```

   `secrets` 섹션에 REDIS_URL 추가:
   ```yaml
   secrets: |
     DATABASE_URL=database-url:latest
     JWT_SECRET=jwt-secret:latest
     JWT_REFRESH_SECRET=jwt-refresh-secret:latest
     KAKAO_CLIENT_ID=kakao-client-id:latest
     KAKAO_CLIENT_SECRET=kakao-client-secret:latest
     NAVER_CLIENT_ID=naver-client-id:latest
     NAVER_CLIENT_SECRET=naver-client-secret:latest
     GOOGLE_CLIENT_ID=google-client-id:latest
     GOOGLE_CLIENT_SECRET=google-client-secret:latest
     REDIS_URL=redis-url:latest
   ```

   UPSTASH 관련 환경변수가 deploy.yml에 있다면 제거 (현재 없으므로 확인만).
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && grep -c 'vpc-egress=private-ranges-only' .github/workflows/deploy.yml && grep -c 'REDIS_URL=redis-url:latest' .github/workflows/deploy.yml && test -x scripts/provision-valkey.sh && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/provision-valkey.sh 파일이 존재하고 실행 권한이 있다
    - scripts/provision-valkey.sh에 `gcloud memorystore instances create` 명령이 있다
    - scripts/provision-valkey.sh에 `gcloud network-connectivity service-connection-policies create` 명령이 있다
    - scripts/provision-valkey.sh에 `--node-type=shared-core-nano` 옵션이 있다
    - scripts/provision-valkey.sh에 `--engine-version=VALKEY_8_0` 옵션이 있다
    - scripts/provision-valkey.sh에 `--location=asia-northeast3` 옵션이 있다
    - deploy.yml flags에 `--network=default` 있음
    - deploy.yml flags에 `--subnet=default` 있음
    - deploy.yml flags에 `--vpc-egress=private-ranges-only` 있음
    - deploy.yml secrets에 `REDIS_URL=redis-url:latest` 있음
    - deploy.yml에 UPSTASH 관련 환경변수 없음
  </acceptance_criteria>
  <done>프로비저닝 스크립트 완성, deploy.yml에 VPC Egress + REDIS_URL Secret 추가</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: GCP Memorystore 프로비저닝 실행 확인</name>
  <files>
    scripts/provision-valkey.sh
  </files>
  <action>
이 checkpoint는 사용자가 직접 GCP 인프라를 프로비저닝하고 결과를 확인하는 단계이다.
Claude가 자동화할 수 없는 GCP 계정 인증 + 리소스 생성 작업이므로 human-verify로 처리한다.
사용자는 scripts/provision-valkey.sh를 실행하고, 생성된 Valkey 엔드포인트를 Secret Manager에 등록한다.
  </action>
  <what-built>
    Memorystore for Valkey 프로비저닝 스크립트(scripts/provision-valkey.sh)와 Cloud Run deploy.yml에 Direct VPC Egress + REDIS_URL Secret 설정을 추가했습니다.
  </what-built>
  <how-to-verify>
    1. 프로비저닝 스크립트 실행 (GCP 프로젝트 ID 필요):
       ```bash
       ./scripts/provision-valkey.sh <YOUR_GCP_PROJECT_ID>
       ```
    2. 인스턴스 생성 완료 후 엔드포인트 확인:
       ```bash
       gcloud memorystore instances describe grapit-valkey \
         --location=asia-northeast3 \
         --format="value(discoveryEndpoints[0].address,discoveryEndpoints[0].port)"
       ```
    3. GCP Secret Manager에 REDIS_URL 등록:
       ```bash
       echo -n "redis://<위에서_확인한_IP>:<PORT>" | gcloud secrets create redis-url --data-file=-
       gcloud secrets add-iam-policy-binding redis-url \
         --member="serviceAccount:grapit-cloudrun@<PROJECT_ID>.iam.gserviceaccount.com" \
         --role="roles/secretmanager.secretAccessor"
       ```
    4. deploy.yml 확인: `--vpc-egress=private-ranges-only`와 `REDIS_URL=redis-url:latest` 존재 확인
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint - user runs provision script and registers secret"</automated>
  </verify>
  <done>Memorystore for Valkey 인스턴스가 ACTIVE 상태이고, REDIS_URL이 GCP Secret Manager에 등록됨</done>
  <resume-signal>인프라 프로비저닝 완료 시 "approved", 이슈 있으면 내용 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run -> Valkey (PSC) | Private Service Connect 내부 네트워크. 외부 접근 불가 |
| GitHub Actions -> GCP | Workload Identity Federation (OIDC)으로 인증 |
| Secret Manager -> Cloud Run | IAM 기반 시크릿 접근 제어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing | deploy.yml GCP auth | mitigate | Workload Identity Federation (OIDC) 사용. 서비스 계정 키 파일 미사용 (기존 패턴 유지) |
| T-07-08 | Information Disclosure | REDIS_URL Secret | mitigate | GCP Secret Manager에 저장, IAM으로 Cloud Run 서비스 계정에만 접근 허용. deploy.yml에 평문 미포함 |
| T-07-09 | Tampering | Valkey 네트워크 접근 | mitigate | PSC + Direct VPC Egress로 내부 네트워크만 허용. `--vpc-egress=private-ranges-only`로 private IP만 VPC 경유 |
</threat_model>

<verification>
1. `scripts/provision-valkey.sh` 파일 존재 + 실행 권한
2. `deploy.yml`에 VPC Egress 플래그 3개 + REDIS_URL Secret 존재
3. (수동) gcloud memorystore instances describe 결과에 discoveryEndpoints 존재
</verification>

<success_criteria>
- 프로비저닝 스크립트가 실행 가능하고 GCP API 활성화 + PSC 정책 + Memorystore 인스턴스 생성을 포함
- deploy.yml이 Direct VPC Egress + REDIS_URL Secret을 포함
- (수동) Memorystore 인스턴스가 ACTIVE 상태이고 Cloud Run에서 접근 가능
</success_criteria>

<output>
After completion, create `.planning/phases/07-valkey/07-03-SUMMARY.md`
</output>
