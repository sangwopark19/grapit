---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/sms/sms.service.ts
  - apps/api/src/modules/sms/sms.service.spec.ts
  - apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
autonomous: true
requirements: [SC-2, SC-3]
tags: [sms, valkey, cluster, hash-tag, refactor]
must_haves:
  truths:
    - "sms.service.ts exports smsOtpKey/smsAttemptsKey/smsVerifiedKey 키 빌더 3종 (single-source-of-truth for tests)"
    - "sms.service.ts exports VERIFY_AND_INCREMENT_LUA 상수 (기존 const -> export const)"
    - "sendVerificationCode pipeline 2줄과 verifyCode EVAL KEYS 3개가 모두 {sms:${e164}}:role hash tag 형태로 전환"
    - "Lua 본체(L55-79) 는 byte-identical 유지 (D-05)"
    - "rate-limit / cooldown / resend 키 (sms:phone:send:, sms:phone:verify:, sms:resend:, sms:cooldown:) 는 변경 없음 (D-04/D-15)"
    - "sms.service.spec.ts 와 redis.provider.spec.ts 의 과거 리터럴 기대값 (`expect.stringContaining('sms:otp:')`, exact `sms:otp:+821...`, `sms:attempts:+...`, `sms:verified:` 등) 이 모두 새 hash-tag 형태 (`{sms:+...}:otp` 등) 또는 빌더 호출로 교체 (REVIEWS.md HIGH#2)"
  artifacts:
    - path: "apps/api/src/modules/sms/sms.service.ts"
      provides: "hash-tag 적용된 3개 OTP 키 + export 된 키 빌더 + export 된 Lua 상수. 이 4개 export 는 SC-2 (cluster-mode guard test at 14-03) 의 물리적 전제이자 SC-3 (전체 API suite green, 14-02 에서 import) 의 single-source-of-truth."
      contains: "export const smsOtpKey"
      contains_also:
        - "export const smsAttemptsKey"
        - "export const smsVerifiedKey"
        - "export const VERIFY_AND_INCREMENT_LUA"
        - "{sms:${e164}}:otp"
    - path: "apps/api/src/modules/sms/sms.service.spec.ts"
      provides: "unit test 기대값이 새 hash-tag 키와 일치하도록 정리 — drift 제거 (REVIEWS.md HIGH#2 / MEDIUM#3)"
      contains: "{sms:"
    - path: "apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts"
      provides: "pipeline 스모크 테스트 키 리터럴이 새 hash-tag 스킴으로 갱신 (REVIEWS.md HIGH#2 / MEDIUM#3)"
      contains: "{sms:"
  key_links:
    - from: "SmsService.sendVerificationCode (pipeline)"
      to: "smsOtpKey(e164), smsAttemptsKey(e164)"
      via: "ioredis pipeline set/del"
      pattern: "pipeline\\.set\\(smsOtpKey"
    - from: "SmsService.verifyCode (EVAL)"
      to: "smsOtpKey/smsAttemptsKey/smsVerifiedKey"
      via: "redis.eval(VERIFY_AND_INCREMENT_LUA, 3, ...)"
      pattern: "redis\\.eval\\(VERIFY_AND_INCREMENT_LUA,\\s*3,\\s*smsOtpKey"
---

<review_traceability>
Addresses REVIEWS.md concerns:
- **HIGH#2** — sms.service.spec.ts / redis.provider.spec.ts 의 리터럴 stale key 기대값을 Task 2 에서 모두 갱신 (`expect.stringContaining('sms:otp:')`, exact `sms:otp:+821...`, `sms:attempts:+...`, `sms:verified:` 3패턴 모두 처리)
- **MEDIUM#3** — Task 1 의 최종 grep gate 에서 spec/__tests__ 오탐을 방지 (옵션 B 선택: spec 파일까지 새 hash-tag 로 갱신 완료 후 glob 제한 없이 검사)
</review_traceability>

<objective>
SmsService 에서 OTP 관련 3개 Valkey 키를 booking.service.ts (`b382e39`) 가 정립한 `{<common>}:<role>` hash-tag 스킴으로 전환하고, 키 빌더 함수 3종 + `VERIFY_AND_INCREMENT_LUA` 상수를 module-level export 로 승격한다. 이 export 는 Plan 02/03 의 통합 테스트가 import 해서 drift 재발을 구조적으로 막는 single-source-of-truth 이다. 동시에 현재 존재하는 unit/pipeline spec 2개(`sms.service.spec.ts`, `redis.provider.spec.ts`)의 과거 리터럴 기대값을 모두 새 hash-tag 형태로 갱신한다 (REVIEWS.md HIGH#2 / MEDIUM#3).

Purpose: Phase 13 UAT gap 10 (프로덕션 `/v1/sms/verify-code` → CROSSSLOT) 의 **루트 픽스**. CRC16 이 동일 slot 으로 매핑되어 cluster-mode Memorystore for Valkey 에서 `VERIFY_AND_INCREMENT_LUA` 가 정상 EVAL 된다. 또한 spec 파일 drift 를 소스와 동시에 끊어, 최종 gate `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` 가 spec/__tests__ 오탐 없이 0 을 반환한다.

Output:
- `apps/api/src/modules/sms/sms.service.ts` — 키 빌더 3종 + Lua 상수 export, 호출부(`sendVerificationCode` pipeline 2줄 + `verifyCode` EVAL KEYS 3개) 전환, Lua docstring 의 KEYS 주석 갱신.
- `apps/api/src/modules/sms/sms.service.spec.ts` — 과거 `sms:otp:` / `sms:attempts:` / `sms:verified:` 리터럴 기대값 3종 (`expect.stringContaining(...)`, exact string, template literal) 을 모두 새 hash-tag 형태 또는 빌더 import 호출로 교체.
- `apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` — pipeline 스모크 테스트의 exact 리터럴 (`sms:otp:+821012345678`, `sms:attempts:+821012345678`) 을 새 hash-tag 리터럴 (`{sms:+821012345678}:otp`, `{sms:+821012345678}:attempts`) 로 교체. 이 파일은 booking 모듈이 소유하므로 sms.service.ts export 에 의존하지 않고 리터럴만 교체한다 (booking 도메인 cross-module dep 신설 회피).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-RESEARCH.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md

<!-- Primary analog — copy its {<common>}:<role> convention verbatim -->
@apps/api/src/modules/booking/booking.service.ts

<!-- File being modified -->
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/sms.service.spec.ts
@apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts

<interfaces>
<!-- Key exports this plan CREATES (consumed by Plan 02/03). -->
<!-- Keep these signatures STABLE across revisions — downstream tests import them by name. -->

From apps/api/src/modules/sms/sms.service.ts (AFTER this plan):
```typescript
export const smsOtpKey: (e164: string) => string;
export const smsAttemptsKey: (e164: string) => string;
export const smsVerifiedKey: (e164: string) => string;
export const VERIFY_AND_INCREMENT_LUA: string;
export interface SendResult { success: boolean; message: string }
export interface VerifyResult { verified: boolean; message?: string }
```

From apps/api/src/modules/booking/booking.service.ts L127-130 (REFERENCE — hash-tag inline literal pattern):
```typescript
const userSeatsKey   = `{${showtimeId}}:user-seats:${userId}`;
const lockKey        = `{${showtimeId}}:seat:${seatId}`;
const lockedSeatsKey = `{${showtimeId}}:locked-seats`;
```

From apps/api/src/modules/booking/booking.service.ts L17-54 (REFERENCE — Lua docstring header with hash-tag note):
```
* KEYS[1] = {showtimeId}:user-seats:{userId}
* ...
* Hash tag {showtimeId} ensures all keys hash to the same Redis Cluster slot.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export 3 key builders + VERIFY_AND_INCREMENT_LUA from sms.service.ts (hash-tag scheme + D-13 single-source-of-truth)</name>
  <files>apps/api/src/modules/sms/sms.service.ts</files>

  <read_first>
    1. Read `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md` § Locked Decisions — D-01/D-02/D-05/D-13 (hash-tag 규격 + Lua 본체 불변 + module-level export 결정).
    2. Read `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md` § 1b (export pattern) + § 1a (Lua docstring 갱신 지시 + "Hash tag ... ensures all keys hash to the same Redis Cluster slot" 문장 복제).
    3. Read `apps/api/src/modules/sms/sms.service.ts` 전체 (420 lines, Read tool 1회로 충분). 특히 L39-79 (현재 Lua + docstring), L81-82 (interface 위치), L220-222 (pipeline), L360-369 (EVAL).
    4. Read `apps/api/src/modules/booking/booking.service.ts` L17-54 + L127-143 (analog — 주석 문구 + inline literal 패턴).
  </read_first>

  <behavior>
    - After edit: `grep -nE "^export const (smsOtpKey|smsAttemptsKey|smsVerifiedKey|VERIFY_AND_INCREMENT_LUA)" apps/api/src/modules/sms/sms.service.ts` — 4개 라인 출력
    - After edit: `grep -c 'sms:otp:\\|sms:attempts:\\|sms:verified:' apps/api/src/modules/sms/sms.service.ts` — **0** (Lua docstring 안의 과거 리터럴도 제거되어야 함)
    - After edit: `grep -c '{sms:\\${e164}}:otp\\|{sms:\\${e164}}:attempts\\|{sms:\\${e164}}:verified' apps/api/src/modules/sms/sms.service.ts` — **3** (smsOtpKey/smsAttemptsKey/smsVerifiedKey 각 1회)
    - Lua 본체 3개 redis.call 시그니처(`GET KEYS[1]`, `INCR KEYS[2]`, `SETEX KEYS[3], ...`) 는 byte-identical (D-05)
    - rate-limit 키 리터럴 (`sms:phone:send:${e164}`, `sms:phone:verify:${e164}`, `sms:resend:${e164}`, `cooldownKey`) 은 한 문자도 바뀌지 않음 (D-04/D-15)
    - `pipeline.set(smsOtpKey(e164), otp, 'PX', OTP_TTL_MS); pipeline.del(smsAttemptsKey(e164));` 두 줄이 정확히 존재 (L220-222 근방)
    - `redis.eval(VERIFY_AND_INCREMENT_LUA, 3, smsOtpKey(e164), smsAttemptsKey(e164), smsVerifiedKey(e164), code, String(OTP_MAX_ATTEMPTS), String(VERIFIED_FLAG_TTL_SEC))` 가 정확히 존재 (L360 근방)
  </behavior>

  <action>
    1. **Export 3 key builders** — file 상단 `export interface SendResult` (현재 L81) 바로 **위**에 다음을 추가한다. 주석 포함 정확히 이 문구:

    ```typescript
    // [Phase 14 / D-01 D-02 D-13] Hash-tag keyed OTP storage keys.
    // All 3 keys MUST share the `{sms:${e164}}` hash tag so CRC16 maps them
    // to the same Redis Cluster slot — otherwise `VERIFY_AND_INCREMENT_LUA`
    // (multi-key EVAL) raises `CROSSSLOT Keys in request don't hash to the same slot`
    // on cluster-mode Valkey / Memorystore. Pattern lifted from
    // booking.service.ts (commit b382e39).
    export const smsOtpKey      = (e164: string): string => `{sms:${e164}}:otp`;
    export const smsAttemptsKey = (e164: string): string => `{sms:${e164}}:attempts`;
    export const smsVerifiedKey = (e164: string): string => `{sms:${e164}}:verified`;
    ```

    2. **Promote VERIFY_AND_INCREMENT_LUA to export const** — L55 의 `const VERIFY_AND_INCREMENT_LUA = \`` 를 `export const VERIFY_AND_INCREMENT_LUA = \`` 로 바꾼다. Lua 본체(L56-79 = `local stored = redis.call('GET', KEYS[1])` 시작부터 closing backtick 까지)는 **byte-identical** 유지 (D-05).

    3. **Lua docstring 갱신** — L39-54 의 JSDoc KEYS 주석을 아래 형태로 교체. 문구의 순서·들여쓰기·주석 기호를 아래와 정확히 동일하게:

    ```typescript
    /**
     * [Phase 10.1 / hash-tag 적용 Phase 14] Atomic OTP verify + attempt counter + verified flag.
     * KEYS:
     *   [1] {sms:{e164}}:otp        — 6-digit OTP body (TTL 180s)
     *   [2] {sms:{e164}}:attempts   — wrong-attempt counter (TTL 900s on first INCR)
     *   [3] {sms:{e164}}:verified   — verified flag (SETEX on VERIFIED, TTL 600s)
     * ARGV:
     *   [1] user-provided code (6 digits)
     *   [2] max attempts (e.g. '5')
     *   [3] verified flag TTL seconds (e.g. '600')
     * Returns: [result_string, number]
     *   {'VERIFIED', attempts}        -- correct. otp/attempts DEL, verified SETEX.
     *   {'WRONG', remaining}          -- wrong. attempts INCR(+EXPIRE if first).
     *   {'EXPIRED', 0}                -- otp expired/missing.
     *   {'NO_MORE_ATTEMPTS', 0}       -- exceeded. otp/attempts DEL.
     *
     * Hash tag `{sms:{e164}}` ensures all 3 keys hash to the same Redis Cluster slot.
     */
    ```

    4. **pipeline 전환** (L220-222):
    ```typescript
    // BEFORE
    pipeline.set(`sms:otp:${e164}`, otp, 'PX', OTP_TTL_MS);
    pipeline.del(`sms:attempts:${e164}`);
    // AFTER
    pipeline.set(smsOtpKey(e164), otp, 'PX', OTP_TTL_MS);
    pipeline.del(smsAttemptsKey(e164));
    ```

    5. **EVAL 전환** (L360-369):
    ```typescript
    // BEFORE
    const result = (await this.redis.eval(
      VERIFY_AND_INCREMENT_LUA,
      3,
      `sms:otp:${e164}`,
      `sms:attempts:${e164}`,
      `sms:verified:${e164}`,
      code,
      String(OTP_MAX_ATTEMPTS),
      String(VERIFIED_FLAG_TTL_SEC),
    )) as [string, number];
    // AFTER
    const result = (await this.redis.eval(
      VERIFY_AND_INCREMENT_LUA,
      3,
      smsOtpKey(e164),
      smsAttemptsKey(e164),
      smsVerifiedKey(e164),
      code,
      String(OTP_MAX_ATTEMPTS),
      String(VERIFIED_FLAG_TTL_SEC),
    )) as [string, number];
    ```

    6. **인라인 주석도 새 hash-tag 형태로** — L210 의 `// OTP storage. sms:attempts:{e164} TTL is 900s` 같은 주석 안의 과거 리터럴(`sms:attempts:{e164}`, `sms:verified:{e164}`) 도 `{sms:{e164}}:attempts`, `{sms:{e164}}:verified` 형태로 치환 (L210, L321, L339, L351 근방). Code 로직은 건드리지 않고 주석 텍스트만 교체해서 최종 production grep gate 가 0을 반환하도록.

    7. **DO NOT TOUCH** (명시적 가드 — D-04/D-06/D-14/D-15):
       - L31-37 `ATOMIC_INCR_LUA`
       - L176 `sms:resend:${e164}` / `cooldownKey`
       - L189 `sms:phone:send:${e164}`
       - L259 `this.redis.decr(\`sms:phone:send:${e164}\`)`
       - L326 `atomicIncr(\`sms:phone:verify:${e164}\`, ...)`
       - L398 `this.redis.decr(\`sms:phone:verify:${e164}\`)`
       - L390-415 catch 블록 message 문자열 (D-14 — 이 문자열은 D-07/Plan 04 에서 프론트가 서버 메시지 우선 표시로 노출)

    8. Type check: `pnpm --filter @grabit/api typecheck` 가 0 에러여야 한다.
  </action>

  <acceptance_criteria>
    - [ ] `grep -nE "^export const smsOtpKey" apps/api/src/modules/sms/sms.service.ts` — 1 line
    - [ ] `grep -nE "^export const smsAttemptsKey" apps/api/src/modules/sms/sms.service.ts` — 1 line
    - [ ] `grep -nE "^export const smsVerifiedKey" apps/api/src/modules/sms/sms.service.ts` — 1 line
    - [ ] `grep -nE "^export const VERIFY_AND_INCREMENT_LUA" apps/api/src/modules/sms/sms.service.ts` — 1 line
    - [ ] `grep -c "sms:otp:\\|sms:attempts:\\|sms:verified:" apps/api/src/modules/sms/sms.service.ts` → **0** (과거 리터럴, 과거 docstring 자취, 과거 인라인 주석 모두 제거)
    - [ ] **Exact-count guard (D-04/D-15 불변 가드 강화):** `grep -E "(sms:phone:send|sms:phone:verify|sms:resend):" apps/api/src/modules/sms/sms.service.ts | wc -l` → **5** (pre-edit 실측값, 2026-04-24 기준). 값이 5 보다 작으면 rate-limit 키 누락, 5 보다 크면 신규 키 유입 — 둘 다 D-04 위반이므로 즉시 reject.
    - [ ] `grep -n "smsOtpKey(e164)" apps/api/src/modules/sms/sms.service.ts` — sendVerificationCode 에서 1회 + verifyCode EVAL 에서 1회 = **2 occurrences**
    - [ ] `grep -n "smsAttemptsKey(e164)" apps/api/src/modules/sms/sms.service.ts` — 2 occurrences
    - [ ] `grep -n "smsVerifiedKey(e164)" apps/api/src/modules/sms/sms.service.ts` — 1 occurrence (verifyCode EVAL only; sendVerificationCode pipeline 은 verified 키 건드리지 않음)
    - [ ] `grep -n "Hash tag .sms:.e164.. ensures" apps/api/src/modules/sms/sms.service.ts` — 1 line (docstring 끝부분 마커)
    - [ ] Lua body 라인 수 불변: `awk '/^export const VERIFY_AND_INCREMENT_LUA = \`/,/^\`;$/' apps/api/src/modules/sms/sms.service.ts | wc -l` 은 현재 값과 동일 (본체 byte-identical 확인)
    - [ ] `pnpm --filter @grabit/api typecheck` exits 0
    - [ ] `pnpm --filter @grabit/api lint` — 신규 warning 없음
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @grabit/api typecheck && pnpm --filter @grabit/api lint && grep -q "^export const smsOtpKey" apps/api/src/modules/sms/sms.service.ts && grep -q "^export const VERIFY_AND_INCREMENT_LUA" apps/api/src/modules/sms/sms.service.ts && ! grep -qE "sms:otp:|sms:attempts:|sms:verified:" apps/api/src/modules/sms/sms.service.ts && [ "$(grep -E '(sms:phone:send|sms:phone:verify|sms:resend):' apps/api/src/modules/sms/sms.service.ts | wc -l | tr -d ' ')" = "5" ]</automated>
  </verify>

  <done>
    sms.service.ts 가 4개 심볼(`smsOtpKey`, `smsAttemptsKey`, `smsVerifiedKey`, `VERIFY_AND_INCREMENT_LUA`)을 export 하고, OTP 3개 키 호출부 3군데(pipeline 2줄 + EVAL KEYS 3개)가 모두 `{sms:${e164}}:<role>` 형태. 과거 `sms:otp:`/`sms:attempts:`/`sms:verified:` 리터럴은 파일 전체에서 0개(Lua docstring + 인라인 주석 포함). rate-limit/cooldown/resend 키 출현 횟수는 pre-edit 과 정확히 동일(5회, exact-count). typecheck+lint green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update sms.service.spec.ts + redis.provider.spec.ts — stale key 기대값을 새 hash-tag 형태로 전면 교체 (REVIEWS.md HIGH#2)</name>
  <files>
    apps/api/src/modules/sms/sms.service.spec.ts
    apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
  </files>

  <read_first>
    1. Read `apps/api/src/modules/sms/sms.service.spec.ts` 전체 (약 800 lines). 특히:
       - L24 (주석 안의 `sms:attempts:{e164}` 리터럴)
       - L292 `expect.stringContaining('sms:otp:')` — pipeline assert 패턴
       - L472-474 주석 속 `sms:attempts:{e164}` 문구
       - L497 exact `'sms:otp:+821012345678'` 인자 매칭
       - L504 exact `'sms:attempts:+821012345678'` 인자 매칭
       - L573 설명 문구 `sms:verified:{e164}`
       - L589-591 세 줄 `expect.stringContaining('sms:otp:')` / `('sms:attempts:')` / `('sms:verified:')`
       - L726/L740/L750/L775 근방 verified flag 관련 `expect.stringContaining('sms:verified:')` 및 주석
    2. Read `apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` 전체. 특히 L153-164 의 3 리터럴: `'sms:otp:+821012345678'`, `'sms:attempts:+821012345678'`, `redis.get('sms:otp:+821012345678')`.
    3. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md` § SP-2 (single-source-of-truth rule).
    4. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md` § Concerns — HIGH#2 / MEDIUM#3 (리터럴 3 패턴 전면 갱신 + 최종 grep gate 옵션 B 선택 근거).
  </read_first>

  <behavior>
    **Target state — 두 파일 모두:**
    - `expect.stringContaining('sms:otp:')` → `expect.stringContaining('{sms:')` 또는 `expect.stringContaining(':otp')` (hash-tag 새 형태)
    - exact string `'sms:otp:+821012345678'` → `'{sms:+821012345678}:otp'`
    - exact string `'sms:attempts:+821012345678'` → `'{sms:+821012345678}:attempts'`
    - exact string `'sms:verified:+...'` (있으면) → `'{sms:+...}:verified'`
    - `expect.stringContaining('sms:attempts:')` → `expect.stringContaining(':attempts')`
    - `expect.stringContaining('sms:verified:')` → `expect.stringContaining(':verified')`
    - 주석 안의 `sms:attempts:{e164}`, `sms:verified:{e164}` 같은 템플릿형 문구 → `{sms:{e164}}:attempts`, `{sms:{e164}}:verified`
    - **sms.service.spec.ts** 에 한해 추가로: Task 1 이 export 한 `smsOtpKey / smsAttemptsKey / smsVerifiedKey` 를 import 해서 exact 리터럴 자리에 builder 호출로 치환해도 동등 (권장 — drift 재방지). redis.provider.spec.ts 는 booking 모듈 소유이므로 cross-module dep 신설 회피: 리터럴만 교체.
    - **DO NOT TOUCH** — rate-limit / resend / cooldown 리터럴 (`sms:phone:send:`, `sms:phone:verify:`, `sms:resend:`, `sms:cooldown:`) 및 `ATOMIC_INCR_LUA` 관련 기대값 — D-04.
    - After edit: 아래 5개 grep gate 가 모두 0 또는 >0 (명세 참조 acceptance_criteria)
    - After edit: `pnpm --filter @grabit/api test` 가 exit 0 (두 spec 파일 기대값과 Task 1 수정 후 production code 의 키 출력이 일치)
  </behavior>

  <action>
    ### Part A — `apps/api/src/modules/sms/sms.service.spec.ts`

    1. **Import 추가** (파일 상단, `describe` 선언 위):
       ```typescript
       import {
         smsOtpKey,
         smsAttemptsKey,
         smsVerifiedKey,
       } from './sms.service.js';
       ```

    2. **L292 전후 `expect.stringContaining('sms:otp:')`** — 새 hash-tag 형태로:
       ```typescript
       // BEFORE
       expect.stringContaining('sms:otp:'),
       // AFTER
       expect.stringContaining('{sms:'),
       // 또는 더 엄밀히 (권장):
       smsOtpKey('+821012345678'),
       ```

    3. **L497 / L504 exact 리터럴 인자 매칭** — builder 호출 또는 새 리터럴로 치환:
       ```typescript
       // BEFORE
       'sms:otp:+821012345678',
       // AFTER (권장)
       smsOtpKey('+821012345678'),
       // 또는 리터럴로:
       '{sms:+821012345678}:otp',

       // BEFORE
       'sms:attempts:+821012345678',
       // AFTER
       smsAttemptsKey('+821012345678'),   // 또는 '{sms:+821012345678}:attempts'
       ```

    4. **L589-591 세 줄 `expect.stringContaining(...)`**:
       ```typescript
       // BEFORE
       expect.stringContaining('sms:otp:'),
       expect.stringContaining('sms:attempts:'),
       expect.stringContaining('sms:verified:'),
       // AFTER
       smsOtpKey('+821012345678'),
       smsAttemptsKey('+821012345678'),
       smsVerifiedKey('+821012345678'),
       ```
       exact phone 값이 test setup 에서 다르면 해당 변수로 대체 (test context 에 정의된 phone 사용).

    5. **L726-775 근방 verified-flag 블록의 `expect.stringContaining('sms:verified:')`** — 모두 `smsVerifiedKey(phone)` 또는 `':verified'` substring 으로 치환.

    6. **주석 문구** (L24, L472-474, L573, L726, L740 근방) 의 `sms:attempts:{e164}`, `sms:verified:{e164}` 는 `{sms:{e164}}:attempts`, `{sms:{e164}}:verified` 로 수정. 의미 보존, 리터럴 제거.

    ### Part B — `apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts`

    이 파일은 booking 모듈에 속하므로 sms.service import 를 새로 만들지 않는다. **리터럴만 교체:**

    7. **L153-154 pipeline set/del**:
       ```typescript
       // BEFORE
       .set('sms:otp:+821012345678', '654321', 'PX', 180_000)
       .del('sms:attempts:+821012345678')
       // AFTER
       .set('{sms:+821012345678}:otp', '654321', 'PX', 180_000)
       .del('{sms:+821012345678}:attempts')
       ```

    8. **L164 redis.get**:
       ```typescript
       // BEFORE
       expect(await redis.get('sms:otp:+821012345678')).toBe('654321');
       // AFTER
       expect(await redis.get('{sms:+821012345678}:otp')).toBe('654321');
       ```

    9. **주석 (있다면)** — `sms:otp:+...` / `sms:attempts:+...` 를 `{sms:+...}:otp` / `{sms:+...}:attempts` 형태로 수정.

    ### 공통 (D-04 가드)
    10. 두 파일 모두, **rate-limit / resend / cooldown 리터럴은 한 글자도 건드리지 않는다**: `sms:phone:send:`, `sms:phone:verify:`, `sms:resend:`, `sms:cooldown:`.

    11. Run:
        - `pnpm --filter @grabit/api typecheck` — 0 errors
        - `pnpm --filter @grabit/api test` — 전체 unit suite green (sms.service.spec.ts + redis.provider.spec.ts 포함)
  </action>

  <acceptance_criteria>
    - [ ] **sms.service.spec.ts — 과거 OTP 리터럴 3종 제거:** `grep -cE "sms:otp:|sms:attempts:|sms:verified:" apps/api/src/modules/sms/sms.service.spec.ts` → **0** (리터럴 + template 형 주석 포함 모두 0)
    - [ ] **sms.service.spec.ts — `expect.stringContaining('sms:otp:')` 제거:** `grep -c "expect.stringContaining('sms:otp:')" apps/api/src/modules/sms/sms.service.spec.ts` → **0**
    - [ ] **sms.service.spec.ts — 새 hash-tag 형태 유입:** `grep -c "{sms:" apps/api/src/modules/sms/sms.service.spec.ts` → **>0** (리터럴 또는 builder 출력 일치 확인용)
    - [ ] **sms.service.spec.ts — builder import 사용:** `grep -c "smsOtpKey\\|smsAttemptsKey\\|smsVerifiedKey" apps/api/src/modules/sms/sms.service.spec.ts` → **≥3**
    - [ ] **redis.provider.spec.ts — 과거 OTP 리터럴 제거:** `grep -c "sms:otp:|sms:attempts:|sms:verified:" apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` → **0**
    - [ ] **redis.provider.spec.ts — 새 hash-tag 형태 유입:** `grep -c "{sms:+821012345678}:otp\\|{sms:+821012345678}:attempts" apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` → **≥2**
    - [ ] **D-04 가드 — rate-limit 리터럴 불변 (두 파일):** `grep -cE "sms:phone:send:|sms:phone:verify:|sms:resend:|sms:cooldown:" apps/api/src/modules/sms/sms.service.spec.ts` 값이 **pre-edit 과 동일** (변경 전 `git diff` 로 실측). redis.provider.spec.ts 는 pre-edit 에 이 리터럴이 없으므로 **0 유지**.
    - [ ] **Production grep gate (MEDIUM#3 옵션 B):** `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` → **0 matches (production + spec 전체에서)**. spec 파일이 갱신됐으므로 glob 제외 없이도 깨끗.
    - [ ] `pnpm --filter @grabit/api typecheck` exits 0
    - [ ] `pnpm --filter @grabit/api test` exits 0 (두 spec 모두 green)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @grabit/api typecheck && pnpm --filter @grabit/api test && [ "$(grep -c 'sms:otp:\|sms:attempts:\|sms:verified:' apps/api/src/modules/sms/sms.service.spec.ts)" = "0" ] && [ "$(grep -c 'sms:otp:\|sms:attempts:\|sms:verified:' apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts)" = "0" ] && grep -q "{sms:" apps/api/src/modules/sms/sms.service.spec.ts && grep -q "{sms:+821012345678}:otp" apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts</automated>
  </verify>

  <done>
    sms.service.spec.ts 와 redis.provider.spec.ts 의 과거 OTP 리터럴 3종 (`sms:otp:`, `sms:attempts:`, `sms:verified:`) 이 **리터럴·주석·`expect.stringContaining` 3 패턴 모두** 새 hash-tag 형태(`{sms:+...}:<role>`) 또는 Task 1 export builder 호출로 완전 치환. rate-limit 리터럴은 불변. `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` 가 glob 제외 없이 0 반환 (MEDIUM#3 옵션 B 달성). unit suite 전체 green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API `/v1/sms/verify-code` | untrusted 6-digit OTP + phone E.164 가 cluster-mode Valkey EVAL 로 내려감 — 이번 phase 의 surface |
| NestJS SmsService → IORedis single-node client → Memorystore Valkey Cluster | 키 3개가 같은 slot 으로 매핑되어야 Lua atomic 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `SmsService.verifyCode` catch message → client | accept | 기존 generic 문구("인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.") 는 스택 트레이스·슬롯 번호·키 이름을 포함하지 않음 (D-14 유지). 프론트가 노출하는 문구도 이 generic 카피뿐 (Plan 04 에서 확인). |
| T-14-02 | Elevation (brute-force window) | 키 스킴 전환 overlap 시 in-flight OTP | accept | D-04 rate-limit 키 (`sms:phone:verify:${e164}`, `sms:phone:send:${e164}`) 는 **불변** → brute-force counter 가 키 스킴 전환과 무관하게 유지. OTP TTL 180s → 최장 15분 내 자연 drain (D-19). |
| T-14-03 | Tampering (atomicity loss) | `VERIFY_AND_INCREMENT_LUA` 본체 오변형 | mitigate | Task 1 acceptance: Lua body 라인 수 불변 + byte-identical 검증 (grep diff). D-05 가드. |
| T-14-12 | Tampering (test-source drift re-emergence in unit/pipeline spec) | sms.service.spec.ts / redis.provider.spec.ts 가 과거 리터럴 기대값을 유지 → production 갱신과 drift | mitigate | Task 2 acceptance: 5개 grep gate (리터럴 제거 + 새 hash-tag 유입 + builder import 존재 + MEDIUM#3 옵션 B production grep 0). REVIEWS.md HIGH#2 직접 대응. |

Total risk delta for this plan: **+0 HIGH**, **+0 MEDIUM**, **+4 LOW (all accepted/mitigated)**.

RESEARCH.md § Security Domain + §2 공격 모델 이 ASVS L1 기준으로 검토 완료 — 신규 attack surface 0 건 (키 내부 이름만 변경, 외부 계약/응답 shape/인증 로직 모두 불변).
</threat_model>

<verification>
- `pnpm --filter @grabit/api typecheck` — 0 에러
- `pnpm --filter @grabit/api lint` — 신규 warning 0
- `pnpm --filter @grabit/api test` — unit suite green (sms.service.spec.ts + redis.provider.spec.ts 포함)
- grep 검증 (Task 1 + Task 2 acceptance_criteria 전체 pass)
- Lua body byte-identical 확인: `awk '/^export const VERIFY_AND_INCREMENT_LUA/,/^\`;$/' apps/api/src/modules/sms/sms.service.ts` 의 출력이 Phase 10.1 의 L55-79 와 동일
- **MEDIUM#3 옵션 B 최종 게이트:** `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` → empty (glob 제외 없이도 0)
</verification>

<success_criteria>
SC-3 (API test suite 녹색) 의 **사전 조건** 이 충족된다 — 이 plan 만으로는 integration 테스트가 녹색으로 전환되지 않고 Plan 02 가 정합화 완료 후에 전체 API suite 가 녹색이 된다. 단, unit suite (sms.service.spec.ts + redis.provider.spec.ts) 는 Task 2 완료 시점에 이미 녹색.

추가로 **SC-2 (cluster-mode guard test at 14-03) 의 물리적 전제** 를 제공: 14-03 의 sms-cluster-crossslot.integration.spec.ts 는 여기서 export 된 `smsOtpKey/smsAttemptsKey/smsVerifiedKey/VERIFY_AND_INCREMENT_LUA` 4 symbol 을 import 해야 cluster-mode 회귀 가드가 drift-free 상태를 유지한다.

**Measurable:**
- [ ] 4개 심볼 export (Task 1 acceptance 4개)
- [ ] production OTP 키 과거 리터럴 = 0 (Lua docstring + 인라인 주석 포함)
- [ ] rate-limit/cooldown/resend 키 출현 횟수 = 5 (exact-count, pre-edit 실측)
- [ ] pipeline + EVAL 호출부 = 새 빌더 사용
- [ ] sms.service.spec.ts + redis.provider.spec.ts 의 리터럴·`expect.stringContaining`·주석 3 패턴 전부 새 hash-tag 형태로 교체 (Task 2)
- [ ] `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` → **empty** (MEDIUM#3 옵션 B)
- [ ] typecheck + lint + unit test green
</success_criteria>

<output>
Plan 완료 후 `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-01-SUMMARY.md` 작성. 내용:
- 수정된 라인 범위 (L39-79 docstring + L55 export 추가 + L81 위 3 builder 추가 + L220-222 pipeline + L360-369 EVAL + 인라인 주석)
- Task 2 spec 파일 수정 라인 (sms.service.spec.ts 6-8개 지점 + redis.provider.spec.ts 3개 지점)
- Export 4종 signature 확정 (Plan 02/03 가 import 할 정확한 이름)
- Lua body byte-identical 증빙 (diff summary)
- Do-not-touch 리스트 재확인 (L31-37 ATOMIC_INCR, L176/189/259/326/398 rate-limit) + rate-limit exact-count 5 확증
- REVIEWS.md HIGH#2 / MEDIUM#3 해소 증빙 (`rg` 최종 gate 스크린샷 또는 출력)
</output>
</plan>
</content>
</invoke>