---
phase: 14
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/api/test/sms-cluster-crossslot.integration.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [SC-2]
tags: [sms, valkey, cluster, integration-test, regression-guard, testcontainers, ci]
must_haves:
  truths:
    - "testcontainers 가 cluster-enabled 모드로 `valkey/valkey:8` 을 기동하고 CLUSTER ADDSLOTSRANGE 0 16383 로 single-shard cluster 부트스트랩"
    - "IORedis.Cluster 클라이언트가 natMap 옵션과 함께 연결되어 EVAL 을 보낼 수 있다"
    - "scenario 1: 과거 스킴 키 3개 (hash-tag 없음) EVAL → CROSSSLOT ReplyError (negative guard, D-10)"
    - "scenario 2: 새 빌더 3개 키 EVAL → VERIFIED / WRONG / EXPIRED / NO_MORE_ATTEMPTS 4 분기 모두 pass"
    - "scenario 3: CLUSTER KEYSLOT 호출 → 3개 키 slot 번호 동일 (hash-tag 수학적 증명)"
    - "scenario 4: sendVerificationCode-style pipeline (SET otp, DEL attempts) 가 cluster 에서 성공"
    - "scenario 5: 다양한 e164 포맷 3개(+82..., +1..., +86...) 모두 slot 동일성 유지"
    - "natMap 은 CLUSTER SLOTS 결과를 동적으로 파싱해 구성 — container internal IP 대응 (REVIEWS.md MEDIUM#4)"
    - "`.github/workflows/ci.yml` 이 `pnpm --filter @grabit/api test:integration` 을 실행하고 PR 에서 green 상태로 finalize 됨 (REVIEWS.md HIGH#1 / MEDIUM#5 — SC-2 CI 편입 실체화)"
  artifacts:
    - path: "apps/api/test/sms-cluster-crossslot.integration.spec.ts"
      provides: "cluster-mode Valkey CROSSSLOT regression guard (Phase 10.1 공백 메우기) + 동적 natMap"
      min_lines: 140
      contains: "CLUSTER"
      contains_also:
        - "ADDSLOTSRANGE"
        - "IORedis.Cluster"
        - "VERIFY_AND_INCREMENT_LUA"
        - "smsOtpKey"
        - "CROSSSLOT"
        - "CLUSTER', 'SLOTS'"
    - path: ".github/workflows/ci.yml"
      provides: "PR/push pipeline 이 integration suite (testcontainers + Docker) 를 자동 실행 — SC-2 실체화 (REVIEWS.md HIGH#1)"
      contains: "test:integration"
  key_links:
    - from: "test beforeAll"
      to: "valkey/valkey:8 container with --cluster-enabled yes"
      via: "GenericContainer.withCommand([...])"
      pattern: "--cluster-enabled"
    - from: "test beforeAll"
      to: "CLUSTER ADDSLOTSRANGE 0 16383"
      via: "boot.call('CLUSTER', 'ADDSLOTSRANGE', ...)"
      pattern: "ADDSLOTSRANGE"
    - from: "test beforeAll"
      to: "natMap dynamic build from CLUSTER SLOTS"
      via: "boot.call('CLUSTER', 'SLOTS') → parse → Object.fromEntries"
      pattern: "CLUSTER', 'SLOTS'"
    - from: "scenario 1"
      to: "CROSSSLOT regex assertion"
      via: "expect(...).rejects.toThrow(/CROSSSLOT/)"
      pattern: "rejects.toThrow\\(/CROSSSLOT/\\)"
    - from: ".github/workflows/ci.yml"
      to: "pnpm --filter @grabit/api test:integration"
      via: "GitHub Actions run step"
      pattern: "@grabit/api test:integration"
---

<review_traceability>
Addresses REVIEWS.md concerns:
- **HIGH#1** — Task 2 신설: `.github/workflows/ci.yml` 에 `pnpm --filter @grabit/api test:integration` step 을 명시 추가. `files_modified` 에 ci.yml 추가. SC-2 "CI 편입" 물리적 충족.
- **MEDIUM#4** — Task 1 의 cluster bootstrap 헬퍼를 `CLUSTER SLOTS` 결과로 natMap 을 동적 구성 + 실패 시 진단 로그를 throw 하도록 보강. container internal IP 환경차 방어.
- **MEDIUM#5** — must_haves 에 "ci.yml 이 test:integration 을 실행하고 PR 에서 green 으로 finalize" 항목 추가. Success Criteria 도 명시.
</review_traceability>

<objective>
Cluster-mode Valkey 위에서 `VERIFY_AND_INCREMENT_LUA` 를 EVAL 하는 통합 테스트 파일 `apps/api/test/sms-cluster-crossslot.integration.spec.ts` 를 신규 작성하고, `.github/workflows/ci.yml` 에 integration suite 실행 step 을 추가한다. Phase 13 UAT gap 10 의 root failure — Phase 10.1 당시 standalone Valkey 에서만 테스트해 cluster CROSSSLOT 을 못 잡았다는 구조적 커버리지 공백 — 을 구조적으로 제거하고, CI 편입을 실체화해 "CI 에서 자동 실행된다" 를 SC-2 기준으로 충족한다.

Purpose: SC-2 (cluster-mode Valkey 통합 테스트: 과거 스킴 CROSSSLOT fail, 신규 스킴 pass + CI 자동 실행). 이 파일은 이번 phase 의 **2차 deliverable** 로서, D-12 요구대로 CI 에 편입되어 "다음에도 같은 실수가 repeat 되지 않는다" 를 보장한다.

Output:
- `apps/api/test/sms-cluster-crossslot.integration.spec.ts` — RESEARCH.md §Pattern 2 (single-master + ADDSLOTSRANGE) 를 그대로 구현한 5 시나리오 + 동적 natMap.
- `.github/workflows/ci.yml` — `pnpm --filter @grabit/api test:integration` step 추가 (Docker 는 ubuntu-latest runner 에 기본 포함).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-RESEARCH.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-01-PLAN.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md

<!-- Closest analogs for container lifecycle + lua smoke pattern -->
@apps/api/test/sms-throttle.integration.spec.ts
@apps/api/test/admin-dashboard.integration.spec.ts

<!-- Source of imports (produced by Plan 01) -->
@apps/api/src/modules/sms/sms.service.ts

<!-- CI workflow to modify (REVIEWS.md HIGH#1) -->
@.github/workflows/ci.yml

<interfaces>
From apps/api/src/modules/sms/sms.service.ts (Plan 01):
```typescript
export const smsOtpKey: (e164: string) => string;      // `{sms:${e164}}:otp`
export const smsAttemptsKey: (e164: string) => string; // `{sms:${e164}}:attempts`
export const smsVerifiedKey: (e164: string) => string; // `{sms:${e164}}:verified`
export const VERIFY_AND_INCREMENT_LUA: string;
```

IORedis.Cluster constructor options (from `ioredis` 5.x type decls):
```typescript
new IORedis.Cluster(
  startupNodes: { host: string; port: number }[],
  options: {
    natMap?: Record<string, { host: string; port: number }>;
    lazyConnect?: boolean;
    scaleReads?: 'master' | 'slave' | 'all';
    enableReadyCheck?: boolean;
    redisOptions?: { maxRetriesPerRequest?: number };
  },
);
```

CLUSTER SLOTS response shape (ioredis returns nested arrays):
```typescript
// [[startSlot, endSlot, [ip, port, nodeId], ...replicas], ...]
type ClusterSlotsReply = [number, number, [string, number, string], ...[string, number, string][]][];
```

testcontainers pattern (`GenericContainer.withCommand`):
- `valkey-server` with `--cluster-enabled yes` + `--cluster-require-full-coverage no`
- CLUSTER ADDSLOTSRANGE 0 16383 assigns all slots to the single node
- Poll `CLUSTER INFO` until `cluster_state:ok` (≤5s)
- Parse `CLUSTER SLOTS` for every advertised `ip:port` → insert into natMap (REVIEWS.md MEDIUM#4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cluster-mode testcontainer bootstrap + 5 scenarios with dynamic natMap (CROSSSLOT regression guard)</name>
  <files>apps/api/test/sms-cluster-crossslot.integration.spec.ts</files>

  <read_first>
    1. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-RESEARCH.md` § Pattern 2 (single-node cluster bootstrap 완전한 코드 블록) + §4 비교표 (grokzen 3-master 대신 valkey/valkey:8 1-master 선택 이유) + §9 (5 시나리오 요구사항 명세) + §Common Pitfalls #4 (cluster_state:ok 폴링) + #5 (natMap 필수 옵션).
    2. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md` § 4a + § 4b + § 4c (5 scenarios body).
    3. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md` § Concerns — MEDIUM#4 (동적 natMap 필요성 + 진단 로그).
    4. Read `apps/api/test/sms-throttle.integration.spec.ts` L1-30 (imports 패턴) + L318-331 (testcontainer lifecycle — 이것이 standalone 분석 analog) + L337-400 (4 시나리오 VERIFIED/WRONG/EXPIRED/NO_MORE_ATTEMPTS assertion 형태).
    5. Read `apps/api/test/admin-dashboard.integration.spec.ts` L33-83 (dual-container lifecycle 대안 — `StartedTestContainer` 타이핑 참조).
    6. Read `apps/api/vitest.integration.config.ts` 로 integration config 의 include pattern / timeout 확인. 신규 파일명이 `*.integration.spec.ts` 패턴에 걸려야 함.
    7. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md` D-10 (negative guard 필수), D-12 (CI 편입).
  </read_first>

  <behavior>
    - **beforeAll (≤180s timeout):**
      1. `valkey/valkey:8` 컨테이너를 `--cluster-enabled yes` + `--cluster-require-full-coverage no` + `--appendonly no` 로 기동
      2. 임시 IORedis 클라이언트 `boot` 로 `CLUSTER ADDSLOTSRANGE 0 16383` 실행
      3. `CLUSTER INFO` 폴링 (250ms × 20 회 = 5s max) 하여 `cluster_state:ok` 확인
      4. **`CLUSTER SLOTS` 결과를 파싱해 모든 advertised `ip:port` 를 `natMap` 에 자동 매핑** (REVIEWS.md MEDIUM#4 — container internal IP 방어)
      5. natMap mismatch 시 (parsing 실패, 0 entry 등) 진단 메시지를 throw: `CLUSTER SLOTS returned no usable ip:port tuples — check --cluster-announce-ip`
      6. `boot.quit()` → `new IORedis.Cluster([{host,port}], { natMap, ... })` 생성 후 `await cluster.connect()`
    - **afterAll:** `cluster.quit()` → `container.stop()` 순서
    - **beforeEach:** `cluster.del(smsOtpKey(phone), smsAttemptsKey(phone), smsVerifiedKey(phone))` — 3-key same-slot 삭제
    - **Scenario 1 [GUARD — negative]:** 과거 스킴 (`sms:otp:${phone}` 등, hash-tag 없음) 으로 EVAL → `CROSSSLOT` throw
    - **Scenario 2 [PASS — 4 분기]:** 새 빌더 키로 VERIFIED / WRONG / EXPIRED / NO_MORE_ATTEMPTS 4 분기 assertion
    - **Scenario 3 [HASH]:** `CLUSTER KEYSLOT` 3회 호출 → s1 === s2 === s3
    - **Scenario 4 [PIPELINE]:** sendVerificationCode-style `cluster.pipeline().set(smsOtpKey, ...).del(smsAttemptsKey)` 실행, `pipeline.exec()` 가 실패 entry 없음
    - **Scenario 5 [VARIATION]:** 여러 e164 (`+821012345678`, `+13125551234`, `+8613812345678`) 각각 3개 키 slot 동일성 유지
  </behavior>

  <action>
    1. **파일 신규 생성** — 아래 골격을 그대로 사용 (RESEARCH.md §Pattern 2 body 와 1:1 + MEDIUM#4 동적 natMap 반영). 파일 위치: `apps/api/test/sms-cluster-crossslot.integration.spec.ts`.

    ```typescript
    import { describe, it, expect, beforeAll, afterAll, beforeEach } from 'vitest';
    import { GenericContainer, type StartedTestContainer } from 'testcontainers';
    import IORedis, { Cluster } from 'ioredis';
    import {
      VERIFY_AND_INCREMENT_LUA,
      smsOtpKey,
      smsAttemptsKey,
      smsVerifiedKey,
    } from '../src/modules/sms/sms.service.js';

    /**
     * [Phase 14 / SC-2 / D-10 / D-12] Cluster-mode Valkey 회귀 가드.
     *
     * Phase 10.1 의 sms-throttle.integration.spec.ts 는 standalone valkey 에서
     * 녹색이었기에 프로덕션 Memorystore Cluster 의 CROSSSLOT 을 놓쳤다.
     * 이 스펙은 single-shard cluster(CLUSTER ADDSLOTSRANGE 0 16383)로 실제
     * CROSSSLOT wire-format 을 재현하며, 신규 `{sms:${e164}}:<role>` 스킴이
     * 동일 slot 으로 매핑되는 것을 기계적으로 증명한다.
     *
     * [REVIEWS.md MEDIUM#4] natMap 은 CLUSTER SLOTS 결과를 동적 파싱해 구성한다.
     * Mac/Linux/CI runner 간 container internal IP 차이로 인한 flakiness 방어.
     */

    const PHONE = '+821012345678';

    // [MEDIUM#4] Build natMap from CLUSTER SLOTS so container-internal IPs are
    // remapped to the host-visible {host, mapped port}. Returns the natMap and
    // logs the raw slot tuples for post-mortem when something misbehaves.
    type ClusterSlotTuple = [number, number, [string, number, string], ...[string, number, string][]];
    function buildNatMap(
      slots: ClusterSlotTuple[],
      host: string,
      port: number,
    ): Record<string, { host: string; port: number }> {
      const seen = new Set<string>();
      const natMap: Record<string, { host: string; port: number }> = {};
      for (const slot of slots) {
        // slot = [startSlot, endSlot, [ip, port, id], ...replicas]
        for (let i = 2; i < slot.length; i++) {
          const node = slot[i] as [string, number, string];
          const key = `${node[0]}:${node[1]}`;
          if (seen.has(key)) continue;
          seen.add(key);
          natMap[key] = { host, port };
        }
      }
      if (Object.keys(natMap).length === 0) {
        // eslint-disable-next-line no-console
        console.error('[cluster-bootstrap] raw CLUSTER SLOTS reply:', JSON.stringify(slots));
        throw new Error(
          'CLUSTER SLOTS returned no usable ip:port tuples — check --cluster-announce-ip or testcontainers host resolution.',
        );
      }
      // Always also map the literal host:6379 advertised by `--cluster-enabled`
      // default so naive replies still resolve (safe to overwrite).
      natMap[`${host}:6379`] = { host, port };
      return natMap;
    }

    describe('SMS OTP — Valkey Cluster mode (hash-tag regression guard)', () => {
      let container: StartedTestContainer;
      let cluster: Cluster;

      beforeAll(async () => {
        container = await new GenericContainer('valkey/valkey:8')
          .withExposedPorts(6379)
          .withCommand([
            'valkey-server',
            '--port', '6379',
            '--cluster-enabled', 'yes',
            '--cluster-config-file', 'nodes.conf',
            '--cluster-node-timeout', '5000',
            '--appendonly', 'no',
            '--cluster-require-full-coverage', 'no',
          ])
          .start();

        const host = container.getHost();
        const port = container.getMappedPort(6379);

        // Bootstrap cluster: single master owns all 16384 slots.
        const boot = new IORedis(`redis://${host}:${port}`, { maxRetriesPerRequest: 3 });
        await boot.call('CLUSTER', 'ADDSLOTSRANGE', '0', '16383');

        // Poll until cluster_state:ok (RESEARCH.md §Common Pitfalls #4).
        for (let i = 0; i < 20; i++) {
          const info = (await boot.call('CLUSTER', 'INFO')) as string;
          if (info.includes('cluster_state:ok')) break;
          await new Promise((r) => setTimeout(r, 250));
        }

        // [MEDIUM#4] Dynamic natMap — read CLUSTER SLOTS and remap every
        // advertised ip:port to the host-reachable {host, mapped port}.
        const slots = (await boot.call('CLUSTER', 'SLOTS')) as ClusterSlotTuple[];
        const natMap = buildNatMap(slots, host, port);
        // eslint-disable-next-line no-console
        console.log('[cluster-bootstrap] natMap:', natMap);
        await boot.quit();

        cluster = new IORedis.Cluster(
          [{ host, port }],
          {
            natMap,
            lazyConnect: true,
            scaleReads: 'master',
            enableReadyCheck: true,
            redisOptions: { maxRetriesPerRequest: 3 },
          },
        );
        await cluster.connect();
      }, 180_000);

      afterAll(async () => {
        await cluster?.quit();
        await container?.stop();
      });

      beforeEach(async () => {
        // Same-slot 3-key delete is legal by hash-tag construction.
        await cluster.del(smsOtpKey(PHONE), smsAttemptsKey(PHONE), smsVerifiedKey(PHONE));
      });

      // --- Scenario 1: NEGATIVE GUARD — legacy scheme must CROSSSLOT ---
      describe('과거 스킴 (hash-tag 없음) 은 cluster-mode 에서 CROSSSLOT 을 던진다', () => {
        it('rejects with CROSSSLOT reply error', async () => {
          await expect(
            cluster.eval(
              VERIFY_AND_INCREMENT_LUA,
              3,
              `sms:otp:${PHONE}`,
              `sms:attempts:${PHONE}`,
              `sms:verified:${PHONE}`,
              '123456',
              '5',
              '600',
            ),
          ).rejects.toThrow(/CROSSSLOT/);
        });
      });

      // --- Scenario 2: 4-branch Lua atomic verify on NEW hash-tagged keys ---
      describe('신규 hash-tag 스킴은 4분기 모두 정상 EVAL', () => {
        it('VERIFIED: correct code → 3 keys DEL + verified flag SETEX', async () => {
          await cluster.set(smsOtpKey(PHONE), '123456', 'PX', 180_000);
          const [status, attempts] = (await cluster.eval(
            VERIFY_AND_INCREMENT_LUA, 3,
            smsOtpKey(PHONE), smsAttemptsKey(PHONE), smsVerifiedKey(PHONE),
            '123456', '5', '600',
          )) as [string, number];
          expect(status).toBe('VERIFIED');
          expect(attempts).toBe(1);
          expect(await cluster.get(smsOtpKey(PHONE))).toBeNull();
          expect(await cluster.get(smsVerifiedKey(PHONE))).toBe('1');
        });

        it('WRONG: incorrect code → attempts INCR, remaining count returned', async () => {
          await cluster.set(smsOtpKey(PHONE), '123456', 'PX', 180_000);
          const [status, remaining] = (await cluster.eval(
            VERIFY_AND_INCREMENT_LUA, 3,
            smsOtpKey(PHONE), smsAttemptsKey(PHONE), smsVerifiedKey(PHONE),
            '999999', '5', '600',
          )) as [string, number];
          expect(status).toBe('WRONG');
          expect(remaining).toBe(4);
        });

        it('EXPIRED: no stored otp → returns EXPIRED', async () => {
          const [status] = (await cluster.eval(
            VERIFY_AND_INCREMENT_LUA, 3,
            smsOtpKey(PHONE), smsAttemptsKey(PHONE), smsVerifiedKey(PHONE),
            '123456', '5', '600',
          )) as [string, number];
          expect(status).toBe('EXPIRED');
        });

        it('NO_MORE_ATTEMPTS: 6th wrong attempt → DEL + NO_MORE_ATTEMPTS', async () => {
          await cluster.set(smsOtpKey(PHONE), '123456', 'PX', 180_000);
          // 5 wrong attempts consume the quota; 6th triggers exhaustion.
          for (let i = 0; i < 5; i++) {
            await cluster.eval(
              VERIFY_AND_INCREMENT_LUA, 3,
              smsOtpKey(PHONE), smsAttemptsKey(PHONE), smsVerifiedKey(PHONE),
              '999999', '5', '600',
            );
          }
          const [status] = (await cluster.eval(
            VERIFY_AND_INCREMENT_LUA, 3,
            smsOtpKey(PHONE), smsAttemptsKey(PHONE), smsVerifiedKey(PHONE),
            '999999', '5', '600',
          )) as [string, number];
          expect(status).toBe('NO_MORE_ATTEMPTS');
          expect(await cluster.get(smsOtpKey(PHONE))).toBeNull();
        });
      });

      // --- Scenario 3: CLUSTER KEYSLOT equality (hash-tag math proof) ---
      describe('3개 키가 동일 CLUSTER KEYSLOT 으로 매핑', () => {
        it('smsOtpKey / smsAttemptsKey / smsVerifiedKey share one slot', async () => {
          const s1 = await cluster.call('CLUSTER', 'KEYSLOT', smsOtpKey(PHONE));
          const s2 = await cluster.call('CLUSTER', 'KEYSLOT', smsAttemptsKey(PHONE));
          const s3 = await cluster.call('CLUSTER', 'KEYSLOT', smsVerifiedKey(PHONE));
          expect(s1).toBe(s2);
          expect(s2).toBe(s3);
        });
      });

      // --- Scenario 4: Pipeline SET + DEL (sendVerificationCode shape) ---
      describe('sendVerificationCode-style pipeline succeeds on cluster', () => {
        it('pipeline.set(otp) + del(attempts) executes without CROSSSLOT', async () => {
          const pipeline = cluster.pipeline();
          pipeline.set(smsOtpKey(PHONE), '654321', 'PX', 180_000);
          pipeline.del(smsAttemptsKey(PHONE));
          const results = await pipeline.exec();
          expect(results).not.toBeNull();
          expect(results!.every((r) => r[0] === null)).toBe(true);
          expect(await cluster.get(smsOtpKey(PHONE))).toBe('654321');
        });
      });

      // --- Scenario 5: multiple e164 formats all hold slot equality ---
      describe('다양한 e164 포맷에서도 3-key slot 동일성 유지', () => {
        it.each([
          ['+821012345678'],
          ['+13125551234'],
          ['+8613812345678'],
        ])('phone=%s', async (phone) => {
          const s1 = await cluster.call('CLUSTER', 'KEYSLOT', smsOtpKey(phone));
          const s2 = await cluster.call('CLUSTER', 'KEYSLOT', smsAttemptsKey(phone));
          const s3 = await cluster.call('CLUSTER', 'KEYSLOT', smsVerifiedKey(phone));
          expect(s1).toBe(s2);
          expect(s2).toBe(s3);
        });
      });
    });
    ```

    2. **파일명 & include pattern 확인** — `apps/api/vitest.integration.config.ts` 의 `include` 가 `test/**/*.integration.spec.ts` 면 자동 발견. 아니면 config 업데이트 없이 실행되는지 `pnpm --filter @grabit/api test:integration sms-cluster-crossslot -- --run` 으로 검증.

    3. **자주 발생하는 오류 대응** (RESEARCH.md §Common Pitfalls + REVIEWS.md MEDIUM#4):
       - `cluster_state:fail` 지속: ADDSLOTSRANGE 가 빠졌거나 polling 미작동. boot 이 정확히 한 번 호출됐는지 확인.
       - `natMap` 누락 시 `CLUSTERDOWN Hash slot not served` — **이제 CLUSTER SLOTS 기반 동적 구성 + `host:6379` fallback 으로 완화**. 그래도 `buildNatMap` 이 0 entry 반환 시 진단 로그 + throw.
       - `enableReadyCheck: false` 로 두면 lazyConnect + connect() 이 race 를 유발 — `true` 유지.
       - `ioredis` v5 에서 `Cluster` 는 named export 가 아니라 `IORedis.Cluster` 네임스페이스. `import IORedis, { Cluster } from 'ioredis'` 는 유효 (Cluster 는 타입).

    4. Run: `pnpm --filter @grabit/api test:integration sms-cluster-crossslot -- --run` — 5 시나리오(scenario 2 내부 4 it + scenario 5 it.each 3 subtest 포함) 총 10 it 모두 pass.
  </action>

  <acceptance_criteria>
    - [ ] `test -f apps/api/test/sms-cluster-crossslot.integration.spec.ts` — exists
    - [ ] `wc -l apps/api/test/sms-cluster-crossslot.integration.spec.ts` ≥ 140 (동적 natMap helper 추가로 120→140)
    - [ ] `grep -c "import {" apps/api/test/sms-cluster-crossslot.integration.spec.ts` ≥ 3 (vitest + testcontainers + ioredis + sms.service)
    - [ ] `grep -c "VERIFY_AND_INCREMENT_LUA\\|smsOtpKey\\|smsAttemptsKey\\|smsVerifiedKey" apps/api/test/sms-cluster-crossslot.integration.spec.ts` ≥ 10
    - [ ] `grep -n "from '../src/modules/sms/sms.service" apps/api/test/sms-cluster-crossslot.integration.spec.ts` — 1 match (import 는 반드시 Plan 01 export 소스에서)
    - [ ] `grep -n "cluster-enabled.*yes" apps/api/test/sms-cluster-crossslot.integration.spec.ts` — 1 match
    - [ ] `grep -n "ADDSLOTSRANGE" apps/api/test/sms-cluster-crossslot.integration.spec.ts` — 1 match
    - [ ] `grep -n "IORedis.Cluster" apps/api/test/sms-cluster-crossslot.integration.spec.ts` — 1 match
    - [ ] `grep -n "rejects.toThrow(/CROSSSLOT/)" apps/api/test/sms-cluster-crossslot.integration.spec.ts` — 1 match (D-10 negative guard 존재)
    - [ ] `grep -n "CLUSTER', 'KEYSLOT'" apps/api/test/sms-cluster-crossslot.integration.spec.ts` ≥ 2 matches (scenario 3 + scenario 5)
    - [ ] **[MEDIUM#4] Dynamic natMap 근거 존재:** `rg "CLUSTER SLOTS|cluster.call\\('SLOTS'\\)|CLUSTER', 'SLOTS'|cluster_slots|buildNatMap" apps/api/test/sms-cluster-crossslot.integration.spec.ts` — ≥1 matches
    - [ ] **[MEDIUM#4] natMap 진단 throw 존재:** `grep -c "no usable ip:port" apps/api/test/sms-cluster-crossslot.integration.spec.ts` ≥ 1
    - [ ] `grep -cE "\\bVERIFIED\\b|\\bWRONG\\b|\\bEXPIRED\\b|\\bNO_MORE_ATTEMPTS\\b" apps/api/test/sms-cluster-crossslot.integration.spec.ts` ≥ 4 (4분기 커버)
    - [ ] `pnpm --filter @grabit/api typecheck` exits 0
    - [ ] `pnpm --filter @grabit/api test:integration sms-cluster-crossslot -- --run` exits 0 (Docker 필수 — 약 10-15s 오버헤드)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @grabit/api typecheck && pnpm --filter @grabit/api test:integration sms-cluster-crossslot -- --run && grep -q "rejects.toThrow(/CROSSSLOT/)" apps/api/test/sms-cluster-crossslot.integration.spec.ts && grep -q "ADDSLOTSRANGE" apps/api/test/sms-cluster-crossslot.integration.spec.ts && grep -q "CLUSTER', 'SLOTS'" apps/api/test/sms-cluster-crossslot.integration.spec.ts && grep -q "no usable ip:port" apps/api/test/sms-cluster-crossslot.integration.spec.ts</automated>
  </verify>

  <done>
    파일 생성 + CI 실행 가능. 5 시나리오 모두 녹색:
    - scenario 1 : 과거 스킴 EVAL 이 CROSSSLOT 을 던짐 (negative guard pass)
    - scenario 2 : 신규 스킴 EVAL 이 VERIFIED/WRONG/EXPIRED/NO_MORE_ATTEMPTS 4 분기 모두 정상
    - scenario 3 : KEYSLOT 3 개 동일
    - scenario 4 : pipeline SET+DEL 성공
    - scenario 5 : 3 개 e164 포맷 모두 slot 동일성 유지
    동적 natMap 이 CLUSTER SLOTS 기반으로 구성되고 0-entry 시 진단 throw (MEDIUM#4). typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Add `pnpm --filter @grabit/api test:integration` step to .github/workflows/ci.yml (REVIEWS.md HIGH#1 / MEDIUM#5 — SC-2 CI 편입 실체화)</name>
  <files>.github/workflows/ci.yml</files>

  <read_first>
    1. Read `.github/workflows/ci.yml` 전체. 특히:
       - Job name `check` (ubuntu-latest)
       - 현재 step 순서: install → lint → typecheck → **pnpm test** → (DB migrate/seed/E2E …)
       - 새 step 을 `pnpm test` 바로 **다음** 에 삽입 (단위 테스트 green 이후 integration 실행)
    2. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md` § Concerns HIGH#1 + MEDIUM#5 (실체화 요구사항).
    3. `apps/api/package.json` 의 `scripts."test:integration"` 존재 확인 — 없다면 Task 2 는 **선행 조건 미충족** 으로 reject (Plan 02/03 중 하나가 이 script 를 제공하거나 이미 존재해야 함. 2026-04-24 기준 존재).
    4. Docker 가용성 — `ubuntu-latest` GitHub runner 는 Docker Engine 을 기본 탑재 (`docker version` 사용 가능). 별도 Docker-in-Docker 불필요.
  </read_first>

  <behavior>
    - `.github/workflows/ci.yml` 의 `check` job 에 **새 step** 추가: `pnpm --filter @grabit/api test:integration`
    - 위치: `pnpm test` step 직후, DB migrate 이전 (integration test 는 testcontainers 가 자체 DB/Valkey 를 띄우므로 outer postgres service 와 독립)
    - step name: `Integration tests (testcontainers — SC-2 Valkey Cluster CROSSSLOT guard)`
    - timeout: 기본 (일반 테스트 15-20s, 보수적으로 10분 default 사용 — 별도 `timeout-minutes` 불필요)
    - `testcontainers` 는 runner 의 Docker socket 을 자동 사용 (`/var/run/docker.sock`). `ubuntu-latest` 는 이를 기본 제공. 별도 `services:` 항목이나 Docker-in-Docker 설정 불필요 — 이는 14-HUMAN-UAT.md / SUMMARY 에 명기.
    - 후속 step (DB migrations, E2E) 의 동작·env·조건 일체 건드리지 않음 — 새 step 하나만 insert.
  </behavior>

  <action>
    1. `.github/workflows/ci.yml` 의 `- run: pnpm test` 라인(대략 L48) **바로 다음** 에 아래 step 을 삽입:

    ```yaml
      # [Phase 14 / SC-2 / REVIEWS.md HIGH#1] Integration tests — testcontainers-based
      # Valkey Cluster + Postgres smokes. Docker daemon on ubuntu-latest is sufficient
      # (testcontainers uses /var/run/docker.sock). This step makes SC-2 "CI 편입" 실체화:
      # sms-cluster-crossslot.integration.spec.ts 가 PR/push 에서 자동 실행되어 cluster
      # CROSSSLOT 회귀를 영구 차단.
      - name: Integration tests (testcontainers — SC-2 Valkey Cluster CROSSSLOT guard)
        run: pnpm --filter @grabit/api test:integration
    ```

    2. 삽입 후 상위·하위 step 의 들여쓰기/indent 를 근처 step 들과 정확히 동일(6 spaces 또는 현재 convention) 하게 맞춘다.

    3. **DO NOT TOUCH:**
       - `services.postgres` 블록 (Playwright E2E 가 사용)
       - 기존 env 변수 (`DATABASE_URL`, `JWT_SECRET`, …)
       - Toss secret gate, seed 스크립트, Playwright 실행 step
       - `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test` 순서

    4. **YAML 유효성 검증:** 삽입 후 `pnpm --filter @grabit/api exec node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))" 2>&1 || true` 또는 `actionlint` (사용 가능 시) 로 syntax check. 실패 시 indent 조정.

    5. **push 하지 않고 local git diff 확인:**
       ```bash
       git diff .github/workflows/ci.yml
       ```
       diff 가 **단 한 개 step 추가** 로만 국한되는지 시각 확인.

    6. **PR 에서 실제 green 확인은 Plan 04 checkpoint 직전에 수행** (Phase 14 PR 이 main 에 merge 되기 전 CI 가 통과하는 것이 SC-2 최종 증빙). 이 task 의 acceptance 는 파일 수정까지로 국한되고, PR green 은 HUMAN-UAT checkpoint 에서 확인.
  </action>

  <acceptance_criteria>
    - [ ] `test -f .github/workflows/ci.yml` — 존재 (당연)
    - [ ] **[HIGH#1]** `grep -n "@grabit/api test:integration" .github/workflows/ci.yml` — 1 match
    - [ ] **[HIGH#1]** `grep -n "Integration tests (testcontainers" .github/workflows/ci.yml` — 1 match
    - [ ] `grep -cE "pnpm test$|pnpm test " .github/workflows/ci.yml` ≥ 1 (기존 unit test step 보존 — 삭제되지 않았음)
    - [ ] Step 순서 유지: `grep -n "pnpm test\\|test:integration\\|drizzle-kit migrate" .github/workflows/ci.yml | head -5` 결과에서 `pnpm test` → `test:integration` → `drizzle-kit migrate` 순서로 line number 가 증가.
    - [ ] YAML syntax 유효: `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"` exits 0 (js-yaml 이 없으면 `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` 로 대체)
    - [ ] `git diff --stat .github/workflows/ci.yml` — 1 file changed, 추가 line ≤ 10 (step 5~8 줄 범위)
    - [ ] **[MEDIUM#5 / HIGH#1] PR green 는 14-HUMAN-UAT.md pre-condition 에서 확인** — `grep -c "@grabit/api test:integration" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1 (14-04 Plan 에서 HUMAN-UAT 생성 시 이미 포함하도록 연동)
  </acceptance_criteria>

  <verify>
    <automated>grep -q "@grabit/api test:integration" .github/workflows/ci.yml && grep -q "Integration tests (testcontainers" .github/workflows/ci.yml && node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))" 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>

  <done>
    `.github/workflows/ci.yml` 에 `pnpm --filter @grabit/api test:integration` step 이 `pnpm test` 직후에 추가됨. YAML 유효. 기존 step 및 env 는 건드리지 않음. SC-2 "CI 편입" 이 physical 하게 충족 (REVIEWS.md HIGH#1). PR green 은 Plan 04 HUMAN-UAT pre-condition 에서 재확인.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → testcontainers Valkey cluster (임시 포트) | CI runner 내부에서만 도달 가능 |
| test client (IORedis.Cluster) ↔ single-shard cluster node | 실 프로덕션 Memorystore(shard-count=1) 와 동형 토폴로지 |
| GitHub Actions runner → Docker daemon (/var/run/docker.sock) | testcontainers 통신 경로 — `ubuntu-latest` 기본 포함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Denial of Service (test flakiness) | CLUSTER ADDSLOTSRANGE 미완료 상태에서 EVAL 실행 | mitigate | `cluster_state:ok` polling (250ms × 20 = 5s max) + `beforeAll` timeout 180s (RESEARCH.md Pitfall #4). |
| T-14-07 | Tampering (negative guard 누락) | scenario 1 이 빠지면 과거 스킴으로도 green — 회귀 탐지 불능 | mitigate | acceptance_criteria 에 `rejects.toThrow(/CROSSSLOT/)` grep gate. D-10 LOCKED. |
| T-14-08 | Information Disclosure (container 외부 노출) | testcontainers 바인딩 | accept | CI runner 내부 ephemeral 포트 — 외부 도달 불가. 타 integration spec 과 동형. |
| T-14-13 | DoS (natMap mismatch flakiness) | container internal IP 가 natMap 에 없으면 `CLUSTERDOWN` (REVIEWS.md MEDIUM#4) | mitigate | `buildNatMap` 이 CLUSTER SLOTS 결과로 동적 구성 + 0-entry 시 진단 throw. `host:6379` fallback 도 함께 매핑. |
| T-14-14 | Tampering (CI bypass) | `test:integration` 이 CI 에서 실행되지 않으면 SC-2 가 공허 | mitigate | Task 2 가 ci.yml 에 step 삽입. acceptance 로 grep + YAML syntax 체크. HUMAN-UAT 가 PR green 을 재확인 (REVIEWS.md HIGH#1/MEDIUM#5). |

Total risk delta: **+0 HIGH**, **+0 MEDIUM**, **+5 LOW (mitigated/accepted)**.

이 plan 은 테스트 인프라 추가 + CI 워크플로우 수정뿐 — 프로덕션 코드 미변경 → attack surface 변화 0.
</threat_model>

<verification>
- `pnpm --filter @grabit/api typecheck` green
- `pnpm --filter @grabit/api test:integration sms-cluster-crossslot -- --run` 10 subtest 모두 pass (scenario 2 의 4 + scenario 5 의 3 + 나머지 3)
- Docker 부재 환경에선 skip 가능하도록 testcontainers 가 자연스럽게 error → CI 는 Docker 가 이미 사용 중이므로 무관
- **[HIGH#1] CI workflow 검증:** `grep -q "@grabit/api test:integration" .github/workflows/ci.yml` → success
- **[MEDIUM#5] CI workflow green:** 이 Plan 이 포함된 PR 이 main 브랜치에 open 됐을 때 GitHub Actions `check` job 이 녹색 finalize 됨을 Plan 04 checkpoint 에서 확인
- **[MEDIUM#4] Dynamic natMap:** `grep -q "CLUSTER', 'SLOTS'" apps/api/test/sms-cluster-crossslot.integration.spec.ts` → success
</verification>

<success_criteria>
**SC-2 완전 달성 (physical):** cluster-mode 에서
- 과거 스킴 EVAL → CROSSSLOT (negative guard)
- 신규 스킴 EVAL → 4 분기 정상
- 3 개 키 slot 동일
- **CI 워크플로우가 실제로 test:integration 을 실행 + PR 에서 green finalize (REVIEWS.md HIGH#1 / MEDIUM#5)**

**Measurable:**
- [ ] 신규 파일 존재 + min 140 lines
- [ ] Task 1 acceptance 15개 모두 pass (dynamic natMap 포함)
- [ ] 10 개 it 시나리오 전부 녹색
- [ ] Task 2: ci.yml 에 integration step 추가 + YAML 유효
- [ ] Plan 04 HUMAN-UAT 에서 PR green 재확인
</success_criteria>

<output>
Plan 완료 후 `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-03-SUMMARY.md` 작성:
- 신규 파일 전체 경로 + 라인 수
- 5 시나리오 각각의 `it` 이름 + 실행 시간
- `buildNatMap` 동적 로직 요약 (REVIEWS.md MEDIUM#4 해소 증빙)
- `.github/workflows/ci.yml` diff 요약 (추가된 step 의 YAML 블록 + line 번호)
- CI 통합 상태: PR green 확인 여부 (REVIEWS.md HIGH#1 / MEDIUM#5 해소)
- testcontainers 초기화 시간 (prime 테스트 beforeAll 실 측정)
- Plan 04 로의 연결 메모 (HUMAN-UAT pre-condition 에 ci.yml green 항목 포함 확인)
</output>
</plan>
</content>
</invoke>
