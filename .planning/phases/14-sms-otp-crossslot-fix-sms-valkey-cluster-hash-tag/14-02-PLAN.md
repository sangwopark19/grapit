---
phase: 14
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/api/test/sms-throttle.integration.spec.ts
autonomous: true
requirements: [SC-3]
tags: [sms, valkey, integration-test, refactor, drift-removal]
must_haves:
  truths:
    - "sms-throttle.integration.spec.ts 가 Plan 01 에서 export 된 VERIFY_AND_INCREMENT_LUA / smsOtpKey / smsAttemptsKey / smsVerifiedKey 를 import 한다 (D-13 SoT)"
    - "파일 내부의 Lua 본체 문자열 복제본(L272-316 구간의 const VERIFY_AND_INCREMENT_LUA = ...)이 완전히 제거됨"
    - "3개 OTP 키 리터럴(`sms:otp:${...}` 등)이 파일 전체에서 0 occurrence — 모두 빌더 호출로 교체"
    - "rate-limit 키 리터럴(`sms:phone:send:${...}`, `sms:phone:verify:${...}`)는 **그대로 유지** (D-04)"
    - "기존 테스트 시나리오 4개 (정답/오답/만료/소진) 의 assertion 이 녹색으로 유지"
  artifacts:
    - path: "apps/api/test/sms-throttle.integration.spec.ts"
      provides: "drift-free integration test importing single source of truth"
      contains: "import {"
      contains_also:
        - "VERIFY_AND_INCREMENT_LUA"
        - "smsOtpKey"
        - "smsAttemptsKey"
        - "smsVerifiedKey"
        - "from '../src/modules/sms/sms.service"
  key_links:
    - from: "sms-throttle.integration.spec.ts"
      to: "apps/api/src/modules/sms/sms.service.ts (exports)"
      via: "import statement"
      pattern: "from ['\"]\\.\\./src/modules/sms/sms.service"
---

<review_traceability>
Addresses REVIEWS.md concerns: **NONE directly** (이 plan 은 codex 리뷰에서 별도 concern 이 발견되지 않았음).

**간접 연동 (downstream impact):**
- **HIGH#2 / MEDIUM#3 (Plan 01 Task 2 에서 해결)** — `sms.service.spec.ts` 와 `redis.provider.spec.ts` 의 stale literal 은 Plan 01 Task 2 에서 처리. 이 plan 은 `apps/api/test/sms-throttle.integration.spec.ts` 만 담당하며, 이 파일은 이미 builder import + rate-limit 불변 가드로 drift-free 설계. 추가 변경 없음.
- **MEDIUM#3 최종 gate** — `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` → 0 목표는 Plan 01 Task 2 완료 후 자연스럽게 달성 (이 파일은 `apps/api/src` 하위가 아니라 `apps/api/test` 이므로 애초에 gate 대상 외).
- **SC-3 전체 green** 은 Plan 01 + Plan 02 + Plan 04 의 세 spec 경로 수정이 모두 완료된 시점에 Plan 04 의 phase-wide verification 에서 최종 확인.

따라서 이 plan 의 본 body 는 revision 없이 유지. (`<review_traceability>` 블록만 신규 추가)
</review_traceability>

<objective>
Plan 01 에서 export 된 `VERIFY_AND_INCREMENT_LUA` + 3개 키 빌더를 `apps/api/test/sms-throttle.integration.spec.ts` 가 import 하도록 리팩터. 파일 내부에 복제돼 있던 Lua 본체 문자열 (L272-316 근방) 과 `sms:otp:/sms:attempts:/sms:verified:` 리터럴을 모두 제거해, Phase 10.1 때 드러난 **테스트-소스 drift 구조 (standalone 테스트는 녹색, 프로덕션 cluster 는 CROSSSLOT)** 를 원천 차단한다.

Purpose: SC-3 (API 전체 테스트 녹색). Phase 10.1 의 standalone Lua smoke test 는 hash-tag 없는 키를 써도 standalone Valkey 에서 녹색이었기 때문에 cluster CROSSSLOT 을 잡아낼 수 없었다. 이 plan 은 그 파일을 **소스-진실과 물리적으로 묶는다** — 앞으로 sms.service.ts 가 키 스킴을 바꾸면 이 테스트는 자동으로 동기화된다.

Output:
- `apps/api/test/sms-throttle.integration.spec.ts` — import 추가, 중복 Lua const 제거, `keys(phone)` 헬퍼 + 모든 `redis.get/set/ttl/del(...)` 호출이 새 빌더 사용.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-RESEARCH.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-01-PLAN.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md

<!-- File being modified -->
@apps/api/test/sms-throttle.integration.spec.ts

<!-- Source of imports (produced by Plan 01) -->
@apps/api/src/modules/sms/sms.service.ts

<interfaces>
<!-- Plan 01 produces these. This plan CONSUMES them. -->

From apps/api/src/modules/sms/sms.service.ts (produced by Plan 01):
```typescript
export const smsOtpKey: (e164: string) => string;     // => `{sms:${e164}}:otp`
export const smsAttemptsKey: (e164: string) => string; // => `{sms:${e164}}:attempts`
export const smsVerifiedKey: (e164: string) => string; // => `{sms:${e164}}:verified`
export const VERIFY_AND_INCREMENT_LUA: string;         // identical Lua body as Phase 10.1
```

Import path in tests (relative, ESM `.js` suffix per Node16 module resolution):
```typescript
import {
  VERIFY_AND_INCREMENT_LUA,
  smsOtpKey,
  smsAttemptsKey,
  smsVerifiedKey,
} from '../src/modules/sms/sms.service.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace duplicated Lua body + key literal helper with imports from sms.service.ts</name>
  <files>apps/api/test/sms-throttle.integration.spec.ts</files>

  <read_first>
    1. Read `apps/api/test/sms-throttle.integration.spec.ts` 전체 (429 lines). 특히:
       - L1-30 (imports)
       - L272-316 (`const VERIFY_AND_INCREMENT_LUA = \`...\``) — 복제된 Lua 본체
       - L312-316 (`const keys = (phone: string) => [...]`) — 복제된 키 리터럴 헬퍼
       - L318-331 (testcontainer lifecycle — 이 부분은 건드리지 않음)
       - L337-427 (4개 시나리오 `it(...)` — 각 시나리오의 `redis.get/set/ttl` 호출에 하드코드된 키 리터럴이 있음)
    2. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md` § 3a + § 3b (정확한 치환 예시).
    3. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md` § D-04 / D-13 / D-15 (rate-limit 키 불변 가드).
  </read_first>

  <behavior>
    - After edit: `grep -c "^const VERIFY_AND_INCREMENT_LUA" apps/api/test/sms-throttle.integration.spec.ts` → **0** (복제 제거)
    - After edit: `grep -c "^\\s*VERIFY_AND_INCREMENT_LUA,$" apps/api/test/sms-throttle.integration.spec.ts` ≥ 1 (import 라인)
    - After edit: `grep -cE "sms:otp:\\\$\\{|sms:attempts:\\\$\\{|sms:verified:\\\$\\{" apps/api/test/sms-throttle.integration.spec.ts` → **0** (OTP 키 리터럴 전부 제거)
    - After edit: `grep -cE "sms:phone:send:\\\$\\{|sms:phone:verify:\\\$\\{" apps/api/test/sms-throttle.integration.spec.ts` 은 **변경 전과 동일** (rate-limit 리터럴 불변, D-04)
    - 기존 테스트 4개 시나리오 모두 녹색
  </behavior>

  <action>
    1. **Add imports** (L10-11 근방, 기존 IORedis import 바로 아래):
       ```typescript
       import {
         VERIFY_AND_INCREMENT_LUA,
         smsOtpKey,
         smsAttemptsKey,
         smsVerifiedKey,
       } from '../src/modules/sms/sms.service.js';
       ```
       `.js` 접미사 필수 (ESM + Node 모듈 해상도) — 다른 `apps/api/test/*.integration.spec.ts` 파일들의 import 규약 확인.

    2. **Remove duplicated Lua body** (기존 L272-316 구간) — `const VERIFY_AND_INCREMENT_LUA = \`...\`` 블록 **전체를 삭제**. 블록 시작부("// Must match sms.service.ts ..." 주석이나 const 선언) 부터 closing backtick + 세미콜론까지 완전 제거.

    3. **Replace `keys(phone)` helper** (기존 L312-316) — 리터럴 배열을 빌더 호출로 교체:
       ```typescript
       // BEFORE
       const keys = (phone: string) => [
         `sms:otp:${phone}`,
         `sms:attempts:${phone}`,
         `sms:verified:${phone}`,
       ];
       // AFTER
       const keys = (phone: string) => [
         smsOtpKey(phone),
         smsAttemptsKey(phone),
         smsVerifiedKey(phone),
       ];
       ```

    4. **Replace per-scenario inline key literals** — L337-426 각 `it(...)` 안에서 호출되는 `redis.get/set/ttl/del(\`sms:otp:${phone}\`)` / `` `sms:attempts:${phone}` `` / `` `sms:verified:${phone}` `` 을 모두 빌더 호출로 교체. 예시:
       ```typescript
       // BEFORE
       await redis.set(`sms:otp:${phone}`, '123456', 'PX', 180_000);
       expect(await redis.get(`sms:otp:${phone}`)).toBeNull();
       expect(await redis.ttl(`sms:attempts:${phone}`)).toBeLessThanOrEqual(900);
       // AFTER
       await redis.set(smsOtpKey(phone), '123456', 'PX', 180_000);
       expect(await redis.get(smsOtpKey(phone))).toBeNull();
       expect(await redis.ttl(smsAttemptsKey(phone))).toBeLessThanOrEqual(900);
       ```
       PATTERNS.md § 3b 에서 추정한 ~12 occurrences. 실제 개수는 grep 으로 확인: `grep -n "sms:otp:\\|sms:attempts:\\|sms:verified:" apps/api/test/sms-throttle.integration.spec.ts`.

    5. **DO NOT TOUCH** — rate-limit 테스트 블록(L56-270 근방의 `@nestjs/throttler` 관련 `sms:phone:send:${e164}` / `sms:phone:verify:${e164}` / `sms:cooldown:` / `sms:resend:` 리터럴). D-04 잠금 — 스킴 유지.

    6. **Preserve `VERIFY_AND_INCREMENT_LUA` assertion code** — 복제 Lua const 만 지우고 `cluster.eval(VERIFY_AND_INCREMENT_LUA, 3, ...keys(phone), ...)` 호출부는 그대로 둔다 (import 된 심볼이 동일 이름이므로 자동 교체).

    7. Run: `pnpm --filter @grabit/api test:integration sms-throttle -- --run` — 4개 시나리오 모두 pass.
  </action>

  <acceptance_criteria>
    - [ ] `grep -c "^const VERIFY_AND_INCREMENT_LUA" apps/api/test/sms-throttle.integration.spec.ts` → **0**
    - [ ] `grep -nE "^import \\{$|VERIFY_AND_INCREMENT_LUA,$" apps/api/test/sms-throttle.integration.spec.ts` — import 블록 발견 (VERIFY_AND_INCREMENT_LUA 심볼 포함)
    - [ ] `grep -c "from '../src/modules/sms/sms.service" apps/api/test/sms-throttle.integration.spec.ts` ≥ 1
    - [ ] `grep -cE 'sms:otp:\\\$\\{|sms:attempts:\\\$\\{|sms:verified:\\\$\\{' apps/api/test/sms-throttle.integration.spec.ts` → **0**
    - [ ] `grep -cE 'sms:phone:send:\\\$\\{|sms:phone:verify:\\\$\\{' apps/api/test/sms-throttle.integration.spec.ts` — 변경 전 값과 동일 (rate-limit 보존 가드, 사전에 `git diff` 로 개수 고정)
    - [ ] `grep -c "smsOtpKey(phone)\\|smsAttemptsKey(phone)\\|smsVerifiedKey(phone)" apps/api/test/sms-throttle.integration.spec.ts` ≥ 3
    - [ ] `pnpm --filter @grabit/api test:integration sms-throttle -- --run` exits 0 (Docker 필요)
    - [ ] `pnpm --filter @grabit/api typecheck` exits 0
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @grabit/api typecheck && pnpm --filter @grabit/api test:integration sms-throttle -- --run && ! grep -qE "sms:otp:\\\$\\{|sms:attempts:\\\$\\{|sms:verified:\\\$\\{" apps/api/test/sms-throttle.integration.spec.ts && ! grep -q "^const VERIFY_AND_INCREMENT_LUA" apps/api/test/sms-throttle.integration.spec.ts</automated>
  </verify>

  <done>
    sms-throttle.integration.spec.ts 가 Plan 01 export 4종을 import 해서 사용. 복제 Lua 본체와 OTP 키 리터럴 모두 제거. rate-limit 키 리터럴은 한 글자도 변경 없음. 기존 4개 시나리오 integration test 녹색. typecheck green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → testcontainers Valkey | 프로덕션 과 무관, CI runner 내부 임시 포트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering (test-source drift re-emergence) | sms-throttle.integration.spec.ts 가 다시 리터럴을 하드코드 | mitigate | Task 1 acceptance: 키 리터럴 0 개 grep gate 로 구조적 차단. Plan 03 는 동일 import 를 cluster-mode 테스트에도 적용. |
| T-14-05 | Information Disclosure (test env 외부 노출) | testcontainers 가 임시 포트 바인딩 | accept | testcontainers 는 CI runner 내부 ephemeral 포트, 외부 도달 불가. 타 phase 동일 패턴(admin-dashboard/sms-throttle 기존) 과 동형. |

Total risk delta: **+0 HIGH**, **+0 MEDIUM**, **+2 LOW (accepted/mitigated)**.

신규 attack surface 0 — 테스트 파일 리팩터만 (프로덕션 코드 미변경).
</threat_model>

<verification>
- `pnpm --filter @grabit/api typecheck` green
- `pnpm --filter @grabit/api test:integration sms-throttle -- --run` green (Docker required)
- Pre-edit baseline: `grep -cE "sms:phone:send:|sms:phone:verify:" apps/api/test/sms-throttle.integration.spec.ts` 값 기록 → post-edit 값과 동일 확인 (rate-limit literal 불변 증빙)
- `git diff apps/api/test/sms-throttle.integration.spec.ts` — 변경 범위가 L1-30 (import), L272-316 삭제, L312-316 헬퍼, 4개 시나리오 내 key 호출로 국한됨을 시각 확인
</verification>

<success_criteria>
SC-3 부분 달성: 기존 통합 테스트가 소스 진실과 구조적으로 묶여 drift 가 재발하지 않는 상태. 전체 API suite 녹색 (fully-green gate 는 Plan 03 까지 완료된 뒤 Plan 04 와 함께 최종 확인).

**Measurable:**
- [ ] 4개 import 심볼 사용 확인
- [ ] 복제 Lua const 0 + OTP 키 리터럴 0
- [ ] rate-limit 키 리터럴 개수 불변
- [ ] integration test 4개 시나리오 pass
</success_criteria>

<output>
Plan 완료 후 `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-02-SUMMARY.md` 작성:
- 삭제된 라인 범위 (복제 Lua + 키 리터럴 헬퍼 치환)
- 교체된 key 호출 개수 (grep 기반 정확한 숫자)
- 기존 rate-limit 블록이 건드려지지 않았음을 `git diff --stat` 로 증명
- integration test 실행 로그 (4/4 pass)
- REVIEWS.md 연동 메모: 이 plan 은 concerns 에 직접 해당 없음, 상위 Plan 01 Task 2 가 spec 파일 계열 HIGH#2 를 해소
</output>
</plan>
</content>
</invoke>
