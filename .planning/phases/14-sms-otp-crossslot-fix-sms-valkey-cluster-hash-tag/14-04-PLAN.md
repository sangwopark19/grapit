---
phase: 14
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/auth/phone-verification.tsx
  - apps/web/components/auth/__tests__/phone-verification.test.tsx
  - .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md
autonomous: false
requirements: [SC-1, SC-4]
tags: [sms, frontend, ux, uat, checkpoint]
user_setup:
  - service: "Cloud Run (grabit-api, grabit-web)"
    why: "SC-1 — 프로덕션 heygrabit.com 실기기 회원가입 SMS 인증 성공 검증 (배포 후 실 SMS 왕복)"
    env_vars: []
    dashboard_config:
      - task: "grabit-api 배포 (main 브랜치 push → GitHub Actions deploy → Cloud Run 새 revision 활성)"
        location: "GitHub Actions deploy.yml + Cloud Run console (grabit-api)"
      - task: "배포 후 실기기(휴대폰) 로 heygrabit.com/signup 3단계 회원가입 SMS 인증 성공 확인"
        location: "https://heygrabit.com/signup + 실제 휴대폰"
      - task: "Sentry grabit-api 에서 **100% traffic 전환 + 15분 drain** 이후부터 72시간 CROSSSLOT 0 건 확인 (overlap 구간 제외, REVIEWS.md LOW#7)"
        location: "Sentry 콘솔 → grabit-api 프로젝트 → Issues 검색 (D-17)"
must_haves:
  truths:
    - "phone-verification.tsx 의 handleVerifyCode 가 res.verified === false && res.message 가 truthy 이면 서버 메시지를 setVerifyError 에 사용 (D-07)"
    - "res.message 가 undefined 또는 빈 문자열이면 하드코드 '인증번호가 일치하지 않습니다' 로 fallback (D-08)"
    - "apiClient.post generic 을 `<{ verified: boolean; message?: string }>` 로 변경 — 서버 VerifyResult optional message 와 일치 (REVIEWS.md LOW#6)"
    - "기존 catch 블록 (ApiClientError 410/422 → setTimeLeft(0)) 은 변경 없음 (D-09)"
    - "phone-verification.test.tsx 에 describe('서버 message 우선 (D-07)', ...) 블록이 추가되어 3 시나리오를 커버 (SC-4a/4b/4c)"
    - "프로덕션 heygrabit.com/signup 에서 실기기 SMS 인증 성공 (SC-1) — HUMAN-UAT 체크리스트 완료"
    - "Sentry 72h zero-count window 는 **새 API revision 100% traffic + 15분 drain 경과 이후** 부터 카운트 시작 (REVIEWS.md LOW#7) — HUMAN-UAT 에 명시"
  artifacts:
    - path: "apps/web/components/auth/phone-verification.tsx"
      provides: "server-message-priority branch in handleVerifyCode + optional message generic"
      contains: "typeof res.message === 'string'"
      contains_also:
        - "message?: string"
    - path: "apps/web/components/auth/__tests__/phone-verification.test.tsx"
      provides: "3 unit tests for D-07 behavior (message priority / fallback / verified regression)"
      contains: "서버 message 우선 (D-07)"
    - path: ".planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md"
      provides: "HUMAN-UAT checklist for SC-1 + D-17 72h observation + Sentry overlap 명시 + ci.yml test:integration PR green"
      min_lines: 40
  key_links:
    - from: "handleVerifyCode (res.verified === false branch)"
      to: "setVerifyError(serverMessage ?? fallback)"
      via: "typeof-string length-check guard (D-08)"
      pattern: "typeof res\\.message === 'string'"
    - from: "phone-verification.test.tsx describe('서버 message 우선 (D-07)')"
      to: "phone-verification.tsx handleVerifyCode"
      via: "apiClient.post mock chain + userEvent type/click"
      pattern: "mockResolvedValueOnce.*verified: false"
    - from: "Plan 04 generic type"
      to: "apps/api/src/modules/sms/sms.service.ts VerifyResult (optional message)"
      via: "contract sync (REVIEWS.md LOW#6)"
      pattern: "message\\?: string"
---

<review_traceability>
Addresses REVIEWS.md concerns:
- **LOW#6** — Task 2 에서 `apiClient.post<{ verified: boolean; message: string }>` 를 `<{ verified: boolean; message?: string }>` 로 변경. 서버 `VerifyResult` (apps/api/src/modules/sms/sms.service.ts:82 `interface VerifyResult { verified: boolean; message?: string }`) 와 타입 계약 일치.
- **LOW#7** — Task 3 의 14-HUMAN-UAT.md D-17 섹션에 "100% traffic + 15분 drain 경과 이후부터 72h zero-count 카운트 시작. 그 이전 overlap 이벤트는 분리 카운트" 명시.
- **MEDIUM#5 (연동)** — Task 3 의 14-HUMAN-UAT.md Pre-conditions 에 ".github/workflows/ci.yml green (test:integration 포함)" 체크박스 추가 — Plan 03 의 SC-2 "CI 편입" 최종 증빙을 여기서 닫음.
</review_traceability>

<objective>
프론트엔드 `phone-verification.tsx` 의 `handleVerifyCode` 에 server-message-priority 분기를 추가하고 response generic 타입을 `message?: string` (optional) 로 서버 계약과 정렬, `__tests__/phone-verification.test.tsx` 에 3 시나리오(SC-4a/4b/4c) 를 append 한다. 마지막으로 Phase 14 HUMAN-UAT 체크리스트를 작성해 프로덕션 배포 후 실기기 회원가입 SMS 인증(SC-1) + D-17 Sentry 72h 관측 항목 + REVIEWS.md LOW#7 (overlap 분리) 및 MEDIUM#5 (ci.yml green) 을 명시한다.

Purpose:
- **SC-4 완전 달성** — 사용자 경험상 "틀린 OTP" 와 "시스템 에러" 가 구분되도록 서버가 내려주는 message(예: CROSSSLOT 시 "인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.") 를 노출.
- **REVIEWS.md LOW#6 해소** — 프론트 generic 이 서버 `VerifyResult` (optional message) 와 계약 일치.
- **SC-1 트래킹** — 이 phase 의 최종 deliverable 은 프로덕션 실기기 SMS 인증 성공이며, 그 검증은 코드 배포 후 **수동 (checkpoint)** 이어야 한다 (실 Infobip 발송 + 실제 휴대폰 수신).
- **REVIEWS.md LOW#7 해소** — Sentry 72h zero-count 창이 배포 overlap 에 false-negative 되지 않도록 기준 재정의.
- **REVIEWS.md MEDIUM#5 재확인** — Plan 03 에서 추가한 ci.yml `test:integration` step 이 PR 에서 실제 green 인지 HUMAN-UAT pre-condition 에 명시.

Output:
- `apps/web/components/auth/phone-verification.tsx` — L135 generic 변경 + L139-144 의 5 라인 치환
- `apps/web/components/auth/__tests__/phone-verification.test.tsx` — `describe('서버 message 우선 (D-07)', ...)` append (3 it)
- `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` — SC-1 + D-17 (overlap 명시) + MEDIUM#5 ci green 체크리스트
- Checkpoint: 사용자가 프로덕션 배포 후 실기기 인증 성공 확인 → "approved"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-RESEARCH.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-VALIDATION.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md

<!-- Files being modified -->
@apps/web/components/auth/phone-verification.tsx
@apps/web/components/auth/__tests__/phone-verification.test.tsx

<!-- Server-side VerifyResult contract (REVIEWS.md LOW#6 source of truth) -->
@apps/api/src/modules/sms/sms.service.ts

<interfaces>
<!-- Existing response contract (Plan 04 aligns frontend with server-side optional message) -->
```typescript
// Server side — apps/api/src/modules/sms/sms.service.ts L82:
export interface VerifyResult { verified: boolean; message?: string }

// Existing frontend — apps/web/components/auth/phone-verification.tsx L135 (BEFORE):
const res = await apiClient.post<{ verified: boolean; message: string }>(
  '/api/v1/sms/verify-code',
  { phone, code },
);

// AFTER (REVIEWS.md LOW#6):
const res = await apiClient.post<{ verified: boolean; message?: string }>(
  '/api/v1/sms/verify-code',
  { phone, code },
);
```

<!-- Server message source (reference; do not change API) -->
```typescript
// apps/api/src/modules/sms/sms.service.ts verifyCode — produces:
// - { verified: true }
// - { verified: false, message: '인증번호가 일치하지 않습니다' }
// - { verified: false, message: '인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.' }  ← 시스템 에러 (D-14)
```

<!-- Existing test pattern in same file (analog for new tests) -->
```typescript
// apps/web/components/auth/__tests__/phone-verification.test.tsx L161-187 (기존 400 에러 시)
(apiClient.post as ReturnType<typeof vi.fn>)
  .mockResolvedValueOnce({ success: true })         // send
  .mockResolvedValueOnce({                           // verify
    verified: false,
    message: '인증번호가 일치하지 않습니다',
  });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append 3 unit tests for D-07 behavior (RED — fails against current code)</name>
  <files>apps/web/components/auth/__tests__/phone-verification.test.tsx</files>

  <read_first>
    1. Read `apps/web/components/auth/__tests__/phone-verification.test.tsx` 전체 — 현재 파일은 이미 존재 (VALIDATION.md 가 "신규" 라고 표기했지만 실제로는 MODIFY — PATTERNS.md §5 확인). 특히:
       - L1-42 (imports + mocking + defaultProps + beforeEach/afterEach)
       - L161-187 (400 에러 시 "일치하지 않습니다" 테스트 — 새 D-07 테스트의 정확한 analog)
       - role="alert" 사용하는 기존 테스트 (있으면 selector 재활용)
    2. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md` § 5a/5b/5c (3 시나리오 body 완전한 예시).
    3. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md` D-07/D-08/D-09 (서버 message 우선 / 빈 문자열 방어 / catch 분기 유지).
  </read_first>

  <behavior>
    - `describe('서버 message 우선 (D-07)', ...)` 블록을 파일 하단 (기존 `describe` 블록들과 동일 레벨, 최상위 `describe('PhoneVerification', ...)` 내부) 에 append
    - SC-4a: verified=false + message='인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.' → alert 에 서버 문구 표시
    - SC-4b-1: verified=false + message=undefined → alert 에 '인증번호가 일치하지 않습니다' fallback
    - SC-4b-2: verified=false + message='' (빈 문자열) → alert 에 '인증번호가 일치하지 않습니다' fallback (D-08 empty-string 방어)
    - SC-4c: verified=true → `onVerified('123456')` 호출됨 + alert 미표시 (회귀 가드)
    - 시나리오 실행 전에는 **RED** 상태 (handleVerifyCode 가 서버 메시지를 우선시하지 않아 4a 실패) — Task 2 후 GREEN
  </behavior>

  <action>
    1. 파일 끝에 아래 describe 블록을 append. 기존 `describe('PhoneVerification', () => { ... })` 의 closing `});` 바로 앞에 삽입 (즉, 최상위 describe 내부에 추가):

    ```typescript
      // ---------- 서버 message 우선 (D-07) ----------
      describe('서버 message 우선 (D-07)', () => {
        it('SC-4a: verified=false + message 존재 → 서버 메시지 표시', async () => {
          const { apiClient } = await import('@/lib/api-client');
          (apiClient.post as ReturnType<typeof vi.fn>)
            .mockResolvedValueOnce({ success: true })
            .mockResolvedValueOnce({
              verified: false,
              message: '인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.',
            });

          render(<PhoneVerification {...defaultProps} />);
          const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
          await user.click(screen.getByRole('button', { name: /인증번호 발송/ }));

          await waitFor(() => {
            expect(screen.getByPlaceholderText(/인증번호 6자리/)).toBeInTheDocument();
          });

          await user.type(screen.getByPlaceholderText(/인증번호 6자리/), '123456');
          await user.click(screen.getByRole('button', { name: /확인/ }));

          await waitFor(() => {
            expect(screen.getByRole('alert')).toHaveTextContent(
              '인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.',
            );
          });
        });

        it('SC-4b-1: verified=false + message undefined → 하드코드 fallback', async () => {
          const { apiClient } = await import('@/lib/api-client');
          (apiClient.post as ReturnType<typeof vi.fn>)
            .mockResolvedValueOnce({ success: true })
            .mockResolvedValueOnce({ verified: false }); // message undefined

          render(<PhoneVerification {...defaultProps} />);
          const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
          await user.click(screen.getByRole('button', { name: /인증번호 발송/ }));

          await waitFor(() => {
            expect(screen.getByPlaceholderText(/인증번호 6자리/)).toBeInTheDocument();
          });

          await user.type(screen.getByPlaceholderText(/인증번호 6자리/), '999999');
          await user.click(screen.getByRole('button', { name: /확인/ }));

          await waitFor(() => {
            expect(screen.getByRole('alert')).toHaveTextContent('인증번호가 일치하지 않습니다');
          });
        });

        it('SC-4b-2: verified=false + message 빈 문자열 → 하드코드 fallback (D-08)', async () => {
          const { apiClient } = await import('@/lib/api-client');
          (apiClient.post as ReturnType<typeof vi.fn>)
            .mockResolvedValueOnce({ success: true })
            .mockResolvedValueOnce({ verified: false, message: '' });

          render(<PhoneVerification {...defaultProps} />);
          const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
          await user.click(screen.getByRole('button', { name: /인증번호 발송/ }));

          await waitFor(() => {
            expect(screen.getByPlaceholderText(/인증번호 6자리/)).toBeInTheDocument();
          });

          await user.type(screen.getByPlaceholderText(/인증번호 6자리/), '999999');
          await user.click(screen.getByRole('button', { name: /확인/ }));

          await waitFor(() => {
            expect(screen.getByRole('alert')).toHaveTextContent('인증번호가 일치하지 않습니다');
          });
        });

        it('SC-4c: verified=true → onVerified 호출 + alert 미표시 (회귀 가드)', async () => {
          const { apiClient } = await import('@/lib/api-client');
          const onVerified = vi.fn();
          (apiClient.post as ReturnType<typeof vi.fn>)
            .mockResolvedValueOnce({ success: true })
            .mockResolvedValueOnce({ verified: true });

          render(<PhoneVerification {...defaultProps} onVerified={onVerified} />);
          const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
          await user.click(screen.getByRole('button', { name: /인증번호 발송/ }));

          await waitFor(() => {
            expect(screen.getByPlaceholderText(/인증번호 6자리/)).toBeInTheDocument();
          });

          await user.type(screen.getByPlaceholderText(/인증번호 6자리/), '123456');
          await user.click(screen.getByRole('button', { name: /확인/ }));

          await waitFor(() => {
            expect(onVerified).toHaveBeenCalledWith('123456');
          });
          // 성공 분기에서는 에러 alert 가 없어야 함 (D-07 regression guard)
          expect(screen.queryByRole('alert')).toBeNull();
        });
      });
    ```

    2. `typeof res.message === 'string' && res.message.length > 0` 상태를 **아직 코드가 충족하지 않으므로** SC-4a 테스트는 실패해야 한다 — 이것이 TDD RED 단계. Task 2 후 GREEN.

    3. Run: `pnpm --filter @grabit/web test phone-verification -- --run` — SC-4a FAIL, 나머지 3개는 이미 기존 동작과 일치하므로 PASS.
  </action>

  <acceptance_criteria>
    - [ ] `grep -c "서버 message 우선 (D-07)" apps/web/components/auth/__tests__/phone-verification.test.tsx` = 1
    - [ ] `grep -c "SC-4a\\|SC-4b-1\\|SC-4b-2\\|SC-4c" apps/web/components/auth/__tests__/phone-verification.test.tsx` = 4
    - [ ] `grep -c "인증번호 확인에 실패했습니다" apps/web/components/auth/__tests__/phone-verification.test.tsx` ≥ 1
    - [ ] `pnpm --filter @grabit/web test phone-verification -- --run` — SC-4a가 **FAIL** (RED), 다른 기존 테스트 PASS, SC-4b/4c PASS (RED 상태의 증명)
    - [ ] `pnpm --filter @grabit/web typecheck` exits 0
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @grabit/web typecheck && grep -q "서버 message 우선 (D-07)" apps/web/components/auth/__tests__/phone-verification.test.tsx && grep -q "SC-4a" apps/web/components/auth/__tests__/phone-verification.test.tsx && grep -q "SC-4b-2" apps/web/components/auth/__tests__/phone-verification.test.tsx && grep -qE "message: *[\x27\x22][\x27\x22]|빈 문자열" apps/web/components/auth/__tests__/phone-verification.test.tsx</automated>
  </verify>

  <done>
    테스트 파일에 D-07 describe 블록(4개 it) 추가됨. RED 단계 — SC-4a 가 실패, 나머지는 기존 동작과 일치해 PASS. typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply server-message-priority branch + optional message generic in handleVerifyCode (GREEN + REVIEWS.md LOW#6)</name>
  <files>apps/web/components/auth/phone-verification.tsx</files>

  <read_first>
    1. Read `apps/web/components/auth/phone-verification.tsx` 전체 — 특히 L129-158 (`handleVerifyCode`), L135 의 `apiClient.post<{ verified: boolean; message: string }>` generic.
    2. Read `apps/api/src/modules/sms/sms.service.ts` L82 (`export interface VerifyResult { verified: boolean; message?: string }`) — 서버 계약 (REVIEWS.md LOW#6 source of truth).
    3. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-PATTERNS.md` § 2a (정확한 before/after patch).
    4. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md` D-07/D-08/D-09 (server-message 우선 + 빈 문자열 방어 + catch 분기 유지).
    5. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md` § Concerns LOW#6 (generic optional 변경 근거 — 서버 VerifyResult 와 계약 일치).
  </read_first>

  <behavior>
    - `handleVerifyCode` 의 `apiClient.post` generic 을 `<{ verified: boolean; message?: string }>` (optional `?`) 로 변경 — 서버 `VerifyResult` 와 일치 (REVIEWS.md LOW#6).
    - `res.verified === false` 분기에서 서버 `message` 우선 사용.
    - `typeof res.message === 'string' && res.message.length > 0` 조건 (D-08 빈 문자열 방어).
    - catch 블록(L145-157) 은 건드리지 않음 (D-09).
    - `res.verified === true` 분기(L139-141) 그대로 유지 — `clearTimer()` + `onVerified(code)` 순서.
    - 실행 후: Task 1 의 4개 D-07 테스트 모두 GREEN.
  </behavior>

  <action>
    1. **[LOW#6] Generic 타입 변경** — `apps/web/components/auth/phone-verification.tsx` L135:
    ```typescript
    // BEFORE
    const res = await apiClient.post<{ verified: boolean; message: string }>(
      '/api/v1/sms/verify-code',
      { phone, code },
    );
    // AFTER (REVIEWS.md LOW#6 — 서버 VerifyResult 의 optional message 와 일치)
    const res = await apiClient.post<{ verified: boolean; message?: string }>(
      '/api/v1/sms/verify-code',
      { phone, code },
    );
    ```

    2. **L139-144 의 분기 치환** — 현재:
    ```typescript
    if (res.verified) {
      clearTimer();
      onVerified(code);
    } else {
      setVerifyError('인증번호가 일치하지 않습니다');
    }
    ```
    를 다음으로 교체:
    ```typescript
    if (res.verified) {
      clearTimer();
      onVerified(code);
    } else {
      // [D-07] 서버가 system error context 를 message 로 내려준 경우 (예: Valkey EVAL
      // 장애 → '인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.') 하드코드 대신
      // 서버 문구를 우선 표시해 "틀린 OTP" vs "시스템 에러" 를 사용자 관점에서 구분.
      // [D-08] 빈 문자열 방어: res.message 가 undefined 이거나 길이 0 이면 fallback.
      // [REVIEWS.md LOW#6] generic 이 message?: string 이므로 typeof-string 가드가
      // 타입 narrowing 도 겸한다 (undefined 제거 + length 확인).
      const fallback = '인증번호가 일치하지 않습니다';
      const serverMessage =
        typeof res.message === 'string' && res.message.length > 0
          ? res.message
          : null;
      setVerifyError(serverMessage ?? fallback);
    }
    ```

    3. **DO NOT TOUCH** (D-09):
       - L145-157 catch 블록 (`mapErrorToCopy` + 410/422 → `setTimeLeft(0)`)
       - `handleSendCode` (L111-127)
       - Other render logic

    4. Run: `pnpm --filter @grabit/web test phone-verification -- --run` — Task 1 의 4개 D-07 테스트 모두 pass. 기존 테스트 전체 green.
    5. Run: `pnpm --filter @grabit/web typecheck` — 0 에러 (optional generic + typeof 가드 조합 OK).
    6. Run: `pnpm --filter @grabit/web lint` — 신규 warning 없음.
  </action>

  <acceptance_criteria>
    - [ ] **[LOW#6]** `rg "message\\?:" apps/web/components/auth/phone-verification.tsx` — ≥1 matches (optional 타입 명시 존재)
    - [ ] **[LOW#6]** `grep -c "apiClient.post<{ verified: boolean; message: string }>" apps/web/components/auth/phone-verification.tsx` → **0** (required message 제거 확인)
    - [ ] **[LOW#6]** `grep -c "apiClient.post<{ verified: boolean; message?: string }>" apps/web/components/auth/phone-verification.tsx` → **1** (optional 로 교체 확인)
    - [ ] `grep -n "typeof res.message === 'string'" apps/web/components/auth/phone-verification.tsx` — 1 match
    - [ ] `grep -n "setVerifyError(serverMessage ?? fallback)" apps/web/components/auth/phone-verification.tsx` — 1 match
    - [ ] `grep -cE "setVerifyError\\('인증번호가 일치하지 않습니다'\\)" apps/web/components/auth/phone-verification.tsx` → **0** (하드코드 호출부가 제거되고 const 로 이동)
    - [ ] `grep -c "const fallback = '인증번호가 일치하지 않습니다'" apps/web/components/auth/phone-verification.tsx` ≥ 1
    - [ ] `grep -n "clearTimer()" apps/web/components/auth/phone-verification.tsx` — 존재 (L140 verified 분기 유지 증명)
    - [ ] `pnpm --filter @grabit/web test phone-verification -- --run` exits 0 (SC-4a/4b-1/4b-2/4c 모두 GREEN + 기존 테스트 regression 없음)
    - [ ] `pnpm --filter @grabit/web typecheck` exits 0
    - [ ] `pnpm --filter @grabit/web lint` — 신규 warning 0
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @grabit/web typecheck && pnpm --filter @grabit/web lint && pnpm --filter @grabit/web test phone-verification -- --run && grep -q "typeof res.message === 'string'" apps/web/components/auth/phone-verification.tsx && grep -q "apiClient.post<{ verified: boolean; message?: string }>" apps/web/components/auth/phone-verification.tsx && ! grep -q "apiClient.post<{ verified: boolean; message: string }>" apps/web/components/auth/phone-verification.tsx</automated>
  </verify>

  <done>
    handleVerifyCode 의 generic 이 `message?: string` optional 로 변경되어 서버 VerifyResult 와 계약 일치 (REVIEWS.md LOW#6). 서버 message 를 우선 사용하고 빈 문자열/undefined 에서만 fallback. D-07 테스트 4개 모두 GREEN. typecheck/lint/기존 테스트 regression 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 14 HUMAN-UAT checklist for SC-1 + D-17 (overlap 명시 LOW#7) + MEDIUM#5 ci.yml green</name>
  <files>.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md</files>

  <read_first>
    1. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-CONTEXT.md` D-17 (Sentry 72h 관측), D-19 (15분 drain 창), D-20 SC-1, D-21 (Phase 13 UAT gap 10 scenario 재현).
    2. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-VALIDATION.md` § Manual-Only Verifications (3 항목 — 실기기 + Sentry 72h + 배포 overlap 5분).
    3. `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-REVIEWS.md` § Concerns LOW#7 (72h window 를 100% traffic + drain 이후로 한정) + MEDIUM#5 (ci.yml test:integration PR green 확인).
    4. `.planning/phases/13-grapit-grabit-rename/13-UAT.md` 가 있으면 Phase 13 HUMAN-UAT 체크리스트 형식 참고.
  </read_first>

  <action>
    새 파일 `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` 를 아래 내용으로 작성:

    ```markdown
    # Phase 14 HUMAN-UAT — SMS OTP CROSSSLOT fix

    **Created:** 2026-04-24
    **Goal:** 배포 후 프로덕션 `heygrabit.com` 에서 실기기로 회원가입 SMS OTP 인증이 정상 성공하는지 검증하고, D-17 72시간 Sentry 관측 창을 닫는다.

    **Pre-conditions (코드 측 필요 완료 상태):**
    - [ ] Plan 01 merged (sms.service.ts hash-tag 적용 + 4 export + 두 spec 파일 stale key 갱신 — REVIEWS.md HIGH#2)
    - [ ] Plan 02 merged (sms-throttle.integration.spec.ts drift 제거)
    - [ ] Plan 03 merged (sms-cluster-crossslot.integration.spec.ts 5 시나리오 green + dynamic natMap — REVIEWS.md MEDIUM#4)
    - [ ] Plan 03 merged (`.github/workflows/ci.yml` 에 `pnpm --filter @grabit/api test:integration` step 추가 — REVIEWS.md HIGH#1)
    - [ ] Plan 04 Task 1-2 merged (phone-verification.tsx + test 녹색, generic `message?: string` optional — REVIEWS.md LOW#6)
    - [ ] `pnpm --filter @grabit/api test && pnpm --filter @grabit/api test:integration && pnpm --filter @grabit/web test` 전체 green
    - [ ] **[MEDIUM#5] GitHub Actions CI 녹색:** 이 phase 의 PR (main 대상) 에서 `check` job 이 green finalize. 특히 `Integration tests (testcontainers — SC-2 Valkey Cluster CROSSSLOT guard)` step 이 success.
    - [ ] GitHub Actions deploy.yml → Cloud Run `grabit-api` 새 revision 이 ACTIVE
    - [ ] GitHub Actions deploy.yml → Cloud Run `grabit-web` 새 revision 이 ACTIVE

    ---

    ## SC-1: 프로덕션 실기기 회원가입 SMS 인증 성공 (LOCKED D-20)

    **Scenario reproduces Phase 13 UAT gap 10 (D-21):** 이전에 이 시나리오는 `CROSSSLOT` 로 인해 "틀린 인증번호" 메시지로 masking 됐다. 이번 배포 이후 정상 성공해야 한다.

    **Steps:**
    1. 실제 휴대폰(iOS 또는 Android 실기기) 으로 `https://heygrabit.com/signup` 접속
    2. 회원가입 3단계 (전화번호 인증) 에서 **실제 수신 가능한 번호** 입력 → "인증번호 발송" 클릭
    3. SMS 수신 확인 — 문구: `[Grabit] 인증번호 NNNNNN (3분 이내 입력)`
    4. 수신한 6자리 코드 입력 → "확인" 클릭
    5. **Expected:** 4단계 (비밀번호 설정) 로 진행됨. 에러 메시지 없음.
    6. 만약 실패 시:
       - 에러 문구가 "인증번호가 일치하지 않습니다" 인가? → **REGRESSION** (D-07 fallback 이 잘못 발동) → 롤백 준비
       - 에러 문구가 "인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요." 인가? → 시스템 에러 여전 → Sentry 확인 후 롤백

    **체크리스트:**
    - [ ] SMS 수신 시간 < 30s
    - [ ] 코드 입력 후 성공 응답 시간 < 2s
    - [ ] 에러 alert 미표시
    - [ ] 회원가입 완료까지 진행

    ---

    ## D-17: Sentry 72h CROSSSLOT 0-occurrence (배포 overlap 분리 — REVIEWS.md LOW#7)

    **Purpose:** hash-tag 수정이 100% 적용돼 어떤 e164 포맷으로도 CROSSSLOT 이 재발하지 않음을 확증.

    **[REVIEWS.md LOW#7] Window 정의 재조정:**
    - Sentry 72h zero-count window 는 **새 API revision 이 100% traffic 을 받고 + 최소 15분 drain 경과 이후 시점부터 카운트 시작** 한다.
    - 그 전(= 0% → 100% traffic 전환 진행 중 + drain 15분 이내)에 발생한 CROSSSLOT 이벤트는 **overlap 이벤트로 분류**하고 72h window 계산에서 제외한다.
    - Overlap 이벤트는 별도 라벨로 기록하되, D-17 pass/fail 판정에는 반영하지 않는다. 이는 배포 cutover 중 기존 revision 의 in-flight 요청이 구 코드 경로로 남는 정상 현상을 false-negative 없이 처리하기 위함.

    **Steps:**
    1. 배포 시작 시각 기록: ________________ (ISO8601)
    2. **100% traffic 도달 시각** 기록 (Cloud Run 콘솔 → Revisions → Traffic allocation 100% 확인): ________________
    3. **15분 drain 종료 시각** 기록 (100% traffic 시각 + 15분): ________________ ← 이 시점부터 72h window 시작
    4. Sentry → `grabit-api` 프로젝트 → Issues 검색: `event.type:error message:"CROSSSLOT"`
    5. 72h window 동안의 누적 카운트 (위 Step 3 시각 이후):
       - [ ] 0 건 (PASS)
       - [ ] 1+ 건 (FAIL → 즉시 알림 + 재조사)
    6. Overlap 이벤트 카운트 (배포 시작 ~ Step 3 사이): ________________ (참고용, pass/fail 무관)
    7. 72h window 종료 시각: ________________
    8. Sentry 쿼리 최종 스크린샷을 이 파일에 첨부(옵션) 또는 SUMMARY 에 포함

    ---

    ## D-19: 배포 overlap 5분 UX 관측

    **Purpose:** 키 스킴이 한 번에 전환되는 cutover 에서 기존 in-flight OTP 를 가진 사용자가 새 revision 에 도달해도 service level impact 가 최소임을 확인.

    **Steps:**
    1. Cloud Run 콘솔 → `grabit-api` → Logs → 배포 시각 ± 5분 윈도 필터
    2. `sms.sent` 카운트 vs `sms.verify_failed` (주의: 이 카운트는 자연 발생하는 오타 실패도 포함) 비율 관측
    3. **Acceptance:** `sms.verify_failed` 카운트가 배포 직전 1시간 평균보다 **<2×** 상승에 그친다 (15분 drain 창 내 자연 과도 상태)
    4. **Red flag:** `sms.verify_failed` 가 5× 이상 급증 → 기존 OTP 를 가진 사용자들이 집단적으로 실패 → 마이그레이션 스크립트 없이 즉시 전환한 D-19 가정에 대한 압력. 대응: CLI 로 `redis-cli --scan --pattern "sms:otp:*"` 실행해 잔존 키 개수 확인, 2-3분 더 관측 후 자연 drain 확인.

    ---

    ## Sign-off

    - [ ] SC-1 체크리스트 PASS
    - [ ] D-17 72h 관측 0 CROSSSLOT (**100% traffic + 15분 drain 이후 기준**, overlap 제외 — REVIEWS.md LOW#7)
    - [ ] D-19 5분 overlap 관측 정상
    - [ ] **[MEDIUM#5] ci.yml `test:integration` step 이 이 phase PR 에서 green finalize 확인**
    - [ ] 검증자: ____________________
    - [ ] 완료 날짜: ____________________
    - [ ] `.planning/STATE.md` 의 Phase 14 상태를 "shipped (code+prod UAT)" 로 업데이트

    ---

    **Rollback 기준:**
    - SC-1 실패 (틀린 인증번호 메시지 노출 + SMS 인증 재현 가능) → 즉시 이전 revision 으로 롤백 (`gcloud run services update-traffic grabit-api --to-revisions=<previous>=100`)
    - D-17 중 (100% traffic + 15분 drain 경과 후) CROSSSLOT 1건 이상 발생 → 원인 조사 후 롤백 여부 결정
    ```
  </action>

  <acceptance_criteria>
    - [ ] `test -f .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md`
    - [ ] `wc -l .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 60
    - [ ] `grep -c "SC-1\\|D-17\\|D-19\\|D-21" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 4
    - [ ] `grep -c "heygrabit.com/signup" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1
    - [ ] `grep -c "CROSSSLOT" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1
    - [ ] `grep -c "Rollback" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1
    - [ ] **[LOW#7]** `rg "100% traffic|drain|overlap" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` — **≥3 matches** (세 키워드 모두 등장)
    - [ ] **[LOW#7]** `grep -c "15분 drain" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1
    - [ ] **[MEDIUM#5]** `grep -c "@grabit/api test:integration" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1
    - [ ] **[MEDIUM#5]** `grep -c "ci.yml" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md` ≥ 1
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "SC-1" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "D-17" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "heygrabit.com/signup" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "100% traffic" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "15분 drain" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "overlap" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md && grep -q "@grabit/api test:integration" .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md</automated>
  </verify>

  <done>
    14-HUMAN-UAT.md 파일 생성 완료 — SC-1 / D-17 (overlap 명시, LOW#7) / D-19 3 섹션 + Rollback 기준 + MEDIUM#5 ci.yml test:integration pre-condition 포함.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] 프로덕션 배포 후 SC-1 실기기 회원가입 SMS 인증 성공 확인 + ci.yml green 확인 (MEDIUM#5)</name>
  <files>.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-HUMAN-UAT.md</files>
  <action>
    **User-action checkpoint.** Claude 는 이 단계에서 implementation 을 수행하지 않는다. 대신 `<how-to-verify>` 절차를 사용자가 실기기에서 실행한 뒤 `<resume-signal>` 에 따라 재개. 14-HUMAN-UAT.md 체크리스트를 갱신해 증거를 남긴다.
  </action>
  <verify>
    <automated>MISSING — manual checkpoint (SC-1 실기기 회원가입 SMS 인증 성공). Automated 대체 없음. 증거는 14-HUMAN-UAT.md SC-1 체크박스 + 사용자 resume-signal ('approved' / 'fail: ...' / 'deferred-d17').</automated>
  </verify>
  <done>
    사용자가 프로덕션 실기기로 회원가입 3단계 SMS 인증 성공을 확인하고 "approved" 로 resume. 14-HUMAN-UAT.md SC-1 섹션 + MEDIUM#5 ci.yml green 체크박스 기입 완료. 실패 시 ("fail: ...") 롤백 경로 발동.
  </done>

  <what-built>
    - sms.service.ts 의 3개 OTP 키가 `{sms:${e164}}:<role>` hash-tag 스킴으로 전환됨 (Plan 01)
    - sms.service.spec.ts 와 redis.provider.spec.ts 의 stale literal 기대값이 새 hash-tag 으로 교체됨 (Plan 01 Task 2 — REVIEWS.md HIGH#2)
    - 기존 integration test (sms-throttle) 가 single-source-of-truth 로 정리됨 (Plan 02)
    - cluster-mode 회귀 가드 (sms-cluster-crossslot.integration.spec.ts) 가 동적 natMap 과 함께 추가됨 (Plan 03 — REVIEWS.md MEDIUM#4)
    - `.github/workflows/ci.yml` 에 `pnpm --filter @grabit/api test:integration` step 이 추가되어 PR 에서 자동 실행됨 (Plan 03 — REVIEWS.md HIGH#1)
    - phone-verification.tsx generic 이 `message?: string` optional 로 변경됨 + 서버 `message` 우선 + 빈 문자열 fallback 패치 (Plan 04 Task 1-2 — REVIEWS.md LOW#6)
    - 14-HUMAN-UAT.md 체크리스트가 Phase 14 폴더에 작성됨 (overlap 명시 LOW#7 + ci.yml MEDIUM#5 포함)
  </what-built>

  <how-to-verify>
    **사전 조건 — 이 checkpoint 에 도달하기 전 필수:**
    1. Plan 01/02/03/04 Task 1-3 모두 merge 되어 main 에 반영
    2. `pnpm --filter @grabit/api test && pnpm --filter @grabit/api test:integration && pnpm --filter @grabit/web test` 전체 green
    3. **[MEDIUM#5]** 이 phase 의 PR 에서 GitHub Actions `check` job 이 green, 특히 `Integration tests (testcontainers — SC-2 Valkey Cluster CROSSSLOT guard)` step 이 success 로 finalize — PR URL 기록.
    4. GitHub Actions deploy.yml 이 성공적으로 완료되어 Cloud Run `grabit-api` 와 `grabit-web` 모두 새 revision ACTIVE
    5. 100% traffic 도달 시각 기록 (LOW#7 72h window start 계산용)

    **검증 절차 (14-HUMAN-UAT.md 와 정확히 동일):**
    1. `https://heygrabit.com/signup` 접속 (실기기)
    2. 3단계 (전화번호 인증) 에서 실제 수신 가능한 번호 입력
    3. "인증번호 발송" 클릭 → SMS 수신 확인 (`[Grabit] 인증번호 NNNNNN (3분 이내 입력)`)
    4. 수신한 코드 입력 → "확인" 클릭
    5. **Expected:** 4단계 비밀번호 설정으로 진행. 에러 alert 없음.
    6. 14-HUMAN-UAT.md 의 SC-1 / D-19 / (시간 허락 시 D-17) / MEDIUM#5 섹션 체크박스 채우기

    **Failure 케이스:**
    - "인증번호가 일치하지 않습니다" 표시 → D-07 회귀 가능 → 즉시 롤백
    - "인증번호 확인에 실패했습니다..." 표시 → 시스템 에러 재발 → Cloud Run 로그 + Sentry 확인
    - SMS 자체 미수신 → Infobip 연동 문제 (이번 phase 범위 외, 별도 조사)
    - ci.yml `test:integration` step 이 red/skip → Plan 03 Task 2 의 YAML 구성 재검토 → MEDIUM#5 미충족
  </how-to-verify>

  <resume-signal>
    - "approved" — SC-1 pass 확인 (실기기 회원가입 SMS 성공) + MEDIUM#5 ci.yml green + 14-HUMAN-UAT.md SC-1 체크박스 기록
    - "fail: <reason>" — 재조사 + 롤백 검토
    - "deferred-d17" — SC-1 성공했으나 D-17 72h 관측은 별도 track (이 경우 phase 를 "shipped (code+SC-1)" 로 마킹하고 D-17 은 별도 action item). MEDIUM#5 는 deferred 불가 — PR merge 전 confirm 필수.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client ↔ `/api/v1/sms/verify-code` | OTP + phone 제출 |
| `apiClient` response `{ verified, message? }` → UI `setVerifyError` | 서버 message (optional) 가 UI 로 노출됨 — 이번 plan 의 확장 영역 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Information Disclosure | 서버 `message` 가 UI 에 노출되면서 내부 구현 힌트 유출 | accept | 서버 쪽은 이미 사용자 친화적 generic 문구만 내려줌 — "인증번호가 일치하지 않습니다" / "인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요." / "인증번호가 만료되었습니다. 재발송해주세요". 스택 트레이스, 키 이름, slot 번호, Valkey wire-format 모두 노출 없음. RESEARCH.md §2 공격 모델 검토 완료. |
| T-14-10 | Tampering (D-08 bypass — 빈 문자열 스푸핑) | 공격자가 proxy 로 `{verified:false, message:''}` 를 주입 | mitigate | D-08 `typeof res.message === 'string' && res.message.length > 0` 가드 + SC-4b-2 테스트 직접 커버 (빈 문자열 → fallback 적용). Generic optional (LOW#6) 변경 후에도 동일 가드 유효. |
| T-14-11 | DoS (UAT rollback window 누락) | SC-1 실패 시 즉시 롤백 경로 없음 | mitigate | 14-HUMAN-UAT.md § Rollback 기준 + gcloud 명령어 예시 포함. |
| T-14-15 | Tampering (72h window false-negative) | 배포 overlap 중의 CROSSSLOT 을 D-17 window 에 포함시키면 실제 잘못된 roll-forward 결정을 유도 | mitigate | REVIEWS.md LOW#7 대응 — 14-HUMAN-UAT.md 에 "100% traffic + 15분 drain 이후" window 시작 기준 명시 + overlap 이벤트 별도 카운트. |

Total risk delta: **+0 HIGH**, **+0 MEDIUM**, **+4 LOW (accepted/mitigated)**.

이번 plan 의 서버 계약 확장 0 건 — 기존 `VerifyResult { verified, message? }` 를 프론트가 사용하는 방식만 추가 + 프론트 generic 을 서버 타입과 정렬 (LOW#6).
</threat_model>

<verification>
- Task 1: 테스트 파일 append + RED 확인
- Task 2: generic optional 변경 + 5-line patch + GREEN 확인 (`pnpm --filter @grabit/web test phone-verification -- --run`) — LOW#6 grep gate 포함
- Task 3: HUMAN-UAT 파일 생성 (LOW#7 overlap + MEDIUM#5 ci.yml green 포함)
- Task 4: Checkpoint — 사용자가 배포 후 실기기로 SC-1 확인 + MEDIUM#5 ci.yml green 확인
- **Phase-wide verification (모든 plan 완료 후, Task 4 checkpoint 직전):**
  - `pnpm --filter @grabit/api typecheck && pnpm --filter @grabit/api lint && pnpm --filter @grabit/api test && pnpm --filter @grabit/api test:integration` 전체 green (Docker 필요)
  - `pnpm --filter @grabit/web typecheck && pnpm --filter @grabit/web lint && pnpm --filter @grabit/web test` 전체 green
  - `rg "sms:otp:|sms:attempts:|sms:verified:" apps/api/src` → empty (SP-1 post-edit gate — spec 파일 포함 모두 0, MEDIUM#3 옵션 B 달성)
  - `rg "sms:phone:send:|sms:phone:verify:|sms:resend:|sms:cooldown:" apps/api/src` → 변경 전과 동일 occurrence (D-04 불변 가드)
  - `grep -q "@grabit/api test:integration" .github/workflows/ci.yml` → success (HIGH#1)
</verification>

<success_criteria>
**SC-1 + SC-4 동시 완료:**
- [ ] SC-4a/4b-1/4b-2/4c 4개 테스트 all green (Task 2)
- [ ] **[LOW#6]** `message?: string` optional generic 적용 + `typeof res.message === 'string'` guard + fallback 분기 존재 (Task 2)
- [ ] **[LOW#7]** 14-HUMAN-UAT.md D-17 섹션이 "100% traffic + 15분 drain 이후" window 기준 및 overlap 분리 규칙 명시 (Task 3)
- [ ] **[MEDIUM#5]** 14-HUMAN-UAT.md pre-conditions 에 ci.yml `test:integration` PR green 항목 포함 (Task 3)
- [ ] 14-HUMAN-UAT.md 작성 (Task 3)
- [ ] 프로덕션 실기기 SC-1 pass — 사용자 "approved" (Task 4 checkpoint)
- [ ] Sentry 72h D-17 관측 (100% traffic + drain 이후 기준 — checkpoint 후 별도 track 가능, resume-signal "deferred-d17" 허용)
</success_criteria>

<output>
Plan 완료 후 `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-04-SUMMARY.md` 작성:
- Task 1/2/3 각각의 변경 라인 범위 + 실행 결과
- **[LOW#6]** generic 변경 diff (`message: string` → `message?: string`) + grep gate pass 증빙
- 4 unit tests (SC-4a/4b-1/4b-2/4c) all green 증거
- 14-HUMAN-UAT.md 요약 + SC-1 checkpoint 실행 결과 기록 (user approved / fail / deferred-d17 중 하나)
- **[LOW#7]** D-17 72h window 기준 (100% traffic + 15분 drain 이후) 반영 증빙
- **[MEDIUM#5]** ci.yml `test:integration` PR green 확인 결과 (PR URL + Actions run URL)
- Phase 14 전체 wrap-up: 4 plans (01 backend refactor + spec drift, 02 integration drift, 03 cluster guard + CI, 04 frontend UX + UAT) 의 최종 체크리스트
- D-17 72h 관측 상태 (진행 중/완료/deferred)
- ROADMAP.md Phase 14 섹션 업데이트 권고 ("Goal" 와 "Requirements: SC-1..SC-4" + Plans 목록)
- REVIEWS.md 7 concerns 모두 해소 트레이스 (HIGH#1 → Plan 03 Task 2 ci.yml / HIGH#2 → Plan 01 Task 2 spec update / MEDIUM#3 → 옵션 B / MEDIUM#4 → dynamic natMap / MEDIUM#5 → UAT ci green / LOW#6 → optional generic / LOW#7 → overlap 명시)
</output>
</plan>
</content>
</invoke>
