---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - turbo.json
  - package.json
  - tsconfig.base.json
  - .env.example
  - .gitignore
  - .nvmrc
  - docker-compose.yml
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/fonts.ts
  - apps/web/app/globals.css
  - apps/web/public/fonts/PretendardVariable.woff2
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/tsconfig.build.json
  - apps/api/nest-cli.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/vitest.config.ts
  - apps/api/drizzle.config.ts
  - apps/api/src/database/drizzle.module.ts
  - apps/api/src/database/drizzle.provider.ts
  - apps/api/src/database/schema/index.ts
  - apps/api/src/database/schema/users.ts
  - apps/api/src/database/schema/social-accounts.ts
  - apps/api/src/database/schema/refresh-tokens.ts
  - apps/api/src/database/schema/terms-agreements.ts
  - apps/api/src/config/database.config.ts
  - apps/api/src/config/auth.config.ts
  - apps/api/src/common/pipes/zod-validation.pipe.ts
  - apps/api/src/common/decorators/public.decorator.ts
  - apps/api/src/common/decorators/current-user.decorator.ts
  - apps/api/src/common/filters/http-exception.filter.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/schemas/auth.schema.ts
  - packages/shared/src/schemas/user.schema.ts
  - packages/shared/src/types/auth.types.ts
  - packages/shared/src/types/user.types.ts
  - packages/shared/src/constants/index.ts
  - apps/web/vitest.config.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-06

must_haves:
  truths:
    - "pnpm turbo build completes without errors for all 3 workspaces"
    - "NestJS dev server starts on port 8080 and responds to GET /api/v1/health"
    - "Next.js dev server starts on port 3000 and renders a page"
    - "Docker PostgreSQL is accessible and Drizzle migrations create users/social_accounts/refresh_tokens/terms_agreements tables"
    - "Shared package exports zod schemas and TypeScript types importable from both apps"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace definition"
      contains: "apps/*"
    - path: "apps/api/src/database/schema/users.ts"
      provides: "Users table schema with all D-03 fields"
      contains: "pgTable('users'"
    - path: "packages/shared/src/schemas/auth.schema.ts"
      provides: "Shared zod validation schemas for auth forms"
      exports: ["loginSchema", "registerStep1Schema", "registerStep2Schema", "registerStep3Schema"]
    - path: "apps/api/src/database/drizzle.provider.ts"
      provides: "Drizzle ORM database provider for NestJS DI"
      contains: "DRIZZLE"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/database/drizzle.module.ts"
      via: "NestJS module import"
      pattern: "DrizzleModule"
    - from: "packages/shared/src/schemas/auth.schema.ts"
      to: "apps/api and apps/web"
      via: "workspace dependency import"
      pattern: "@grapit/shared"
---

<objective>
Scaffold the pnpm + Turborepo monorepo with Next.js 16 (apps/web), NestJS 11 (apps/api), and shared types package (packages/shared). Set up Docker PostgreSQL, Drizzle ORM schema with all auth-related tables, shared zod validation schemas, and NestJS common utilities (pipes, decorators, filters). This is the foundation every subsequent plan depends on.

Purpose: Establish the project structure, database schema, and shared contracts so that backend auth (Plan 02) and frontend UI (Plan 03) can be built in parallel.
Output: Working monorepo with dev servers, database with migration, shared type exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo structure with pnpm workspaces, Turborepo, and all package.json files</name>
  <files>
    pnpm-workspace.yaml
    turbo.json
    package.json
    tsconfig.base.json
    .env.example
    .gitignore
    .nvmrc
    docker-compose.yml
    apps/web/package.json
    apps/web/next.config.ts
    apps/web/tsconfig.json
    apps/web/app/layout.tsx
    apps/web/app/page.tsx
    apps/web/app/fonts.ts
    apps/web/app/globals.css
    apps/api/package.json
    apps/api/tsconfig.json
    apps/api/tsconfig.build.json
    apps/api/nest-cli.json
    apps/api/src/main.ts
    apps/api/src/app.module.ts
    apps/api/vitest.config.ts
    apps/web/vitest.config.ts
    packages/shared/package.json
    packages/shared/tsconfig.json
    packages/shared/src/index.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-RESEARCH.md
    .planning/phases/01-foundation-auth/01-UI-SPEC.md
    CLAUDE.md
  </read_first>
  <action>
    Create the monorepo from scratch. No existing source code exists.

    **Root files:**
    - `pnpm-workspace.yaml`: packages: ["apps/*", "packages/*"]
    - `.nvmrc`: `22` (pin Node 22 LTS per CLAUDE.md stack spec)
    - `package.json`: name "@grapit/root", private true, scripts: { "dev": "turbo dev", "build": "turbo build", "lint": "turbo lint", "typecheck": "turbo typecheck", "test": "turbo test" }, devDependencies: turbo 2.8.x, typescript 5.9.x (per CLAUDE.md, NOT 6.0)
    - `tsconfig.base.json`: target ES2022, module ESNext, moduleResolution bundler, strict true, esModuleInterop true, skipLibCheck true, forceConsistentCasingInFileNames true, resolveJsonModule true, isolatedModules true, declaration true, declarationMap true, composite false
    - `turbo.json`: tasks for build (dependsOn ^build, outputs .next/** dist/**), dev (persistent true, cache false), lint (dependsOn ^build), typecheck (dependsOn ^build), test (dependsOn ^build)
    - `.env.example`:
      ```
      # Database
      DATABASE_URL=postgresql://grapit:grapit_dev@localhost:5432/grapit
      # Auth
      JWT_SECRET=your-jwt-secret-here
      JWT_REFRESH_SECRET=your-jwt-refresh-secret-here
      # OAuth - Kakao
      KAKAO_CLIENT_ID=
      KAKAO_CLIENT_SECRET=
      KAKAO_CALLBACK_URL=http://localhost:8080/api/v1/auth/social/kakao/callback
      # OAuth - Naver
      NAVER_CLIENT_ID=
      NAVER_CLIENT_SECRET=
      NAVER_CALLBACK_URL=http://localhost:8080/api/v1/auth/social/naver/callback
      # OAuth - Google
      GOOGLE_CLIENT_ID=
      GOOGLE_CLIENT_SECRET=
      GOOGLE_CALLBACK_URL=http://localhost:8080/api/v1/auth/social/google/callback
      # Twilio (SMS)
      TWILIO_ACCOUNT_SID=
      TWILIO_AUTH_TOKEN=
      TWILIO_VERIFY_SERVICE_SID=
      # Email (nodemailer)
      SMTP_HOST=
      SMTP_PORT=587
      SMTP_USER=
      SMTP_PASS=
      SMTP_FROM=noreply@grapit.com
      # App
      FRONTEND_URL=http://localhost:3000
      API_URL=http://localhost:8080
      NODE_ENV=development
      ```
    - `.gitignore`: node_modules, dist, .next, .turbo, .env, .env.local, *.tsbuildinfo, .DS_Store, coverage, pgdata
    - `docker-compose.yml`: PostgreSQL 16-alpine on port 5432, POSTGRES_DB=grapit, POSTGRES_USER=grapit, POSTGRES_PASSWORD=grapit_dev, volume pgdata:/var/lib/postgresql/data

    **apps/web (Next.js 16):**
    - `package.json`: name "@grapit/web", dependencies: next 16.2.x, react (Next.js bundled), react-dom, @grapit/shared (workspace:*). devDependencies: typescript 5.9.x, @types/react, @types/node, tailwindcss 4.2.x, vitest 4.x, @testing-library/react 16.x
    - `next.config.ts`: output "standalone", transpilePackages: ["@grapit/shared"]
    - `tsconfig.json`: extends ../../tsconfig.base.json, compilerOptions: { jsx: "preserve", paths: { "@/*": ["./src/*"], "@grapit/shared": ["../../packages/shared/src"] } }, include app/**/*.ts app/**/*.tsx, exclude node_modules
    - `app/fonts.ts`: import localFont from 'next/font/local', export pretendard with src './fonts/PretendardVariable.woff2', display 'swap', weight '100 900', variable '--font-pretendard'
    - `app/globals.css`: Tailwind CSS v4 CSS-first config. Import tailwindcss. Define CSS custom properties for Grapit brand palette:
      --color-primary: #6C3CE0; --color-gray-900: #1A1A2E; --color-gray-100: #F5F5F7;
      --color-error-text: #C62828; --color-error-surface: #FEF2F2;
      --color-success-text: #15803D; --color-success-surface: #F0FDF4;
      --color-warning-text: #8B6306; --color-warning-surface: #FFFBEB;
      --color-info-text: #6C3CE0; --color-info-surface: #F3EFFF;
      (Per D-14, D-15, D-16 and UI-SPEC WCAG-compliant values)
    - `app/layout.tsx`: root layout with Pretendard font applied via className={pretendard.variable}, html lang="ko", metadata title "Grapit - 공연 티켓 예매"
    - `app/page.tsx`: minimal placeholder "Grapit" text (proper Home page built in Plan 03)
    - `vitest.config.ts`: Vitest config for React component testing with jsdom environment

    **apps/api (NestJS 11):**
    - `package.json`: name "@grapit/api", dependencies: @nestjs/core 11.x, @nestjs/common 11.x, @nestjs/platform-express 11.x, @nestjs/config 4.x, @nestjs/terminus 11.x, @nestjs/throttler 6.x, drizzle-orm 0.45.x, pg 8.x, helmet 8.x, cookie-parser 1.4.x, @grapit/shared (workspace:*). devDependencies: @nestjs/cli 11.x, @nestjs/testing 11.x, typescript 5.9.x, vitest 4.x, drizzle-kit 0.31.x, @types/cookie-parser, @types/express
    - `tsconfig.json`: extends ../../tsconfig.base.json, compilerOptions: { module: "commonjs" is WRONG -- use ESNext per CLAUDE.md. Actually NestJS 11 defaults to ESM now. Use moduleResolution: "bundler", module: "ESNext", emitDecoratorMetadata: true, experimentalDecorators: true }, paths: { "@grapit/shared": ["../../packages/shared/src"] }
    - `tsconfig.build.json`: extends ./tsconfig.json, exclude: ["node_modules", "test", "dist", "**/*.spec.ts"]
    - `nest-cli.json`: collection @nestjs/schematics, sourceRoot src, compilerOptions: { deleteOutDir: true, builder: "swc" }
    - `src/main.ts`: NestFactory.create(AppModule), app.enableCors({ origin: process.env.FRONTEND_URL || 'http://localhost:3000', credentials: true }), app.use(helmet()), app.use(cookieParser()), app.useGlobalFilters(new HttpExceptionFilter()), app.useGlobalPipes(new ZodValidationPipe()), await app.listen(process.env.PORT || 8080). Import cookie-parser and helmet.
    - `src/app.module.ts`: imports [ConfigModule.forRoot({ isGlobal: true }), DrizzleModule, ThrottlerModule.forRoot([{ ttl: 60000, limit: 60 }])], controllers: [HealthController], providers: [{ provide: APP_GUARD, useClass: ThrottlerGuard }]. Also include TerminusModule health check at /api/v1/health.
    - `vitest.config.ts`: Vitest config for NestJS with node environment, paths mapping
    - `drizzle.config.ts`: schema: "./src/database/schema/index.ts", out: "./src/database/migrations", dialect: "postgresql", dbCredentials: { url: process.env.DATABASE_URL! }

    **packages/shared:**
    - `package.json`: name "@grapit/shared", main: "./src/index.ts", types: "./src/index.ts", exports: { ".": "./src/index.ts" }, dependencies: zod 4.3.x
    - `tsconfig.json`: extends ../../tsconfig.base.json, include: ["src"]
    - `src/index.ts`: re-exports from ./schemas and ./types

    Run `pnpm install` after creating all package.json files. Verify install completes without errors.

    Download PretendardVariable.woff2 from the official Pretendard GitHub releases (or CDN: https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css -- extract the woff2 URL). Place in apps/web/app/fonts/ directory. If download fails, create a placeholder note and flag as user action needed.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm install && pnpm turbo build --dry-run 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml contains 'apps/*' and 'packages/*'
    - package.json contains "turbo" in devDependencies
    - .nvmrc contains "22"
    - tsconfig.base.json contains "strict": true
    - docker-compose.yml contains "postgres:16-alpine"
    - .env.example contains "DATABASE_URL=", "JWT_SECRET=", "KAKAO_CLIENT_ID=", "TWILIO_ACCOUNT_SID=", "SMTP_HOST="
    - apps/web/package.json contains "next" in dependencies and "@grapit/shared" in dependencies
    - apps/api/package.json contains "@nestjs/core" in dependencies and "drizzle-orm" in dependencies
    - packages/shared/package.json contains "zod" in dependencies
    - apps/web/app/fonts.ts contains "PretendardVariable.woff2" and "--font-pretendard"
    - apps/web/app/globals.css contains "--color-primary: #6C3CE0"
    - apps/web/app/layout.tsx contains 'lang="ko"' and "pretendard"
    - turbo.json contains "build" and "dev" tasks
    - pnpm install completes with exit code 0
  </acceptance_criteria>
  <done>Monorepo structure exists with 3 workspaces (web, api, shared). pnpm install succeeds. TypeScript compilation configured. Docker Compose ready for PostgreSQL. Vitest configs exist for both apps.</done>
</task>

<task type="auto">
  <name>Task 2: Create Drizzle database schema, NestJS common utilities, and shared validation schemas</name>
  <files>
    apps/api/src/database/drizzle.module.ts
    apps/api/src/database/drizzle.provider.ts
    apps/api/src/database/schema/index.ts
    apps/api/src/database/schema/users.ts
    apps/api/src/database/schema/social-accounts.ts
    apps/api/src/database/schema/refresh-tokens.ts
    apps/api/src/database/schema/terms-agreements.ts
    apps/api/src/config/database.config.ts
    apps/api/src/config/auth.config.ts
    apps/api/src/common/pipes/zod-validation.pipe.ts
    apps/api/src/common/decorators/public.decorator.ts
    apps/api/src/common/decorators/current-user.decorator.ts
    apps/api/src/common/filters/http-exception.filter.ts
    packages/shared/src/schemas/auth.schema.ts
    packages/shared/src/schemas/user.schema.ts
    packages/shared/src/types/auth.types.ts
    packages/shared/src/types/user.types.ts
    packages/shared/src/constants/index.ts
    packages/shared/src/index.ts
  </files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/package.json
    packages/shared/package.json
    packages/shared/src/index.ts
    .planning/phases/01-foundation-auth/01-RESEARCH.md
    .planning/phases/01-foundation-auth/01-CONTEXT.md
  </read_first>
  <action>
    **Drizzle schema (apps/api/src/database/schema/):**

    `users.ts` -- Per D-03 (mandatory additional info: name, gender, country, birthDate, phone):
    ```typescript
    import { pgTable, uuid, varchar, boolean, timestamp, pgEnum } from 'drizzle-orm/pg-core';
    export const genderEnum = pgEnum('gender', ['male', 'female', 'unspecified']);
    export const users = pgTable('users', {
      id: uuid('id').defaultRandom().primaryKey(),
      email: varchar('email', { length: 255 }).unique().notNull(),
      passwordHash: varchar('password_hash', { length: 255 }), // null for social-only
      name: varchar('name', { length: 100 }).notNull(),
      phone: varchar('phone', { length: 20 }).notNull(),
      gender: genderEnum('gender').notNull(),
      country: varchar('country', { length: 100 }).notNull().default('KR'),
      birthDate: varchar('birth_date', { length: 10 }).notNull(), // YYYY-MM-DD
      isPhoneVerified: boolean('is_phone_verified').notNull().default(false),
      isEmailVerified: boolean('is_email_verified').notNull().default(false),
      marketingConsent: boolean('marketing_consent').notNull().default(false),
      role: varchar('role', { length: 20 }).notNull().default('user'), // user | admin
      createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
      updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
    });
    ```

    `social-accounts.ts`:
    ```typescript
    export const socialAccounts = pgTable('social_accounts', {
      id: uuid('id').defaultRandom().primaryKey(),
      userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
      provider: varchar('provider', { length: 20 }).notNull(), // kakao | naver | google
      providerId: varchar('provider_id', { length: 255 }).notNull(),
      providerEmail: varchar('provider_email', { length: 255 }),
      createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
    });
    ```
    Add unique constraint on (provider, providerId) combination.

    `refresh-tokens.ts`:
    ```typescript
    export const refreshTokens = pgTable('refresh_tokens', {
      id: uuid('id').defaultRandom().primaryKey(),
      userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
      tokenHash: varchar('token_hash', { length: 255 }).notNull(),
      family: varchar('family', { length: 255 }).notNull(), // for rotation detection
      expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
      createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
      revokedAt: timestamp('revoked_at', { withTimezone: true }),
    });
    ```
    Note: `family` field enables refresh token rotation -- all tokens in a family share the same family ID. If a revoked token is reused, revoke the entire family (possible theft).

    `terms-agreements.ts` -- Per D-02 (3 terms: service required, privacy required, marketing optional):
    ```typescript
    export const termsAgreements = pgTable('terms_agreements', {
      id: uuid('id').defaultRandom().primaryKey(),
      userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
      termsOfService: boolean('terms_of_service').notNull(),
      privacyPolicy: boolean('privacy_policy').notNull(),
      marketingConsent: boolean('marketing_consent').notNull().default(false),
      agreedAt: timestamp('agreed_at', { withTimezone: true }).notNull().defaultNow(),
    });
    ```

    `index.ts`: Re-export all schemas and the genderEnum.

    **Drizzle NestJS provider:**

    `drizzle.provider.ts`:
    ```typescript
    import { Pool } from 'pg';
    import { drizzle, NodePgDatabase } from 'drizzle-orm/node-postgres';
    import { ConfigService } from '@nestjs/config';
    import * as schema from './schema';
    export const DRIZZLE = Symbol('DRIZZLE');
    export type DrizzleDB = NodePgDatabase<typeof schema>;
    export const drizzleProvider = {
      provide: DRIZZLE,
      inject: [ConfigService],
      useFactory: (config: ConfigService) => {
        const pool = new Pool({ connectionString: config.get<string>('DATABASE_URL') });
        return drizzle(pool, { schema });
      },
    };
    ```

    `drizzle.module.ts`: @Global() @Module with drizzleProvider, exports DRIZZLE.

    **Config files:**
    `database.config.ts`: registerAs('database') with url from DATABASE_URL env.
    `auth.config.ts`: registerAs('auth') with jwtSecret, jwtRefreshSecret, jwtExpiresIn: '15m', jwtRefreshExpiresIn: '7d', from env vars.

    **Common utilities:**
    `zod-validation.pipe.ts`: PipeTransform that accepts ZodSchema, calls safeParse, throws BadRequestException with flattened fieldErrors on failure.
    `public.decorator.ts`: SetMetadata('isPublic', true) decorator. Used to mark routes that skip JWT auth guard.
    `current-user.decorator.ts`: createParamDecorator that extracts user from request.user.
    `http-exception.filter.ts`: ExceptionFilter that catches HttpException and formats response as { statusCode, message, errors?, timestamp }.

    **Shared schemas (packages/shared/src/schemas/):**

    `auth.schema.ts`:
    - loginSchema: { email: z.string().email(), password: z.string().min(1) }
    - passwordSchema: z.string().min(8).regex(/[a-zA-Z]/).regex(/[0-9]/).regex(/[!@#$%^&*(),.?":{}|<>]/) with Korean error messages
    - registerStep1Schema: { email, password: passwordSchema, passwordConfirm } with refine for password match
    - registerStep2Schema: { termsOfService: z.literal(true), privacyPolicy: z.literal(true), marketingConsent: z.boolean() } -- per D-02
    - registerStep3Schema: { name, gender: z.enum(['male','female','unspecified']), country, birthYear, birthMonth, birthDay, phone, phoneVerificationCode: z.string().length(6) } -- per D-03
    - resetPasswordRequestSchema: { email: z.string().email() }
    - resetPasswordSchema: { token: z.string(), newPassword: passwordSchema, newPasswordConfirm } with refine
    - Export all inferred types (LoginInput, RegisterStep1Input, etc.)

    `user.schema.ts`:
    - updateProfileSchema: { name: z.string().min(1).max(50).optional(), phone: z.string().optional() }
    - Export UpdateProfileInput type

    **Shared types (packages/shared/src/types/):**

    `auth.types.ts`:
    ```typescript
    export interface AuthResponse { accessToken: string; user: UserProfile; }
    export interface TokenRefreshResponse { accessToken: string; }
    export interface SocialAuthResult { status: 'authenticated' | 'needs_registration'; accessToken?: string; registrationToken?: string; user?: UserProfile; socialProfile?: { provider: string; providerId: string; email?: string; name?: string; }; }
    ```

    `user.types.ts`:
    ```typescript
    export interface UserProfile { id: string; email: string; name: string; phone: string; gender: 'male' | 'female' | 'unspecified'; country: string; birthDate: string; isPhoneVerified: boolean; role: 'user' | 'admin'; createdAt: string; }
    ```

    **Shared constants:**
    `constants/index.ts`: AUTH_COOKIE_NAME = 'refreshToken', ACCESS_TOKEN_EXPIRY = '15m', REFRESH_TOKEN_EXPIRY_DAYS = 7, SMS_CODE_LENGTH = 6, SMS_CODE_EXPIRY_SECONDS = 180, PASSWORD_MIN_LENGTH = 8

    **packages/shared/src/index.ts**: Re-export everything from schemas/*, types/*, constants/*.

    After creating schema files, run `docker compose up -d` to start PostgreSQL, then run `pnpm --filter api drizzle-kit generate` to generate the initial migration, then `pnpm --filter api drizzle-kit migrate` to apply it.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && docker compose up -d && sleep 3 && pnpm --filter @grapit/api drizzle-kit generate 2>&1 | tail -5 && pnpm --filter @grapit/api drizzle-kit migrate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/database/schema/users.ts contains "pgTable('users'" and "genderEnum" and "passwordHash" and "phone" and "birthDate" and "country"
    - apps/api/src/database/schema/social-accounts.ts contains "pgTable('social_accounts'" and "provider" and "providerId"
    - apps/api/src/database/schema/refresh-tokens.ts contains "pgTable('refresh_tokens'" and "tokenHash" and "family" and "revokedAt"
    - apps/api/src/database/schema/terms-agreements.ts contains "pgTable('terms_agreements'" and "termsOfService" and "privacyPolicy" and "marketingConsent"
    - apps/api/src/database/drizzle.provider.ts contains "DRIZZLE" and "drizzle(pool" and "DrizzleDB"
    - apps/api/src/database/drizzle.module.ts contains "@Global()" and "DrizzleModule"
    - apps/api/src/common/pipes/zod-validation.pipe.ts contains "ZodValidationPipe" and "safeParse" and "BadRequestException"
    - apps/api/src/common/decorators/public.decorator.ts contains "isPublic" and "SetMetadata"
    - packages/shared/src/schemas/auth.schema.ts contains "loginSchema" and "registerStep1Schema" and "registerStep2Schema" and "registerStep3Schema" and "passwordSchema"
    - packages/shared/src/types/auth.types.ts contains "AuthResponse" and "SocialAuthResult" and "needs_registration"
    - packages/shared/src/types/user.types.ts contains "UserProfile" and "gender" and "birthDate"
    - packages/shared/src/constants/index.ts contains "AUTH_COOKIE_NAME" and "ACCESS_TOKEN_EXPIRY"
    - apps/api/src/database/migrations/ directory exists with at least one .sql file
    - docker compose ps shows postgres container running
  </acceptance_criteria>
  <done>All 4 database tables created via Drizzle migration. Shared zod schemas export validation for all 3 signup steps. NestJS common utilities (ZodValidationPipe, @Public, @CurrentUser, HttpExceptionFilter) ready. DrizzleModule globally provides database instance.</done>
</task>

</tasks>

<verification>
1. `pnpm install` exits 0
2. `docker compose up -d` starts PostgreSQL
3. Drizzle migration creates tables: `docker exec $(docker compose ps -q postgres) psql -U grapit -c "\dt"` shows users, social_accounts, refresh_tokens, terms_agreements
4. `pnpm turbo typecheck` passes for all workspaces
5. NestJS health check: start API and curl http://localhost:8080/api/v1/health returns 200
</verification>

<success_criteria>
- Working monorepo with 3 workspaces that builds without errors
- PostgreSQL running with 4 auth tables migrated
- Shared package exports all zod schemas and TypeScript types
- NestJS dev server starts with health endpoint responding
- Next.js dev server starts with Pretendard font configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
