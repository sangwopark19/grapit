---
phase: 11-admin-dashboard
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/11-admin-dashboard/11-VALIDATION.md
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06]
tags: [admin, dashboard, integration, e2e, verification]
must_haves:
  truths:
    - "Plan 01이 만든 unit spec 8/8 GREEN (백엔드 cache/DB 로직)"
    - "Plan 01이 만든 controller access-control spec 3/3 GREEN (401/403/200)"
    - "Plan 01이 만든 integration spec (Postgres+Valkey testcontainers) 2/2 GREEN"
    - "Plan 01이 만든 E2E spec 3/3 GREEN (Plan 02+03 완료 후 Wave 3 gate에서 실행 — review HIGH 2)"
    - "전체 monorepo `pnpm test` 0 failure"
    - "dev 환경에서 /admin 접속 시 차트 3개 모두 시각적 렌더 + 각 SVG 내 자식 노드(path/rect/circle) count > 0 (Playwright chart blank guard, review LOW 12)"
    - "사용자가 브라우저에서 7/30/90일 토글 전환 후 차트 업데이트 관찰"
    - "UI-SPEC Typography 스캔이 apps/web/app/admin/page.tsx + apps/web/components/admin/dashboard/**/*.tsx 모두에 대해 0 위반 (review MEDIUM 8)"
  artifacts: []
  key_links: []
---

<objective>
Wave 3 Gate: Plan 02 (API) + Plan 03 (Web) 완성 후 Plan 01이 만든 모든 자동 테스트(unit 8 + controller access-control 3 + integration 2 + E2E 3)를 실제 실행하여 GREEN 여부 확인 + chart blank Playwright smoke + UI-SPEC Typography scope-확장 스캔 + 수동 QA.

Purpose:
- 자동 테스트 파이프라인이 VALIDATION.md에 정의된 sampling rate 달성 여부 확증
- review HIGH 2: E2E GREEN 검증을 Wave 2(Plan 03)에서 Wave 3(Plan 04)으로 이동
- recharts 3.8.x + React 19.2 렌더 회귀(#6857, A5 medium-risk) 실측 + Playwright SVG child count assertion (review LOW 12)
- UI-SPEC grep scope 확장: `apps/web/components/admin/dashboard/**` 전체 스캔 (review MEDIUM 8)
- Admin 대시보드 5개 Success Criteria 검증 (ROADMAP)

Output:
- 자동 테스트 실행 결과 (unit/controller/integration/E2E) 기록
- Pitfall 1 workaround 필요 여부 결정 (react-is override)
- 수동 체크포인트 승인
- VALIDATION/ROADMAP/REQUIREMENTS 3개 문서 완료 상태 갱신 (실행일 기준)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-dashboard/11-CONTEXT.md
@.planning/phases/11-admin-dashboard/11-VALIDATION.md
@.planning/phases/11-admin-dashboard/11-UI-SPEC.md
@.planning/phases/11-admin-dashboard/11-01-SUMMARY.md
@.planning/phases/11-admin-dashboard/11-02-SUMMARY.md
@.planning/phases/11-admin-dashboard/11-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 04-01: 자동 테스트 풀 파이프라인 실행 (unit + controller access-control + integration + E2E) + UI-SPEC scope-확장 스캔 + Playwright chart blank guard</name>
  <read_first>
    - .planning/phases/11-admin-dashboard/11-VALIDATION.md (Per-Task Verification Map + Full suite command)
    - .planning/phases/11-admin-dashboard/11-01-SUMMARY.md (RED 테스트 목록)
    - .planning/phases/11-admin-dashboard/11-UI-SPEC.md §Typography (금지 클래스 목록)
    - apps/api/vitest.config.ts
    - apps/api/vitest.integration.config.ts
    - apps/web/playwright.config.ts (있다면)
    - apps/web/e2e/admin-dashboard.spec.ts (Plan 01 산출 — Wave 3 gate에서 실제 GREEN 전환)
  </read_first>
  <files>
    (실행 위주; react-is override 필요 시 apps/web/package.json 또는 root package.json 수정 가능성 있음 — 실측 후 결정. E2E 파일에 chart blank guard 테스트 추가 가능 — 아래 B항 참조)
  </files>
  <action>
    순서대로 실행:

    **1) 백엔드 unit 테스트** (Plan 02 Task 02-01에서 이미 실행했으나 최종 재확인)
    ```bash
    pnpm --filter @grapit/api exec vitest run admin-dashboard.service.spec.ts
    ```
    기대: `Tests: 8 passed (8)`

    **2) 백엔드 controller access-control 테스트 (review HIGH 3)**
    ```bash
    pnpm --filter @grapit/api exec vitest run admin-dashboard.controller.spec.ts
    ```
    기대: `Tests: 3 passed (3)` — 401 / 403 / 200 세 케이스.

    **3) 백엔드 integration 테스트**
    ```bash
    pnpm --filter @grapit/api test:integration -- admin-dashboard.integration.spec.ts
    ```
    사전조건: Docker daemon 기동 중. 실행 시 Postgres 16 + Valkey 8 testcontainer가 자동 기동되고 Drizzle migration 적용 후 2개 테스트 실행.
    기대: `Tests: 2 passed (2)`, 런타임 ~60s (container startup 포함)

    **4) 프론트엔드 typecheck + lint**
    ```bash
    pnpm --filter @grapit/web typecheck
    pnpm --filter @grapit/web lint
    ```
    기대: 0 exit

    **5) UI-SPEC Typography scope 확장 스캔 (review MEDIUM 8)**

    Plan 03의 task acceptance가 이미 각 파일별로 스캔하지만, Wave 3 gate에서 **scope 전체**를 한번 더 확증한다:
    ```bash
    # 금지 클래스: text-base, text-lg, text-2xl, text-3xl, font-medium, font-bold
    # scope: admin dashboard page + all dashboard components
    VIOLATIONS=$(grep -rE "text-base|text-lg|text-2xl|text-3xl|font-medium|font-bold" \
      apps/web/app/admin/page.tsx \
      apps/web/components/admin/dashboard/ \
      || true)
    if [ -n "$VIOLATIONS" ]; then
      echo "UI-SPEC Typography 위반:"
      echo "$VIOLATIONS"
      exit 1
    fi
    echo "UI-SPEC Typography 스캔 통과"
    ```
    기대: 0 결과. 위반 있으면 Plan 03으로 revert.

    **6) E2E 테스트 + chart blank Playwright smoke (review HIGH 2, review LOW 12)**

    **6-1) `apps/web/e2e/admin-dashboard.spec.ts`에 chart blank guard 테스트 추가 (review LOW 12):**

    기존 Plan 01이 만든 3개 테스트(landing-smoke / period-filter / sidebar-nav) 뒤에 4번째 테스트를 append:
    ```typescript
    test('chart-blank-guard: SVG child count > 0 for all 3 charts (recharts regression)', async ({ page }) => {
      await loginAsTestUser(page);
      await page.goto('/admin');
      // 차트 렌더 대기 (data fetch + recharts paint)
      await page.getByRole('heading', { name: '매출 추이' }).waitFor();
      await page.waitForTimeout(500);

      // 3개 차트 section 각각의 내부 <svg>에 자식 노드 존재 확인
      const sections = [
        { heading: '매출 추이' },
        { heading: '장르별 예매 분포' },
        { heading: '결제수단 분포' },
      ];
      for (const s of sections) {
        const section = page.locator('section').filter({ has: page.getByRole('heading', { name: s.heading }) });
        const svg = section.locator('svg').first();
        // recharts blank 회귀면 svg가 있어도 자식 노드가 없음. path/g/rect/circle 중 하나라도 > 0 이어야 함.
        const childCount = await svg.locator(':scope > *').count();
        expect(childCount, `${s.heading} 차트 SVG에 자식 노드가 없음 (recharts blank 회귀 가능성)`).toBeGreaterThan(0);
      }
    });
    ```

    **6-2) 전체 E2E 실행:**
    ```bash
    # Playwright config의 webServer가 자동 기동, 아니면 수동으로 web+api 기동 후 실행
    pnpm --filter @grapit/web test:e2e -- admin-dashboard.spec.ts
    ```
    기대: `4 passed` (landing-smoke / period-filter / sidebar-nav / chart-blank-guard). 실패 시 실패 내역 기록 + 디버깅.

    **7) 모노레포 전체 테스트**
    ```bash
    pnpm test
    ```
    기대: 회귀 없음. Plan 11 이전 대비 실패 개수 증가 0.

    **실패 시 조치:**
    - unit RED: Plan 02 service 구현 버그. `Task 02-01` 재실행 범위.
    - controller access-control RED:
      - 401/403/200 중 어느 것이 실패했는지 확인 후 RolesGuard 또는 global auth guard 설정 확인
      - Plan 02 Task 02-02의 controller handler 또는 module 등록 확인
    - integration RED:
      - Drizzle migration 실패 → integration spec의 migrator 호출 경로 확인
      - testcontainer 기동 실패 → `docker ps` / Docker Desktop 상태 확인
    - UI-SPEC violation:
      - grep 결과에 나온 파일/라인을 Plan 03 action으로 revert하여 금지 클래스를 허용 클래스로 교체
    - E2E RED:
      - admin 쿠키 미획득 → `admin@grapit.test` seed 존재 확인 (STATE.md 260413-jw1), 없으면 `cd apps/api && pnpm db:seed` 실행
      - 차트 렌더 실패 (Pitfall 1, chart-blank-guard에서 포착) → react-is override 적용: `apps/web/package.json`의 `pnpm.overrides` 또는 root `package.json`의 `pnpm.overrides`에 `"react-is": "19.1.0"` 추가 후 `pnpm install`
      - period 토글 미작동 → toggle-group primitive 설치 확인

    **결과 기록:**
    실행 결과를 Task 04-03의 checkpoint 프롬프트에 첨부할 수 있도록 마지막 출력 라인 (passing count / failing count) 스크린샷 또는 복사.

    **금지사항:**
    - `--no-verify` / `--skip` / `test.only` 등으로 테스트 우회 금지
    - 실패 원인이 명확하지 않은 상태에서 `test.fixme` 추가 금지 — Phase 9/9.1의 교훈
  </action>
  <verify>
    <automated>pnpm --filter @grapit/api exec vitest run admin-dashboard.service.spec.ts admin-dashboard.controller.spec.ts && pnpm --filter @grapit/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @grapit/api exec vitest run admin-dashboard.service.spec.ts` 출력에 `8 passed` 또는 `Tests  8 passed` 포함
    - `pnpm --filter @grapit/api exec vitest run admin-dashboard.controller.spec.ts` 출력에 `3 passed` 포함 (401/403/200)
    - `pnpm --filter @grapit/api test:integration -- admin-dashboard.integration.spec.ts` 출력에 `2 passed` 포함
    - `pnpm --filter @grapit/web typecheck` 0 exit
    - `pnpm --filter @grapit/web lint` 0 exit
    - UI-SPEC Typography scope-확장 스캔: `grep -rE "text-base|text-lg|text-2xl|text-3xl|font-medium|font-bold" apps/web/app/admin/page.tsx apps/web/components/admin/dashboard/` 결과 0 라인 (review MEDIUM 8)
    - `pnpm --filter @grapit/web test:e2e -- admin-dashboard.spec.ts` 출력에 `4 passed` 포함 (3 original + 1 chart-blank-guard) — review LOW 12
    - e2e spec grep: `test\('chart-blank-guard` match (신규 테스트 추가됨)
    - `pnpm test` 실행 결과 기록됨 (실패 건수 명시)
  </acceptance_criteria>
  <done>
    VALIDATION.md의 per-task verification map 항목 모두 자동 실행 + 결과 기록. E2E 전체 GREEN (Wave 2에서 이동된 review HIGH 2 충족). Chart blank smoke + UI-SPEC scope-확장 스캔 통과.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-02: 수동 QA — 차트 렌더 / 기간 토글 / 사이드바 상호작용 / UI-SPEC 색상 / a11y</name>
  <what-built>
    - 백엔드 5개 GET 엔드포인트 (/api/v1/admin/dashboard/*)
    - 프론트엔드 /admin 대시보드 (KPI 4 + chart 3 + Top 10 + 기간 필터 + 사이드바 NAV)
    - shadcn Chart + purple monochromatic palette
    - Valkey read-through 캐시 (60s TTL)
    - ChartPanelState 3-mode 공통 컴포넌트 + sr-only summary + prefers-reduced-motion
  </what-built>
  <how-to-verify>
    **사전 준비:**
    1. API 기동: `cd apps/api && pnpm start:dev` (포트 8080)
    2. Web 기동: `cd apps/web && pnpm dev` (포트 3000)
    3. DB seed 확인: admin 계정 `admin@grapit.test` 존재 여부. 없으면 `pnpm --filter @grapit/api db:seed` (STATE.md 260413-jw1 참조)

    **검증 1 — 라우팅 & 사이드바 (D-01/D-02/D-03):**
    - [ ] `http://localhost:3000/admin`에 접속, admin 계정으로 로그인 (`admin@grapit.test` / 개발 패스워드)
    - [ ] `/admin` URL이 404가 아닌 대시보드 페이지를 렌더
    - [ ] 사이드바 최상단 NAV 항목이 "대시보드" + LayoutDashboard 아이콘 → 클릭 시 active 하이라이트
    - [ ] 사이드바 로고 "Grapit Admin" 클릭 시 `/admin`으로 이동
    - [ ] 다른 탭(`/admin/performances`)으로 이동 후 사이드바에서 "대시보드"만 active 상태가 해제되는지 (exact match 확인)

    **검증 2 — KPI 카드 (ADM-01):**
    - [ ] 4장의 KPI 카드 렌더: 오늘 예매수 / 오늘 매출 / 오늘 취소 / 활성 공연
    - [ ] 각 카드에 lucide 아이콘 (Ticket / Banknote / RotateCcw / Theater)
    - [ ] 숫자가 loading skeleton → 실제 값으로 바뀌는 전환 관찰
    - [ ] 오늘 매출이 ₩ 심볼 + 천 단위 구분 또는 M 축약 표시

    **검증 3 — 차트 렌더 (Pitfall 1 실측 + sr-only 검증):**
    - [ ] "매출 추이" area chart가 **시각적으로** 보라색 그라디언트 영역으로 렌더 (빈 흰 영역이 아님)
    - [ ] "장르별 예매 분포" donut chart가 innerRadius 60/outerRadius 100으로 렌더
    - [ ] "결제수단 분포" bar chart가 보라색 막대로 렌더
    - [ ] DevTools Elements 탭에서 각 `<svg>` 요소 내부에 `<path>`/`<g>`/`<rect>`/`<circle>` 자식 노드 존재 확인 (또는 Task 04-01의 chart-blank-guard E2E가 PASS)
    - [ ] **빈 `<svg>`만 있을 경우 → 이슈 보고 필요 (Pitfall 1 react-is override 적용 필수)**
    - [ ] 각 차트 컨테이너 근처에 `sr-only` 요약 텍스트 존재 확인 (스크린리더가 읽을 수 있는 기술: "매출 추이: N개 구간, 매출 발생 K개, 합계 X원", "장르별 예매 분포: N개 장르, 총 M건", "결제수단 분포: N개 결제수단, 총 M건") — review LOW 13

    **검증 4 — UI-SPEC 색상 (Chart palette):**
    - [ ] Area chart stroke/fill 색상이 `#6C3CE0` (보라 primary) 또는 근접한 보라색
    - [ ] Donut slice가 진한 보라 → 연한 보라 → 회색으로 5단계 gradient
    - [ ] **shadcn 기본 oklch rainbow 색상(파/녹/주/빨)이 보이면 → globals.css override 실패 (Task 03-01 재검토)**

    **검증 5 — 기간 필터 (D-09, D-11):**
    - [ ] 매출 추이 헤더 오른쪽에 7일 / 30일 / 90일 3개 토글 버튼
    - [ ] 초기 선택이 "30일"
    - [ ] 7일 클릭 시:
      - Network 탭에서 `/api/v1/admin/dashboard/revenue?period=7d`, `/genre?period=7d`, `/payment?period=7d` 3개 요청 발생
      - 3개 차트 동시 skeleton → 업데이트
      - Top 10은 변경 없음 (30d 고정, D-10)
    - [ ] 90일 클릭 시 area chart의 x축 bucket이 주별 포맷 (YYYY-WNN) 관찰
    - [ ] 빈 주/날짜도 0값으로 chart에 표시 (review MEDIUM 6) — x축 간격이 고르게 보이는지 확인

    **검증 6 — Top 10 (ADM-04):**
    - [ ] 테이블 컬럼: 순위 / 포스터 / 공연명 / 장르 / 예매수
    - [ ] 예매수 desc 정렬
    - [ ] 데이터 없을 때 `아직 인기 공연이 없습니다` / `최근 30일 예매가 누적되면 랭킹이 표시됩니다` 카피 표시
    - [ ] 서브카피 `최근 30일 예매 건수 기준` 표시

    **검증 7 — 캐시 관찰 (ADM-06):**
    - [ ] Network 탭에서 페이지 첫 로드 시 5개 GET 요청의 response time 기록
    - [ ] 페이지 새로고침 (Cmd+R) 시 같은 요청들의 response time이 **현저히 짧아짐** (caching 효과)
    - [ ] 60초 이상 대기 후 다시 새로고침 시 캐시 expire → 다시 DB 쿼리 latency로 복귀
    - [ ] API 로그에서 `CacheService` get/set 로그에 key만 노출되고 값(매출액 등)은 노출되지 않음 (T-07-11 inherit)

    **검증 8 — 비관리자 접근 차단 (T-11-03):**
    - [ ] 비관리자 또는 비로그인 상태에서 `curl -i http://localhost:8080/api/v1/admin/dashboard/summary` → 401 또는 403
    - [ ] 일반 유저로 로그인 후 `/admin` 이동 시 `/`로 redirect (기존 layout guard)
    - [ ] Task 04-01의 controller access-control 테스트 3/3 GREEN (자동화된 보완 — review HIGH 3)

    **검증 9 — UI-SPEC Typography + Error/Empty 분리 (review MEDIUM 7/8):**
    - [ ] DevTools Elements에서 `<h1>대시보드</h1>`가 `text-xl font-semibold`
    - [ ] 모든 섹션 `<h2>`가 `text-sm font-semibold`
    - [ ] Task 04-01의 UI-SPEC 스캔이 0 위반 (자동화됨)
    - [ ] 네트워크를 offline으로 바꿔도 "대시보드를 불러오지 못했습니다" (error) 카피가 표시되고, "표시할 데이터가 없습니다" (empty) 카피와 **별도로** 렌더됨 (review MEDIUM 7)
    - [ ] 에러 상태에서 "다시 시도" 버튼 클릭 시 refetch 발생

    **검증 10 — a11y (review LOW 13):**
    - [ ] macOS VoiceOver로 /admin 진입 시 각 차트 섹션에 sr-only 요약이 읽힘
    - [ ] OS 설정에서 "Reduce Motion" 활성화 후 새로고침 시 차트 진입 애니메이션이 스킵됨 (isAnimationActive=false + motion-reduce CSS)

    **검증 11 — Success Criteria 5개 (ROADMAP):**
    - [ ] 관리자가 /admin에서 오늘의 예매/매출/취소/활성 공연 확인 ✓
    - [ ] 일별/주별 매출 추이 area chart 시각화 (빈 bucket 0 채움 포함) ✓
    - [ ] 장르별 donut + 결제수단 bar 차트 ✓
    - [ ] 인기 공연 Top 10 랭킹 ✓
    - [ ] Valkey 캐싱으로 대시보드 로딩이 빠름 ✓
  </how-to-verify>
  <resume-signal>Type "approved" 혹은 발견된 이슈를 모두 나열. 이슈가 있으면 해당 task로 revert하여 수정 후 재검증.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 04-03: VALIDATION.md 상태 갱신 + Phase 11 ROADMAP 체크박스 + REQUIREMENTS.md ADM-01~06 처리 (실행일 기준)</name>
  <read_first>
    - .planning/phases/11-admin-dashboard/11-VALIDATION.md
    - .planning/ROADMAP.md §Phase 11
    - .planning/REQUIREMENTS.md §어드민 대시보드 §Traceability
  </read_first>
  <files>
    - .planning/phases/11-admin-dashboard/11-VALIDATION.md (수정)
    - .planning/ROADMAP.md (수정)
    - .planning/REQUIREMENTS.md (수정)
  </files>
  <action>
    **실행일 변수 설정 (review LOW 10 — 하드코딩 금지):**
    ```bash
    TODAY=$(date -u +%F)   # ISO date 기준, 예: 2026-04-21
    echo "Today: $TODAY"
    ```
    아래 모든 날짜 기입은 `$TODAY`로 치환한다. 예시 날짜를 그대로 쓰지 않는다.

    **A) `11-VALIDATION.md` 갱신:**
    - Frontmatter: `status: draft` → `status: approved`, `nyquist_compliant: false` → `true`, `wave_0_complete: false` → `true`
    - Per-Task Verification Map의 `Status` 컬럼: 모든 `⬜ pending` → `✅ green` (Task 04-01 실행 결과 기반)
    - Checker Sign-Off 체크리스트 6개 항목 전부 체크
    - Manual-Only Verifications 섹션에 Task 04-02 결과 기록 (차트 렌더 PASS / 색상 PASS / 캐시 관찰 PASS / a11y PASS)
    - Validation Sign-Off 체크리스트 6개 전부 체크

    **B) `ROADMAP.md` 갱신:**
    - Progress 테이블에서 Phase 11 행: `Plans Complete` → `4/4`, `Status` → `Complete`, `Completed` → `$TODAY` (하드코딩 금지)
    - Phase 11 상세 섹션의 `**Plans**: TBD` 줄을 실제 완료 plan 목록으로 교체:
      ```
      **Plans:** 4/4 plans complete
      Plans:
      - [x] 11-01-PLAN.md — Wave 1 RED: shared schema + service/controller skeleton + unit/controller-access/integration/E2E 테스트 스캐폴딩
      - [x] 11-02-PLAN.md — Wave 2 API: kst-boundary helper + AdminDashboardService/Controller + AdminModule 등록
      - [x] 11-03-PLAN.md — Wave 2 Web: shadcn chart + sidebar diff + ChartPanelState + 5 컴포넌트 + /admin/page.tsx
      - [x] 11-04-PLAN.md — Wave 3 검증: 자동 테스트 풀 실행 + chart blank guard + UI-SPEC scope 스캔 + 수동 QA
      ```
    - 상단 milestones 섹션에서 `[ ] Phase 11: 어드민 대시보드` → `[x] Phase 11: 어드민 대시보드` (completed $TODAY)

    **C) `REQUIREMENTS.md` 갱신:**
    - §어드민 대시보드: `- [ ] **ADM-01**` → `- [x] **ADM-01**` (6개 전부)
    - §Traceability 표에서 ADM-01 ~ ADM-06 6개 행의 `Status` 컬럼: `Pending` → `Complete`

    **금지사항:**
    - STATE.md는 `/gsd-state-sync` 워크플로우가 관리 — 직접 수정 금지
    - Phase 11 완료 시 재귀적으로 Phase 12 Plans를 TBD 상태로 두고 체크박스 체크하지 말 것 (Phase 12는 별도 phase plan 대상)
    - 완료 날짜는 예시 날짜 하드코딩 금지 — 반드시 `date -u +%F` 실행일 사용 (review LOW 10)
  </action>
  <verify>
    <automated>grep -q '11-01-PLAN.md' .planning/ROADMAP.md && grep -q 'ADM-01.*Complete' .planning/REQUIREMENTS.md && grep -q 'status: approved' .planning/phases/11-admin-dashboard/11-VALIDATION.md && echo "all docs updated"</automated>
  </verify>
  <acceptance_criteria>
    - `11-VALIDATION.md` frontmatter에 `status: approved` + `nyquist_compliant: true` + `wave_0_complete: true`
    - `11-VALIDATION.md` Per-Task Verification Map에서 `⬜ pending` 0건, `✅ green` ≥ 13건 (unit 8 + controller 3 + integration 2)
    - `ROADMAP.md` Progress 테이블의 Phase 11 행이 `4/4 | Complete | {실행일 기준 YYYY-MM-DD}` (예시 날짜 하드코딩 없음)
    - `ROADMAP.md` §Phase 11 상세 섹션에 `11-01-PLAN.md` / `11-02-PLAN.md` / `11-03-PLAN.md` / `11-04-PLAN.md` 4개 `[x]` 항목
    - `REQUIREMENTS.md`의 어드민 대시보드 섹션에서 ADM-01 ~ ADM-06 6개 모두 `[x]`
    - `REQUIREMENTS.md` Traceability 표의 ADM-01 ~ ADM-06 `Status` 컬럼 6개 모두 `Complete`
  </acceptance_criteria>
  <done>
    Phase 11 완료 상태가 프로젝트 문서에 반영됨. 완료 날짜는 실행일 기준. STATE.md는 후속 `/gsd-state-sync`에서 업데이트.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification plan — 새 runtime boundary 없음) | 기존 Plan 02/03 boundary 재검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Spoofing | 수동 QA 시 admin 계정 탈취 위험 | accept | dev 환경 admin 계정은 테스트 전용. 프로덕션 계정과 격리됨. |
| T-11-12 | Repudiation | Wave 3 승인 누락으로 gap phase 진행 | mitigate | Task 04-03에서 ROADMAP/REQUIREMENTS/VALIDATION 3개 문서 동기화 갱신으로 완료 상태 추적. 차기 phase 시작 시 STATE sync로 최종 반영. |
| T-11-13 | Denial of Service (regression) | recharts blank 차트 회귀 | mitigate | Task 04-01의 Playwright chart-blank-guard 테스트가 SVG 자식 노드 count > 0을 assert하여 회귀 자동 검출 (review LOW 12). |

</threat_model>

<verification>
Wave 3 완료 확인:
1. `pnpm test` 결과 기록 — 회귀 없음
2. E2E 4개 (landing-smoke + period-filter + sidebar-nav + chart-blank-guard) PASS 기록
3. Pitfall 1 (recharts blank) 미발생 확인 (chart-blank-guard 테스트 + manual verify Task 04-02)
4. UI-SPEC Typography scope-확장 스캔 0 위반 (review MEDIUM 8)
5. ROADMAP.md / REQUIREMENTS.md / VALIDATION.md 3개 문서 갱신 확인 (실행일 기준)
</verification>

<success_criteria>
- [ ] 자동 테스트 전부 PASS (unit 8 + controller access-control 3 + integration 2 + E2E 4 = 17개)
- [ ] 수동 QA 11개 검증 모두 승인
- [ ] recharts 회귀 없음 (chart-blank-guard E2E PASS, 또는 react-is override 적용 완료)
- [ ] UI-SPEC Typography scope-확장 스캔 0 위반 (apps/web/app/admin/page.tsx + apps/web/components/admin/dashboard/)
- [ ] a11y: sr-only 차트 summary 읽힘, prefers-reduced-motion 대응
- [ ] VALIDATION.md / ROADMAP.md / REQUIREMENTS.md 3개 문서 완료 상태 반영 (실행일 기준)
- [ ] ADM-01 ~ ADM-06 6개 요구사항 Complete
</success_criteria>

<output>
완료 후 `.planning/phases/11-admin-dashboard/11-04-SUMMARY.md` 생성.
Phase 11 전체 종료.
</output>
