---
phase: 11-admin-dashboard
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/web/package.json
  - apps/web/app/globals.css
  - apps/web/components/ui/chart.tsx
  - apps/web/components/ui/toggle-group.tsx
  - apps/web/components/admin/admin-sidebar.tsx
  - apps/web/hooks/use-admin-dashboard.ts
  - apps/web/components/admin/dashboard/revenue-area-chart.tsx
  - apps/web/components/admin/dashboard/genre-donut-chart.tsx
  - apps/web/components/admin/dashboard/payment-bar-chart.tsx
  - apps/web/components/admin/dashboard/top-performances-table.tsx
  - apps/web/components/admin/dashboard/period-filter.tsx
  - apps/web/app/admin/page.tsx
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06]
tags: [admin, dashboard, frontend, shadcn, recharts, tanstack-query]
must_haves:
  truths:
    - "'/admin' 타이핑 또는 사이드바 로고 클릭 시 대시보드 렌더 (D-01, D-02)"
    - "사이드바 NAV_ITEMS 최상단에 '대시보드' 항목 표시, LayoutDashboard 아이콘 (D-03)"
    - "KPI 4장 (오늘 예매수 / 오늘 매출 / 오늘 취소 / 활성 공연) 렌더"
    - "매출 추이 area chart + 장르 donut + 결제수단 bar + Top 10 table 렌더"
    - "7/30/90일 ToggleGroup 클릭 시 revenue/genre/payment 3개 chart 동시 refetch (D-11)"
    - "차트 color palette가 --chart-1 #6C3CE0 → --chart-5 #A1A1AA monochromatic purple+gray (UI-SPEC)"
    - "모든 client component에 'use client' 지시어 존재 (Pitfall 3)"
    - "recharts 3.8.x 이상 pinned 설치, react-is 버전 일치"
  artifacts:
    - path: "apps/web/components/ui/chart.tsx"
      provides: "shadcn Chart primitives (ChartContainer/Tooltip/Legend)"
      min_lines: 100
    - path: "apps/web/components/ui/toggle-group.tsx"
      provides: "shadcn ToggleGroup (period filter)"
      min_lines: 30
    - path: "apps/web/hooks/use-admin-dashboard.ts"
      provides: "5개 useQuery 훅: useDashboardSummary/Revenue/Genre/Payment/Top10"
      exports: ["useDashboardSummary", "useDashboardRevenue", "useDashboardGenre", "useDashboardPayment", "useDashboardTop10"]
      min_lines: 55
    - path: "apps/web/components/admin/dashboard/revenue-area-chart.tsx"
      provides: "area chart wrapping shadcn Chart + recharts AreaChart"
    - path: "apps/web/components/admin/dashboard/genre-donut-chart.tsx"
      provides: "donut chart"
    - path: "apps/web/components/admin/dashboard/payment-bar-chart.tsx"
      provides: "bar chart"
    - path: "apps/web/components/admin/dashboard/top-performances-table.tsx"
      provides: "Top 10 테이블 (rank/썸네일/공연명/장르/예매수)"
    - path: "apps/web/components/admin/dashboard/period-filter.tsx"
      provides: "7/30/90일 ToggleGroup"
    - path: "apps/web/app/admin/page.tsx"
      provides: "/admin 랜딩 대시보드 composition"
  key_links:
    - from: "apps/web/app/admin/page.tsx"
      to: "apps/web/hooks/use-admin-dashboard.ts"
      via: "useDashboardSummary/Revenue/Genre/Payment/Top10 hook 호출"
      pattern: "useDashboard"
    - from: "apps/web/hooks/use-admin-dashboard.ts"
      to: "/api/v1/admin/dashboard/*"
      via: "apiClient.get"
      pattern: "/api/v1/admin/dashboard"
    - from: "apps/web/components/admin/admin-sidebar.tsx"
      to: "/admin (dashboard)"
      via: "Link href + NAV_ITEMS"
      pattern: "LayoutDashboard"
    - from: "apps/web/components/admin/dashboard/revenue-area-chart.tsx"
      to: "apps/web/components/ui/chart.tsx"
      via: "ChartContainer/ChartTooltip import"
      pattern: "from '@/components/ui/chart'"
---

<objective>
Plan 01 RED E2E를 GREEN으로 전환할 프론트엔드 구현. `/admin` 랜딩을 읽기 전용 대시보드로 전환하고, 5개 API를 consume하는 훅 + 3종 차트 + Top 10 테이블 + 기간 필터 + 사이드바 diff.

Purpose:
- D-01~D-04: 라우팅/IA 전환
- D-05: shadcn Chart 설치
- D-09/D-11: 기간 필터 + 차트 3종 공유
- UI-SPEC 전체 준수 (copywriting / color palette / typography / layout)
- Plan 01 E2E 3개 테스트(landing-smoke/period-filter/sidebar-nav) GREEN 전환

Output:
- shadcn chart + toggle-group 설치 완료, --chart-1~5 purple monochromatic override 반영
- 5개 신규 dashboard 컴포넌트 + 1 훅 파일
- admin-sidebar.tsx 2-line diff (NAV_ITEMS + logo href)
- apps/web/app/admin/page.tsx 대시보드 composition
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-dashboard/11-CONTEXT.md
@.planning/phases/11-admin-dashboard/11-RESEARCH.md
@.planning/phases/11-admin-dashboard/11-PATTERNS.md
@.planning/phases/11-admin-dashboard/11-UI-SPEC.md
@.planning/phases/11-admin-dashboard/11-01-SUMMARY.md

@apps/web/app/admin/layout.tsx
@apps/web/app/admin/bookings/page.tsx
@apps/web/components/admin/admin-sidebar.tsx
@apps/web/components/admin/admin-stat-card.tsx
@apps/web/components/admin/admin-booking-dashboard.tsx
@apps/web/components/admin/admin-booking-table.tsx
@apps/web/hooks/use-admin.ts
@apps/web/hooks/use-reservations.ts
@apps/web/lib/api-client.ts
@apps/web/components/ui/skeleton.tsx
@apps/web/components/ui/button.tsx
@apps/web/components/ui/table.tsx
@apps/web/app/globals.css
@apps/web/components.json

<interfaces>
<!-- Consuming from @grapit/shared (Plan 01): -->
```typescript
export const dashboardPeriodSchema: z.ZodEnum<['7d','30d','90d']>;
export type DashboardPeriod = '7d' | '30d' | '90d';

export interface DashboardSummaryDto { todayBookings: number; todayRevenue: number; todayCancelled: number; activePerformances: number; }
export type DashboardRevenueDto = { bucket: string; revenue: number; count: number; }[];
export type DashboardGenreDto = { genre: string; count: number; }[];
export type DashboardPaymentDto = { method: string; count: number; }[];
export type DashboardTopDto = { performanceId: string; title: string; genre: string; posterUrl: string | null; bookingCount: number; }[];
```

<!-- Consuming from Plan 02 (backend routes — not imported, just fetched): -->
GET /api/v1/admin/dashboard/summary → DashboardSummaryDto
GET /api/v1/admin/dashboard/revenue?period=7d|30d|90d → DashboardRevenueDto
GET /api/v1/admin/dashboard/genre?period=... → DashboardGenreDto
GET /api/v1/admin/dashboard/payment?period=... → DashboardPaymentDto
GET /api/v1/admin/dashboard/top-performances → DashboardTopDto

<!-- Existing project API: -->
From @/components/admin/admin-stat-card:
```typescript
interface AdminStatCardProps {
  label: string;
  value: number;
  icon: LucideIcon;
  format: 'count' | 'currency' | 'percent';
}
```

From @/lib/api-client:
```typescript
export const apiClient: { get<T>(url: string): Promise<T>; post<T>(url: string, body: unknown): Promise<T>; ... };
```

From @/components/ui/skeleton: `<Skeleton className="..." />`
From @/components/ui/table: `<Table><TableHeader><TableRow><TableHead/><TableCell/>...` (shadcn new-york)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-01: shadcn chart + toggle-group 설치 + --chart-1~5 purple override + sidebar 2-line diff</name>
  <read_first>
    - apps/web/components.json (shadcn config — aliases.ui = "@/components/ui" 확인)
    - apps/web/app/globals.css (현재 @theme 블록 + :root 블록 전체 — --chart 변수 주입 위치 확인)
    - apps/web/components/admin/admin-sidebar.tsx (D-02/D-03 diff 적용 대상)
    - .planning/phases/11-admin-dashboard/11-UI-SPEC.md §Chart color palette (5개 hex 값)
    - .planning/phases/11-admin-dashboard/11-RESEARCH.md §Pitfall 1 (recharts + react-is workaround)
    - apps/web/package.json (현재 recharts / react-is 상태 확인)
  </read_first>
  <files>
    - apps/web/components/ui/chart.tsx (shadcn CLI로 생성)
    - apps/web/components/ui/toggle-group.tsx (shadcn CLI로 생성)
    - apps/web/package.json (recharts 3.8.x 의존성 추가, shadcn CLI가 수행)
    - apps/web/app/globals.css (--chart-1~5 override 수동 수정)
    - apps/web/components/admin/admin-sidebar.tsx (2-line diff)
  </files>
  <action>
    **A) shadcn chart + toggle-group 설치 (apps/web cwd)**

    ```bash
    cd apps/web && pnpm dlx shadcn@latest add chart toggle-group
    ```

    CLI가 자동 수행:
    - `apps/web/components/ui/chart.tsx` 생성
    - `apps/web/components/ui/toggle-group.tsx` 생성
    - `apps/web/package.json`의 `dependencies`에 `recharts` + (필요 시) `@radix-ui/react-toggle-group` 추가
    - `apps/web/app/globals.css`의 `:root` 블록에 `--chart-1~5` (기본 oklch 값) 주입

    설치 완료 후 `apps/web/package.json`에서 `recharts` 버전이 `3.8.x` 이상인지 확인. 3.8.0 미만이면:
    ```bash
    cd apps/web && pnpm add recharts@^3.8.1
    ```

    **react-is 버전 워크어라운드 대비 (Pitfall 1 / A5 medium-risk):**
    `apps/web/package.json`의 `dependencies`에 `react-is`가 명시되어 있는지 확인. 없으면:
    ```bash
    cd apps/web && pnpm add react-is@19.1.0
    ```

    **B) `apps/web/app/globals.css` 수정 — --chart-1~5 purple override**

    shadcn CLI가 주입한 기본 `--chart-1` ~ `--chart-5` (oklch 기반 rainbow) 줄들을 찾아서 다음 정확한 값으로 교체:
    ```css
    :root {
      /* ... 기존 토큰 ... */
      --chart-1: #6C3CE0;
      --chart-2: #8B6DE8;
      --chart-3: #B8A3EF;
      --chart-4: #D1D1DB;
      --chart-5: #A1A1AA;
    }
    ```

    dark 모드 블록이 있다면 동일 값 반복 (다크 대응 out of scope — UI-SPEC admin 다크모드 Out of Scope 명시).

    **C) `apps/web/components/admin/admin-sidebar.tsx` 수정 — 3개 diff**

    현재 import line 5:
    ```typescript
    import { Theater, Image, Ticket } from 'lucide-react';
    ```
    → 변경:
    ```typescript
    import { Theater, Image, Ticket, LayoutDashboard } from 'lucide-react';
    ```

    현재 NAV_ITEMS (lines 8-24):
    ```typescript
    const NAV_ITEMS = [
      { label: '공연 관리', href: '/admin/performances', icon: Theater },
      { label: '배너 관리', href: '/admin/banners', icon: Image },
      { label: '예매 관리', href: '/admin/bookings', icon: Ticket },
    ] as const;
    ```
    → 변경 (최상단에 대시보드 추가, D-03):
    ```typescript
    const NAV_ITEMS = [
      { label: '대시보드', href: '/admin', icon: LayoutDashboard },
      { label: '공연 관리', href: '/admin/performances', icon: Theater },
      { label: '배너 관리', href: '/admin/banners', icon: Image },
      { label: '예매 관리', href: '/admin/bookings', icon: Ticket },
    ] as const;
    ```

    현재 line ~32 로고 링크:
    ```typescript
    <Link href="/admin/performances" className="text-lg font-semibold">
    ```
    → 변경 (D-02):
    ```typescript
    <Link href="/admin" className="text-lg font-semibold">
    ```

    **active 하이라이트 특수처리 (PATTERNS 주의):**
    기존 `isActive` 로직 (`pathname.startsWith(item.href)`)은 `/admin`이 `/admin/*` 전체에서 매치되어 오탐 발생. 현재 map 안의 `isActive` 계산을 다음과 같이 교체:
    ```typescript
    const isActive = item.href === '/admin'
      ? pathname === '/admin'
      : pathname.startsWith(item.href);
    ```

    **금지사항:**
    - toggle-group 설치 전에 수동으로 `toggle-group.tsx` 작성 금지 — shadcn CLI가 표준 구조 생성
    - `--chart` 값에 oklch/hsl wrapper 사용 금지 — UI-SPEC은 raw hex 사용 (shadcn Chart는 `var(--chart-1)` direct 참조)
    - 기존 NAV_ITEMS 순서/라벨 변경 금지 (대시보드만 최상단 추가)
    - `font-medium` / `font-bold` / `text-lg` 등 UI-SPEC 미허용 스타일 추가 금지
  </action>
  <verify>
    <automated>test -f apps/web/components/ui/chart.tsx && test -f apps/web/components/ui/toggle-group.tsx && grep -q "recharts" apps/web/package.json && grep -q "#6C3CE0" apps/web/app/globals.css && grep -q "LayoutDashboard" apps/web/components/admin/admin-sidebar.tsx && echo "all checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/ui/chart.tsx` 존재, 라인 수 ≥ 100
    - `apps/web/components/ui/toggle-group.tsx` 존재, 라인 수 ≥ 25
    - `apps/web/package.json`의 dependencies에 `recharts` 포함 (버전 3.8.x 이상)
    - `apps/web/app/globals.css`가 `--chart-1: #6C3CE0`, `--chart-2: #8B6DE8`, `--chart-3: #B8A3EF`, `--chart-4: #D1D1DB`, `--chart-5: #A1A1AA` 5개 line 모두 포함
    - `apps/web/components/admin/admin-sidebar.tsx`의 import에 `LayoutDashboard` 포함
    - `admin-sidebar.tsx`의 NAV_ITEMS 배열에 `{ label: '대시보드', href: '/admin', icon: LayoutDashboard }` 포함
    - `admin-sidebar.tsx`에 `href="/admin"` (로고 Link) 포함, `href="/admin/performances"` 로고 링크로 사용되는 부분 제거됨
    - `admin-sidebar.tsx`에 `item.href === '/admin'` exact match 처리 포함
    - `pnpm --filter @grapit/web lint` → 0 exit (이 단계에서)
  </acceptance_criteria>
  <done>
    shadcn chart/toggle-group 설치, purple palette 적용, 사이드바에 대시보드 진입점 추가.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-02: use-admin-dashboard.ts 훅 + 5개 dashboard 컴포넌트 (charts + table + period-filter)</name>
  <read_first>
    - apps/web/hooks/use-admin.ts (useQuery + apiClient.get 패턴)
    - apps/web/hooks/use-reservations.ts (placeholderData/keepPreviousData 패턴)
    - apps/web/lib/api-client.ts (apiClient.get 시그니처)
    - apps/web/components/admin/admin-booking-table.tsx (Table + skeleton + empty 패턴)
    - apps/web/components/admin/admin-stat-card.tsx (재사용 대상)
    - apps/web/components/ui/chart.tsx (Task 03-01 산출 — ChartContainer/Tooltip/Legend export 확인)
    - apps/web/components/ui/toggle-group.tsx (Task 03-01 산출)
    - .planning/phases/11-admin-dashboard/11-RESEARCH.md §Code Example 3, 4, 6
    - .planning/phases/11-admin-dashboard/11-UI-SPEC.md §Copywriting, §Chart color palette, §Layout
    - .planning/phases/11-admin-dashboard/11-PATTERNS.md §chart components, §top-performances-table, §period-filter
  </read_first>
  <behavior>
    - `use-admin-dashboard.ts`: 5개 named export hook, 각 `staleTime: 30_000`, `refetchOnWindowFocus: false`
    - `revenue-area-chart.tsx`: AreaChart + linearGradient fill, `var(--chart-1)` stroke/fill
    - `genre-donut-chart.tsx`: PieChart + innerRadius 60, outerRadius 100, PALETTE 5색 rotation
    - `payment-bar-chart.tsx`: BarChart + `var(--chart-1)` fill, radius [4,4,0,0]
    - `top-performances-table.tsx`: rank/썸네일/공연명/장르/예매수 컬럼, empty/loading tri-state, UI-SPEC 카피
    - `period-filter.tsx`: ToggleGroup type=single, `aria-label="기간 선택"`, 7/30/90일
  </behavior>
  <files>
    - apps/web/hooks/use-admin-dashboard.ts (신규)
    - apps/web/components/admin/dashboard/revenue-area-chart.tsx (신규)
    - apps/web/components/admin/dashboard/genre-donut-chart.tsx (신규)
    - apps/web/components/admin/dashboard/payment-bar-chart.tsx (신규)
    - apps/web/components/admin/dashboard/top-performances-table.tsx (신규)
    - apps/web/components/admin/dashboard/period-filter.tsx (신규)
  </files>
  <action>
    **A) `apps/web/hooks/use-admin-dashboard.ts`** 신규:
    ```typescript
    'use client';

    import { useQuery } from '@tanstack/react-query';
    import { apiClient } from '@/lib/api-client';
    import type {
      DashboardSummaryDto,
      DashboardRevenueDto,
      DashboardGenreDto,
      DashboardPaymentDto,
      DashboardTopDto,
      DashboardPeriod,
    } from '@grapit/shared';

    const STALE_TIME = 30_000; // 서버 TTL 60s의 절반
    const FOCUS_REFETCH = false; // D-11/UI-SPEC UX: 스켈레톤 깜빡임 방지

    export function useDashboardSummary() {
      return useQuery({
        queryKey: ['admin', 'dashboard', 'summary'],
        queryFn: () => apiClient.get<DashboardSummaryDto>('/api/v1/admin/dashboard/summary'),
        staleTime: STALE_TIME,
        refetchOnWindowFocus: FOCUS_REFETCH,
      });
    }

    export function useDashboardRevenue(period: DashboardPeriod) {
      return useQuery({
        queryKey: ['admin', 'dashboard', 'revenue', period],
        queryFn: () => apiClient.get<DashboardRevenueDto>(`/api/v1/admin/dashboard/revenue?period=${period}`),
        staleTime: STALE_TIME,
        refetchOnWindowFocus: FOCUS_REFETCH,
      });
    }

    export function useDashboardGenre(period: DashboardPeriod) {
      return useQuery({
        queryKey: ['admin', 'dashboard', 'genre', period],
        queryFn: () => apiClient.get<DashboardGenreDto>(`/api/v1/admin/dashboard/genre?period=${period}`),
        staleTime: STALE_TIME,
        refetchOnWindowFocus: FOCUS_REFETCH,
      });
    }

    export function useDashboardPayment(period: DashboardPeriod) {
      return useQuery({
        queryKey: ['admin', 'dashboard', 'payment', period],
        queryFn: () => apiClient.get<DashboardPaymentDto>(`/api/v1/admin/dashboard/payment?period=${period}`),
        staleTime: STALE_TIME,
        refetchOnWindowFocus: FOCUS_REFETCH,
      });
    }

    export function useDashboardTop10() {
      return useQuery({
        queryKey: ['admin', 'dashboard', 'top10'],
        queryFn: () => apiClient.get<DashboardTopDto>('/api/v1/admin/dashboard/top-performances'),
        staleTime: STALE_TIME,
        refetchOnWindowFocus: FOCUS_REFETCH,
      });
    }
    ```

    **B) `revenue-area-chart.tsx`** (RESEARCH Code Example 3 verbatim):
    ```typescript
    'use client';
    import { Area, AreaChart, CartesianGrid, XAxis, YAxis } from 'recharts';
    import { ChartContainer, ChartTooltip, ChartTooltipContent, type ChartConfig } from '@/components/ui/chart';
    import type { DashboardRevenueDto } from '@grapit/shared';

    const chartConfig: ChartConfig = {
      revenue: { label: '매출', color: 'var(--chart-1)' },
    };

    interface Props { data: DashboardRevenueDto; }

    export function RevenueAreaChart({ data }: Props) {
      return (
        <ChartContainer config={chartConfig} className="h-[280px] w-full">
          <AreaChart data={data} accessibilityLayer>
            <defs>
              <linearGradient id="fillRevenue" x1="0" y1="0" x2="0" y2="1">
                <stop offset="5%" stopColor="var(--chart-1)" stopOpacity={0.8} />
                <stop offset="95%" stopColor="var(--chart-1)" stopOpacity={0.1} />
              </linearGradient>
            </defs>
            <CartesianGrid vertical={false} strokeDasharray="3 3" />
            <XAxis dataKey="bucket" tickLine={false} axisLine={false} tickMargin={8} className="text-xs" />
            <YAxis tickLine={false} axisLine={false} className="text-xs" />
            <ChartTooltip content={<ChartTooltipContent labelKey="bucket" nameKey="revenue" />} />
            <Area type="monotone" dataKey="revenue" stroke="var(--chart-1)" fill="url(#fillRevenue)" />
          </AreaChart>
        </ChartContainer>
      );
    }
    ```

    **C) `genre-donut-chart.tsx`** (RESEARCH Code Example 4 verbatim):
    ```typescript
    'use client';
    import { Pie, PieChart, Cell } from 'recharts';
    import {
      ChartContainer,
      ChartTooltip,
      ChartTooltipContent,
      ChartLegend,
      ChartLegendContent,
      type ChartConfig,
    } from '@/components/ui/chart';
    import type { DashboardGenreDto } from '@grapit/shared';

    const PALETTE = ['var(--chart-1)', 'var(--chart-2)', 'var(--chart-3)', 'var(--chart-4)', 'var(--chart-5)'];

    interface Props { data: DashboardGenreDto; }

    export function GenreDonutChart({ data }: Props) {
      const chartConfig: ChartConfig = Object.fromEntries(
        data.map((d, i) => [d.genre, { label: d.genre, color: PALETTE[i % PALETTE.length] }]),
      );
      return (
        <ChartContainer config={chartConfig} className="h-[280px] w-full">
          <PieChart>
            <ChartTooltip content={<ChartTooltipContent nameKey="genre" />} />
            <Pie data={data} dataKey="count" nameKey="genre" innerRadius={60} outerRadius={100} paddingAngle={2}>
              {data.map((_, i) => (<Cell key={i} fill={PALETTE[i % PALETTE.length]} />))}
            </Pie>
            <ChartLegend content={<ChartLegendContent nameKey="genre" />} />
          </PieChart>
        </ChartContainer>
      );
    }
    ```

    **D) `payment-bar-chart.tsx`** (PATTERNS §payment-bar-chart):
    ```typescript
    'use client';
    import { Bar, BarChart, CartesianGrid, XAxis, YAxis } from 'recharts';
    import { ChartContainer, ChartTooltip, ChartTooltipContent, type ChartConfig } from '@/components/ui/chart';
    import type { DashboardPaymentDto } from '@grapit/shared';

    const chartConfig: ChartConfig = { count: { label: '건수', color: 'var(--chart-1)' } };

    interface Props { data: DashboardPaymentDto; }

    export function PaymentBarChart({ data }: Props) {
      return (
        <ChartContainer config={chartConfig} className="h-[280px] w-full">
          <BarChart data={data} accessibilityLayer>
            <CartesianGrid vertical={false} strokeDasharray="3 3" />
            <XAxis dataKey="method" tickLine={false} axisLine={false} className="text-xs" />
            <YAxis tickLine={false} axisLine={false} className="text-xs" />
            <ChartTooltip content={<ChartTooltipContent nameKey="method" />} />
            <Bar dataKey="count" fill="var(--chart-1)" radius={[4, 4, 0, 0]} />
          </BarChart>
        </ChartContainer>
      );
    }
    ```

    **E) `top-performances-table.tsx`** (PATTERNS §top-performances-table, UI-SPEC Copywriting):
    ```typescript
    'use client';
    import { Skeleton } from '@/components/ui/skeleton';
    import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
    import type { DashboardTopDto } from '@grapit/shared';

    interface Props { data: DashboardTopDto | undefined; isLoading: boolean; }

    export function TopPerformancesTable({ data, isLoading }: Props) {
      const rows = data ?? [];
      return (
        <div className="rounded-lg bg-white shadow-sm">
          <Table>
            <TableHeader>
              <TableRow className="bg-[#F5F5F7]">
                <TableHead scope="col" className="text-sm font-semibold text-gray-600 w-12">순위</TableHead>
                <TableHead scope="col" className="text-sm font-semibold text-gray-600 w-16">포스터</TableHead>
                <TableHead scope="col" className="text-sm font-semibold text-gray-600">공연명</TableHead>
                <TableHead scope="col" className="text-sm font-semibold text-gray-600 w-32">장르</TableHead>
                <TableHead scope="col" className="text-sm font-semibold text-gray-600 w-24 text-right">예매수</TableHead>
              </TableRow>
            </TableHeader>
            <TableBody>
              {isLoading && Array.from({ length: 5 }).map((_, i) => (
                <TableRow key={`sk-${i}`}>
                  <TableCell><Skeleton className="h-4 w-6" /></TableCell>
                  <TableCell><Skeleton className="h-10 w-10" /></TableCell>
                  <TableCell><Skeleton className="h-4 w-40" /></TableCell>
                  <TableCell><Skeleton className="h-4 w-16" /></TableCell>
                  <TableCell><Skeleton className="h-4 w-12" /></TableCell>
                </TableRow>
              ))}
              {!isLoading && rows.length === 0 && (
                <TableRow>
                  <TableCell colSpan={5} className="py-12 text-center">
                    <p className="text-base font-semibold text-gray-900">아직 인기 공연이 없습니다</p>
                    <p className="mt-1 text-sm text-gray-600">최근 30일 예매가 누적되면 랭킹이 표시됩니다</p>
                  </TableCell>
                </TableRow>
              )}
              {!isLoading && rows.map((row, i) => (
                <TableRow key={row.performanceId}>
                  <TableCell className="text-sm text-gray-900 font-semibold">{i + 1}</TableCell>
                  <TableCell>
                    {row.posterUrl ? (
                      // eslint-disable-next-line @next/next/no-img-element
                      <img src={row.posterUrl} alt="" className="h-10 w-10 rounded object-cover" />
                    ) : (
                      <div className="h-10 w-10 rounded bg-gray-100" />
                    )}
                  </TableCell>
                  <TableCell className="text-sm text-gray-900">{row.title}</TableCell>
                  <TableCell className="text-sm text-gray-600">{row.genre}</TableCell>
                  <TableCell className="text-sm text-gray-900 text-right">{row.bookingCount.toLocaleString()}건</TableCell>
                </TableRow>
              ))}
            </TableBody>
          </Table>
        </div>
      );
    }
    ```

    **F) `period-filter.tsx`** (PATTERNS §period-filter + UI-SPEC Copywriting):
    ```typescript
    'use client';
    import { ToggleGroup, ToggleGroupItem } from '@/components/ui/toggle-group';
    import type { DashboardPeriod } from '@grapit/shared';

    interface PeriodFilterProps {
      value: DashboardPeriod;
      onChange: (v: DashboardPeriod) => void;
    }

    export function PeriodFilter({ value, onChange }: PeriodFilterProps) {
      return (
        <ToggleGroup
          type="single"
          value={value}
          onValueChange={(v) => { if (v) onChange(v as DashboardPeriod); }}
          aria-label="기간 선택"
        >
          <ToggleGroupItem value="7d">7일</ToggleGroupItem>
          <ToggleGroupItem value="30d">30일</ToggleGroupItem>
          <ToggleGroupItem value="90d">90일</ToggleGroupItem>
        </ToggleGroup>
      );
    }
    ```

    **금지사항:**
    - 서버 컴포넌트에서 recharts/hook import 금지 (Pitfall 3) — 각 파일 최상단 `'use client'` 필수
    - `oklch()` / `hsl(var(--chart-1))` 래핑 금지 — `var(--chart-1)` 직접 사용 (Pitfall 2)
    - 장르/결제수단 label을 영문 그대로 표시 (e.g. 'musical')해도 됨. 번역 맵은 out of scope (v2 ADM-08과 함께).
    - `any` 타입 사용 금지
    - 이미지는 `<img>` (기존 booking 테이블 패턴 답습). Next.js Image 컴포넌트는 remotePatterns 설정 의존 — 기존 pattern 따름.
    - 5개 차트 컴포넌트 중 어느 하나라도 `'use client'` 누락 시 build 실패 예상
  </action>
  <verify>
    <automated>pnpm --filter @grapit/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/hooks/use-admin-dashboard.ts` 존재, 라인 수 ≥ 55
    - grep `export function useDashboardSummary\|useDashboardRevenue\|useDashboardGenre\|useDashboardPayment\|useDashboardTop10` → 5개 모두 match
    - grep `staleTime.*30_000\|staleTime.*30000` match
    - grep `refetchOnWindowFocus:\s*false` match
    - 5개 dashboard 컴포넌트 파일 모두 존재 (`revenue-area-chart.tsx`, `genre-donut-chart.tsx`, `payment-bar-chart.tsx`, `top-performances-table.tsx`, `period-filter.tsx`)
    - 5개 파일 모두 최상단에 `'use client';` 존재
    - `grep -l "var(--chart-1)" apps/web/components/admin/dashboard/*.tsx` → 최소 3개 (area/donut/bar)
    - `top-performances-table.tsx`에 copy `아직 인기 공연이 없습니다` / `최근 30일 예매가 누적되면 랭킹이 표시됩니다` 모두 포함
    - `period-filter.tsx`에 `aria-label="기간 선택"`, `7일`, `30일`, `90일` 포함
    - `pnpm --filter @grapit/web typecheck` → 0 exit
  </acceptance_criteria>
  <done>
    5개 컴포넌트 + 1 훅 모두 typecheck 통과. Task 03-03의 page composition에서 즉시 사용 가능.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-03: /admin/page.tsx 대시보드 composition + Playwright E2E GREEN</name>
  <read_first>
    - apps/web/app/admin/bookings/page.tsx (thin page shell analog)
    - apps/web/components/admin/admin-booking-dashboard.tsx (KPI grid + section composition 패턴)
    - apps/web/components/admin/admin-stat-card.tsx (props shape)
    - .planning/phases/11-admin-dashboard/11-UI-SPEC.md §Layout Contract + §Copywriting
    - .planning/phases/11-admin-dashboard/11-PATTERNS.md §app/admin/page.tsx
    - apps/web/e2e/admin-dashboard.spec.ts (Plan 01 산출 — 이 테스트가 GREEN이 되도록 page composition 설계)
  </read_first>
  <behavior>
    - `/admin` 접근 시 렌더, layout.tsx의 기존 admin role guard 자동 적용
    - 페이지 상단 `<h1>대시보드</h1>` (text-xl font-semibold text-gray-900 mb-6)
    - KPI 4장 grid-cols-2 lg:grid-cols-4 gap-4
    - 기간 필터 + 매출 추이 area chart (flex justify-between 헤더 + 차트)
    - 장르 donut + 결제수단 bar (grid-cols-1 lg:grid-cols-2 gap-6)
    - Top 10 table (fixed 30d, period filter 영향 안 받음)
    - 로딩/에러/빈 상태 각 섹션별 처리 (UI-SPEC Interaction Contract)
    - Playwright E2E 3개 테스트 (landing-smoke / period-filter / sidebar-nav) 전부 GREEN
  </behavior>
  <files>
    - apps/web/app/admin/page.tsx (신규)
  </files>
  <action>
    파일 신규 작성:
    ```typescript
    'use client';

    import { useState } from 'react';
    import { Ticket, Banknote, RotateCcw, Theater, TrendingUp, PieChart as PieIcon, CreditCard, Trophy } from 'lucide-react';
    import { AdminStatCard } from '@/components/admin/admin-stat-card';
    import { Skeleton } from '@/components/ui/skeleton';
    import { Button } from '@/components/ui/button';
    import { PeriodFilter } from '@/components/admin/dashboard/period-filter';
    import { RevenueAreaChart } from '@/components/admin/dashboard/revenue-area-chart';
    import { GenreDonutChart } from '@/components/admin/dashboard/genre-donut-chart';
    import { PaymentBarChart } from '@/components/admin/dashboard/payment-bar-chart';
    import { TopPerformancesTable } from '@/components/admin/dashboard/top-performances-table';
    import {
      useDashboardSummary,
      useDashboardRevenue,
      useDashboardGenre,
      useDashboardPayment,
      useDashboardTop10,
    } from '@/hooks/use-admin-dashboard';
    import type { DashboardPeriod } from '@grapit/shared';

    const PERIOD_LABEL: Record<DashboardPeriod, string> = {
      '7d': '7일',
      '30d': '30일',
      '90d': '90일',
    };

    export default function AdminDashboardPage() {
      const [period, setPeriod] = useState<DashboardPeriod>('30d');

      const summary = useDashboardSummary();
      const revenue = useDashboardRevenue(period);
      const genre = useDashboardGenre(period);
      const payment = useDashboardPayment(period);
      const top10 = useDashboardTop10();

      return (
        <div className="space-y-8">
          <header>
            <h1 className="mb-2 text-xl font-semibold text-gray-900">대시보드</h1>
            <p className="text-xs text-gray-600">오늘의 예매·매출 현황과 최근 추이를 확인하세요</p>
          </header>

          {/* KPI row (ADM-01) */}
          <section aria-labelledby="kpi-heading">
            <h2 id="kpi-heading" className="sr-only">오늘의 요약</h2>
            <div className="grid grid-cols-2 gap-4 lg:grid-cols-4">
              {summary.isLoading ? (
                Array.from({ length: 4 }).map((_, i) => <Skeleton key={i} className="h-24 w-full" />)
              ) : summary.isError ? (
                <div className="col-span-full rounded-lg bg-white p-6 shadow-sm">
                  <p className="text-base font-semibold text-gray-900">대시보드를 불러오지 못했습니다</p>
                  <p className="mt-1 text-sm text-gray-600">잠시 후 다시 시도해주세요. 문제가 계속되면 관리자에게 문의하세요.</p>
                  <Button onClick={() => summary.refetch()} variant="outline" className="mt-3">다시 시도</Button>
                </div>
              ) : (
                <>
                  <AdminStatCard icon={Ticket} label="오늘 예매수" value={summary.data?.todayBookings ?? 0} format="count" />
                  <AdminStatCard icon={Banknote} label="오늘 매출" value={summary.data?.todayRevenue ?? 0} format="currency" />
                  <AdminStatCard icon={RotateCcw} label="오늘 취소" value={summary.data?.todayCancelled ?? 0} format="count" />
                  <AdminStatCard icon={Theater} label="활성 공연" value={summary.data?.activePerformances ?? 0} format="count" />
                </>
              )}
            </div>
          </section>

          {/* Revenue area (ADM-02) */}
          <section aria-labelledby="revenue-heading" className="rounded-lg bg-white p-6 shadow-sm">
            <div className="mb-4 flex items-center justify-between">
              <div className="flex items-center gap-2">
                <TrendingUp className="h-5 w-5 text-gray-600" aria-hidden="true" />
                <h2 id="revenue-heading" className="text-sm font-semibold text-gray-900">매출 추이</h2>
              </div>
              <PeriodFilter value={period} onChange={setPeriod} />
            </div>
            <p className="mb-4 text-xs text-gray-600">{`최근 ${PERIOD_LABEL[period]} 기준`}</p>
            {revenue.isLoading ? (
              <Skeleton className="h-[280px] w-full" />
            ) : revenue.isError ? (
              <div className="flex h-[280px] items-center justify-center">
                <div className="text-center">
                  <p className="text-base font-semibold text-gray-900">대시보드를 불러오지 못했습니다</p>
                  <p className="mt-1 text-sm text-gray-600">잠시 후 다시 시도해주세요.</p>
                  <Button onClick={() => revenue.refetch()} variant="outline" className="mt-3">다시 시도</Button>
                </div>
              </div>
            ) : !revenue.data || revenue.data.length === 0 ? (
              <div className="flex h-[280px] items-center justify-center">
                <div className="text-center">
                  <p className="text-base font-semibold text-gray-900">표시할 데이터가 없습니다</p>
                  <p className="mt-1 text-sm text-gray-600">해당 기간 동안 예매 내역이 없습니다</p>
                </div>
              </div>
            ) : (
              <RevenueAreaChart data={revenue.data} />
            )}
          </section>

          {/* Genre + Payment row (ADM-03 + ADM-05) */}
          <div className="grid grid-cols-1 gap-6 lg:grid-cols-2">
            <section aria-labelledby="genre-heading" className="rounded-lg bg-white p-6 shadow-sm">
              <div className="mb-4 flex items-center gap-2">
                <PieIcon className="h-5 w-5 text-gray-600" aria-hidden="true" />
                <h2 id="genre-heading" className="text-sm font-semibold text-gray-900">장르별 예매 분포</h2>
              </div>
              {genre.isLoading ? (
                <Skeleton className="h-[280px] w-full" />
              ) : genre.isError || !genre.data || genre.data.length === 0 ? (
                <div className="flex h-[280px] items-center justify-center">
                  <div className="text-center">
                    <p className="text-base font-semibold text-gray-900">표시할 데이터가 없습니다</p>
                    <p className="mt-1 text-sm text-gray-600">해당 기간 동안 예매 내역이 없습니다</p>
                  </div>
                </div>
              ) : (
                <GenreDonutChart data={genre.data} />
              )}
            </section>

            <section aria-labelledby="payment-heading" className="rounded-lg bg-white p-6 shadow-sm">
              <div className="mb-4 flex items-center gap-2">
                <CreditCard className="h-5 w-5 text-gray-600" aria-hidden="true" />
                <h2 id="payment-heading" className="text-sm font-semibold text-gray-900">결제수단 분포</h2>
              </div>
              {payment.isLoading ? (
                <Skeleton className="h-[280px] w-full" />
              ) : payment.isError || !payment.data || payment.data.length === 0 ? (
                <div className="flex h-[280px] items-center justify-center">
                  <div className="text-center">
                    <p className="text-base font-semibold text-gray-900">표시할 데이터가 없습니다</p>
                    <p className="mt-1 text-sm text-gray-600">해당 기간 동안 결제 내역이 없습니다</p>
                  </div>
                </div>
              ) : (
                <PaymentBarChart data={payment.data} />
              )}
            </section>
          </div>

          {/* Top 10 (ADM-04) */}
          <section aria-labelledby="top10-heading" className="rounded-lg bg-white p-6 shadow-sm">
            <div className="mb-2 flex items-center gap-2">
              <Trophy className="h-5 w-5 text-gray-600" aria-hidden="true" />
              <h2 id="top10-heading" className="text-sm font-semibold text-gray-900">인기 공연 Top 10</h2>
            </div>
            <p className="mb-4 text-xs text-gray-600">최근 30일 예매 건수 기준</p>
            <TopPerformancesTable data={top10.data} isLoading={top10.isLoading} />
          </section>
        </div>
      );
    }
    ```

    **E2E 실행 확인:**
    ```bash
    # apps/api 기동 (백그라운드): cd apps/api && pnpm start:dev
    # apps/web 기동 (백그라운드): cd apps/web && pnpm dev
    # 혹은 Playwright config의 webServer 설정이 자동 기동
    pnpm --filter @grapit/web test:e2e -- admin-dashboard.spec.ts
    ```

    3개 테스트 (landing-smoke / period-filter / sidebar-nav) 모두 GREEN 기대.

    **UI-SPEC 준수 self-check (구현 후):**
    - Typography: `text-xl`, `text-sm`, `text-xs` 3개만 사용 (`text-lg`, `text-2xl`, `font-medium`, `font-bold` 금지)
    - Color: `text-gray-900`, `text-gray-600`, `bg-white`, `bg-gray-100` (destructive 미사용 OK)
    - Spacing: `gap-4`, `gap-6`, `p-6`, `mb-2`, `mb-4` (8pt grid)
    - Copywriting: 모든 카피가 UI-SPEC Copywriting Contract verbatim

    **금지사항:**
    - `'use client'` 누락 금지 (hook 사용으로 필수)
    - `router.push` 또는 `next/navigation` 임포트 불필요 (read-only 페이지, navigation 액션 없음)
    - `destructive` / `AlertDialog` 금지 (UI-SPEC "Destructive actions: None in this phase")
    - TanStack Query `refetchInterval` 금지 (60s TTL로 충분)
    - `text-2xl`, `text-3xl`, `text-base`, `text-lg`, `font-medium`, `font-bold` 사용 금지 (UI-SPEC Typography 엄격 제한)
  </action>
  <verify>
    <automated>pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web lint apps/web/app/admin/page.tsx apps/web/components/admin/dashboard 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/admin/page.tsx` 존재, 라인 수 ≥ 130
    - 파일 최상단에 `'use client';`
    - grep `export default function AdminDashboardPage` match
    - grep `useDashboardSummary\|useDashboardRevenue\|useDashboardGenre\|useDashboardPayment\|useDashboardTop10` → 5개 모두 호출됨
    - grep `'대시보드'` (h1 카피) match
    - grep `'오늘 예매수'` / `'오늘 매출'` / `'오늘 취소'` / `'활성 공연'` 4개 모두 match (UI-SPEC Copywriting verbatim)
    - grep `'매출 추이'` / `'장르별 예매 분포'` / `'결제수단 분포'` / `'인기 공연 Top 10'` 4개 모두 match
    - grep `'최근 30일 예매 건수 기준'` match (Top 10 subcopy fixed D-10)
    - grep `useState<DashboardPeriod>\('30d'\)` match (기본값 D-09)
    - grep `text-2xl\|text-3xl\|text-base\|text-lg\|font-medium\|font-bold` 결과 없음 (UI-SPEC Typography 위반 없음)
    - `pnpm --filter @grapit/web typecheck` → 0 exit
    - `pnpm --filter @grapit/web lint` → 0 exit (기존 경고는 유지, 신규 파일의 에러 0)
  </acceptance_criteria>
  <done>
    `/admin`이 대시보드를 렌더. E2E 3개 테스트 GREEN을 목표로 완전한 composition 완성.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /admin 페이지 | admin 역할 사용자만 layout.tsx guard 통과 (기존) |
| Browser → /api/v1/admin/dashboard/* | 쿠키 기반 인증, Plan 02 backend가 RolesGuard로 403 처리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Elevation of Privilege | /admin 페이지 접근 | accept | 기존 `apps/web/app/admin/layout.tsx`의 useAuthStore guard 재사용 — 비관리자는 `/`로 redirect. 본 플랜은 guard 수정 없음. |
| T-11-09 | Information Disclosure | Top 10 posterUrl CDN URL | accept | posterUrl은 이미 public CDN (Phase 8 완료). 추가 노출 위험 없음. |
| T-11-10 | Tampering | period state (client-side) | accept | 사용자가 devtools로 `'invalid'` 주입해도 백엔드 zod enum에서 400 반환. FE 방어는 선택적 — 신뢰 경계는 API. |

</threat_model>

<verification>
Wave 2 Web 완료 확인:
1. `pnpm --filter @grapit/web typecheck` 0 exit
2. `pnpm --filter @grapit/web lint` 0 exit (새 파일 에러 없음)
3. Dev 서버 수동 확인:
   ```bash
   cd apps/web && pnpm dev
   # 브라우저: http://localhost:3000/admin
   # → 대시보드 렌더, 7/30/90일 토글 작동, 차트 3종 + Top 10 표시
   ```
4. 차트 blank 회귀 확인 (Pitfall 1): 페이지 로드 후 각 차트 영역에 `<path>`/`<rect>`/`<circle>` SVG 자식 렌더 확인 — 빈 `<svg>`만 있으면 react-is override 필요
5. grep 검증:
   - `rg "'use client'" apps/web/components/admin/dashboard/` → 5개 파일 모두 match
   - `rg "var\(--chart-" apps/web/components/admin/dashboard/` → 3개 차트 파일에 match
</verification>

<success_criteria>
- [ ] shadcn chart + toggle-group 설치, recharts 3.8.x pinned
- [ ] globals.css의 --chart-1~5이 purple monochromatic으로 override
- [ ] admin-sidebar에 대시보드 NAV + 로고 href 전환 + isActive exact match
- [ ] use-admin-dashboard.ts 5개 훅 (staleTime 30s)
- [ ] 5개 dashboard 컴포넌트 (chart 3 + table + filter) 모두 'use client' + typecheck 통과
- [ ] /admin/page.tsx가 모든 섹션 조립, UI-SPEC typography/color 엄격 준수
- [ ] Plan 01 E2E 3개 테스트 (landing-smoke/period-filter/sidebar-nav) GREEN 전환
</success_criteria>

<output>
완료 후 `.planning/phases/11-admin-dashboard/11-03-SUMMARY.md` 생성.
</output>
