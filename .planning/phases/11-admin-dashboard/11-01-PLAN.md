---
phase: 11-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/admin-dashboard.schema.ts
  - packages/shared/src/types/admin-dashboard.types.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/admin/admin-dashboard.service.ts
  - apps/api/src/modules/admin/admin-dashboard.controller.ts
  - apps/api/src/modules/admin/__tests__/admin-dashboard.service.spec.ts
  - apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts
  - apps/api/test/admin-dashboard.integration.spec.ts
  - apps/web/e2e/admin-dashboard.spec.ts
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06]
tags: [admin, dashboard, testing, zod]
must_haves:
  truths:
    - "DashboardPeriod zod enum + periodQuerySchema가 @grapit/shared에서 export되어 API/FE 모두 import 가능"
    - "AdminDashboardService 및 AdminDashboardController skeleton 파일이 존재하고 각 public method/handler는 `throw new Error('Not implemented')` (GREEN은 Plan 02의 몫)"
    - "admin-dashboard.service.spec.ts가 RED 상태로 존재 (describe/it 구조 + summary/kst-boundary/revenue-weekly/genre/payment/cache-hit/cache-set-ttl/cache-degradation 8개 테스트 모두 .skip 없이 assertion 실패)"
    - "admin-dashboard.controller.spec.ts가 RED 상태로 존재 (unauth→401 / user role→403 / admin role→200 3개 access control 테스트 모두 assertion 실패)"
    - "admin-dashboard.integration.spec.ts가 RED 상태로 존재 (Postgres testcontainer + Valkey testcontainer + revenue-daily/top10 2개 테스트)"
    - "admin-dashboard.spec.ts(e2e)가 RED 상태로 존재 (landing-smoke/period-filter/sidebar-nav 3개 테스트, admin@grapit.test 로그인 사용)"
  artifacts:
    - path: "packages/shared/src/schemas/admin-dashboard.schema.ts"
      provides: "dashboardPeriodSchema (z.enum ['7d','30d','90d']) + periodQuerySchema (default '30d')"
      exports: ["dashboardPeriodSchema", "DashboardPeriod", "periodQuerySchema"]
    - path: "packages/shared/src/types/admin-dashboard.types.ts"
      provides: "5개 응답 DTO 타입"
      exports: ["DashboardSummaryDto", "DashboardRevenueDto", "DashboardGenreDto", "DashboardPaymentDto", "DashboardTopDto"]
    - path: "packages/shared/src/index.ts"
      provides: "새 schema/types re-export 추가"
      contains: "export * from './schemas/admin-dashboard.schema'"
    - path: "apps/api/src/modules/admin/admin-dashboard.service.ts"
      provides: "AdminDashboardService skeleton — 모든 public method가 throw new Error('Not implemented')"
      exports: ["AdminDashboardService"]
      min_lines: 35
    - path: "apps/api/src/modules/admin/admin-dashboard.controller.ts"
      provides: "AdminDashboardController skeleton — 모든 handler가 throw new Error('Not implemented'), RolesGuard + @Roles('admin') 적용"
      exports: ["AdminDashboardController"]
      min_lines: 40
    - path: "apps/api/src/modules/admin/__tests__/admin-dashboard.service.spec.ts"
      provides: "RED unit tests covering ADM-01~06"
      min_lines: 150
    - path: "apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts"
      provides: "RED access control tests (unauth / user / admin)"
      min_lines: 60
    - path: "apps/api/test/admin-dashboard.integration.spec.ts"
      provides: "RED integration tests with Postgres + Valkey testcontainers"
      min_lines: 100
    - path: "apps/web/e2e/admin-dashboard.spec.ts"
      provides: "RED Playwright E2E covering sidebar/landing/period-filter"
      min_lines: 60
  key_links:
    - from: "apps/web/e2e/admin-dashboard.spec.ts"
      to: "apps/web/e2e/helpers/auth.ts"
      via: "loginAsTestUser import (admin@grapit.test seed)"
      pattern: "loginAsTestUser"
    - from: "apps/api/test/admin-dashboard.integration.spec.ts"
      to: "testcontainers GenericContainer + postgres:16 + valkey/valkey:8"
      via: "beforeAll container startup"
      pattern: "new GenericContainer"
    - from: "apps/api/src/modules/admin/__tests__/admin-dashboard.service.spec.ts"
      to: "packages/shared admin-dashboard.schema"
      via: "type import of DashboardPeriod"
      pattern: "import type { DashboardPeriod"
    - from: "apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts"
      to: "apps/api/src/common/guards/roles.guard.ts"
      via: "Test.createTestingModule + RolesGuard override simulating user roles"
      pattern: "RolesGuard"
---

<objective>
Wave 0 (RED): 대시보드 구현 전에 shared zod schema + 응답 DTO 타입 + service/controller skeleton + 모든 자동 검증 테스트(RED)를 먼저 커밋한다. Skeleton 파일은 공개 메서드/핸들러가 `throw new Error('Not implemented')`라서 테스트가 **module-not-found가 아닌 assertion failure**로 실패한다. Plan 02는 이 skeleton을 실제 구현으로 채워 GREEN으로 전환한다.

Purpose:
- Plan 02/03이 동일 Wave 2에서 병렬 실행되려면 공유 타입 계약 + skeleton이 먼저 존재해야 함 (Interface-First 원칙)
- TDD 원칙: 테스트 파일(RED)이 먼저 커밋되어야 실패가 관측 가능하고 GREEN 구현의 완료 기준이 명확해짐. 단, **module-not-found 대신 assertion failure로 RED를 만든다** (review HIGH 1).
- Admin API access control(401/403/200) 자동 테스트가 Wave 1에서 RED로 존재해야 함 (review HIGH 3).
- admin@grapit.test seed 유저는 이미 존재 (STATE.md 260413-jw1 quick task 참조)

Output:
- packages/shared에 admin-dashboard schema + DTO types
- AdminDashboardService / AdminDashboardController **skeleton** (public method는 `throw new Error('Not implemented')`)
- API unit spec / controller access-control spec / integration spec (testcontainers) / Web E2E spec — 모두 RED (assertion failure 기반)
- VALIDATION.md의 per-task verification map 항목 중 모든 `<automated>` 커맨드가 파일에는 존재하되 RED로 실패
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-dashboard/11-CONTEXT.md
@.planning/phases/11-admin-dashboard/11-RESEARCH.md
@.planning/phases/11-admin-dashboard/11-PATTERNS.md
@.planning/phases/11-admin-dashboard/11-VALIDATION.md

@packages/shared/src/index.ts
@packages/shared/src/schemas/booking.schema.ts
@apps/api/src/modules/admin/admin-booking.service.spec.ts
@apps/api/src/modules/admin/admin-booking.controller.ts
@apps/api/src/common/guards/roles.guard.ts
@apps/api/src/common/decorators/roles.decorator.ts
@apps/api/test/sms-throttle.integration.spec.ts
@apps/web/e2e/toss-payment.spec.ts
@apps/web/e2e/helpers/auth.ts

<interfaces>
<!-- Plan 02/03이 이 플랜 산출물을 consume함. 핵심 계약: -->

shared schema (packages/shared/src/schemas/admin-dashboard.schema.ts):
```typescript
import { z } from 'zod';

export const dashboardPeriodSchema = z.enum(['7d', '30d', '90d']);
export type DashboardPeriod = z.infer<typeof dashboardPeriodSchema>;

export const periodQuerySchema = z.object({
  period: dashboardPeriodSchema.default('30d'),
});
```

shared DTO types (packages/shared/src/types/admin-dashboard.types.ts):
```typescript
export interface DashboardSummaryDto {
  todayBookings: number;
  todayRevenue: number;
  todayCancelled: number;
  activePerformances: number;
}

export interface DashboardRevenueBucketDto {
  bucket: string;   // 'YYYY-MM-DD' for day, 'IYYY-"W"IW' for week
  revenue: number;
  count: number;
}
export type DashboardRevenueDto = DashboardRevenueBucketDto[];

export interface DashboardGenreBucketDto {
  genre: string;
  count: number;
}
export type DashboardGenreDto = DashboardGenreBucketDto[];

export interface DashboardPaymentBucketDto {
  method: string;
  count: number;
}
export type DashboardPaymentDto = DashboardPaymentBucketDto[];

export interface DashboardTopPerformanceDto {
  performanceId: string;
  title: string;
  genre: string;
  posterUrl: string | null;
  bookingCount: number;
}
export type DashboardTopDto = DashboardTopPerformanceDto[];
```

index.ts re-export 추가:
```typescript
export * from './schemas/admin-dashboard.schema';
export * from './types/admin-dashboard.types';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 01-01: shared admin-dashboard schema + DTO types 작성 및 index re-export</name>
  <read_first>
    - packages/shared/src/index.ts (현재 re-export 패턴 확인)
    - packages/shared/src/schemas/booking.schema.ts (zod 패턴 analog)
    - packages/shared/src/types/booking.types.ts (존재하면 DTO 스타일)
    - .planning/phases/11-admin-dashboard/11-CONTEXT.md §Decisions D-09, D-14 (period enum + 키 네임스페이스)
  </read_first>
  <files>
    - packages/shared/src/schemas/admin-dashboard.schema.ts (신규)
    - packages/shared/src/types/admin-dashboard.types.ts (신규)
    - packages/shared/src/index.ts (수정, 2줄 추가)
  </files>
  <action>
    1) `packages/shared/src/schemas/admin-dashboard.schema.ts` 신규 작성. 정확한 내용:
    ```typescript
    import { z } from 'zod';

    export const dashboardPeriodSchema = z.enum(['7d', '30d', '90d']);
    export type DashboardPeriod = z.infer<typeof dashboardPeriodSchema>;

    export const periodQuerySchema = z.object({
      period: dashboardPeriodSchema.default('30d'),
    });
    ```
    (D-09 per user decision: 7/30/90일 세그먼트 3단, 기본 30일)

    2) `packages/shared/src/types/admin-dashboard.types.ts` 신규 작성. 정확한 내용:
    ```typescript
    export interface DashboardSummaryDto {
      todayBookings: number;
      todayRevenue: number;
      todayCancelled: number;
      activePerformances: number;
    }

    export interface DashboardRevenueBucketDto {
      bucket: string;
      revenue: number;
      count: number;
    }
    export type DashboardRevenueDto = DashboardRevenueBucketDto[];

    export interface DashboardGenreBucketDto {
      genre: string;
      count: number;
    }
    export type DashboardGenreDto = DashboardGenreBucketDto[];

    export interface DashboardPaymentBucketDto {
      method: string;
      count: number;
    }
    export type DashboardPaymentDto = DashboardPaymentBucketDto[];

    export interface DashboardTopPerformanceDto {
      performanceId: string;
      title: string;
      genre: string;
      posterUrl: string | null;
      bookingCount: number;
    }
    export type DashboardTopDto = DashboardTopPerformanceDto[];
    ```

    3) `packages/shared/src/index.ts`에 다음 두 줄 추가 (기존 `export * from './schemas/booking.schema';` 아래, 기존 `export * from './types/booking.types';` 아래):
    ```typescript
    export * from './schemas/admin-dashboard.schema';
    export * from './types/admin-dashboard.types';
    ```

    **금지사항:**
    - class-validator / class-transformer import 금지 (프로젝트 전체 zod 표준, CLAUDE.md "What NOT to Use")
    - `any` 타입 금지 (CLAUDE.md strict typing)
    - period 기본값을 `'30d'` 이외의 값으로 바꾸지 말 것 (D-09)
    - enum 순서 `['7d','30d','90d']` 유지 (UI-SPEC Copywriting 순서와 일치)
  </action>
  <verify>
    <automated>pnpm --filter @grapit/shared build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/schemas/admin-dashboard.schema.ts` 파일이 존재하고 `dashboardPeriodSchema` / `DashboardPeriod` / `periodQuerySchema` 세 개를 export한다 (grep `export const dashboardPeriodSchema` / `export type DashboardPeriod` / `export const periodQuerySchema` 모두 match)
    - `packages/shared/src/types/admin-dashboard.types.ts` 파일이 존재하고 `DashboardSummaryDto`, `DashboardRevenueDto`, `DashboardGenreDto`, `DashboardPaymentDto`, `DashboardTopDto` 다섯 타입을 export한다
    - `packages/shared/src/index.ts`가 `export * from './schemas/admin-dashboard.schema';` 라인을 포함한다
    - `packages/shared/src/index.ts`가 `export * from './types/admin-dashboard.types';` 라인을 포함한다
    - `pnpm --filter @grapit/shared build` 가 0 exit code로 성공
    - `grep -r "class-validator\|class-transformer" packages/shared/src/schemas/admin-dashboard.schema.ts packages/shared/src/types/admin-dashboard.types.ts` 결과 없음
  </acceptance_criteria>
  <done>
    @grapit/shared에서 DashboardPeriod / periodQuerySchema / 5개 DTO 타입이 export되며 typecheck 통과. Plan 02/03에서 `import { ... } from '@grapit/shared'` 로 consume 가능.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 01-02: AdminDashboardService / Controller skeleton (throw Not implemented)</name>
  <read_first>
    - apps/api/src/modules/admin/admin-booking.service.ts (DI 패턴 확인: `@Inject(DRIZZLE)` + `CacheService` 주입)
    - apps/api/src/modules/admin/admin-booking.controller.ts (`@Controller('admin')` + `@UseGuards(RolesGuard)` + `@Roles('admin')` + `ZodValidationPipe` 패턴)
    - apps/api/src/common/guards/roles.guard.ts (guard 시그니처)
    - apps/api/src/common/decorators/roles.decorator.ts
    - apps/api/src/common/pipes/zod-validation.pipe.ts (pipe 사용법)
    - apps/api/src/database/drizzle.provider.ts (DRIZZLE 토큰 / DrizzleDB 타입)
    - apps/api/src/modules/performance/cache.service.ts (CacheService 시그니처)
    - .planning/phases/11-admin-dashboard/11-CONTEXT.md §Decisions D-09/D-14
  </read_first>
  <files>
    - apps/api/src/modules/admin/admin-dashboard.service.ts (신규 — skeleton)
    - apps/api/src/modules/admin/admin-dashboard.controller.ts (신규 — skeleton)
  </files>
  <action>
    이 task의 목적은 **module-not-found RED**를 피하고 **assertion-failure RED**로 만드는 것이다. Skeleton은 "잘못된" 동작을 보장해야 한다 — 모든 public method/handler가 `throw new Error('Not implemented')`. Plan 02가 이 파일들을 실제 구현으로 교체한다.

    **A) `apps/api/src/modules/admin/admin-dashboard.service.ts` 신규 작성 (skeleton):**
    ```typescript
    import { Injectable, Inject } from '@nestjs/common';
    import type {
      DashboardSummaryDto,
      DashboardRevenueDto,
      DashboardGenreDto,
      DashboardPaymentDto,
      DashboardTopDto,
      DashboardPeriod,
    } from '@grapit/shared';
    import { DRIZZLE, type DrizzleDB } from '../../database/drizzle.provider.js';
    import { CacheService } from '../performance/cache.service.js';

    /**
     * RED skeleton — Plan 02(Task 02-01)이 이 메서드들을 실제 구현으로 교체한다.
     * 이 상태에서 spec 파일들은 module-not-found가 아닌 assertion failure로 RED가 된다.
     */
    @Injectable()
    export class AdminDashboardService {
      constructor(
        @Inject(DRIZZLE) private readonly db: DrizzleDB,
        private readonly cache: CacheService,
      ) {
        // db/cache는 Plan 02에서 사용. skeleton 단계에서는 reference만 보관.
        void this.db;
        void this.cache;
      }

      async getSummary(): Promise<DashboardSummaryDto> {
        throw new Error('Not implemented');
      }

      async getRevenueTrend(_period: DashboardPeriod): Promise<DashboardRevenueDto> {
        throw new Error('Not implemented');
      }

      async getGenreDistribution(_period: DashboardPeriod): Promise<DashboardGenreDto> {
        throw new Error('Not implemented');
      }

      async getPaymentDistribution(_period: DashboardPeriod): Promise<DashboardPaymentDto> {
        throw new Error('Not implemented');
      }

      async getTopPerformances(): Promise<DashboardTopDto> {
        throw new Error('Not implemented');
      }
    }
    ```

    **B) `apps/api/src/modules/admin/admin-dashboard.controller.ts` 신규 작성 (skeleton):**
    ```typescript
    import { Controller, Get, Query, UseGuards } from '@nestjs/common';
    import { periodQuerySchema } from '@grapit/shared';
    import type { DashboardPeriod } from '@grapit/shared';
    import { ZodValidationPipe } from '../../common/pipes/zod-validation.pipe.js';
    import { RolesGuard } from '../../common/guards/roles.guard.js';
    import { Roles } from '../../common/decorators/roles.decorator.js';
    import { AdminDashboardService } from './admin-dashboard.service.js';

    /**
     * RED skeleton — Plan 02(Task 02-02)가 handler body를 실제 구현으로 교체한다.
     * RolesGuard + @Roles('admin') 데코레이터는 skeleton에서도 반드시 존재해야 한다.
     * (controller spec의 access-control 테스트가 401/403/200을 assertion failure로 검증)
     */
    @Controller('admin/dashboard')
    @UseGuards(RolesGuard)
    @Roles('admin')
    export class AdminDashboardController {
      constructor(private readonly service: AdminDashboardService) {
        void this.service;
      }

      @Get('summary')
      async getSummary(): Promise<never> {
        throw new Error('Not implemented');
      }

      @Get('revenue')
      async getRevenue(
        @Query(new ZodValidationPipe(periodQuerySchema)) _query: { period: DashboardPeriod },
      ): Promise<never> {
        throw new Error('Not implemented');
      }

      @Get('genre')
      async getGenre(
        @Query(new ZodValidationPipe(periodQuerySchema)) _query: { period: DashboardPeriod },
      ): Promise<never> {
        throw new Error('Not implemented');
      }

      @Get('payment')
      async getPayment(
        @Query(new ZodValidationPipe(periodQuerySchema)) _query: { period: DashboardPeriod },
      ): Promise<never> {
        throw new Error('Not implemented');
      }

      @Get('top-performances')
      async getTopPerformances(): Promise<never> {
        throw new Error('Not implemented');
      }
    }
    ```

    **AdminModule 등록은 Plan 02(Task 02-02)에서 수행.** 이 task는 skeleton 파일만 생성하며 module wiring은 건드리지 않는다. (Plan 01 커밋이 controller를 모듈에 등록하지 않아도 spec은 `Test.createTestingModule`로 직접 로드하므로 문제 없음.)

    **금지사항:**
    - method body에 실제 Drizzle/Cache 로직을 작성하지 말 것 (Plan 02의 몫)
    - `any` 타입 사용 금지 (CLAUDE.md strict typing). `_param` prefix로 unused 경고 회피.
    - `class-validator` decorator 사용 금지 (zod 표준)
    - `@Roles('admin')` 데코레이터 및 `@UseGuards(RolesGuard)` 제거 금지 — controller spec이 이를 기반으로 access control을 검증
    - `admin.module.ts` 수정 금지 (Plan 02 Task 02-02가 담당)
  </action>
  <verify>
    <automated>pnpm --filter @grapit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `apps/api/src/modules/admin/admin-dashboard.service.ts` 존재, 라인 수 ≥ 35
    - 파일 `apps/api/src/modules/admin/admin-dashboard.controller.ts` 존재, 라인 수 ≥ 40
    - service grep: `export class AdminDashboardService` match
    - service grep: `throw new Error\('Not implemented'\)` 최소 5회 match (5개 public method)
    - controller grep: `@Controller\('admin/dashboard'\)` match
    - controller grep: `@UseGuards\(RolesGuard\)` match
    - controller grep: `@Roles\('admin'\)` match
    - controller grep: `@Get\('summary'\)` / `@Get\('revenue'\)` / `@Get\('genre'\)` / `@Get\('payment'\)` / `@Get\('top-performances'\)` 5개 모두 match
    - controller grep: `throw new Error\('Not implemented'\)` 최소 5회 match
    - `pnpm --filter @grapit/api typecheck` 0 exit
    - admin.module.ts 변경 없음 (`git diff --stat apps/api/src/modules/admin/admin.module.ts` 빈 결과)
  </acceptance_criteria>
  <done>
    Service/Controller skeleton 존재. import는 resolve되므로 후속 spec들이 module-not-found가 아닌 assertion failure로 RED가 됨. Plan 02가 handler body를 채워 GREEN으로 전환.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03: admin-dashboard.service.spec.ts (RED unit tests — assertion failure)</name>
  <read_first>
    - apps/api/src/modules/admin/admin-booking.service.spec.ts (mock Drizzle + proxy chainable 패턴 analog)
    - apps/api/src/modules/performance/__tests__/cache.service.spec.ts (cache mock 패턴 analog)
    - apps/api/src/modules/admin/admin-dashboard.service.ts (Task 01-02 산출 — skeleton이 존재함을 확인)
    - .planning/phases/11-admin-dashboard/11-PATTERNS.md §admin-dashboard.service.spec.ts (PATTERNS가 지정한 테스트 shape)
    - .planning/phases/11-admin-dashboard/11-VALIDATION.md §Per-Task Verification Map (8개 unit 테스트 요구사항)
    - .planning/phases/11-admin-dashboard/11-RESEARCH.md §Code Example 1 (getSummary 서명) §Example 2 (getRevenueTrend 서명)
  </read_first>
  <behavior>
    이 스펙은 RED이지만 **테스트 이름과 기대값**은 최종 구현과 동일해야 함. GREEN 단계(Plan 02)에서 service 구현이 이 테스트를 통과시킨다. Skeleton(Task 01-02)이 존재하므로 `import { AdminDashboardService }`가 resolve되고, 각 테스트는 `throw new Error('Not implemented')`로 인한 **assertion failure**로 RED가 된다(module-not-found 아님).
    - Test 1 (`summary`): getSummary()가 `{ todayBookings, todayRevenue, todayCancelled, activePerformances }` 4개 숫자 필드 반환 (mock DB: bookings=10, revenue=150000, cancelled=1, active=3)
    - Test 2 (`kst-boundary`): getSummary()가 KST 자정 경계를 적용하도록 DB where 절에 시점 비교가 존재 (review MEDIUM 4: 컬럼 래핑이 아닌 UTC boundary 비교이므로 where 인자가 `reservations.createdAt`과 Date 객체를 사용했는지 검증 — serialized SQL 조각에 `Asia/Seoul`이 포함될 수도, helper가 pre-compute한 경우 포함되지 않을 수도 있음. 테스트는 "where가 createdAt 컬럼을 참조"하는 것까지만 assert하고 timezone 문자열 포함 여부는 **not-strict**)
    - Test 3 (`revenue-weekly`): getRevenueTrend('90d')가 `bucket` 포맷 `IYYY-"W"IW`를 사용하도록 SQL 구성 (to_char 또는 raw 사용 인자 검증) + 반환 배열이 bucket skeleton(빈 주 채움)을 포함 — review MEDIUM 6 (최소: mock DB가 빈 배열 반환 시에도 결과 length ≥ 1)
    - Test 4 (`genre`): getGenreDistribution(period)가 performances.genre GROUP BY 패턴 → mock으로 3개 장르 배열 반환 검증
    - Test 5 (`payment`): getPaymentDistribution(period)가 payments.method GROUP BY + `reservations.status = 'CONFIRMED'` AND `payments.status = 'DONE'` 조건 (review MEDIUM 5 / Pitfall 5 방지)
    - Test 6 (`cache-hit`): cache.get이 값을 반환하면 db.select가 호출되지 않음
    - Test 7 (`cache-set-ttl`): cache.miss 시 cache.set이 세 번째 인자 `60`으로 호출됨
    - Test 8 (`cache-degradation`): cache.get/set이 null 반환 + throw 해도 service는 throw 없이 결과 반환
  </behavior>
  <files>
    - apps/api/src/modules/admin/__tests__/admin-dashboard.service.spec.ts (신규)
  </files>
  <action>
    파일 신규 생성. 구조:
    ```typescript
    import { describe, it, expect, beforeEach, vi } from 'vitest';
    import { AdminDashboardService } from '../admin-dashboard.service.js';
    import type { DashboardPeriod } from '@grapit/shared';

    function createChainMock(resolvedValue: unknown) {
      const handler: ProxyHandler<object> = {
        get(_target, prop) {
          if (prop === 'then') {
            return (resolve: (v: unknown) => void) => resolve(resolvedValue);
          }
          return (..._args: unknown[]) => new Proxy({}, handler);
        },
      };
      return new Proxy({}, handler);
    }

    function createMockDb() {
      return { select: vi.fn() };
    }
    function createMockCache() {
      return {
        get: vi.fn(),
        set: vi.fn(),
        invalidate: vi.fn(),
        invalidatePattern: vi.fn(),
      };
    }

    describe('AdminDashboardService', () => {
      let service: AdminDashboardService;
      let mockDb: ReturnType<typeof createMockDb>;
      let mockCache: ReturnType<typeof createMockCache>;

      beforeEach(() => {
        mockDb = createMockDb();
        mockCache = createMockCache();
        service = new AdminDashboardService(mockDb as any, mockCache as any);
      });

      describe('summary', () => {
        it('returns todayBookings, todayRevenue, todayCancelled, activePerformances', async () => {
          mockCache.get.mockResolvedValue(null);
          mockDb.select
            .mockReturnValueOnce(createChainMock([{ count: 10 }]))
            .mockReturnValueOnce(createChainMock([{ sum: 150000 }]))
            .mockReturnValueOnce(createChainMock([{ count: 1 }]))
            .mockReturnValueOnce(createChainMock([{ count: 3 }]));
          const result = await service.getSummary();
          expect(result).toEqual({
            todayBookings: 10,
            todayRevenue: 150000,
            todayCancelled: 1,
            activePerformances: 3,
          });
        });
      });

      describe('kst-boundary', () => {
        it('applies KST day boundary via where clause referencing reservations.createdAt', async () => {
          mockCache.get.mockResolvedValue(null);
          const whereSpy = vi.fn(() => createChainMock([{ count: 1 }]));
          const fromSpy = vi.fn(() => ({ where: whereSpy }));
          mockDb.select.mockReturnValue({ from: fromSpy });
          await service.getSummary();
          // where가 적어도 한 번은 호출되고, 인자가 존재해야 함.
          // (구현 방식에 따라 SQL 조각에 'Asia/Seoul' 리터럴이 있을 수도, UTC boundary Date와 raw 컬럼 비교일 수도 있음.)
          expect(whereSpy).toHaveBeenCalled();
          const whereArg = whereSpy.mock.calls[0]?.[0];
          expect(whereArg).toBeDefined();
        });
      });

      describe('revenue-weekly', () => {
        it('uses week granularity for 90d period and fills empty buckets', async () => {
          mockCache.get.mockResolvedValue(null);
          const orderBySpy = vi.fn(() => createChainMock([])); // DB가 빈 배열 반환
          const groupBySpy = vi.fn(() => ({ orderBy: orderBySpy }));
          const whereSpy = vi.fn(() => ({ groupBy: groupBySpy }));
          const fromSpy = vi.fn(() => ({ where: whereSpy }));
          mockDb.select.mockReturnValue({ from: fromSpy });
          const result = await service.getRevenueTrend('90d' as DashboardPeriod);
          const selectArg = mockDb.select.mock.calls[0]?.[0] as Record<string, unknown> | undefined;
          const serialized = JSON.stringify(selectArg);
          // 'week' granularity 표시 또는 IYYY-"W"IW 포맷 흔적
          expect(serialized.toLowerCase()).toMatch(/week|iyyy/);
          // review MEDIUM 6: 빈 DB 결과에도 bucket skeleton이 채워져서 최소 1개 이상 (90d → 13 weeks skeleton)
          expect(Array.isArray(result)).toBe(true);
          expect(result.length).toBeGreaterThanOrEqual(1);
        });
      });

      describe('genre', () => {
        it('returns genre count array', async () => {
          mockCache.get.mockResolvedValue(null);
          const orderBySpy = vi.fn(() => createChainMock([
            { genre: 'musical', count: 5 },
            { genre: 'concert', count: 3 },
            { genre: 'play', count: 1 },
          ]));
          const groupBySpy = vi.fn(() => ({ orderBy: orderBySpy }));
          const whereSpy = vi.fn(() => ({ groupBy: groupBySpy }));
          const fromSpy = vi.fn(() => ({ innerJoin: vi.fn(() => ({ innerJoin: vi.fn(() => ({ where: whereSpy })) })), where: whereSpy }));
          mockDb.select.mockReturnValue({ from: fromSpy });
          const result = await service.getGenreDistribution('30d' as DashboardPeriod);
          expect(Array.isArray(result)).toBe(true);
          expect(result.length).toBe(3);
        });
      });

      describe('payment', () => {
        it('joins payments with reservations where reservations.status = CONFIRMED AND payments.status = DONE (Pitfall 5 + review MEDIUM 5)', async () => {
          mockCache.get.mockResolvedValue(null);
          const groupBySpy = vi.fn(() => createChainMock([{ method: 'card', count: 8 }]));
          const whereSpy = vi.fn(() => ({ groupBy: groupBySpy }));
          const innerJoinSpy = vi.fn(() => ({ where: whereSpy }));
          const fromSpy = vi.fn(() => ({ innerJoin: innerJoinSpy }));
          mockDb.select.mockReturnValue({ from: fromSpy });
          await service.getPaymentDistribution('30d' as DashboardPeriod);
          const whereArg = whereSpy.mock.calls[0]?.[0];
          const serialized = JSON.stringify(whereArg);
          expect(serialized).toContain('CONFIRMED');
          expect(serialized).toContain('DONE');
        });
      });

      describe('cache-hit', () => {
        it('does not call db when cache returns value', async () => {
          mockCache.get.mockResolvedValue({
            todayBookings: 5, todayRevenue: 50000, todayCancelled: 0, activePerformances: 2,
          });
          const result = await service.getSummary();
          expect(mockDb.select).not.toHaveBeenCalled();
          expect(result.todayBookings).toBe(5);
        });
      });

      describe('cache-set-ttl', () => {
        it('calls cache.set with ttlSeconds = 60 exactly', async () => {
          mockCache.get.mockResolvedValue(null);
          mockDb.select
            .mockReturnValueOnce(createChainMock([{ count: 10 }]))
            .mockReturnValueOnce(createChainMock([{ sum: 150000 }]))
            .mockReturnValueOnce(createChainMock([{ count: 1 }]))
            .mockReturnValueOnce(createChainMock([{ count: 3 }]));
          await service.getSummary();
          expect(mockCache.set).toHaveBeenCalledWith(
            'cache:admin:dashboard:summary',
            expect.anything(),
            60,
          );
        });
      });

      describe('cache-degradation', () => {
        it('falls back to db when cache.get returns null and cache.set silently no-ops', async () => {
          mockCache.get.mockResolvedValue(null);
          mockCache.set.mockResolvedValue(undefined);
          mockDb.select
            .mockReturnValueOnce(createChainMock([{ count: 10 }]))
            .mockReturnValueOnce(createChainMock([{ sum: 150000 }]))
            .mockReturnValueOnce(createChainMock([{ count: 1 }]))
            .mockReturnValueOnce(createChainMock([{ count: 3 }]));
          const result = await service.getSummary();
          expect(result.todayBookings).toBe(10);
        });
      });
    });
    ```

    **주의:**
    - Task 01-02의 skeleton 덕분에 `import { AdminDashboardService }`는 resolve됨. 각 테스트는 `throw new Error('Not implemented')`로 인해 **assertion failure**(또는 rejected promise)로 RED가 됨. **이게 목표** — skip/fixme 사용 금지.
    - Plan 02가 method body를 채워 GREEN으로 만든다.
    - `as any` 캐스트는 테스트 내부 mock에만 허용됨 (production 코드는 strict 유지).
  </action>
  <verify>
    <automated>pnpm --filter @grapit/api exec vitest run admin-dashboard.service.spec.ts --reporter=verbose 2>&1 | grep -E "failed|Error|admin-dashboard" | head -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `apps/api/src/modules/admin/__tests__/admin-dashboard.service.spec.ts` 존재
    - 파일 라인 수 ≥ 150
    - grep `describe\('summary'\|describe\('kst-boundary'\|describe\('revenue-weekly'\|describe\('genre'\|describe\('payment'\|describe\('cache-hit'\|describe\('cache-set-ttl'\|describe\('cache-degradation'` → 8개 모두 match
    - grep `\.skip\|\.todo\|\.fixme` → 0 match
    - grep `cache:admin:dashboard:summary` → match (정확한 키 네임스페이스 D-14)
    - grep `ttlSeconds.*=.*60\|.*60.*\)` 또는 `60,` → match
    - `pnpm --filter @grapit/api exec vitest run admin-dashboard.service.spec.ts` 실행 시 **fail** 하되, 실패 사유가 `Error: Not implemented` 또는 `expect(...)` assertion failure이어야 함. 출력에 `Cannot find module` 또는 `Failed to load url` 문자열이 없어야 함 (RED가 module-not-found가 아닌 assertion failure임을 보장).
  </acceptance_criteria>
  <done>
    8개 테스트 이름/기대값 모두 정의됨. vitest 실행 시 skeleton의 `throw new Error('Not implemented')` 또는 assertion mismatch로 RED. Plan 02가 service를 구현하면 GREEN이 됨.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-04: admin-dashboard.controller.spec.ts (RED access control — 401/403/200) + integration.spec.ts + e2e.spec.ts</name>
  <read_first>
    - apps/api/src/modules/admin/admin-booking.service.spec.ts (Test.createTestingModule + overrideGuard 패턴 확인; 없으면 admin-booking.controller.ts의 기존 관례 확인)
    - apps/api/src/common/guards/roles.guard.ts (canActivate 시그니처 — request.user?.roles 읽는지 확인)
    - apps/api/src/modules/admin/admin-dashboard.controller.ts (Task 01-02 산출)
    - apps/api/test/sms-throttle.integration.spec.ts (testcontainers 패턴 analog — Postgres + Valkey)
    - apps/web/e2e/toss-payment.spec.ts (Playwright login + page.goto analog)
    - apps/web/e2e/helpers/auth.ts (loginAsTestUser — admin@grapit.test 기본값 확인)
    - .planning/phases/11-admin-dashboard/11-VALIDATION.md §Per-Task Verification Map
    - .planning/phases/11-admin-dashboard/11-RESEARCH.md §KST Boundary Test (sketch)
    - apps/api/vitest.integration.config.ts (integration test config 확인)
  </read_first>
  <files>
    - apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts (신규)
    - apps/api/test/admin-dashboard.integration.spec.ts (신규)
    - apps/web/e2e/admin-dashboard.spec.ts (신규)
  </files>
  <action>
    **A) `apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts` 신규 작성 (review HIGH 3)**

    unauth/user/admin 3-케이스를 고정한다. supertest 기반 e2e 스타일이 아니라 `Test.createTestingModule` + guard override로 최소 유닛 레벨에서 검증한다. 기대:
    - 비인증 요청 → 401 (RolesGuard가 request.user 없으면 `UnauthorizedException`을 던지거나, 프로젝트의 global auth guard가 401 반환)
    - user role → 403 (RolesGuard가 admin을 요구하므로 `ForbiddenException`)
    - admin role → 200 (skeleton은 `throw new Error('Not implemented')`로 500을 반환할 수 있으므로, 이 테스트는 Plan 02에서 GREEN 전환. 지금은 RED가 정상)

    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { Test, type TestingModule } from '@nestjs/testing';
    import { INestApplication, type ExecutionContext, ForbiddenException, UnauthorizedException } from '@nestjs/common';
    import request from 'supertest';
    import { AdminDashboardController } from '../admin-dashboard.controller.js';
    import { AdminDashboardService } from '../admin-dashboard.service.js';
    import { RolesGuard } from '../../../common/guards/roles.guard.js';
    import { DRIZZLE } from '../../../database/drizzle.provider.js';
    import { CacheService } from '../../performance/cache.service.js';

    type RoleMode = 'anonymous' | 'user' | 'admin';

    /**
     * AdminDashboardController access control — review HIGH 3.
     * 대표 엔드포인트 GET /admin/dashboard/summary로 3케이스 고정.
     */
    describe('AdminDashboardController (access control)', () => {
      let app: INestApplication;
      let mode: RoleMode = 'admin';

      beforeAll(async () => {
        const moduleRef: TestingModule = await Test.createTestingModule({
          controllers: [AdminDashboardController],
          providers: [
            AdminDashboardService,
            // service constructor의 @Inject(DRIZZLE)과 CacheService 충족용 더미
            { provide: DRIZZLE, useValue: {} },
            { provide: CacheService, useValue: { get: async () => null, set: async () => undefined } },
          ],
        })
          .overrideGuard(RolesGuard)
          .useValue({
            canActivate: (ctx: ExecutionContext) => {
              const req = ctx.switchToHttp().getRequest();
              if (mode === 'anonymous') {
                throw new UnauthorizedException();
              }
              req.user = mode === 'admin'
                ? { id: 'admin-1', roles: ['admin'] }
                : { id: 'user-1', roles: ['user'] };
              if (!req.user.roles.includes('admin')) {
                throw new ForbiddenException();
              }
              return true;
            },
          })
          .compile();

        app = moduleRef.createNestApplication();
        await app.init();
      });

      afterAll(async () => {
        await app?.close();
      });

      it('returns 401 for unauthenticated request', async () => {
        mode = 'anonymous';
        const res = await request(app.getHttpServer()).get('/admin/dashboard/summary');
        expect(res.status).toBe(401);
      });

      it('returns 403 for non-admin authenticated user', async () => {
        mode = 'user';
        const res = await request(app.getHttpServer()).get('/admin/dashboard/summary');
        expect(res.status).toBe(403);
      });

      it('returns 200 for admin role (will GREEN once Plan 02 implements handler)', async () => {
        mode = 'admin';
        const res = await request(app.getHttpServer()).get('/admin/dashboard/summary');
        // RED now (skeleton throws 500). GREEN after Plan 02.
        expect(res.status).toBe(200);
      });
    });
    ```

    이 테스트는 skeleton 상태에서 `admin` 케이스가 500으로 실패 → RED. 401/403 케이스는 RolesGuard override가 적절히 throw하면 통과할 수 있으나, 그래도 전체 파일이 admin 케이스에서 fail하므로 파일 단위 RED 상태 유지.

    **B) `apps/api/test/admin-dashboard.integration.spec.ts` 신규 작성**

    테스트 2개 (revenue-daily + top10), Postgres 16 + Valkey 8 testcontainer 사용. sms-throttle.integration.spec.ts의 container 부트스트랩 패턴 답습:
    ```typescript
    import { describe, it, expect, beforeAll, afterAll, beforeEach } from 'vitest';
    import { GenericContainer, type StartedTestContainer } from 'testcontainers';
    import IORedis from 'ioredis';
    import { drizzle } from 'drizzle-orm/node-postgres';
    import { Pool } from 'pg';
    import { reservations, showtimes, performances, venues } from '../src/database/schema/index.js';
    import { AdminDashboardService } from '../src/modules/admin/admin-dashboard.service.js';
    import { CacheService } from '../src/modules/performance/cache.service.js';
    import { randomUUID } from 'node:crypto';

    describe('AdminDashboardService (integration)', () => {
      let pgContainer: StartedTestContainer;
      let redisContainer: StartedTestContainer;
      let pool: Pool;
      let db: ReturnType<typeof drizzle>;
      let redis: IORedis;
      let service: AdminDashboardService;

      beforeAll(async () => {
        pgContainer = await new GenericContainer('postgres:16')
          .withExposedPorts(5432)
          .withEnvironment({ POSTGRES_PASSWORD: 'test', POSTGRES_DB: 'grapit_test' })
          .start();
        redisContainer = await new GenericContainer('valkey/valkey:8')
          .withExposedPorts(6379)
          .start();

        pool = new Pool({
          host: pgContainer.getHost(),
          port: pgContainer.getMappedPort(5432),
          user: 'postgres',
          password: 'test',
          database: 'grapit_test',
        });
        db = drizzle(pool);
        // 마이그레이션 실행 (drizzle-kit migrate 프로그래매틱 호출 또는 SQL 스크립트 실행)
        // sms-throttle pattern 참조 — 없으면 최소 스키마 수동 생성

        redis = new IORedis({
          host: redisContainer.getHost(),
          port: redisContainer.getMappedPort(6379),
          maxRetriesPerRequest: null,
        });

        const cache = new CacheService(redis as any);
        service = new AdminDashboardService(db as any, cache);
      }, 120_000);

      afterAll(async () => {
        await pool?.end();
        await redis?.quit();
        await pgContainer?.stop();
        await redisContainer?.stop();
      });

      beforeEach(async () => {
        await db.delete(reservations);
        await db.delete(showtimes);
        await db.delete(performances);
        await db.delete(venues);
      });

      it('revenue-daily: returns up to 30 daily buckets for 30d period', async () => {
        // seed: 오늘로부터 -1, -10, -20일 전 CONFIRMED 예매 각 1건 (KST)
        // ... seed code
        const result = await service.getRevenueTrend('30d');
        expect(Array.isArray(result)).toBe(true);
        // review MEDIUM 6: bucket skeleton으로 빈 날짜 채움 → 30d는 30개 bucket
        expect(result.length).toBe(30);
        // bucket 포맷 YYYY-MM-DD 검증
        expect(result[0]?.bucket).toMatch(/^\d{4}-\d{2}-\d{2}$/);
      });

      it('top10: returns up to 10 performances ordered by booking count desc (last 30d CONFIRMED)', async () => {
        // seed 2 performances with 3 and 1 confirmed reservations each
        // ... seed code
        const result = await service.getTopPerformances();
        expect(Array.isArray(result)).toBe(true);
        expect(result.length).toBeLessThanOrEqual(10);
        if (result.length >= 2) {
          expect(result[0].bookingCount).toBeGreaterThanOrEqual(result[1].bookingCount);
        }
      });
    });
    ```

    **seed 세부 구현 주의:**
    - `reservations.createdAt`은 UTC로 저장됨. KST boundary helper(Plan 02)는 UTC Date로 pre-compute한 boundary로 `reservations.createdAt >= startUtc AND reservations.createdAt < endUtc` 비교하므로 `new Date()` (지금)로 삽입하면 오늘로 잡힘
    - CONFIRMED 상태로 `status: 'CONFIRMED'`, `totalAmount: 50000` 등 최소 필드 채움
    - `reservationNumber`는 unique — `randomUUID()` 또는 `'R'+Date.now()`
    - `cancelDeadline`은 notNull — `new Date(Date.now()+86400000)` 같은 임의값

    **drizzle migration 실행:**
    `sms-throttle.integration.spec.ts`가 migration을 어떻게 실행하는지 확인하여 동일 패턴 답습. 확인 불가 시 최소한의 raw SQL로 테이블 생성 가능하나 이는 fragile. migrator 호출 권장:
    ```typescript
    import { migrate } from 'drizzle-orm/node-postgres/migrator';
    await migrate(db, { migrationsFolder: 'drizzle' });
    ```

    **C) `apps/web/e2e/admin-dashboard.spec.ts` 신규 작성**

    3개 테스트 (landing-smoke, period-filter, sidebar-nav). toss-payment.spec.ts의 login helper 패턴 답습:
    ```typescript
    import { test, expect } from '@playwright/test';
    import { loginAsTestUser } from './helpers/auth';

    test.describe('Admin Dashboard E2E', () => {
      test('landing-smoke: /admin renders dashboard with KPI + charts + Top10', async ({ page }) => {
        await loginAsTestUser(page); // admin@grapit.test (helpers/auth.ts:42 기본값)
        await page.goto('/admin');
        await expect(page).toHaveURL(/\/admin$/);
        await expect(page.getByRole('heading', { name: '대시보드', level: 1 })).toBeVisible();
        // KPI 4장 (UI-SPEC Copywriting)
        await expect(page.getByText('오늘 예매수')).toBeVisible();
        await expect(page.getByText('오늘 매출')).toBeVisible();
        await expect(page.getByText('오늘 취소')).toBeVisible();
        await expect(page.getByText('활성 공연')).toBeVisible();
        // 차트 섹션 3개
        await expect(page.getByRole('heading', { name: '매출 추이' })).toBeVisible();
        await expect(page.getByRole('heading', { name: '장르별 예매 분포' })).toBeVisible();
        await expect(page.getByRole('heading', { name: '결제수단 분포' })).toBeVisible();
        // Top 10
        await expect(page.getByRole('heading', { name: '인기 공연 Top 10' })).toBeVisible();
      });

      test('period-filter: 30일 → 7일 클릭 시 revenue/genre/payment 3개 chart 동시 refetch', async ({ page }) => {
        await loginAsTestUser(page);
        await page.goto('/admin');
        const responseWaits = [
          page.waitForResponse((r) => r.url().includes('/api/v1/admin/dashboard/revenue') && r.url().includes('period=7d')),
          page.waitForResponse((r) => r.url().includes('/api/v1/admin/dashboard/genre') && r.url().includes('period=7d')),
          page.waitForResponse((r) => r.url().includes('/api/v1/admin/dashboard/payment') && r.url().includes('period=7d')),
        ];
        await page.getByRole('group', { name: '기간 선택' }).getByRole('button', { name: '7일' }).click();
        await Promise.all(responseWaits);
      });

      test('sidebar-nav: 대시보드 NAV 항목이 active 하이라이트', async ({ page }) => {
        await loginAsTestUser(page);
        await page.goto('/admin');
        const dashboardLink = page.getByRole('link', { name: /대시보드/ }).first();
        await expect(dashboardLink).toBeVisible();
        await expect(dashboardLink).toHaveClass(/text-primary/);
      });
    });
    ```

    **금지사항:**
    - `test.skip`, `test.fixme` 사용 금지 (RED가 목적)
    - seed DB 호출 하드코드 금지 (helpers/auth.ts의 기존 패턴 재사용)
    - `test.describe.only` 사용 금지
  </action>
  <verify>
    <automated>test -f apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts && test -f apps/api/test/admin-dashboard.integration.spec.ts && test -f apps/web/e2e/admin-dashboard.spec.ts && echo "all three spec files exist"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts` 존재, 라인 수 ≥ 60
    - controller spec grep: `describe\('AdminDashboardController \(access control\)'` match
    - controller spec grep: `returns 401 for unauthenticated` match
    - controller spec grep: `returns 403 for non-admin` match
    - controller spec grep: `returns 200 for admin role` match
    - controller spec grep: `/admin/dashboard/summary` match
    - controller spec grep: `expect\(res\.status\)\.toBe\(401\)` / `toBe\(403\)` / `toBe\(200\)` 3개 모두 match
    - 파일 `apps/api/test/admin-dashboard.integration.spec.ts` 존재, 라인 수 ≥ 100
    - 파일 `apps/web/e2e/admin-dashboard.spec.ts` 존재, 라인 수 ≥ 60
    - integration spec grep: `new GenericContainer\('postgres:16'\)` match, `new GenericContainer\('valkey/valkey:8'\)` match
    - integration spec grep: `describe\('AdminDashboardService \(integration\)'` match
    - integration spec grep: `it\('revenue-daily` match, `it\('top10` match
    - e2e spec grep: `test\('landing-smoke\|test\('period-filter\|test\('sidebar-nav` 3개 모두 match
    - e2e spec grep: `loginAsTestUser` match
    - e2e spec grep: `대시보드` match (UI-SPEC Copywriting per D-01)
    - e2e spec grep: `\.skip\|\.fixme\|\.only` 결과 없음 (`test.skip`/`test.fixme`/`test.describe.only` 금지)
    - grep `'오늘 예매수'`, `'오늘 매출'`, `'오늘 취소'`, `'활성 공연'` 4개 모두 e2e 파일에 존재
  </acceptance_criteria>
  <done>
    controller access-control spec (401/403/200), integration, E2E 테스트 파일 존재. Plan 02(API 구현)와 Plan 03(Web 구현)이 완료되면 이 테스트들이 GREEN이 됨.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → Nest DI | controller spec은 Test.createTestingModule로 가벼운 DI 부트스트랩. 실제 HTTP boundary는 supertest in-memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | zod enum `dashboardPeriodSchema` | mitigate | enum 값을 `['7d','30d','90d']`로 strict하게 제한 (D-09). 이외 값은 Plan 02 controller의 `ZodValidationPipe`에서 400 응답. 본 플랜은 enum 정의 + controller skeleton의 pipe wiring만 제공. |
| T-11-02 | Information Disclosure | 테스트 스펙 파일에 실제 secret/credential | mitigate | 테스트는 testcontainers 로컬 Postgres/Valkey만 사용. `process.env.DATABASE_URL` / `REDIS_URL` 직접 읽지 않음. 프로덕션 secret 노출 위험 제로. |
| T-11-03a | Elevation of Privilege | GET /admin/dashboard/summary 인증/권한 검증 누락 | mitigate | controller skeleton이 `@UseGuards(RolesGuard) @Roles('admin')` 적용. controller spec이 unauth→401, user→403, admin→200 세 케이스를 RED로 고정하여 Plan 02 구현 중 회귀 방지 (review HIGH 3). |

</threat_model>

<verification>
Wave 1 완료 확인:
1. `pnpm --filter @grapit/shared build` → 0 exit
2. `pnpm --filter @grapit/api typecheck` → skeleton + shared import 가능 확인
3. `pnpm --filter @grapit/api exec vitest run admin-dashboard.service.spec.ts` → RED (assertion failure, **not module-not-found**)
4. `pnpm --filter @grapit/api exec vitest run admin-dashboard.controller.spec.ts` → RED (admin 케이스 500 assertion failure)
5. `test -f apps/api/test/admin-dashboard.integration.spec.ts && test -f apps/web/e2e/admin-dashboard.spec.ts` → 둘 다 존재
6. `git diff --stat HEAD` → 9개 파일 신규/수정, 라인 수 합계 ≥ 430
</verification>

<success_criteria>
- [ ] packages/shared에 admin-dashboard zod schema + 5개 DTO 타입이 export되고 `pnpm --filter @grapit/shared build` 통과
- [ ] AdminDashboardService / AdminDashboardController skeleton 존재, 모든 public method/handler가 `throw new Error('Not implemented')`
- [ ] admin-dashboard.service.spec.ts RED (assertion failure): 8개 테스트 이름 모두 정의됨, skip/fixme 없음
- [ ] admin-dashboard.controller.spec.ts RED (assertion failure): 401/403/200 3개 access control 테스트 정의됨
- [ ] admin-dashboard.integration.spec.ts RED: Postgres/Valkey testcontainer + 2개 테스트 정의됨
- [ ] admin-dashboard.spec.ts (E2E) RED: 3개 테스트 정의 + loginAsTestUser 사용
- [ ] 모든 테스트 파일의 import 경로가 `@grapit/shared` 공유 타입을 참조 (Interface-First)
- [ ] unit spec 실행 출력에 `Cannot find module` / `Failed to load url` 문자열 없음 (review HIGH 1)
</success_criteria>

<output>
완료 후 `.planning/phases/11-admin-dashboard/11-01-SUMMARY.md` 생성.
</output>
