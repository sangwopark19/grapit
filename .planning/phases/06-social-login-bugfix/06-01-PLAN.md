---
phase: 06-social-login-bugfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/auth/strategies/kakao.strategy.ts
  - apps/api/src/modules/auth/strategies/naver.strategy.ts
  - apps/api/src/modules/auth/strategies/google.strategy.ts
  - apps/api/src/modules/auth/guards/social-auth.guard.ts
  - apps/api/src/modules/auth/auth.controller.ts
  - apps/api/src/modules/auth/auth.service.ts
  - apps/web/app/auth/callback/page.tsx
  - apps/web/components/auth/login-form.tsx
  - apps/api/src/modules/auth/strategies/kakao.strategy.spec.ts
  - apps/api/src/modules/auth/strategies/naver.strategy.spec.ts
  - apps/api/src/modules/auth/strategies/google.strategy.spec.ts
  - apps/api/src/modules/auth/guards/social-auth.guard.spec.ts
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "소셜 로그인 callback URL이 컨트롤러 라우트(/api/v1/auth/social/{provider}/callback)와 일치한다"
    - "OAuth 에러 발생 시 JSON 응답 대신 프론트엔드로 에러 코드와 함께 redirect된다"
    - "프로덕션에서도 OAuth redirect 후 refresh token 쿠키가 정상 전달된다 (sameSite: lax)"
    - "소셜 로그인 각 단계에서 구조화된 로깅이 출력된다"
    - "프론트엔드 callback 페이지에서 에러 코드별 한국어 메시지와 재시도 버튼이 표시된다"
    - "로그인 페이지에서 소셜 로그인 redirect 에러가 인라인으로 표시된다"
  artifacts:
    - path: "apps/api/src/modules/auth/strategies/kakao.strategy.ts"
      provides: "수정된 callbackURL 기본값"
      contains: "/api/v1/auth/social/kakao/callback"
    - path: "apps/api/src/modules/auth/guards/social-auth.guard.ts"
      provides: "handleRequest 오버라이드로 OAuth 에러 시 redirect"
      contains: "handleRequest"
    - path: "apps/api/src/modules/auth/auth.controller.ts"
      provides: "handleSocialCallback 에러 핸들링 + 로깅"
      contains: "try {"
    - path: "apps/web/app/auth/callback/page.tsx"
      provides: "에러 상태 UI + 재시도 버튼"
      contains: "다시 로그인하기"
  key_links:
    - from: "apps/api/src/modules/auth/guards/social-auth.guard.ts"
      to: "apps/web/app/auth/callback/page.tsx"
      via: "redirect with ?error= query parameter"
      pattern: "error=oauth_failed"
    - from: "apps/api/src/modules/auth/auth.controller.ts"
      to: "apps/web/app/auth/callback/page.tsx"
      via: "redirect with ?error= on catch"
      pattern: "error=server_error"
---

<objective>
소셜 로그인(카카오/네이버/구글)의 재로그인 실패 버그를 수정하고 OAuth 에러 핸들링 전체 경로를 개선한다.

Purpose: v1.0에서 발견된 소셜 로그인 재로그인 불가 버그(AUTH-01)를 해결하고, Guard -> Controller -> Frontend 전 경로에 구조화된 에러 핸들링과 로깅을 추가하여 소셜 로그인 플로우의 안정성을 확보한다.
Output: 수정된 strategy 파일 3개, guard 파일 1개, controller 1개, service 1개, 프론트엔드 callback 페이지 1개, 로그인 폼 1개, 단위 테스트 파일 4개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-social-login-bugfix/06-CONTEXT.md
@.planning/phases/06-social-login-bugfix/06-RESEARCH.md
@.planning/phases/06-social-login-bugfix/06-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/api/src/modules/auth/interfaces/social-profile.interface.ts:
```typescript
export interface SocialProfile {
  provider: string;
  providerId: string;
  email?: string;
  name?: string;
}
```

From packages/shared/src/types/auth.types.ts:
```typescript
export interface SocialAuthResult {
  status: 'authenticated' | 'needs_registration';
  accessToken?: string;
  refreshToken?: string;
  user?: UserProfile;
  registrationToken?: string;
  socialProfile?: { provider: string; providerId: string; email?: string; name?: string };
}
```

From apps/api/src/modules/auth/auth.controller.ts (current handleSocialCallback):
```typescript
private async handleSocialCallback(req: Request, res: Response): Promise<void> {
  const profile = req.user as SocialProfile;
  const result = await this.authService.findOrCreateSocialUser(profile);
  const frontendUrl = this.configService.get<string>('FRONTEND_URL', 'http://localhost:3000');
  // ... redirect based on result.status
}
```

Auth cookie constant from @grapit/shared/constants/index.js:
```typescript
export const AUTH_COOKIE_NAME = 'grapit_refresh_token';
```

Current social auth guard pattern (apps/api/src/modules/auth/guards/social-auth.guard.ts):
```typescript
@Injectable()
export class KakaoAuthGuard extends AuthGuard('kakao') {
  canActivate(context: ExecutionContext) {
    return super.canActivate(context);
  }
}
// NaverAuthGuard, GoogleAuthGuard follow same pattern
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 백엔드 소셜 로그인 버그 수정 + 에러 핸들링 + 로깅</name>
  <files>
    apps/api/src/modules/auth/strategies/kakao.strategy.ts
    apps/api/src/modules/auth/strategies/naver.strategy.ts
    apps/api/src/modules/auth/strategies/google.strategy.ts
    apps/api/src/modules/auth/guards/social-auth.guard.ts
    apps/api/src/modules/auth/auth.controller.ts
    apps/api/src/modules/auth/auth.service.ts
    apps/api/src/modules/auth/strategies/kakao.strategy.spec.ts
    apps/api/src/modules/auth/strategies/naver.strategy.spec.ts
    apps/api/src/modules/auth/strategies/google.strategy.spec.ts
    apps/api/src/modules/auth/guards/social-auth.guard.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/auth/strategies/kakao.strategy.ts
    apps/api/src/modules/auth/strategies/naver.strategy.ts
    apps/api/src/modules/auth/strategies/google.strategy.ts
    apps/api/src/modules/auth/guards/social-auth.guard.ts
    apps/api/src/modules/auth/auth.controller.ts
    apps/api/src/modules/auth/auth.service.ts
    apps/api/src/modules/auth/interfaces/social-profile.interface.ts
    apps/api/src/modules/auth/strategies/kakao.strategy.spec.ts
    apps/api/src/modules/auth/strategies/naver.strategy.spec.ts
    apps/api/src/modules/auth/strategies/google.strategy.spec.ts
    apps/api/vitest.config.ts
  </read_first>
  <behavior>
    Strategy callbackURL 테스트:
    - Test: KakaoStrategy의 기본 callbackURL이 'http://localhost:8080/api/v1/auth/social/kakao/callback'이다
    - Test: NaverStrategy의 기본 callbackURL이 'http://localhost:8080/api/v1/auth/social/naver/callback'이다
    - Test: GoogleStrategy의 기본 callbackURL이 'http://localhost:8080/api/v1/auth/social/google/callback'이다
    - Test: 환경변수 KAKAO_CALLBACK_URL이 설정되면 해당 값이 사용된다

    AuthGuard handleRequest 테스트:
    - Test: KakaoAuthGuard.handleRequest에서 err가 있으면 frontendUrl + '/auth/callback?error=oauth_failed&provider=kakao'로 redirect
    - Test: KakaoAuthGuard.handleRequest에서 user가 null이면 동일하게 redirect
    - Test: KakaoAuthGuard.handleRequest에서 user가 있으면 user를 그대로 반환
    - Test: NaverAuthGuard, GoogleAuthGuard도 동일 패턴으로 각각 provider 파라미터가 다르게 설정

    handleSocialCallback 에러 핸들링 (auth.controller.ts 내부 private 메서드이므로 컨트롤러 엔드포인트를 통해 간접 검증):
    - 기존 auth.service.spec.ts의 findOrCreateSocialUser 테스트가 여전히 통과해야 함
  </behavior>
  <action>
    **A. Strategy callbackURL 수정 (per D-04)**

    세 strategy 파일의 callbackURL 기본값을 수정한다. `/social/` 세그먼트가 빠진 것이 근본 버그이다.

    kakao.strategy.ts 26행:
    ```typescript
    callbackURL: configService.get<string>(
      'KAKAO_CALLBACK_URL',
      'http://localhost:8080/api/v1/auth/social/kakao/callback',
    ),
    ```
    (변경: `/auth/kakao/callback` -> `/auth/social/kakao/callback`)

    naver.strategy.ts 25행:
    ```typescript
    callbackURL: configService.get<string>(
      'NAVER_CALLBACK_URL',
      'http://localhost:8080/api/v1/auth/social/naver/callback',
    ),
    ```
    (변경: `/auth/naver/callback` -> `/auth/social/naver/callback`)

    google.strategy.ts 19행:
    ```typescript
    callbackURL: configService.get<string>(
      'GOOGLE_CALLBACK_URL',
      'http://localhost:8080/api/v1/auth/social/google/callback',
    ),
    ```
    (변경: `/auth/google/callback` -> `/auth/social/google/callback`)

    **B. AuthGuard handleRequest 오버라이드 추가 (per D-03)**

    `guards/social-auth.guard.ts`를 전면 수정한다. 세 Guard 모두 `handleRequest` 메서드를 오버라이드하여 OAuth 에러 시 JSON 응답 대신 프론트엔드로 redirect한다.

    에러 코드 체계 (Claude's Discretion):
    - `oauth_denied`: 사용자 동의 거부 (err.message에 'denied' 또는 'cancel' 포함)
    - `oauth_failed`: provider 인증 실패 (그 외 에러)

    구현: BaseSocialAuthGuard 패턴을 사용하여 중복을 제거한다. 각 Guard는 provider 이름만 다르다.

    ```typescript
    import { Injectable, type ExecutionContext, Logger } from '@nestjs/common';
    import { AuthGuard } from '@nestjs/passport';
    import { ConfigService } from '@nestjs/config';
    import type { Response } from 'express';

    function createSocialAuthGuard(strategyName: string, providerName: string) {
      @Injectable()
      class SocialGuard extends AuthGuard(strategyName) {
        private readonly logger = new Logger(`${providerName}AuthGuard`);

        constructor(private readonly configService: ConfigService) {
          super();
        }

        canActivate(context: ExecutionContext) {
          return super.canActivate(context);
        }

        handleRequest<T>(err: Error | null, user: T, _info: unknown, context: ExecutionContext): T {
          if (err || !user) {
            const frontendUrl = this.configService.get<string>('FRONTEND_URL', 'http://localhost:3000');
            const errorCode = err?.message?.toLowerCase().includes('denied') || err?.message?.toLowerCase().includes('cancel')
              ? 'oauth_denied'
              : 'oauth_failed';
            this.logger.warn(`${providerName} OAuth failed: ${err?.message ?? 'no user returned'}`);
            const res = context.switchToHttp().getResponse<Response>();
            res.redirect(`${frontendUrl}/auth/callback?error=${errorCode}&provider=${providerName}`);
            return null as T;
          }
          return user;
        }
      }
      return SocialGuard;
    }

    @Injectable()
    export class KakaoAuthGuard extends createSocialAuthGuard('kakao', 'kakao') {}

    @Injectable()
    export class NaverAuthGuard extends createSocialAuthGuard('naver', 'naver') {}

    @Injectable()
    export class GoogleAuthGuard extends createSocialAuthGuard('google', 'google') {}
    ```

    **주의:** NestJS DI에서 `createSocialAuthGuard` 팩토리가 `ConfigService`를 주입받으려면 `@Injectable()` 데코레이터가 반환 클래스와 확장 클래스 모두에 필요하다. 만약 DI 문제가 발생하면 팩토리 패턴 대신 각 Guard를 개별 클래스로 구현하되 공통 로직을 private helper 함수로 추출한다.

    **C. handleSocialCallback 에러 핸들링 + 로깅 (per D-02, D-03)**

    `auth.controller.ts`의 `handleSocialCallback` 메서드를 수정한다:

    1. `Logger` 인스턴스를 클래스에 추가: `private readonly logger = new Logger(AuthController.name);`
    2. `req.user` 체크를 추가 (Guard에서 redirect 후 null이 올 수 있음)
    3. `findOrCreateSocialUser` 호출을 try-catch로 감싸고 에러 시 redirect
    4. 각 단계에서 로깅 추가

    수정된 handleSocialCallback:
    ```typescript
    private async handleSocialCallback(req: Request, res: Response): Promise<void> {
      const frontendUrl = this.configService.get<string>('FRONTEND_URL', 'http://localhost:3000');

      if (!req.user) {
        this.logger.warn('Social callback received without user profile');
        return;
      }

      const profile = req.user as SocialProfile;

      try {
        this.logger.log(`Social callback: provider=${profile.provider}, providerId=${profile.providerId}`);
        const result = await this.authService.findOrCreateSocialUser(profile);

        if (result.status === 'authenticated') {
          this.logger.log(`Social login authenticated: provider=${profile.provider}, providerId=${profile.providerId}`);
          if (result.refreshToken) {
            this.setRefreshTokenCookie(res, result.refreshToken);
          }
          res.redirect(`${frontendUrl}/auth/callback?status=authenticated`);
        } else {
          this.logger.log(`Social login needs registration: provider=${profile.provider}`);
          res.redirect(
            `${frontendUrl}/auth/callback?registrationToken=${result.registrationToken}&status=needs_registration`,
          );
        }
      } catch (error) {
        this.logger.error(`Social callback failed: provider=${profile.provider}`, (error as Error).stack);
        res.redirect(`${frontendUrl}/auth/callback?error=server_error&provider=${profile.provider}`);
      }
    }
    ```

    **D. SameSite 쿠키 수정 (Bug 3 from RESEARCH)**

    `auth.controller.ts`의 `setRefreshTokenCookie`에서 `sameSite`를 항상 `'lax'`로 변경한다. OAuth redirect chain에서 `strict`은 쿠키를 차단한다.

    수정 전: `sameSite: isProduction ? 'strict' : 'lax',`
    수정 후: `sameSite: 'lax',`
    (해당 줄의 isProduction 조건부를 제거하고 상수 'lax'로 변경)

    **E. auth.service.ts 로깅 추가 (per D-02)**

    `findOrCreateSocialUser`와 `completeSocialRegistration` 메서드에 NestJS Logger를 추가한다:
    - 클래스에 `private readonly logger = new Logger(AuthService.name);` 추가
    - findOrCreateSocialUser 시작: `this.logger.log(\`findOrCreateSocialUser: provider=${profile.provider}, providerId=${profile.providerId}\`);`
    - 기존 사용자 발견: `this.logger.log(\`Social user found: userId=${socialAccount.userId}\`);`
    - 새 사용자 등록 필요: `this.logger.log(\`New social user, registration required: provider=${profile.provider}\`);`
    - completeSocialRegistration 시작 및 완료에도 동일한 패턴으로 로깅 추가

    **F. 기존 Strategy spec 파일에 callbackURL 검증 테스트 추가**

    세 strategy spec 파일(kakao, naver, google)에 각각 callbackURL 기본값 검증 테스트를 추가한다. 기존 테스트에서 mockConfigService.get이 undefined를 반환하도록 하고, strategy에 전달된 callbackURL에 `/social/` 세그먼트가 포함되는지 검증한다.

    **G. AuthGuard spec 파일 신규 생성**

    `apps/api/src/modules/auth/guards/social-auth.guard.spec.ts` 파일을 생성하여 handleRequest 오버라이드를 테스트한다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api exec vitest run src/modules/auth/strategies src/modules/auth/guards</automated>
  </verify>
  <acceptance_criteria>
    - kakao.strategy.ts 26행 부근에 문자열 '/api/v1/auth/social/kakao/callback' 포함
    - naver.strategy.ts 25행 부근에 문자열 '/api/v1/auth/social/naver/callback' 포함
    - google.strategy.ts 19행 부근에 문자열 '/api/v1/auth/social/google/callback' 포함
    - social-auth.guard.ts에 'handleRequest' 메서드 존재
    - social-auth.guard.ts에 'res.redirect' 호출 존재
    - social-auth.guard.ts에 'oauth_failed' 문자열 포함
    - social-auth.guard.ts에 'oauth_denied' 문자열 포함
    - social-auth.guard.ts에 Logger import 및 인스턴스 존재
    - auth.controller.ts에 'private readonly logger = new Logger(AuthController.name)' 존재
    - auth.controller.ts handleSocialCallback에 'try {' 블록 존재
    - auth.controller.ts handleSocialCallback에 'catch (error)' 블록 존재
    - auth.controller.ts handleSocialCallback에 'error=server_error' redirect 존재
    - auth.controller.ts handleSocialCallback에 'if (!req.user)' 체크 존재
    - auth.controller.ts setRefreshTokenCookie에 "sameSite: 'lax'" (조건 분기 없이 상수)
    - auth.service.ts에 'private readonly logger = new Logger(AuthService.name)' 존재
    - auth.service.ts findOrCreateSocialUser에 this.logger.log 호출 존재
    - social-auth.guard.spec.ts 파일 존재
    - kakao.strategy.spec.ts에 callbackURL 검증 테스트 존재
    - `pnpm --filter @grapit/api exec vitest run src/modules/auth/strategies src/modules/auth/guards` 종료 코드 0
  </acceptance_criteria>
  <done>
    3개 strategy의 callbackURL 기본값이 /social/ 세그먼트를 포함하고, AuthGuard가 OAuth 에러 시 프론트엔드로 redirect하며, handleSocialCallback이 에러를 try-catch로 처리하고, sameSite 쿠키가 항상 lax이며, 전 과정에 구조화된 로깅이 추가되었다. 모든 단위 테스트 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 에러 상태 UI + 로그인 페이지 에러 표시</name>
  <files>
    apps/web/app/auth/callback/page.tsx
    apps/web/components/auth/login-form.tsx
  </files>
  <read_first>
    apps/web/app/auth/callback/page.tsx
    apps/web/components/auth/login-form.tsx
    apps/web/components/auth/social-login-button.tsx
    .planning/phases/06-social-login-bugfix/06-UI-SPEC.md
    apps/web/app/globals.css
  </read_first>
  <action>
    **A. callback/page.tsx 에러 상태 추가 (per D-08, D-09)**

    1. 에러 코드-메시지 매핑 상수를 파일 상단에 추가한다 (UI-SPEC Copywriting Contract 기반):

    ```typescript
    import { AlertCircle } from 'lucide-react';
    import { Button } from '@/components/ui/button';

    const SOCIAL_ERROR_MESSAGES: Record<string, { title: string; detail: string }> = {
      oauth_denied: {
        title: '로그인이 취소되었습니다.',
        detail: '다시 로그인해주세요.',
      },
      oauth_failed: {
        title: '소셜 로그인에 실패했습니다.',
        detail: '잠시 후 다시 시도해주세요.',
      },
      token_expired: {
        title: '로그인 세션이 만료되었습니다.',
        detail: '다시 로그인해주세요.',
      },
      server_error: {
        title: '일시적인 오류가 발생했습니다.',
        detail: '잠시 후 다시 시도해주세요.',
      },
      account_conflict: {
        title: '이미 다른 계정에 연결된 소셜 계정입니다.',
        detail: '기존 계정으로 로그인해주세요.',
      },
    };
    ```

    2. CallbackContent 컴포넌트에 에러 상태를 추가한다:

    - 새 state: `const [errorInfo, setErrorInfo] = useState<{ code: string; provider?: string } | null>(null);`
    - useEffect 내부에서 `searchParams.get('error')` 체크를 최우선으로 추가한다 (기존 status 체크 전):

    ```typescript
    const errorCode = searchParams.get('error');
    const provider = searchParams.get('provider');

    if (errorCode) {
      setErrorInfo({ code: errorCode, provider: provider ?? undefined });
      setIsProcessing(false);
      return;
    }
    ```

    3. 에러 상태 렌더링을 isProcessing 체크 앞에 추가한다:

    ```typescript
    if (errorInfo) {
      const messages = SOCIAL_ERROR_MESSAGES[errorInfo.code] ?? SOCIAL_ERROR_MESSAGES['server_error']!;
      return (
        <main className="flex flex-1 items-center justify-center">
          <div className="flex max-w-[400px] flex-col items-center gap-4 px-4">
            <AlertCircle className="h-8 w-8 text-error" />
            <div className="text-center">
              <p className="text-base font-semibold text-gray-900">
                {messages.title}
              </p>
              <p className="mt-2 text-caption text-gray-600">
                {messages.detail}
              </p>
            </div>
            <Button
              size="lg"
              className="mt-2 w-full max-w-[280px]"
              onClick={() => router.push('/auth')}
            >
              다시 로그인하기
            </Button>
          </div>
        </main>
      );
    }
    ```

    UI-SPEC 준수 사항:
    - AlertCircle 아이콘: h-8 w-8 (32px), text-error 색상
    - Primary message: text-base (16px) / font-semibold / text-gray-900, 아이콘에서 gap-4 (16px)
    - Detail message: text-caption (14px) / text-gray-600, primary에서 mt-2 (8px)
    - Retry 버튼: shadcn Button default variant, size="lg", w-full max-w-[280px], detail에서 mt-2
    - 컨테이너: max-w-[400px], flex items-center justify-center (기존 processing 상태와 동일 레이아웃)

    **B. login-form.tsx 소셜 에러 query parameter 표시 (per D-08)**

    1. `useSearchParams`를 import에 추가하고 컴포넌트 내에서 사용한다:

    ```typescript
    import { useRouter, useSearchParams } from 'next/navigation';
    ```

    2. 컴포넌트 상단에서 소셜 에러 query parameter를 읽는다:

    ```typescript
    const searchParams = useSearchParams();
    const socialError = searchParams.get('error');
    ```

    3. 소셜 에러 메시지 매핑 (callback/page.tsx와 동일한 에러 코드 사용):

    ```typescript
    const SOCIAL_LOGIN_ERRORS: Record<string, string> = {
      oauth_denied: '소셜 로그인이 취소되었습니다.',
      oauth_failed: '소셜 로그인에 실패했습니다. 다시 시도해주세요.',
      token_expired: '로그인 세션이 만료되었습니다. 다시 시도해주세요.',
      server_error: '일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요.',
      account_conflict: '이미 다른 계정에 연결된 소셜 계정입니다.',
    };
    ```

    4. 소셜 로그인 버튼 섹션(div.space-y-3) 바로 위에 에러 메시지를 표시한다. 기존 loginError와 동일한 스타일 패턴을 사용한다:

    ```tsx
    {socialError && SOCIAL_LOGIN_ERRORS[socialError] && (
      <p className="text-caption text-error animate-in fade-in duration-150">
        {SOCIAL_LOGIN_ERRORS[socialError]}
      </p>
    )}
    ```

    이 에러 메시지는 Separator("또는") 아래, SocialLoginButton들 위에 위치한다.

    5. LoginForm 컴포넌트를 Suspense로 감싸는 것을 고려해야 한다. `useSearchParams`는 Suspense boundary가 필요하다. 이미 login-form.tsx가 'use client'이므로, 부모 페이지(apps/web/app/auth/page.tsx)에서 Suspense를 추가하거나, login-form.tsx 내부에서 Suspense를 사용한다. **기존 패턴 확인 후 적절히 처리할 것** -- callback/page.tsx에서는 이미 Suspense가 적용되어 있으므로 동일 패턴을 따른다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - callback/page.tsx에 'SOCIAL_ERROR_MESSAGES' 상수 존재
    - callback/page.tsx에 'oauth_denied', 'oauth_failed', 'token_expired', 'server_error', 'account_conflict' 키 존재
    - callback/page.tsx에 '다시 로그인하기' 텍스트의 Button 컴포넌트 존재
    - callback/page.tsx에 AlertCircle import 존재
    - callback/page.tsx에 "searchParams.get('error')" 호출 존재
    - callback/page.tsx에 errorInfo state 존재
    - login-form.tsx에 'useSearchParams' import 존재
    - login-form.tsx에 "searchParams.get('error')" 호출 존재
    - login-form.tsx에 'SOCIAL_LOGIN_ERRORS' 상수 존재
    - login-form.tsx에 'text-caption text-error animate-in fade-in duration-150' 스타일 존재
    - `pnpm --filter @grapit/web build` 종료 코드 0 (타입 에러 없음)
  </acceptance_criteria>
  <done>
    소셜 로그인 에러 시 callback 페이지에 에러 코드별 한국어 메시지와 AlertCircle 아이콘, "다시 로그인하기" 재시도 버튼이 표시된다. 로그인 페이지에서도 ?error= query parameter로 전달된 소셜 에러가 인라인으로 표시된다. 빌드 정상 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth Provider -> Backend Callback | 외부 소셜 provider가 callback URL로 인증 결과 전달 |
| Backend -> Frontend Redirect | 백엔드가 에러 코드를 query parameter로 프론트엔드에 전달 |
| Frontend -> Backend API | 프론트엔드가 refresh token cookie로 인증 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | OAuth callbackURL | mitigate | callbackURL 기본값을 컨트롤러 라우트와 일치시키고, 환경변수로 override 가능. provider 콘솔에서도 Redirect URI 등록 필요 (checkpoint:human-action in Plan 02) |
| T-06-02 | Tampering | CSRF via OAuth | accept | Passport.js가 내장 state parameter로 CSRF 방어. 추가 구현 불필요 |
| T-06-03 | Information Disclosure | Refresh token cookie | mitigate | httpOnly=true, secure=true(prod), sameSite='lax'로 설정. lax는 top-level navigation에서만 쿠키 전송하므로 CSRF POST 차단 유지. strict에서 lax로 변경은 OAuth redirect chain 호환성을 위한 의도적 완화이며 ASVS L1 허용 범위 |
| T-06-04 | Elevation of Privilege | Open redirect after OAuth | mitigate | FRONTEND_URL 환경변수를 configService에서 읽어 redirect 대상을 고정. 임의 URL로 redirect 불가 |
| T-06-05 | Information Disclosure | Error code in query param | accept | error code는 generic 코드(oauth_failed, server_error 등)만 사용. provider 내부 에러 상세는 서버 로그에만 기록하고 클라이언트에 노출하지 않음 |
</threat_model>

<verification>
1. `pnpm --filter @grapit/api exec vitest run src/modules/auth` -- auth 모듈 전체 테스트 통과
2. `pnpm --filter @grapit/web build` -- 프론트엔드 빌드 성공 (타입 에러 없음)
3. grep으로 callbackURL에 '/social/' 세그먼트 포함 확인
4. grep으로 handleRequest, try-catch, sameSite: 'lax' 확인
</verification>

<success_criteria>
- 3개 strategy의 callbackURL 기본값에 /social/ 세그먼트가 포함되어 컨트롤러 라우트와 일치
- AuthGuard가 OAuth 에러 시 프론트엔드로 에러 코드와 함께 redirect
- handleSocialCallback이 에러를 try-catch로 처리하고 프론트엔드로 redirect
- sameSite 쿠키가 항상 'lax'로 설정
- 소셜 로그인 전 과정에 구조화된 NestJS Logger 로깅 존재
- callback 페이지에 에러 코드별 한국어 메시지 + AlertCircle 아이콘 + "다시 로그인하기" 버튼
- 로그인 페이지에 소셜 에러 인라인 표시
- 모든 단위 테스트 통과, 프론트엔드 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/06-social-login-bugfix/06-01-SUMMARY.md`
</output>
