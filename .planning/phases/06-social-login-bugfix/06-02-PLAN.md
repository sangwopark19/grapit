---
phase: 06-social-login-bugfix
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/web/e2e/social-login.spec.ts
  - apps/web/playwright.config.ts
  - apps/web/package.json
autonomous: false
requirements:
  - AUTH-01

must_haves:
  truths:
    - "소셜 로그인 콜백 에러 시나리오가 E2E 테스트로 검증된다"
    - "소셜 로그인 성공 시나리오(callback mock)가 E2E 테스트로 검증된다"
    - "카카오/네이버/구글 세 프로바이더 실제 재로그인이 수동으로 확인된다"
  artifacts:
    - path: "apps/web/e2e/social-login.spec.ts"
      provides: "소셜 로그인 E2E 테스트"
      contains: "test.describe('Social Login'"
    - path: "apps/web/playwright.config.ts"
      provides: "Playwright 설정"
      contains: "defineConfig"
  key_links:
    - from: "apps/web/e2e/social-login.spec.ts"
      to: "apps/web/app/auth/callback/page.tsx"
      via: "page.goto('/auth/callback?error=...')"
      pattern: "auth/callback"
---

<objective>
소셜 로그인 E2E 테스트를 작성하고 실제 프로바이더로 수동 검증을 수행한다.

Purpose: D-05, D-06 결정에 따라 Playwright E2E 테스트로 소셜 로그인 에러/성공 시나리오를 자동 검증하고, D-07에 따라 실제 테스트 계정으로 세 프로바이더의 재로그인을 확인한다.
Output: Playwright 설정, E2E 테스트 파일, 수동 검증 체크리스트 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-social-login-bugfix/06-CONTEXT.md
@.planning/phases/06-social-login-bugfix/06-RESEARCH.md
@.planning/phases/06-social-login-bugfix/06-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성/수정된 에러 코드 체계 -->

Social login error codes (query parameter로 전달):
- oauth_denied: 사용자가 OAuth 동의 거부
- oauth_failed: provider 인증 실패
- token_expired: 세션/토큰 만료
- server_error: 내부 서버 에러
- account_conflict: 이미 연결된 계정

Callback page URL patterns:
- 에러: /auth/callback?error={code}&provider={provider}
- 인증 성공: /auth/callback?status=authenticated
- 등록 필요: /auth/callback?registrationToken={token}&status=needs_registration

Login page URL with error:
- /auth?error={code}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright 설정 + 소셜 로그인 E2E 테스트 작성</name>
  <files>
    apps/web/e2e/social-login.spec.ts
    apps/web/playwright.config.ts
    apps/web/package.json
  </files>
  <read_first>
    apps/web/package.json
    apps/web/app/auth/callback/page.tsx
    apps/web/components/auth/login-form.tsx
    .planning/phases/06-social-login-bugfix/06-01-SUMMARY.md
  </read_first>
  <action>
    **A. Playwright 설치 및 설정 (per D-05)**

    1. @playwright/test를 apps/web에 devDependency로 설치:
    ```bash
    pnpm --filter @grapit/web add -D @playwright/test
    npx playwright install chromium
    ```

    2. `apps/web/playwright.config.ts` 생성:
    ```typescript
    import { defineConfig, devices } from '@playwright/test';

    export default defineConfig({
      testDir: './e2e',
      fullyParallel: true,
      forbidOnly: !!process.env['CI'],
      retries: process.env['CI'] ? 2 : 0,
      workers: process.env['CI'] ? 1 : undefined,
      reporter: 'html',
      use: {
        baseURL: 'http://localhost:3000',
        trace: 'on-first-retry',
      },
      projects: [
        {
          name: 'chromium',
          use: { ...devices['Desktop Chrome'] },
        },
      ],
      webServer: {
        command: 'pnpm dev',
        url: 'http://localhost:3000',
        reuseExistingServer: !process.env['CI'],
      },
    });
    ```

    **B. 소셜 로그인 E2E 테스트 작성 (per D-05, D-06)**

    OAuth provider 실제 인증 페이지는 봇 감지로 자동화가 불가능하므로(RESEARCH Pitfall 5), callback URL을 직접 접근하는 방식으로 테스트한다. 이 방식은 백엔드 callback 이후의 전체 프론트엔드 플로우를 검증한다.

    `apps/web/e2e/social-login.spec.ts`:
    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Social Login - Error Scenarios', () => {
      test('oauth_denied 에러 시 취소 메시지와 재시도 버튼이 표시된다', async ({ page }) => {
        await page.goto('/auth/callback?error=oauth_denied&provider=kakao');
        await expect(page.getByText('로그인이 취소되었습니다.')).toBeVisible();
        await expect(page.getByText('다시 로그인하기')).toBeVisible();
      });

      test('oauth_failed 에러 시 실패 메시지와 재시도 버튼이 표시된다', async ({ page }) => {
        await page.goto('/auth/callback?error=oauth_failed&provider=naver');
        await expect(page.getByText('소셜 로그인에 실패했습니다.')).toBeVisible();
        await expect(page.getByText('다시 로그인하기')).toBeVisible();
      });

      test('server_error 에러 시 일시적 오류 메시지가 표시된다', async ({ page }) => {
        await page.goto('/auth/callback?error=server_error&provider=google');
        await expect(page.getByText('일시적인 오류가 발생했습니다.')).toBeVisible();
      });

      test('account_conflict 에러 시 계정 충돌 메시지가 표시된다', async ({ page }) => {
        await page.goto('/auth/callback?error=account_conflict&provider=kakao');
        await expect(page.getByText('이미 다른 계정에 연결된 소셜 계정입니다.')).toBeVisible();
      });

      test('token_expired 에러 시 만료 메시지가 표시된다', async ({ page }) => {
        await page.goto('/auth/callback?error=token_expired');
        await expect(page.getByText('로그인 세션이 만료되었습니다.')).toBeVisible();
      });

      test('알 수 없는 에러 코드 시 기본 에러 메시지가 표시된다', async ({ page }) => {
        await page.goto('/auth/callback?error=unknown_error');
        await expect(page.getByText('일시적인 오류가 발생했습니다.')).toBeVisible();
      });

      test('재시도 버튼 클릭 시 /auth 로그인 페이지로 이동한다', async ({ page }) => {
        await page.goto('/auth/callback?error=oauth_failed&provider=kakao');
        await page.getByText('다시 로그인하기').click();
        await expect(page).toHaveURL(/\/auth$/);
      });
    });

    test.describe('Social Login - Login Page Error Display', () => {
      test('소셜 에러 query parameter로 로그인 페이지에 에러 메시지가 표시된다', async ({ page }) => {
        await page.goto('/auth?error=oauth_failed');
        await expect(page.getByText('소셜 로그인에 실패했습니다')).toBeVisible();
      });
    });

    test.describe('Social Login - Processing State', () => {
      test('에러 없이 callback 접근 시 처리 중 상태가 표시된다', async ({ page }) => {
        // status=authenticated이지만 refresh token이 없으므로 결국 에러로 넘어감
        // 최소한 processing 상태가 먼저 표시되는지 확인
        await page.goto('/auth/callback?status=authenticated');
        // 짧은 시간 동안 "로그인 처리 중..." 텍스트가 표시됨 (fetch 실패 전)
        await expect(page.getByText('로그인 처리 중...')).toBeVisible({ timeout: 2000 }).catch(() => {
          // fetch가 빠르게 실패하면 이미 redirect 되었을 수 있음 -- acceptable
        });
      });
    });
    ```

    3. package.json에 E2E 테스트 스크립트 추가:
    ```json
    "test:e2e": "playwright test"
    ```
    (기존 scripts 객체에 추가)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec playwright test e2e/social-login.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/playwright.config.ts 파일 존재하고 'defineConfig' 포함
    - apps/web/e2e/social-login.spec.ts 파일 존재
    - social-login.spec.ts에 'oauth_denied' 에러 시나리오 테스트 존재
    - social-login.spec.ts에 'oauth_failed' 에러 시나리오 테스트 존재
    - social-login.spec.ts에 'server_error' 에러 시나리오 테스트 존재
    - social-login.spec.ts에 'account_conflict' 에러 시나리오 테스트 존재
    - social-login.spec.ts에 '다시 로그인하기' 버튼 클릭 -> /auth 이동 테스트 존재
    - social-login.spec.ts에 로그인 페이지 에러 표시 테스트 존재
    - apps/web/package.json의 scripts에 "test:e2e" 키 존재
    - Playwright 테스트 중 Error Scenarios 테스트가 pass (dev 서버 실행 상태에서)
  </acceptance_criteria>
  <done>
    Playwright가 설치되고 설정 파일이 생성되었다. 소셜 로그인 에러 시나리오(5개 에러 코드 + 재시도 버튼 + 로그인 페이지 에러)가 E2E 테스트로 작성되었다. 에러 시나리오 테스트가 통과한다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 소셜 로그인 재로그인 수동 검증</name>
  <files>none -- verification only</files>
  <read_first>
    .planning/phases/06-social-login-bugfix/06-01-SUMMARY.md
  </read_first>
  <action>
    사용자에게 수동 검증 체크리스트를 제시한다. 이 태스크는 checkpoint:human-verify로, 실제 OAuth provider 인증을 사람이 수행하여 재로그인 버그가 해결되었는지 확인한다. 자동화 불가능한 이유: OAuth provider 로그인 페이지의 봇 감지(CAPTCHA, 2FA)로 Playwright 자동화가 차단됨 (RESEARCH Pitfall 5).
  </action>
  <what-built>
    소셜 로그인 버그 수정 완료:
    1. 3개 strategy callbackURL 기본값 수정 (/social/ 세그먼트 추가)
    2. AuthGuard OAuth 에러 시 프론트엔드 redirect 처리
    3. handleSocialCallback try-catch 에러 핸들링
    4. sameSite 쿠키 lax 고정
    5. 구조화된 디버그 로깅
    6. 프론트엔드 에러 상태 UI (에러 메시지 + 재시도 버튼)
    7. 로그인 페이지 소셜 에러 인라인 표시
    8. E2E 테스트 자동화
  </what-built>
  <how-to-verify>
    **사전 준비 (per D-07):**
    - 카카오/네이버/구글 OAuth 개발자 콘솔에서 Redirect URI가 올바른지 확인:
      - 카카오: `http://localhost:8080/api/v1/auth/social/kakao/callback`
      - 네이버: `http://localhost:8080/api/v1/auth/social/naver/callback`
      - 구글: `http://localhost:8080/api/v1/auth/social/google/callback`
    - 환경변수에 KAKAO_CALLBACK_URL, NAVER_CALLBACK_URL, GOOGLE_CALLBACK_URL이 설정되어 있다면 위 URL과 일치하는지 확인

    **테스트 시나리오 (per D-06):**

    각 프로바이더(카카오, 네이버, 구글)에 대해:
    1. 개발 서버 실행: `pnpm dev` (web: 3000, api: 8080)
    2. http://localhost:3000/auth 접속
    3. 소셜 로그인 버튼 클릭
    4. 최초 소셜 회원가입 완료 (추가 정보 입력)
    5. 홈으로 이동 확인
    6. 로그아웃
    7. 다시 소셜 로그인 버튼 클릭
    8. **재로그인 성공** 확인 (기존 사용자로 바로 홈 이동, 추가 정보 입력 없이)
    9. 기존 사용자 데이터(예매 내역 등)가 정상 연결되어 있는지 확인

    **에러 UI 확인:**
    10. http://localhost:3000/auth/callback?error=oauth_failed&provider=kakao 접속
    11. "소셜 로그인에 실패했습니다." 메시지 확인
    12. "다시 로그인하기" 버튼 확인 및 클릭 -> /auth 이동 확인

    **API 로그 확인:**
    13. 소셜 로그인 과정에서 API 서버 콘솔에 구조화된 로그가 출력되는지 확인:
        - "Social callback: provider=kakao, providerId=..."
        - "Social login authenticated: provider=kakao, ..."
  </how-to-verify>
  <verify>Human verifies all 3 providers (kakao, naver, google) re-login successfully</verify>
  <done>카카오/네이버/구글 세 프로바이더 모두 최초 가입 -> 로그아웃 -> 재로그인 성공 확인. 에러 UI 정상 표시 확인. API 로그 출력 확인.</done>
  <resume-signal>"approved" 입력 또는 발견된 이슈를 설명해주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E Test -> Frontend | 테스트가 callback URL을 직접 접근하여 프론트엔드 에러 처리 검증 |
| User -> OAuth Provider | 사용자가 실제 OAuth provider에서 인증 수행 (수동 검증) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | OAuth Redirect URI in provider console | mitigate | checkpoint:human-verify에서 각 provider 콘솔의 Redirect URI를 수동 확인하도록 체크리스트에 포함 |
| T-06-07 | Information Disclosure | E2E test credentials | accept | 테스트 계정은 개발용이며 프로덕션 데이터 접근 불가. .env에 저장되어 gitignore 대상 |
</threat_model>

<verification>
1. Playwright 에러 시나리오 테스트 전체 통과
2. 수동 검증: 카카오/네이버/구글 각각 최초 가입 -> 로그아웃 -> 재로그인 성공
3. 에러 UI가 UI-SPEC에 맞게 표시됨 확인
</verification>

<success_criteria>
- Playwright E2E 테스트 설정 완료 및 에러 시나리오 테스트 통과
- 카카오/네이버/구글 세 프로바이더 모두 재로그인 수동 검증 완료
- API 서버 콘솔에 구조화된 소셜 로그인 로그 출력 확인
- 에러 UI (메시지 + 재시도 버튼)가 정상 동작 확인
</success_criteria>

<output>
After completion, create `.planning/phases/06-social-login-bugfix/06-02-SUMMARY.md`
</output>
