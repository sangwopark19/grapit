---
phase: 09-tech-debt
plan: 04
type: execute
wave: gap-closure
gap_closure: true
depends_on: ["09-03"]
requires: ["09-03"]
files_modified:
  - apps/web/app/auth/reset-password/page.tsx
  - apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx
autonomous: false
requirements: ["DEBT-01"]

must_haves:
  truths:
    - "이메일 링크(/auth/reset-password?token=...) 클릭 시 새 비밀번호 입력 UI가 렌더된다 (이메일 입력 폼이 아니다)"
    - "새 비밀번호 입력 후 제출 시 POST /api/v1/auth/password-reset/confirm 으로 { token, newPassword, newPasswordConfirm } 이 전송된다"
    - "유효하지 않거나 만료된 토큰일 때 명시적 에러 UI가 표시되고 '다시 요청하기' 링크가 /auth/reset-password (token 없음) 로 연결된다"
    - "비밀번호 변경 성공 시 /auth 로 리다이렉트되고 성공 toast 가 표시된다"
    - "token 이 없는 채로 /auth/reset-password 접속 시 기존 이메일 입력 폼이 그대로 표시된다 (회귀 없음)"
  artifacts:
    - path: "apps/web/app/auth/reset-password/page.tsx"
      provides: "token query 분기 + confirm 폼 렌더링"
      contains: "useSearchParams"
    - path: "apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx"
      provides: "confirm 폼 렌더링 + 성공 경로 테스트"
      contains: "describe"
  key_links:
    - from: "apps/web/app/auth/reset-password/page.tsx"
      to: "POST /api/v1/auth/password-reset/confirm"
      via: "fetch body { token, newPassword, newPasswordConfirm }"
      pattern: "/api/v1/auth/password-reset/confirm"
    - from: "apps/web/app/auth/reset-password/page.tsx"
      to: "@grapit/shared resetPasswordSchema"
      via: "zodResolver"
      pattern: "resetPasswordSchema"
---

<objective>
Phase 9 UAT Test 11 gap closure. 비밀번호 재설정 이메일 링크를 클릭했을 때 사용자가 실제로 새 비밀번호를 입력할 수 있도록 frontend confirm UI 를 추가한다.

**Root cause (UAT 진단 완료):** `apps/web/app/auth/reset-password/page.tsx` 가 "이메일 입력 → 재설정 요청" 단일 용도로만 구현되어 `?token=` query 를 읽거나 분기하는 로직이 전혀 없다. Backend (`POST /auth/password-reset/confirm`) 와 email 링크 URL 은 정상 동작하므로, 변경 범위는 오직 이 한 page 파일 + 신규 테스트 파일뿐이다.

**Chosen approach: Option A (최소 변경)** — UAT `missing` 목록의 첫 번째 항목이자 debug session 이 "권장 (최소 변경)" 으로 명시한 방식. 같은 `page.tsx` 안에서 `useSearchParams()` 로 `token` 을 읽어 분기한다. Backend path (`/auth/reset-password?token=...`) 를 그대로 유지하여 이메일 템플릿 / `auth.service.ts:248` 변경을 피한다.

Purpose: Phase 9 DEBT-01 success criteria ("비밀번호 재설정 시 실제 이메일이 발송되고 링크를 통해 비밀번호 변경 완료") 의 마지막 미완 조각을 닫는다.

Output:
- `apps/web/app/auth/reset-password/page.tsx` 에 token 분기 + confirm 폼 추가
- `apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx` 에 vitest 테스트 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-tech-debt/09-UAT.md
@.planning/debug/password-reset-link-redirects-to-request.md
@apps/web/app/auth/reset-password/page.tsx
@apps/web/components/auth/signup-step1.tsx
@apps/web/components/auth/password-input.tsx
@apps/web/lib/api-client.ts
@packages/shared/src/schemas/auth.schema.ts
@apps/api/src/modules/auth/auth.controller.ts
@apps/api/src/modules/auth/dto/reset-password.dto.ts
@apps/api/src/modules/auth/auth.service.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

**Shared zod schema (packages/shared/src/schemas/auth.schema.ts):**
```typescript
// Already exists — reuse as-is. Do NOT redefine.
export const resetPasswordSchema = z
  .object({
    token: z.string().min(1, '토큰이 필요합니다'),
    newPassword: passwordSchema, // min 8 + letter + number + special char
    newPasswordConfirm: z.string().min(1, '비밀번호 확인을 입력해주세요'),
  })
  .refine((data) => data.newPassword === data.newPasswordConfirm, {
    message: '비밀번호가 일치하지 않습니다',
    path: ['newPasswordConfirm'],
  });

export type ResetPasswordInput = z.infer<typeof resetPasswordSchema>;
// = { token: string; newPassword: string; newPasswordConfirm: string }
```

**Backend contract (apps/api/src/modules/auth/auth.controller.ts:131-142):**
```typescript
@Post('password-reset/confirm')
// Body schema = resetPasswordBodySchema = resetPasswordSchema (same as shared)
// 200 OK → { message: '비밀번호가 변경되었습니다' }
// 401   → { message: '유효하지 않은 재설정 토큰입니다' }  (invalid or expired token)
// 400   → zod validation error (weak password, mismatched confirm, etc.)
// 429   → Too Many Requests (3 req / 15min / IP)
```
Full URL (through web proxy): `POST /api/v1/auth/password-reset/confirm`

**apiClient caveat (apps/web/lib/api-client.ts:95-104):**
`apiClient.post` 는 401 응답을 받으면 자동으로 `window.location.href = '/auth'` 로 리다이렉트한다. 이는 "세션 만료" 처리용이지만, **reset-password confirm 의 401 은 "토큰이 유효하지 않음"이라는 의미이므로 이 자동 리다이렉트를 피해야 한다**. 따라서 confirm 제출에는 `apiClient.post` 를 사용하지 말고 raw `fetch` 를 직접 호출한다. Request form 쪽은 기존대로 `apiClient.post` 를 유지한다.

**Form pattern (apps/web/components/auth/signup-step1.tsx):**
```typescript
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { resetPasswordSchema, type ResetPasswordInput } from '@grapit/shared';
import { Form, FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage } from '@/components/ui/form';
import { PasswordInput } from '@/components/auth/password-input';

const form = useForm<ResetPasswordInput>({
  resolver: zodResolver(resetPasswordSchema),
  defaultValues: { token, newPassword: '', newPasswordConfirm: '' },
  mode: 'onBlur',
  reValidateMode: 'onChange',
});
```

**Test pattern (apps/web/app/__tests__/not-found.test.tsx):**
- vitest + @testing-library/react + jsdom (already configured in `apps/web/vitest.config.ts`)
- Mock `next/link` and `next/navigation` via `vi.mock(...)`
- Tests run via `pnpm --filter @grapit/web test` or `pnpm --filter @grapit/web test reset-password`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TDD RED): vitest 실패 테스트 추가</name>
  <files>apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx</files>

  <behavior>
    Test 1 — "token query 없을 때 기존 이메일 입력 폼 렌더":
      useSearchParams mock 이 빈 URLSearchParams 반환 → render → "가입 시 사용한 이메일을 입력하세요" 텍스트 존재 + `input[type="email"]` 존재
    Test 2 — "token query 있을 때 새 비밀번호 입력 UI 렌더":
      useSearchParams mock 이 `new URLSearchParams('token=abc.def.ghi')` 반환 → render →
        - "새 비밀번호 설정" 또는 "새 비밀번호" 헤딩 텍스트 존재
        - 이메일 입력 폼 텍스트("가입 시 사용한 이메일을 입력하세요")는 존재하지 않음
        - `input[autoComplete="new-password"]` 가 정확히 2개 존재 (비밀번호 + 비밀번호 확인)
    Test 3 — "confirm 제출 성공 시 /api/v1/auth/password-reset/confirm 호출":
      global.fetch 를 `vi.fn().mockResolvedValue({ ok: true, status: 200, json: async () => ({ message: '비밀번호가 변경되었습니다' }) } as Response)` 로 mock →
      userEvent 로 비밀번호 "Test1234!" 두 필드에 입력 → 제출 버튼 클릭 →
      `fetch` 가 `/api/v1/auth/password-reset/confirm` 로 POST 되었고 body JSON 에 `token: 'abc.def.ghi'`, `newPassword: 'Test1234!'`, `newPasswordConfirm: 'Test1234!'` 포함되었음을 검증
    Test 4 — "confirm 401 응답 시 에러 UI + 다시 요청하기 링크 표시":
      fetch mock 이 `{ ok: false, status: 401, json: async () => ({ message: '유효하지 않은 재설정 토큰입니다' }) }` 반환 →
      userEvent 로 유효한 비밀번호 입력 + 제출 → 에러 메시지 + `a[href="/auth/reset-password"]` 또는 "다시 요청하기" 텍스트 링크 존재
  </behavior>

  <action>
Create `apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx`:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import ResetPasswordPage from '../page';

// --- next/navigation mock (hoisted so useSearchParams per-test can swap) ---
const mockSearchParams = { current: new URLSearchParams() };
const mockPush = vi.fn();
vi.mock('next/navigation', () => ({
  useSearchParams: () => mockSearchParams.current,
  useRouter: () => ({ push: mockPush, replace: mockPush, refresh: vi.fn() }),
}));

// next/link mock (same pattern as not-found.test.tsx)
vi.mock('next/link', () => ({
  default: ({ href, children }: { href: string; children: React.ReactNode }) => (
    <a href={href}>{children}</a>
  ),
}));

// sonner toast mock — confirm success path uses toast.success
vi.mock('sonner', () => ({
  toast: { success: vi.fn(), error: vi.fn() },
}));

describe('ResetPasswordPage', () => {
  beforeEach(() => {
    mockSearchParams.current = new URLSearchParams();
    mockPush.mockReset();
    vi.stubGlobal('fetch', vi.fn());
  });

  afterEach(() => {
    vi.unstubAllGlobals();
  });

  describe('token query 없을 때 (request 모드)', () => {
    it('기존 이메일 입력 폼 텍스트가 렌더된다', () => {
      render(<ResetPasswordPage />);
      expect(screen.getByText('가입 시 사용한 이메일을 입력하세요')).toBeDefined();
      expect(screen.getByLabelText('이메일')).toBeDefined();
    });
  });

  describe('token query 있을 때 (confirm 모드)', () => {
    beforeEach(() => {
      mockSearchParams.current = new URLSearchParams('token=abc.def.ghi');
    });

    it('새 비밀번호 입력 UI가 렌더된다 (이메일 입력 폼이 아니다)', () => {
      render(<ResetPasswordPage />);
      // heading — executor decides exact text ("새 비밀번호 설정" 권장)
      expect(screen.queryByText('가입 시 사용한 이메일을 입력하세요')).toBeNull();
      const pwInputs = document.querySelectorAll('input[autocomplete="new-password"]');
      expect(pwInputs.length).toBe(2);
    });

    it('confirm 제출 성공 시 fetch 가 올바른 path + body 로 호출된다', async () => {
      const fetchMock = vi.fn().mockResolvedValue({
        ok: true,
        status: 200,
        json: async () => ({ message: '비밀번호가 변경되었습니다' }),
      } as Response);
      vi.stubGlobal('fetch', fetchMock);

      render(<ResetPasswordPage />);
      const user = userEvent.setup();
      const pwInputs = document.querySelectorAll<HTMLInputElement>(
        'input[autocomplete="new-password"]',
      );
      await user.type(pwInputs[0], 'Test1234!');
      await user.type(pwInputs[1], 'Test1234!');
      await user.click(screen.getByRole('button', { name: /비밀번호 변경|변경하기|설정|확인/ }));

      // Wait for async submit
      await vi.waitFor(() => {
        expect(fetchMock).toHaveBeenCalled();
      });

      const [url, init] = fetchMock.mock.calls[0];
      expect(String(url)).toContain('/api/v1/auth/password-reset/confirm');
      expect((init as RequestInit).method).toBe('POST');
      const body = JSON.parse((init as RequestInit).body as string);
      expect(body.token).toBe('abc.def.ghi');
      expect(body.newPassword).toBe('Test1234!');
      expect(body.newPasswordConfirm).toBe('Test1234!');
    });

    it('401 응답 시 에러 UI + "다시 요청하기" 링크가 표시된다', async () => {
      const fetchMock = vi.fn().mockResolvedValue({
        ok: false,
        status: 401,
        json: async () => ({ message: '유효하지 않은 재설정 토큰입니다' }),
      } as Response);
      vi.stubGlobal('fetch', fetchMock);

      render(<ResetPasswordPage />);
      const user = userEvent.setup();
      const pwInputs = document.querySelectorAll<HTMLInputElement>(
        'input[autocomplete="new-password"]',
      );
      await user.type(pwInputs[0], 'Test1234!');
      await user.type(pwInputs[1], 'Test1234!');
      await user.click(screen.getByRole('button', { name: /비밀번호 변경|변경하기|설정|확인/ }));

      await vi.waitFor(() => {
        // Error message from backend shown to user
        expect(screen.queryByText(/유효하지 않은|만료|다시 요청/)).not.toBeNull();
      });
      // "다시 요청하기" link back to the request page (no token)
      const retryLink = Array.from(document.querySelectorAll('a'))
        .find((a) => a.getAttribute('href') === '/auth/reset-password');
      expect(retryLink).toBeDefined();
    });
  });
});
```

**Run and confirm RED:** `pnpm --filter @grapit/web test reset-password` — Tests 2/3/4 MUST fail (page.tsx doesn't branch yet). Test 1 (request mode) should pass unchanged. Commit only after confirming the expected failures.
  </action>

  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web test reset-password 2>&1 | tee /tmp/reset-pw-red.log; grep -E "(FAIL|failed|✗)" /tmp/reset-pw-red.log</automated>
  </verify>

  <done>
테스트 파일 존재. 최소 한 개 테스트가 실패한다 (RED 상태). Test 1 (request 모드) 은 passing. Commit 메시지: `test(09-04): add failing tests for reset-password confirm flow`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (TDD GREEN): page.tsx 에 token 분기 + confirm 폼 구현</name>
  <files>apps/web/app/auth/reset-password/page.tsx</files>

  <behavior>
    - `useSearchParams()` 로 `token` 을 읽는다. `const token = searchParams.get('token') ?? ''`.
    - `token === ''` → 기존 request 폼 그대로 유지 (이메일 입력 → `apiClient.post('/api/v1/auth/password-reset/request', data)`). 회귀 없음.
    - `token !== ''` → confirm 폼 렌더:
        * Heading: "새 비밀번호 설정"
        * Description: "새 비밀번호를 입력해주세요. 비밀번호는 8자 이상, 영문 + 숫자 + 특수문자를 포함해야 합니다."
        * react-hook-form + `zodResolver(resetPasswordSchema)` (from `@grapit/shared`)
        * defaultValues: `{ token, newPassword: '', newPasswordConfirm: '' }`
        * Two `PasswordInput` fields: `newPassword` + `newPasswordConfirm`, `autoComplete="new-password"` (both)
        * Submit handler: raw `fetch('/api/v1/auth/password-reset/confirm', { method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify(data) })`
          - `credentials: 'include'` matches apiClient convention (`lib/api-client.ts`).
          - **NOT `apiClient.post`** because apiClient 는 401 에서 `/auth` 로 자동 리다이렉트해 에러 UI 를 가린다.
    - 성공 (`res.ok`): `toast.success('비밀번호가 변경되었습니다. 다시 로그인해주세요.')` → `router.push('/auth')`
    - 실패:
        * `res.status === 401`: 폼을 숨기고 에러 UI 렌더 — "유효하지 않거나 만료된 링크입니다" 메시지 + "다시 요청하기" 링크 (`<Link href="/auth/reset-password">`, token 쿼리 없음).
        * `res.status === 400`: zod validation 에러 — `toast.error(message)` 후 폼 유지.
        * `res.status === 429`: `toast.error('요청이 너무 많습니다. 잠시 후 다시 시도해주세요.')`
        * 그 외 / 네트워크 에러: `toast.error('오류가 발생했습니다. 다시 시도해주세요.')`
    - Loading 상태: 기존 request 폼과 동일한 `<Loader2 className="h-5 w-5 animate-spin" />` 사용.
    - 로그인 복귀 링크: confirm 모드에서도 하단에 `<Link href="/auth">로그인으로 돌아가기</Link>` 유지.
  </behavior>

  <action>
Rewrite `apps/web/app/auth/reset-password/page.tsx`. 기존 request 모드 JSX / `isSent` 분기는 유지하고, 새 `ConfirmView` 서브컴포넌트 (또는 동일 함수 내 조건 렌더) 를 추가한다.

**Structural outline (executor implements):**

```typescript
'use client';

import { useState } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { Loader2 } from 'lucide-react';
import Link from 'next/link';
import { useRouter, useSearchParams } from 'next/navigation';
import { toast } from 'sonner';
import {
  resetPasswordRequestSchema,
  resetPasswordSchema,
  type ResetPasswordRequestInput,
  type ResetPasswordInput,
} from '@grapit/shared';
import { apiClient } from '@/lib/api-client';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { PasswordInput } from '@/components/auth/password-input';
import {
  Form, FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage,
} from '@/components/ui/form';

export default function ResetPasswordPage() {
  const searchParams = useSearchParams();
  const token = searchParams.get('token') ?? '';

  if (token !== '') {
    return <ConfirmView token={token} />;
  }
  return <RequestView />;
}

function RequestView() {
  // ... keep existing implementation (isSent, sentEmail, isLoading, form, onSubmit → apiClient.post request) ...
}

function ConfirmView({ token }: { token: string }) {
  const router = useRouter();
  const [isLoading, setIsLoading] = useState(false);
  const [tokenError, setTokenError] = useState(false);

  const form = useForm<ResetPasswordInput>({
    resolver: zodResolver(resetPasswordSchema),
    defaultValues: { token, newPassword: '', newPasswordConfirm: '' },
    mode: 'onBlur',
    reValidateMode: 'onChange',
  });

  async function onSubmit(data: ResetPasswordInput) {
    setIsLoading(true);
    try {
      const res = await fetch('/api/v1/auth/password-reset/confirm', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(data),
      });

      if (res.ok) {
        toast.success('비밀번호가 변경되었습니다. 다시 로그인해주세요.');
        router.push('/auth');
        return;
      }

      if (res.status === 401) {
        setTokenError(true);
        return;
      }

      let message = '오류가 발생했습니다. 다시 시도해주세요.';
      if (res.status === 429) message = '요청이 너무 많습니다. 잠시 후 다시 시도해주세요.';
      try {
        const data = (await res.json()) as { message?: string };
        if (res.status === 400 && data.message) message = data.message;
      } catch {
        // ignore
      }
      toast.error(message);
    } catch {
      toast.error('오류가 발생했습니다. 다시 시도해주세요.');
    } finally {
      setIsLoading(false);
    }
  }

  if (tokenError) {
    return (
      <main className="flex flex-1 items-center justify-center px-4 py-12">
        <div className="w-full max-w-[400px] space-y-6">
          <div className="space-y-3">
            <h1 className="text-heading font-semibold text-gray-900">
              유효하지 않은 링크
            </h1>
            <p className="text-base text-gray-700">
              비밀번호 재설정 링크가 만료되었거나 이미 사용되었습니다. 다시 요청해주세요.
            </p>
          </div>
          <Button asChild size="lg" className="w-full">
            <Link href="/auth/reset-password">다시 요청하기</Link>
          </Button>
          <div className="text-center">
            <Link href="/auth" className="text-caption text-gray-500 hover:text-primary">
              로그인으로 돌아가기
            </Link>
          </div>
        </div>
      </main>
    );
  }

  return (
    <main className="flex flex-1 items-center justify-center px-4 py-12">
      <div className="w-full max-w-[400px] space-y-6">
        <div className="space-y-3">
          <h1 className="text-heading font-semibold text-gray-900">새 비밀번호 설정</h1>
          <p className="text-base text-gray-700">
            새 비밀번호를 입력해주세요.
          </p>
        </div>

        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField
              control={form.control}
              name="newPassword"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>새 비밀번호 <span className="text-error">*</span></FormLabel>
                  <FormControl>
                    <PasswordInput
                      placeholder="새 비밀번호를 입력해주세요"
                      autoComplete="new-password"
                      {...field}
                    />
                  </FormControl>
                  <FormDescription>8자 이상, 영문+숫자+특수문자 포함</FormDescription>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={form.control}
              name="newPasswordConfirm"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>새 비밀번호 확인 <span className="text-error">*</span></FormLabel>
                  <FormControl>
                    <PasswordInput
                      placeholder="새 비밀번호를 다시 입력해주세요"
                      autoComplete="new-password"
                      {...field}
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            <Button type="submit" size="lg" className="w-full" disabled={isLoading}>
              {isLoading ? (
                <><Loader2 className="h-5 w-5 animate-spin" /> 변경 중...</>
              ) : (
                '비밀번호 변경'
              )}
            </Button>
          </form>
        </Form>

        <div className="text-center">
          <Link href="/auth" className="text-caption text-gray-500 hover:text-primary">
            로그인으로 돌아가기
          </Link>
        </div>
      </div>
    </main>
  );
}
```

**CRITICAL do / don't checklist:**
- DO reuse `resetPasswordSchema` from `@grapit/shared` — do not redefine a local zod schema.
- DO send `newPasswordConfirm` in the body (backend `resetPasswordBodySchema` includes it; schema would reject extra keys stripped, but refine check runs server-side too).
- DO use raw `fetch` with `credentials: 'include'` — **NOT** `apiClient.post` (401 auto-redirect issue).
- DO NOT touch `auth.service.ts` or email templates (Option A preserves path).
- DO keep `RequestView` 100% behavioral-compatible with the existing implementation (`apiClient.post('/api/v1/auth/password-reset/request', data)`, `setIsSent`, success screen). Regression on Test 1 is a failure.
- DO handle the Next.js 16 requirement that `useSearchParams` needs a Suspense boundary in some contexts. If build complains, wrap the component export: `export default function Page() { return <Suspense fallback={null}><ResetPasswordInner /></Suspense>; }`. Resolve only if build fails.

**Run and confirm GREEN:** `pnpm --filter @grapit/web test reset-password` — all 4 tests pass.
  </action>

  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web test reset-password 2>&1 | tee /tmp/reset-pw-green.log; grep -qE "✓|passed" /tmp/reset-pw-green.log && ! grep -qE "✗|failed" /tmp/reset-pw-green.log</automated>
  </verify>

  <done>
모든 테스트 pass. page.tsx 에 `useSearchParams`, `/api/v1/auth/password-reset/confirm`, `newPassword`, `resetPasswordSchema` 식별자 모두 존재 (grep 검증 가능). Commit 메시지: `feat(09-04): add confirm UI to reset-password page with token query branching`
  </done>
</task>

<task type="auto">
  <name>Task 3: typecheck + lint + build 통과 확인</name>
  <files>(없음 — 품질 게이트 실행만)</files>

  <action>
다음 명령을 순차 실행하고 모두 exit 0 이어야 한다:

```bash
cd /Users/sangwopark19/icons/grapit

# 1. Typecheck — strict 타입 보장 (CLAUDE.md user rule)
pnpm --filter @grapit/web typecheck

# 2. Lint — 변경된 파일에 대한 에러/경고 수정 (user rule: 에러는 반드시, 경고는 내가 만진 코드만)
pnpm --filter @grapit/web lint

# 3. 전체 web test — 회귀 없음 확인
pnpm --filter @grapit/web test

# 4. Build — Next.js production 빌드까지 통과
pnpm --filter @grapit/web build
```

실패 시:
- typecheck 에러 → 타입 좁히기 (`searchParams.get('token')` 은 `string | null` 임을 주의; `?? ''` 로 대응)
- lint warning-only → 변경 파일 (`page.tsx`, 테스트 파일) 에 한해 수정
- build 에러 중 `useSearchParams must be wrapped in Suspense` 계열 → `<Suspense>` 로 감싸서 해결 (Next.js 16 App Router 관례)
- 다른 앱/테스트 실패 → 이 PR 범위 외면 기존 실패인지 확인 후 UAT 리로그로 에스컬레이션
  </action>

  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web lint && pnpm --filter @grapit/web test && pnpm --filter @grapit/web build</automated>
  </verify>

  <done>
네 명령 모두 exit 0. 커밋 불필요 (새 파일/변경 없음). 다음 task 진행.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5 (TDD RED): backend resetPassword preliminary verify regression test</name>
  <files>apps/api/src/modules/auth/auth.service.spec.ts</files>

  <background>
Human UAT (2026-04-15) 발견: Plan 09-04 Task 1-3 완료 후 실제 브라우저에서 이메일 링크 → confirm 폼 → 비밀번호 제출 시 backend 가 401 반환. 원인 분석 결과 `auth.service.ts:264-276` 의 preliminary `jwtService.verifyAsync(token, { secret: jwtSecret, ignoreExpiration: true })` 가 **서명을 실제로 검증**하는데, 토큰은 `jwtSecret + user.passwordHash` 로 서명되어 있어 서명 key 불일치로 **합법 토큰도 항상 401** 이 발생한다.

커밋 `bc3b434 fix(09): CR-01 verify JWT signature before resetPassword DB lookup` 에서 도입된 regression. 기존 단위 테스트 (`auth.service.spec.ts:621-633`) 의 `mockJwtService.verifyAsync.mockImplementation` 이 opts.secret 기반 분기로 실제 서명 검증을 시뮬하지 않아 regression 을 놓쳤다.

CR-02 로 이 gap 을 닫는다.
  </background>

  <behavior>
실제 `@nestjs/jwt` `JwtService` 를 사용한 integration 테스트를 추가해, 정상 토큰으로 `AuthService.resetPassword` 가 성공해야 함을 검증한다. 현재 코드에서는 preliminary 단계 서명 불일치로 FAIL 해야 한다 (RED 확정).

Test 5-A — "정상 토큰 (jwtSecret + passwordHash 로 서명) 이 preliminary 에서 차단되지 않고 final verify 까지 도달해 성공한다":
  - 실제 `JwtService` (nestjs/jwt) 를 주입 (mock 아님)
  - `jwtSecret = 'test-jwt-secret-for-reset'`, 실제 argon2 hash `passwordHash`
  - `signAsync({ sub: userId, purpose: 'password-reset' }, { secret: jwtSecret + passwordHash, expiresIn: '1h' })` 로 token 생성
  - `userRepository.findById` mock 이 `{ id, passwordHash }` 반환
  - `authService.resetPassword(token, 'NewPass123!')` 호출 → throw 없이 resolve
  - `userRepository.updatePassword` 호출됨 확인

Test 5-B — "decoded.sub 이 UUID 형식이 아니면 401":
  - 동일 integration 셋업에서 `sub: 'not-a-uuid'` 로 sign 한 token → `resetPassword` → `UnauthorizedException` throw

Test 5-C (회귀 가드) — "jwtSecret 만으로 서명된 token (잘못된 secret) 은 final verify 에서 401":
  - `signAsync(..., { secret: jwtSecret })` (passwordHash 없이) 로 생성한 token → `resetPassword` → 401 throw
  - preliminary 통과는 허용 (decode 기반이므로 서명 무관) 하지만 final verify 는 실패해야 함
  </behavior>

  <action>
`apps/api/src/modules/auth/auth.service.spec.ts` 파일 하단 (기존 `describe('Blocker B4 integration'...` 섹션 근처) 에 새 `describe('resetPassword (integration — real JwtService)', ...)` 블록을 추가한다.

**구조:**
```typescript
import { JwtModule, JwtService } from '@nestjs/jwt';

describe('resetPassword (integration — real JwtService — CR-02 regression guard)', () => {
  const jwtSecret = 'test-jwt-secret-for-reset-integration';
  let realJwtService: JwtService;
  let authServiceLocal: AuthService;
  let integUserRepo: typeof mockUserRepo;
  let integConfig: { get: ReturnType<typeof vi.fn> };
  let integDb: typeof mockDb;
  let integSms: { sendVerification: ReturnType<typeof vi.fn>; verifyCode: ReturnType<typeof vi.fn> };
  let integEmail: typeof mockEmailService;

  beforeEach(async () => {
    // Create a real JwtService instance (JwtModule for integration)
    realJwtService = new JwtService({ secret: jwtSecret });

    integUserRepo = {
      findByEmail: vi.fn(),
      findById: vi.fn(),
      create: vi.fn(),
      updatePassword: vi.fn(),
    };
    integConfig = {
      get: vi.fn((key: string) => {
        if (key === 'auth.jwtSecret') return jwtSecret;
        if (key === 'FRONTEND_URL') return 'http://localhost:3000';
        return undefined;
      }),
    };
    integDb = {
      insert: vi.fn(),
      select: vi.fn(),
      update: vi.fn().mockReturnValue({
        set: vi.fn().mockReturnValue({ where: vi.fn().mockResolvedValue([]) }),
      }),
    };
    integSms = { sendVerification: vi.fn(), verifyCode: vi.fn() };
    integEmail = { sendPasswordResetEmail: vi.fn() };

    authServiceLocal = new AuthService(
      integUserRepo as any,
      realJwtService,
      integConfig as any,
      integSms as any,
      integDb as any,
      integEmail as any,
    );
  });

  it('정상 token (secret = jwtSecret + passwordHash) 은 성공적으로 resetPassword 를 완료한다', async () => {
    const userId = randomUUID();
    const user = { ...createMockUser(), id: userId, passwordHash: preHashedPassword };
    integUserRepo.findById.mockResolvedValue(user);

    const token = await realJwtService.signAsync(
      { sub: userId, purpose: 'password-reset' },
      { secret: jwtSecret + user.passwordHash, expiresIn: '1h' },
    );

    await expect(
      authServiceLocal.resetPassword(token, 'NewPass123!'),
    ).resolves.toBeUndefined();
    expect(integUserRepo.updatePassword).toHaveBeenCalledWith(userId, expect.any(String));
  }, 15000);

  it('sub 이 UUID 형식이 아니면 401 (DoS 가드 유지)', async () => {
    const token = await realJwtService.signAsync(
      { sub: 'not-a-uuid', purpose: 'password-reset' },
      { secret: jwtSecret + preHashedPassword, expiresIn: '1h' },
    );
    await expect(
      authServiceLocal.resetPassword(token, 'NewPass123!'),
    ).rejects.toThrow(UnauthorizedException);
  });

  it('passwordHash 누락된 secret 으로 서명된 token 은 final verify 에서 401', async () => {
    const userId = randomUUID();
    const user = { ...createMockUser(), id: userId, passwordHash: preHashedPassword };
    integUserRepo.findById.mockResolvedValue(user);

    const tokenWithoutHash = await realJwtService.signAsync(
      { sub: userId, purpose: 'password-reset' },
      { secret: jwtSecret, expiresIn: '1h' },  // passwordHash 빠짐
    );

    await expect(
      authServiceLocal.resetPassword(tokenWithoutHash, 'NewPass123!'),
    ).rejects.toThrow(UnauthorizedException);
  });
});
```

**주의:**
- `AuthService` 의 실제 생성자 인수 순서와 타입을 확인하고 맞춘다 (필요한 mock 주입만, `as any` 로 타입 우회).
- `createMockUser()` 는 기존 helper 재사용.
- `preHashedPassword` 도 기존 `beforeAll` 의 것을 재사용.

**Run and confirm RED:** `pnpm --filter @grapit/api test auth.service` — 첫 번째 테스트 ("정상 token... resetPassword 완료") 가 FAIL 해야 한다. Commit 전에 실패 내용 스크린샷/로그 확인.
  </action>

  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api test auth.service 2>&1 | tee /tmp/reset-bk-red.log; grep -qE "FAIL|failed|✗" /tmp/reset-bk-red.log && echo "RED confirmed"</automated>
  </verify>

  <done>
첫 번째 integration test ("정상 token... 성공") 가 FAIL (RED) 상태. 나머지 두 테스트는 pass 여도 OK. Commit 메시지: `test(09-04): add real-JwtService integration test reproducing CR-02 regression`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6 (TDD GREEN): backend preliminary verify → decode 로 교체 + mock 정리</name>
  <files>apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/auth/auth.service.spec.ts</files>

  <behavior>
    - `auth.service.ts:264-276` 의 preliminary `jwtService.verifyAsync` 호출을 `jwtService.decode` 로 교체한다. decode 는 서명 검증을 수행하지 않으므로 preliminary 단계에서 합법 토큰을 거짓 양성으로 차단하지 않는다.
    - DoS 가드 목적은 유지: decoded.sub 이 string 이 아니거나 UUID_REGEX 에 맞지 않으면 즉시 401.
    - final verify (line 286-296) 는 그대로 유지 — `jwtSecret + passwordHash` 로 서명 + 만료 검증이므로 위조 토큰은 여기서 걸러짐.
    - 기존 unit test mock 정리:
      * `auth.service.spec.ts:621-633` 의 `mockJwtService.verifyAsync.mockImplementation` 에서 `ignoreExpiration` 분기를 제거하고 final verify 용 단일 분기만 남긴다 (preliminary 는 더 이상 verifyAsync 호출 안 함).
      * `mockJwtService` 객체에 `decode: vi.fn()` 필드를 추가하고, 필요한 테스트에서 `{ sub: userId, purpose: 'password-reset' }` 반환하도록 설정.
      * `describe('resetPassword')` 의 첫 테스트 ("should update password hash with valid token", line 376-409) 도 `mockJwtService.decode.mockReturnValue(...)` 추가.
  </behavior>

  <action>
**Step 1 — `apps/api/src/modules/auth/auth.service.ts` 수정:**

기존 (line 264-276):
```typescript
let preliminarySub: string;
try {
  const decoded = await this.jwtService.verifyAsync<{ sub: unknown }>(
    token,
    { secret: jwtSecret, ignoreExpiration: true },
  );
  if (typeof decoded.sub !== 'string' || !UUID_REGEX.test(decoded.sub)) {
    throw new Error('invalid sub');
  }
  preliminarySub = decoded.sub;
} catch {
  throw new UnauthorizedException('유효하지 않은 재설정 토큰입니다');
}
```

→ 변경:
```typescript
// Preliminary: decode(서명 검증 없음) 로 sub 만 추출. DoS 가드는 UUID 형식 검사로 유지한다.
// 서명은 아래 final verify 에서 jwtSecret + passwordHash 로 엄격 검증되므로 보안 약화 아님.
let preliminarySub: string;
try {
  const decoded = this.jwtService.decode<{ sub?: unknown } | null>(token);
  if (!decoded || typeof decoded.sub !== 'string' || !UUID_REGEX.test(decoded.sub)) {
    throw new Error('invalid sub');
  }
  preliminarySub = decoded.sub;
} catch {
  throw new UnauthorizedException('유효하지 않은 재설정 토큰입니다');
}
```

주석의 CR-01 관련 설명 주석(line 254-257) 도 현재 동작에 맞게 업데이트:
```typescript
// 1. Preliminary: decode 로 sub 만 추출. 서명은 검증하지 않고 형식(UUID) 검사로 DB lookup 전
//    payload-amplification DoS 와 PostgreSQL 22P02 에러 누출을 차단한다. 서명/만료 검증은
//    아래 final verify 단계 (jwtSecret + passwordHash) 에서 수행된다.
```

**Step 2 — `apps/api/src/modules/auth/auth.service.spec.ts` mock 정리:**

`mockJwtService` 타입에 `decode` 추가:
```typescript
let mockJwtService: {
  signAsync: ReturnType<typeof vi.fn>;
  verifyAsync: ReturnType<typeof vi.fn>;
  decode: ReturnType<typeof vi.fn>;
};
```

`beforeEach` 에서 `mockJwtService = { signAsync: vi.fn(), verifyAsync: vi.fn(), decode: vi.fn() }` 로 초기화.

`describe('resetPassword')` 첫 테스트 (line 376-409) 에 `mockJwtService.decode.mockReturnValue({ sub: userId, purpose: 'password-reset' })` 추가.

`describe('requestPasswordReset → ... resetPassword 성공')` integration 섹션 (line 600 근처) 의 `mockJwtService.verifyAsync.mockImplementation` 에서 preliminary 분기 제거:
```typescript
mockJwtService.decode.mockReturnValue({ sub: currentUser.id, purpose: 'password-reset' });
mockJwtService.verifyAsync.mockImplementation(async (_token, opts) => {
  const fullSecret = `${TEST_JWT_SECRET}${currentUser.passwordHash ?? ''}`;
  if (opts.secret === fullSecret) {
    return { sub: currentUser.id, purpose: 'password-reset' };
  }
  throw new Error('invalid signature');
});
```

동일 integration 섹션의 line 667-672 assertion 도 단순화 (preliminary 호출 기대 제거).

두 번째 integration test (rotation prevent second use, line 717 이후) 도 유사하게 조정.

**Run and confirm GREEN:**
```bash
pnpm --filter @grapit/api test auth.service
```
Task 5 에서 추가한 integration 테스트 3개 + 기존 unit 테스트 모두 pass.
  </action>

  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api test auth.service 2>&1 | tee /tmp/reset-bk-green.log; grep -qE "✓|passed" /tmp/reset-bk-green.log && ! grep -qE "FAIL|failed|✗" /tmp/reset-bk-green.log</automated>
  </verify>

  <done>
모든 auth.service 테스트 pass. Commit 메시지: `fix(09-04): use jwtService.decode for preliminary sub extraction (CR-02)`
  </done>
</task>

<task type="auto">
  <name>Task 7: api 품질 게이트 (typecheck + lint + test + build)</name>
  <files>(없음 — 품질 게이트 실행만)</files>

  <action>
```bash
cd /Users/sangwopark19/icons/grapit
pnpm --filter @grapit/api typecheck
pnpm --filter @grapit/api lint
pnpm --filter @grapit/api test
pnpm --filter @grapit/api build
```
네 명령 모두 exit 0 이어야 한다. 실패 시 수정.
  </action>

  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api typecheck && pnpm --filter @grapit/api lint && pnpm --filter @grapit/api test && pnpm --filter @grapit/api build</automated>
  </verify>

  <done>
네 명령 모두 exit 0. 커밋 불필요. Task 4 (human-verify) 재개 가능.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 실제 dev 이메일 링크 → confirm 폼 수동 스모크</name>

  <what-built>
Phase 9 UAT Test 11 gap 을 닫는 변경:
1. `apps/web/app/auth/reset-password/page.tsx` — `useSearchParams()` 로 `token` query 분기 추가. token 존재 시 새 비밀번호 입력 폼 + `POST /api/v1/auth/password-reset/confirm` 호출.
2. `apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx` — vitest 4 tests (request 모드 회귀 + confirm 모드 렌더 + 성공 경로 + 401 에러 UI).
Backend / 이메일 템플릿 / `auth.service.ts` 변경 없음.
  </what-built>

  <how-to-verify>
**Prereq:** `.env` 에 `FRONTEND_URL=http://localhost:3000` 설정. `RESEND_API_KEY` 는 없어도 됨 (dev mock 모드로 콘솔에 이메일 URL 이 찍힘).

1. **Dev 서버 기동:**
   ```bash
   pnpm dev
   ```
   - api: port 8080, web: port 3000 기동 확인.

2. **회원가입 또는 기존 계정으로 reset request 트리거:**
   ```bash
   curl -X POST http://localhost:8080/api/v1/auth/password-reset/request \
     -H "Content-Type: application/json" \
     -d '{"email":"<존재하는-이메일>"}'
   ```
   → `200 OK`.

3. **`apps/api` 콘솔에서 `[DEV EMAIL]` 로그 확인:**
   - 로그 안에 `http://localhost:3000/auth/reset-password?token=<JWT>` 형태의 URL 이 출력됨.
   - 이 URL 을 복사한다.

4. **그 URL 을 브라우저 주소창에 붙여넣고 이동:**
   - **예상:** "새 비밀번호 설정" 제목 + "새 비밀번호" / "새 비밀번호 확인" 입력 필드 2개 + "비밀번호 변경" 버튼 렌더.
   - **실패 지표 (gap 재현):** "가입 시 사용한 이메일을 입력하세요" 텍스트가 보이면 실패 → 보고.

5. **새 비밀번호 입력 + 제출:**
   - 예: `Test1234!` 양쪽 동일 입력 → "비밀번호 변경" 클릭.
   - **예상:** 성공 toast ("비밀번호가 변경되었습니다. 다시 로그인해주세요.") → `/auth` 로 리다이렉트.

6. **변경된 비밀번호로 로그인 확인:**
   - `/auth` 에서 해당 이메일 + 새 비밀번호로 로그인 → 성공.

7. **회귀 체크 — token 없이 접속:**
   - `http://localhost:3000/auth/reset-password` (token 쿼리 없음) 직접 접속
   - **예상:** 기존 "비밀번호 찾기" / 이메일 입력 폼 그대로 렌더 (Test 1 pass 상태 유지).

8. **에러 경로 — 만료/위조 토큰:**
   - `http://localhost:3000/auth/reset-password?token=invalid.fake.token` 접속 → 비밀번호 입력 + 제출
   - **예상:** "유효하지 않은 링크" 에러 UI + "다시 요청하기" 버튼 표시. 이 버튼 클릭 시 `/auth/reset-password` (토큰 없음) 로 이동.

**Grep-verifiable acceptance (대시보드 참고용):**
```bash
cd /Users/sangwopark19/icons/grapit
grep -q "useSearchParams" apps/web/app/auth/reset-password/page.tsx && echo "OK: useSearchParams"
grep -q "/api/v1/auth/password-reset/confirm" apps/web/app/auth/reset-password/page.tsx && echo "OK: confirm endpoint"
grep -q "newPassword" apps/web/app/auth/reset-password/page.tsx && echo "OK: newPassword field"
grep -q "resetPasswordSchema" apps/web/app/auth/reset-password/page.tsx && echo "OK: shared schema"
```
네 줄 모두 OK 출력되어야 함.
  </how-to-verify>

  <resume-signal>
**선행 조건:** Task 5/6/7 (backend CR-02 fix) 완료 후에만 이 checkpoint 가 의미 있다. 1차 human UAT (2026-04-15) 에서 frontend 는 정상이지만 backend preliminary verify regression 으로 401 이 발생했기 때문.

`approved` (4–8번 모두 예상대로 동작) 또는 어떤 단계에서 어떤 현상이 발생했는지 설명.
실패 시 스크린샷/콘솔 로그와 함께 보고 → 필요 시 Task 2 (frontend) 또는 Task 6 (backend) 재작업.
  </resume-signal>
</task>

</tasks>

<verification>
## Phase-level 자동 검증

```bash
cd /Users/sangwopark19/icons/grapit

# 1. Grep-based acceptance (UAT 계약)
grep -q "useSearchParams" apps/web/app/auth/reset-password/page.tsx
grep -q "/api/v1/auth/password-reset/confirm" apps/web/app/auth/reset-password/page.tsx
grep -q "newPassword" apps/web/app/auth/reset-password/page.tsx
grep -q "resetPasswordSchema" apps/web/app/auth/reset-password/page.tsx

# 2. Tests
pnpm --filter @grapit/web test reset-password

# 3. Quality gates
pnpm --filter @grapit/web typecheck
pnpm --filter @grapit/web lint
pnpm --filter @grapit/web build
```

모두 exit 0 이어야 한다.
</verification>

<success_criteria>
- [ ] `apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx` 생성, 4 테스트 모두 pass.
- [ ] `apps/web/app/auth/reset-password/page.tsx` 내부에서 `useSearchParams()` 로 token 분기 추가. token 있으면 confirm UI, 없으면 기존 request UI.
- [ ] Confirm 제출이 `fetch('/api/v1/auth/password-reset/confirm', ...)` 로 이뤄진다 (apiClient 아님 — 401 auto-redirect 회피).
- [ ] Confirm body 에 `token`, `newPassword`, `newPasswordConfirm` 세 필드 포함.
- [ ] 401 응답 시 "다시 요청하기" 링크 (`/auth/reset-password`) 가 렌더된다.
- [ ] 성공 시 `/auth` 로 리다이렉트 + `toast.success` 호출.
- [ ] `pnpm --filter @grapit/web typecheck` + `lint` + `test` + `build` 모두 exit 0.
- [ ] 수동 스모크 (Task 4) 에서 실제 dev 이메일 링크 → 새 비밀번호 폼 → 비밀번호 변경 → 새 비밀번호로 로그인까지 성공.
- [ ] Backend / auth.service.ts / 이메일 템플릿 / 공유 스키마 변경 **없음**.
- [ ] Token 없는 `/auth/reset-password` 접속 시 기존 이메일 입력 폼 회귀 없음.
- [ ] UAT.md 의 gap (truth: "/auth/reset-password?token=... 에서 새 비밀번호 입력 UI 표시") → status: fixed 로 업데이트 가능.
</success_criteria>

<output>
실행 완료 후 `.planning/phases/09-tech-debt/09-04-SUMMARY.md` 생성. 포함:
- 구현한 Option (A — 최소 변경) 과 그 이유.
- 변경된 파일 (`page.tsx`, 신규 테스트) + 커밋 SHA 2개 (test/ + feat/).
- apiClient 대신 raw fetch 를 쓴 이유 (401 auto-redirect 회피) — 다음 phase 에서 재활용 가능한 learning.
- UAT Test 11 의 status 를 issue → pass 로 업데이트.
- ROADMAP.md DEBT-01 체크 (phase 09 success criteria #1 충족 여부).
</output>
