---
phase: 09-tech-debt
plan: 01
title: Quick Cleanup (DEBT-03 toast 일원화 + DEBT-04 formatDateTime 유틸 + DEBT-06 useShowtimes 제거)
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/ui/sonner.tsx
  - apps/web/components/booking/booking-page.tsx
  - apps/web/lib/format-datetime.ts
  - apps/web/lib/format-datetime.test.ts
  - apps/web/components/admin/admin-booking-detail-modal.tsx
  - apps/web/hooks/use-booking.ts
autonomous: true
requirements:
  - DEBT-03
  - DEBT-04
  - DEBT-06

must_haves:
  truths:
    - "sonner Toaster에 info 변형이 클래스 기반으로 정의되어 booking-page의 인라인 style이 제거된다"
    - "formatDateTime은 string | null | undefined 를 입력받아 null/undefined 시 em dash '—' (U+2014)를 반환한다"
    - "useShowtimes 훅 정의 및 모든 호출부가 제거되고 booking-page는 performance?.showtimes ?? [] 만 사용한다"
    - "pnpm --filter @grapit/web test seat-map-viewer 는 7/7 PASS 상태를 유지한다 (회귀 방지)"
    - "pnpm typecheck 0 errors, pnpm lint 0 errors, pnpm test 전체 PASS"
  artifacts:
    - path: "apps/web/lib/format-datetime.ts"
      provides: "Nullable-safe date/time formatter (string | null | undefined) -> string"
      exports: ["formatDateTime"]
    - path: "apps/web/lib/format-datetime.test.ts"
      provides: "Unit tests covering null, undefined, valid date cases"
      min_lines: 20
    - path: "apps/web/components/ui/sonner.tsx"
      provides: "Toaster with classNames.info override"
      contains: "info: 'bg-info-surface text-info border-info/20'"
  key_links:
    - from: "apps/web/components/booking/booking-page.tsx"
      to: "sonner toast.info"
      via: "toast.info('이미 다른 사용자가 선택한 좌석입니다') — NO inline style prop"
      pattern: "toast\\.info\\('이미 다른 사용자가 선택한 좌석입니다'\\)"
    - from: "apps/web/components/admin/admin-booking-detail-modal.tsx"
      to: "apps/web/lib/format-datetime.ts"
      via: "import { formatDateTime } from '@/lib/format-datetime'"
      pattern: "from '@/lib/format-datetime'"
    - from: "apps/web/components/booking/booking-page.tsx"
      to: "performance.showtimes"
      via: "allShowtimes = performance?.showtimes ?? [] (useShowtimes 제거 후)"
      pattern: "performance\\?\\.showtimes \\?\\? \\[\\]"
---

<objective>
v1.0에서 누적된 "빠른 정리" 기술부채 3건(DEBT-03/04/06)을 서로 독립적으로 묶어 한 번에 처리한다. 외부 의존성 0, 위험도 낮음, CI 초록 확보가 목적.

**Purpose:**
- CI 초록 상태를 Plan 2(외부 서비스 연동) 진입 전에 확정
- UI-SPEC이 확정한 "info toast classNames 일원화" 수용
- formatDateTime 중복 정의 제거 + nullable-safe 시그니처로 DRY 및 타입 안전성 확보
- dead code(`useShowtimes`) 제거로 유지보수 부담 감소

**Output:**
- 신규 `apps/web/lib/format-datetime.ts` 유틸 + 테스트
- `sonner.tsx` classNames.info 추가
- `booking-page.tsx`의 인라인 toast style 2곳 제거
- `admin-booking-detail-modal.tsx`에서 인라인 formatDateTime 제거, 유틸 import로 교체
- `use-booking.ts`의 useShowtimes 훅 제거 + booking-page.tsx 참조 제거

**CRITICAL 재프레이밍 (RESEARCH.md §Current State Probe Results 반영):**
- **DEBT-03 "회귀"는 존재하지 않는다.** `pnpm --filter @grapit/web test seat-map-viewer` → 7/7 PASS 확인됨 (RESEARCH §Current State Probe DEBT-03). `seat-map-viewer.tsx:161-162`은 이미 locked 허용 + sold 차단으로 D-17/D-18을 정확히 구현 중. **Production code `seat-map-viewer.tsx` 에는 절대 손대지 말 것.** 이 Plan의 DEBT-03 작업은 (a) `sonner.tsx`에 `classNames.info` 추가, (b) `booking-page.tsx` L193-195, L251-253의 인라인 `style={{ backgroundColor, color }}` 제거(클래스 기반으로 sonner가 처리)뿐이다.
- **DEBT-04 타입 경고는 이미 없다.** `pnpm typecheck` → 0 errors 확인됨 (RESEARCH §Current State Probe DEBT-04). 실제 작업은 `formatDateTime` 공통 유틸 추출 + nullable 수용 시그니처 도입. UI-SPEC 323-330 Discretion 확장 범위를 따라 `apps/web/lib/format-datetime.ts` 신설 후 admin-booking-detail-modal만 교체 (min scope). 나머지 3개 파일(booking-complete, reservation-detail, admin-booking-table)은 호출부가 non-null이므로 건드리지 않는다 — Pitfall 5(출력 포맷 불일치) 회피.
- **DEBT-06 useShowtimes 호출부 3곳:** `use-booking.ts:26-35` 훅 정의, `booking-page.tsx:10` import, `booking-page.tsx:44` 호출, `booking-page.tsx:70-72` fallback 삼항 → `performance?.showtimes ?? []` 로 단순화.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt/09-CONTEXT.md
@.planning/phases/09-tech-debt/09-RESEARCH.md
@.planning/phases/09-tech-debt/09-UI-SPEC.md
@.planning/phases/09-tech-debt/09-VALIDATION.md

@apps/web/components/ui/sonner.tsx
@apps/web/components/booking/booking-page.tsx
@apps/web/components/admin/admin-booking-detail-modal.tsx
@apps/web/hooks/use-booking.ts
@apps/web/components/booking/__tests__/seat-map-viewer.test.tsx

<interfaces>
<!-- 기존 코드 주요 시그니처 (executor가 변경 없이 보존해야 하는 contract) -->

From apps/web/components/ui/sonner.tsx (기존):
```typescript
// SonnerToaster wrapped with richColors + classNames.{success, error}.
// 신규: classNames.info 추가 — 기존 구조는 유지.
```

From apps/web/components/booking/booking-page.tsx L190-197 (기존 — 인라인 style 제거 대상):
```typescript
if (seatState === 'locked' && !selectedSeatIds.has(seatId)) {
  toast.info('이미 다른 사용자가 선택한 좌석입니다', {
    style: { backgroundColor: 'var(--color-info-surface)', color: 'var(--color-info)' },
  });
  return;
}
```
→ 목표:
```typescript
if (seatState === 'locked' && !selectedSeatIds.has(seatId)) {
  toast.info('이미 다른 사용자가 선택한 좌석입니다');
  return;
}
```

From apps/web/components/admin/admin-booking-detail-modal.tsx L41-49 (기존):
```typescript
function formatDateTime(dateString: string): string {
  const date = new Date(dateString);
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, '0');
  const d = String(date.getDate()).padStart(2, '0');
  const h = String(date.getHours()).padStart(2, '0');
  const min = String(date.getMinutes()).padStart(2, '0');
  return `${y}.${m}.${d} ${h}:${min}`;
}
```
→ 목표: `apps/web/lib/format-datetime.ts` 로 이동 + 시그니처 `(string | null | undefined) => string`. 파일 내부에서는 import 하고 local function 제거. L150 삼항 제거: `value={formatDateTime(booking.paymentInfo.paidAt)}` 로 단순화.

From apps/web/hooks/use-booking.ts L26-35 (기존):
```typescript
export function useShowtimes(performanceId: string) {
  return useQuery({
    queryKey: ['showtimes', performanceId],
    queryFn: () => apiClient.get<Showtime[]>(`/api/v1/performances/${performanceId}/showtimes`),
    enabled: false,
  });
}
```
→ 목표: 이 함수 전체 삭제 + import 정리. 호출하는 엔드포인트 `/api/v1/performances/:id/showtimes` 는 서버에 존재하지 않음 (RESEARCH §Current State Probe DEBT-06 확인).

From apps/web/components/booking/booking-page.tsx L10, L44, L68-72 (기존):
```typescript
import { useShowtimes, useSeatStatus, ... } from '@/hooks/use-booking';
// ...
const { data: showtimesData } = useShowtimes(performanceId);
// ...
const allShowtimes = useMemo(
  () => showtimesData ?? performance?.showtimes ?? [],
  [showtimesData, performance?.showtimes],
);
```
→ 목표:
```typescript
import { useSeatStatus, ... } from '@/hooks/use-booking';  // useShowtimes 제거
// useShowtimes 호출 줄 삭제
const allShowtimes = useMemo(
  () => performance?.showtimes ?? [],
  [performance?.showtimes],
);
```
</interfaces>
</context>

<tasks>

<task id="9-01-t0-wave0-tests" type="auto" tdd="true">
  <name>Task 0 (Wave 0 — TDD): formatDateTime 공통 유틸 테스트 작성 (RED)</name>
  <files>apps/web/lib/format-datetime.test.ts</files>
  <read_first>
    - apps/web/components/admin/admin-booking-detail-modal.tsx (L41-49: 기존 포맷 `YYYY.MM.DD HH:mm` 참조)
    - .planning/phases/09-tech-debt/09-UI-SPEC.md (L164-174: DEBT-04 empty state 규격 — em dash U+2014)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pitfall 5: 4개 파일 포맷 불일치 — 본 Plan은 admin-booking-detail-modal 포맷만 추출)
    - apps/web/vitest.config.ts (기존 vitest 설정)
    - apps/web/lib/cn.ts (기존 lib/ 유틸 구조 참조)
  </read_first>
  <behavior>
    - Test 1: `formatDateTime(null)` returns `"—"` (U+2014 em dash, 정확히 이 문자)
    - Test 2: `formatDateTime(undefined)` returns `"—"` (U+2014 em dash)
    - Test 3: `formatDateTime('2026-04-14T14:23:00Z')` returns a string that contains `'2026.'` and `':23'` — 실제 로컬 시간대에 따라 시간은 달라지나 연·월·일 포맷 `YYYY.MM.DD` + `HH:mm` 패턴이 보장되어야 함
    - Test 4 (optional): `formatDateTime('invalid-date-string')` returns `"—"` (NaN 처리) — 구현에 따라 포함
  </behavior>
  <action>
Create a new file `apps/web/lib/format-datetime.test.ts` with vitest tests. 이 파일은 아직 존재하지 않는 `formatDateTime` 을 import하므로 실행 시 MUST FAIL (RED 상태).

정확한 파일 내용:

```typescript
import { describe, it, expect } from 'vitest';
import { formatDateTime } from './format-datetime';

describe('formatDateTime', () => {
  it('returns em dash for null input', () => {
    expect(formatDateTime(null)).toBe('—');
  });

  it('returns em dash for undefined input', () => {
    expect(formatDateTime(undefined)).toBe('—');
  });

  it('formats a valid ISO date string to YYYY.MM.DD HH:mm', () => {
    const result = formatDateTime('2026-04-14T14:23:00Z');
    // 로컬 타임존에 따라 HH가 달라질 수 있으나, 연·월·일·콜론 포맷은 보장
    expect(result).toMatch(/^\d{4}\.\d{2}\.\d{2} \d{2}:\d{2}$/);
    expect(result).toContain('2026.');
  });

  it('returns em dash for invalid date strings', () => {
    expect(formatDateTime('not-a-date')).toBe('—');
  });
});
```

- U+2014 em dash 문자를 정확히 사용할 것 (하이픈 `-`, en dash `–` 금지).
- import 경로는 상대 경로 `./format-datetime` 로 — 같은 디렉토리 파일이므로 `@/` alias 불필요.

커밋은 TDD RED 단계로 별도 커밋: `test(09-tech-debt): add failing format-datetime tests`.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web test format-datetime 2>&1 | tee /tmp/09-01-t0.log ; grep -q "Cannot find module\|format-datetime" /tmp/09-01-t0.log && echo "RED CONFIRMED: formatDateTime module missing as expected"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/format-datetime.test.ts` exists
    - File contains exact string `"formatDateTime(null)"` and `"'—'"` (U+2014 em dash)
    - `pnpm --filter @grapit/web test format-datetime` exits non-zero (RED) with "Cannot find module './format-datetime'" or equivalent
    - git log shows commit `test(09-tech-debt): add failing format-datetime tests`
  </acceptance_criteria>
  <done>RED 테스트가 예상대로 실패하고, 다음 task가 GREEN을 만들 수 있는 상태</done>
</task>

<task id="9-01-t1-format-datetime-util" type="auto" tdd="true">
  <name>Task 1 (DEBT-04): formatDateTime 공통 유틸 생성 (GREEN) + admin-booking-detail-modal 교체</name>
  <files>
    apps/web/lib/format-datetime.ts,
    apps/web/components/admin/admin-booking-detail-modal.tsx
  </files>
  <read_first>
    - apps/web/components/admin/admin-booking-detail-modal.tsx (전체 — 특히 L41-49 formatDateTime 정의, L127/L150 호출부)
    - apps/web/lib/format-datetime.test.ts (Task 0 RED 테스트)
    - .planning/phases/09-tech-debt/09-UI-SPEC.md (L164-174: em dash U+2014, min scope — admin-booking-detail-modal만 교체)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pitfall 5: 4개 파일 통합 시 포맷 불일치 리스크 → 본 Plan은 1개 파일만)
    - packages/shared/src/types/booking.types.ts (paidAt 타입 확인 — string | null)
  </read_first>
  <action>
**Step 1.** Create `apps/web/lib/format-datetime.ts` with following exact content (Task 0 테스트를 GREEN으로 전환):

```typescript
/**
 * Format a date string to `YYYY.MM.DD HH:mm` (local time).
 * Returns em dash `—` (U+2014) when input is null, undefined, or invalid.
 *
 * Phase 9 DEBT-04: nullable-safe signature introduced to remove ternary callers.
 * See .planning/phases/09-tech-debt/09-UI-SPEC.md §DEBT-04 empty state.
 */
export function formatDateTime(dateString: string | null | undefined): string {
  if (dateString === null || dateString === undefined) {
    return '—';
  }

  const date = new Date(dateString);
  if (Number.isNaN(date.getTime())) {
    return '—';
  }

  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, '0');
  const d = String(date.getDate()).padStart(2, '0');
  const h = String(date.getHours()).padStart(2, '0');
  const min = String(date.getMinutes()).padStart(2, '0');
  return `${y}.${m}.${d} ${h}:${min}`;
}
```

**Step 2.** Modify `apps/web/components/admin/admin-booking-detail-modal.tsx`:
- Add import at top (after existing imports): `import { formatDateTime } from '@/lib/format-datetime';`
- Delete the inline `function formatDateTime(dateString: string): string { ... }` block (L41-49)
- Change L150 from:
  `value={booking.paymentInfo.paidAt ? formatDateTime(booking.paymentInfo.paidAt) : '—'}`
  to:
  `value={formatDateTime(booking.paymentInfo.paidAt)}`
- L127 호출은 showDateTime 이 non-null string이므로 그대로 두되, 유틸 함수의 signature로 호출은 문제 없음.

**Step 3.** Commit: `refactor(09-tech-debt): extract formatDateTime util with nullable signature (DEBT-04)`.

**Do NOT touch (scope guard — Pitfall 5):**
- `apps/web/components/booking/booking-complete.tsx` (Intl.DateTimeFormat 기반 — 다른 포맷)
- `apps/web/components/reservation/reservation-detail.tsx`
- `apps/web/components/admin/admin-booking-table.tsx`

위 3개 파일의 로컬 `formatDateTime`은 이번 Plan 범위 외. 호출부가 non-null로 타입이 안전하므로 건드리지 않음 — UI-SPEC 323-330 min scope 준수.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web test format-datetime && pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/format-datetime.ts` exists with exported `formatDateTime` function
    - `pnpm --filter @grapit/web test format-datetime` exits 0, all 4 tests pass
    - `pnpm --filter @grapit/web typecheck` exits 0
    - `grep -c "function formatDateTime" apps/web/components/admin/admin-booking-detail-modal.tsx` returns 0 (inline definition removed)
    - `grep -c "from '@/lib/format-datetime'" apps/web/components/admin/admin-booking-detail-modal.tsx` returns 1 (import added)
    - `grep -q "? formatDateTime(booking.paymentInfo.paidAt) : '—'" apps/web/components/admin/admin-booking-detail-modal.tsx` returns non-zero (삼항 제거됨)
    - Commit message matches `refactor(09-tech-debt): extract formatDateTime util with nullable signature (DEBT-04)`
    - booking-complete.tsx, reservation-detail.tsx, admin-booking-table.tsx 의 `function formatDateTime` 정의는 **그대로 유지** (3개 유지 확인)
  </acceptance_criteria>
  <done>formatDateTime 유틸이 nullable-safe 시그니처로 추출되고, admin-booking-detail-modal이 import로 사용하며 테스트가 모두 PASS</done>
</task>

<task id="9-01-t2-sonner-info-toast" type="auto" tdd="false">
  <name>Task 2 (DEBT-03): sonner classNames.info 추가 + booking-page 인라인 style 제거</name>
  <files>
    apps/web/components/ui/sonner.tsx,
    apps/web/components/booking/booking-page.tsx
  </files>
  <read_first>
    - apps/web/components/ui/sonner.tsx (전체 — 현재 classNames.success/error만 존재)
    - apps/web/components/booking/booking-page.tsx (L6, L190-197, L244-260)
    - apps/web/components/booking/__tests__/seat-map-viewer.test.tsx (L135-185 — 회귀 방지 참조)
    - .planning/phases/09-tech-debt/09-UI-SPEC.md (L143-161: DEBT-03 toast 변형 결정)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Current State Probe DEBT-03 — 7/7 PASS 유지 필수 / §Pitfall 4 production code 수정 금지)
    - apps/web/app/globals.css (L43-44 `--color-info`, `--color-info-surface` 토큰 존재 확인)
  </read_first>
  <action>
**CRITICAL:** Production 파일 `apps/web/components/booking/seat-map-viewer.tsx` 및 테스트 파일 `apps/web/components/booking/__tests__/seat-map-viewer.test.tsx` 에는 **절대 손대지 말 것**. 기존 7/7 PASS 상태를 유지해야 한다 (Pitfall 4).

**Step 1.** `apps/web/components/ui/sonner.tsx` 수정 — `classNames` 객체에 `info` 키 추가:

기존 (L15-18):
```typescript
classNames: {
  success: 'bg-success-surface text-success border-success/20',
  error: 'bg-error-surface text-error border-error/20',
},
```

변경 후:
```typescript
classNames: {
  success: 'bg-success-surface text-success border-success/20',
  error: 'bg-error-surface text-error border-error/20',
  info: 'bg-info-surface text-info border-info/20',
},
```

**Step 2.** `apps/web/components/booking/booking-page.tsx` 의 인라인 style 2곳 제거:

**Location 1 (L193-195, locked seat click):**
기존:
```typescript
toast.info('이미 다른 사용자가 선택한 좌석입니다', {
  style: { backgroundColor: 'var(--color-info-surface)', color: 'var(--color-info)' },
});
```
변경 후:
```typescript
toast.info('이미 다른 사용자가 선택한 좌석입니다');
```

**Location 2 (L251-253, 409 Conflict on lock API error):**
기존:
```typescript
toast.info('이미 다른 사용자가 선택한 좌석입니다', {
  style: { backgroundColor: 'var(--color-info-surface)', color: 'var(--color-info)' },
});
```
변경 후:
```typescript
toast.info('이미 다른 사용자가 선택한 좌석입니다');
```

**Step 3.** Commit: `refactor(09-tech-debt): unify locked-seat info toast via sonner classNames (DEBT-03)`.

**NOT allowed in this task:**
- `seat-map-viewer.tsx` production 코드 수정 (Pitfall 4)
- `seat-map-viewer.test.tsx` 수정 (이미 green, 건드리지 말 것)
- Sold seat 동작 변경 (D-18에 의해 현재 차단 동작 유지)
- toast.info 호출 message 문구 변경 (UI-SPEC L149 contract)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web test seat-map-viewer && grep -c "info: 'bg-info-surface text-info border-info/20'" apps/web/components/ui/sonner.tsx && ! grep -q "color-info-surface" apps/web/components/booking/booking-page.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @grapit/web test seat-map-viewer` exits 0, all 7 tests PASS (회귀 없음)
    - `grep -c "info: 'bg-info-surface text-info border-info/20'" apps/web/components/ui/sonner.tsx` returns 1
    - `grep -c "backgroundColor: 'var(--color-info-surface)'" apps/web/components/booking/booking-page.tsx` returns 0 (인라인 style 제거됨)
    - `grep -c "style: { backgroundColor" apps/web/components/booking/booking-page.tsx` returns 0
    - `grep -c "toast.info\\('이미 다른 사용자가 선택한 좌석입니다'\\)" apps/web/components/booking/booking-page.tsx` returns 2 (두 호출부 모두 단순 호출)
    - `git diff HEAD~1 -- apps/web/components/booking/seat-map-viewer.tsx` returns empty (production 코드 미변경)
    - `git diff HEAD~1 -- apps/web/components/booking/__tests__/seat-map-viewer.test.tsx` returns empty (테스트 미변경)
    - Commit message matches `refactor(09-tech-debt): unify locked-seat info toast via sonner classNames (DEBT-03)`
  </acceptance_criteria>
  <done>Info toast가 sonner classNames로 일원화되고, 인라인 style이 2곳 모두 제거되며, seat-map-viewer 회귀 없음(7/7 PASS)</done>
</task>

<task id="9-01-t3-remove-useShowtimes" type="auto" tdd="false">
  <name>Task 3 (DEBT-06): useShowtimes 훅 및 호출부 전체 제거</name>
  <files>
    apps/web/hooks/use-booking.ts,
    apps/web/components/booking/booking-page.tsx
  </files>
  <read_first>
    - apps/web/hooks/use-booking.ts (L1-50 — useShowtimes 정의 위치 및 import 구조)
    - apps/web/components/booking/booking-page.tsx (L1-80 — import, 호출, allShowtimes useMemo)
    - .planning/phases/09-tech-debt/09-CONTEXT.md (D-15, D-16: 삭제 범위)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Current State Probe DEBT-06 — endpoint 미존재 확인)
    - apps/api/src/modules/performance/performance.controller.ts (endpoint 목록 확인 — showtimes 라우트 없음)
  </read_first>
  <action>
**Step 1.** `apps/web/hooks/use-booking.ts` 에서 `useShowtimes` 함수 전체 제거 (L26-35 블록 10줄 모두 삭제):

제거할 블록:
```typescript
export function useShowtimes(performanceId: string) {
  return useQuery({
    queryKey: ['showtimes', performanceId],
    queryFn: () =>
      apiClient.get<import('@grapit/shared').Showtime[]>(
        `/api/v1/performances/${performanceId}/showtimes`,
      ),
    enabled: false,
  });
}
```

함수 전체 및 바로 앞뒤의 빈 줄을 정리하여 파일이 lint 에러 없이 깔끔하게 유지되도록 한다.

**Step 2.** `apps/web/components/booking/booking-page.tsx` 에서 useShowtimes 참조 제거:

**2a. Import 제거 (L9-16):**
기존:
```typescript
import {
  useShowtimes,
  useSeatStatus,
  useMyLocks,
  useLockSeat,
  useUnlockSeat,
  useUnlockAllSeats,
} from '@/hooks/use-booking';
```
변경 후 (useShowtimes 한 줄만 제거):
```typescript
import {
  useSeatStatus,
  useMyLocks,
  useLockSeat,
  useUnlockSeat,
  useUnlockAllSeats,
} from '@/hooks/use-booking';
```

**2b. 호출 줄 제거 (L44):**
`const { data: showtimesData } = useShowtimes(performanceId);` — 이 한 줄 완전 삭제

**2c. allShowtimes useMemo 단순화 (L68-72):**
기존:
```typescript
const allShowtimes = useMemo(
  () => showtimesData ?? performance?.showtimes ?? [],
  [showtimesData, performance?.showtimes],
);
```
변경 후:
```typescript
const allShowtimes = useMemo(
  () => performance?.showtimes ?? [],
  [performance?.showtimes],
);
```

**Step 3.** Commit: `refactor(09-tech-debt): remove dead useShowtimes hook (DEBT-06)`.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web lint && pnpm --filter @grapit/web test && test "$(grep -rn 'useShowtimes' apps/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useShowtimes" apps/` returns zero matches (완전 제거)
    - `grep -rn "showtimesData" apps/web/` returns zero matches
    - `grep -c "export function useShowtimes" apps/web/hooks/use-booking.ts` returns 0
    - `pnpm --filter @grapit/web typecheck` exits 0
    - `pnpm --filter @grapit/web lint` exits 0
    - `pnpm --filter @grapit/web test` exits 0 (all existing unit tests pass)
    - `grep -c "performance?.showtimes ?? \\[\\]" apps/web/components/booking/booking-page.tsx` returns 1
    - Commit message matches `refactor(09-tech-debt): remove dead useShowtimes hook (DEBT-06)`
  </acceptance_criteria>
  <done>useShowtimes 훅과 모든 호출부가 제거되고 booking-page가 단순화된 fallback만 사용하며 전체 빌드/타입/린트/테스트가 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 | Plan 01은 클라이언트 측 refactor만 — 사용자 입력/외부 서비스/신규 권한 boundary 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | n/a | n/a | accept | **Plan 01은 신규 위협을 유입하지 않는다.** 모든 변경은 (a) 내부 유틸 추출, (b) Toaster 스타일 일원화, (c) dead code 제거로 구성. 외부 서비스/PII/권한 변경 없음. ASVS L1 해당 사항 없음. |
| T-09-02 | Information Disclosure (잔여) | useShowtimes 제거로 endpoint fingerprinting 감소 | accept | `/api/v1/performances/:id/showtimes` 는 서버에 존재하지 않으므로 클라이언트 참조 삭제가 실제 exposure 감소는 아니지만, dead route 참조 제거로 codebase 신뢰도 소폭 상승. |

RESEARCH §Security Domain V5/V7/V14 카테고리 해당 없음 — Plan 01은 입력 검증/에러 처리/설정 없음.
</threat_model>

<verification>
**Wave-level sampling (VALIDATION.md §Sampling Rate 준수):**

1. **각 태스크 커밋 직후 (per-task, < 60s):**
   - `pnpm --filter @grapit/web test <narrow-pattern>`

2. **Plan 전체 완료 직후 (per-wave, < 90s):**
   - `pnpm typecheck` (루트)
   - `pnpm lint` (루트)
   - `pnpm test` (루트)

3. **수동 검증 (선택, UX 연결성 확인):**
   - 로컬 dev 서버에서 좌석 예매 페이지 진입 → locked 좌석 클릭 → info toast가 기존과 동일한 보라색(`#6C3CE0`/`#F3EFFF` 계열) 배경으로 뜨는지 확인 (sonner classNames로 전환되었으므로 시각적 차이 없음이 목표)

**Failure recovery:**
- seat-map-viewer 테스트가 7/7 미만으로 내려가면 즉시 롤백 — Plan 01은 production seat-map-viewer.tsx를 건드리지 않도록 설계됨
- typecheck 실패 시 formatDateTime import 경로 (`@/lib/format-datetime`) 확인
</verification>

<success_criteria>
- [ ] `pnpm typecheck` (루트) exits 0
- [ ] `pnpm lint` (루트) exits 0
- [ ] `pnpm test` (루트) exits 0 — 특히 seat-map-viewer 7/7, format-datetime 4/4 PASS
- [ ] `grep -rn "useShowtimes" apps/` returns 0 matches
- [ ] `apps/web/lib/format-datetime.ts` exists + exports `formatDateTime` with signature `(string | null | undefined) => string`
- [ ] `apps/web/components/ui/sonner.tsx` contains `info: 'bg-info-surface text-info border-info/20'`
- [ ] `apps/web/components/booking/booking-page.tsx` contains no `style: { backgroundColor` inline prop on `toast.info`
- [ ] `apps/web/components/booking/seat-map-viewer.tsx` is **unchanged** (git diff empty vs HEAD before plan)
- [ ] 3개의 의미 있는 커밋 생성 (test RED, DEBT-04 refactor, DEBT-03 refactor, DEBT-06 refactor) — 4 커밋 (TDD RED 1개 + 3개 DEBT)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tech-debt/09-01-SUMMARY.md` covering:
- DEBT-03/04/06 closure evidence (CI commands + results)
- files_modified actual vs planned
- Any Pitfall 4/5 triggers encountered (should be none)
- Time taken
- Next: Plan 02 (Terms + Email) can begin
</output>
