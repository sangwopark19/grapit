---
phase: 09-tech-debt
plan: 02
title: Terms + Email (DEBT-02 약관 MD + LegalDraftBanner + DEBT-01 Resend EmailService + rate limit + reset flow test)
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/main.ts
  - apps/api/src/modules/auth/email/email.service.ts
  - apps/api/src/modules/auth/email/email.service.spec.ts
  - apps/api/src/modules/auth/email/email.module.ts
  - apps/api/src/modules/auth/email/templates/password-reset.tsx
  - apps/api/src/modules/auth/auth.service.ts
  - apps/api/src/modules/auth/auth.service.spec.ts
  - apps/api/src/modules/auth/auth.controller.ts
  - apps/api/src/modules/auth/auth.module.ts
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/env.d.ts
  - apps/web/content/legal/terms-of-service.md
  - apps/web/content/legal/privacy-policy.md
  - apps/web/content/legal/marketing-consent.md
  - apps/web/components/legal/legal-draft-banner.tsx
  - apps/web/components/legal/terms-markdown.tsx
  - apps/web/components/auth/signup-step2.tsx
  - .env.example
  - .github/workflows/deploy.yml
  - .planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02

must_haves:
  truths:
    - "EmailService가 NODE_ENV=production + RESEND_API_KEY 미설정 시 bootstrap 시점에 throw (hard-fail, Phase 7 REDIS_URL 패턴 복제)"
    - "EmailService가 NODE_ENV=production + RESEND_FROM_EMAIL 미설정 또는 유효하지 않은 이메일 시 bootstrap 시점에 throw (REVIEWS.md MED 반영 — onboarding@resend.dev fallback 금지)"
    - "NestJS bootstrap(main.ts)에서 NODE_ENV=production + FRONTEND_URL 미설정/non-https 시 process.exit(1) (REVIEWS.md MED 반영)"
    - "dev 환경(RESEND_API_KEY 미설정)에서는 console.log로 reset link 출력, Resend API 호출 없음"
    - "프로덕션 모드에서는 Resend SDK의 emails.send({ react: <PasswordResetEmail resetLink /> })를 호출하고 {data, error} 응답을 처리"
    - "auth.service.ts:217-241 requestPasswordReset의 console.log stub이 this.emailService.sendPasswordResetEmail() 호출로 교체됨 (silent return L220-222 유지 — email enumeration 방지)"
    - "auth.service.spec.ts에 dev-mock 통합 테스트 추가: requestPasswordReset → EmailService.sendPasswordResetEmail 호출 → token 추출 → resetPassword(token, newPassword) 성공 → DB 비밀번호 해시 변경 검증 (REVIEWS.md HIGH-03 반영)"
    - "resetPassword 가 JwtService.verifyAsync 를 호출할 때 secret 이 `jwtSecret + user.passwordHash` 형태로 전달됨이 spec 에서 assert 됨 (Blocker B4: JwtService mock 이 secret rotation 을 bypass 하지 않도록)"
    - "비밀번호 변경 후 동일 토큰을 재사용한 resetPassword 호출은 JwtService.verifyAsync 의 secret mismatch 로 인해 UnauthorizedException 을 throw 함 (Blocker B4: 일회성 토큰 rotation 실증)"
    - "/auth/password-reset/request 및 /auth/password-reset/confirm 엔드포인트에 @Throttle 데코레이터로 per-IP rate limit 적용: 3req/15min (REVIEWS.md HIGH-04 반영) — @nestjs/throttler v6 (package.json 에 ^6.4.0 fixed) 의 `{ default: { limit: 3, ttl: 900000 } }` 객체 시그니처만 사용"
    - "회원가입 약관 Dialog에서 3개 MD 파일이 raw-loader로 정적 번들링되어 LegalDraftBanner + TermsMarkdown JSX로 렌더됨"
    - "privacy-policy.md 가 국외이전(제6조) 섹션을 포함하여 Resend(US) / Twilio(US) 수탁자와 이전 항목·목적·보유기간·이전 방법을 명시 (Blocker B3: 개인정보보호법 제28조의8 준수)"
    - "LegalDraftBanner에 role='note' + aria-label='초안 안내' + '본 약관은 런칭 전 법률 검토를 거쳐 교체될 초안입니다.' 문구 표시 + WCAG AA contrast (대비 4.5:1 이상) 검증 완료 (W5)"
    - "nodemailer + @types/nodemailer 패키지가 제거되어 의존성 혼동 제거됨"
    - ".env.example과 .github/workflows/deploy.yml secrets에 RESEND_API_KEY, RESEND_FROM_EMAIL 추가"
    - "privacy-policy.md에 명시된 외부 위탁사(Resend/Twilio/Toss Payments)와 수집 항목이 실제 users table schema + 실제 코드상 통합 서비스와 일치함 (09-02-LEGAL-ACCURACY-CHECKLIST.md 체크리스트 수기 검증 통과)"
    - "pnpm --filter @grapit/api build 성공 (React Email TSX 실 빌드 검증) — REVIEWS.md MED 반영"
    - "pnpm --filter @grapit/web build 성공 (Turbopack raw-loader 실 빌드 검증) — REVIEWS.md MED 반영"
    - "pnpm --filter @grapit/api test email.service 6-branch 테스트 전체 PASS"
  artifacts:
    - path: "apps/api/src/modules/auth/email/email.service.ts"
      provides: "EmailService with Resend integration + dev mock + prod hard-fail (API_KEY + FROM_EMAIL 둘 다)"
      exports: ["EmailService", "SendEmailResult"]
    - path: "apps/api/src/modules/auth/email/email.service.spec.ts"
      provides: "6-branch unit tests (dev / prod-real / prod-misconfig-api-key / prod-misconfig-from-email-unset / prod-misconfig-from-email-invalid / prod-sdk-error)"
      min_lines: 80
    - path: "apps/api/src/modules/auth/email/email.module.ts"
      provides: "NestJS module exposing EmailService to AuthModule"
      exports: ["EmailModule"]
    - path: "apps/api/src/modules/auth/email/templates/password-reset.tsx"
      provides: "React Email TSX template for password reset"
      exports: ["PasswordResetEmail"]
    - path: "apps/api/src/modules/auth/auth.service.spec.ts"
      provides: "Extended with password reset link flow integration test (dev mock → token extract → resetPassword → DB hash change) + JwtService secret rotation assert + one-time token reuse rejection"
      contains: "describe('password reset flow integration'"
    - path: "apps/web/content/legal/terms-of-service.md"
      provides: "Grapit 이용약관 초안 (KOPICO 표준 기반)"
      min_lines: 40
    - path: "apps/web/content/legal/privacy-policy.md"
      provides: "Grapit 개인정보처리방침 초안 + 국외이전(제6조) 섹션 (개인정보보호법 제28조의8)"
      min_lines: 55
    - path: "apps/web/content/legal/marketing-consent.md"
      provides: "Grapit 마케팅 수신 동의 초안"
      min_lines: 15
    - path: "apps/web/components/legal/legal-draft-banner.tsx"
      provides: "법률 검토 초안 안내 배너 컴포넌트"
      exports: ["LegalDraftBanner"]
    - path: "apps/web/components/legal/terms-markdown.tsx"
      provides: "react-markdown 기반 MD→JSX 타이포 매핑 렌더러"
      exports: ["TermsMarkdown"]
    - path: "apps/web/env.d.ts"
      provides: "*.md module type declaration (string default export)"
      contains: "declare module '*.md'"
    - path: ".planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md"
      provides: "Manual checklist cross-referencing privacy-policy.md against actual users table + integrated services + 국외이전 검증 row"
      contains: "## 수집 항목 vs users table schema"
  key_links:
    - from: "apps/api/src/modules/auth/auth.service.ts L234-240"
      to: "EmailService.sendPasswordResetEmail"
      via: "constructor 주입 + await this.emailService.sendPasswordResetEmail(email, resetLink)"
      pattern: "this\\.emailService\\.sendPasswordResetEmail"
    - from: "apps/api/src/modules/auth/auth.controller.ts"
      to: "@Throttle({ default: { limit: 3, ttl: 900000 } })"
      via: "decorator on POST password-reset/request AND password-reset/confirm"
      pattern: "@Throttle\\("
    - from: "apps/api/src/main.ts"
      to: "FRONTEND_URL validation"
      via: "bootstrap: production NODE_ENV + missing or non-https FRONTEND_URL → throw + process.exit(1)"
      pattern: "FRONTEND_URL.*https://"
    - from: "apps/api/src/modules/auth/auth.module.ts"
      to: "EmailModule"
      via: "imports: [ ..., EmailModule ]"
      pattern: "EmailModule"
    - from: "apps/web/components/auth/signup-step2.tsx"
      to: "LegalDraftBanner + TermsMarkdown + 3 MD files"
      via: "Dialog body renders <LegalDraftBanner /> followed by <TermsMarkdown>{markdownContent}</TermsMarkdown>"
      pattern: "LegalDraftBanner|TermsMarkdown"
    - from: "apps/web/next.config.ts"
      to: "*.md raw loader"
      via: "turbopack.rules: { '*.md': { as: '*.js', loaders: ['raw-loader'] } } — CLAUDE.md REVIEWS.md MED 해결: 한 접근법만 사용"
      pattern: "loaders: \\['raw-loader'\\]"
    - from: ".github/workflows/deploy.yml"
      to: "Secret Manager resend-api-key + resend-from-email"
      via: "secrets: RESEND_API_KEY=resend-api-key:latest, RESEND_FROM_EMAIL=resend-from-email:latest"
      pattern: "RESEND_API_KEY=resend-api-key:latest"
---

<objective>
Phase 9 기술부채 **6건 중 2건(DEBT-01 password reset 이메일 + DEBT-02 이용약관 실제 텍스트)** 을 해소한다. 두 건은 "인프라 도입"이라는 공통 축을 공유하므로 한 Plan에서 처리한다. (Phase 9 전체는 DEBT-01~06 6건; "기술부채 12건" 표현은 본 phase 범위가 아님.)

**Purpose:**
- `auth.service.ts:240` 의 console.log stub을 Resend 기반 실제 이메일 발송으로 교체 (D-01~D-05)
- `signup-step2.tsx:22-38` 의 TERMS_CONTENT 인라인 placeholder를 3개 MD 파일 기반 구조로 교체 (D-06~D-09)
- Phase 7 REDIS_URL hard-fail 원칙을 EmailService에도 적용 (production에서 silent console.log 금지)
- 레거시 `nodemailer` 패키지 정리 (Pitfall 6 회피)

**D-06 구현 전환 주석 (W3 — CONTEXT.md 는 불변):**
- CONTEXT.md D-06 은 "`?raw` import" 를 기술했지만, RESEARCH.md §Pitfall 1 (Turbopack 미지원) 에 의해 **raw-loader 기반 turbopack rule 로 실행 전환**. 같은 목적(MD 정적 번들), 다른 구현. CONTEXT.md 자체는 수정하지 않음 — 본 plan objective 가 footnote 역할.

**REVIEWS.md 피드백 반영 (HIGH/MED 수정):**
- **HIGH-03: password reset link → password change 플로우 실증.** `auth.service.spec.ts` 에 통합 테스트 추가 — dev mock 모드에서 EmailService를 spy한 뒤 `requestPasswordReset(email)` → spy로 resetLink 추출 → `resetPassword(token, newPwd)` 호출 → `userRepository.updatePassword` 가 새 해시로 호출되었는지 assert.
- **HIGH-04: 공개 엔드포인트 rate limiting 부재.** `@Throttle({ default: { limit: 3, ttl: 900000 } })` 를 `/auth/password-reset/request` 와 `/confirm` 에 추가 (3 req / 15 min / IP). `@nestjs/throttler@^6.4.0` 이 `apps/api/package.json` 에 fixed — v6 객체 시그니처만 사용 (v5 fallback 제거).
- **MED (RESEND_FROM_EMAIL hard-fail):** production NODE_ENV 에서 `RESEND_FROM_EMAIL` 이 없거나 이메일 형식이 아니면 throw. `onboarding@resend.dev` fallback은 dev 전용.
- **MED (FRONTEND_URL validation):** NestJS bootstrap(`main.ts`) 에서 production NODE_ENV 일 때 `FRONTEND_URL` 이 없거나 `https://` 로 시작하지 않으면 로그 후 `process.exit(1)`.
- **MED (MD raw import 통일):** 접근 방식을 **raw-loader 단일화** — `turbopack.rules: { '*.md': { as: '*.js', loaders: ['raw-loader'] } }` + `pnpm add -D -F @grapit/web raw-loader`. `type: 'raw'` 옵션은 문서 곳곳에서 언급하지 말 것 (일관성 위반 제거). `pnpm --filter @grapit/web build` 가 acceptance criteria 에 들어감.
- **MED (API React Email TSX build):** `apps/api/tsconfig.json` 에 `"jsx": "react-jsx"` + `"jsxImportSource": "react"` 추가. `apps/api/package.json` 에 `react` runtime dependency 추가 (peer 아닌 direct). `pnpm --filter @grapit/api build` 가 acceptance criteria 에 들어감 (Task 3 — Task 2 직후 typecheck 는 피함. 이유: Task 0 의 RED spec 이 아직 GREEN 되지 않아 typecheck 가 필연적으로 실패).
- **MED (Legal MD 정확성):** privacy-policy.md 의 외부 위탁사/수집 항목이 실제와 일치하는지 체크리스트(`.planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md`)로 수동 검증. `users` table schema + 실 통합 서비스(Resend/Twilio/Toss)와 cross-reference. **추가 (Blocker B3): 국외이전(제28조의8) row 포함.**
- **MED (grep -c 버그):** 모든 `<verify>` / `<acceptance_criteria>` 에서 expected-absence는 `! grep -q ...` 또는 `test "$(grep -c ... | tr -d ' ')" = "0"` 패턴만 사용. `grep -c ... && ...` 체이닝 금지.

**Revision-2 Blocker 반영 (이 revision iteration 의 핵심 수정):**
- **Blocker B3: privacy-policy.md 에 국외이전(제6조) 섹션 추가.** 개인정보보호법 제28조의8 — Resend(US), Twilio(US) 수탁자에 대한 이전 항목/목적/보유기간/이전 방법 을 표 형태로 기재. Google Cloud Platform (asia-northeast3, 국내 리전) 은 이전이 아니므로 제외. Toss(KR) 는 제3자 제공(제4조) 에 이미 기재. 기존 조항 번호 재배치: 제5조 (위탁) → 제6조 (국외이전, 신설) → 제7조 (정보주체 권리, 기존 제6조) → … — 이하 넘버링 밀기.
- **Blocker B4: Task 6 JwtService mock 이 secret rotation bypass 허용.** 수정 방안 A (mock 유지 + secret arg assert + one-time reuse reject test) 선택. JwtService.verifyAsync mock 을 `mockImplementation` 으로 재작성하여 secret argument 를 검증하고, password 변경 후 동일 토큰 재사용 시 secret mismatch 로 인해 UnauthorizedException throw 되는 시나리오 추가.
- **Blocker B5: @nestjs/throttler v5 fallback 제거.** `apps/api/package.json` 에 `@nestjs/throttler@^6.4.0` 이 fixed 되어 있음 — v6 객체 시그니처만 사용. Task 5 `read_first` 에 package.json 확인 step 추가.
- **Blocker B6: Task 2 의 typecheck verify 불가능.** Task 2 (tsconfig.json JSX 추가) 시점엔 Task 0 의 RED spec 이 이미 존재해 typecheck 가 실패할 수밖에 없음. Task 2 verify 를 grep-only 로 축소. typecheck / build 검증은 Task 3 (EmailService GREEN) 으로 이동.
- **W1: Plan 3 Task 5 `<files>` 공란.** (Plan 3 범위 — 본 Plan 에 영향 없음)
- **W2: Plan 2 scope 유지 (12 tasks, 22 files).** 분할하지 않음. 대신 objective 에 **Task 6 완료 후 checkpoint** 지시를 명시 — executor 는 Task 6 커밋 후 잠시 멈추고 상태를 확인한 뒤 Task 7-11 로 진행.

**Executor checkpoint (W2):**
Plan 2 는 12 tasks / 22 files 로 규모가 크다. 분할 대신 다음 인터벌에서 executor 가 상태를 자체 점검하고 진행:
1. **Task 6 커밋 완료 후 (half-way checkpoint):** `pnpm --filter @grapit/api test` + `pnpm --filter @grapit/api typecheck` + `pnpm --filter @grapit/api build` 모두 green 인지 확인. git log 에서 6 commits 존재 (t0~t6, t3 은 GREEN commit). Green 실패 시 Task 7 로 진행하지 말고 원인 수정.
2. **Task 11 완료 후 (plan 완료):** `pnpm typecheck` + `pnpm lint` + `pnpm test` + `pnpm --filter @grapit/web build` 모두 green. 12 commits 존재.

**CRITICAL 재프레이밍 (RESEARCH.md 반영):**
- **Resend SDK는 `render()` 호출이 불필요.** `resend.emails.send({ react: <PasswordResetEmail /> })` 가 TSX를 직접 수용한다 (RESEARCH §Pattern 2, Pitfall 2). `@react-email/render` 패키지 설치 금지 — `@react-email/components` 만 설치.
- **Resend SDK는 throw하지 않는다.** `{ data, error }` 반환 구조 — try/catch가 아니라 `if (error)` 분기 필수 (Pitfall 2).
- **NODE_ENV=production + RESEND_API_KEY 미설정 시 throw.** Phase 7의 `REDIS_URL` hard-fail 원칙 복제 (RESEARCH §Example 3, §Security V14 misconfig). Silent console.log fallback 금지.
- **Turbopack `?raw` import는 지원되지 않는다.** `turbopack.rules: { '*.md': { as: '*.js', loaders: ['raw-loader'] } }` + `declare module '*.md'` 타입 선언 (RESEARCH §Pattern 3, Pitfall 1).
- **nodemailer 명시적 제거.** `apps/api/package.json:38,61` 에 `nodemailer@^8.0.4` + `@types/nodemailer@^7.0.11` 설치되어 있으나 사용처 없음 (Pitfall 6). 본 Plan에서 `pnpm --filter @grapit/api remove` 로 제거.
- **이메일 enumeration 방지 보존.** `auth.service.ts:220-222` silent return (user not found 시 void 반환) — V7 Error Handling 보안 요건. 변경 금지.
- **EmailService는 별도 모듈로.** `auth.module.ts` providers 배열에 직접 추가하지 말고 `email.module.ts` 를 별도로 만들어 `exports: [EmailService]` + `imports: [EmailModule]` (Pitfall 7, SMS/User 모듈 패턴 일치).

**Dependency on Plan 01:** Plan 01 완료 후 CI green 상태에서 시작. 새로 추가되는 web 파일들은 `@/lib/format-datetime` 등 Plan 01 산출물과 독립적이지만, sonner classNames 변경 이후 signup-step2 내 toast가 정상 동작해야 하므로 Plan 01의 artifact가 배포 가능 상태여야 함.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt/09-CONTEXT.md
@.planning/phases/09-tech-debt/09-RESEARCH.md
@.planning/phases/09-tech-debt/09-UI-SPEC.md
@.planning/phases/09-tech-debt/09-VALIDATION.md
@.planning/phases/09-tech-debt/09-REVIEWS.md
@.planning/phases/09-tech-debt/09-01-PLAN.md

# API 참조 (EmailService SMS 패턴 복제 + AppModule Throttler 이미 등록)
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/sms.module.ts
@apps/api/src/modules/booking/providers/redis.provider.ts
@apps/api/src/modules/auth/auth.service.ts
@apps/api/src/modules/auth/auth.controller.ts
@apps/api/src/modules/auth/auth.module.ts
@apps/api/src/modules/auth/auth.service.spec.ts
@apps/api/src/app.module.ts
@apps/api/src/main.ts
@apps/api/tsconfig.json
@apps/api/package.json

# Web 참조 (Dialog 구조 + 기존 dev-server config)
@apps/web/components/auth/signup-step2.tsx
@apps/web/next.config.ts
@apps/web/components/ui/dialog.tsx
@apps/web/package.json

# Infra 참조
@.github/workflows/deploy.yml

<interfaces>
<!-- EmailService contract (REVIEWS.md MED hard-fail 두 키 모두 반영) -->

Target signature for `apps/api/src/modules/auth/email/email.service.ts`:
```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Resend } from 'resend';
import { PasswordResetEmail } from './templates/password-reset.js';

export interface SendEmailResult {
  success: boolean;
  id?: string;
  error?: string;
}

const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

@Injectable()
export class EmailService {
  private readonly logger = new Logger(EmailService.name);
  private readonly resend: Resend | null;
  private readonly from: string;
  private readonly isDevMode: boolean;

  constructor(private readonly configService: ConfigService) {
    const apiKey = this.configService.get<string>('RESEND_API_KEY');
    const fromEmail = this.configService.get<string>('RESEND_FROM_EMAIL');
    const nodeEnv = this.configService.get<string>('NODE_ENV');
    const isProd = nodeEnv === 'production';

    // REVIEWS.md MED hard-fail 1: RESEND_API_KEY
    if (isProd && !apiKey) {
      throw new Error(
        '[email] RESEND_API_KEY is required in production environment. ' +
          'Silent console.log fallback is disabled to prevent missed password reset emails. ' +
          'Check Cloud Run secret binding.',
      );
    }

    // REVIEWS.md MED hard-fail 2: RESEND_FROM_EMAIL — no onboarding@resend.dev fallback in prod
    if (isProd) {
      if (!fromEmail || !EMAIL_PATTERN.test(fromEmail)) {
        throw new Error(
          '[email] RESEND_FROM_EMAIL must be a valid email in production. ' +
            `Received: ${fromEmail ?? '<unset>'}. ` +
            'Phishing/deliverability risk — configure a verified sender in Resend dashboard.',
        );
      }
      this.from = fromEmail;
    } else {
      // dev/test: default to onboarding@resend.dev only when not production
      this.from = fromEmail ?? 'onboarding@resend.dev';
    }

    this.isDevMode = !apiKey;
    if (this.isDevMode) {
      this.resend = null;
      this.logger.warn('Email Service running in DEV MOCK mode (no RESEND_API_KEY)');
    } else {
      this.resend = new Resend(apiKey!);
    }
  }

  async sendPasswordResetEmail(to: string, resetLink: string): Promise<SendEmailResult> {
    if (this.isDevMode) {
      this.logger.log(`DEV EMAIL: password reset link for ${to}: ${resetLink}`);
      return { success: true };
    }
    const { data, error } = await this.resend!.emails.send({
      from: this.from,
      to,
      subject: '[Grapit] 비밀번호 재설정',
      react: PasswordResetEmail({ resetLink }),
    });
    if (error) {
      this.logger.error(`Resend send failed for ${to}: ${error.message}`);
      return { success: false, error: error.message };
    }
    return { success: true, id: data?.id };
  }
}
```

Target signature for `apps/api/src/modules/auth/email/email.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { EmailService } from './email.service.js';

@Module({
  imports: [ConfigModule],
  providers: [EmailService],
  exports: [EmailService],
})
export class EmailModule {}
```

Target signature for `apps/api/src/modules/auth/email/templates/password-reset.tsx`:
```tsx
import { Html, Head, Body, Container, Heading, Text, Button, Hr, Section } from '@react-email/components';

interface PasswordResetEmailProps {
  resetLink: string;
}

export function PasswordResetEmail({ resetLink }: PasswordResetEmailProps) {
  return (
    <Html lang="ko">
      <Head />
      <Body style={{ backgroundColor: '#f5f5f7', fontFamily: 'system-ui, sans-serif' }}>
        <Container style={{ backgroundColor: '#ffffff', padding: '32px', maxWidth: '560px' }}>
          <Heading style={{ fontSize: '20px', color: '#1A1A2E' }}>비밀번호 재설정 안내</Heading>
          <Text style={{ fontSize: '14px', color: '#4A4A5E' }}>
            아래 버튼을 눌러 비밀번호를 재설정해주세요. 이 링크는 1시간 동안만 유효합니다.
          </Text>
          <Section style={{ textAlign: 'center', margin: '24px 0' }}>
            <Button href={resetLink} style={{ backgroundColor: '#6C3CE0', color: '#ffffff', padding: '12px 24px', borderRadius: '6px' }}>
              비밀번호 재설정
            </Button>
          </Section>
          <Hr />
          <Text style={{ fontSize: '12px', color: '#6B6B7B' }}>
            본 메일을 요청하지 않으셨다면 무시하셔도 됩니다.
          </Text>
        </Container>
      </Body>
    </Html>
  );
}
```

AuthService integration delta (auth.service.ts):
- constructor: 기존 파라미터 목록에 `private readonly emailService: EmailService,` 추가 (SmsService 바로 뒤, @Inject(DRIZZLE) 바로 앞)
- L234-240 console.log 스텁을 다음으로 교체:
  ```typescript
  const frontendUrl = this.configService.get<string>('FRONTEND_URL');
  const resetLink = `${frontendUrl}/auth/reset-password?token=${resetToken}`;
  await this.emailService.sendPasswordResetEmail(email, resetLink);
  ```
- `import { EmailService } from './email/email.service.js';` 를 기존 import 블록에 추가
- L220-222 silent return (user 미존재 시 void 반환)은 **변경 금지** (V7 email enumeration 방지)

AuthController rate limit delta (auth.controller.ts):
- `import { Throttle } from '@nestjs/throttler';` 추가
- `POST /auth/password-reset/request` (L117-126) 위에:
  ```typescript
  @Throttle({ default: { limit: 3, ttl: 900000 } })  // 3 req / 15 min (REVIEWS.md HIGH-04; v6 object signature, 900000ms = 15min NOT 900 seconds)
  ```
- `POST /auth/password-reset/confirm` (L128-137) 위에 동일 decorator 추가 (confirm도 abuse 대상)

AuthModule integration delta (auth.module.ts):
- `imports: [ ..., SmsModule, EmailModule ]` — SmsModule 바로 뒤에 EmailModule 추가
- `import { EmailModule } from './email/email.module.js';` 추가

main.ts bootstrap FRONTEND_URL validation (REVIEWS.md MED):
```typescript
// Add right after `const app = await NestFactory.create(AppModule);`
const frontendUrl = process.env['FRONTEND_URL'];
if (process.env['NODE_ENV'] === 'production') {
  if (!frontendUrl || !frontendUrl.startsWith('https://')) {
    console.error(
      `[bootstrap] FRONTEND_URL must be an https URL in production. Received: ${frontendUrl ?? '<unset>'}. ` +
        'Reset links and email deliverability depend on this. Aborting startup.',
    );
    process.exit(1);
  }
}
```

apps/api/tsconfig.json delta (REVIEWS.md MED — React Email TSX build):
- `"jsx": "react-jsx"` 및 `"jsxImportSource": "react"` 를 `compilerOptions` 에 추가 (없을 경우)
- 기존 NestJS 설정과 충돌 없음 (JSX 파일만 영향)

apps/api/package.json delta (REVIEWS.md MED — React Email runtime dep):
- dependencies 에 `"react": "^19.0.0"` 추가 (Next.js 16 버전과 동일하게)
- `@react-email/components` 의 peer React 요구사항 충족 (직접 dependency로)

Target for `apps/web/next.config.ts` turbopack rules (REVIEWS.md MED — 단일 접근법, raw-loader만 사용):
- 기존 `turbopack: { root: resolve(__dirname, '../../') }` 블록에 `rules` 추가:
  ```typescript
  turbopack: {
    root: resolve(__dirname, '../../'),
    rules: {
      '*.md': { as: '*.js', loaders: ['raw-loader'] },
    },
  },
  ```

Target for `apps/web/env.d.ts` (신규 파일):
```typescript
declare module '*.md' {
  const content: string;
  export default content;
}
```

Target for `apps/web/components/legal/legal-draft-banner.tsx` (RESEARCH §Pattern 5 — W5 contrast 반영):
```tsx
import { AlertTriangle } from 'lucide-react';

export function LegalDraftBanner() {
  return (
    <div
      role="note"
      aria-label="초안 안내"
      className="mb-6 flex items-start gap-2 rounded-md border border-warning/30 border-l-4 border-l-warning bg-warning-surface px-4 py-3"
    >
      <AlertTriangle className="size-4 text-warning shrink-0 mt-0.5" aria-hidden="true" />
      <span className="text-caption text-warning-foreground">
        본 약관은 런칭 전 법률 검토를 거쳐 교체될 초안입니다.
      </span>
    </div>
  );
}
```
(W5: 기존 하드코딩 `text-[#8B6306]` 대비 globals.css 의 `text-warning-foreground` 토큰 사용 — 디자인 시스템이 WCAG AA contrast 를 이미 보장. Task 9 에서 토큰 존재 확인 후 적용; 미존재 시 `text-[#8B6306]` 유지 + `bg-warning-surface` 조합 manual 대비 측정 ≥ 4.5:1 확인 필요.)

Target for `apps/web/components/legal/terms-markdown.tsx` (RESEARCH §Pattern 4 verbatim):
```tsx
'use client';

import ReactMarkdown, { type Components } from 'react-markdown';
import remarkGfm from 'remark-gfm';

const components: Components = {
  h1: () => null,
  h2: (props) => <h2 className="text-base font-semibold text-gray-900 mt-6 first:mt-0" {...props} />,
  h3: (props) => <h3 className="text-caption font-semibold text-gray-800 mt-4" {...props} />,
  p: (props) => <p className="text-caption leading-relaxed text-gray-700 mt-2" {...props} />,
  ul: (props) => <ul className="text-caption leading-relaxed text-gray-700 mt-2 ml-5 list-disc" {...props} />,
  ol: (props) => <ol className="text-caption leading-relaxed text-gray-700 mt-2 ml-5 list-decimal" {...props} />,
  li: (props) => <li className="mt-1" {...props} />,
  strong: (props) => <strong className="font-semibold text-gray-900" {...props} />,
  a: (props) => <a className="text-primary underline hover:text-primary/80" target="_blank" rel="noopener noreferrer" {...props} />,
  hr: () => <hr className="my-4 border-gray-200" />,
};

export function TermsMarkdown({ children }: { children: string }) {
  return <ReactMarkdown components={components} remarkPlugins={[remarkGfm]}>{children}</ReactMarkdown>;
}
```

Target for `apps/web/components/auth/signup-step2.tsx` L22-38 (TERMS_CONTENT 교체):
- 기존 `const TERMS_CONTENT` 객체 제거
- 파일 상단 추가 import:
  ```typescript
  import termsOfServiceMd from '@/content/legal/terms-of-service.md';
  import privacyPolicyMd from '@/content/legal/privacy-policy.md';
  import marketingConsentMd from '@/content/legal/marketing-consent.md';
  import { LegalDraftBanner } from '@/components/legal/legal-draft-banner';
  import { TermsMarkdown } from '@/components/legal/terms-markdown';
  ```
- 새 상수:
  ```typescript
  const LEGAL_CONTENT: Record<string, { title: string; content: string }> = {
    termsOfService: { title: '이용약관', content: termsOfServiceMd },
    privacyPolicy: { title: '개인정보처리방침', content: privacyPolicyMd },
    marketingConsent: { title: '마케팅 수신 동의', content: marketingConsentMd },
  };
  ```
- L183-195 Dialog body 교체 (LegalDraftBanner + TermsMarkdown 조합)

deploy.yml secrets delta:
- API job `secrets:` 블록 끝에 추가 (L105 뒤):
  ```yaml
  RESEND_API_KEY=resend-api-key:latest
  RESEND_FROM_EMAIL=resend-from-email:latest
  ```
</interfaces>
</context>

<tasks>

<task id="9-02-t0-wave0-email-test" type="auto" tdd="true">
  <name>Task 0 (Wave 0 — TDD): EmailService 6-branch spec 작성 (RED)</name>
  <files>apps/api/src/modules/auth/email/email.service.spec.ts</files>
  <read_first>
    - apps/api/src/modules/sms/sms.service.ts (dev mock 패턴 참조)
    - apps/api/src/modules/booking/providers/redis.provider.ts L220-236 (REDIS_URL hard-fail 패턴)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pattern 1 verbatim, §Pitfall 2 Resend {data, error})
    - .planning/phases/09-tech-debt/09-VALIDATION.md (Per-Task map Plan 02 rows)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: RESEND_FROM_EMAIL hard-fail)
    - apps/api/vitest.config.ts (vitest 설정)
  </read_first>
  <behavior>
    - Test 1 (dev mock): RESEND_API_KEY 미설정 + NODE_ENV=development 환경 → EmailService 생성은 성공, `sendPasswordResetEmail('a@b.com', 'https://x.test')` 호출 시 Resend 없이 `{ success: true }` 반환
    - Test 2 (prod real): RESEND_API_KEY='re_test_key' + RESEND_FROM_EMAIL='no-reply@grapit.com' + NODE_ENV=production → Resend SDK의 `emails.send`가 호출됨 (mock). `{ data: { id: 'mock-id' }, error: null }` 반환 시 `{ success: true, id: 'mock-id' }` 반환. `from` 파라미터가 `no-reply@grapit.com` 과 일치.
    - Test 3 (prod misconfig — API_KEY): RESEND_API_KEY 미설정 + NODE_ENV=production → 생성자가 `RESEND_API_KEY is required in production` 메시지로 throw
    - Test 4 (prod misconfig — FROM_EMAIL): RESEND_API_KEY='re_test_key' + RESEND_FROM_EMAIL 미설정 + NODE_ENV=production → 생성자가 `RESEND_FROM_EMAIL must be a valid email in production` 메시지로 throw (REVIEWS.md MED)
    - Test 5 (prod misconfig — FROM_EMAIL invalid): RESEND_API_KEY='re_test_key' + RESEND_FROM_EMAIL='not-an-email' + NODE_ENV=production → 생성자가 `RESEND_FROM_EMAIL must be a valid email in production` 메시지로 throw
    - Test 6 (prod SDK error): RESEND_API_KEY 설정 + Resend가 `{ data: null, error: { message: 'rate limit' } }` 반환 → `{ success: false, error: 'rate limit' }` 반환 (try/catch 아님, Pitfall 2)
  </behavior>
  <action>
Create file `apps/api/src/modules/auth/email/email.service.spec.ts` with 6 test cases. `EmailService` 모듈은 아직 존재하지 않으므로 import가 실패해야 함 (RED).

정확한 파일 내용:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { ConfigService } from '@nestjs/config';
import { EmailService } from './email.service.js';

// Mock the resend module — hoisted by vitest so the import in email.service picks up the mock
vi.mock('resend', () => {
  const sendMock = vi.fn();
  return {
    Resend: vi.fn().mockImplementation(() => ({
      emails: { send: sendMock },
    })),
    __sendMock: sendMock,
  };
});

import * as resendModule from 'resend';

function makeConfig(env: Record<string, string | undefined>): ConfigService {
  return {
    get: vi.fn(<T>(key: string, defaultValue?: T): T | undefined => {
      const v = env[key];
      return (v !== undefined ? v : defaultValue) as T | undefined;
    }),
  } as unknown as ConfigService;
}

describe('EmailService', () => {
  const originalEnv = { ...process.env };

  beforeEach(() => {
    vi.clearAllMocks();
  });

  afterEach(() => {
    process.env = { ...originalEnv };
  });

  it('DEV mode: no RESEND_API_KEY + NODE_ENV=development → returns success without calling Resend', async () => {
    const config = makeConfig({ NODE_ENV: 'development' });
    const svc = new EmailService(config);
    const result = await svc.sendPasswordResetEmail('user@example.com', 'https://app.test/reset?t=abc');
    expect(result).toEqual({ success: true });
    expect((resendModule as unknown as { Resend: ReturnType<typeof vi.fn> }).Resend).not.toHaveBeenCalled();
  });

  it('PROD mode: RESEND_API_KEY + RESEND_FROM_EMAIL set + NODE_ENV=production → calls Resend.emails.send with react template and correct from', async () => {
    const config = makeConfig({
      RESEND_API_KEY: 're_test_key',
      RESEND_FROM_EMAIL: 'no-reply@grapit.com',
      NODE_ENV: 'production',
    });
    const mod = resendModule as unknown as { __sendMock: ReturnType<typeof vi.fn> };
    mod.__sendMock.mockResolvedValueOnce({ data: { id: 'mock-id' }, error: null });

    const svc = new EmailService(config);
    const result = await svc.sendPasswordResetEmail('user@example.com', 'https://app.test/reset?t=abc');

    expect(mod.__sendMock).toHaveBeenCalledTimes(1);
    const callArg = mod.__sendMock.mock.calls[0]?.[0] as { from: string; to: string; subject: string; react: unknown };
    expect(callArg.from).toBe('no-reply@grapit.com');
    expect(callArg.to).toBe('user@example.com');
    expect(callArg.subject).toContain('비밀번호 재설정');
    expect(callArg.react).toBeDefined();
    expect(result).toEqual({ success: true, id: 'mock-id' });
  });

  it('PROD misconfig (API_KEY): no RESEND_API_KEY + NODE_ENV=production → throws on construction', () => {
    const config = makeConfig({ NODE_ENV: 'production' });
    expect(() => new EmailService(config)).toThrow(/RESEND_API_KEY is required in production/);
  });

  it('PROD misconfig (FROM_EMAIL unset): RESEND_API_KEY set + RESEND_FROM_EMAIL unset + NODE_ENV=production → throws on construction', () => {
    const config = makeConfig({ RESEND_API_KEY: 're_test_key', NODE_ENV: 'production' });
    expect(() => new EmailService(config)).toThrow(/RESEND_FROM_EMAIL must be a valid email in production/);
  });

  it('PROD misconfig (FROM_EMAIL invalid): RESEND_API_KEY set + RESEND_FROM_EMAIL is not an email + NODE_ENV=production → throws on construction', () => {
    const config = makeConfig({
      RESEND_API_KEY: 're_test_key',
      RESEND_FROM_EMAIL: 'not-an-email',
      NODE_ENV: 'production',
    });
    expect(() => new EmailService(config)).toThrow(/RESEND_FROM_EMAIL must be a valid email in production/);
  });

  it('PROD SDK error: Resend returns { error } → returns { success: false, error }', async () => {
    const config = makeConfig({
      RESEND_API_KEY: 're_test_key',
      RESEND_FROM_EMAIL: 'no-reply@grapit.com',
      NODE_ENV: 'production',
    });
    const mod = resendModule as unknown as { __sendMock: ReturnType<typeof vi.fn> };
    mod.__sendMock.mockResolvedValueOnce({ data: null, error: { message: 'rate limit exceeded' } });

    const svc = new EmailService(config);
    const result = await svc.sendPasswordResetEmail('user@example.com', 'https://app.test/reset?t=abc');

    expect(result).toEqual({ success: false, error: 'rate limit exceeded' });
  });
});
```

Commit: `test(09-tech-debt): add failing EmailService 6-branch spec (DEBT-01)`.

**NOT allowed:** real Resend API calls (mock 필수), real process.env mutation outside beforeEach/afterEach.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api test email.service 2>&1 | tee /tmp/09-02-t0.log ; grep -q "Cannot find module\|Failed to resolve" /tmp/09-02-t0.log && echo "RED CONFIRMED: EmailService module not yet created"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `apps/api/src/modules/auth/email/email.service.spec.ts` exists
    - File contains 6 `it(...)` test blocks
    - File contains exact string `RESEND_API_KEY is required in production`
    - File contains exact string `RESEND_FROM_EMAIL must be a valid email in production`
    - `pnpm --filter @grapit/api test email.service` exits non-zero with module-not-found for `./email.service.js`
    - Commit matches `test(09-tech-debt): add failing EmailService 6-branch spec (DEBT-01)`
  </acceptance_criteria>
  <done>6개 RED 테스트가 예상대로 모듈 미존재로 실패하고 있음</done>
</task>

<task id="9-02-t1-install-deps" type="auto" tdd="false">
  <name>Task 1: 패키지 설치 (resend + @react-email/components + react + raw-loader + react-markdown + remark-gfm) 및 nodemailer 제거</name>
  <files>
    apps/api/package.json,
    apps/web/package.json
  </files>
  <read_first>
    - apps/api/package.json (L38, L61: nodemailer + @types/nodemailer 존재 확인)
    - apps/web/package.json (현재 의존성 — react-markdown/remark-gfm/raw-loader 미설치 확인)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pitfall 6 nodemailer 혼동 / §Don't Hand-Roll / §Standard Stack §Core)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: API에 react runtime 필요 + web에 raw-loader 필요)
    - CLAUDE.md (pnpm filter + DOTENV_CONFIG_PATH convention)
  </read_first>
  <action>
**Step 1 (API 추가 — REVIEWS.md MED: React runtime dep + Resend + React Email):**
```bash
pnpm --filter @grapit/api add resend @react-email/components react
```
- Version pinning: pnpm이 최신 안정 버전을 선택 (resend ^4.x, @react-email/components ^0.x, react ^19.x).
- `react` 는 `@react-email/components` 의 peer dependency이자 TSX 컴포넌트 런타임. 프로덕션 번들에 포함됨.
- `@react-email/render` 는 **설치하지 말 것** (Resend SDK가 react 파라미터로 직접 렌더 — RESEARCH §Anti-Patterns).

**Step 2 (API 제거):** `nodemailer` + `@types/nodemailer` 제거.
```bash
pnpm --filter @grapit/api remove nodemailer @types/nodemailer
```
- 제거 후 `apps/api/src` 내부에 nodemailer 참조가 남아있지 않은지 확인: `grep -rn "nodemailer" apps/api/src` → 0 matches.

**Step 3 (Web 추가 — REVIEWS.md MED: raw-loader 단일 접근법):**
```bash
pnpm --filter @grapit/web add react-markdown remark-gfm
pnpm --filter @grapit/web add -D raw-loader
```
- `raw-loader` 는 devDependencies로 추가 (빌드타임에만 사용).
- Tailwind Typography (`@tailwindcss/typography`) 는 **설치하지 말 것** — UI-SPEC L71 "번들 증가 회피" 지시.

**Step 4:** 루트 `pnpm install` 로 lockfile 정합성 확인.

**Step 5:** Commit: `chore(09-tech-debt): install resend + react-email + react + raw-loader + react-markdown, remove nodemailer`.

**Post-check (REVIEWS.md MED 모두 반영):**
- `apps/api/package.json` dependencies에 `resend`, `@react-email/components`, `react` 존재
- `apps/api/package.json` dependencies에 `nodemailer` 부재
- `apps/api/package.json` devDependencies에 `@types/nodemailer` 부재
- `apps/web/package.json` dependencies에 `react-markdown`, `remark-gfm` 존재
- `apps/web/package.json` devDependencies에 `raw-loader` 존재
- pnpm-lock.yaml 업데이트됨
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q '"resend"' apps/api/package.json && grep -q '"@react-email/components"' apps/api/package.json && grep -q '"react":' apps/api/package.json && ! grep -q '"nodemailer"' apps/api/package.json && ! grep -q '"@types/nodemailer"' apps/api/package.json && grep -q '"react-markdown"' apps/web/package.json && grep -q '"remark-gfm"' apps/web/package.json && grep -q '"raw-loader"' apps/web/package.json && test "$(grep -rn 'nodemailer' apps/api/src | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/package.json` contains `"resend"`, `"@react-email/components"`, `"react"` in dependencies
    - `apps/api/package.json` does NOT contain `"nodemailer"` in dependencies (`! grep -q '"nodemailer"' apps/api/package.json`)
    - `apps/api/package.json` does NOT contain `"@types/nodemailer"` (`! grep -q '"@types/nodemailer"' apps/api/package.json`)
    - `apps/web/package.json` contains `"react-markdown"` and `"remark-gfm"` in dependencies
    - `apps/web/package.json` contains `"raw-loader"` in devDependencies
    - `test "$(grep -rn 'nodemailer' apps/api/src | wc -l | tr -d ' ')" = "0"`
    - `pnpm install` exits 0 (lockfile clean)
    - Commit matches `chore(09-tech-debt): install resend + react-email + react + raw-loader + react-markdown, remove nodemailer`
  </acceptance_criteria>
  <done>API/Web 패키지가 설치되고 nodemailer가 제거되며 lockfile이 정합함</done>
</task>

<task id="9-02-t2-api-tsconfig-jsx" type="auto" tdd="false">
  <name>Task 2 (REVIEWS.md MED, Blocker B6): apps/api/tsconfig.json에 JSX 설정 추가 (grep-only verify)</name>
  <files>
    apps/api/tsconfig.json
  </files>
  <read_first>
    - apps/api/tsconfig.json (현재 compilerOptions)
    - apps/api/nest-cli.json (NestJS 빌드 설정)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: API React Email TSX build risk)
    - node_modules/@react-email/components/package.json (peer react 버전 확인)
  </read_first>
  <action>
**Step 1.** `apps/api/tsconfig.json` 의 `compilerOptions` 객체에 JSX 관련 옵션 추가:

기존 (예상):
```json
{
  "compilerOptions": {
    "module": "NodeNext",
    "target": "ES2023",
    ...
  }
}
```

변경 (JSX 두 개 키 추가):
```json
{
  "compilerOptions": {
    "module": "NodeNext",
    "target": "ES2023",
    ...
    "jsx": "react-jsx",
    "jsxImportSource": "react"
  }
}
```

- `"jsx": "react-jsx"` — React 17+ 자동 JSX 변환 (import React 불필요)
- `"jsxImportSource": "react"` — JSX 런타임을 `react/jsx-runtime` 에서 import

**Step 2.** NestJS 빌드 호환 확인:
- NestJS 11 의 swc 빌드는 `nest-cli.json` 의 `compilerOptions` 또는 프로젝트 tsconfig를 사용.
- `apps/api/nest-cli.json` 에 별도 JSX 설정이 없으면 tsconfig 가 유일한 source of truth — Step 1 수정으로 충분.
- 만약 `nest-cli.json` 에 `"plugins"` 혹은 별도 compiler options가 있다면 JSX 설정 유지되는지 확인.

**Step 3.** Commit: `chore(09-tech-debt): enable JSX in apps/api/tsconfig for React Email TSX (DEBT-01)`.

**Blocker B6 — Why no typecheck / no build at this task:**
- Task 0 이 이미 `apps/api/src/modules/auth/email/email.service.spec.ts` 를 RED (import `./email.service.js` 실패) 상태로 만들어 두었음.
- 따라서 이 시점에 `pnpm --filter @grapit/api typecheck` 는 `Cannot find module './email.service.js'` 로 반드시 실패함. Task 2 verify 에 typecheck 를 넣으면 executor 가 "내 변경 때문에 실패한 것 아닌가" 로 혼란을 겪게 됨.
- 대신 JSX 설정이 파일에 정확히 적용되었는지만 `grep -q` 로 확인. 실제 typecheck / build 검증은 **Task 3** (EmailService GREEN) 에서 수행.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q '"jsx":\s*"react-jsx"' apps/api/tsconfig.json && grep -q '"jsxImportSource":\s*"react"' apps/api/tsconfig.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/tsconfig.json` contains `"jsx": "react-jsx"` (정규식: `"jsx":\s*"react-jsx"`)
    - `apps/api/tsconfig.json` contains `"jsxImportSource": "react"` (정규식: `"jsxImportSource":\s*"react"`)
    - Commit matches `chore(09-tech-debt): enable JSX in apps/api/tsconfig for React Email TSX (DEBT-01)`
    - (typecheck / build 검증은 Task 3 로 이동됨 — Blocker B6)
  </acceptance_criteria>
  <done>apps/api tsconfig 가 TSX 파일을 이해할 준비 완료 (파일 단독 변경 기준으로만 검증; typecheck 는 Task 3 에서)</done>
</task>

<task id="9-02-t3-email-service-green" type="auto" tdd="true">
  <name>Task 3 (DEBT-01): EmailService + template + module 구현 (GREEN) + API typecheck + API build 검증 (Blocker B6 이동분)</name>
  <files>
    apps/api/src/modules/auth/email/email.service.ts,
    apps/api/src/modules/auth/email/email.module.ts,
    apps/api/src/modules/auth/email/templates/password-reset.tsx
  </files>
  <read_first>
    - apps/api/src/modules/auth/email/email.service.spec.ts (Task 0 RED 테스트 — GREEN 목표)
    - apps/api/src/modules/sms/sms.service.ts (복제 원본 — SmsService.constructor L22-37 패턴 참조)
    - apps/api/src/modules/sms/sms.module.ts (별도 모듈 + exports 구조)
    - apps/api/src/modules/booking/providers/redis.provider.ts L220-236 (REDIS_URL hard-fail 원본)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pattern 1 + §Pattern 2 verbatim)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: RESEND_FROM_EMAIL prod hard-fail, API build check)
    - apps/api/tsconfig.json (Task 2 결과 — JSX 활성화 확인)
  </read_first>
  <action>
**Step 1.** 디렉토리 생성 및 3개 파일 작성.

**File: `apps/api/src/modules/auth/email/email.service.ts`** (REVIEWS.md MED — RESEND_FROM_EMAIL hard-fail 반영):
```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Resend } from 'resend';
import { PasswordResetEmail } from './templates/password-reset.js';

export interface SendEmailResult {
  success: boolean;
  id?: string;
  error?: string;
}

const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

@Injectable()
export class EmailService {
  private readonly logger = new Logger(EmailService.name);
  private readonly resend: Resend | null;
  private readonly from: string;
  private readonly isDevMode: boolean;

  constructor(private readonly configService: ConfigService) {
    const apiKey = this.configService.get<string>('RESEND_API_KEY');
    const fromEmail = this.configService.get<string>('RESEND_FROM_EMAIL');
    const nodeEnv = this.configService.get<string>('NODE_ENV');
    const isProd = nodeEnv === 'production';

    // Phase 7 REDIS_URL hard-fail 원칙 (RESEARCH §Example 3)
    if (isProd && !apiKey) {
      throw new Error(
        '[email] RESEND_API_KEY is required in production environment. ' +
          'Silent console.log fallback is disabled to prevent missed password reset emails. ' +
          'Check Cloud Run secret binding.',
      );
    }

    // REVIEWS.md MED: RESEND_FROM_EMAIL 도 production hard-fail (no onboarding@resend.dev fallback)
    if (isProd) {
      if (!fromEmail || !EMAIL_PATTERN.test(fromEmail)) {
        throw new Error(
          '[email] RESEND_FROM_EMAIL must be a valid email in production. ' +
            `Received: ${fromEmail ?? '<unset>'}. ` +
            'Phishing/deliverability risk — configure a verified sender in Resend dashboard.',
        );
      }
      this.from = fromEmail;
    } else {
      // dev/test: fallback 허용
      this.from = fromEmail ?? 'onboarding@resend.dev';
    }

    this.isDevMode = !apiKey;
    if (this.isDevMode) {
      this.resend = null;
      this.logger.warn('Email Service running in DEV MOCK mode (no RESEND_API_KEY)');
    } else {
      this.resend = new Resend(apiKey!);
    }
  }

  async sendPasswordResetEmail(to: string, resetLink: string): Promise<SendEmailResult> {
    if (this.isDevMode) {
      this.logger.log(`DEV EMAIL: password reset link for ${to}: ${resetLink}`);
      return { success: true };
    }

    // Resend returns { data, error } — does NOT throw (RESEARCH §Pitfall 2)
    const { data, error } = await this.resend!.emails.send({
      from: this.from,
      to,
      subject: '[Grapit] 비밀번호 재설정',
      react: PasswordResetEmail({ resetLink }),
    });

    if (error) {
      this.logger.error(`Resend send failed for ${to}: ${error.message}`);
      return { success: false, error: error.message };
    }

    return { success: true, id: data?.id };
  }
}
```

**File: `apps/api/src/modules/auth/email/email.module.ts`**:
```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { EmailService } from './email.service.js';

@Module({
  imports: [ConfigModule],
  providers: [EmailService],
  exports: [EmailService],
})
export class EmailModule {}
```

**File: `apps/api/src/modules/auth/email/templates/password-reset.tsx`** (RESEARCH §Pattern 2 verbatim):
```tsx
import { Html, Head, Body, Container, Heading, Text, Button, Hr, Section } from '@react-email/components';

interface PasswordResetEmailProps {
  resetLink: string;
}

export function PasswordResetEmail({ resetLink }: PasswordResetEmailProps) {
  return (
    <Html lang="ko">
      <Head />
      <Body style={{ backgroundColor: '#f5f5f7', fontFamily: 'system-ui, sans-serif' }}>
        <Container style={{ backgroundColor: '#ffffff', padding: '32px', maxWidth: '560px' }}>
          <Heading style={{ fontSize: '20px', color: '#1A1A2E' }}>비밀번호 재설정 안내</Heading>
          <Text style={{ fontSize: '14px', color: '#4A4A5E' }}>
            아래 버튼을 눌러 비밀번호를 재설정해주세요. 이 링크는 1시간 동안만 유효합니다.
          </Text>
          <Section style={{ textAlign: 'center', margin: '24px 0' }}>
            <Button href={resetLink} style={{ backgroundColor: '#6C3CE0', color: '#ffffff', padding: '12px 24px', borderRadius: '6px' }}>
              비밀번호 재설정
            </Button>
          </Section>
          <Hr />
          <Text style={{ fontSize: '12px', color: '#6B6B7B' }}>
            본 메일을 요청하지 않으셨다면 무시하셔도 됩니다.
          </Text>
        </Container>
      </Body>
    </Html>
  );
}
```

**Step 2.** 테스트 GREEN 확인 + **Blocker B6 — Task 2 에서 이동된 typecheck/build 검증**:
```bash
pnpm --filter @grapit/api test email.service   # 6/6 PASS 기대
pnpm --filter @grapit/api typecheck            # 0 errors (Blocker B6: Task 2 에서 이동)
pnpm --filter @grapit/api build                # dist/ 생성 성공 — React Email TSX 실 빌드 검증 (REVIEWS.md MED + Blocker B6)
```

만약 빌드 실패:
- NestJS CLI 가 TSX 파일을 skip하는 경우 `nest-cli.json` `"compilerOptions"` 에 `"include": ["**/*.ts", "**/*.tsx"]` 또는 관련 설정 추가.
- swc transform 실패는 Task 2 tsconfig 설정 재확인.

**Step 3.** Commit: `feat(09-tech-debt): add EmailService with Resend + React Email template + hard-fail (DEBT-01)`.

**DO NOT:**
- `auth.module.ts` / `auth.service.ts` 수정 (Task 5/6에서 처리)
- `@react-email/render` import (불필요 — Resend가 내부에서 렌더)
- try/catch로 resend.emails.send 감싸기 (Pitfall 2: {error} 분기 사용)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api test email.service && pnpm --filter @grapit/api typecheck && pnpm --filter @grapit/api build
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/auth/email/email.service.ts` exists with `export class EmailService`
    - `apps/api/src/modules/auth/email/email.module.ts` exists with `export class EmailModule` exporting EmailService
    - `apps/api/src/modules/auth/email/templates/password-reset.tsx` exists with `export function PasswordResetEmail`
    - `test "$(grep -c 'RESEND_API_KEY is required in production' apps/api/src/modules/auth/email/email.service.ts | tr -d ' ')" = "1"`
    - `test "$(grep -c 'RESEND_FROM_EMAIL must be a valid email in production' apps/api/src/modules/auth/email/email.service.ts | tr -d ' ')" = "1"`
    - `grep -q "this.resend!.emails.send" apps/api/src/modules/auth/email/email.service.ts`
    - `grep -q "const { data, error }" apps/api/src/modules/auth/email/email.service.ts` (try/catch 미사용)
    - `! grep -r "@react-email/render" apps/api/src/modules/auth/email/` (render import 금지)
    - `pnpm --filter @grapit/api test email.service` exits 0 with 6 passed
    - `pnpm --filter @grapit/api typecheck` exits 0 (Blocker B6 — Task 2 에서 이동)
    - `pnpm --filter @grapit/api build` exits 0 (REVIEWS.md MED + Blocker B6 — 실 빌드 검증)
    - Commit matches `feat(09-tech-debt): add EmailService with Resend + React Email template + hard-fail (DEBT-01)`
  </acceptance_criteria>
  <done>EmailService + template + module 3파일이 생성되고 6/6 spec PASS, typecheck PASS, build PASS</done>
</task>

<task id="9-02-t4-main-frontend-url-validation" type="auto" tdd="false">
  <name>Task 4 (REVIEWS.md MED): main.ts bootstrap에 FRONTEND_URL https 검증 추가</name>
  <files>
    apps/api/src/main.ts
  </files>
  <read_first>
    - apps/api/src/main.ts (전체 — L13-46 bootstrap 함수)
    - apps/api/src/modules/booking/providers/redis.provider.ts (REDIS_URL hard-fail 동일 패턴 참조)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: FRONTEND_URL 검증 없음)
  </read_first>
  <action>
**Step 1.** `apps/api/src/main.ts` 의 `bootstrap` 함수에 FRONTEND_URL 검증 추가.

기존 L13-15:
```typescript
async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // Wire Socket.IO to the shared ioredis REDIS_CLIENT ...
```

변경 후 (NestFactory 직후 삽입):
```typescript
async function bootstrap() {
  // REVIEWS.md MED: FRONTEND_URL production hard-fail
  // Password reset email links embed FRONTEND_URL; a missing or non-https value
  // breaks password recovery and phishing-protection guarantees.
  if (process.env['NODE_ENV'] === 'production') {
    const frontendUrl = process.env['FRONTEND_URL'];
    if (!frontendUrl || !frontendUrl.startsWith('https://')) {
      console.error(
        `[bootstrap] FRONTEND_URL must be an https URL in production. ` +
          `Received: ${frontendUrl ?? '<unset>'}. ` +
          'Reset links and email deliverability depend on this. Aborting startup.',
      );
      process.exit(1);
    }
  }

  const app = await NestFactory.create(AppModule);

  // Wire Socket.IO to the shared ioredis REDIS_CLIENT ...
```

**Step 2.** Manual verification (로컬):
```bash
NODE_ENV=production FRONTEND_URL=http://insecure.com pnpm --filter @grapit/api start  # exit 1 + error log
NODE_ENV=production pnpm --filter @grapit/api start                                    # exit 1 + error log
NODE_ENV=development pnpm --filter @grapit/api start                                   # 정상 기동
```

(executor는 코드 변경까지만 하고 3가지 manual run은 post-plan SUMMARY에 기록.)

**Step 3.** Commit: `feat(09-tech-debt): hard-fail on missing/non-https FRONTEND_URL in production (DEBT-01)`.

**NOT allowed:**
- 개발 환경(NODE_ENV !== 'production')에서 `FRONTEND_URL` 검증 추가 (로컬 개발 편의 손상)
- `throw new Error` 대신 `process.exit(1)` 사용 (NestFactory 전에는 전역 에러 핸들러 미연결)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q "FRONTEND_URL must be an https URL in production" apps/api/src/main.ts && grep -q "process.exit(1)" apps/api/src/main.ts && pnpm --filter @grapit/api typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/main.ts` contains `FRONTEND_URL must be an https URL in production`
    - `apps/api/src/main.ts` contains `process.exit(1)`
    - `apps/api/src/main.ts` contains `frontendUrl.startsWith('https://')`
    - `pnpm --filter @grapit/api typecheck` exits 0
    - Commit matches `feat(09-tech-debt): hard-fail on missing/non-https FRONTEND_URL in production (DEBT-01)`
  </acceptance_criteria>
  <done>NestJS 부트스트랩이 production + invalid FRONTEND_URL 시 즉시 종료</done>
</task>

<task id="9-02-t5-auth-wiring-throttle" type="auto" tdd="false">
  <name>Task 5 (DEBT-01 + REVIEWS.md HIGH-04, Blocker B5): AuthService에 EmailService 주입 + controller에 @Throttle v6 객체 시그니처 적용 + auth.module.ts import</name>
  <files>
    apps/api/src/modules/auth/auth.service.ts,
    apps/api/src/modules/auth/auth.controller.ts,
    apps/api/src/modules/auth/auth.module.ts
  </files>
  <read_first>
    - apps/api/src/modules/auth/auth.service.ts (L1-60 imports/constructor, L217-241 requestPasswordReset)
    - apps/api/src/modules/auth/auth.controller.ts (L1-40 imports, L117-137 password reset endpoints)
    - apps/api/src/modules/auth/auth.module.ts (L12-13 imports, L32 providers)
    - apps/api/src/app.module.ts (L3: ThrottlerModule 이미 전역 등록 L29-46)
    - apps/api/package.json (Blocker B5 확인 — `@nestjs/throttler` 버전이 `^6.4.0` 인지 확인. v6 객체 시그니처만 사용 가능)
    - apps/api/src/modules/auth/email/email.module.ts (Task 3 output — 참조 대상)
    - apps/api/src/modules/auth/email/email.service.ts (Task 3 output — 주입 대상)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-04: rate limiting / MED: silent return 보존)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Security Domain V7 email enumeration)
  </read_first>
  <action>
**Blocker B5 prerequisite (executor MUST verify before coding):**
```bash
grep '"@nestjs/throttler"' apps/api/package.json
# 기대 출력: "@nestjs/throttler": "^6.4.0"
# v6 이상이 fixed. v5 fallback 은 존재하지 않음 — 본 task 는 @Throttle 의 v6 객체 시그니처만 사용.
# 만약 이 prerequisite 가 다른 버전을 반환하면 planning 단계로 돌아가 revision 요청.
```

**Step 1.** `apps/api/src/modules/auth/auth.service.ts` 수정:

**1a. Import 추가 (기존 SmsService import 근처 L17):**
```typescript
import { EmailService } from './email/email.service.js';
```

**1b. Constructor에 EmailService 주입 (L53-59):**
기존:
```typescript
constructor(
  private readonly jwtService: JwtService,
  private readonly configService: ConfigService,
  private readonly userRepository: UserRepository,
  private readonly smsService: SmsService,
  @Inject(DRIZZLE) private readonly db: DrizzleDB,
) {}
```
변경 후:
```typescript
constructor(
  private readonly jwtService: JwtService,
  private readonly configService: ConfigService,
  private readonly userRepository: UserRepository,
  private readonly smsService: SmsService,
  private readonly emailService: EmailService,
  @Inject(DRIZZLE) private readonly db: DrizzleDB,
) {}
```

**1c. requestPasswordReset 교체 (L234-240):**
기존:
```typescript
const frontendUrl = this.configService.get<string>('FRONTEND_URL');
const resetLink = `${frontendUrl}/auth/reset-password?token=${resetToken}`;

// TODO: Wire up nodemailer transport in production
// For now, log the link (will be replaced with actual email service)
console.log(`[Password Reset] Link for ${email}: ${resetLink}`);
```
변경 후:
```typescript
const frontendUrl = this.configService.get<string>('FRONTEND_URL');
const resetLink = `${frontendUrl}/auth/reset-password?token=${resetToken}`;

await this.emailService.sendPasswordResetEmail(email, resetLink);
```

**DO NOT TOUCH (V7 이메일 enumeration 방지 — 변경 금지):**
- L217-223 silent return when user not found:
  ```typescript
  async requestPasswordReset(email: string): Promise<void> {
    const user = await this.userRepository.findByEmail(email);
    if (!user) {
      return;   // ← 이 silent return이 V7 보안 요건. 절대 변경 금지
    }
  ```

**Step 2.** `apps/api/src/modules/auth/auth.controller.ts` 수정 — **REVIEWS.md HIGH-04 rate limit 적용 (Blocker B5: v6 객체 시그니처만)**:

**2a. Import 추가 (L14 뒤):**
```typescript
import { Throttle } from '@nestjs/throttler';
```

**2b. `POST /auth/password-reset/request` 위에 @Throttle 추가 (L117 앞):**
```typescript
@Public()
@HttpCode(HttpStatus.OK)
@Throttle({ default: { limit: 3, ttl: 900000 } })   // 3 req / 15 min / IP (REVIEWS.md HIGH-04; v6 객체 시그니처; ttl 단위는 ms — 900000 = 900_000ms = 15min, NOT 900초)
@Post('password-reset/request')
async requestReset(
  @Body(new ZodValidationPipe(resetPasswordRequestBodySchema))
  dto: ResetPasswordRequestBody,
) {
  await this.authService.requestPasswordReset(dto.email);
  return { message: '비밀번호 재설정 링크를 발송했습니다' };
}
```

**2c. `POST /auth/password-reset/confirm` 위에 동일 @Throttle 추가 (L128 앞):**
```typescript
@Public()
@HttpCode(HttpStatus.OK)
@Throttle({ default: { limit: 3, ttl: 900000 } })   // 3 req / 15 min / IP (REVIEWS.md HIGH-04; v6 object signature)
@Post('password-reset/confirm')
async confirmReset(
  @Body(new ZodValidationPipe(resetPasswordBodySchema))
  dto: ResetPasswordBody,
) {
  await this.authService.resetPassword(dto.token, dto.newPassword);
  return { message: '비밀번호가 변경되었습니다' };
}
```

**Blocker B5 — DO NOT use v5 syntax:**
- `@Throttle(3, 900)` (positional args, 초 단위) — **사용 금지**. `@nestjs/throttler@^6.4.0` 이 fixed 되어 있으므로 반드시 v6 객체 시그니처(`{ default: { limit, ttl } }`, ttl ms)만 사용.
- `ttl: 900` (초) — **사용 금지**. 15분 = 900000ms (`ttl: 900000`).
- 만약 executor 가 실수로 `ttl: 900` 을 쓰면 1.5초 rate window 가 되어 사실상 무효.

**Step 3.** `apps/api/src/modules/auth/auth.module.ts` 수정:

**3a. Import 추가 (L13 SmsModule 뒤):**
```typescript
import { EmailModule } from './email/email.module.js';
```

**3b. imports 배열에 EmailModule 추가:**
기존:
```typescript
imports: [
  PassportModule,
  UserModule,
  SmsModule,
  JwtModule.registerAsync({ ... }),
],
```
변경 후:
```typescript
imports: [
  PassportModule,
  UserModule,
  SmsModule,
  EmailModule,
  JwtModule.registerAsync({ ... }),
],
```

**Step 4.** 기존 auth.service.spec.ts 에 EmailService mock을 주입해야 할 수도 있음 — Task 6 에서 integration test와 함께 업데이트. Step 4 에서는 `pnpm --filter @grapit/api test` 가 기존 auth.service.spec 을 실행할 때 에러 나지 않도록 **Task 6 와 함께 같은 커밋 배치** 가능.

실제로 auth.service.spec.ts (L132-139) 의 `new AuthService(...)` 호출에 EmailService mock 추가 필요 — executor가 Task 6에서 함께 처리.

**Step 5.** Commit: `feat(09-tech-debt): wire EmailService + rate-limit password reset endpoints v6 (DEBT-01, HIGH-04)`.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api typecheck && test "$(grep -c 'console.log.*Password Reset' apps/api/src/modules/auth/auth.service.ts | tr -d ' ')" = "0" && grep -q "this.emailService.sendPasswordResetEmail" apps/api/src/modules/auth/auth.service.ts && grep -q "@Throttle" apps/api/src/modules/auth/auth.controller.ts && grep -q "ttl: 900000" apps/api/src/modules/auth/auth.controller.ts && grep -q "limit: 3" apps/api/src/modules/auth/auth.controller.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c 'console.log.*Password Reset.*Link' apps/api/src/modules/auth/auth.service.ts | tr -d ' ')" = "0"` (stub removed)
    - `grep -q "this.emailService.sendPasswordResetEmail" apps/api/src/modules/auth/auth.service.ts`
    - `grep -q "private readonly emailService: EmailService" apps/api/src/modules/auth/auth.service.ts`
    - `test "$(grep -c '@Throttle' apps/api/src/modules/auth/auth.controller.ts | tr -d ' ')" = "2"` (request + confirm)
    - `grep -q "ttl: 900000" apps/api/src/modules/auth/auth.controller.ts` (Blocker B5 — v6 ms 단위 정확성 보장, 900초 typo 방지)
    - `grep -q "limit: 3" apps/api/src/modules/auth/auth.controller.ts` (Blocker B5 — v6 객체 시그니처)
    - `! grep -qE "@Throttle\(3,\s*900\)" apps/api/src/modules/auth/auth.controller.ts` (Blocker B5 — v5 시그니처 금지)
    - `grep -q "import { Throttle } from '@nestjs/throttler'" apps/api/src/modules/auth/auth.controller.ts`
    - `test "$(grep -c 'EmailModule' apps/api/src/modules/auth/auth.module.ts | tr -d ' ')" = "2"` (import 문 + imports 배열)
    - `grep -q "if (!user) {" apps/api/src/modules/auth/auth.service.ts` (silent return 보존 확인 — 개수 변동 없음)
    - `pnpm --filter @grapit/api typecheck` exits 0
    - Commit matches `feat(09-tech-debt): wire EmailService + rate-limit password reset endpoints v6 (DEBT-01, HIGH-04)`
  </acceptance_criteria>
  <done>AuthService에 EmailService 주입, 두 reset endpoint에 v6 @Throttle 객체 시그니처 (900000ms) 적용, enumeration 방지 보존</done>
</task>

<task id="9-02-t6-reset-flow-integration-test" type="auto" tdd="false">
  <name>Task 6 (REVIEWS.md HIGH-03 + Blocker B4): auth.service.spec.ts에 reset link → password change 통합 테스트 추가 + JwtService secret rotation assert + one-time 재사용 거부 테스트</name>
  <files>
    apps/api/src/modules/auth/auth.service.spec.ts
  </files>
  <read_first>
    - apps/api/src/modules/auth/auth.service.spec.ts (전체 — L57-140 기존 describe + beforeEach)
    - apps/api/src/modules/auth/auth.service.ts (L217-296: requestPasswordReset L217-241 + resetPassword L243-296. 특히 L227 + L255-272 secret = jwtSecret + passwordHash rotation 로직)
    - apps/api/src/modules/auth/email/email.service.ts (Task 3 결과 — 주입 대상)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-03: 링크 → 변경 완료 flow 검증)
    - .planning/phases/09-tech-debt/09-VALIDATION.md (manual-only table — E2E 대신 integration test)
  </read_first>
  <action>
**Blocker B4 배경 (executor 필독):**
`auth.service.ts:227` 에서 reset token 생성 시 secret 은 `jwtSecret + (user.passwordHash ?? '')` 로 구성된다. `resetPassword` 의 `verifyAsync` 호출 (L268-271) 도 동일 secret 공식을 사용. 이 구조의 목적은 **password 가 변경되면 passwordHash 가 바뀌므로 같은 토큰을 두 번 사용할 수 없음 (secret rotation = 자연스러운 one-time token)**. 따라서 기존 mock:
```typescript
mockJwtService.verifyAsync.mockResolvedValue({ sub, purpose })
```
는 secret argument 를 완전히 무시하므로 rotation 로직을 bypass 한다. 본 task 는 이 gap 을 닫는다.

**Step 1.** `apps/api/src/modules/auth/auth.service.spec.ts` 에 EmailService mock을 beforeEach 에 추가.

기존 L127-139 beforeEach 마지막 부분:
```typescript
const mockSmsService = {
  verifyCode: vi.fn().mockResolvedValue({ verified: true }),
  sendVerificationCode: vi.fn().mockResolvedValue({ success: true, message: '' }),
};

// Instantiate AuthService directly (no NestJS DI overhead)
authService = new AuthService(
  mockJwtService as unknown as JwtService,
  mockConfigService as unknown as ConfigService,
  mockUserRepo as any,
  mockSmsService as any,
  mockDb as any,
);
```

변경 후:
```typescript
const mockSmsService = {
  verifyCode: vi.fn().mockResolvedValue({ verified: true }),
  sendVerificationCode: vi.fn().mockResolvedValue({ success: true, message: '' }),
};

// REVIEWS.md HIGH-03: capture reset link via EmailService spy
const mockEmailService = {
  sendPasswordResetEmail: vi.fn().mockResolvedValue({ success: true }),
};

// Instantiate AuthService directly (no NestJS DI overhead)
authService = new AuthService(
  mockJwtService as unknown as JwtService,
  mockConfigService as unknown as ConfigService,
  mockUserRepo as any,
  mockSmsService as any,
  mockEmailService as any,       // 새 주입
  mockDb as any,
);
```

**Step 2.** 기존 describe 끝에 `describe('password reset flow integration'` 블록 추가. **Blocker B4 반영: secret rotation assert + one-time reuse reject tests 포함.**

기존 spec의 마지막 `describe(...)` 뒤에 새 블록 삽입:

```typescript
// REVIEWS.md HIGH-03 + Blocker B4 (revision-2): End-to-end password reset flow within service layer.
// Proves that:
//   (1) the reset link captured from EmailService spy can be successfully used
//       to complete password change — closing the roadmap success criterion
//       "실제 이메일 발송 + 링크를 통해 비밀번호 변경 완료" without requiring real Resend send.
//   (2) JwtService.verifyAsync is called with secret = `${jwtSecret}${user.passwordHash}`
//       (Blocker B4 — secret-rotation argument assertion; previous mock bypassed this).
//   (3) Once password is reset, reusing the SAME token fails because passwordHash changed,
//       which is the one-time-token guarantee built into auth.service.ts:227 + L262-272.
describe('password reset flow integration', () => {
  const TEST_JWT_SECRET = 'test-jwt-secret';

  beforeEach(() => {
    // The test assumes configService returns a known jwtSecret so we can compose expected secret.
    mockConfigService.get.mockImplementation((key: string) => {
      if (key === 'auth.jwtSecret') return TEST_JWT_SECRET;
      if (key === 'FRONTEND_URL') return 'http://localhost:3000';
      return undefined;
    });
  });

  it('requestPasswordReset → EmailService.sendPasswordResetEmail captures resetLink → token extracted → resetPassword succeeds → password hash changed (+ JwtService secret rotation asserted)', async () => {
    // Setup: existing user with a current password
    const email = 'reset@test.com';
    const currentUser = {
      ...mockUser,
      id: 'test-user-id',
      email,
      passwordHash: preHashedPassword, // argon2 hash of 'Test1234!'
    };
    mockUserRepo.findByEmail.mockResolvedValue(currentUser);
    mockUserRepo.findById.mockResolvedValue(currentUser);

    // Mock jwt sign to return a deterministic token whose base64url payload decodes to
    // { sub: 'test-user-id', purpose: 'password-reset' } (auth.service.ts:247-249 relies on this).
    const fakeToken = 'header.eyJzdWIiOiJ0ZXN0LXVzZXItaWQiLCJwdXJwb3NlIjoicGFzc3dvcmQtcmVzZXQifQ.sig';
    mockJwtService.signAsync.mockResolvedValue(fakeToken);

    // Blocker B4: use mockImplementation so we can assert the `secret` arg, and emulate
    // the real JwtService behavior where verifyAsync fails if secret is wrong.
    mockJwtService.verifyAsync.mockImplementation(async (_token: string, opts: { secret: string }) => {
      const expected = `${TEST_JWT_SECRET}${currentUser.passwordHash ?? ''}`;
      if (opts.secret !== expected) {
        // Simulate the real behavior of jwtService.verifyAsync when secret mismatches.
        throw new Error('invalid signature');
      }
      return { sub: currentUser.id, purpose: 'password-reset' };
    });

    // --- Act 1: request password reset ---
    await authService.requestPasswordReset(email);

    // --- Assert 1a: EmailService.sendPasswordResetEmail called with a valid reset URL ---
    expect(mockEmailService.sendPasswordResetEmail).toHaveBeenCalledTimes(1);
    const [toArg, linkArg] = mockEmailService.sendPasswordResetEmail.mock.calls[0] as [string, string];
    expect(toArg).toBe(email);
    expect(linkArg).toContain('/auth/reset-password?token=');
    expect(linkArg).toContain(fakeToken); // token is embedded in the link

    // --- Assert 1b (Blocker B4): jwtService.signAsync was called with rotation-aware secret ---
    expect(mockJwtService.signAsync).toHaveBeenCalledWith(
      { sub: currentUser.id, purpose: 'password-reset' },
      expect.objectContaining({ secret: `${TEST_JWT_SECRET}${currentUser.passwordHash ?? ''}`, expiresIn: '1h' }),
    );

    // --- Act 2: extract token from link (mirrors frontend `useSearchParams().get('token')`) ---
    const tokenFromLink = new URL(linkArg, 'http://localhost:3000').searchParams.get('token');
    expect(tokenFromLink).toBe(fakeToken);

    // --- Act 3: complete password reset with new password ---
    const newPassword = 'NewPwd9876!';
    await authService.resetPassword(tokenFromLink!, newPassword);

    // --- Assert 3a: verifyAsync was called with the OLD passwordHash in secret ---
    // (This is what passes because findById returned currentUser with preHashedPassword.)
    expect(mockJwtService.verifyAsync).toHaveBeenCalledWith(
      tokenFromLink!,
      expect.objectContaining({ secret: `${TEST_JWT_SECRET}${currentUser.passwordHash ?? ''}` }),
    );

    // --- Assert 3b: userRepository.updatePassword was called with a new argon2 hash ---
    expect(mockUserRepo.updatePassword).toHaveBeenCalledTimes(1);
    const [updatedUserId, newHash] = mockUserRepo.updatePassword.mock.calls[0] as [string, string];
    expect(updatedUserId).toBe('test-user-id');
    expect(newHash).not.toBe(preHashedPassword); // hash changed
    expect(newHash).toMatch(/^\$argon2id\$/); // argon2id format

    // --- Assert 4: the new hash actually verifies against the new password ---
    const argon2 = await import('argon2');
    const isValidNew = await argon2.verify(newHash, newPassword);
    expect(isValidNew).toBe(true);

    // --- Assert 5: old password no longer verifies ---
    const isValidOld = await argon2.verify(newHash, 'Test1234!');
    expect(isValidOld).toBe(false);
  }, 15000); // argon2 hash creation is expensive

  it('Blocker B4: one-time token — reusing the same reset token after password change throws UnauthorizedException (passwordHash-based secret rotated)', async () => {
    const email = 'rotate@test.com';
    const userBefore = {
      ...mockUser,
      id: 'rotate-user-id',
      email,
      passwordHash: preHashedPassword,
    };
    mockUserRepo.findByEmail.mockResolvedValue(userBefore);

    const fakeToken = 'header.eyJzdWIiOiJyb3RhdGUtdXNlci1pZCIsInB1cnBvc2UiOiJwYXNzd29yZC1yZXNldCJ9.sig';
    mockJwtService.signAsync.mockResolvedValue(fakeToken);

    // Mutable ref — findById returns the "current" user state. After first resetPassword,
    // the test mutates `currentStoredHash` so the second findById returns the rotated user.
    let currentStoredHash = preHashedPassword;
    mockUserRepo.findById.mockImplementation(async (id: string) =>
      id === userBefore.id ? { ...userBefore, passwordHash: currentStoredHash } : null,
    );

    // updatePassword simulates the DB write by updating the mutable ref.
    mockUserRepo.updatePassword.mockImplementation(async (_id: string, newHash: string) => {
      currentStoredHash = newHash;
    });

    // Blocker B4: verifyAsync honors the secret argument.
    mockJwtService.verifyAsync.mockImplementation(async (_token: string, opts: { secret: string }) => {
      const expected = `${TEST_JWT_SECRET}${currentStoredHash}`;
      if (opts.secret !== expected) {
        throw new Error('invalid signature');
      }
      return { sub: userBefore.id, purpose: 'password-reset' };
    });

    // --- First use: succeeds ---
    await authService.requestPasswordReset(email);
    await authService.resetPassword(fakeToken, 'FirstNewPwd!1');

    // Hash should have rotated.
    expect(currentStoredHash).not.toBe(preHashedPassword);

    // --- Second use: the controller-level resetPassword manually parses JWT header and
    //     then calls verifyAsync with secret = jwtSecret + NEW passwordHash.
    //     But signAsync used OLD passwordHash as secret. So verifyAsync with NEW secret fails.
    await expect(
      authService.resetPassword(fakeToken, 'SecondAttempt!2'),
    ).rejects.toThrow(/유효하지 않은 재설정 토큰입니다/);

    // updatePassword was only called once (first time).
    expect(mockUserRepo.updatePassword).toHaveBeenCalledTimes(1);
  }, 15000);

  it('requestPasswordReset for unknown user → EmailService is NOT called (enumeration prevention preserved)', async () => {
    mockUserRepo.findByEmail.mockResolvedValue(null);

    await authService.requestPasswordReset('unknown@test.com');

    expect(mockEmailService.sendPasswordResetEmail).not.toHaveBeenCalled();
  });
});
```

**Step 3.** Import 누락 확인: 파일 상단에 `import { Buffer } from 'node:buffer'` 가 필요하지 않은지 확인 (Node 22 LTS globals 로 충분).

**Step 4.** 테스트 실행:
```bash
pnpm --filter @grapit/api test auth.service
```
모든 기존 테스트 + 신규 3 tests (happy flow / one-time rotation / enumeration) PASS 기대.

**Step 5.** Commit: `test(09-tech-debt): verify password reset link → password change integration flow + B4 secret rotation (HIGH-03, B4)`.

**Blocker B4 Approach 선택 (planner decision — executor 는 이대로 실행):**
- **Approach A (mock + argument-assert)** 를 선택 — 현재 테스트가 이미 `JwtService` 를 mock 으로 처리하고 있어 real JwtService 로 교체하면 기존 테스트 다수의 setup 이 연쇄 수정되어야 함. 대신 `mockImplementation` 으로 secret 인자를 검증하고, secret mismatch 시 실제 JwtService 처럼 throw 하도록 흉내냄.
- Approach B (real JwtService with @nestjs/testing Test.createTestingModule) 는 장점이 있으나 본 task 가 "Blocker 최소 targeted 수정" 목적이라 기각.

**NOT allowed:**
- 실제 Resend API 호출 (통합 test이지만 mock이 원칙)
- 실제 DB write 시도 (mockUserRepo 유지)
- `vi.mock('resend')` 호출 (EmailService를 spy로 주입하므로 Resend mock 불필요)
- `process.env` mutation (dev mock 분기는 이미 테스트했음 — 이 test는 flow 자체 검증)
- `JwtService` 를 실제 인스턴스로 교체 (Approach B — 기각됨)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/api test auth.service && grep -q "password reset flow integration" apps/api/src/modules/auth/auth.service.spec.ts && grep -q "mockEmailService.sendPasswordResetEmail" apps/api/src/modules/auth/auth.service.spec.ts && grep -q "Blocker B4" apps/api/src/modules/auth/auth.service.spec.ts && grep -q "one-time token" apps/api/src/modules/auth/auth.service.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "describe('password reset flow integration'" apps/api/src/modules/auth/auth.service.spec.ts`
    - `grep -q "mockEmailService" apps/api/src/modules/auth/auth.service.spec.ts`
    - `grep -q "argon2.verify" apps/api/src/modules/auth/auth.service.spec.ts` (신규 해시 검증)
    - `grep -q "Blocker B4" apps/api/src/modules/auth/auth.service.spec.ts` (Blocker B4 — secret rotation awareness)
    - `grep -q "one-time token" apps/api/src/modules/auth/auth.service.spec.ts` (B4 — rotation test 존재)
    - `grep -qE "expect\\(mockJwtService\\.signAsync\\)\\.toHaveBeenCalledWith" apps/api/src/modules/auth/auth.service.spec.ts` (B4 — signAsync secret arg assert)
    - `grep -qE "expect\\(mockJwtService\\.verifyAsync\\)\\.toHaveBeenCalledWith" apps/api/src/modules/auth/auth.service.spec.ts` (B4 — verifyAsync secret arg assert)
    - `pnpm --filter @grapit/api test auth.service` exits 0 (기존 + 신규 3 tests 모두 PASS)
    - 신규 테스트 파일에 최소 3개 `it(...)` 블록 (integration / one-time-rotation / enumeration-prevention)
    - Commit matches `test(09-tech-debt): verify password reset link → password change integration flow + B4 secret rotation (HIGH-03, B4)`
  </acceptance_criteria>
  <done>auth.service.spec가 reset link→password change 전체 플로우 + secret rotation argument assert + 동일 토큰 재사용 reject 을 mock 수준에서 실증하며, DEBT-01 roadmap success criterion 1 + Blocker B4 를 동시 closure</done>
</task>

<!-- ============================================================================ -->
<!-- Executor checkpoint (W2): Task 6 커밋 완료 후 half-way 확인                     -->
<!--                                                                               -->
<!-- pnpm --filter @grapit/api test && pnpm --filter @grapit/api typecheck &&       -->
<!-- pnpm --filter @grapit/api build 모두 green 인지 확인한 뒤 Task 7 로 진행.       -->
<!-- Green 실패 시 Task 7 로 진행하지 말고 원인 수정.                                -->
<!-- ============================================================================ -->

<task id="9-02-t7-legal-md-files" type="auto" tdd="false">
  <name>Task 7 (DEBT-02 + Blocker B3): 3개 MD 법률 문서 작성 (KOPICO 표준 기반 초안) + privacy-policy.md 국외이전(제6조) 추가 + 정확성 checklist</name>
  <files>
    apps/web/content/legal/terms-of-service.md,
    apps/web/content/legal/privacy-policy.md,
    apps/web/content/legal/marketing-consent.md,
    .planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md
  </files>
  <read_first>
    - .planning/phases/09-tech-debt/09-CONTEXT.md (D-07: KOPICO 표준 + Grapit 각색, "법률 검토 전 교체" 배너 유지)
    - .planning/phases/09-tech-debt/09-UI-SPEC.md (L69-87 MD 본문 타이포그래피 매핑)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Risk 5: KOPICO 표준 문안 저작권 — Grapit 각색)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: Legal 정확성 checklist)
    - packages/shared/src/types/user.types.ts (실 users table schema 참조 — 수집 항목 cross-check)
    - apps/api/src/database/schema/users.ts (테이블 실 column 확인)
    - apps/api/src/modules/sms/sms.service.ts (Twilio 통합 확인)
    - apps/api/src/modules/auth/email/email.service.ts (Resend 통합 확인)
    - apps/api/src/modules/payment/toss-payments.client.ts (Toss 통합 확인)
  </read_first>
  <action>
**Step 1:** 3개 MD 파일 작성. **Blocker B3: privacy-policy.md 는 국외이전 조항(제6조) 포함 — Resend(US), Twilio(US) 수탁자 명시.** 기존 조항 번호 한 단계씩 뒤로 밀림.

**File: `apps/web/content/legal/terms-of-service.md`** — 이용약관 (기존 Plan 02 버전 그대로 사용, 15조 이상):

```markdown
# 이용약관

## 제1조 (목적)
본 약관은 Grapit(이하 "회사")이 제공하는 라이브 엔터테인먼트 티켓 예매 서비스(이하 "서비스")의 이용조건 및 절차, 회원과 회사의 권리, 의무 및 책임 사항 등을 규정함을 목적으로 합니다.

## 제2조 (정의)
본 약관에서 사용하는 용어의 정의는 다음과 같습니다.

1. **"서비스"**란 회사가 온라인으로 제공하는 공연·전시·스포츠 등 라이브 엔터테인먼트 티켓 예매, 결제, 취소 및 관련 부가 서비스를 말합니다.
2. **"회원"**이란 본 약관에 동의하고 회사에 개인정보를 제공하여 회원등록을 한 자로서, 서비스를 지속적으로 이용할 수 있는 자를 말합니다.
3. **"아이디(ID)"**란 회원의 식별과 서비스 이용을 위하여 회원이 선정하고 회사가 승인하는 이메일 주소를 말합니다.
4. **"비밀번호"**란 회원이 부여받은 아이디와 일치되는 회원임을 확인하고, 비밀보호를 위해 회원 자신이 정한 문자 또는 숫자의 조합을 말합니다.

## 제3조 (약관의 효력 및 변경)
1. 본 약관은 서비스를 이용하고자 하는 모든 회원에 대하여 그 효력을 발생합니다.
2. 회사는 필요한 경우 관련 법령을 위배하지 않는 범위에서 본 약관을 변경할 수 있으며, 변경된 약관은 서비스 내 공지사항을 통하여 공지합니다.
3. 회원은 변경된 약관에 동의하지 않을 권리가 있으며, 변경된 약관에 동의하지 않을 경우 서비스 이용을 중단하고 이용 계약을 해지할 수 있습니다.

## 제4조 (회원가입)
1. 서비스 이용을 희망하는 자는 본 약관의 내용에 동의하고 회사가 정한 양식에 따라 회원가입 신청을 합니다.
2. 회사는 다음 각 호에 해당하는 신청에 대하여 가입을 승인하지 않거나 추후 회원자격을 상실시킬 수 있습니다.
   - 타인의 명의 또는 정보를 이용하여 신청한 경우
   - 신청서의 내용을 허위로 기재하여 신청한 경우
   - 만 14세 미만인 자가 법정대리인의 동의 없이 신청한 경우

## 제5조 (회원정보의 변경)
회원은 개인정보관리화면을 통하여 언제든지 본인의 정보를 열람하고 수정할 수 있습니다. 회원은 회원가입 시 기재한 사항이 변경되었을 경우 온라인에서 직접 수정하거나 전자우편 등의 방법으로 회사에 변경사항을 통지하여야 합니다.

## 제6조 (서비스의 제공 및 변경)
1. 회사는 다음과 같은 서비스를 제공합니다.
   - 공연·전시·스포츠 등 라이브 엔터테인먼트 정보 제공
   - 좌석 선택 및 티켓 예매
   - 결제 대행
   - 예매 내역 조회 및 취소
2. 회사는 서비스의 내용, 이용방법, 이용시간을 변경할 경우에는 그 사유 및 내용을 변경 전 사전에 공지합니다.

## 제7조 (서비스의 중단)
회사는 시스템 점검, 보수 또는 천재지변, 전시, 사변 등 불가항력적인 사유가 발생한 경우 서비스의 제공을 일시적으로 중단할 수 있습니다.

## 제8조 (예매 및 결제)
1. 회원은 서비스를 통해 제공되는 티켓에 대하여 좌석을 선택하고 결제를 완료함으로써 예매를 확정합니다.
2. 결제는 회사가 지정한 전자결제수단(신용카드, 계좌이체, 간편결제 등)을 통해 이루어집니다.
3. 결제 완료 후 예매 확정 내역은 서비스 내에서 확인할 수 있습니다.

## 제9조 (예매 취소 및 환불)
1. 회원은 공연/행사의 예매 취소 가능 기간 내에 예매를 취소할 수 있으며, 취소 시점에 따라 환불 수수료가 부과될 수 있습니다.
2. 공연 취소, 변경 등 회사의 귀책사유로 인한 환불의 경우 전액 환불을 원칙으로 합니다.
3. 구체적인 취소·환불 규정은 각 공연의 상세 페이지에서 확인할 수 있습니다.

## 제10조 (회원의 의무)
회원은 다음 행위를 하여서는 안 됩니다.
- 신청 또는 변경 시 허위내용의 등록
- 타인의 정보 도용
- 회사가 게시한 정보의 변경
- 회사의 서비스 운영을 방해하거나 타 회원의 이용을 방해하는 행위

## 제11조 (회원자격의 상실)
회원이 본 약관 제10조의 의무를 위반한 경우 회사는 회원자격을 상실시킬 수 있으며, 관련 법령에 따라 법적 책임을 물을 수 있습니다.

## 제12조 (회사의 의무)
1. 회사는 관련 법령과 본 약관이 금지하거나 미풍양속에 반하는 행위를 하지 않으며 지속적이고 안정적으로 서비스를 제공하기 위해 최선을 다합니다.
2. 회사는 회원의 개인정보 보호를 위한 보안시스템을 구축합니다.

## 제13조 (면책조항)
1. 회사는 천재지변, 전쟁, 기간통신사업자의 서비스 중지, 온라인 시스템의 장애 등 불가항력으로 인한 서비스 중단에 대하여 책임을 지지 않습니다.
2. 회사는 회원의 귀책사유로 인한 서비스 이용 장애에 대하여 책임을 지지 않습니다.

## 제14조 (준거법 및 관할법원)
본 약관과 회원과 회사 간의 분쟁에 관한 사항은 대한민국 법을 준거법으로 하며, 분쟁 발생 시 민사소송법에 따른 관할법원에 제기합니다.

## 제15조 (연락처)
본 약관에 관한 문의사항은 다음 연락처로 문의하시기 바랍니다.
- 서비스명: Grapit
- 이메일: support@grapit.com

***

**부칙**: 본 약관은 2026년 4월 14일부터 시행됩니다.
```

**File: `apps/web/content/legal/privacy-policy.md`** — 개인정보처리방침. **Blocker B3 반영 — 제6조 국외이전 추가, 이하 조항 번호 재배치:**

```markdown
# 개인정보처리방침

Grapit(이하 "회사")은 「개인정보 보호법」 제30조에 따라 정보주체의 개인정보를 보호하고 이와 관련한 고충을 신속하고 원활하게 처리할 수 있도록 다음과 같은 처리방침을 두고 있습니다.

## 제1조 (개인정보의 처리 목적)
회사는 다음의 목적을 위하여 개인정보를 처리합니다.

1. **회원 가입 및 관리**: 회원 가입의사 확인, 회원제 서비스 제공에 따른 본인 식별·인증, 회원자격 유지·관리
2. **서비스 제공**: 티켓 예매, 결제, 환불, 고객 상담
3. **마케팅 및 광고에의 활용** (동의 시): 이벤트 및 광고성 정보 제공 및 참여기회 제공

## 제2조 (처리하는 개인정보 항목)
회사는 다음의 개인정보 항목을 처리하고 있습니다.

**필수항목**
- 이메일 주소
- 비밀번호 (argon2id로 단방향 해시 저장)
- 이름
- 휴대전화번호
- 생년월일
- 성별
- 국적

**선택항목**
- 마케팅 수신 동의 여부

**자동수집 항목**
- IP 주소, 쿠키, 서비스 이용 기록, 접속 로그

## 제3조 (개인정보의 처리 및 보유 기간)
1. 회사는 법령에 따른 개인정보 보유·이용기간 또는 정보주체로부터 개인정보를 수집 시에 동의받은 개인정보 보유·이용기간 내에서 개인정보를 처리·보유합니다.
2. 각각의 개인정보 처리 및 보유 기간은 다음과 같습니다.
   - 회원가입 및 관리: 회원 탈퇴 시까지
   - 전자상거래에서의 계약·청약철회, 대금결제, 재화 등 공급기록: 5년 (전자상거래법)
   - 소비자의 불만 또는 분쟁처리에 관한 기록: 3년 (전자상거래법)

## 제4조 (개인정보의 제3자 제공)
회사는 정보주체의 별도의 동의, 법률의 특별한 규정 등 「개인정보 보호법」 제17조 및 제18조에 해당하는 경우에만 개인정보를 제3자에게 제공합니다.

- **결제 처리**: 토스페이먼츠(주) (대한민국) — 결제 처리 및 정산 목적, 결제 완료 후 지체없이 파기

## 제5조 (개인정보처리의 위탁)
회사는 원활한 개인정보 업무처리를 위하여 다음과 같이 개인정보 처리업무를 위탁하고 있습니다.

- **클라우드 호스팅**: Google Cloud Platform — 서비스 인프라 제공 (asia-northeast3 서울 리전, 국내 처리)
- **이메일 발송**: Resend — 비밀번호 재설정 등 트랜잭션 이메일 발송 (국외 수탁 — 제6조 참조)
- **SMS 발송**: Twilio — 휴대전화 인증 (국외 수탁 — 제6조 참조)
- **객체 스토리지**: Cloudflare R2 — 포스터/좌석맵 등 정적 파일 저장 (분산 CDN, 사용자 개인정보 미저장)

## 제6조 (개인정보의 국외이전)

회사는 원활한 서비스 제공을 위하여 다음과 같이 개인정보를 국외로 이전하고 있습니다. (「개인정보 보호법」 제28조의8)

| 수탁자 | 이전 국가 | 이전 항목 | 이전 목적 | 보유·이용 기간 | 이전 일시 및 방법 |
|--------|----------|----------|----------|-----------------|--------------------|
| Resend, Inc. | 미국 (United States) | 이메일 주소 | 비밀번호 재설정 트랜잭션 이메일 발송 | 발송 완료 시까지 (Resend 자체 로그 최대 90일) | 서비스 이용 시점, HTTPS API 전송 (TLS 1.2 이상) |
| Twilio Inc. | 미국 (United States) | 휴대전화번호 | SMS 본인인증 OTP 발송 | 발송 완료 시까지 (Twilio 자체 로그 최대 30일) | 회원가입·재인증 시점, HTTPS API 전송 (TLS 1.2 이상) |

정보주체는 본 국외이전에 대하여 거부할 권리가 있으며, 거부할 경우 해당 기능(비밀번호 재설정 이메일, SMS 본인인증) 일부가 제한될 수 있습니다. 이전 거부 또는 상세 문의는 `privacy@grapit.com` 으로 요청해 주시기 바랍니다.

## 제7조 (정보주체의 권리·의무 및 행사방법)
정보주체는 회사에 대해 언제든지 다음 각 호의 개인정보 보호 관련 권리를 행사할 수 있습니다.

1. 개인정보 열람 요구
2. 오류 등이 있을 경우 정정 요구
3. 삭제 요구
4. 처리정지 요구

위 권리 행사는 서비스 내 마이페이지에서 직접 하거나 `support@grapit.com` 으로 요청하실 수 있습니다.

## 제8조 (개인정보의 파기)
1. 회사는 개인정보 보유기간의 경과, 처리목적 달성 등 개인정보가 불필요하게 되었을 때에는 지체없이 해당 개인정보를 파기합니다.
2. 파기 방법: 전자적 파일 형태의 정보는 기록을 재생할 수 없도록 안전하게 삭제, 종이에 출력된 개인정보는 분쇄기로 분쇄하거나 소각.

## 제9조 (개인정보의 안전성 확보조치)
회사는 개인정보의 안전성 확보를 위해 다음과 같은 조치를 취하고 있습니다.

1. **관리적 조치**: 내부관리계획 수립·시행, 정기적 직원 교육
2. **기술적 조치**: 개인정보처리시스템의 접근권한 관리, 접근통제시스템 설치, 고유식별정보 등의 암호화, 보안프로그램 설치
3. **물리적 조치**: 전산실, 자료보관실 등의 접근통제

## 제10조 (개인정보 보호책임자)
회사는 개인정보 처리에 관한 업무를 총괄해서 책임지고, 개인정보 처리와 관련한 정보주체의 불만처리 및 피해구제 등을 위하여 아래와 같이 개인정보 보호책임자를 지정하고 있습니다.

- **개인정보 보호책임자**: Grapit 대표
- **연락처**: `privacy@grapit.com`

## 제11조 (개인정보처리방침의 변경)
본 개인정보처리방침은 시행일로부터 적용되며, 법령 및 방침에 따른 변경내용의 추가, 삭제 및 정정이 있는 경우에는 변경사항 시행 7일 전부터 공지사항을 통하여 고지할 것입니다.

***

**본 방침은 2026년 4월 14일부터 적용됩니다.**
```

**File: `apps/web/content/legal/marketing-consent.md`** — 마케팅 수신 동의 (기존 Plan 02 초안 유지):

```markdown
# 마케팅 수신 동의

Grapit은 회원님께 유용한 공연·전시 정보와 맞춤형 혜택을 제공하기 위해 마케팅 정보 수신 동의를 요청드립니다. 본 동의는 선택사항이며, 동의하지 않으셔도 서비스 이용에는 제한이 없습니다.

## 수집·이용 목적

- 새로운 공연 및 이벤트 정보 안내
- 맞춤형 혜택 및 프로모션 정보 제공
- 관심 장르 추천 알림

## 수집·이용 항목

- 이메일 주소
- 휴대전화번호
- 관심 장르 (서비스 이용 이력 기반)

## 보유 및 이용 기간

회원 탈퇴 시 또는 수신 동의 철회 시까지

## 전송 수단

- 이메일
- SMS/LMS

## 동의 거부 권리

회원님은 마케팅 정보 수신 동의를 거부할 권리가 있으며, 동의 이후에도 언제든지 마이페이지 또는 수신 거부 링크를 통해 철회할 수 있습니다.

***

**본 동의서는 2026년 4월 14일부터 적용됩니다.**
```

**Step 2: REVIEWS.md MED — Legal accuracy checklist 작성. Blocker B3 반영 — 국외이전 row 포함.**

Create `.planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md`:

```markdown
# Legal MD Accuracy Checklist (REVIEWS.md MED + Blocker B3)

**Phase:** 9 (기술부채 청산) — Plan 02
**Created:** 2026-04-14
**Purpose:** privacy-policy.md의 기재 내용이 실제 users table schema + 실 통합 서비스와 일치하는지 수기 검증 체크리스트. 자동 cross-check 불가 (법적 완결성은 판단 영역).

---

## 수집 항목 vs users table schema

1인 개발자는 아래 each row 를 수동으로 검증한 후 체크박스를 표시한다.

| privacy-policy.md 항목 | users table column (apps/api/src/database/schema/users.ts) | 상태 |
|------------------------|-------------------------------------------------------------|-----|
| 이메일 주소 | `email` (unique, not null) | [ ] 일치 [ ] 불일치 |
| 비밀번호 (argon2id 해시) | `passwordHash` | [ ] 일치 [ ] 불일치 |
| 이름 | `name` | [ ] 일치 [ ] 불일치 |
| 휴대전화번호 | `phone` | [ ] 일치 [ ] 불일치 |
| 생년월일 | `birthDate` | [ ] 일치 [ ] 불일치 |
| 성별 | `gender` | [ ] 일치 [ ] 불일치 |
| 국적 | `country` | [ ] 일치 [ ] 불일치 |
| 마케팅 수신 동의 | `marketingConsent` | [ ] 일치 [ ] 불일치 |

**검증 방법:** `grep -E "(email|passwordHash|name|phone|birthDate|gender|country|marketingConsent):" apps/api/src/database/schema/users.ts`

---

## 외부 처리 위탁사 (privacy-policy.md 제5조) vs 실제 통합

| 위탁사 | 목적 | 실 통합 코드 경로 | 상태 |
|-------|-----|------------------|-----|
| Google Cloud Platform (KR, asia-northeast3) | Cloud Run 서비스 호스팅 | `.github/workflows/deploy.yml` 에 GCP 배포 설정 | [ ] 확인 [ ] 제거 |
| Resend (US) | 트랜잭션 이메일 (password reset) | `apps/api/src/modules/auth/email/email.service.ts` | [ ] 확인 [ ] 제거 |
| Twilio (US) | SMS OTP (Phase 10에서 실 연동 예정, dev mock 중) | `apps/api/src/modules/sms/sms.service.ts` | [ ] 확인 [ ] Phase 10 이후 연동 시 제거 여부 결정 |
| Cloudflare R2 | 포스터/SVG 객체 저장 | `.github/workflows/deploy.yml` R2 secrets + `apps/api` UploadService | [ ] 확인 [ ] 제거 |

**제3자 제공 (privacy-policy.md 제4조):**
| 수취인 | 목적 | 실 통합 | 상태 |
|-------|-----|---------|-----|
| 토스페이먼츠(주) (KR) | 결제 처리/정산 | `apps/api/src/modules/payment/toss-payments.client.ts` | [ ] 확인 [ ] 제거 |

---

## 국외이전 (privacy-policy.md 제6조) — 「개인정보 보호법」 제28조의8 (Blocker B3)

| 항목 | 검증 내용 | 현황 |
|------|---------|------|
| 수탁자: Resend (US) | privacy-policy.md 제6조 표에 이전 국가(미국) / 이전 항목(이메일 주소) / 이전 목적(비밀번호 재설정 이메일 발송) / 보유·이용 기간 / 이전 일시 및 방법 명시 | [ ] 확인 [ ] 미흡 |
| 수탁자: Twilio (US) | privacy-policy.md 제6조 표에 이전 국가(미국) / 이전 항목(휴대전화번호) / 이전 목적(SMS OTP) / 보유·이용 기간 / 이전 일시 및 방법 명시 | [ ] 확인 [ ] 미흡 |
| 이전 거부권 고지 | 정보주체의 이전 거부권 + 거부 시 서비스 일부 제한 + 문의 연락처(`privacy@grapit.com`) 명시 | [ ] 확인 [ ] 미흡 |
| 국내 수탁사 vs 국외 수탁사 분리 | GCP(KR, 국내) / 토스페이먼츠(KR, 국내) 는 국외이전 섹션에 포함하지 않음. Resend/Twilio 만 포함. | [ ] 확인 [ ] 미흡 |

---

## 보유 기간 (privacy-policy.md 제3조)

- [ ] 회원가입 및 관리: 회원 탈퇴 시까지 — (현재 구현: 탈퇴 flow 있는지 확인) — [ ] TODO Phase
- [ ] 전자상거래 기록: 5년 — (legal 요구로 결제/예매 기록이 실제 5년 보존되는지 확인) — [ ] TODO Phase

---

## 연락처

- [ ] `support@grapit.com` — 실제 수신 계정 존재 여부
- [ ] `privacy@grapit.com` — 실제 수신 계정 존재 여부
- [ ] 위 둘 중 미확보 시 privacy-policy.md 에서 해당 주소 수정

---

## Sign-off

검증 완료자: ______________
검증 일시: ______________
검증 결과:
- [ ] 모든 row "일치/확인" — privacy-policy.md 프로덕션 배포 가능
- [ ] 불일치/제거 필요 row 존재 — privacy-policy.md 수정 후 재검증
- [ ] Phase 9 DEBT-02 scope 내 LegalDraftBanner 로 "법률 검토 전 초안" 명시되어 있음을 확인
```

**Step 3.** Commit: `feat(09-tech-debt): add KOPICO-based legal MD drafts + cross-border transfer clause + accuracy checklist (DEBT-02, MED, B3)`.

**NOT allowed:**
- KOPICO 표준 문구를 그대로 복제 (RESEARCH §Risk 5 저작권)
- 법률 검토 완료 표시 (배너는 Task 8에서 "초안" 표시 유지)
- 국외이전 조항 (제6조) 누락 — Blocker B3 의 핵심 요구
- GCP(KR 서울 리전) 를 국외이전 섹션에 포함 (국내 처리이므로 28조의8 대상 아님 — 단, 위탁 제5조에는 명시)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && test -f apps/web/content/legal/terms-of-service.md && test -f apps/web/content/legal/privacy-policy.md && test -f apps/web/content/legal/marketing-consent.md && test -f .planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md && test $(wc -l < apps/web/content/legal/terms-of-service.md) -gt 40 && test $(wc -l < apps/web/content/legal/privacy-policy.md) -gt 55 && test $(wc -l < apps/web/content/legal/marketing-consent.md) -gt 15 && grep -q "Resend" apps/web/content/legal/privacy-policy.md && grep -q "Twilio" apps/web/content/legal/privacy-policy.md && grep -q "토스페이먼츠" apps/web/content/legal/privacy-policy.md && grep -q "Cloudflare R2" apps/web/content/legal/privacy-policy.md && grep -q "개인정보의 국외이전" apps/web/content/legal/privacy-policy.md && grep -q "제28조의8" apps/web/content/legal/privacy-policy.md && grep -q "국외이전" .planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md
    </automated>
  </verify>
  <acceptance_criteria>
    - 3개 MD 파일 존재 (terms-of-service.md, privacy-policy.md, marketing-consent.md)
    - `.planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md` 존재
    - `terms-of-service.md` 40줄 이상 + 최소 10개 `##` 조항
    - `privacy-policy.md` 55줄 이상 (Blocker B3 — 국외이전 조항 추가분 반영)
    - `privacy-policy.md` 에 `Resend` / `Twilio` / `토스페이먼츠` / `Cloudflare R2` 모두 언급 (실 통합 서비스와 일치)
    - `privacy-policy.md` 에 exact `개인정보의 국외이전` 섹션명 포함 (Blocker B3)
    - `privacy-policy.md` 에 exact `제28조의8` 법조 인용 포함 (Blocker B3 — 개인정보보호법 근거)
    - `privacy-policy.md` 에 `미국 (United States)` 2회 이상 언급 (Resend + Twilio row)
    - `marketing-consent.md` 15줄 이상
    - checklist 파일에 `## 수집 항목 vs users table schema` + `## 외부 처리 위탁사` + `## 국외이전` 섹션 존재 (Blocker B3 — checklist row 추가)
    - Commit matches `feat(09-tech-debt): add KOPICO-based legal MD drafts + cross-border transfer clause + accuracy checklist (DEBT-02, MED, B3)`
  </acceptance_criteria>
  <done>3개 법률 MD 초안이 실 통합 서비스와 일관되게 작성되고 개인정보보호법 제28조의8 국외이전 조항 + 정확성 checklist 초안 생성됨</done>
</task>

<task id="9-02-t8-turbopack-md-config" type="auto" tdd="false">
  <name>Task 8 (DEBT-02): next.config.ts Turbopack rule (raw-loader 단일) + env.d.ts 타입 선언</name>
  <files>
    apps/web/next.config.ts,
    apps/web/env.d.ts
  </files>
  <read_first>
    - apps/web/next.config.ts (현 상태 — turbopack 블록 L28-34 근처)
    - apps/web/next-env.d.ts (기존 Next.js 자동 생성 파일 — 건드리지 말 것)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pattern 3 Option A)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: 단일 접근법 — raw-loader)
  </read_first>
  <action>
**Step 1.** `apps/web/env.d.ts` 생성 (신규 파일):

```typescript
// Type declarations for non-TS modules imported at build time.
// Phase 9 DEBT-02: MD files imported as raw strings via Turbopack raw-loader rule.

declare module '*.md' {
  const content: string;
  export default content;
}
```

**Step 2.** `apps/web/next.config.ts` 의 `turbopack` 블록에 `rules` 추가 — **REVIEWS.md MED: raw-loader 단일 접근법만 사용**:

기존 L33-35:
```typescript
turbopack: {
  root: resolve(__dirname, '../../'),
},
```

변경 후 (`type: 'raw'` 옵션은 언급하지 않음 — 일관성 유지):
```typescript
turbopack: {
  root: resolve(__dirname, '../../'),
  rules: {
    '*.md': { as: '*.js', loaders: ['raw-loader'] },
  },
},
```

**Step 3.** 타입 충돌 확인:
- `apps/web/env.d.ts` 는 `next-env.d.ts` 와 공존 가능 — next-env는 Next.js가 자동 생성/관리.
- `tsconfig.json` 의 `include` 배열에 `"**/*.d.ts"` 또는 `"env.d.ts"` 가 이미 포함되어 있는지 확인. 없다면 추가.

**Step 4. REVIEWS.md MED — 실 빌드 검증:**
```bash
pnpm --filter @grapit/web build
```
dist 결과가 정상적으로 생성되어야 함. 빌드 실패 시 raw-loader 버전 업그레이드 또는 `as: '*.js'` 옵션 조정 (모듈 형태 지시).

**Step 5.** Commit: `feat(09-tech-debt): enable MD raw import via Turbopack raw-loader (DEBT-02)`.

**NOT allowed:**
- `next-env.d.ts` 파일 수정 (Next.js 자동 관리)
- `?raw` query string 사용 (Pitfall 1 — Turbopack 미지원)
- `type: 'raw'` 옵션 시도 (REVIEWS.md MED — 단일 접근법 고수)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && test -f apps/web/env.d.ts && grep -q "declare module '\\*.md'" apps/web/env.d.ts && grep -q "'raw-loader'" apps/web/next.config.ts && ! grep -q "type: 'raw'" apps/web/next.config.ts && pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web build
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/env.d.ts` exists
    - `apps/web/env.d.ts` contains `declare module '*.md'`
    - `apps/web/next.config.ts` contains `'raw-loader'` in turbopack rules
    - `apps/web/next.config.ts` does NOT contain `type: 'raw'` (단일 접근법 — REVIEWS.md MED)
    - `apps/web/next.config.ts` does NOT contain `?raw` (Pitfall 1)
    - `pnpm --filter @grapit/web typecheck` exits 0
    - `pnpm --filter @grapit/web build` exits 0 (REVIEWS.md MED — 실 빌드 검증)
    - `apps/web/next-env.d.ts` unchanged (`test -z "$(git diff HEAD -- apps/web/next-env.d.ts)"`)
    - Commit matches `feat(09-tech-debt): enable MD raw import via Turbopack raw-loader (DEBT-02)`
  </acceptance_criteria>
  <done>Turbopack이 *.md를 raw-loader로 처리하도록 설정되고 실 빌드가 성공</done>
</task>

<task id="9-02-t9-legal-components" type="auto" tdd="false">
  <name>Task 9 (DEBT-02 + W5): LegalDraftBanner + TermsMarkdown 컴포넌트 생성 + WCAG AA contrast 검증</name>
  <files>
    apps/web/components/legal/legal-draft-banner.tsx,
    apps/web/components/legal/terms-markdown.tsx
  </files>
  <read_first>
    - apps/web/app/globals.css (L43-46 warning 토큰 확인 — `text-warning-foreground` 또는 대체 토큰 존재 여부)
    - .planning/phases/09-tech-debt/09-UI-SPEC.md (L107-125 배너 스펙, L69-87 MD 타이포 매핑)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pattern 5 Banner, §Pattern 4 react-markdown mapping)
    - apps/web/lib/cn.ts (기존 className util — 필요 시 참조)
    - node_modules/react-markdown/package.json (버전 확인 — Task 1에서 설치)
  </read_first>
  <action>
**W5 — WCAG AA contrast handling:**
- 기존 하드코딩 `text-[#8B6306]` on `bg-warning-surface` 조합은 디자인 시스템 외부이므로 contrast 수기 측정이 필요.
- 우선 `apps/web/app/globals.css` 에 `text-warning-foreground` (또는 `text-warning-strong` / `text-warning-text` 등) 토큰이 이미 존재하는지 확인. 존재하면 토큰 사용.
- 존재하지 않으면 `text-[#8B6306]` 유지하되, 다음 중 하나 실행:
  - (a) https://webaim.org/resources/contrastchecker/ 에서 foreground `#8B6306` / background `#FFFBEB` (혹은 `bg-warning-surface` 의 실 hex) 입력 → 대비 비율 ≥ 4.5:1 확인 → 결과를 Plan SUMMARY 에 기록.
  - (b) 대비가 4.5 미만이면 foreground 를 더 진한 tone (`#6B4D04` 등) 으로 수정.

**File: `apps/web/components/legal/legal-draft-banner.tsx`** (RESEARCH §Pattern 5 verbatim — W5 contrast 처리):

```tsx
import { AlertTriangle } from 'lucide-react';

/**
 * LegalDraftBanner
 * Phase 9 DEBT-02: 법률 검토 완료 전 초안임을 안내하는 배너.
 * 런칭 후 법률 검토 완료 시 이 컴포넌트를 제거하거나 `IS_DRAFT` 플래그로 노출 제어.
 *
 * UI-SPEC §Color L107-125, §Accessibility L291-297 준수.
 * W5: foreground `#8B6306` on `bg-warning-surface` WCAG AA 대비 ≥ 4.5:1 확인 필수
 *     (디자인 시스템에 `text-warning-foreground` 토큰 존재 시 토큰 우선 사용).
 */
export function LegalDraftBanner() {
  return (
    <div
      role="note"
      aria-label="초안 안내"
      className="mb-6 flex items-start gap-2 rounded-md border border-warning/30 border-l-4 border-l-warning bg-warning-surface px-4 py-3"
    >
      <AlertTriangle className="size-4 text-warning shrink-0 mt-0.5" aria-hidden="true" />
      <span className="text-caption text-[#8B6306]">
        본 약관은 런칭 전 법률 검토를 거쳐 교체될 초안입니다.
      </span>
    </div>
  );
}
```

(만약 Step read_first 에서 `text-warning-foreground` 또는 동등한 토큰을 발견했다면 `text-[#8B6306]` 을 해당 토큰으로 교체. 이 결정은 executor 가 globals.css 실제 내용을 보고 판단.)

**File: `apps/web/components/legal/terms-markdown.tsx`** (RESEARCH §Pattern 4):

```tsx
'use client';

import ReactMarkdown, { type Components } from 'react-markdown';
import remarkGfm from 'remark-gfm';

/**
 * TermsMarkdown
 * Phase 9 DEBT-02: 법률 MD 원문을 Dialog 안에서 prose-like 스타일로 렌더.
 * `h1` 은 null 처리 (DialogTitle이 이미 제목 역할).
 *
 * UI-SPEC §Typography L75-87 타이포 매핑 적용.
 */
const components: Components = {
  h1: () => null,
  h2: (props) => (
    <h2 className="mt-6 text-base font-semibold text-gray-900 first:mt-0" {...props} />
  ),
  h3: (props) => (
    <h3 className="mt-4 text-caption font-semibold text-gray-800" {...props} />
  ),
  p: (props) => (
    <p className="mt-2 text-caption leading-relaxed text-gray-700" {...props} />
  ),
  ul: (props) => (
    <ul className="mt-2 ml-5 list-disc text-caption leading-relaxed text-gray-700" {...props} />
  ),
  ol: (props) => (
    <ol className="mt-2 ml-5 list-decimal text-caption leading-relaxed text-gray-700" {...props} />
  ),
  li: (props) => <li className="mt-1" {...props} />,
  strong: (props) => <strong className="font-semibold text-gray-900" {...props} />,
  a: (props) => (
    <a
      className="text-primary underline hover:text-primary/80"
      target="_blank"
      rel="noopener noreferrer"
      {...props}
    />
  ),
  hr: () => <hr className="my-4 border-gray-200" />,
};

export function TermsMarkdown({ children }: { children: string }) {
  return (
    <ReactMarkdown components={components} remarkPlugins={[remarkGfm]}>
      {children}
    </ReactMarkdown>
  );
}
```

Commit: `feat(09-tech-debt): add LegalDraftBanner + TermsMarkdown components + WCAG AA contrast check (DEBT-02, W5)`.

**NOT allowed:**
- `@tailwindcss/typography` plugin 사용 (UI-SPEC L71 prohibits)
- `dangerouslySetInnerHTML` (XSS risk)
- `h1` 렌더링 (중복 제거 — UI-SPEC L75)
- WCAG AA contrast 검증 생략 (W5 requirement — SUMMARY 에 측정값 기록 필수)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && test -f apps/web/components/legal/legal-draft-banner.tsx && test -f apps/web/components/legal/terms-markdown.tsx && grep -q 'role="note"' apps/web/components/legal/legal-draft-banner.tsx && grep -q "본 약관은 런칭 전 법률 검토를 거쳐 교체될 초안입니다" apps/web/components/legal/legal-draft-banner.tsx && grep -q "react-markdown" apps/web/components/legal/terms-markdown.tsx && pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/legal/legal-draft-banner.tsx` exists
    - 파일에 `role="note"` 와 `aria-label="초안 안내"` 포함
    - 정확한 문구 `"본 약관은 런칭 전 법률 검토를 거쳐 교체될 초안입니다."` 포함
    - `AlertTriangle` from `lucide-react` + `aria-hidden="true"` 포함
    - W5 — WCAG AA contrast: foreground / `bg-warning-surface` 조합의 대비 비율 ≥ 4.5:1 (수기 검증 후 SUMMARY 에 측정값 기록. `text-warning-foreground` 같은 디자인 토큰 사용 시 설계상 충족 간주)
    - `apps/web/components/legal/terms-markdown.tsx` exists
    - `react-markdown` + `remark-gfm` import
    - `h1: () => null` 매핑 (UI-SPEC L75)
    - `! grep -q "@tailwindcss/typography" apps/web/components/legal/terms-markdown.tsx` (Typography 금지)
    - `pnpm --filter @grapit/web typecheck` exits 0
    - Commit matches `feat(09-tech-debt): add LegalDraftBanner + TermsMarkdown components + WCAG AA contrast check (DEBT-02, W5)`
  </acceptance_criteria>
  <done>두 컴포넌트가 생성되고 typecheck PASS, UI-SPEC 접근성/타이포 준수, W5 WCAG AA contrast 측정값 SUMMARY에 기록됨</done>
</task>

<task id="9-02-t10-signup-step2-wire" type="auto" tdd="false">
  <name>Task 10 (DEBT-02): signup-step2.tsx에 MD 3개 + 배너 + 마크다운 렌더 연결</name>
  <files>apps/web/components/auth/signup-step2.tsx</files>
  <read_first>
    - apps/web/components/auth/signup-step2.tsx (전체 — 특히 L22-38 TERMS_CONTENT, L183-195 Dialog body)
    - apps/web/components/legal/legal-draft-banner.tsx (Task 9 결과)
    - apps/web/components/legal/terms-markdown.tsx (Task 9 결과)
    - apps/web/content/legal/*.md (Task 7 결과 3개)
    - apps/web/env.d.ts (Task 8 결과 — MD 모듈 타입)
    - .planning/phases/09-tech-debt/09-UI-SPEC.md (L222-235 상호작용 흐름, L131-141 Copy)
  </read_first>
  <action>
`apps/web/components/auth/signup-step2.tsx` 수정.

**변경 포인트:**

1. **Import 추가 (기존 import 블록 끝에):**
```typescript
import termsOfServiceMd from '@/content/legal/terms-of-service.md';
import privacyPolicyMd from '@/content/legal/privacy-policy.md';
import marketingConsentMd from '@/content/legal/marketing-consent.md';
import { LegalDraftBanner } from '@/components/legal/legal-draft-banner';
import { TermsMarkdown } from '@/components/legal/terms-markdown';
```

2. **L22-38 TERMS_CONTENT 객체 교체**:
```typescript
const LEGAL_CONTENT: Record<string, { title: string; content: string }> = {
  termsOfService: { title: '이용약관', content: termsOfServiceMd },
  privacyPolicy: { title: '개인정보처리방침', content: privacyPolicyMd },
  marketingConsent: { title: '마케팅 수신 동의', content: marketingConsentMd },
};
```

3. **L183-195 Dialog body 교체:**
기존:
```tsx
<DialogContent className="max-h-[80vh] overflow-y-auto">
  <DialogHeader>
    <DialogTitle>{TERMS_CONTENT[dialogKey]?.title}</DialogTitle>
    <DialogDescription className="sr-only">
      {TERMS_CONTENT[dialogKey]?.title} 상세 내용
    </DialogDescription>
  </DialogHeader>
  <div className="text-caption leading-relaxed text-gray-700">
    {TERMS_CONTENT[dialogKey]?.content}
  </div>
</DialogContent>
```

변경 후:
```tsx
<DialogContent className="max-h-[80vh] overflow-y-auto">
  <DialogHeader>
    <DialogTitle>{LEGAL_CONTENT[dialogKey]?.title}</DialogTitle>
    <DialogDescription className="sr-only">
      {LEGAL_CONTENT[dialogKey]?.title} 상세 내용
    </DialogDescription>
  </DialogHeader>
  <LegalDraftBanner />
  <TermsMarkdown>{LEGAL_CONTENT[dialogKey]?.content ?? ''}</TermsMarkdown>
</DialogContent>
```

4. **나머지 로직은 모두 유지** — `handleSelectAll`, checkbox 상태 관리, "보기" 버튼 onClick, 제출 로직 변경 없음.

Commit: `feat(09-tech-debt): wire legal MD + draft banner into signup-step2 (DEBT-02)`.

**NOT allowed:**
- Checkbox 동작 변경
- 약관 동의 데이터 구조 변경 (RegisterStep2Input 영향 금지)
- `LEGAL_CONTENT` 를 client state로 이동 (정적 상수 유지)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web lint && test "$(grep -c 'TERMS_CONTENT' apps/web/components/auth/signup-step2.tsx | tr -d ' ')" = "0" && grep -q "LEGAL_CONTENT" apps/web/components/auth/signup-step2.tsx && grep -q "LegalDraftBanner" apps/web/components/auth/signup-step2.tsx && grep -q "TermsMarkdown" apps/web/components/auth/signup-step2.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c 'TERMS_CONTENT' apps/web/components/auth/signup-step2.tsx | tr -d ' ')" = "0"` (old constant removed)
    - `test "$(grep -c 'LEGAL_CONTENT' apps/web/components/auth/signup-step2.tsx | tr -d ' ')" -ge 3` (definition + usages)
    - `test "$(grep -c '<LegalDraftBanner' apps/web/components/auth/signup-step2.tsx | tr -d ' ')" = "1"`
    - `test "$(grep -c '<TermsMarkdown>' apps/web/components/auth/signup-step2.tsx | tr -d ' ')" = "1"`
    - `grep -q "terms-of-service.md" apps/web/components/auth/signup-step2.tsx`
    - `grep -q "privacy-policy.md" apps/web/components/auth/signup-step2.tsx`
    - `grep -q "marketing-consent.md" apps/web/components/auth/signup-step2.tsx`
    - `pnpm --filter @grapit/web typecheck` exits 0
    - `pnpm --filter @grapit/web lint` exits 0
    - Commit matches `feat(09-tech-debt): wire legal MD + draft banner into signup-step2 (DEBT-02)`
  </acceptance_criteria>
  <done>signup-step2가 3개 MD + LegalDraftBanner + TermsMarkdown 조합으로 렌더링되고 기존 Dialog 동작 유지</done>
</task>

<task id="9-02-t11-env-deploy-config" type="auto" tdd="false">
  <name>Task 11: .env.example + deploy.yml에 RESEND 환경변수 추가 (hard-fail 대상)</name>
  <files>
    .env.example,
    .github/workflows/deploy.yml
  </files>
  <read_first>
    - .env.example (루트 — 기존 RESEND_API_KEY/RESEND_FROM_EMAIL 부재 확인)
    - .github/workflows/deploy.yml (L90-105 secrets 섹션)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Runtime State Inventory — 신규 필수 env)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (MED: RESEND_FROM_EMAIL hard-fail이므로 secrets 필수)
    - CLAUDE.md (환경변수 섹션 — 루트 .env 사용, Secret Manager 연동 원칙)
  </read_first>
  <action>
**Step 1.** `.env.example` (루트) 에 Resend 블록 추가.

기존 파일의 적절한 섹션(예: SMS 섹션 뒤 또는 OAuth 뒤) 에 다음 블록을 추가:

```bash
# -- Email (Resend) --
# Required in production. Dev environment without these values falls back to console.log mock.
# Production: both RESEND_API_KEY and RESEND_FROM_EMAIL are hard-required (see EmailService constructor).
# RESEND_FROM_EMAIL must be a verified sender domain configured in Resend dashboard.
# RESEND_API_KEY=re_your_api_key_here
# RESEND_FROM_EMAIL=no-reply@grapit.com
```

**Step 2.** `.github/workflows/deploy.yml` 의 `deploy-api` job, `secrets:` 블록 (L90-105) 끝에 추가:

기존 L105 `R2_SECRET_ACCESS_KEY=r2-secret-access-key:latest` 뒤에 새 줄로:
```yaml
            RESEND_API_KEY=resend-api-key:latest
            RESEND_FROM_EMAIL=resend-from-email:latest
```

YAML 들여쓰기는 기존 secrets 블록과 동일해야 함 (12 spaces).

**Step 3.** Commit: `feat(09-tech-debt): wire RESEND env vars into .env.example + deploy.yml (DEBT-01)`.

**POST-DEPLOY manual step (Plan에 기록만 — executor 실행 대상 아님, SUMMARY에 체크리스트):**
1. Resend 대시보드에서 API 키 발급 (https://resend.com/api-keys)
2. Resend 대시보드에서 발신 도메인(예: `grapit.com`) DKIM/SPF 검증
3. `gcloud secrets create resend-api-key --data-file=-` 로 Secret Manager에 등록
4. `gcloud secrets create resend-from-email --data-file=-` 로 발신 주소 등록 (검증된 grapit.com 도메인)
5. Cloud Run 배포가 정상적으로 secret을 mount 하는지 확인

**NOT allowed:**
- `.env` 파일 (gitignore 대상) 수정
- 실제 RESEND_API_KEY 값을 `.env.example` 에 기록 (placeholder 주석만)
- env_vars 블록에 RESEND_API_KEY 노출 (반드시 secrets 블록)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q "RESEND_API_KEY" .env.example && grep -q "RESEND_FROM_EMAIL" .env.example && grep -q "RESEND_API_KEY=resend-api-key:latest" .github/workflows/deploy.yml && grep -q "RESEND_FROM_EMAIL=resend-from-email:latest" .github/workflows/deploy.yml && ! grep -E "^RESEND_API_KEY=re_[a-zA-Z0-9]+$" .env.example
    </automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `RESEND_API_KEY` (commented or placeholder)
    - `.env.example` contains `RESEND_FROM_EMAIL`
    - `! grep -E "^RESEND_API_KEY=re_[a-zA-Z0-9]+$" .env.example` (실 키 값 금지)
    - `.github/workflows/deploy.yml` contains `RESEND_API_KEY=resend-api-key:latest` in secrets block
    - `.github/workflows/deploy.yml` contains `RESEND_FROM_EMAIL=resend-from-email:latest` in secrets block
    - Commit matches `feat(09-tech-debt): wire RESEND env vars into .env.example + deploy.yml (DEBT-01)`
  </acceptance_criteria>
  <done>.env.example + deploy.yml secrets 블록에 RESEND 설정 추가 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API (`/auth/password-reset/request`, `/confirm`) | 공개 엔드포인트 — 이메일 주소/토큰을 body로 받아 Resend 발송/DB 업데이트 트리거 |
| API → Resend SaaS | HTTPS + API 키 기반 서버 간 통신 (TLS 암호화, SDK 기본) |
| Cloud Run → Google Secret Manager | RESEND_API_KEY / RESEND_FROM_EMAIL 은 secret-mount로만 노출 |
| Web Client → MD 정적 자산 | 빌드타임에 번들링된 raw string (런타임 fetch 아님) |
| NestJS bootstrap → runtime env | FRONTEND_URL production validation으로 misconfig 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure (V7) | `POST /auth/password-reset/request` | mitigate | `auth.service.ts:220-222` silent return when user not found — 변경 금지. email enumeration 방지. 컨트롤러는 항상 동일한 성공 메시지 반환 (`auth.controller.ts:124-125`). auth.service.spec 에 enumeration-prevention 테스트 추가. |
| T-09-04 | Spoofing / Tampering (V14) | `EmailService` Resend 클라이언트 초기화 | mitigate | NODE_ENV=production + !RESEND_API_KEY → throw. NODE_ENV=production + !RESEND_FROM_EMAIL (or invalid) → throw (REVIEWS.md MED). Silent console.log fallback은 dev 전용. |
| T-09-05 | Information Disclosure (V14) | Cloud Run 로그 | mitigate | RESEND_API_KEY / RESEND_FROM_EMAIL은 Secret Manager 경유만 주입, `deploy.yml` env_vars 블록에 배치 금지 (secrets 전용). logger는 error message만 기록하고 API 키 자체는 never logged. |
| T-09-06 | Tampering (V5) | Resend SDK 응답 처리 | mitigate | SDK는 throw하지 않고 `{data, error}` 반환 (Pitfall 2). EmailService는 `if (error)` 분기로 실패 감지하여 `{success: false, error: error.message}` 반환. |
| T-09-07 | Information Disclosure (V7) | 비밀번호 재설정 응답 | mitigate | `auth.service.ts.requestPasswordReset` 은 `Promise<void>` 반환 — 내부 실패도 사용자에게는 동일 "메시지 발송됨" 응답. 실패 시 logger에만 기록. |
| T-09-08 | Tampering (V8) | 법률 MD 문서 | mitigate | MD 문서는 빌드타임 번들링 (client fetch 아님). git 이력으로 버전 추적 (D-08). react-markdown은 HTML을 parse하지 않고 AST로 직접 렌더 (XSS 안전). |
| T-09-09 | Repudiation | Email 발송 이력 | accept | Resend 대시보드가 발송/바운스 로그 제공. Grapit 내부 DB에 이메일 로그 별도 저장하지 않음. |
| T-09-10 | DoS / Abuse (V13) | `/auth/password-reset/request`, `/confirm` | mitigate (REVIEWS.md HIGH-04) | `@Throttle({ default: { limit: 3, ttl: 900000 } })` 데코레이터 적용 — 15분당 3회/IP 제한 (v6 객체 시그니처, ttl ms 단위). AppModule 의 기본 throttler (60/min) 을 per-route로 타이트하게 override. Resend 무료 3,000건/월 quota burn 방지. 초과 시 429 응답. |
| T-09-11 | Phishing (V5 L1) | Password reset 이메일 spoofing | mitigate | RESEND_FROM_EMAIL 은 production에서 검증된 도메인만 허용 (EmailService hard-fail). onboarding@resend.dev fallback 금지 — deliverability + 신뢰도 손상 방지 (REVIEWS.md MED). |
| T-09-12 | Tampering / Phishing | Reset link URL injection | mitigate | `FRONTEND_URL` production validation (main.ts bootstrap) — non-https URL로 시작 시 `process.exit(1)`. reset link가 fake http:// 도메인으로 전송되는 시나리오 차단. |
| T-09-13 | Abuse (V7) | Unknown email → resource enumeration | mitigate | `requestPasswordReset` silent return (변경 금지) + 컨트롤러 unified 응답 + auth.service.spec enumeration test 추가. |
| T-09-18 | Tampering (V6 Token) | Password reset token one-time-use | mitigate (Blocker B4) | `auth.service.ts:227 + L262-272` 에서 secret = `jwtSecret + user.passwordHash` 구성 — 패스워드 변경 시 passwordHash 가 바뀌므로 같은 토큰 재사용 불가 (자연스러운 one-time token). `auth.service.spec.ts` 의 Blocker B4 테스트가 `signAsync`/`verifyAsync` 의 secret argument 와 재사용 시 reject 동작을 assert. |
| T-09-19 | Privacy (개인정보보호법 제28조의8) | 국외 수탁사 Resend/Twilio (US) | mitigate (Blocker B3) | privacy-policy.md 제6조 에 수탁자·이전 국가·항목·목적·보유기간·이전방법 표 형태 기재 + 정보주체 이전 거부권 고지. 자동 검증: `grep -q "제28조의8"` + `grep -q "개인정보의 국외이전"`. 수기 검증: 09-02-LEGAL-ACCURACY-CHECKLIST.md 국외이전 섹션. |

RESEARCH §Security Domain V7 이메일 enumeration + V14 misconfig + V13 rate limit + V6 one-time token rotation + 개인정보보호법 제28조의8 완화 조치 완료.
</threat_model>

<verification>
**Wave-level sampling (VALIDATION.md §Sampling Rate):**

1. **태스크별 (per-task, < 60s):**
   - Task 0: `pnpm --filter @grapit/api test email.service` (RED 기대)
   - Task 2 (Blocker B6): grep-only — typecheck 는 Task 3 로 이동
   - Task 3: `pnpm --filter @grapit/api test email.service` (6/6 GREEN) + `pnpm --filter @grapit/api typecheck` + `pnpm --filter @grapit/api build`
   - Task 5: `pnpm --filter @grapit/api typecheck` + grep for v6 Throttle 시그니처
   - Task 6: `pnpm --filter @grapit/api test auth.service` (기존 + 신규 3 통합 테스트 전체 PASS — Blocker B4)
   - Task 8: `pnpm --filter @grapit/web build` (REVIEWS.md MED)
   - Task 9/10: `pnpm --filter @grapit/web typecheck` + `pnpm --filter @grapit/web lint`

2. **Plan 전체 완료 후 (per-wave, < 180s — build 포함):**
   - `pnpm typecheck` (루트)
   - `pnpm lint` (루트)
   - `pnpm test` (루트 — api + web 전체)
   - `pnpm --filter @grapit/api build`
   - `pnpm --filter @grapit/web build`

3. **수동 검증 (manual — VALIDATION.md §Manual-Only + REVIEWS.md MED):**
   - Resend dev mock 동작: `apps/api` dev server 기동 → `curl -X POST http://localhost:8080/api/v1/auth/password-reset/request -H "Content-Type: application/json" -d '{"email":"existing-user@grapit.test"}'` → api 로그에 "DEV EMAIL: password reset link" 확인
   - FRONTEND_URL hard-fail 동작: `NODE_ENV=production pnpm --filter @grapit/api start` → exit 1 + "FRONTEND_URL must be an https" 로그
   - Rate limit 동작 (v6 ms 단위): 15분 내 4번째 password-reset/request 호출 → 429 (1.5초 window 가 아님을 확인)
   - Dialog UI 수동 확인: 회원가입 step 2 → "이용약관 보기" → LegalDraftBanner 황갈색 배너 + MD 본문 렌더 확인
   - W5 contrast 확인: Banner foreground/background 조합 WCAG AA 대비 ≥ 4.5:1 측정값 기록
   - 법률 정확성 checklist (`09-02-LEGAL-ACCURACY-CHECKLIST.md`) sign-off — 국외이전 섹션 row 포함 (Blocker B3)

**Executor checkpoint (W2):**
Task 6 commit 완료 시점에서 pnpm test / typecheck / build 확인 → green 이어야 Task 7 로 진행.

**Expected-absence verify 패턴 (REVIEWS.md MED):**
- 모든 expected-absence check는 `! grep -q ...` 또는 `test "$(grep -c ... | tr -d ' ')" = "0"` 사용. `grep -c ... && ...` 체이닝 금지 — Plan 02 의 acceptance criteria 전체를 이 패턴으로 작성.

**Failure recovery:**
- Turbopack raw-loader 빌드 실패 시 `raw-loader` 버전 다운그레이드 (e.g. `^4.0.2`) 고려
- `@react-email/components` 의 React peer 충돌 시 `apps/api/package.json` 의 react 버전을 web/과 일치시킴
- Throttler 시그니처 버전 차이 발생 시 `apps/api/package.json` 의 `@nestjs/throttler` 버전 재확인 — `^6.4.0` fixed 이므로 v5 fallback 은 불필요 (Blocker B5)
</verification>

<success_criteria>
- [ ] `pnpm typecheck` exits 0 (루트)
- [ ] `pnpm lint` exits 0 (루트)
- [ ] `pnpm test` exits 0 (루트)
- [ ] `pnpm --filter @grapit/api build` exits 0 (REVIEWS.md MED)
- [ ] `pnpm --filter @grapit/web build` exits 0 (REVIEWS.md MED)
- [ ] `pnpm --filter @grapit/api test email.service` 6/6 PASS (RESEND_FROM_EMAIL 검증 포함)
- [ ] `pnpm --filter @grapit/api test auth.service` 기존 + 신규 integration test PASS (HIGH-03 + Blocker B4)
- [ ] `apps/api/src/modules/auth/email/` 디렉토리에 3개 파일(service, module, template) 존재
- [ ] `test "$(grep -c 'console.log.*Password Reset' apps/api/src/modules/auth/auth.service.ts | tr -d ' ')" = "0"`
- [ ] `grep -q "this.emailService.sendPasswordResetEmail" apps/api/src/modules/auth/auth.service.ts`
- [ ] `test "$(grep -c '@Throttle' apps/api/src/modules/auth/auth.controller.ts | tr -d ' ')" = "2"` (HIGH-04)
- [ ] `grep -q "ttl: 900000" apps/api/src/modules/auth/auth.controller.ts` (Blocker B5 — ms 정확성)
- [ ] `grep -q "limit: 3" apps/api/src/modules/auth/auth.controller.ts` (Blocker B5 — v6 객체 시그니처)
- [ ] `grep -q "FRONTEND_URL must be an https" apps/api/src/main.ts` (MED)
- [ ] `test "$(grep -c 'EmailModule' apps/api/src/modules/auth/auth.module.ts | tr -d ' ')" = "2"`
- [ ] `apps/api/package.json` 에 `resend`, `@react-email/components`, `react` 존재 + `nodemailer`, `@types/nodemailer` 부재
- [ ] `apps/api/tsconfig.json` 에 `"jsx": "react-jsx"` 존재 (MED)
- [ ] `apps/web/content/legal/` 에 3개 MD 파일 존재
- [ ] `apps/web/content/legal/privacy-policy.md` 에 `제28조의8` + `개인정보의 국외이전` 섹션 포함 (Blocker B3)
- [ ] `apps/web/components/legal/legal-draft-banner.tsx` + `terms-markdown.tsx` 존재
- [ ] WCAG AA contrast ≥ 4.5:1 Banner 에 대해 측정값 SUMMARY 기록 (W5)
- [ ] `apps/web/env.d.ts` 에 `declare module '*.md'` 존재
- [ ] `apps/web/next.config.ts` 에 `'raw-loader'` 존재 (MED: 단일 접근법)
- [ ] `signup-step2.tsx` 에서 `TERMS_CONTENT` 제거 + `LEGAL_CONTENT` + `LegalDraftBanner` + `TermsMarkdown` 사용
- [ ] `.env.example` 에 `RESEND_API_KEY`, `RESEND_FROM_EMAIL` 기록
- [ ] `.github/workflows/deploy.yml` secrets 블록에 Resend 2개 키 기록
- [ ] `auth.service.ts:220-222` silent return 보존 (email enumeration 방지)
- [ ] `.planning/phases/09-tech-debt/09-02-LEGAL-ACCURACY-CHECKLIST.md` 존재 + 국외이전 row 포함 (MED + Blocker B3)
- [ ] `auth.service.spec.ts` 에 `Blocker B4` + `one-time token` + `signAsync` secret arg assert + `verifyAsync` secret arg assert 포함 (Blocker B4)
- [ ] 12개의 의미 있는 커밋 (test RED / install deps / tsconfig jsx / EmailService GREEN / main.ts FRONTEND_URL / auth wiring + throttle v6 / reset flow integration test + B4 / legal MDs + B3 cross-border + checklist / next.config / legal components + W5 / signup-step2 wiring / env+deploy)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tech-debt/09-02-SUMMARY.md` covering:
- DEBT-01 / DEBT-02 closure evidence
- REVIEWS.md + Revision-2 Blocker 대응 증거:
  - HIGH-03: auth.service.spec의 integration test 결과 첨부
  - HIGH-04: @Throttle 적용 확인 + v6 객체 시그니처 + ms 단위
  - Blocker B3: privacy-policy.md 제6조 국외이전 섹션 + 정확성 checklist 국외이전 row
  - Blocker B4: auth.service.spec Blocker B4 + one-time token 테스트 통과 결과
  - Blocker B5: @nestjs/throttler@^6.4.0 확인 + `@Throttle({ default: { limit: 3, ttl: 900000 } })` 적용 확인
  - Blocker B6: Task 2 는 grep-only verify, Task 3 에서 typecheck/build 검증 완료
  - MED RESEND_FROM_EMAIL hard-fail: test 4/5 PASS
  - MED FRONTEND_URL: main.ts manual run 3가지 결과
  - MED MD raw import: `pnpm --filter @grapit/web build` 결과
  - MED API React Email build: `pnpm --filter @grapit/api build` 결과
  - MED Legal accuracy: checklist sign-off 상태
  - MED grep 버그: acceptance_criteria 전체 패턴 점검 완료
  - W5: Banner contrast 측정값 기록
  - W2: Task 6 halfway checkpoint 실시 결과
- files_modified actual vs planned
- Resend dashboard setup prerequisite checklist (1인 개발자가 실행할 외부 작업)
- Next: Plan 03 (Toss E2E) can begin
</output>
</content>
</invoke>