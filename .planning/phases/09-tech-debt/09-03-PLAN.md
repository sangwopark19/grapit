---
phase: 09-tech-debt
plan: 03
title: Toss E2E (DEBT-05 Playwright 실 widget mount + confirm API intercept + CI hard-gate + 키 격리)
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - apps/web/e2e/toss-payment.spec.ts
  - apps/web/e2e/fixtures/booking-store.ts
  - apps/web/e2e/helpers/auth.ts
  - apps/web/playwright.config.ts
  - apps/web/stores/use-booking-store.ts
  - .env.example
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - DEBT-05

must_haves:
  truths:
    - "Happy path test가 실제로 confirm page 에 진입 → Toss Payments SDK widget iframe이 마운트되었음을 assert → page.route로 POST /api/v1/payments/confirm 를 200 응답으로 인터셉트하고 실제로 intercept 되었음을 assert (REVIEWS.md HIGH-01)"
    - "confirmIntercepted 값이 `true` 로 엄격 비교됨 (typeof boolean 만 검사하는 기존 smoke 아님)"
    - "Cancel / Decline 시나리오는 '기존 error URL params UI regression test' 로 명시되고, happy path가 DEBT-05 를 실제로 닫는 유일한 시나리오임이 objective에 기재됨"
    - "CI 워크플로우에서 TOSS_CLIENT_KEY_TEST 부재 감지 시: (a) fork PR 에서는 skip 허용, (b) main push / workflow_dispatch / schedule 에서는 verify-secrets step 이 exit 1로 빌드 실패 (REVIEWS.md HIGH-02)"
    - "TOSS_CLIENT_KEY_TEST / TOSS_SECRET_KEY_TEST 는 CI 전용 시크릿으로 ci.yml에만 주입되며 deploy.yml에는 **절대** 주입되지 않음 (D-13 격리)"
    - ".env.example 에 TOSS_CLIENT_KEY_TEST / TOSS_SECRET_KEY_TEST 플레이스홀더 추가"
    - "playwright.config.ts에 env 변수 전달 설정 추가 (webServer.env) — dev 서버가 NEXT_PUBLIC_TOSS_CLIENT_KEY 를 받도록"
    - "기존 social-login.spec.ts는 영향 없음 + 기존 CI job 순서 유지"
    - "Blocker B1: fixture + intercept payload 의 `seats` 는 `packages/shared/src/types/booking.types.ts` 의 `SeatSelection { seatId, tierName, price, row, number, tierColor? }` 형태만 사용 (`{id, label, price, grade}` 금지)"
    - "Blocker B2: happy path 는 새 helper `apps/web/e2e/helpers/auth.ts` 의 `loginAsTestUser(page)` 를 호출하여 실 `/api/v1/auth/login` 을 page.request 로 호출한 뒤 `refreshToken` httpOnly 쿠키를 `page.context()` 에 저장 — AuthGuard redirect (`/booking/[id]/complete` → `/auth/login`) 을 회피하고 confirmMutation 이 발동"
    - "TEST_USER_EMAIL / TEST_USER_PASSWORD env 가 설정되지 않은 경우 seed 기본값 (admin@grapit.test / TestAdmin2026!) 으로 fallback (seed.mjs L39-50 기반)"
  artifacts:
    - path: "apps/web/e2e/toss-payment.spec.ts"
      provides: "Toss Payments E2E test suite with env-gated skip + widget mount assertion + confirm API intercept assertion + auth helper integration"
      min_lines: 90
    - path: "apps/web/e2e/fixtures/booking-store.ts"
      provides: "Playwright fixture to inject booking store state via page.addInitScript (bypass /booking/:id redirect) with SeatSelection-shaped seats (Blocker B1)"
      exports: ["injectBookingFixture"]
    - path: "apps/web/e2e/helpers/auth.ts"
      provides: "Playwright auth helper — `loginAsTestUser(page)` performs request-fixture login against /api/v1/auth/login and seeds refreshToken cookie into page context (Blocker B2)"
      exports: ["loginAsTestUser"]
    - path: "apps/web/stores/use-booking-store.ts"
      provides: "Extended with dev/test-only `__BOOKING_FIXTURE__` hook that seeds booking state via setBookingData (Blocker B1: payload shape matches SeatSelection[])"
      contains: "__BOOKING_FIXTURE__"
    - path: ".github/workflows/ci.yml"
      provides: "CI job extended with secrets hard-gate + E2E step + TEST_USER_EMAIL/TEST_USER_PASSWORD injection"
      contains: "verify-toss-secrets"
    - path: ".env.example"
      provides: "Toss test key placeholders for local development + TEST_USER_* fallback (Blocker B2)"
      contains: "TOSS_CLIENT_KEY_TEST"
  key_links:
    - from: "apps/web/e2e/toss-payment.spec.ts happy path"
      to: "widget iframe mount assertion"
      via: "expect(page.locator('#payment-method iframe')).toBeVisible({ timeout: 20000 })"
      pattern: "#payment-method iframe"
    - from: "apps/web/e2e/toss-payment.spec.ts happy path"
      to: "confirmIntercepted strict assert"
      via: "expect(confirmIntercepted).toBe(true) 또는 expect.poll(() => confirmIntercepted).toBe(true)"
      pattern: "expect\\(confirmIntercepted\\)\\.toBe\\(true\\)|expect\\.poll.*confirmIntercepted.*toBe\\(true\\)"
    - from: "apps/web/e2e/toss-payment.spec.ts happy path top"
      to: "loginAsTestUser(page)"
      via: "await loginAsTestUser(page) before any navigation (Blocker B2: AuthGuard on /complete)"
      pattern: "loginAsTestUser\\(page\\)"
    - from: "apps/web/e2e/fixtures/booking-store.ts (injectBookingFixture args)"
      to: "SeatSelection shape"
      via: "seats: Array<{ seatId: string; tierName: string; price: number; row: string; number: string; tierColor?: string }> (Blocker B1)"
      pattern: "seatId.*tierName.*row.*number"
    - from: ".github/workflows/ci.yml"
      to: "secrets hard-gate"
      via: "verify-toss-secrets step: if event_name is push/schedule/workflow_dispatch AND secrets empty → exit 1"
      pattern: "verify-toss-secrets|Missing TOSS_CLIENT_KEY_TEST"
    - from: ".github/workflows/deploy.yml"
      to: "TOSS_CLIENT_KEY_TEST (절대 주입 금지)"
      via: "NEGATIVE — deploy.yml env_vars/secrets 에 TOSS_*_TEST 키 미존재 확인"
      pattern: "TOSS_CLIENT_KEY_TEST|TOSS_SECRET_KEY_TEST"
---

<objective>
Phase 9 기술부채 **6건 중 1건(DEBT-05)** 을 해소한다. v1.0에서 미검증 상태였던 Toss Payments 결제 플로우를 Playwright E2E로 자동 검증하여 "실 SDK + 실 sandbox 키" 로직이 regression 없이 동작함을 CI에서 매번 확인 가능한 상태로 만든다. (Phase 9 전체 범위는 DEBT-01~06 6건; "기술부채 12건" 은 본 phase 범위가 아님.)

**Purpose:**
- Toss Payments 공식 테스트 키 기반 E2E (D-10, D-11, D-14)
- 커버리지 계층화:
  - **Happy path (DEBT-05 closure 증거):** 실 reservation seed + 실제 confirm page 진입 + Toss widget iframe mount assert + `page.route` 로 confirm API intercept + `confirmIntercepted === true` strict assert. REVIEWS.md HIGH-01 이 요구한 기준.
  - **Cancel / Decline (UI regression test, 부가):** URL simulation (`?error=true&code=...`) — 이는 DEBT-05 closure 기준을 단독으로 충족하지 않으며, 실제 Toss sandbox flow를 대체하지 않음을 objective/코드 주석에 명시 (REVIEWS.md HIGH-01 후속 지적 반영).
- env-gated skip은 **오직 fork PR** 에 한정. main push / workflow_dispatch / schedule 은 secrets 누락 시 **빌드 실패** (REVIEWS.md HIGH-02).
- 테스트 키를 프로덕션 키와 완전 격리 (D-13) — CI secrets 전용 별칭 (`TOSS_CLIENT_KEY_TEST` / `TOSS_SECRET_KEY_TEST`)
- CI workflow에 E2E step + secrets gate step 추가하여 PR/main push 시 자동 검증

**REVIEWS.md 피드백 반영 (HIGH/MED/LOW):**
- **HIGH-01: Plan 3가 DEBT-05를 실제로 닫지 못함.** 해결:
  - `apps/web/e2e/fixtures/booking-store.ts` 에 `injectBookingFixture` 를 추가 — `page.addInitScript` 로 booking store 의 zustand state를 강제 주입하여 `/booking/:id` redirect (confirm/page.tsx:62-66) 를 우회. 실제 confirm page 가 mount 함.
  - Happy path는 실 Toss SDK 로드 → `#payment-method iframe` 가 20초 내 가시 상태 되었음 assert. `widgetReady` state 도 `page.getByText('결제하기')` 버튼이 enabled 되는 것으로 간접 검증.
  - confirm API 인터셉션: `page.route('**/api/v1/payments/confirm', async route => { confirmIntercepted = true; await route.fulfill({status:200, body: ...}); })` — Toss sandbox가 successUrl 로 리다이렉트하면 `/booking/:id/complete` 가 confirm API를 호출 → intercept → `confirmIntercepted === true` 로 strict assert.
  - Note on flow 완전 자동화: Toss sandbox iframe 내부 카드 입력은 cross-origin (Pitfall 3) — 여기서는 SDK 로딩 + widget mount 까지만 E2E 범위. successUrl 로 직접 navigate 하여 confirm API 호출 path를 발동 시키는 방식 (hybrid D-14).
- **HIGH-01 (후속): Cancel/Decline의 한계 명시.** 두 시나리오는 `page.goto('/booking/.../confirm?error=true&code=PAY_PROCESS_CANCELED')` 로 URL만 흉내내므로 실 Toss flow 를 재현하지 않음. Plan 3 objective + spec 파일 주석 모두에 "UI regression test only — not a replacement for real sandbox flow" 라고 명시.
- **HIGH-02: CI가 main push에서 secrets 누락 시 silent skip.** 해결:
  - `ci.yml`에 `verify-toss-secrets` step 을 E2E step 앞에 삽입.
  - Gate 조건: `github.event_name in (push, schedule, workflow_dispatch)` AND `secrets.TOSS_CLIENT_KEY_TEST == ''` → echo 후 `exit 1`.
  - `pull_request` 는 fork 여부 분기: `github.event.pull_request.head.repo.full_name != github.repository` (= fork) 이면 skip 허용, same-repo PR 은 hard-require.
- **LOW: 12건 vs 6건.** Plan 3 objective 첫 줄에 "6건 중 1건(DEBT-05)" 명시.
- **MED: grep -c 체이닝 버그.** Plan 3 의 모든 `<acceptance_criteria>` 는 expected-absence 를 `! grep -q` 또는 `test "$(grep -c ... | tr -d ' ')" = "0"` 로 작성.

**Revision-2 Blocker 반영 (이 revision iteration 의 핵심 수정):**
- **Blocker B1: `seats` 타입 불일치 해결.** 기존 planner draft 는 `seats: [{ id, label, price, grade }]` 를 사용했으나, `packages/shared/src/types/booking.types.ts:30-37` 의 `SeatSelection` 인터페이스는 `{ seatId: string; tierName: string; tierColor?: string; price: number; row: string; number: string }` 임. `useBookingStore.setBookingData(...)` 의 `selectedSeats` 파라미터가 `SeatSelection[]` 타입이므로 **fixture payload 와 intercept response (mocked ReservationDetail) 양쪽을 전부 SeatSelection shape 으로 교체**. `ReservationDetail` 는 `ReservationListItem` 을 상속하여 `seats: SeatSelection[]` 을 가지므로 intercept body 도 동일 shape 필요.
- **Blocker B2: happy path auth cookie TODO 제거.** `/booking/[id]/complete` 는 AuthGuard 에 보호됨 — 인증이 없으면 `/auth` 로 redirect 되어 confirmMutation 이 실행되지 않고 `confirmIntercepted` 가 영원히 false 로 남음. 해결:
  - 신규 helper `apps/web/e2e/helpers/auth.ts` 에 `loginAsTestUser(page: Page): Promise<void>` 추가. 내부에서 `page.request.post('/api/v1/auth/login', { data: { email, password } })` 호출 후, 응답의 `Set-Cookie: refreshToken=...` 을 `page.context().addCookies([...])` 로 저장.
  - 로그인 성공 시 accessToken 은 response body 에서 추출하여 `page.addInitScript` 로 `localStorage.setItem('auth-storage', ...)` 또는 auth store 에 직접 주입 (auth-store 구현 확인 필요 — `apps/web/stores/use-auth-store.ts` 참조).
  - happy path 시작에서 `await loginAsTestUser(page)` 호출 후 fixture/navigate.
  - env: `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` 가 설정된 경우 해당 계정, 미설정 시 seed 기본값 (`admin@grapit.test` / `TestAdmin2026!` from seed.mjs L40+L49) 으로 fallback.
- **W1: Task 5 `<files>` 공란.** Task 5 는 verification-only 이지만 결과를 `.planning/phases/09-tech-debt/09-03-SUMMARY.md` 에 기록 — 이를 deliverable 로 명시하여 `<files>` 에 추가.

**CRITICAL 재프레이밍 (RESEARCH.md 반영):**
- **새 키 발급 불필요.** `.env`에 이미 `NEXT_PUBLIC_TOSS_CLIENT_KEY=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm` + `TOSS_SECRET_KEY=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6` 존재. Plan 3은 동일 키 값을 `TOSS_CLIENT_KEY_TEST` / `TOSS_SECRET_KEY_TEST` 별칭으로 **CI에만 등록**.
- **Toss sandbox iframe cross-origin.** 결제창 내부 카드 입력 필드는 Playwright로 접근 불가 (PCI 규정). D-14가 허용한 하이브리드 접근 — 실 SDK 로딩/위젯 마운트까지는 실 sandbox 사용, confirm API 응답은 `page.route()` 로 인터셉트 (RESEARCH §Pitfall 3, §Pattern 6 후반부).
- **deploy.yml 절대 불가침.** `TOSS_CLIENT_KEY_TEST` / `TOSS_SECRET_KEY_TEST` 는 Cloud Run에 주입하지 말 것 (D-13 격리, RESEARCH §Security Domain T-09-03).
- **Playwright config 수정 필요.** 기존 `webServer.command: 'pnpm dev'` 는 process.env 를 상속하므로 `NEXT_PUBLIC_TOSS_CLIENT_KEY` 가 셸에 있어야 SDK 로딩이 성공. config에 `webServer.env` 를 명시하여 CI/로컬 양쪽에서 확실하게 주입.

**Dependency on Plan 02:** Plan 02 완료 (EmailService + Legal MD + signup-step2 wiring + typecheck/lint/build green) 후 시작. 본 Plan은 결제 페이지(`/booking/:id/confirm`) 만 다루므로 signup/auth 변경사항과 독립적이지만 CI workflow 수정이 Plan 02의 deploy.yml 변경 이후에 이뤄져야 merge conflict 회피.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt/09-CONTEXT.md
@.planning/phases/09-tech-debt/09-RESEARCH.md
@.planning/phases/09-tech-debt/09-UI-SPEC.md
@.planning/phases/09-tech-debt/09-VALIDATION.md
@.planning/phases/09-tech-debt/09-REVIEWS.md
@.planning/phases/09-tech-debt/09-01-PLAN.md
@.planning/phases/09-tech-debt/09-02-PLAN.md

# E2E 참조
@apps/web/playwright.config.ts
@apps/web/e2e/social-login.spec.ts

# 결제 페이지 구조 참조 (redirect + SDK 로드 + confirm API 호출 경로)
@apps/web/app/booking/[performanceId]/confirm/page.tsx
@apps/web/app/booking/[performanceId]/complete/page.tsx
@apps/web/components/booking/toss-payment-widget.tsx
@apps/web/stores/use-booking-store.ts
@apps/web/hooks/use-booking.ts

# Auth/login 참조 (Blocker B2)
@apps/web/components/auth/login-form.tsx
@apps/web/app/auth/page.tsx
@apps/web/stores/use-auth-store.ts
@apps/api/src/modules/auth/auth.controller.ts
@packages/shared/src/constants/index.ts

# SeatSelection 타입 참조 (Blocker B1)
@packages/shared/src/types/booking.types.ts

# Seed 참조 (admin@grapit.test)
@apps/api/src/database/seed.mjs

# CI/CD 참조
@.github/workflows/ci.yml
@.github/workflows/deploy.yml

<interfaces>
<!-- Key flow paths executor must respect -->

**Confirm page redirect trigger (apps/web/app/booking/[performanceId]/confirm/page.tsx:62-66):**
```typescript
useEffect(() => {
  if (selectedSeats.length === 0) {
    router.replace(`/booking/${performanceId}`);
  }
}, [selectedSeats.length, performanceId, router]);
```
→ E2E는 `selectedSeats.length > 0` 상태로 접속해야 page가 유지됨. `page.addInitScript` 로 zustand state를 초기 주입.

**SeatSelection type (packages/shared/src/types/booking.types.ts:30-37) — Blocker B1 truth source:**
```typescript
export interface SeatSelection {
  seatId: string;      // NOT `id`
  tierName: string;    // NOT `grade`
  tierColor?: string;  // optional
  price: number;
  row: string;         // required — e.g. 'A'
  number: string;      // required — e.g. '1' (stringified to match DB)
}
```
→ fixture `seats` payload + `setBookingData.selectedSeats` 인자 + intercept body `seats` 필드 모두 이 shape 준수. `{ id, label, price, grade }` 는 **절대 사용 금지**.

**useBookingStore.setBookingData signature (apps/web/stores/use-booking-store.ts:30-39) — Blocker B1 receiver:**
```typescript
setBookingData: (data: {
  selectedSeats: SeatSelection[];
  showtimeId: string | null;
  performanceId: string | null;
  performanceTitle: string | null;
  showDateTime: string | null;
  venue: string | null;
  posterUrl: string | null;
  expiresAt: number | null;
}) => void;
```
→ Task 2 의 fixture hook 에서 이 형태로 호출.

**AuthGuard / login path (Blocker B2):**
- `apps/web/components/auth/login-form.tsx:73`:
  ```typescript
  const res = await apiClient.post<AuthResponse>('/api/v1/auth/login', data);
  // res = { accessToken, user }
  // response sets httpOnly `refreshToken` cookie (7 days)
  ```
- Cookie name: `packages/shared/src/constants/index.ts:1` → `export const AUTH_COOKIE_NAME = 'refreshToken';`
- Cookie options: `httpOnly: true, secure: true, sameSite: 'none', path: '/', maxAge: 7days` (auth.controller.ts:249-257)
- `/booking/[id]/complete` 가 AuthGuard 를 사용한다면 이 refreshToken cookie + accessToken (client store) 이 필요. **executor 는 `apps/web/stores/use-auth-store.ts` 와 `/booking/[id]/complete/page.tsx` 를 먼저 읽고 accessToken 주입 방식을 확정한 뒤 helper 구현.**

**Toss widget DOM anchor (apps/web/components/booking/toss-payment-widget.tsx:133-141):**
```tsx
<div id="payment-method" aria-label="결제 수단 선택" className={isLoading ? 'hidden' : ''} />
<div id="agreement" className={isLoading ? 'hidden' : ''} />
```
→ widget mount 검증은 `page.locator('#payment-method iframe')` 으로 가능. Toss SDK가 이 DOM 에 iframe을 주입.

**Confirm API call path (apps/web/app/booking/[performanceId]/complete/page.tsx:71-79):**
```typescript
const result = await confirmMutation.mutateAsync({ paymentKey, orderId, amount: Number(amount) });
```
→ `confirmMutation` 은 POST `/api/v1/payments/confirm` 을 호출. E2E는 이 URL을 `page.route` 로 가로채 `ReservationDetail`-shaped body 로 응답.

**ReservationDetail shape (packages/shared/src/types/booking.types.ts:52-59):**
```typescript
interface ReservationDetail extends ReservationListItem {
  paymentMethod: string;
  paidAt: string;
  cancelDeadline: string;
  cancelledAt: string | null;
  cancelReason: string | null;
  paymentKey: string;
}
// ReservationListItem (L39-50):
interface ReservationListItem {
  id: string;
  reservationNumber: string;
  status: ReservationStatus;
  performanceTitle: string;
  posterUrl: string | null;
  showDateTime: string;
  venue: string;
  seats: SeatSelection[];   // Blocker B1: SeatSelection[], not {id, label, price, grade}[]
  totalAmount: number;
  createdAt: string;
}
```
→ intercept `route.fulfill({ body: ... })` 의 JSON 은 ReservationDetail 전체 필드를 포함하거나, 최소한 `/complete` page 가 읽는 필드만 포함 (coverage 는 complete/page.tsx 참조).

**Successful Toss flow after `requestPayment()` call:**
- sandbox SDK → Toss server → redirect to `successUrl` = `${origin}/booking/${performanceId}/complete?paymentKey=...&orderId=...&amount=...`
- complete page → AuthGuard check (→ loginAsTestUser 로 pre-seeded) → confirmMutation → POST /api/v1/payments/confirm
→ intercept가 이 POST 시점에 발동.

**Test-only booking fixture injection pattern (apps/web/e2e/fixtures/booking-store.ts — Blocker B1 corrected):**
```typescript
import type { Page } from '@playwright/test';
import type { SeatSelection } from '@grapit/shared';  // Blocker B1

/**
 * Inject minimal booking state into the zustand store before navigation so the
 * confirm page doesn't redirect to /booking/:id. Run BEFORE page.goto.
 *
 * seats shape MUST match SeatSelection (Blocker B1 — booking.types.ts:30-37).
 */
export async function injectBookingFixture(page: Page, args: {
  performanceId: string;
  showtimeId: string;
  seats: SeatSelection[];   // Blocker B1: { seatId, tierName, price, row, number, tierColor? }
  performanceTitle: string;
  showDateTime: string;
  venue: string;
  posterUrl?: string;
}) {
  await page.addInitScript((fixtureArgs) => {
    (window as unknown as { __BOOKING_FIXTURE__: typeof fixtureArgs }).__BOOKING_FIXTURE__ = fixtureArgs;
  }, args);
}
```

**Auth helper skeleton (apps/web/e2e/helpers/auth.ts — Blocker B2):**
```typescript
import type { Page } from '@playwright/test';

interface LoginResponse {
  accessToken: string;
  user: { id: string; email: string; name: string; role: 'user' | 'admin' };
}

/**
 * Log in the test user against the real /api/v1/auth/login endpoint via page.request,
 * then copy the resulting `refreshToken` cookie into page.context() and inject the
 * accessToken into localStorage so AuthGuard-protected pages do not redirect.
 *
 * Env: TEST_USER_EMAIL / TEST_USER_PASSWORD (fall back to seed.mjs defaults).
 * Blocker B2: replaces the empty `page.context().addCookies([])` TODO that left
 * confirmIntercepted stuck at false because /booking/:id/complete redirected to /auth.
 */
export async function loginAsTestUser(page: Page): Promise<void> {
  const email = process.env['TEST_USER_EMAIL'] ?? 'admin@grapit.test';
  const password = process.env['TEST_USER_PASSWORD'] ?? 'TestAdmin2026!';

  // 1. Hit the real login endpoint via Playwright's request fixture.
  const res = await page.request.post('/api/v1/auth/login', {
    data: { email, password },
    headers: { 'Content-Type': 'application/json' },
  });
  if (!res.ok()) {
    throw new Error(
      `[loginAsTestUser] Login failed: ${res.status()} ${res.statusText()}. ` +
      `Check DB is seeded (pnpm --filter @grapit/api seed) and TEST_USER_* env match.`,
    );
  }
  const body = (await res.json()) as LoginResponse;

  // 2. refreshToken cookie was set via Set-Cookie header — Playwright's request
  //    fixture keeps it in the storageState of the same context automatically when
  //    `page.request` shares the browser context. Verify it landed:
  const cookies = await page.context().cookies();
  if (!cookies.find((c) => c.name === 'refreshToken')) {
    throw new Error('[loginAsTestUser] refreshToken cookie missing after login.');
  }

  // 3. Inject accessToken into auth store (client-side) so AuthGuard sees auth state
  //    before first navigation. Read use-auth-store.ts to confirm the localStorage key
  //    (e.g. 'auth-storage' with { state: { accessToken, user } } if using zustand persist).
  await page.addInitScript(({ accessToken, user }) => {
    // NOTE: adjust the key below to match the actual zustand persist name in
    // apps/web/stores/use-auth-store.ts (likely 'auth-store' or 'auth-storage').
    const storageValue = JSON.stringify({
      state: { accessToken, user, isAuthenticated: true },
      version: 0,
    });
    window.localStorage.setItem('auth-store', storageValue);
  }, { accessToken: body.accessToken, user: body.user });
}
```
→ executor 는 실제 zustand persist key 를 `apps/web/stores/use-auth-store.ts` 에서 확인한 뒤 storageKey 를 조정. 확인 방법:
```bash
grep -nE "persist\(|createJSONStorage|name:\s*'" apps/web/stores/use-auth-store.ts
```

**Fixture hook in use-booking-store.ts (Task 2 에서 추가 — Blocker B1 corrected):**
```typescript
// At the bottom of useBookingStore definition, after create(...):
if (typeof window !== 'undefined' && process.env.NODE_ENV !== 'production') {
  queueMicrotask(() => {
    const fixture = (window as unknown as {
      __BOOKING_FIXTURE__?: {
        performanceId: string;
        showtimeId: string;
        seats: SeatSelection[];   // Blocker B1
        performanceTitle: string;
        showDateTime: string;
        venue: string;
        posterUrl?: string;
      };
    }).__BOOKING_FIXTURE__;
    if (fixture) {
      useBookingStore.getState().setBookingData({
        selectedSeats: fixture.seats,
        showtimeId: fixture.showtimeId,
        performanceId: fixture.performanceId,
        performanceTitle: fixture.performanceTitle,
        showDateTime: fixture.showDateTime,
        venue: fixture.venue,
        posterUrl: fixture.posterUrl ?? null,
        expiresAt: Date.now() + 10 * 60 * 1000,
      });
    }
  });
}
```

**Alternative approach (Option B) — without store modification:**
- Playwright `page.evaluate(() => { /* call setBookingData via window-exposed store */ })` after DOM ready.
- Requires exposing store on `window` in dev mode.
- Heavier but avoids production-only code gate.
- Executor's choice; Option A is recommended.

Target skeleton for `apps/web/e2e/toss-payment.spec.ts` (REVIEWS.md HIGH-01 + Blocker B1 + Blocker B2 반영):
```typescript
import { test, expect, type Route } from '@playwright/test';
import { injectBookingFixture } from './fixtures/booking-store';
import { loginAsTestUser } from './helpers/auth';

test.describe('Toss Payments E2E', () => {
  test.skip(
    !process.env['TOSS_CLIENT_KEY_TEST'],
    'Skipped: TOSS_CLIENT_KEY_TEST not set. CI main-push gate should have failed before reaching this.',
  );

  // ---- Scenario 1: HAPPY PATH (DEBT-05 closure evidence per REVIEWS.md HIGH-01) ----
  test('happy path: widget mounts AND confirm API is intercepted when complete page lands', async ({ page }) => {
    let confirmIntercepted = false;

    // Blocker B2: pre-seed auth so /complete does not redirect to /auth.
    await loginAsTestUser(page);

    // 1. Register intercept BEFORE any navigation
    await page.route('**/api/v1/payments/confirm', async (route: Route) => {
      confirmIntercepted = true;
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          id: 'test-reservation-id',
          reservationNumber: 'GRP-TEST-0001',
          status: 'CONFIRMED',
          performanceTitle: 'E2E Test Performance',
          posterUrl: null,
          showDateTime: new Date(Date.now() + 86400000).toISOString(),
          venue: 'E2E Venue',
          // Blocker B1: SeatSelection shape
          seats: [{ seatId: 's1', tierName: 'VIP', price: 50000, row: 'A', number: '1' }],
          totalAmount: 50000,
          createdAt: new Date().toISOString(),
          paymentMethod: 'card',
          paidAt: new Date().toISOString(),
          cancelDeadline: new Date(Date.now() + 86400000).toISOString(),
          cancelledAt: null,
          cancelReason: null,
          paymentKey: 'test_payment_key',
        }),
      });
    });

    // 2. Inject booking state so confirm page doesn't redirect
    await injectBookingFixture(page, {
      performanceId: 'e2e-test-performance',
      showtimeId: 'e2e-test-showtime',
      // Blocker B1: SeatSelection shape
      seats: [{ seatId: 's1', tierName: 'VIP', price: 50000, row: 'A', number: '1' }],
      performanceTitle: 'E2E Test Performance',
      showDateTime: new Date(Date.now() + 86400000).toISOString(),
      venue: 'E2E Venue',
    });

    // 3. Navigate to confirm page
    await page.goto('/booking/e2e-test-performance/confirm');

    // 4. Assert confirm page rendered (NOT redirected to /booking/:id or /auth)
    await expect(page).toHaveURL(/\/confirm/);

    // 5. Assert Toss widget iframe mounted — this proves real SDK loaded
    await expect(page.locator('#payment-method iframe')).toBeVisible({ timeout: 20000 });

    // 6. Simulate Toss success redirect: navigate complete page with mocked Toss params.
    //    This triggers the complete page's confirmMutation, which hits /api/v1/payments/confirm.
    const paymentKey = 'test_payment_key_' + Date.now();
    const orderId = 'test_order_' + Date.now();
    const amount = '50000';
    await page.goto(
      `/booking/e2e-test-performance/complete?paymentKey=${paymentKey}&orderId=${orderId}&amount=${amount}`,
    );

    // 7. Assert confirm API was called (strict true — REVIEWS.md HIGH-01)
    await expect.poll(() => confirmIntercepted, { timeout: 10000 }).toBe(true);

    // 8. Assert complete page renders success state
    await expect(page.getByText(/예매가 완료|완료되었습니다/)).toBeVisible({ timeout: 10000 });
  });

  // ---- Scenario 2: UI REGRESSION (cancel) — NOT a replacement for real Toss flow ----
  test('UI regression: cancel error URL → toast renders', async ({ page }) => {
    // NOTE: This test validates the confirm page's URL-param branch (PAY_PROCESS_CANCELED).
    // It does NOT replace the happy-path real-SDK test; it catches UI regressions only.
    await page.goto('/booking/e2e-test-performance/confirm?error=true&code=PAY_PROCESS_CANCELED');
    await expect(page.getByText('결제가 취소되었습니다.')).toBeVisible({ timeout: 5000 });
  });

  // ---- Scenario 3: UI REGRESSION (decline) — NOT a replacement for real Toss flow ----
  test('UI regression: decline error URL → message renders', async ({ page }) => {
    // NOTE: Same disclaimer as Scenario 2 — URL simulation only.
    const msg = '카드 승인 거절';
    await page.goto(
      `/booking/e2e-test-performance/confirm?error=true&code=INVALID_CARD&message=${encodeURIComponent(msg)}`,
    );
    await expect(page.getByText(/카드 승인 거절|결제에 실패/)).toBeVisible({ timeout: 5000 });
  });
});
```

Target ci.yml delta (secrets gate + E2E step + test user env, REVIEWS.md HIGH-02 + Blocker B2 반영):
```yaml
      - run: pnpm test

      # REVIEWS.md HIGH-02: verify Toss test secrets on main push / schedule / manual dispatch
      - name: Verify Toss test secrets present (hard-gate on non-fork events)
        if: |
          github.event_name == 'push' ||
          github.event_name == 'schedule' ||
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository)
        env:
          TOSS_CLIENT_KEY_TEST: ${{ secrets.TOSS_CLIENT_KEY_TEST }}
          TOSS_SECRET_KEY_TEST: ${{ secrets.TOSS_SECRET_KEY_TEST }}
        run: |
          if [ -z "$TOSS_CLIENT_KEY_TEST" ] || [ -z "$TOSS_SECRET_KEY_TEST" ]; then
            echo "Missing TOSS_CLIENT_KEY_TEST or TOSS_SECRET_KEY_TEST in repo secrets."
            echo "Register via: gh secret set TOSS_CLIENT_KEY_TEST"
            echo "See .planning/phases/09-tech-debt/09-CONTEXT.md D-13."
            exit 1
          fi
          echo "Toss test secrets present."

      - name: Install Playwright browsers
        run: pnpm --filter @grapit/web exec playwright install --with-deps chromium

      - name: E2E tests (Toss Payments)
        env:
          TOSS_CLIENT_KEY_TEST: ${{ secrets.TOSS_CLIENT_KEY_TEST }}
          TOSS_SECRET_KEY_TEST: ${{ secrets.TOSS_SECRET_KEY_TEST }}
          NEXT_PUBLIC_TOSS_CLIENT_KEY: ${{ secrets.TOSS_CLIENT_KEY_TEST }}
          # Blocker B2: test user credentials — falls back to seed defaults if unset in spec
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
        run: pnpm --filter @grapit/web test:e2e
```

Target `.env.example` delta (Blocker B2 — TEST_USER_* fallback documented):
```bash
# -- Toss Payments (E2E test-only — CI-only keys, NEVER in deploy.yml) --
# Public docs test keys from Toss documentation; safe to commit (no real funds).
# Used exclusively by Playwright E2E. Key isolation: see 09-CONTEXT.md D-13.
TOSS_CLIENT_KEY_TEST=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm
TOSS_SECRET_KEY_TEST=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6

# -- Playwright E2E test user (Blocker B2) --
# Local: fall back to seed defaults (admin@grapit.test / TestAdmin2026!) — set only to override.
# CI: must be set in GitHub secrets if the seed credentials differ between envs.
# TEST_USER_EMAIL=admin@grapit.test
# TEST_USER_PASSWORD=TestAdmin2026!
```

Target `apps/web/playwright.config.ts` delta (webServer.env 추가):
```typescript
webServer: {
  command: 'pnpm dev',
  url: 'http://localhost:3000',
  reuseExistingServer: !process.env['CI'],
  env: {
    // Propagate Toss test key to the dev server so the Toss SDK client can initialize.
    NEXT_PUBLIC_TOSS_CLIENT_KEY: process.env['TOSS_CLIENT_KEY_TEST'] ?? process.env['NEXT_PUBLIC_TOSS_CLIENT_KEY'] ?? '',
  },
},
```

**CRITICAL negative check:** `.github/workflows/deploy.yml` MUST NOT contain `TOSS_CLIENT_KEY_TEST` or `TOSS_SECRET_KEY_TEST` anywhere (env_vars OR secrets block).
</interfaces>
</context>

<tasks>

<task id="9-03-t0-e2e-fixture-and-spec" type="auto" tdd="true">
  <name>Task 0 (Wave 0 — TDD): Playwright Toss E2E spec + booking fixture + auth helper 작성 (RED: widget mount 미검증, confirmIntercepted strict assert, Blocker B1+B2 반영)</name>
  <files>
    apps/web/e2e/toss-payment.spec.ts,
    apps/web/e2e/fixtures/booking-store.ts,
    apps/web/e2e/helpers/auth.ts
  </files>
  <read_first>
    - apps/web/e2e/social-login.spec.ts (기존 E2E 패턴 — page.goto + getByText 조합)
    - apps/web/playwright.config.ts (기존 설정 — baseURL 3000, webServer auto)
    - apps/web/app/booking/[performanceId]/confirm/page.tsx (L60-90: redirect 조건 + error URL 파라미터 처리 로직)
    - apps/web/app/booking/[performanceId]/complete/page.tsx (L63-79: confirmMutation 호출 — intercept 대상. AuthGuard 존재 여부 확인)
    - apps/web/components/booking/toss-payment-widget.tsx (L22-95: SDK 로딩 로직, L133-141 DOM anchor)
    - apps/web/stores/use-booking-store.ts (L1-120: zustand create / setBookingData. Blocker B1: L30-39 setBookingData signature → selectedSeats: SeatSelection[])
    - apps/web/stores/use-auth-store.ts (Blocker B2: zustand persist key 확인 — localStorage key 이름 필수)
    - apps/web/hooks/use-booking.ts (useConfirmPayment — /payments/confirm 호출)
    - apps/web/components/auth/login-form.tsx (Blocker B2: L68-73 login payload + response shape)
    - apps/api/src/modules/auth/auth.controller.ts (Blocker B2: L68-79 login endpoint + L249-257 refreshToken cookie)
    - packages/shared/src/types/booking.types.ts (Blocker B1: L30-37 SeatSelection + L52-59 ReservationDetail)
    - packages/shared/src/constants/index.ts (Blocker B2: AUTH_COOKIE_NAME = 'refreshToken')
    - apps/api/src/database/seed.mjs (Blocker B2: L39-50 admin@grapit.test / TestAdmin2026! fallback)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Pattern 6 + §Pitfall 3 cross-origin + §Example 4 env-gated skip)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-01: widget mount + confirmIntercepted strict)
  </read_first>
  <behavior>
    - Test 1 (happy path — **DEBT-05 closure**):
      - Preconditions:
        - (Blocker B2) `loginAsTestUser(page)` 호출 — 실 /api/v1/auth/login POST → refreshToken cookie 저장 + accessToken localStorage 주입
        - booking fixture 주입 (Blocker B1: SeatSelection shape)
      - Act 1: confirm page로 navigate
      - Assert A: URL이 `/confirm` 경로를 포함 (redirect 안됨 — selectedSeats > 0 + 로그인 상태 확인)
      - Assert B: `#payment-method iframe` visible within 20s (실 Toss SDK 로드 증거)
      - Act 2: complete page 로 navigate with mocked Toss query params (paymentKey/orderId/amount)
      - Assert C: `/api/v1/payments/confirm` 이 intercept 되었음 (`confirmIntercepted` strict === true, expect.poll 사용)
      - Assert D: complete page 가 "예매가 완료" 메시지 렌더
    - Test 2 (UI regression, cancel): URL 주입 → "결제가 취소되었습니다." 렌더
    - Test 3 (UI regression, decline): URL 주입 → "카드 승인 거절" 또는 "결제에 실패" 렌더
    - Test skip behavior: `TOSS_CLIENT_KEY_TEST` 미설정 시 전체 describe block 이 skip 되어 테스트 실행 시간 0초, exit 0. CI main push 에서는 별도 verify-secrets step 이 먼저 fail 시키므로 여기까지 오지 않음.
    - auth helper behavior (Blocker B2):
      - `loginAsTestUser` 가 email/password 를 env 또는 기본값에서 읽음
      - `page.request.post('/api/v1/auth/login')` → 200 검증
      - response body 에서 accessToken 추출 → addInitScript 로 localStorage 주입
      - `page.context().cookies()` 에서 `refreshToken` 존재 확인 후 throw if missing
  </behavior>
  <action>
**Step 1.** `apps/web/e2e/fixtures/booking-store.ts` 생성 (Blocker B1: SeatSelection shape):

```typescript
import type { Page } from '@playwright/test';
import type { SeatSelection } from '@grapit/shared';

/**
 * Inject minimal booking store state into the window before page navigation so the
 * confirm page does not redirect to /booking/:id (see confirm/page.tsx:62-66).
 *
 * Run BEFORE `page.goto('/booking/.../confirm')`.
 *
 * The companion hook in `apps/web/stores/use-booking-store.ts` reads
 * `window.__BOOKING_FIXTURE__` once on client init (dev/test only) and seeds the
 * store via `setBookingData`.
 *
 * Blocker B1 (revision-2): seats shape MUST match SeatSelection from
 * packages/shared/src/types/booking.types.ts:30-37 — { seatId, tierName, price,
 * row, number, tierColor? } — NOT the older { id, label, price, grade } form.
 */
export async function injectBookingFixture(
  page: Page,
  args: {
    performanceId: string;
    showtimeId: string;
    seats: SeatSelection[];
    performanceTitle: string;
    showDateTime: string;
    venue: string;
    posterUrl?: string;
  },
): Promise<void> {
  await page.addInitScript((fixtureArgs) => {
    (window as unknown as { __BOOKING_FIXTURE__: typeof fixtureArgs }).__BOOKING_FIXTURE__ =
      fixtureArgs;
  }, args);
}
```

**Step 2 (Blocker B2).** `apps/web/e2e/helpers/auth.ts` 생성:

```typescript
import type { Page } from '@playwright/test';

interface LoginResponse {
  accessToken: string;
  user: { id: string; email: string; name: string; role: 'user' | 'admin' };
}

/**
 * Log the test user in via the real POST /api/v1/auth/login endpoint and propagate
 * both the httpOnly refreshToken cookie (via page.context) and the accessToken (via
 * localStorage addInitScript) so that AuthGuard-protected pages do not redirect.
 *
 * Env: TEST_USER_EMAIL / TEST_USER_PASSWORD. If unset, falls back to the seed
 * credentials from apps/api/src/database/seed.mjs:39-50.
 *
 * Blocker B2 (revision-2): previous implementation left page.context().addCookies([])
 * empty, which caused /booking/[id]/complete → /auth redirect and permanently stuck
 * `confirmIntercepted` at false.
 *
 * NOTE: the localStorage key below MUST match the zustand persist `name` in
 * apps/web/stores/use-auth-store.ts. Executor: read that file first and adjust
 * `AUTH_STORE_KEY` below. Grep hint:
 *   grep -nE "persist\\(|createJSONStorage|name:\\s*'" apps/web/stores/use-auth-store.ts
 */
const AUTH_STORE_KEY = 'auth-store';  // adjust to match zustand persist name

export async function loginAsTestUser(page: Page): Promise<void> {
  const email = process.env['TEST_USER_EMAIL'] ?? 'admin@grapit.test';
  const password = process.env['TEST_USER_PASSWORD'] ?? 'TestAdmin2026!';

  // 1. Hit the real login endpoint.
  const res = await page.request.post('/api/v1/auth/login', {
    data: { email, password },
    headers: { 'Content-Type': 'application/json' },
  });
  if (!res.ok()) {
    throw new Error(
      `[loginAsTestUser] Login failed: ${res.status()} ${res.statusText()}. ` +
        `Run 'pnpm --filter @grapit/api seed' and ensure TEST_USER_* env matches seed.mjs:39-50.`,
    );
  }
  const body = (await res.json()) as LoginResponse;

  // 2. Verify refreshToken cookie landed in the shared browser context.
  const cookies = await page.context().cookies();
  const refresh = cookies.find((c) => c.name === 'refreshToken');
  if (!refresh) {
    throw new Error(
      '[loginAsTestUser] refreshToken cookie missing after login — check auth.controller.ts setRefreshTokenCookie.',
    );
  }

  // 3. Seed accessToken into the auth store via addInitScript so client-side
  //    AuthGuard sees an authenticated state before the first render.
  await page.addInitScript(
    ({ accessToken, user, key }) => {
      const persisted = JSON.stringify({
        state: { accessToken, user, isAuthenticated: true },
        version: 0,
      });
      window.localStorage.setItem(key, persisted);
    },
    { accessToken: body.accessToken, user: body.user, key: AUTH_STORE_KEY },
  );
}
```

**Executor decision points for auth helper (Blocker B2 clarifications):**
- If `apps/web/stores/use-auth-store.ts` does NOT use zustand persist, the localStorage injection is unnecessary — delete step 3 and rely on a React Context bootstrap if the store reads from cookies.
- If persist `name` differs from `'auth-store'`, update `AUTH_STORE_KEY`.
- If AuthGuard only checks the refreshToken cookie (not accessToken in client state), step 3 can be removed — but typically the client store is also checked.
- If `/booking/[id]/complete` is NOT behind AuthGuard (check with `grep -n 'AuthGuard\\|requireAuth\\|useAuthStore' apps/web/app/booking/\\[performanceId\\]/complete/page.tsx`), `loginAsTestUser` can be a no-op — but executor MUST verify by rendering /complete anonymously in local run; if it redirects to `/auth`, the helper is required.

**Step 3.** `apps/web/e2e/toss-payment.spec.ts` 생성:

```typescript
import { test, expect, type Route } from '@playwright/test';
import { injectBookingFixture } from './fixtures/booking-store';
import { loginAsTestUser } from './helpers/auth';

/**
 * Toss Payments E2E tests.
 *
 * Phase 9 DEBT-05: Validates the payment flow end-to-end.
 * Uses the hybrid approach (D-14) — real SDK loading + `page.route()` interception
 * of the final confirm API call — because Toss sandbox iframes are cross-origin
 * and cannot be automated via Playwright `frameLocator` (RESEARCH §Pitfall 3).
 *
 * Scenario 1 (happy path) is the single source of DEBT-05 closure evidence.
 * Scenarios 2 and 3 are UI regression tests only — they do not substitute for
 * real Toss sandbox flow (REVIEWS.md HIGH-01).
 *
 * Env gate (D-12): Skips entirely when `TOSS_CLIENT_KEY_TEST` is not set.
 * CI main-push runs fail earlier via the `verify-toss-secrets` step, so this
 * skip only applies to fork PRs or local runs without a .env setup.
 *
 * Key isolation (D-13): `TOSS_CLIENT_KEY_TEST` / `TOSS_SECRET_KEY_TEST` are
 * CI-only aliases; they MUST NOT appear in `deploy.yml`.
 *
 * Blocker B1 (revision-2): fixture/intercept `seats` use SeatSelection shape.
 * Blocker B2 (revision-2): loginAsTestUser seeds auth state before navigation.
 */
test.describe('Toss Payments E2E', () => {
  test.skip(
    !process.env['TOSS_CLIENT_KEY_TEST'],
    'Skipped: TOSS_CLIENT_KEY_TEST not set. CI main-push gate should have failed before reaching this.',
  );

  // ============================================================================
  // Scenario 1: HAPPY PATH — DEBT-05 closure evidence (REVIEWS.md HIGH-01)
  // ============================================================================
  test('happy path: widget mounts AND confirm API intercepts on complete page', async ({ page }) => {
    let confirmIntercepted = false;

    // 0. Blocker B2: seed auth so /complete does not redirect to /auth.
    await loginAsTestUser(page);

    // 1. Register intercept BEFORE any navigation so complete page's POST is caught.
    await page.route('**/api/v1/payments/confirm', async (route: Route) => {
      confirmIntercepted = true;
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          id: 'test-reservation-id',
          reservationNumber: 'GRP-TEST-0001',
          status: 'CONFIRMED',
          performanceTitle: 'E2E Test Performance',
          posterUrl: null,
          showDateTime: new Date(Date.now() + 86400000).toISOString(),
          venue: 'E2E Venue',
          // Blocker B1: SeatSelection[] shape matching packages/shared/src/types/booking.types.ts
          seats: [{ seatId: 's1', tierName: 'VIP', price: 50000, row: 'A', number: '1' }],
          totalAmount: 50000,
          createdAt: new Date().toISOString(),
          // ReservationDetail additional fields (booking.types.ts:52-59)
          paymentMethod: 'card',
          paidAt: new Date().toISOString(),
          cancelDeadline: new Date(Date.now() + 86400000).toISOString(),
          cancelledAt: null,
          cancelReason: null,
          paymentKey: 'test_payment_key',
        }),
      });
    });

    // 2. Seed booking fixture so confirm page doesn't redirect to /booking/:id.
    const perfId = 'e2e-test-performance';
    await injectBookingFixture(page, {
      performanceId: perfId,
      showtimeId: 'e2e-test-showtime',
      // Blocker B1: SeatSelection shape
      seats: [{ seatId: 's1', tierName: 'VIP', price: 50000, row: 'A', number: '1' }],
      performanceTitle: 'E2E Test Performance',
      showDateTime: new Date(Date.now() + 86400000).toISOString(),
      venue: 'E2E Venue',
    });

    // 3. Navigate to confirm page.
    await page.goto(`/booking/${perfId}/confirm`);

    // 4. Assert page is on /confirm (NOT redirected to /booking/:id or /auth).
    await expect(page).toHaveURL(/\/confirm/);

    // 5. Assert Toss widget iframe mounted within 20s — proves real SDK loaded.
    await expect(page.locator('#payment-method iframe')).toBeVisible({ timeout: 20000 });

    // 6. Simulate Toss successUrl redirect by directly navigating the complete page.
    //    This triggers `confirmMutation` in complete/page.tsx:71-79, which hits
    //    POST /api/v1/payments/confirm — our intercept catches it.
    const paymentKey = `test_payment_key_${Date.now()}`;
    const orderId = `test_order_${Date.now()}`;
    const amount = '50000';
    await page.goto(
      `/booking/${perfId}/complete?paymentKey=${paymentKey}&orderId=${orderId}&amount=${amount}`,
    );

    // 7. Strictly assert intercept fired (REVIEWS.md HIGH-01: no typeof-only check).
    await expect.poll(() => confirmIntercepted, { timeout: 10000, message: 'POST /api/v1/payments/confirm was not intercepted' }).toBe(true);

    // 8. Assert complete page shows success state.
    await expect(page.getByText(/예매가 완료|완료되었습니다/)).toBeVisible({ timeout: 10000 });
  });

  // ============================================================================
  // Scenario 2: UI REGRESSION (cancel) — URL simulation only, not DEBT-05 evidence
  // ============================================================================
  test('UI regression: cancel error URL → toast renders', async ({ page }) => {
    // NOTE: This test validates the confirm page's URL-param branch (PAY_PROCESS_CANCELED).
    // It does NOT replace the happy-path real-SDK test.
    // It catches UI regressions in the error handling branch (confirm/page.tsx:75-76).
    await page.goto('/booking/e2e-test-performance/confirm?error=true&code=PAY_PROCESS_CANCELED');
    await expect(page.getByText('결제가 취소되었습니다.')).toBeVisible({ timeout: 5000 });
  });

  // ============================================================================
  // Scenario 3: UI REGRESSION (decline) — URL simulation only, not DEBT-05 evidence
  // ============================================================================
  test('UI regression: decline error URL → error message renders', async ({ page }) => {
    // NOTE: Same disclaimer as Scenario 2 — URL simulation only.
    const declineMessage = '카드 승인 거절';
    await page.goto(
      `/booking/e2e-test-performance/confirm?error=true&code=INVALID_CARD&message=${encodeURIComponent(declineMessage)}`,
    );
    await expect(page.getByText(/카드 승인 거절|결제에 실패/)).toBeVisible({ timeout: 5000 });
  });
});
```

**Step 4.** Commit: `test(09-tech-debt): add Toss E2E spec + fixture + auth helper (DEBT-05, HIGH-01, B1, B2)`.

**Implementation notes for executor:**
- Happy path가 실제로 GREEN 되려면 Task 2 (booking store fixture hook) + Task 1-3 (env/config) + API seeded DB 가 완료되어야 함.
- Task 0 단계에서는 3 파일 작성 + `pnpm --filter @grapit/web typecheck` 만 passthrough. 실 E2E 실행은 Task 5.
- 실 RED 상태 판단: `pnpm --filter @grapit/web test:e2e toss-payment` 실행 시 happy path 가 (1) fixture hook 미존재로 인한 redirect OR (2) auth 미seed 로 인한 redirect 중 하나로 fail 하는 것이 기대 RED.

**NOT allowed:**
- `frameLocator('iframe[src*="toss"]')` 로 Toss sandbox iframe 내부 카드 입력 조작 (Pitfall 3 — cross-origin 차단)
- 실 결제 금액 > 0 원으로 sandbox 호출 (intercept 로 차단 보장)
- `process.env.TOSS_SECRET_KEY_TEST` 를 클라이언트 번들에 노출 (backend confirm 호출에서만 사용, E2E 내부에서도 참조 금지)
- Blocker B1 위반: `seats: [{ id, label, price, grade }]` — `{ seatId, tierName, price, row, number }` 만 허용
- Blocker B2 위반: `page.context().addCookies([])` 빈 배열 사용 (TODO 남기기 금지)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && test -f apps/web/e2e/toss-payment.spec.ts && test -f apps/web/e2e/fixtures/booking-store.ts && test -f apps/web/e2e/helpers/auth.ts && grep -q "test.skip(" apps/web/e2e/toss-payment.spec.ts && grep -q "TOSS_CLIENT_KEY_TEST" apps/web/e2e/toss-payment.spec.ts && grep -q "expect(confirmIntercepted)\|expect\.poll" apps/web/e2e/toss-payment.spec.ts && grep -q "page.locator('#payment-method iframe')" apps/web/e2e/toss-payment.spec.ts && grep -q "loginAsTestUser" apps/web/e2e/toss-payment.spec.ts && grep -q "seatId" apps/web/e2e/toss-payment.spec.ts && grep -q "tierName" apps/web/e2e/toss-payment.spec.ts && ! grep -qE "grade:\s*'" apps/web/e2e/toss-payment.spec.ts && ! grep -qE "label:\s*'" apps/web/e2e/toss-payment.spec.ts && ! grep -q "frameLocator.*toss" apps/web/e2e/toss-payment.spec.ts && ! grep -q "TOSS_SECRET_KEY_TEST" apps/web/e2e/toss-payment.spec.ts && grep -q "SeatSelection" apps/web/e2e/fixtures/booking-store.ts && grep -q "page.request.post" apps/web/e2e/helpers/auth.ts && grep -q "TEST_USER_EMAIL" apps/web/e2e/helpers/auth.ts && pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/e2e/toss-payment.spec.ts` exists (min 90 lines)
    - `apps/web/e2e/fixtures/booking-store.ts` exists with `injectBookingFixture` export
    - `apps/web/e2e/helpers/auth.ts` exists with `loginAsTestUser` export (Blocker B2)
    - spec 파일에 `test.skip(!process.env['TOSS_CLIENT_KEY_TEST']` 포함
    - spec 파일에 3 `test(...)` 블록 (happy path / cancel / decline)
    - spec 파일에 `page.locator('#payment-method iframe')` 포함 (widget mount assert — REVIEWS.md HIGH-01)
    - spec 파일에 `expect(confirmIntercepted).toBe(true)` 또는 `expect.poll(() => confirmIntercepted).toBe(true)` 포함 (strict assert — REVIEWS.md HIGH-01)
    - spec 파일에 `page.route('**/api/v1/payments/confirm'` 포함
    - spec 파일에 "NOT a replacement" 또는 "UI regression" 주석 (cancel/decline 한계 명시)
    - spec 파일에 `loginAsTestUser(page)` 호출 (Blocker B2 — AuthGuard 우회 보장)
    - spec 파일에 `seatId` + `tierName` + `row` + `number` 키 존재 (Blocker B1 — SeatSelection shape)
    - `! grep -qE "grade:\\s*'" apps/web/e2e/toss-payment.spec.ts` (Blocker B1 — 기존 잘못된 키 제거)
    - `! grep -qE "label:\\s*'" apps/web/e2e/toss-payment.spec.ts` (Blocker B1 — 기존 잘못된 키 제거)
    - `! grep -q 'frameLocator.*toss' apps/web/e2e/toss-payment.spec.ts` (Pitfall 3)
    - `! grep -q 'TOSS_SECRET_KEY_TEST' apps/web/e2e/toss-payment.spec.ts` (secret 클라이언트 노출 금지)
    - fixture 파일에 `SeatSelection` import (Blocker B1)
    - auth helper 에 `page.request.post` + `TEST_USER_EMAIL` + `TestAdmin2026!` fallback 포함 (Blocker B2)
    - `pnpm --filter @grapit/web typecheck` exits 0
    - Commit matches `test(09-tech-debt): add Toss E2E spec + fixture + auth helper (DEBT-05, HIGH-01, B1, B2)`
  </acceptance_criteria>
  <done>E2E spec + fixture + auth helper 3 파일이 SeatSelection shape + loginAsTestUser 구조로 작성되고 typecheck PASS (full GREEN은 Task 2 store hook + DB seed 후 Task 5 에서)</done>
</task>

<task id="9-03-t1-env-example-toss-test" type="auto" tdd="false">
  <name>Task 1 (DEBT-05 + Blocker B2): .env.example 에 Toss 테스트 키 별칭 + TEST_USER_* 추가</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (루트 — 현 상태 / Plan 02에서 RESEND 추가됨)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Current State Probe DEBT-05: 공개 docs 키 재사용)
    - .planning/phases/09-tech-debt/09-CONTEXT.md (D-13 키 격리)
    - CLAUDE.md (`.env`는 gitignore, `.env.example` 은 placeholder 전용 — 단 Toss는 공개 docs 키라 예외)
    - apps/api/src/database/seed.mjs (Blocker B2: seed default credentials)
  </read_first>
  <action>
`.env.example` 에 다음 두 블록 추가 (기존 `NEXT_PUBLIC_TOSS_CLIENT_KEY` / `TOSS_SECRET_KEY` 정의 뒤 또는 적절한 섹션에):

```bash
# -- Toss Payments (E2E test-only — CI-only keys, NEVER in deploy.yml) --
# Public docs test keys from Toss documentation (https://docs.tosspayments.com);
# safe to commit — no real funds move. Used exclusively by Playwright E2E to prove
# we can reach Toss sandbox from CI without mixing test keys into production env.
# Key isolation rationale: 09-CONTEXT.md D-13.
TOSS_CLIENT_KEY_TEST=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm
TOSS_SECRET_KEY_TEST=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6

# -- Playwright E2E test user (Blocker B2) --
# Local: falls back to seed credentials (admin@grapit.test / TestAdmin2026! from
# apps/api/src/database/seed.mjs:39-50). Set these only if your seed differs.
# CI: set as GitHub secrets (TEST_USER_EMAIL / TEST_USER_PASSWORD) when the CI DB
# seeds different credentials.
# TEST_USER_EMAIL=admin@grapit.test
# TEST_USER_PASSWORD=TestAdmin2026!
```

**왜 실제 Toss 값을 `.env.example` 에 기록하는가:**
- 이 키들은 Toss 공식 docs 에 공개된 테스트 키로, secret 가치 없음.
- 로컬 개발자가 별도 키 발급 없이 즉시 E2E 실행 가능해야 함 (1인 개발 원칙).
- 프로덕션 격리는 `deploy.yml` 에 이 키 이름을 넣지 않음으로써 보장 (Task 5에서 검증).

**왜 TEST_USER_* 는 주석 처리하는가:**
- seed.mjs 의 기본값 (admin@grapit.test / TestAdmin2026!) 으로 로컬 E2E 가 즉시 동작해야 함 — env 미지정 시 auth helper 가 fallback.
- 실 값을 넣으면 .env (gitignore) 를 덮어쓰지 않는 한 개발자마다 매번 수정해야 하므로 비효율.

Commit: `feat(09-tech-debt): add Toss test key aliases + TEST_USER_* fallback hints to .env.example (DEBT-05, B2)`.

**NOT allowed:**
- 프로덕션 Toss 키를 `.env.example` 에 기록 (이미 한 번도 없었음 — 유지)
- `deploy.yml` 수정 (Task 5에서 negative verify 만 수행)
- TEST_USER_PASSWORD 의 실 값을 uncomment 하여 commit (주석 상태 유지)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q "TOSS_CLIENT_KEY_TEST=test_gck_docs_" .env.example && grep -q "TOSS_SECRET_KEY_TEST=test_gsk_docs_" .env.example && grep -q "NEVER in deploy.yml" .env.example && grep -q "TEST_USER_EMAIL" .env.example && grep -q "TEST_USER_PASSWORD" .env.example
    </automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `TOSS_CLIENT_KEY_TEST=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm`
    - `.env.example` contains `TOSS_SECRET_KEY_TEST=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6`
    - `.env.example` contains exact string `NEVER in deploy.yml`
    - `.env.example` contains `TEST_USER_EMAIL` and `TEST_USER_PASSWORD` (commented — Blocker B2 — fallback docs)
    - Commit matches `feat(09-tech-debt): add Toss test key aliases + TEST_USER_* fallback hints to .env.example (DEBT-05, B2)`
  </acceptance_criteria>
  <done>.env.example 에 Toss 테스트 키 별칭 + TEST_USER_* fallback 주석이 기록되고 D-13 격리 의도 주석 포함</done>
</task>

<task id="9-03-t2-booking-store-fixture-hook" type="auto" tdd="false">
  <name>Task 2 (REVIEWS.md HIGH-01 + Blocker B1): booking store에 `__BOOKING_FIXTURE__` 주입 hook 추가 (dev/test 전용, SeatSelection shape)</name>
  <files>
    apps/web/stores/use-booking-store.ts
  </files>
  <read_first>
    - apps/web/stores/use-booking-store.ts (전체 — 특히 L30-39 setBookingData signature, L93-103 구현)
    - apps/web/e2e/fixtures/booking-store.ts (Task 0 결과 — `window.__BOOKING_FIXTURE__` payload 구조)
    - packages/shared/src/types/booking.types.ts (Blocker B1: SeatSelection import 경로)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-01: happy path가 redirect 않도록 fixture 필요)
  </read_first>
  <action>
`apps/web/stores/use-booking-store.ts` 의 `create(...)` 호출 **직후** 에 production이 아닌 환경에서만 실행되는 fixture hook을 추가.

기존 파일 마지막 부분 (createStore 호출 뒤, L108 이후) 에 다음 블록 삽입:

```typescript
// ============================================================================
// E2E fixture hook (dev/test only) — Phase 9 DEBT-05 / REVIEWS.md HIGH-01
// Allows Playwright specs to inject booking state via `window.__BOOKING_FIXTURE__`
// so the confirm page doesn't redirect to /booking/:id (see confirm/page.tsx:62-66).
//
// Blocker B1 (revision-2): seats payload uses SeatSelection shape from
// packages/shared/src/types/booking.types.ts:30-37 — { seatId, tierName, price,
// row, number, tierColor? } — matching setBookingData's selectedSeats parameter.
// ============================================================================
if (typeof window !== 'undefined' && process.env.NODE_ENV !== 'production') {
  // Defer to next tick so the store is fully constructed when we read it.
  queueMicrotask(() => {
    const fixture = (window as unknown as {
      __BOOKING_FIXTURE__?: {
        performanceId: string;
        showtimeId: string;
        seats: SeatSelection[];   // Blocker B1 — imported at top of file
        performanceTitle: string;
        showDateTime: string;
        venue: string;
        posterUrl?: string;
      };
    }).__BOOKING_FIXTURE__;

    if (fixture) {
      useBookingStore.getState().setBookingData({
        selectedSeats: fixture.seats,
        showtimeId: fixture.showtimeId,
        performanceId: fixture.performanceId,
        performanceTitle: fixture.performanceTitle,
        showDateTime: fixture.showDateTime,
        venue: fixture.venue,
        posterUrl: fixture.posterUrl ?? null,
        expiresAt: Date.now() + 10 * 60 * 1000,
      });
    }
  });
}
```

주의:
- `SeatSelection` 은 이미 파일 상단 (L4) 에 import 되어 있으므로 추가 import 불필요. 확인:
  ```bash
  grep -n "SeatSelection" apps/web/stores/use-booking-store.ts
  # → L4 예상: import type { SeatSelection } from '@grapit/shared';
  ```
- `seats` 타입이 `SeatSelection[]` 와 호환되어야 함 — Blocker B1 해결로 이 불일치가 사라졌음.
- production 빌드는 `process.env.NODE_ENV === 'production'` 이므로 이 블록 전체가 tree-shake 되어 번들에 포함되지 않음 (Next.js/Turbopack dead code elimination).

Commit: `feat(09-tech-debt): add booking store fixture hook for E2E (DEBT-05, HIGH-01, B1)`.

**NOT allowed:**
- production 에서도 실행되도록 만들기 (tree-shake 보장 실패)
- `window.__BOOKING_FIXTURE__` 를 public 타입 export (테스트 내부 전용)
- `persist` middleware 추가 (기존 세션 UX 변경 금지)
- Blocker B1 위반: `seats: Array<{ id; label; price; grade }>` 형태 사용
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q "__BOOKING_FIXTURE__" apps/web/stores/use-booking-store.ts && grep -q "NODE_ENV !== 'production'" apps/web/stores/use-booking-store.ts && grep -q "queueMicrotask" apps/web/stores/use-booking-store.ts && grep -q "SeatSelection\[\]" apps/web/stores/use-booking-store.ts && pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "__BOOKING_FIXTURE__" apps/web/stores/use-booking-store.ts`
    - `grep -q "NODE_ENV !== 'production'" apps/web/stores/use-booking-store.ts`
    - `grep -q "queueMicrotask" apps/web/stores/use-booking-store.ts`
    - `grep -q "SeatSelection\\[\\]" apps/web/stores/use-booking-store.ts` (Blocker B1 — fixture payload seats 타입 명시)
    - `pnpm --filter @grapit/web typecheck` exits 0
    - `pnpm --filter @grapit/web build` exits 0 (production build 에서 dead code elimination 확인)
    - Commit matches `feat(09-tech-debt): add booking store fixture hook for E2E (DEBT-05, HIGH-01, B1)`
  </acceptance_criteria>
  <done>Booking store가 dev/test 환경에서 `window.__BOOKING_FIXTURE__` 를 한 번 읽어 초기 state 로 seed (SeatSelection[] 타입 정합), production 빌드는 영향 없음</done>
</task>

<task id="9-03-t3-playwright-config-env" type="auto" tdd="false">
  <name>Task 3 (REVIEWS.md HIGH-01 지원): playwright.config.ts 에 webServer.env 추가</name>
  <files>
    apps/web/playwright.config.ts
  </files>
  <read_first>
    - apps/web/playwright.config.ts (현재 L1-26)
    - apps/web/components/booking/toss-payment-widget.tsx (L66: `process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY` 참조)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-01: happy path 가 widget mount 하려면 NEXT_PUBLIC_TOSS_CLIENT_KEY 가 dev 서버에 전달되어야 함)
  </read_first>
  <action>
`apps/web/playwright.config.ts` 의 `webServer` 블록에 `env` 필드 추가.

기존 L20-24:
```typescript
webServer: {
  command: 'pnpm dev',
  url: 'http://localhost:3000',
  reuseExistingServer: !process.env['CI'],
},
```

변경 후:
```typescript
webServer: {
  command: 'pnpm dev',
  url: 'http://localhost:3000',
  reuseExistingServer: !process.env['CI'],
  env: {
    // Propagate Toss test key to the dev server so the SDK can initialize.
    // Phase 9 DEBT-05 / REVIEWS.md HIGH-01.
    NEXT_PUBLIC_TOSS_CLIENT_KEY:
      process.env['TOSS_CLIENT_KEY_TEST'] ?? process.env['NEXT_PUBLIC_TOSS_CLIENT_KEY'] ?? '',
  },
},
```

Commit: `feat(09-tech-debt): propagate Toss test key to Playwright dev server env (DEBT-05)`.

**NOT allowed:**
- Toss **프로덕션** 키를 `webServer.env` 에 노출 (CI secret 전용)
- `TOSS_SECRET_KEY_TEST` 를 `NEXT_PUBLIC_*` 접두사로 노출 (client 번들 유입 금지)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q "NEXT_PUBLIC_TOSS_CLIENT_KEY" apps/web/playwright.config.ts && grep -q "env:" apps/web/playwright.config.ts && pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "env:" apps/web/playwright.config.ts`
    - `grep -q "NEXT_PUBLIC_TOSS_CLIENT_KEY" apps/web/playwright.config.ts`
    - `grep -q "TOSS_CLIENT_KEY_TEST" apps/web/playwright.config.ts`
    - `! grep -q "TOSS_SECRET_KEY_TEST" apps/web/playwright.config.ts`
    - `pnpm --filter @grapit/web typecheck` exits 0
    - Commit matches `feat(09-tech-debt): propagate Toss test key to Playwright dev server env (DEBT-05)`
  </acceptance_criteria>
  <done>Playwright webServer가 Toss test key 를 dev server 프로세스에 전달 → SDK 초기화 가능</done>
</task>

<task id="9-03-t4-ci-workflow-e2e-and-gate" type="auto" tdd="false">
  <name>Task 4 (DEBT-05 + REVIEWS.md HIGH-02 + Blocker B2): ci.yml 에 verify-toss-secrets hard-gate + E2E step + TEST_USER_* 주입 + deploy.yml 격리 검증</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (전체 — 매우 짧음, L1-22 수준)
    - .github/workflows/deploy.yml (TOSS_*_TEST가 절대 없어야 함을 확인)
    - .planning/phases/09-tech-debt/09-CONTEXT.md (D-12, D-13)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-02: main push / schedule / dispatch 에서 secrets 누락 시 fail)
    - .planning/phases/09-tech-debt/09-RESEARCH.md (§Security T-V14-leak — 테스트 키 프로덕션 유입 방지)
  </read_first>
  <action>
**Step 1.** `.github/workflows/ci.yml` 확장. 기존 L1-22:
```yaml
name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
```

변경 후 (trigger 에 `workflow_dispatch` 추가 — HIGH-02 hard-require 대응):
```yaml
name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test

      # REVIEWS.md HIGH-02: Hard-gate on non-fork events.
      # main push / schedule / workflow_dispatch / same-repo PR MUST have Toss test secrets.
      # Fork PRs: skip gracefully (GitHub Actions strips secrets for forks automatically).
      - name: Verify Toss test secrets present (hard-gate on non-fork events)
        if: |
          github.event_name == 'push' ||
          github.event_name == 'schedule' ||
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository)
        env:
          TOSS_CLIENT_KEY_TEST: ${{ secrets.TOSS_CLIENT_KEY_TEST }}
          TOSS_SECRET_KEY_TEST: ${{ secrets.TOSS_SECRET_KEY_TEST }}
        run: |
          if [ -z "$TOSS_CLIENT_KEY_TEST" ] || [ -z "$TOSS_SECRET_KEY_TEST" ]; then
            echo "Missing TOSS_CLIENT_KEY_TEST or TOSS_SECRET_KEY_TEST in repo secrets."
            echo "Register via: gh secret set TOSS_CLIENT_KEY_TEST"
            echo "See .planning/phases/09-tech-debt/09-CONTEXT.md D-13 for rationale."
            exit 1
          fi
          echo "Toss test secrets present."

      - name: Install Playwright browsers
        run: pnpm --filter @grapit/web exec playwright install --with-deps chromium

      - name: E2E tests (Toss Payments)
        env:
          TOSS_CLIENT_KEY_TEST: ${{ secrets.TOSS_CLIENT_KEY_TEST }}
          TOSS_SECRET_KEY_TEST: ${{ secrets.TOSS_SECRET_KEY_TEST }}
          NEXT_PUBLIC_TOSS_CLIENT_KEY: ${{ secrets.TOSS_CLIENT_KEY_TEST }}
          # Blocker B2: propagate CI test user credentials (auth helper falls back to
          # seed.mjs defaults when unset, which matches local developer flow).
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
        run: pnpm --filter @grapit/web test:e2e
```

**Step 2.** `deploy.yml` negative check — 편집하지 말고 단순히 `TOSS_*_TEST` 가 존재하지 않음을 확인:
```bash
test "$(grep -c 'TOSS_CLIENT_KEY_TEST\|TOSS_SECRET_KEY_TEST' .github/workflows/deploy.yml | tr -d ' ')" = "0"
```
→ 0 이어야 함 (D-13 격리).

**Step 3.** GitHub Actions secrets 등록 (수동 작업 — executor는 코드 변경만; 실제 등록은 Plan SUMMARY에 수동 step으로 문서화).

1인 개발자 수동 작업 (Plan SUMMARY 체크리스트):
```
gh secret set TOSS_CLIENT_KEY_TEST --body "test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm"
gh secret set TOSS_SECRET_KEY_TEST --body "test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6"
# Blocker B2: TEST_USER_* 은 선택 — CI DB 가 seed.mjs 와 다른 admin credentials 를
# 사용한다면 아래 2개도 등록. 아니면 auth helper 의 fallback 이 자동 적용.
# gh secret set TEST_USER_EMAIL --body "admin@grapit.test"
# gh secret set TEST_USER_PASSWORD --body "TestAdmin2026!"
gh secret list   # TOSS_*_TEST 2개 (+ 선택 TEST_USER_*) 확인
```

**Step 4.** Commit: `feat(09-tech-debt): add Toss E2E step + secrets hard-gate + TEST_USER_* env in CI (DEBT-05, HIGH-02, B2)`.

**NOT allowed:**
- `deploy.yml` 의 secrets 또는 env_vars 블록에 TOSS_*_TEST 추가
- `TOSS_SECRET_KEY_TEST` 를 `NEXT_PUBLIC_*` 접두사로 노출 (client 번들 유입)
- Gate 조건을 `if: false` 로 단순 비활성화
- Fork PR 에서도 gate를 fail시키는 조건식 (CI 접근성 유지)
- TEST_USER_PASSWORD 를 env_vars 에 평문 노출 (secrets 블록 전용)
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && grep -q "pnpm --filter @grapit/web test:e2e" .github/workflows/ci.yml && grep -q "Verify Toss test secrets present" .github/workflows/ci.yml && grep -q "exit 1" .github/workflows/ci.yml && grep -q "github.event.pull_request.head.repo.full_name" .github/workflows/ci.yml && grep -q "playwright install --with-deps chromium" .github/workflows/ci.yml && grep -q "TEST_USER_EMAIL" .github/workflows/ci.yml && grep -q "TEST_USER_PASSWORD" .github/workflows/ci.yml && test "$(grep -c 'TOSS_CLIENT_KEY_TEST\|TOSS_SECRET_KEY_TEST' .github/workflows/deploy.yml | tr -d ' ')" = "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `pnpm --filter @grapit/web test:e2e`
    - `.github/workflows/ci.yml` contains `Verify Toss test secrets present` step name
    - `.github/workflows/ci.yml` contains `exit 1` in gate step (hard-fail)
    - `.github/workflows/ci.yml` contains `github.event.pull_request.head.repo.full_name == github.repository` (fork 분기)
    - `.github/workflows/ci.yml` contains `TOSS_CLIENT_KEY_TEST: ${{ secrets.TOSS_CLIENT_KEY_TEST }}` in E2E step env
    - `.github/workflows/ci.yml` contains `TOSS_SECRET_KEY_TEST: ${{ secrets.TOSS_SECRET_KEY_TEST }}` in E2E step env
    - `.github/workflows/ci.yml` contains `NEXT_PUBLIC_TOSS_CLIENT_KEY: ${{ secrets.TOSS_CLIENT_KEY_TEST }}` (SDK 로딩용)
    - `.github/workflows/ci.yml` contains `TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}` (Blocker B2 — CI auth helper)
    - `.github/workflows/ci.yml` contains `TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}` (Blocker B2)
    - `.github/workflows/ci.yml` contains `playwright install --with-deps chromium`
    - `test "$(grep -c 'TOSS_CLIENT_KEY_TEST\|TOSS_SECRET_KEY_TEST' .github/workflows/deploy.yml | tr -d ' ')" = "0"` (D-13 격리)
    - 기존 lint/typecheck/test step 이 변경 없이 유지됨 (순서 보존)
    - Commit matches `feat(09-tech-debt): add Toss E2E step + secrets hard-gate + TEST_USER_* env in CI (DEBT-05, HIGH-02, B2)`
  </acceptance_criteria>
  <done>ci.yml이 main push / schedule / workflow_dispatch / same-repo PR 에서 secrets 부재 시 fail, fork PR 은 skip 허용, E2E step이 keys + TEST_USER_* 를 받아 실행</done>
</task>

<task id="9-03-t5-final-e2e-smoke-run" type="auto" tdd="false">
  <name>Task 5 (REVIEWS.md HIGH-01 — GREEN 확인 + W1): 로컬에서 E2E full run + deploy.yml 격리 최종 감사 + 09-03-SUMMARY.md 작성</name>
  <files>
    .planning/phases/09-tech-debt/09-03-SUMMARY.md
  </files>
  <read_first>
    - apps/web/e2e/toss-payment.spec.ts (Task 0)
    - apps/web/e2e/fixtures/booking-store.ts (Task 0)
    - apps/web/e2e/helpers/auth.ts (Task 0 — Blocker B2)
    - apps/web/stores/use-booking-store.ts (Task 2)
    - apps/web/playwright.config.ts (Task 3)
    - .github/workflows/ci.yml (Task 4)
    - .planning/phases/09-tech-debt/09-REVIEWS.md (HIGH-01 closure criteria)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY 템플릿)
  </read_first>
  <action>
**Step 1. 로컬 pre-flight (TOSS_CLIENT_KEY_TEST 미설정 상태):**
```bash
unset TOSS_CLIENT_KEY_TEST
pnpm --filter @grapit/web test:e2e toss-payment
```
기대: 3 tests all skipped, exit 0.

**Step 2. 로컬 full run (TOSS_CLIENT_KEY_TEST 설정 + DB seed):**
```bash
# DB 가 admin@grapit.test 로 seed 되어 있어야 Blocker B2 auth helper 가 동작.
pnpm --filter @grapit/api seed

export TOSS_CLIENT_KEY_TEST=test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm
export TOSS_SECRET_KEY_TEST=test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
# TEST_USER_* 은 생략 — auth helper 가 seed 기본값으로 fallback.
pnpm --filter @grapit/web test:e2e toss-payment
```
기대:
- Scenario 1 (happy path): PASS — widget iframe visible + `confirmIntercepted === true`
- Scenario 2 (cancel UI): PASS
- Scenario 3 (decline UI): PASS

**만약 Scenario 1 FAIL:**
- (a) fixture hook 미작동: booking store hook이 initial state 주입을 놓쳤을 가능성 — timing 문제일 수 있음. `queueMicrotask` 대신 `requestAnimationFrame` 또는 명시적 setTimeout 0 고려.
- (b) **Blocker B2 auth helper 오동작:** `loginAsTestUser` throw 하면 DB seed 누락 또는 zustand persist key mismatch. 먼저 `pnpm --filter @grapit/api seed` 재실행, 그 다음 `apps/web/stores/use-auth-store.ts` 의 persist `name` 을 확인하여 `AUTH_STORE_KEY` 를 재조정.
- (c) widget iframe timeout: Toss SDK 로드가 20초 이상 걸림 — 네트워크 체크 + `NEXT_PUBLIC_TOSS_CLIENT_KEY` 가 dev server env로 전달되는지 확인 (Task 3).

**Step 3. deploy.yml 최종 격리 감사:**
```bash
# D-13 negative check
grep -nE "TOSS_CLIENT_KEY_TEST|TOSS_SECRET_KEY_TEST" .github/workflows/deploy.yml
# → 결과 없음 (exit 1 또는 empty stdout)
```

**Step 4. 프로덕션 Cloud Run env 감사 (수동, SUMMARY에 기록):**
```bash
gcloud run services describe grapit-api --region=asia-northeast3 --format="value(spec.template.spec.containers[0].env[].name)" | grep -E "TOSS_.*_TEST" || echo "OK: no test keys in prod"
# → "OK: no test keys in prod" 출력 기대
```

**Step 5 (W1 — SUMMARY 작성).** `.planning/phases/09-tech-debt/09-03-SUMMARY.md` 생성. 템플릿 기반 + 아래 항목:
- DEBT-05 closure evidence (happy path PASS log + Playwright HTML report 경로)
- REVIEWS.md 대응:
  - HIGH-01: widget mount + strict intercept 증거
  - HIGH-02: verify-toss-secrets step + fork 분기 확인
  - UI regression (cancel/decline) 의 한계 주석 확인
- Revision-2 Blocker 대응:
  - B1: spec / fixture / store hook 에서 SeatSelection shape 사용 grep 결과
  - B2: loginAsTestUser 성공 로그 + refreshToken cookie 존재 + /complete 정상 접근
- files_modified actual (7 files) vs planned (7 files)
- 1인 개발자 수동 작업 체크리스트 (gh secret, gcloud run audit, optional drill)

**Step 6.** Commit: `docs(09-tech-debt): finalize 09-03-SUMMARY with E2E run evidence + blocker closure (DEBT-05)` — SUMMARY 파일 한 개만 신규 생성/커밋.

**If any step 1-4 fails:** 즉시 이전 task 로 롤백하여 수정. Push 금지.
  </action>
  <verify>
    <automated>
      cd /Users/sangwopark19/icons/grapit && test "$(grep -c 'TOSS_CLIENT_KEY_TEST\|TOSS_SECRET_KEY_TEST' .github/workflows/deploy.yml | tr -d ' ')" = "0" && test -f apps/web/e2e/toss-payment.spec.ts && test -f apps/web/e2e/fixtures/booking-store.ts && test -f apps/web/e2e/helpers/auth.ts && grep -q "__BOOKING_FIXTURE__" apps/web/stores/use-booking-store.ts && grep -q "Verify Toss test secrets present" .github/workflows/ci.yml && grep -q "env:" apps/web/playwright.config.ts && test -f .planning/phases/09-tech-debt/09-03-SUMMARY.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @grapit/web test:e2e toss-payment` (keys unset) all 3 tests skipped, exit 0
    - `pnpm --filter @grapit/web test:e2e toss-payment` (keys set + DB seeded) all 3 tests PASS (Scenario 1 widget mount + confirmIntercepted true — Blocker B1+B2 closure)
    - `test "$(grep -c 'TOSS_CLIENT_KEY_TEST\|TOSS_SECRET_KEY_TEST' .github/workflows/deploy.yml | tr -d ' ')" = "0"`
    - `.planning/phases/09-tech-debt/09-03-SUMMARY.md` 존재 with Step 2 full run 결과 + Step 4 GCP production audit 결과 + Blocker B1/B2 grep 증거 (W1)
    - Playwright HTML report에 Scenario 1 screenshot으로 widget iframe 가시 상태 증거
    - Commit matches `docs(09-tech-debt): finalize 09-03-SUMMARY with E2E run evidence + blocker closure (DEBT-05)`
  </acceptance_criteria>
  <done>E2E full run 이 local 에서 실제로 PASS 하여 HIGH-01 + Blocker B1/B2 의 closure 조건을 만족하고 SUMMARY 문서화 완료 (W1)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Toss sandbox | TOSS_CLIENT_KEY_TEST 경유, TLS + PCI 격리 iframe |
| GitHub Actions runner → Grapit web (dev server) | localhost:3000 webServer 자동 기동, NEXT_PUBLIC_TOSS_CLIENT_KEY 전달 |
| Playwright → Grapit confirm API | `page.route()` 인터셉션으로 실 API 호출 차단 |
| CI secrets → Cloud Run (deploy.yml) | **NEGATIVE BOUNDARY — TOSS_*_TEST는 이 경로를 통과하지 않아야 함** (D-13) |
| Window → booking store fixture | dev/test 환경에서만 `__BOOKING_FIXTURE__` 읽음, production tree-shaken |
| Playwright request fixture → /api/v1/auth/login | Blocker B2: helper 내부에서 실 HTTPS-like (dev HTTP) 로그인 호출로 AuthGuard 우회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering (V14 — T-V14-leak) | CI → 프로덕션 Cloud Run 경로 | mitigate | `deploy.yml` 에 `TOSS_CLIENT_KEY_TEST` / `TOSS_SECRET_KEY_TEST` 를 **절대 추가하지 않음**. Task 4/5의 acceptance criteria에 `grep -c ... deploy.yml = 0` 검증 포함. |
| T-09-12 | Tampering / Financial Loss | E2E 실 결제 시도 | mitigate | `test.skip(!process.env['TOSS_CLIENT_KEY_TEST'])` 가드 + Toss sandbox 공개 docs 키 (실 결제 금액 이동 불가) + `page.route()` 가 confirm API 를 인터셉트하여 실 승인 요청 차단. |
| T-09-13 | Information Disclosure | TOSS_SECRET_KEY_TEST 클라이언트 노출 | mitigate | `NEXT_PUBLIC_` 접두사 없이 CI env 에만 주입. Next.js 는 `NEXT_PUBLIC_*` 만 클라이언트 번들에 포함하므로 non-prefixed 키는 서버 전용. E2E spec에서도 `TOSS_SECRET_KEY_TEST` 를 참조하지 않음. |
| T-09-14 | Spoofing | Fake Toss confirm response | accept | `page.route()` 가 의도적으로 확인 API를 mock 하므로 이는 정상 설계. 실 프로덕션 결제 경로에서는 백엔드가 Toss 서버와 직접 통신 (`toss-payments.client.ts:28-96`) — E2E mock과 무관. |
| T-09-15 | Information Disclosure | Toss test keys 가 CI 로그에 출력 | mitigate | `${{ secrets.TOSS_CLIENT_KEY_TEST }}` 는 GitHub Actions가 로그에서 자동 마스킹. `.env.example` 의 실 값은 공개 docs 키이므로 허용. |
| T-09-16 | Tampering (V14) | Fixture hook이 production 번들 유입 | mitigate | `process.env.NODE_ENV !== 'production'` 분기 + Next.js production 빌드 dead code elimination. `pnpm --filter @grapit/web build` 가 Task 2 acceptance 에서 실 번들 감사. |
| T-09-17 | Silent Skip (REVIEWS.md HIGH-02) | CI main push 에서 secrets 미존재 시 silent green | mitigate | Task 4 `verify-toss-secrets` step + fork 분기. push/schedule/dispatch/same-repo PR 에서는 secrets 부재 시 exit 1. |
| T-09-20 | Test Credential Leakage (Blocker B2) | TEST_USER_PASSWORD in CI logs | mitigate | GitHub secrets 로만 주입 (env_vars 평문 금지). `.env.example` 의 TEST_USER_* 는 주석 처리하여 real password 를 commit 하지 않음. fallback 은 seed.mjs 의 로컬 개발용 기본값으로 실 프로덕션 자격증명과 무관. |

RESEARCH §Security Domain T-V14-leak + REVIEWS.md HIGH-02 + Blocker B2 완전 완화.
</threat_model>

<verification>
**Wave-level sampling:**

1. **태스크별 (per-task, < 60s):**
   - Task 0: `pnpm --filter @grapit/web typecheck` (spec/fixture/helper 3 파일 syntax 검증)
   - Task 1: `grep` verify on `.env.example`
   - Task 2: `pnpm --filter @grapit/web typecheck` + `pnpm --filter @grapit/web build` (production tree-shake 검증)
   - Task 3: `pnpm --filter @grapit/web typecheck`
   - Task 4: `grep` verify on `ci.yml` + negative `grep` on `deploy.yml`

2. **Plan 전체 완료 후 (Task 5):**
   - `pnpm --filter @grapit/web test:e2e toss-payment` (키 미설정) → 3 tests skipped, exit 0
   - `pnpm --filter @grapit/web test:e2e toss-payment` (키 설정 + DB seeded) → 3 tests PASS (happy path 포함, B1+B2 closure)
   - `grep -c TOSS_*_TEST deploy.yml` = 0
   - 09-03-SUMMARY.md 작성 완료 (W1)

3. **수동 검증 (VALIDATION.md §Manual-Only + REVIEWS.md HIGH-02 실증):**
   - GitHub Actions secrets 등록 (`gh secret set TOSS_CLIENT_KEY_TEST ...`)
   - 프로덕션 Cloud Run env 확인: `gcloud run services describe grapit-api --region=asia-northeast3 --format="value(spec.template.spec.containers[0].env[].name)" | grep -E "TOSS_.*_TEST" || echo "OK"` → "OK" 출력 (D-13)
   - PR 푸시 시 CI에서 `verify-toss-secrets` step PASS + E2E 실행 로그 확인
   - Secret 을 일시 제거한 상태로 empty-content push 해서 gate 가 실제 fail 하는지 1회 확인 (선택적 drill)

**Expected-absence verify 패턴 (REVIEWS.md MED):**
- 모든 expected-absence 검증은 `test "$(grep -c ... | tr -d ' ')" = "0"` 또는 `! grep -q ...` 패턴.
- `grep -c ... && ...` 체이닝 금지 — 본 Plan 의 acceptance criteria 감사 완료.

**Failure recovery:**
- Playwright browsers 설치가 CI 에서 실패하면 `pnpm --filter @grapit/web exec playwright install-deps` 추가 시도
- happy path widget iframe timeout 이 자주 일어나면 `playwright.config.ts` 의 `use.navigationTimeout` 값 증가 고려
- fixture hook이 dev에서 작동하지 않으면 timing 조정 (`queueMicrotask` → `setTimeout(..., 0)`)
- auth helper 가 throw 하면 (Blocker B2): `pnpm --filter @grapit/api seed` 실행 + `apps/web/stores/use-auth-store.ts` 의 zustand persist `name` 확인 후 `AUTH_STORE_KEY` 조정
- 만약 `deploy.yml` 이 grep 결과 0이 아니라면 **즉시 커밋 중단** + D-13 위반 알람
</verification>

<success_criteria>
- [ ] `apps/web/e2e/toss-payment.spec.ts` 존재 (min 90 lines) + 3 test block + widget mount assert + confirmIntercepted strict assert + loginAsTestUser 호출 (Blocker B2) + SeatSelection shape (Blocker B1)
- [ ] `apps/web/e2e/fixtures/booking-store.ts` 존재 with `injectBookingFixture` export + SeatSelection[] 타입 (Blocker B1)
- [ ] `apps/web/e2e/helpers/auth.ts` 존재 with `loginAsTestUser` export + page.request.post + TEST_USER_* fallback (Blocker B2)
- [ ] `apps/web/stores/use-booking-store.ts` 에 `__BOOKING_FIXTURE__` hook + `NODE_ENV !== 'production'` gate + SeatSelection[] 타입 (Blocker B1)
- [ ] `apps/web/playwright.config.ts` 에 `webServer.env` 블록 + `NEXT_PUBLIC_TOSS_CLIENT_KEY` 전달
- [ ] `.env.example` 에 `TOSS_CLIENT_KEY_TEST` + `TOSS_SECRET_KEY_TEST` + `NEVER in deploy.yml` + `TEST_USER_EMAIL` + `TEST_USER_PASSWORD` 문구 추가
- [ ] `.github/workflows/ci.yml` 에 `Verify Toss test secrets present` step + `exit 1` + fork 분기 조건 포함 (HIGH-02) + TEST_USER_* env (Blocker B2)
- [ ] `.github/workflows/ci.yml` 에 E2E step + `NEXT_PUBLIC_TOSS_CLIENT_KEY` env 추가
- [ ] **`.github/workflows/deploy.yml` 변경 없음 (git diff empty)** — D-13 격리
- [ ] `test "$(grep -c 'TOSS_CLIENT_KEY_TEST\|TOSS_SECRET_KEY_TEST' .github/workflows/deploy.yml | tr -d ' ')" = "0"`
- [ ] `pnpm --filter @grapit/web typecheck` exits 0
- [ ] `pnpm --filter @grapit/web lint` exits 0
- [ ] `pnpm --filter @grapit/web build` exits 0 (fixture hook이 production 번들에서 제거되었는지 검증)
- [ ] `pnpm --filter @grapit/web test:e2e` (키 없는 상태) exits 0 with 3 tests skipped
- [ ] `pnpm --filter @grapit/web test:e2e` (키 있는 상태 + DB seeded) exits 0 with **3 tests PASSED** (happy path 포함 — DEBT-05 closure + Blocker B1/B2 closure)
- [ ] `.planning/phases/09-tech-debt/09-03-SUMMARY.md` 존재 (W1 — Task 5 deliverable)
- [ ] 6개의 의미 있는 커밋 (spec + fixture + auth helper / .env.example / store hook / playwright config / ci.yml / SUMMARY)
</success_criteria>

<output>
After completion, `.planning/phases/09-tech-debt/09-03-SUMMARY.md` is authored as **Task 5 deliverable (W1)** covering:
- DEBT-05 closure evidence:
  - Task 5 full run 로그: Scenario 1 happy path PASS (widget iframe visible + confirmIntercepted === true)
  - Playwright HTML report screenshot 경로
- REVIEWS.md 대응 증거:
  - HIGH-01: happy path 가 widget mount + strict intercept assert 로 DEBT-05 closure (smoke 이상)
  - HIGH-02: ci.yml verify-toss-secrets step 코드 + fork 분기 조건 확인
  - (UI regression) Cancel/Decline 테스트가 부가 커버리지임을 코드 주석에 명시
- Revision-2 Blocker 대응 증거:
  - Blocker B1: `grep -q "seatId.*tierName" apps/web/e2e/toss-payment.spec.ts` + fixture/store hook 동일 체크
  - Blocker B2: `grep -q "loginAsTestUser" apps/web/e2e/toss-payment.spec.ts` + helper page.request.post 동작 확인 + refreshToken cookie 존재
  - W1: 이 SUMMARY 존재 자체가 W1 closure
- files_modified actual vs planned (7 files)
- CI run link after merging (E2E skip/pass log)
- D-13 격리 검증 (`grep -c TOSS_*_TEST deploy.yml` = 0 확인)
- 1인 개발자 수동 작업 체크리스트:
  - [ ] `gh secret set TOSS_CLIENT_KEY_TEST ...` 완료
  - [ ] `gh secret set TOSS_SECRET_KEY_TEST ...` 완료
  - [ ] (선택) `gh secret set TEST_USER_EMAIL / TEST_USER_PASSWORD` — CI DB 가 seed 기본값과 다를 때만
  - [ ] `gh secret list` 에서 두(+) 시크릿 확인
  - [ ] `gcloud run services describe grapit-api --format=... | grep TOSS_.*_TEST` = 결과 없음 (프로덕션 격리 확인)
  - [ ] Optional drill: 시크릿 일시 해제 후 push → `verify-toss-secrets` step이 실제로 fail 하는지 1회 검증
- Phase 9 전체 완료: all 6 DEBT items closed (DEBT-01~06)
- Next: Phase 10 (SMS 인증 실연동) 진행 가능
</output>
