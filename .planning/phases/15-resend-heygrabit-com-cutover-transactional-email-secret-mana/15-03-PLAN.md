---
phase: 15
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md
autonomous: false
requirements: [CUTOVER-02, CUTOVER-03, CUTOVER-05]
user_setup:
  - service: gcp-secret-manager
    why: "resend-from-email secret 의 신규 version 추가 (값 = no-reply@heygrabit.com)"
    env_vars: []
    dashboard_config:
      - task: "gcloud CLI 실행 권한 확인 — project grapit-491806 대상 secrets.versions.add + run.services.update 권한 필요"
        location: "로컬 운영자 머신 (이미 Phase 13 에서 사용 이력 있음)"
  - service: gcp-cloud-run
    why: "grabit-api 신규 revision 기동 + 100% traffic 확인"
    env_vars: []
    dashboard_config:
      - task: "gcloud run services update 실행 후 신규 revision 이 100% traffic 받는지 확인"
        location: "GCP Console → Cloud Run → grabit-api 또는 gcloud CLI"
  - service: resend
    why: "UAT 통과 후 구 grapit.com 도메인 제거 (D-02)"
    env_vars: []
    dashboard_config:
      - task: "Resend Dashboard → Domains → grapit.com → Remove"
        location: "https://resend.com/domains"
  - service: email-mailboxes-uat
    why: "Gmail + Naver + Daum(또는 Kakao) 3사 inbox 수신 확인 — D-14"
    env_vars: []
    dashboard_config:
      - task: "3사 각 계정으로 /auth/forgot-password 트리거 후 각 inbox 수신 확인 (spam 폴더 아님)"
        location: "https://heygrabit.com/auth/forgot-password + 각 메일 서비스"
must_haves:
  truths:
    - "Secret Manager `resend-from-email` 신규 version 의 payload 가 정확히 `no-reply@heygrabit.com` 이다"
    - "grabit-api Cloud Run 신규 revision 이 100% traffic 을 받고 RESEND_FROM_EMAIL 이 신규 secret version 에 바인딩되어 있다"
    - "Gmail + Naver + Daum(또는 Kakao) 3사 inbox 에 `[Grabit] 비밀번호 재설정` 이메일이 from `no-reply@heygrabit.com` 로 spam 분류 없이 수신된다"
    - "`gcloud logging read 'Resend send failed' --freshness=24h` 결과가 empty 이다 (D-13)"
    - "Resend 대시보드에서 구 grapit.com 도메인이 제거되었다 (D-02, UAT 통과 후)"
    - "15-HUMAN-UAT.md 의 모든 Sign-off 체크박스와 Wave 3 Audit Log 가 실제 값으로 채워져 있다"
  artifacts:
    - path: ".planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md"
      provides: "Wave 3 Audit Log + Sign-off 섹션 fill-in 완료"
      contains: "신규 Cloud Run revision ID"
  key_links:
    - from: "Secret Manager resend-from-email (신규 version)"
      to: "Cloud Run grabit-api (신규 revision env RESEND_FROM_EMAIL)"
      via: "gcloud run services update --update-secrets RESEND_FROM_EMAIL=resend-from-email:latest"
      pattern: "revision-start-time :latest resolution (RESEARCH §Pattern 2)"
    - from: "Cloud Run grabit-api 신규 revision"
      to: "Resend API (from=no-reply@heygrabit.com)"
      via: "EmailService.sendPasswordResetEmail 호출"
      pattern: "this.resend.emails.send({from: this.from})"
    - from: "Resend 발송"
      to: "3사 수신 ESP (Gmail/Naver/Daum) → 유저 inbox"
      via: "SPF/DKIM/DMARC alignment pass"
      pattern: "Plan 02 의 DNS 등록이 전제"
---

<objective>
Plan 02 의 Resend Verified 상태를 전제로 (D-08 sequence invariant), Secret Manager `resend-from-email` 에 신규 version `no-reply@heygrabit.com` 을 추가하고 `grabit-api` Cloud Run 서비스를 `--update-secrets` 로 즉시 재배포하여 신규 revision 이 100% traffic 을 받게 한다. 이후 3사 (Gmail/Naver/Daum) 실기기 UAT 를 수행하여 inbox 수신 (spam 아님) 을 확인하고, D-13 의 gcloud logging 쿼리가 empty 임을 확증한 후, Resend 대시보드에서 구 `grapit.com` 도메인을 제거한다 (D-02). 마지막으로 15-HUMAN-UAT.md 의 Wave 3 Audit Log 와 Sign-off 섹션을 실제 값으로 fill-in 한다 (D-16).

Purpose: Phase 15 의 최종 운영 cutover. Phase 13 UAT gap 9 (`.planning/debug/password-reset-email-not-delivered-prod.md`) 의 Resolution 을 실행하여 silent non-delivery 를 해소한다.

Output: (1) Secret Manager 신규 version 활성, (2) Cloud Run 신규 revision 100% traffic, (3) 3사 UAT 수신 확인, (4) 구 grapit.com Resend 제거, (5) 15-HUMAN-UAT.md 완성.

**Sequence invariant (절대 변경 금지):** Task 1 (Secret 추가) → Task 2 (Cloud Run update) → Task 3 (UAT 3사) → Task 4 (Logging empty 확인) → Task 5 (grapit.com 제거). Task 3 FAIL 시 Task 5 금지, 즉시 rollback (D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-CONTEXT.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-RESEARCH.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-PATTERNS.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-02-PLAN.md
@.planning/debug/password-reset-email-not-delivered-prod.md
@.github/workflows/deploy.yml

<!-- 운영 runbook — 코드 계약 없음. gcloud 커맨드 정확한 flag 는 RESEARCH §Pattern 2 L211-227 및 각 task action 에 inline 명시. -->
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자 로컬 머신 ↔ GCP API (gcloud CLI) | Secret Manager / Cloud Run API 호출 — 운영자의 gcloud ADC 자격증명으로 인증. 잘못된 flag 또는 잘못된 version pin 은 운영 영향 |
| Secret Manager ↔ Cloud Run runtime | secret 주입 경계. `:latest` resolution 은 revision start 시 1 회, lifetime 동안 고정 (RESEARCH §Pattern 2). 오해 시 구 값이 남음 |
| Cloud Run grabit-api ↔ Resend API | new revision 이 `from=no-reply@heygrabit.com` 로 호출. Resend Verified 필요 (Plan 02 의 전제) |
| Resend ↔ 3사 ESP (Gmail/Naver/Daum) | SPF/DKIM/DMARC alignment 로 deliverability 결정. spam 분류 시 rollback 트리거 |
| gcloud logging ↔ 운영자 | D-13 관측 — 쿼리 결과가 empty/non-empty 로 성공 판별 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | T (Tampering) | Secret Manager 에 잘못된 payload (공백, 개행, 잘못된 이메일) 주입 | mitigate | Task 1 에서 `printf` (echo 금지, 개행 방지) 사용 + Task 1 직후 `gcloud secrets versions access` 로 read-back 검증. EMAIL_PATTERN 준수 확인 (email.service.ts constructor 가 invalid 시 hard-fail 으로 기동 실패함 — 이중 방어) |
| T-15-13 | R (Repudiation) | Secret version 변경 audit trail 부재 | mitigate | 15-HUMAN-UAT.md Wave 3 Audit Log 에 신규 version 번호, 이전 version 번호 (rollback pin), 실행 시각, 운영자 email 기록 (Task 6). gcloud 자체 audit log 는 GCP IAM Audit 에도 자동 기록 |
| T-15-14 | D (Denial of Service) | Cloud Run new revision 이 100% traffic 전환 실패 → 구 revision 유지로 RESEND_FROM_EMAIL 구 값 남음 | mitigate | Task 2 에서 `gcloud run services describe` 로 traffic 100% 명시 검증 후 Task 3 UAT 진행. 100% 아닌 경우 Task 3 실행 금지 (false-positive UAT 방지) |
| T-15-15 | I (Information Disclosure) | UAT 수신 테스트용 메일박스 (개인 3사) 에 password-reset 링크 노출 | accept | password-reset 링크는 15분 만료 JWT. UAT 수행 후 링크 사용하지 않으면 자동 무효화. 본인 소유 3사 계정에 한정된 노출 |
| T-15-16 | T (Tampering) | UAT 전에 grapit.com 제거 시 in-flight `from=grapit.com` 요청이 422 로 실패 | mitigate | Task 5 는 Task 3 PASS 이후에만 실행 — sequence invariant (D-02, AP-05) 를 runbook 에 명시 + 15-HUMAN-UAT.md Sign-off 에 "UAT PASS 확인 후 제거" 체크 포함 |
| T-15-17 | I (Information Disclosure) | Sentry email-service 이벤트에 PII 포함 (Plan 01 에서 이미 masking — 본 plan 은 관측만) | accept (Plan 01 처리) | 본 plan 은 관측만 수행. Plan 01 Task 1 의 PII masking 이 효과적인지 Task 4 에서 Sentry UI 확인으로 2차 검증 |
| T-15-18 | D (Denial of Service) | Rollback playbook 실행 시 DNS revert 를 시도 (잘못된 대응) | mitigate | D-15 에 명시된 Secret version pinning 만 사용. Task 6 의 15-HUMAN-UAT.md 에 "DNS 레코드는 revert 하지 않음 — propagation 지연으로 fast rollback 수단 아님" 을 명시 기재 (Plan 02 Task 4 에서 이미 템플릿에 포함) |
| T-15-19 | R (Repudiation) | 구 grapit.com 제거 audit trail 부재 | mitigate | Task 5 resume-signal + 15-HUMAN-UAT.md Wave 3 Audit Log "Resend 구 grapit.com 제거 시각" 필드 fill-in (Task 6) |

**Residual risk:** T-15-15 (UAT 테스트 메일박스 노출) 는 accept — 본인 소유 계정 한정. T-15-17 은 Plan 01 의 masking 에 의존.
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Secret Manager `resend-from-email` 신규 version 추가 + read-back 검증</name>
  <files>— (외부 GCP Secret Manager, 코드 변경 없음)</files>
  <read_first>
    - 15-CONTEXT.md D-07 (정확한 gcloud 커맨드) + D-08 (sequence gate — Plan 02 Verified 확인 선행)
    - 15-RESEARCH.md §Pattern 2 L211-227 (stdin pipe 권장 — shell-portable)
    - 15-HUMAN-UAT.md Pre-conditions 섹션 (Plan 02 Task 3 PASS 확인)
    - 15-PATTERNS.md §"No Analog Found" (gcloud secrets versions add 예시값)
  </read_first>
  <what-built>
    본 task 는 gcloud CLI 실행 + Secret Manager read-back — 운영자 본인 로컬에서 실행. Claude 가 CLI 를 자동화할 수 있으나 자격증명 및 blast radius 때문에 checkpoint 로 분류.
  </what-built>
  <how-to-verify>
    **Pre-gate (절대 skip 금지):** 15-HUMAN-UAT.md Pre-conditions 섹션의 Plan 02 항목 3 개가 전부 PASS 여야 함. 특히 "Resend 대시보드 heygrabit.com Verified 전환 시각" 이 기록되어 있어야 함 (D-08).

    **Steps (운영자 수동):**
    1. 현재 `resend-from-email` secret 의 latest version 번호를 rollback pin 용으로 기록:
       ```bash
       gcloud secrets versions list resend-from-email \
         --project=grapit-491806 \
         --limit=5 \
         --format='table(name,state,createTime)'
       ```
       → 최상단 ENABLED version 번호를 15-HUMAN-UAT.md 의 "이전 version 번호 (rollback pin 용)" 필드에 기록. 예: 현재 latest 가 version 2 라면 rollback pin = `2`.

    2. 신규 version 추가 (stdin pipe — shell-portable, RESEARCH §Pattern 2):
       ```bash
       printf 'no-reply@heygrabit.com' | gcloud secrets versions add resend-from-email \
         --data-file=- \
         --project=grapit-491806
       ```
       - `printf` 사용 (echo 금지 — BSD/GNU 간 `-n` 해석 차이로 개행이 끼어들 수 있음)
       - `--data-file=-` 로 stdin 입력 (bash-only `<(process substitution)` 회피 — CONTEXT D-07 의 bash 문법은 zsh 에서도 동작하지만 운영 shell portable 로 stdin pipe 선호)
       - 출력 예시: `Created version [3] of the secret [resend-from-email].` — 신규 version 번호 기록

    3. 신규 version read-back 검증:
       ```bash
       gcloud secrets versions access latest \
         --secret=resend-from-email \
         --project=grapit-491806
       ```
       - 출력이 **정확히** `no-reply@heygrabit.com` 여야 함 (개행/공백/prefix 없음)
       - 만약 끝에 `%` 또는 개행이 보이면 Task 1 Step 2 를 `printf 'no-reply@heygrabit.com'` 의 quote 정확도로 다시 확인 (따옴표 안에 공백 없음)

    4. 구 version (rollback pin) 은 **삭제하지 않음** (D-07, D-15). Disabled 처리도 하지 않음 — latest 가 새 version 으로 자동 바뀌면서 기존 바인딩 `:latest` 는 새로 resolve 됨. Cloud Run 에는 아직 영향 없음 (Task 2 에서 revision update 할 때 비로소 적용).

    **Expected outcome:**
    - 신규 version 번호 (예: 3) 생성됨
    - read-back 출력이 `no-reply@heygrabit.com` 과 정확 일치
    - 이전 version (예: 2) 은 ENABLED 유지

    **⚠ FAIL 대응:**
    - read-back 출력이 예상값과 다르면 (개행, 공백 등) 동일 명령으로 재실행하여 또 다른 version 을 만들고, 잘못된 version 은 `gcloud secrets versions disable <잘못된_번호> --secret=resend-from-email --project=grapit-491806` 로 비활성화
    - permission denied 발생 시 gcloud 인증 (`gcloud auth login` + `gcloud config set project grapit-491806`) 확인
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud secrets versions access latest --secret=resend-from-email --project=grapit-491806` 출력이 정확히 `no-reply@heygrabit.com` (shell 에서 `| xxd -p | head` 등으로 trailing byte 확인 가능)
    - `gcloud secrets versions list resend-from-email --project=grapit-491806 --limit=5` 최상단 version 의 state = ENABLED
    - 이전 version 번호가 15-HUMAN-UAT.md 의 "이전 version 번호 (rollback pin 용)" 필드에 기록됨 (Task 6 에서 최종 반영)
  </acceptance_criteria>
  <resume-signal>
    "Secret 신규 version 추가 완료. 신규 version 번호: N, 이전 version (rollback pin): N-1, read-back 값 = no-reply@heygrabit.com"
  </resume-signal>
  <action>
    See `<what-built>` + `<how-to-verify>` above — 운영자가 로컬에서 `gcloud secrets versions list` 로 이전 latest version 번호 기록 (rollback pin 용), 이어서 `printf 'no-reply@heygrabit.com' | gcloud secrets versions add resend-from-email --data-file=- --project=grapit-491806` 실행. read-back 으로 `gcloud secrets versions access latest --secret=resend-from-email --project=grapit-491806` 가 정확히 `no-reply@heygrabit.com` 을 반환하는지 확인. 이전 version 은 삭제하지 않음 (D-07/D-15 rollback 창).
  </action>
  <verify>
    <automated>gcloud secrets versions access latest --secret=resend-from-email --project=grapit-491806</automated>
  </verify>
  <done>
    Secret Manager `resend-from-email` 에 신규 version (예: 3) 생성, 값 = `no-reply@heygrabit.com` (개행/공백 없음). 이전 version (rollback pin) 은 ENABLED 유지. 두 번호 모두 운영자가 기록.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Cloud Run `grabit-api` --update-secrets 재배포 + 신규 revision 100% traffic 확인</name>
  <files>— (외부 Cloud Run, 코드 변경 없음)</files>
  <read_first>
    - Task 1 의 신규 version 번호 (resume-signal)
    - 15-CONTEXT.md D-09, D-10
    - 15-RESEARCH.md §Pattern 2 (revision-start-time :latest resolution) + §R-05
    - .github/workflows/deploy.yml L118-124 (기존 RESEND_FROM_EMAIL 바인딩 문법 — 본 phase 는 이 파일 수정 금지, gcloud 로 직접 update)
  </read_first>
  <what-built>
    gcloud run services update — 운영자가 직접 실행.
  </what-built>
  <how-to-verify>
    **Steps (운영자 수동):**
    1. 재배포 실행 (D-09):
       ```bash
       gcloud run services update grabit-api \
         --region=asia-northeast3 \
         --project=grapit-491806 \
         --update-secrets RESEND_FROM_EMAIL=resend-from-email:latest
       ```
       - `--update-secrets` 는 다른 secret 바인딩 (DATABASE_URL, JWT_SECRET, REDIS_URL, INFOBIP_*, RESEND_API_KEY 등) 을 건드리지 않음 (partial update)
       - 동일 bindings 표현을 전달해도 Cloud Run 이 새 revision 을 강제 생성 (RESEARCH A4)
       - `:latest` 는 revision start 시 Task 1 의 신규 version 번호로 resolve 되어 lifetime 고정

    2. 실행 시각 기록 (ISO8601 KST) — 15-HUMAN-UAT.md "Cloud Run update 실행 시각"

    3. 1분 대기 후 신규 revision 상태 확인 (D-10):
       ```bash
       gcloud run services describe grabit-api \
         --region=asia-northeast3 \
         --project=grapit-491806 \
         --format='value(status.traffic[0].revisionName,status.traffic[0].percent,status.latestReadyRevisionName)'
       ```
       - 출력 3 필드: (revisionName, percent, latestReadyRevisionName)
       - **기대:** percent = 100, revisionName == latestReadyRevisionName (= 방금 만들어진 신규 revision)
       - revisionName 기록 → 15-HUMAN-UAT.md "신규 Cloud Run revision ID"

    4. 신규 revision 의 RESEND_FROM_EMAIL 바인딩 확인 (이중 검증):
       ```bash
       gcloud run revisions describe <신규_revision_name> \
         --region=asia-northeast3 \
         --project=grapit-491806 \
         --format='value(spec.containers[0].env)'
       ```
       또는
       ```bash
       gcloud run services describe grabit-api \
         --region=asia-northeast3 \
         --project=grapit-491806 \
         --format='json' | jq '.spec.template.spec.containers[0].env[] | select(.name=="RESEND_FROM_EMAIL")'
       ```
       - 출력에 `RESEND_FROM_EMAIL` 이 `secretKeyRef` 로 `resend-from-email:latest` 바인딩됨이 확인되어야 함
       - `:latest` 표기는 Cloud Run 의 bindings 표현이고 실제 revision 내부에서는 이미 Task 1 의 신규 version 번호로 resolve 된 상태

    5. 100% traffic 도달 시각 기록 → 15-HUMAN-UAT.md "100% traffic 도달 시각"

    **Expected outcome:**
    - `gcloud run services update` 성공, 신규 revision 생성
    - 새 revision 이 100% traffic (기존 revision 은 Serving 종료 — deploy.yml 의 기본 traffic policy 가 "latest 에 100%" 이므로 gradual rollout 아님)
    - RESEND_FROM_EMAIL 바인딩이 resend-from-email:latest

    **⚠ FAIL 대응:**
    - 100% 아니고 old revision 이 여전히 serving → 즉시 확인 후 재시도. 1분 더 대기로 해결되는 경우가 다수
    - `:latest` resolution 실패 의심 → Task 1 의 read-back 이 맞는지 재확인
    - revision 이 Not Ready (기동 실패, 예: env 검증으로 email.service.ts constructor throw) → Cloud Run Logs 확인. 가능성: Task 1 read-back 에 개행이 끼어 EMAIL_PATTERN 실패 → Rollback 필요 (D-15)
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud run services describe grabit-api --region=asia-northeast3 --project=grapit-491806 --format='value(status.traffic[0].percent)'` 출력이 `100`
    - `status.traffic[0].revisionName` == `status.latestReadyRevisionName` (동일 revision)
    - 신규 revision 의 env 에 `RESEND_FROM_EMAIL` = `resend-from-email:latest` (secretKeyRef) 바인딩 존재
    - 재배포 실행 시각 + 100% 도달 시각 기록 (운영자가 resume-signal 로 전달)
    - 서비스 Ready 상태 (기동 실패 없음)
  </acceptance_criteria>
  <resume-signal>
    "Cloud Run update 완료. 신규 revision ID: grabit-api-000NN-xxx, 100% traffic 도달 시각: YYYY-MM-DDTHH:MM:SS+09:00, RESEND_FROM_EMAIL=resend-from-email:latest 바인딩 확인"
  </resume-signal>
  <action>
    See `<what-built>` + `<how-to-verify>` above — 운영자가 `gcloud run services update grabit-api --region=asia-northeast3 --project=grapit-491806 --update-secrets RESEND_FROM_EMAIL=resend-from-email:latest` 실행. 1분 wait 후 `gcloud run services describe grabit-api --region=asia-northeast3 --project=grapit-491806 --format='value(status.traffic[0].revisionName,status.traffic[0].percent,status.latestReadyRevisionName)'` 로 신규 revision 이 100% traffic 받는지 확인 (percent=100, revisionName == latestReadyRevisionName). 추가로 revision env 에 RESEND_FROM_EMAIL=resend-from-email:latest 바인딩 확인.
  </action>
  <verify>
    <automated>gcloud run services describe grabit-api --region=asia-northeast3 --project=grapit-491806 --format='value(status.traffic[0].percent)'</automated>
  </verify>
  <done>
    grabit-api 신규 revision 이 100% traffic. RESEND_FROM_EMAIL 바인딩이 resend-from-email:latest (revision 내부적으로 Task 1 신규 version 에 resolve). revision ID + 100% 도달 시각 기록.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 3사 (Gmail + Naver + Daum/Kakao) 실기기 UAT — password-reset 이메일 inbox 수신 확인 (spam 아님)</name>
  <files>— (실기기 UAT, 코드 변경 없음)</files>
  <read_first>
    - 15-CONTEXT.md D-14 (locked 3사 조합 + spam 폴더 아님 조건)
    - 15-RESEARCH.md §R-02 (신규 도메인 reputation, Naver 특히 주의) + §Pitfall 5 (spam 폴더 혼동)
    - 15-HUMAN-UAT.md SC-1 체크리스트 (Plan 02 Task 4 에서 생성한 섹션)
    - Task 2 의 100% traffic 도달 시각 (UAT 는 그 이후에만 유효)
  </read_first>
  <what-built>
    실제 이메일 deliverability UAT — Claude 가 수행 불가. 운영자가 본인의 Gmail/Naver/Daum 계정으로 수행.
  </what-built>
  <how-to-verify>
    **Pre-gate:** Task 2 의 100% traffic 확인 완료 + 15분 이상 경과 (RESEARCH §Wave breakdown — old revision drain). 15분 미경과 시 일부 요청이 구 revision 으로 갈 수 있음 (false-negative 위험).

    **Steps (운영자 수동, 실기기):**
    1. 데스크탑 또는 모바일에서 https://heygrabit.com/auth/forgot-password 접속
    2. **Gmail UAT**: 본인의 Gmail 주소 입력 → "비밀번호 재설정 링크 받기" 클릭
       - 수신까지 최대 2~3분 대기
       - Gmail inbox (받은편지함) 에서 `[Grabit] 비밀번호 재설정` / from `no-reply@heygrabit.com` 수신 확인
       - **반드시 spam/정크 폴더도 확인** — inbox 에 없고 spam 에 있으면 FAIL
       - "자세히 보기" 헤더 확인 — SPF / DKIM / DMARC = PASS 표기
       - 수신 시각 기록 → 15-HUMAN-UAT.md SC-1 Gmail 체크리스트
    3. **Naver UAT**: 본인의 @naver.com 주소로 동일 플로우 반복
       - Naver 는 신규 도메인에 가장 엄격 — 최초 1~2 회는 spam 분류 가능 (reputation warmup)
       - spam 폴더 우선 확인
       - 수신 시각 기록
    4. **Daum 또는 Kakao UAT**: 본인의 @daum.net 또는 @kakao.com 주소로 동일 플로우 반복
       - Daum/Kakao inbox 수신 확인 + spam 분류 여부 확인
       - 수신 시각 기록

    5. 3사 중 하나라도 spam 분류 또는 미수신 (5분+ 지연) 시:
       - Resend 대시보드 → Logs / Emails 에서 해당 발송이 Sent 로 기록됐는지 확인
         - Sent 기록 있음 + inbox 없음 → 수신 측 ESP spam 분류 → SPF/DKIM alignment 재검토 (Plan 02 DNS 재확인)
         - Sent 기록 없음 → Resend API 호출 자체 실패 → gcloud logging read (Task 4) 로 확인 + rollback 결정
       - 실패 시 rollback 절차: 15-HUMAN-UAT.md Rollback 섹션의 `gcloud run services update ... --update-secrets RESEND_FROM_EMAIL=resend-from-email:<previous_version>` 실행
       - **Task 5 (grapit.com 제거) 실행 금지** — UAT PASS 전제

    **Expected outcome:** 3사 전부 inbox (not spam) 수신, from `no-reply@heygrabit.com`, subject `[Grabit] 비밀번호 재설정`

    **⚠ 시간 제약:** UAT 는 Task 2 100% traffic 도달 + 15분 이후에 시작. UAT 종료 후 Task 4 의 gcloud logging 을 실행하기 전에 최소 2분 대기 (로그 propagation).
  </how-to-verify>
  <acceptance_criteria>
    - Gmail inbox 수신 확인 (spam 아님) — 시각 기록
    - Naver inbox 수신 확인 (spam 아님) — 시각 기록
    - Daum 또는 Kakao inbox 수신 확인 (spam 아님) — 시각 기록
    - 3사 전부 from `no-reply@heygrabit.com` + subject `[Grabit] 비밀번호 재설정` 일치
    - 한 곳이라도 spam 분류 → FAIL → rollback 결정 후 Task 5 실행 금지
  </acceptance_criteria>
  <resume-signal>
    "3사 UAT 완료. Gmail: inbox OK (시각), Naver: inbox OK (시각), Daum/Kakao: inbox OK (시각). from/subject 정확 일치. spam 분류 없음." 또는 실패 시 "UAT FAIL — {상세}"
  </resume-signal>
  <action>
    See `<what-built>` + `<how-to-verify>` above — Task 2 의 100% traffic 도달 + 15분 drain 경과 후에만 실행. 운영자가 본인 Gmail/Naver/Daum(또는 Kakao) 3사 계정으로 각각 https://heygrabit.com/auth/forgot-password 에서 트리거. 각 inbox (받은편지함) 에서 subject `[Grabit] 비밀번호 재설정` / from `no-reply@heygrabit.com` 수신 확인. spam/정크 폴더도 반드시 확인 — spam 분류 시 FAIL. 3사 수신 시각 기록.
  </action>
  <verify>
    <automated>MISSING — 실제 이메일 deliverability 는 외부 ESP 의 spam filter 결정에 의존. 자동화 불가. 운영자가 3사 inbox 수신 + spam 미분류를 직접 확인 후 resume-signal 로 전달.</automated>
  </verify>
  <done>
    3사 (Gmail/Naver/Daum 또는 Kakao) 전부 inbox 수신, from/subject 일치, spam 아님. 한 곳이라도 spam → FAIL → Task 5 금지 + rollback 고려.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: gcloud Cloud Logging silent failure empty 확인 (D-13) + Sentry email-service 이벤트 관측</name>
  <files>— (gcloud logging + Sentry UI, 코드 변경 없음)</files>
  <read_first>
    - 15-CONTEXT.md D-13 (정확한 gcloud logging 쿼리)
    - 15-RESEARCH.md §Code Examples Example 3 + §R-07 (중복 신호 설계)
    - Task 3 의 UAT 종료 시각 (freshness=24h 이므로 상대 시각 무관하지만 최신 UAT 발송이 Resend 대시보드 Sent stat 에 카운트됐는지 확인)
  </read_first>
  <what-built>
    gcloud + Sentry 관측 — 운영자가 실행하여 결과를 기록.
  </what-built>
  <how-to-verify>
    **Steps (운영자 수동):**
    1. D-13 정확한 gcloud logging 쿼리 실행 (Task 3 UAT 종료 후 최소 2분 경과):
       ```bash
       gcloud logging read \
         'resource.type="cloud_run_revision" AND resource.labels.service_name="grabit-api" AND (textPayload:"Resend send failed" OR jsonPayload.message:"Resend send failed")' \
         --project=grapit-491806 \
         --freshness=24h \
         --limit=10
       ```
       - 기대: **empty 출력** (Task 2 재배포 이후 24h 내 Resend 실패 없음)
       - 만약 non-empty 이고 timestamp 가 Task 2 이전이면 무시 (구 revision 의 과거 실패, D-13 은 "최근 24h" 지만 Task 2 이전 이벤트는 cutover 이전 상태 반영)
       - Task 2 이후 timestamp 가 포함된 이벤트가 있으면 FAIL → Rollback

    2. Resend 대시보드 → Logs 또는 Emails → 최근 발송 확인 (D-13 의 "Resend 대시보드 Sent/Delivered stat 이 최근 UAT 발송을 기록" 조건):
       - Task 3 UAT 3건이 Sent 로 기록됨
       - Delivered 상태 확인 (Gmail/Naver/Daum 각 ESP 의 delivery webhook 으로 Resend 가 수신)

    3. Sentry `grabit-api` 프로젝트 확인 (https://sentry.io/ 운영자 본인 organization):
       - Issues → 최근 24h 필터
       - `tags:component:email-service` 필터 적용
       - Task 2 재배포 시각 이후 신규 이벤트 **0 건** 이어야 함 (PASS)
       - 만약 이벤트 존재 → 로그 확인. `provider:resend` tag + `Resend send failed: domain not verified` 메시지면 Plan 02 Verified 상태에 문제 → rollback
       - Task 2 이전의 이벤트 (grapit.com 구 revision 시점) 는 본 UAT 범위 밖 (참고 데이터)

    4. 시각 기록 → 15-HUMAN-UAT.md SC-2 체크리스트

    **Expected outcome:**
    - gcloud logging read empty (Task 2 이후 기준)
    - Resend 대시보드 Sent/Delivered stat 에 UAT 3건 기록
    - Sentry email-service 신규 이벤트 0 건

    **⚠ FAIL 대응:**
    - gcloud logging 에 Task 2 이후 이벤트 존재 → Secret 값 확인 (Task 1 read-back 재실행) + Plan 02 Verified 상태 재확인 → 필요 시 rollback
    - Resend 대시보드 Sent 없음 → API 호출 자체가 안 감 → Cloud Run revision 의 env 바인딩 재확인 (Task 2 Step 4)
  </how-to-verify>
  <acceptance_criteria>
    - gcloud logging read 결과가 Task 2 재배포 시각 이후로는 empty
    - Resend 대시보드 Sent stat 에 Task 3 UAT 발송 3건 기록
    - Sentry email-service 태그 신규 이벤트 0 건 (재배포 후 기준)
    - 시각들 15-HUMAN-UAT.md SC-2 에 기록
  </acceptance_criteria>
  <resume-signal>
    "D-13 검증 완료. gcloud logging empty (시각), Resend Sent stat 3건 확인, Sentry email-service 신규 0건."
  </resume-signal>
  <action>
    See `<what-built>` + `<how-to-verify>` above — Task 3 UAT 종료 후 2분 경과. 운영자가 `gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="grabit-api" AND (textPayload:"Resend send failed" OR jsonPayload.message:"Resend send failed")' --project=grapit-491806 --freshness=24h --limit=10` 실행하여 Task 2 재배포 이후 기준 empty 확인 (D-13). Resend 대시보드 Logs/Emails 에서 Task 3 UAT 3건이 Sent/Delivered 로 기록됐는지 확인. Sentry `grabit-api` 프로젝트에서 `tags:component:email-service` 필터로 재배포 후 신규 이벤트 0 건 확인.
  </action>
  <verify>
    <automated>gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="grabit-api" AND (textPayload:"Resend send failed" OR jsonPayload.message:"Resend send failed")' --project=grapit-491806 --freshness=24h --limit=10</automated>
  </verify>
  <done>
    gcloud logging empty (Task 2 이후 기준), Resend Sent stat 에 UAT 3건 기록, Sentry email-service 신규 이벤트 0 건. 시각들 15-HUMAN-UAT.md SC-2 에 기록.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Resend 대시보드에서 구 `grapit.com` 도메인 제거 (D-02, UAT 통과 후)</name>
  <files>— (외부 Resend UI, 코드 변경 없음)</files>
  <read_first>
    - 15-CONTEXT.md D-02 (UAT 통과 후 제거)
    - 15-RESEARCH.md §Pitfall 3 + AP-05 (너무 일찍 제거 금지)
    - Task 3 resume-signal — UAT 3사 PASS 확인
    - Task 4 resume-signal — D-13 PASS 확인
  </read_first>
  <what-built>
    Resend 대시보드 UI 클릭 — 운영자 수행.
  </what-built>
  <how-to-verify>
    **Pre-gate (절대 skip 금지):**
    - Task 3 (3사 UAT 전부 inbox 수신, spam 아님) = PASS
    - Task 4 (gcloud logging empty + Sentry 0 건) = PASS
    - 둘 중 하나라도 FAIL 이면 Task 5 실행 금지. 대신 rollback (Task 6 의 Rollback 섹션 참조)

    **Steps (운영자 수동):**
    1. https://resend.com/domains 접속, 로그인
    2. 도메인 목록에서 `grapit.com` 클릭 (또는 우측 메뉴)
    3. `Remove Domain` 또는 `Delete` 클릭
    4. 확인 프롬프트에서 domain 이름 입력하여 삭제 확정
    5. 제거 후 목록에 `grapit.com` 없음을 확인 — `heygrabit.com` 만 Verified 로 남아 있어야 함
    6. 제거 시각 기록 → 15-HUMAN-UAT.md Wave 3 Audit Log "Resend 구 grapit.com 도메인 제거 시각"

    **Expected outcome:** Resend 대시보드 Domains 목록이 heygrabit.com 1 개만 (Verified) — grapit.com 제거됨

    **Note:** grapit.com DNS 레코드 자체 (외부 DNS provider — 소유 여부 불확실, Phase 13 HANDOFF L248-250) 는 건드리지 않음. 본 task 는 Resend 측 상태만.
  </how-to-verify>
  <acceptance_criteria>
    - Resend 대시보드 Domains 목록에서 grapit.com 부재
    - heygrabit.com 은 여전히 Verified 상태 유지
    - 제거 시각 기록
  </acceptance_criteria>
  <resume-signal>
    "Resend grapit.com 도메인 제거 완료. 제거 시각 YYYY-MM-DDTHH:MM:SS+09:00. 목록에 heygrabit.com 만 Verified 로 남음."
  </resume-signal>
  <action>
    See `<what-built>` + `<how-to-verify>` above — Task 3 (3사 UAT PASS) + Task 4 (D-13 PASS) 전부 충족 후에만 실행. 운영자가 https://resend.com/domains 접속 → grapit.com → Remove Domain. 제거 후 Domains 목록에 heygrabit.com 만 Verified 로 남아있는지 확인. 제거 시각 기록. grapit.com 의 외부 DNS 자체는 건드리지 않음 (본 phase 범위 밖).
  </action>
  <verify>
    <automated>MISSING — Resend 대시보드 UI 조작. 운영자가 제거 후 목록 스크린샷/확인 보고로 검증.</automated>
  </verify>
  <done>
    Resend 대시보드 Domains 목록에서 grapit.com 부재. heygrabit.com 은 Verified 유지. 제거 시각 기록.
  </done>
</task>

<task type="auto">
  <name>Task 6: 15-HUMAN-UAT.md 의 Wave 3 Audit Log + Sign-off 섹션을 실제 값으로 fill-in</name>
  <files>.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md (Plan 02 Task 4 에서 생성된 파일의 현재 상태)
    - Task 1~5 의 resume-signal 에서 운영자가 전달한 값들 (신규 secret version 번호, 이전 version 번호, revision ID, UAT 수신 시각들, dig 결과, 제거 시각 등)
    - 15-CONTEXT.md D-16 (Audit trail 요구사항)
  </read_first>
  <action>
    본 task 는 실행자 (Claude) 가 15-HUMAN-UAT.md 파일을 Edit 으로 수정. Plan 02 Task 4 에서 생성된 템플릿의 플레이스홀더 (`__________`) 를 Task 1~5 의 resume-signal 에서 받은 실제 값으로 교체한다.

    **fill-in 대상 (Edit tool 로 순차 교체):**

    1. **Operational Audit Log → Wave 3 — Secret Manager / Cloud Run:**
       - `Secret Manager 신규 version 번호:` — Task 1 resume-signal 의 신규 version 번호 (예: `projects/grapit-491806/secrets/resend-from-email/versions/3` 또는 간단히 `3`)
       - `gcloud secrets versions access <N> --secret=resend-from-email 출력값:` — `no-reply@heygrabit.com` 확정
       - `이전 version 번호 (rollback pin 용):` — Task 1 에서 기록한 이전 latest (예: `2`)
       - `Cloud Run update 실행 시각:` — Task 2 resume-signal
       - `신규 Cloud Run revision ID:` — Task 2 resume-signal (예: `grabit-api-00019-abc`)
       - `gcloud run services describe traffic 100% 확인 시각:` — Task 2 resume-signal
       - `100% traffic 도달 시각:` — Task 2 resume-signal

    2. **Operational Audit Log → Wave 3 — UAT & cleanup:**
       - `3 사 UAT 수신 시각:` — SC-1 체크리스트 참조 (각 ESP 시각은 SC-1 에 기록)
       - `gcloud logging empty 확인 시각:` — Task 4 resume-signal (SC-2 에 기록)
       - `Resend 구 grapit.com 도메인 제거 시각:` — Task 5 resume-signal

    3. **SC-1 체크리스트:**
       - `Gmail inbox 수신 시각:` — Task 3 의 Gmail 시각
       - `Naver inbox 수신 시각:` — Task 3 의 Naver 시각
       - `Daum(또는 Kakao) inbox 수신 시각:` — Task 3 의 Daum 시각
       - spam 아님 체크박스 ✅ (3사 모두)

    4. **SC-2 체크리스트:**
       - `gcloud logging read 결과 empty 확인 시각:` — Task 4 resume-signal
       - `Sentry email-service 이벤트 0 건 확인 시각:` — Task 4 resume-signal

    5. **Sign-off 섹션:**
       - `- [x] Plan 02 Task 1/2/3 전부 PASS — heygrabit.com Verified`
       - `- [x] Plan 03 Task 1~5 전부 PASS — Secret rotation + Cloud Run update + UAT + grapit.com 제거`
       - `- [x] SC-1 체크리스트 PASS (3사 inbox 수신, spam 아님)`
       - `- [x] SC-2 체크리스트 PASS (gcloud logging empty + Sentry email-service 0 건)`
       - `- [x] 검증자: sangwopark19icons@gmail.com`
       - `- [x] 완료 날짜:` — Task 5 resume-signal 날짜

    6. **Pre-conditions 섹션**:
       - Wave 1 체크박스 (Plan 01 merged, pnpm test green, Cloud Run grabit-api Ready) → 현재 상태 반영하여 `[x]` 로 업데이트 (Plan 01 merge 여부는 본 Task 실행 시점에 확인)
       - Wave 2 Plan 02 체크박스 3개 → `[x]` 로 업데이트 (Plan 02 SUMMARY 기준)

    **변경 금지:**
    - Rollback 기준 섹션의 playbook 스크립트 리터럴 (D-15 준수)
    - Resend 발급 원본 레코드 표 (Plan 02 Task 1 이 기록) — 이미 있는 값이면 유지, 없으면 Plan 02 Task 4 가 누락한 것이므로 Task 6 에서 추가 가능
    - DMARC `rua=mailto:sangwopark19icons@gmail.com` 리터럴 (D-04)

    **if 특정 resume-signal 값이 누락됐으면:**
    - 플레이스홀더를 그대로 유지하지 말고, 명시적 `(pending — 운영자 기록 필요)` 주석으로 교체. 누락 필드 목록을 SUMMARY 에도 기록.

    **Edit 단위:** Plan 02 Task 4 에서 생성된 파일에 대해 `Edit` tool 을 사용하여 각 플레이스홀더 블록을 순차 교체. 파일 전체 rewrite 금지 (기존 Wave 2 섹션 손상 방지).
  </action>
  <verify>
    <automated>test -f .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md && ! grep -c '__________' .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - 15-HUMAN-UAT.md 의 `__________` 플레이스홀더가 Wave 3 관련 섹션에서 0개 (또는 명시적 `(pending — ...)` 주석으로 교체됨)
    - Sign-off 섹션의 체크박스가 모두 `[x]` (단, 명시적으로 fill-in 대상 항목만)
    - `grep -c 'grabit-api-' 15-HUMAN-UAT.md` ≥ 1 (신규 revision ID 기록 확인)
    - `grep -c 'no-reply@heygrabit.com' 15-HUMAN-UAT.md` ≥ 2 (from 주소 + Secret read-back 출력값 양쪽 기록)
    - DMARC `rua=mailto:sangwopark19icons@gmail.com` 리터럴 보존 (grep 확인)
    - Rollback 섹션의 playbook 스크립트 원형 보존
  </acceptance_criteria>
  <done>
    15-HUMAN-UAT.md 가 Wave 3 실행 결과를 반영하여 fill-in 완료. 운영 audit trail (D-16) 확보. Phase 15 ship 판정 근거 제공.
  </done>
</task>

</tasks>

<verification>
**Plan 03 전체 pass 조건 (순차):**
1. Task 1 Secret version 추가 + read-back = `no-reply@heygrabit.com` 확정
2. Task 2 Cloud Run 신규 revision 100% traffic + RESEND_FROM_EMAIL 바인딩 확인
3. Task 3 3사 UAT 전부 inbox (spam 아님) 수신
4. Task 4 gcloud logging empty + Sentry email-service 0 건
5. Task 5 Resend 대시보드에서 grapit.com 제거
6. Task 6 15-HUMAN-UAT.md Wave 3 섹션 fill-in 완료

**Phase 15 ship 판정:** 6 task 전부 PASS → STATE.md 업데이트 (Phase 15 = Complete), ROADMAP.md Phase 15 = [x].

**Rollback playbook (D-15):**
```bash
# Task 2 이후 어느 시점이든 문제 발생 시 (Task 3/4 의 FAIL 시나리오)
gcloud run services update grabit-api \
  --region=asia-northeast3 --project=grapit-491806 \
  --update-secrets RESEND_FROM_EMAIL=resend-from-email:<previous_version_from_Task1>
# 30~60초 rollover → 구 no-reply@grapit.com 복귀
# DNS revert 금지 (D-15)
# grapit.com 은 Task 5 가 실행되지 않았으므로 Resend 에 그대로 유지 → rollback 경로 유효
```
</verification>

<success_criteria>
- Secret Manager `resend-from-email` 신규 version payload = `no-reply@heygrabit.com` (must_have truth #1, CUTOVER-02)
- Cloud Run 신규 revision 100% traffic + 바인딩 확인 (must_have truth #2, CUTOVER-03)
- 3사 (Gmail/Naver/Daum 또는 Kakao) inbox 수신, spam 아님 (must_have truth #3, CUTOVER-05, D-14)
- gcloud logging `Resend send failed` empty (must_have truth #4, D-13)
- Resend 대시보드 grapit.com 제거 (must_have truth #5, D-02)
- 15-HUMAN-UAT.md Wave 3 Audit Log + Sign-off fill-in 완료 (must_have truth #6, D-16)
- Phase 13 UAT gap 9 해소 (`.planning/debug/password-reset-email-not-delivered-prod.md` Resolution 전면 실행)
</success_criteria>

<output>
완료 후 `.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-03-SUMMARY.md` 생성. SUMMARY 에 다음 기록:
- 신규 secret version 번호 + 이전 version (rollback pin)
- 신규 Cloud Run revision ID + 100% traffic 시각
- 3사 UAT 결과 요약 (PASS/FAIL + 시각)
- D-13 PASS 확인
- grapit.com 제거 시각
- Phase 15 ship 판정 (전체 PASS 여부)
</output>
