---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/auth/email/email.service.ts
  - apps/api/src/modules/auth/email/email.service.spec.ts
autonomous: true
requirements: [CUTOVER-04, CUTOVER-06]
must_haves:
  truths:
    - "email.service.ts 가 Resend error branch 에서 Sentry.captureException 을 정확히 1회 호출한다"
    - "기존 5 개 email.service.spec 테스트가 전부 green 으로 유지된다"
    - "Sentry scope context 에 full email address (`to` 전체) 는 들어가지 않는다 (PII masking)"
    - "Sentry captureException 에 전달되는 값이 Error 인스턴스이다 (stack trace 보전)"
  artifacts:
    - path: "apps/api/src/modules/auth/email/email.service.ts"
      provides: "Sentry.withScope + captureException 삽입된 error branch"
      contains: "Sentry.captureException"
    - path: "apps/api/src/modules/auth/email/email.service.spec.ts"
      provides: "Sentry mock + new assertion test case"
      contains: "__captureExceptionMock"
  key_links:
    - from: "apps/api/src/modules/auth/email/email.service.ts (error branch)"
      to: "@sentry/nestjs withScope/captureException"
      via: "namespace import"
      pattern: "import \\* as Sentry from '@sentry/nestjs'"
    - from: "apps/api/src/modules/auth/email/email.service.spec.ts"
      to: "vi.mock('@sentry/nestjs', ...)"
      via: "vi.mock hoisted before import"
      pattern: "vi\\.mock\\('@sentry/nestjs'"
---

<objective>
email.service.ts 의 Resend error branch 에 Sentry.withScope + captureException 을 삽입하여 silent failure 의 운영 관측성을 확보한다 (CONTEXT D-11). auth.service.ts fire-and-forget 구조는 수정 금지 (D-12, enumeration 방어). email.service.spec.ts 에 `@sentry/nestjs` 전역 mock 을 도입하고 기존 "PROD SDK error" 케이스를 확장하여 Sentry 호출을 assert 한다.

Purpose: Wave 2 의 운영 cutover 전에 관측 포인트를 미리 배포 (main merge → deploy.yml 자동 배포) 한다. Sentry 가 구 grapit.com 기반 revision 에서 먼저 활성화되면, Wave 2 를 실행해야 한다는 관측 신호(`domain not verified` 이벤트 포착 or 무이벤트 baseline) 로도 작동한다.

Output: 코드 2 파일 변경 + 기존 5 테스트 green 유지 + 새 어설션 1 개 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-CONTEXT.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-RESEARCH.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-PATTERNS.md
@.planning/debug/password-reset-email-not-delivered-prod.md

@apps/api/src/modules/auth/email/email.service.ts
@apps/api/src/modules/auth/email/email.service.spec.ts
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/instrument.ts

<interfaces>
<!-- Sentry 호출 site 에서 필요한 contract 들. PATTERNS.md §"Sentry namespace import" + §"withScope 호출 순서" 인용. -->

From @sentry/nestjs (namespace import 이미 4 파일에서 사용):
```typescript
// withScope callback 의 scope 객체는 다음 메서드를 가진다
interface Scope {
  setTag(key: string, value: string): void;
  setLevel(level: 'fatal' | 'error' | 'warning' | 'log' | 'info' | 'debug'): void;
  setContext(name: string, context: Record<string, unknown>): void;
}
export function withScope(callback: (scope: Scope) => void): void;
export function captureException(err: unknown): string; // event id; 본 phase 는 반환값 사용 안 함
```

From apps/api/src/modules/auth/email/email.service.ts (수정 대상 L68-82):
```typescript
// 기존 error branch (수정 전):
const { data, error } = await this.resend!.emails.send({ ... });
if (error) {
  this.logger.error(`Resend send failed for ${to}: ${error.message}`);
  return { success: false, error: error.message };
}

// this.from 은 생성자에서 확정된 non-null string (prod: hard-fail, dev: onboarding@resend.dev fallback)
```

From apps/api/src/modules/sms/sms.service.ts (L320-329, 참조 analog — 그대로 복제 금지, PII masking 은 email 에 맞춰 재작성):
```typescript
const country = e164.startsWith('+82') ? 'KR' : 'unknown';
Sentry.withScope((scope) => {
  scope.setTag('provider', 'infobip');
  scope.setTag('country', country);
  if (err instanceof InfobipApiError) {
    scope.setTag('http_status', String(err.status));
  }
  scope.setLevel('error');
  Sentry.captureException(err);
});
this.logger.error({ event: 'sms.send_failed', phone: e164, err: (err as Error).message });
```

From apps/api/src/instrument.ts (Sentry.init — 이미 완료, 추가 작업 불필요):
```typescript
import * as Sentry from '@sentry/nestjs';
Sentry.init({
  dsn: process.env.SENTRY_DSN,
  tracesSampleRate: 0.1,
  environment: process.env.NODE_ENV,
});
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email.service.ts → Sentry SDK | PII (`to` 수신자 이메일) 가 Sentry event payload 로 외부 서비스에 전송될 수 있는 경계. scope.setContext 에 전체 주소가 아닌 domain 만 기록 (PATTERNS.md §"PII masking") |
| email.service.ts → logger | this.logger.error 가 `to` 전체를 기록 (기존 동작, 변경 금지). GCP Cloud Logging 접근 권한 있는 운영자만 열람 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | I (Information Disclosure) | email.service.ts Sentry scope context | mitigate | `scope.setContext('email', { from: this.from, toDomain: to.split('@')[1] ?? 'unknown' })` — 로컬 파트 제외, 도메인만. sms.service.ts `country` tag 관행 계승. Task 1 action 에 리터럴 그대로 강제 |
| T-15-02 | R (Repudiation) | email.service.ts silent-return path | mitigate | Sentry.captureException 삽입으로 silent failure 에도 운영자가 감사 로그 확보. Task 1 에서 scope.setTag('component','email-service') + setTag('provider','resend') 로 필터링 보장 |
| T-15-03 | T (Tampering) | auth.service.ts:250 fire-and-forget semantics | accept (D-12) | 코드 수정 금지. 이 plan 의 acceptance_criteria 에 "auth.service.ts:225-251 diff 0 줄" 명시적 검증 포함 |
| T-15-04 | I (Information Disclosure) | email.service.spec.ts mock assertion 범위 | accept | spec 의 full-message 어설션은 테스트 코드 내부에만 존재, 운영 중 Sentry UI 에 노출되지 않음. Test fixture `rate limit exceeded` 는 실 PII 아님 |
| T-15-05 | D (Denial of Service) | Sentry SDK 예외로 인한 email.service.ts 흐름 차단 | mitigate | Sentry.withScope 는 throw 하지 않는 계약 (공식 문서). 그럼에도 불구하고 email error branch 는 기존대로 `return { success:false }` 를 반환해야 함. Task 1 에서 `Sentry` 호출 뒤 기존 return 문을 유지하여 흐름 보장 |

**Residual risk:** DMARC `rua=mailto:sangwopark19icons@gmail.com` 경로에 대한 Sentry 독립성 — 본 plan 범위 밖 (Wave 2 에서 DNS 로 처리). T-15-03 은 설계 의도대로 accept (enumeration 방어).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: email.service.ts Resend error branch 에 Sentry.withScope + captureException 삽입</name>
  <files>apps/api/src/modules/auth/email/email.service.ts</files>
  <read_first>
    - apps/api/src/modules/auth/email/email.service.ts (현재 L1-84 — namespace import 위치, error branch L77-80 정확한 줄 확인)
    - apps/api/src/modules/sms/sms.service.ts L6, L320-329 (namespace import 선례 + withScope 호출 순서 analog)
    - apps/api/src/instrument.ts (Sentry.init 이미 완료 — 추가 init 불필요)
    - 15-CONTEXT.md D-11, D-12 (정확한 삽입 지점 + auth.service.ts 수정 금지)
    - 15-PATTERNS.md §"Sentry namespace import" + §"withScope 호출 순서" + §"PII masking in Sentry context"
    - 15-RESEARCH.md §Pattern 1 (권장 구현) + §Anti-Patterns AP-01 (try/catch 래퍼 금지)
  </read_first>
  <behavior>
    - Test 1 (기존): DEV mode 에서는 Sentry.captureException 이 호출되지 않는다
    - Test 2 (기존): PROD + data success 경로에서는 Sentry.captureException 이 호출되지 않는다
    - Test 3 (신규, Task 2 에서 작성): PROD + Resend error 경로에서 Sentry.withScope 가 정확히 1회 호출되고, captureException 에 Error 인스턴스가 전달된다. 전달된 Error.message 는 `Resend send failed:` 접두어 + 원본 error.message (`rate limit exceeded`) 를 포함한다
    - Test 4 (신규, Task 2 에서 작성): PII masking — scope.setContext 에 전달되는 context 는 `{ from: this.from, toDomain: 'example.com' }` 형태이며 `to` 전체 주소 (`user@example.com`) 는 들어가지 않는다
  </behavior>
  <action>
    1. apps/api/src/modules/auth/email/email.service.ts L1-4 import 블록에 정확히 아래 한 줄을 추가 (resend import 다음 줄):
       ```typescript
       import * as Sentry from '@sentry/nestjs';
       ```
       (named import 금지, default import 금지 — 프로젝트 전체 4 곳 모두 namespace import 로 통일되어 있음)

    2. L77-80 의 기존 error branch 를 아래 블록으로 교체:
       ```typescript
       if (error) {
         this.logger.error(`Resend send failed for ${to}: ${error.message}`);
         // [Phase 15 D-11] Surface silent failure to Sentry — auth.service.ts:250 fire-and-forget
         // 경로가 return 값을 무시하므로 service-level capture 가 필요. PII masking: `to` 의
         // 로컬 파트는 기록하지 않고 도메인만 context 에 남긴다 (sms.service.ts country-tag 관행 계승).
         Sentry.withScope((scope) => {
           scope.setTag('component', 'email-service');
           scope.setTag('provider', 'resend');
           scope.setLevel('error');
           scope.setContext('email', {
             from: this.from,
             toDomain: to.split('@')[1] ?? 'unknown',
           });
           Sentry.captureException(new Error(`Resend send failed: ${error.message}`));
         });
         return { success: false, error: error.message };
       }
       ```

    3. 변경 금지 사항 (명시적):
       - try/catch 새로 추가 금지 (RESEARCH AP-01 — `Resend returns { data, error } — it does NOT throw` L68-69 주석 준수)
       - this.logger.error 위치 이동 금지 (D-11: "바로 다음 줄에 Sentry 호출")
       - return 문 수정 금지 (T-15-05 — 기존 `{ success: false, error: error.message }` 유지)
       - Sentry.captureException 에 원본 Resend `error` plain object 를 직접 넘기지 말 것 — `new Error(...)` 래핑 필수 (stack trace 품질)

    4. apps/api/src/modules/auth/auth.service.ts 는 어떤 수정도 금지 (D-12 — enumeration 방어 세맨틱 유지)
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- email.service.spec</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import \* as Sentry from '@sentry/nestjs'" apps/api/src/modules/auth/email/email.service.ts` 가 정확히 1 줄 매치
    - `grep -n "Sentry.withScope" apps/api/src/modules/auth/email/email.service.ts` 가 정확히 1 줄 매치
    - `grep -n "Sentry.captureException(new Error" apps/api/src/modules/auth/email/email.service.ts` 가 정확히 1 줄 매치
    - `grep -n "scope.setTag('component', 'email-service')" apps/api/src/modules/auth/email/email.service.ts` 가 정확히 1 줄 매치
    - `grep -n "scope.setTag('provider', 'resend')" apps/api/src/modules/auth/email/email.service.ts` 가 정확히 1 줄 매치
    - `grep -n "toDomain:" apps/api/src/modules/auth/email/email.service.ts` 가 정확히 1 줄 매치
    - `grep -c "try {" apps/api/src/modules/auth/email/email.service.ts` 결과가 0 (try/catch 래퍼 추가 없음)
    - `git diff apps/api/src/modules/auth/auth.service.ts` 가 empty (D-12 준수)
    - `pnpm --filter @grabit/api typecheck` exit 0
    - `pnpm --filter @grabit/api lint -- --no-fix apps/api/src/modules/auth/email/email.service.ts` exit 0
  </acceptance_criteria>
  <done>
    email.service.ts 의 error branch 에 Sentry.withScope + captureException + PII-masked context 가 삽입됨. auth.service.ts 및 다른 파일 변경 없음. typecheck/lint green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: email.service.spec.ts 에 @sentry/nestjs mock 추가 + Sentry 호출 어설션 테스트 2 개 추가</name>
  <files>apps/api/src/modules/auth/email/email.service.spec.ts</files>
  <read_first>
    - apps/api/src/modules/auth/email/email.service.spec.ts (전체 — 기존 6 test case + vi.mock('resend') 구조)
    - 15-PATTERNS.md §"신규 Sentry mock 권장 구조" (L167-193) + §"Assertion 예시" (L196-213)
    - 15-RESEARCH.md §Example 2 (vitest mock hoisting 주의사항) + §Assumptions Log A3
    - Task 1 에서 email.service.ts 에 삽입된 Sentry 호출 블록 (정확한 tag 이름, setContext name `email`, Error wrapping 리터럴)
  </read_first>
  <behavior>
    - 기존 6 테스트 (DEV mode / PROD send / PROD misconfig API_KEY / PROD misconfig FROM_EMAIL unset / PROD misconfig FROM_EMAIL invalid / PROD SDK error) 가 전부 green 유지
    - 신규 테스트 7 "PROD SDK error: Sentry.captureException called with Error wrapping Resend error.message": Sentry mock 이 captureException 호출을 1 회 기록, 전달 인자가 Error 인스턴스이며 message 가 `rate limit exceeded` 를 포함
    - 신규 테스트 8 "PROD SDK error: PII masking — setContext receives toDomain not full address": withScope 콜백이 setContext('email', {from, toDomain}) 만 호출하고 context 객체의 값이 `example.com` (도메인만) 이어야 하며 `user@example.com` (full) 이 아님
    - DEV mode (Test 1) 및 PROD success (Test 2) 에서는 captureException mock 이 호출되지 않음 — 어설션 추가 (회귀 방어)
  </behavior>
  <action>
    1. apps/api/src/modules/auth/email/email.service.spec.ts 파일 최상단 `vi.mock('resend', ...)` 블록 **바로 아래** 에 @sentry/nestjs mock 블록 추가:
       ```typescript
       vi.mock('@sentry/nestjs', () => {
         const captureExceptionMock = vi.fn();
         const scopeStub = {
           setTag: vi.fn(),
           setLevel: vi.fn(),
           setContext: vi.fn(),
         };
         const withScopeMock = vi.fn((cb: (scope: typeof scopeStub) => void) => {
           cb(scopeStub);
         });
         return {
           captureException: captureExceptionMock,
           withScope: withScopeMock,
           __captureExceptionMock: captureExceptionMock,
           __withScopeMock: withScopeMock,
           __scopeStub: scopeStub,
         };
       });

       import * as sentryModule from '@sentry/nestjs';
       ```
       (resend mock 과 동일 패턴 — hoisting 보장을 위해 파일 상단 배치, import 는 바로 아래)

    2. beforeEach 에 추가 (기존 `vi.clearAllMocks()` 만으로도 동작하지만 명시적으로 Sentry mock reset 포함):
       ```typescript
       const sentryMod = sentryModule as unknown as {
         __captureExceptionMock: ReturnType<typeof vi.fn>;
         __withScopeMock: ReturnType<typeof vi.fn>;
         __scopeStub: {
           setTag: ReturnType<typeof vi.fn>;
           setLevel: ReturnType<typeof vi.fn>;
           setContext: ReturnType<typeof vi.fn>;
         };
       };
       ```
       (describe 블록 내부, beforeEach 위쪽에 helper const 로 정의. 기존 `vi.clearAllMocks()` 가 모든 `vi.fn()` 을 reset 하므로 호출 카운트는 자동 초기화됨)

    3. 기존 Test 1 (DEV mode) 에 추가 어설션:
       ```typescript
       expect(sentryMod.__captureExceptionMock).not.toHaveBeenCalled();
       ```

    4. 기존 Test 2 (PROD mode success) 에 추가 어설션:
       ```typescript
       expect(sentryMod.__captureExceptionMock).not.toHaveBeenCalled();
       ```

    5. 기존 Test 6 (PROD SDK error) 는 변경하지 않음 (기존 `expect(result).toEqual({ success: false, error: 'rate limit exceeded' })` 유지).

    6. **신규 Test 7 추가** (Test 6 바로 뒤):
       ```typescript
       it('PROD SDK error: Sentry.captureException called with Error wrapping Resend error.message', async () => {
         const config = makeConfig({
           RESEND_API_KEY: 're_test_key',
           RESEND_FROM_EMAIL: 'no-reply@heygrabit.com',
           NODE_ENV: 'production',
         });
         const mod = resendModule as unknown as { __sendMock: ReturnType<typeof vi.fn> };
         mod.__sendMock.mockResolvedValueOnce({ data: null, error: { message: 'rate limit exceeded' } });

         const svc = new EmailService(config);
         await svc.sendPasswordResetEmail('user@example.com', 'https://app.test/reset?t=abc');

         expect(sentryMod.__withScopeMock).toHaveBeenCalledTimes(1);
         expect(sentryMod.__captureExceptionMock).toHaveBeenCalledTimes(1);
         const capturedArg = sentryMod.__captureExceptionMock.mock.calls[0]?.[0];
         expect(capturedArg).toBeInstanceOf(Error);
         expect((capturedArg as Error).message).toContain('Resend send failed');
         expect((capturedArg as Error).message).toContain('rate limit exceeded');
       });
       ```

    7. **신규 Test 8 추가** (Test 7 바로 뒤):
       ```typescript
       it('PROD SDK error: PII masking — setContext receives toDomain only, not full address', async () => {
         const config = makeConfig({
           RESEND_API_KEY: 're_test_key',
           RESEND_FROM_EMAIL: 'no-reply@heygrabit.com',
           NODE_ENV: 'production',
         });
         const mod = resendModule as unknown as { __sendMock: ReturnType<typeof vi.fn> };
         mod.__sendMock.mockResolvedValueOnce({ data: null, error: { message: 'boom' } });

         const svc = new EmailService(config);
         await svc.sendPasswordResetEmail('user@example.com', 'https://app.test/reset?t=abc');

         expect(sentryMod.__scopeStub.setContext).toHaveBeenCalledWith('email', {
           from: 'no-reply@heygrabit.com',
           toDomain: 'example.com',
         });
         // Negative assertion: full `to` 주소가 어느 setContext/setTag 호출에도 들어가지 않음
         const allSetContextCalls = sentryMod.__scopeStub.setContext.mock.calls;
         const allSetTagCalls = sentryMod.__scopeStub.setTag.mock.calls;
         const serialized = JSON.stringify([allSetContextCalls, allSetTagCalls]);
         expect(serialized).not.toContain('user@example.com');
         // Provider / component tag 확증
         expect(sentryMod.__scopeStub.setTag).toHaveBeenCalledWith('component', 'email-service');
         expect(sentryMod.__scopeStub.setTag).toHaveBeenCalledWith('provider', 'resend');
         expect(sentryMod.__scopeStub.setLevel).toHaveBeenCalledWith('error');
       });
       ```

    8. 변경 금지:
       - 기존 5 개 테스트의 어설션 (expect 본문) 수정 금지 — Test 1, Test 2 의 추가 어설션 외엔 건드리지 않음
       - makeConfig helper 변경 금지
       - afterEach 의 process.env restore 변경 금지
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- email.service.spec</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @grabit/api test -- email.service.spec` 가 8 개 테스트 전부 pass (기존 6 + 신규 2)
    - `grep -c "vi.mock('@sentry/nestjs'" apps/api/src/modules/auth/email/email.service.spec.ts` 결과가 1
    - `grep -c "__captureExceptionMock" apps/api/src/modules/auth/email/email.service.spec.ts` 결과가 4 이상 (mock 정의 1 + 외부 노출 1 + sentryMod 타입 단언 1 + 어설션 여러 곳)
    - `grep -c "toDomain: 'example.com'" apps/api/src/modules/auth/email/email.service.spec.ts` 결과가 1 (Test 8 의 exact match)
    - `grep -c "user@example.com" apps/api/src/modules/auth/email/email.service.spec.ts` 결과가 기존 수와 동일 (신규 테스트의 `svc.sendPasswordResetEmail('user@example.com', ...)` 2 회 추가만 허용 — PII negative assertion 내부 리터럴 계산 시 주의)
    - `pnpm --filter @grabit/api typecheck` exit 0
  </acceptance_criteria>
  <done>
    email.service.spec.ts 가 @sentry/nestjs mock 을 포함하고 Sentry 호출 / PII masking / Error wrapping 을 어설션하는 2 개 신규 테스트 포함. 기존 6 테스트 전부 green 유지. DEV/SUCCESS 경로에 not-called 어설션 추가로 회귀 방어.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @grabit/api test -- email.service.spec` 이 8 테스트 전부 green (실행 <15초)
2. `pnpm --filter @grabit/api test` 전체 스위트 green (기존 코드 회귀 없음, 실행 <90초)
3. `pnpm --filter @grabit/api typecheck` exit 0
4. `pnpm --filter @grabit/api lint` 에서 변경한 2 파일 경고/에러 0
5. `git diff apps/api/src/modules/auth/auth.service.ts` 빈 출력 (D-12 준수)
6. `grep` 기반 acceptance_criteria 전부 PASS
</verification>

<success_criteria>
- Sentry.withScope + captureException 이 email.service.ts Resend error branch 에 정확 1 회 호출되도록 배선됨 (must_have truth #1)
- PII (full email) 가 Sentry 로 전송되지 않음 (must_have truth #3, T-15-01 mitigation)
- Error 인스턴스 래핑으로 stack trace 보전 (must_have truth #4, §Pattern 1 근거)
- 기존 5 테스트 회귀 없음 (must_have truth #2)
- Main merge 시 deploy.yml 이 자동 재배포 → `grabit-api` 새 revision 이 Sentry 통합 코드로 동작 (secret 값은 아직 `no-reply@grapit.com` — Wave 2 에서 교체)
</success_criteria>

<output>
완료 후 `.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-01-SUMMARY.md` 생성.
</output>
