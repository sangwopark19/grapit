---
phase: 12
plan: 03.5
plan_number: 3.5
slug: minimap-smoke-test
status: complete
type: execute
wave: 3
depends_on: ["12-03"]
requirements: [UX-05]
created: 2026-04-21
completed: 2026-04-21
autonomous: false
---

# Plan 12-03.5 — MiniMap Dev-Server Smoke Test Gate

## Outcome

**User sign-off:** `"approved — 3 check all PASS"` (2026-04-21)

3가지 smoke test 모두 PASS. react-zoom-pan-pinch@3.7.0 내장 `MiniMap` export의 런타임 contract가 D-14 의도대로 작동함을 검증. Wave 4 (Plan 12-04) 진입 허가.

## Checks

| # | Check | Expected | Result |
|---|-------|----------|--------|
| 1 | 축소 SVG copy 렌더 | 좌상단 ~120px 박스 안에 `prefixSvgDefsIds(..., 'mini-')` 처리된 SVG copy가 정상 렌더링 (좌석 rect·tier 색상·`<defs>` 기반 색상) | PASS |
| 2 | Viewport rect 동기화 | Brand Purple (#6C3CE0) stroke rect가 메인 좌석맵의 pan/zoom에 실시간 동기. flicker/lag 없음 | PASS |
| 3 | 모바일 breakpoint 숨김 | `useIsMobile() === true` 상태(width < 768px)에서 MiniMap 미마운트 (DOM 부재) | PASS |

## Evidence

- Plan 12-03 Task 1 산출물: `apps/web/components/booking/seat-map-viewer.tsx` 데스크톱 분기에서 `<MiniMap width={120} borderColor="#6C3CE0" ...>` 마운트, children은 `prefixSvgDefsIds(processedSvg, 'mini-')` 처리된 SVG
- Plan 12-03 Task 1 helper: `apps/web/components/booking/__utils__/prefix-svg-defs-ids.ts` (W-2)
- Plan 12-00 scaffolding 테스트 `seat-map-viewer.test.tsx` `prefix-svg-defs-ids.test.ts` 모두 GREEN (136/136 vitest 통과)

## Reviews revision MED #5 Closure

Wave 3 ↔ Wave 4 사이 MiniMap 런타임 contract 검증 gate (12-REVIEWS.md Codex MED #5) 통과. 단위 테스트(`<div data-testid="minimap" />` mock)로는 검증 불가한 라이브러리 contract를 dev-server 육안 검증으로 확인 완료.

D-14 원안(수동 SVG 복제 + `onTransformed` 기반 viewport rect) fallback은 불필요.

## Next

Plan 12-04 Wave 4 manual QA gate 진입 (자동 회귀 + 11개 manual QA + D-19 security debt 공식 기록).
