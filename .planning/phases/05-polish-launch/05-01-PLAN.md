---
phase: 05-polish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/layout/mobile-tab-bar.tsx
  - apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx
  - apps/web/app/layout-shell.tsx
  - apps/web/components/layout/gnb.tsx
  - apps/web/app/page.tsx
  - apps/web/app/genre/[genre]/page.tsx
  - apps/web/app/performance/[id]/page.tsx
  - apps/web/app/search/page.tsx
  - apps/web/app/mypage/page.tsx
  - apps/web/app/mypage/reservations/[id]/page.tsx
  - apps/web/app/booking/[performanceId]/page.tsx
  - apps/web/app/booking/[performanceId]/confirm/page.tsx
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "모바일(< 768px)에서 하단 탭바가 보이고 데스크톱(>= 768px)에서 기존 GNB가 보인다"
    - "하단 탭바 4개 탭(홈, 카테고리, 검색, 마이페이지)이 모두 동작한다"
    - "모든 공개 페이지가 모바일 375px에서 레이아웃 깨짐 없이 렌더링된다"
    - "모든 터치 타겟이 최소 44x44px 크기를 갖는다"
    - "모바일에서 예매 내역 테이블이 카드형 리스트로 표시된다"
    - "공연 상세 모바일에서 포스터가 전체 폭으로 표시된다"
  artifacts:
    - path: "apps/web/components/layout/mobile-tab-bar.tsx"
      provides: "모바일 하단 탭바 컴포넌트"
      exports: ["MobileTabBar"]
    - path: "apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx"
      provides: "MobileTabBar 유닛 테스트"
      contains: "describe.*MobileTabBar"
    - path: "apps/web/app/layout-shell.tsx"
      provides: "MobileTabBar 통합 + 하단 패딩"
      contains: "MobileTabBar"
  key_links:
    - from: "apps/web/app/layout-shell.tsx"
      to: "apps/web/components/layout/mobile-tab-bar.tsx"
      via: "import MobileTabBar"
      pattern: "import.*MobileTabBar.*from"
    - from: "apps/web/components/layout/mobile-tab-bar.tsx"
      to: "usePathname()"
      via: "Next.js navigation hook"
      pattern: "usePathname"
---

<objective>
모바일 하단 탭바 생성 및 전체 공개 페이지의 모바일 반응형 레이아웃 적용.

Purpose: 사용자가 모바일 기기에서 서비스를 원활하게 이용할 수 있도록 터치 최적화된 하단 네비게이션과 반응형 레이아웃을 제공한다.
Output: MobileTabBar 컴포넌트, 수정된 LayoutShell, 전 페이지 모바일 반응형 CSS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-launch/05-CONTEXT.md
@.planning/phases/05-polish-launch/05-UI-SPEC.md
@.planning/phases/05-polish-launch/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/web/app/layout-shell.tsx:
```typescript
'use client';
import { usePathname } from 'next/navigation';
import { GNB } from '@/components/layout/gnb';
import { Footer } from '@/components/layout/footer';

export function LayoutShell({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const isAdmin = pathname.startsWith('/admin');
  const isBookingCheckout =
    pathname.startsWith('/booking') && !pathname.endsWith('/complete');
  const hideShell = isAdmin || isBookingCheckout;
  return (
    <>
      {!hideShell && <GNB />}
      <div className="flex flex-1 flex-col">{children}</div>
      {!hideShell && <Footer />}
    </>
  );
}
```

From apps/web/components/ui/skeleton.tsx:
```typescript
import { cn } from "@/lib"
function Skeleton({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div data-slot="skeleton" className={cn("animate-pulse rounded-md bg-accent", className)} {...props} />
  )
}
export { Skeleton }
```

From apps/web/app/globals.css @theme:
```css
--color-primary: #6C3CE0;
--color-gray-400: #A1A1AA;
--color-gray-500: #71717A;
--color-border: #E4E4E7;
```

Test pattern (from apps/web/components/booking/__tests__/seat-map-controls.test.tsx):
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MobileTabBar 컴포넌트 + 테스트 + LayoutShell 통합</name>
  <files>
    apps/web/components/layout/mobile-tab-bar.tsx,
    apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx,
    apps/web/app/layout-shell.tsx,
    apps/web/components/layout/gnb.tsx
  </files>
  <read_first>
    apps/web/app/layout-shell.tsx,
    apps/web/components/layout/gnb.tsx,
    apps/web/app/layout.tsx,
    apps/web/app/globals.css,
    apps/web/components/booking/__tests__/seat-map-controls.test.tsx,
    .planning/phases/05-polish-launch/05-UI-SPEC.md,
    .planning/phases/05-polish-launch/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: MobileTabBar renders 4 tabs with correct labels (홈, 카테고리, 검색, 마이페이지)
    - Test 2: Active tab uses primary color class when pathname matches href
    - Test 3: Inactive tabs use gray color classes
    - Test 4: Category tab has href="/genre/musical"
    - Test 5: Each tab has role="navigation" and active tab has aria-current="page"
    - Test 6: Component has md:hidden class (hidden on desktop)
  </behavior>
  <action>
    Per D-03: MobileTabBar 하단 탭바 컴포넌트를 생성한다.

    1. `apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx` 테스트 파일을 먼저 작성한다. vi.mock으로 `next/navigation`의 usePathname을 모킹한다. 기존 테스트 패턴(vitest + @testing-library/react)을 따른다.

    2. `apps/web/components/layout/mobile-tab-bar.tsx` 컴포넌트를 생성한다:
       - 'use client' 지시어 사용
       - lucide-react에서 Home, LayoutGrid, Search, User 아이콘 import
       - TABS 배열: [{ href: '/', label: '홈', icon: Home }, { href: '/genre/musical', label: '카테고리', icon: LayoutGrid }, { href: '/search', label: '검색', icon: Search }, { href: '/mypage', label: '마이페이지', icon: User }]
       - usePathname()으로 현재 경로 판별. isActive: href === '/'이면 pathname === '/' 정확 매치, 그 외 pathname.startsWith(href)
       - 최외곽 nav: role="navigation", className="fixed bottom-0 left-0 right-0 z-50 flex h-[56px] border-t border-border bg-white pb-safe md:hidden"
       - 각 탭: Link 컴포넌트, aria-current={active ? 'page' : undefined}, className에 flex min-h-[44px] flex-1 flex-col items-center justify-center gap-0.5
       - 활성 탭: text-primary, label font-semibold
       - 비활성 탭: 아이콘 text-gray-400, 라벨 text-gray-500 font-normal
       - 아이콘: h-5 w-5 (20px)
       - 라벨: text-caption (14px)

    3. `apps/web/app/layout-shell.tsx`를 수정한다:
       - MobileTabBar import 추가
       - GNB를 감싸는 div에 `hidden md:block` 추가하여 모바일에서 GNB 숨김
       - children wrapper div에 `pb-[56px] md:pb-0` 추가 (hideShell이 false일 때만)
       - Footer를 감싸는 div에 `hidden md:block` 추가하여 모바일에서 Footer 숨김
       - MobileTabBar를 hideShell이 false일 때만 렌더링

    4. `apps/web/components/layout/gnb.tsx`를 수정한다:
       - 기존 모바일 메뉴(MobileMenu, hamburger 버튼) 관련 코드를 모바일에서 사용하지 않으므로, md:hidden인 모바일 메뉴 트리거를 제거하거나 md 이상에서만 GNB가 표시되므로 그대로 둬도 됨. LayoutShell에서 GNB 자체를 hidden md:block으로 감싸므로 gnb.tsx 내부 변경은 최소화 -- MobileMenu import와 관련 state(isMobileMenuOpen, setIsMobileMenuOpen)를 제거하여 불필요한 코드 정리만 수행
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec vitest run components/layout/__tests__/mobile-tab-bar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/layout/mobile-tab-bar.tsx contains "export function MobileTabBar"
    - apps/web/components/layout/mobile-tab-bar.tsx contains "md:hidden"
    - apps/web/components/layout/mobile-tab-bar.tsx contains "min-h-[44px]"
    - apps/web/components/layout/mobile-tab-bar.tsx contains "role=\"navigation\""
    - apps/web/components/layout/mobile-tab-bar.tsx contains "aria-current"
    - apps/web/app/layout-shell.tsx contains "MobileTabBar"
    - apps/web/app/layout-shell.tsx contains "pb-[56px]"
    - apps/web/app/layout-shell.tsx contains "hidden md:block"
    - apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx exits 0
  </acceptance_criteria>
  <done>MobileTabBar가 4탭으로 렌더링되고 활성 탭이 primary 색상으로 표시됨. LayoutShell에서 모바일은 하단 탭바, 데스크톱은 GNB가 표시됨. 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 전체 공개 페이지 모바일 반응형 CSS 적용</name>
  <files>
    apps/web/app/page.tsx,
    apps/web/app/genre/[genre]/page.tsx,
    apps/web/app/performance/[id]/page.tsx,
    apps/web/app/search/page.tsx,
    apps/web/app/mypage/page.tsx,
    apps/web/app/mypage/reservations/[id]/page.tsx,
    apps/web/app/booking/[performanceId]/page.tsx,
    apps/web/app/booking/[performanceId]/confirm/page.tsx
  </files>
  <read_first>
    apps/web/app/page.tsx,
    apps/web/app/genre/[genre]/page.tsx,
    apps/web/app/performance/[id]/page.tsx,
    apps/web/app/search/page.tsx,
    apps/web/app/mypage/page.tsx,
    apps/web/app/mypage/reservations/[id]/page.tsx,
    apps/web/app/booking/[performanceId]/page.tsx,
    apps/web/app/booking/[performanceId]/confirm/page.tsx,
    .planning/phases/05-polish-launch/05-UI-SPEC.md
  </read_first>
  <action>
    Per D-01, D-02, D-04, D-05, D-06: 모든 공개 페이지에 모바일 반응형 Tailwind 클래스를 적용한다. Admin 페이지는 데스크톱 전용으로 변경 없음.

    **홈 페이지 (page.tsx) — per D-01:**
    - 배너 carousel: 이미 full width이므로 변경 최소화
    - 장르 grid: 모바일에서 `grid-cols-4 gap-3` (기존 데스크톱 레이아웃 유지하되 모바일 간격 축소)
    - Hot/New 섹션: 모바일에서 `overflow-x-auto snap-x snap-mandatory flex gap-3 px-4` 가로 스크롤
    - 섹션 padding: `px-4 md:px-6`

    **장르 페이지 (genre/[genre]/page.tsx) — per D-01:**
    - 공연 카드 grid: `grid-cols-2 gap-3 md:grid-cols-4 md:gap-4`
    - 서브카테고리 칩: `overflow-x-auto flex gap-2 px-4 md:px-0 md:flex-wrap`
    - 정렬 토글: `w-full md:w-auto`

    **공연 상세 (performance/[id]/page.tsx) — per D-05:**
    - 데스크톱: 2칸 레이아웃 (포스터 왼쪽, 정보 오른쪽) — 기존 유지
    - 모바일: 1칸 레이아웃. 포스터 `w-full aspect-[2/3] max-h-[400px] object-cover`. 정보 아래에 배치
    - 레이아웃 전환: `flex flex-col md:flex-row md:gap-8`
    - 탭 콘텐츠: `px-4 md:px-0`

    **검색 페이지 (search/page.tsx) — per D-01:**
    - 검색 입력: `w-full px-4 md:px-0`
    - 결과: `grid-cols-1 md:grid-cols-2 gap-4`
    - 필터 칩: `overflow-x-auto flex gap-2`

    **마이페이지 (mypage/page.tsx) — per D-02:**
    - 예매 내역 테이블: 데스크톱 유지. 모바일에서 테이블 대신 카드 리스트로 변환
    - 카드 리스트: `block md:hidden` / 테이블: `hidden md:block`
    - 각 카드: 공연명, 날짜, 좌석수, 상태 배지, 가격 표시. 탭하면 상세로 이동
    - 카드 터치 타겟: `min-h-[44px]` 보장

    **예매 상세 (mypage/reservations/[id]/page.tsx) — per D-02:**
    - 단일 컬럼 스택: `flex flex-col gap-4 px-4 md:px-0`

    **예매 좌석 선택 (booking/[performanceId]/page.tsx) — per D-06:**
    - 날짜/회차 선택: 모바일에서 접힘식 아코디언. Collapsible 또는 details/summary 사용
    - 기본 접힌 상태 (collapsed). 탭하면 캘린더 펼침
    - 좌석맵: `w-full` (패딩 없이 뷰포트 전체 폭)
    - 이미 seat-selection-sheet.tsx가 모바일 바텀시트로 존재하므로 활용

    **결제 확인 (booking/[performanceId]/confirm/page.tsx) — per D-02:**
    - 주문 요약: `flex flex-col gap-4 px-4 md:px-0`
    - 결제 위젯: `w-full`

    공통:
    - 모든 버튼/링크/인터랙티브 요소에 `min-h-[44px]` 또는 적절한 패딩으로 44px 터치 타겟 보장 (per D-04)
    - 모바일 기본 gutter: `px-4` (16px)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/performance/[id]/page.tsx contains "flex-col md:flex-row"
    - apps/web/app/performance/[id]/page.tsx contains "max-h-[400px]"
    - apps/web/app/genre/[genre]/page.tsx contains "grid-cols-2"
    - apps/web/app/mypage/page.tsx contains "md:hidden" (table/card toggle)
    - apps/web/app/mypage/page.tsx contains "block md:hidden" (mobile card list)
    - apps/web/app/search/page.tsx contains "grid-cols-1 md:grid-cols-2"
    - apps/web/app/booking/[performanceId]/page.tsx contains collapsible pattern for date/showtime
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>모든 공개 페이지가 모바일(375px)에서 레이아웃 깨짐 없이 렌더링됨. 포스터 전체폭, 카드형 예매내역, 접힘식 날짜 선택이 적용됨. TypeScript 빌드 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 렌더링 | 모바일/데스크톱 분기는 CSS 미디어쿼리로만 처리, 서버측 로직 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | MobileTabBar | accept | 네비게이션 링크만 포함, 민감 데이터 없음 |
| T-05-02 | Spoofing | aria-current | accept | CSS 표시 용도만, 인증/인가 로직 없음 |
</threat_model>

<verification>
1. `pnpm --filter @grapit/web exec vitest run components/layout/__tests__/mobile-tab-bar.test.tsx` — 통과
2. `pnpm --filter @grapit/web exec tsc --noEmit` — 타입 에러 없음
3. 모든 공개 페이지에서 md:hidden / hidden md:block 패턴으로 모바일/데스크톱 분기 확인
</verification>

<success_criteria>
- MobileTabBar 컴포넌트가 4탭으로 동작하고 활성 탭이 primary 색상으로 표시됨
- LayoutShell에서 모바일은 하단 탭바 + 하단 패딩, 데스크톱은 GNB + Footer
- 모든 공개 페이지가 모바일 375px에서 정상 렌더링 (포스터 전체폭, 카드형 리스트, 접힘식 선택기)
- 모든 터치 타겟 44px 이상
- TypeScript 빌드 통과
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-launch/05-01-SUMMARY.md`
</output>
