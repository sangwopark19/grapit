---
phase: 05-polish-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/skeletons/performance-card-skeleton.tsx
  - apps/web/components/skeletons/banner-skeleton.tsx
  - apps/web/components/skeletons/genre-grid-skeleton.tsx
  - apps/web/components/skeletons/section-skeleton.tsx
  - apps/web/components/skeletons/detail-header-skeleton.tsx
  - apps/web/components/skeletons/detail-tabs-skeleton.tsx
  - apps/web/components/skeletons/search-results-skeleton.tsx
  - apps/web/components/skeletons/reservation-list-skeleton.tsx
  - apps/web/components/skeletons/reservation-detail-skeleton.tsx
  - apps/web/components/skeletons/seat-map-skeleton.tsx
  - apps/web/components/skeletons/mypage-profile-skeleton.tsx
  - apps/web/components/skeletons/index.ts
  - apps/web/components/__tests__/skeleton-variants.test.tsx
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "API 데이터 로딩 중 해당 영역에 스켈레톤 placeholder가 표시된다"
    - "정적 영역(GNB, Footer, 탭 헤더)은 즉시 렌더링되고 스켈레톤이 아니다"
    - "데이터 도착 시 스켈레톤이 실제 콘텐츠로 교체된다"
    - "스켈레톤은 animate-pulse 애니메이션을 사용한다"
  artifacts:
    - path: "apps/web/components/skeletons/index.ts"
      provides: "모든 스켈레톤 variant의 barrel export"
      exports: ["PerformanceCardSkeleton", "BannerSkeleton", "GenreGridSkeleton", "SectionSkeleton", "DetailHeaderSkeleton", "DetailTabsSkeleton", "SearchResultsSkeleton", "ReservationListSkeleton", "ReservationDetailSkeleton", "SeatMapSkeleton", "MyPageProfileSkeleton"]
    - path: "apps/web/components/__tests__/skeleton-variants.test.tsx"
      provides: "스켈레톤 variant 유닛 테스트"
      contains: "describe.*Skeleton"
  key_links:
    - from: "apps/web/components/skeletons/*.tsx"
      to: "apps/web/components/ui/skeleton.tsx"
      via: "import Skeleton"
      pattern: "import.*Skeleton.*from.*ui/skeleton"
---

<objective>
11개 컴포넌트별 스켈레톤 variant를 생성하여 API 데이터 로딩 시 구조적 placeholder를 제공한다.

Purpose: 페이지 로딩 시 빈 화면이나 레이아웃 시프트 대신 콘텐츠 형태를 미리 보여줘 사용자 체감 속도를 높인다.
Output: 11개 스켈레톤 컴포넌트 + barrel export + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-launch/05-CONTEXT.md
@.planning/phases/05-polish-launch/05-UI-SPEC.md

<interfaces>
<!-- Executor uses existing Skeleton primitive from shadcn -->

From apps/web/components/ui/skeleton.tsx:
```typescript
import { cn } from "@/lib"
function Skeleton({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div data-slot="skeleton" className={cn("animate-pulse rounded-md bg-accent", className)} {...props} />
  )
}
export { Skeleton }
```

Usage pattern (from existing codebase):
```tsx
<Skeleton className="h-4 w-[200px]" />
<Skeleton className="aspect-[2/3] w-full" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 11개 스켈레톤 variant 컴포넌트 + barrel export + 테스트</name>
  <files>
    apps/web/components/skeletons/performance-card-skeleton.tsx,
    apps/web/components/skeletons/banner-skeleton.tsx,
    apps/web/components/skeletons/genre-grid-skeleton.tsx,
    apps/web/components/skeletons/section-skeleton.tsx,
    apps/web/components/skeletons/detail-header-skeleton.tsx,
    apps/web/components/skeletons/detail-tabs-skeleton.tsx,
    apps/web/components/skeletons/search-results-skeleton.tsx,
    apps/web/components/skeletons/reservation-list-skeleton.tsx,
    apps/web/components/skeletons/reservation-detail-skeleton.tsx,
    apps/web/components/skeletons/seat-map-skeleton.tsx,
    apps/web/components/skeletons/mypage-profile-skeleton.tsx,
    apps/web/components/skeletons/index.ts,
    apps/web/components/__tests__/skeleton-variants.test.tsx
  </files>
  <read_first>
    apps/web/components/ui/skeleton.tsx,
    apps/web/app/globals.css,
    apps/web/components/booking/__tests__/seat-map-controls.test.tsx,
    .planning/phases/05-polish-launch/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: PerformanceCardSkeleton renders poster rectangle (aspect-[2/3]) and 3 text line Skeletons
    - Test 2: BannerSkeleton renders full-width rectangle with aspect-[2.5/1]
    - Test 3: GenreGridSkeleton renders 8 rounded rectangles
    - Test 4: SectionSkeleton renders 4 horizontal card skeletons
    - Test 5: DetailHeaderSkeleton renders poster + 5 text lines
    - Test 6: ReservationListSkeleton renders 3 card skeletons
    - Test 7: All skeletons have aria-busy="true" attribute
    - Test 8: All skeleton containers have aria-label="콘텐츠를 불러오는 중입니다"
  </behavior>
  <action>
    Per D-07, D-08, D-09: 각 컴포넌트별 스켈레톤을 생성한다. 기존 shadcn `Skeleton` 컴포넌트를 조합하여 해당 컴포넌트의 레이아웃 형태를 재현한다.

    1. `apps/web/components/__tests__/skeleton-variants.test.tsx` 테스트를 먼저 작성한다.

    2. 11개 스켈레톤 컴포넌트를 각각 생성한다. 모든 컴포넌트는:
       - `Skeleton` from `@/components/ui/skeleton` 을 import
       - 최외곽 div에 `aria-busy="true"` 와 `aria-label="콘텐츠를 불러오는 중입니다"` 설정
       - export function {Name}Skeleton() 형태

    **각 variant 사양 (per UI-SPEC):**

    a. **PerformanceCardSkeleton**: 포스터 직사각형 `aspect-[2/3] w-full` + 3개 텍스트 라인 `h-4 w-[70%]`, `h-3 w-[50%]`, `h-3 w-[40%]` 간격 `space-y-2 mt-3`

    b. **BannerSkeleton**: 전체 폭 직사각형 `aspect-[2.5/1] w-full`

    c. **GenreGridSkeleton**: `grid grid-cols-4 gap-2` 안에 8개 `h-10 rounded-md` Skeleton

    d. **SectionSkeleton**: 제목줄 `h-6 w-[150px] mb-4` + `flex gap-3 overflow-hidden`으로 4개 가로 카드 (포스터 `w-[120px] aspect-[2/3]` + 2개 텍스트 `h-3`)

    e. **DetailHeaderSkeleton**: 포스터 `aspect-[2/3] w-[300px] md:w-[300px] w-full` + 오른쪽 5개 텍스트 라인 `h-6 w-[60%]`, `h-4 w-[40%]`, `h-4 w-[50%]`, `h-4 w-[45%]`, `h-4 w-[35%]` 간격 `space-y-3`. 모바일은 세로 배치 `flex flex-col md:flex-row gap-6`

    f. **DetailTabsSkeleton**: 탭 바 3개 `h-10 flex gap-2` + 콘텐츠 영역 4개 텍스트 라인 `h-4 w-full`, `h-4 w-[80%]`, `h-4 w-[60%]`, `h-4 w-[90%]` 간격 `space-y-3 mt-4`

    g. **SearchResultsSkeleton**: `space-y-4`로 6개 가로 카드 (포스터 `w-[80px] aspect-[2/3]` + 2개 텍스트) 배치

    h. **ReservationListSkeleton**: `space-y-3`으로 3개 카드 `h-[120px] w-full`

    i. **ReservationDetailSkeleton**: 헤더 라인 `h-6 w-[50%]` + 2개 정보 블록 `h-[200px] w-full` 간격 `space-y-4`

    j. **SeatMapSkeleton**: 좌석맵 `aspect-[4/3] w-full` + 사이드 패널 3개 텍스트 `h-4` 간격 `space-y-3`

    k. **MyPageProfileSkeleton**: 아바타 원 `h-16 w-16 rounded-full` + 3개 텍스트 라인

    3. `apps/web/components/skeletons/index.ts` barrel export 파일을 생성한다:
       ```typescript
       export { PerformanceCardSkeleton } from './performance-card-skeleton';
       export { BannerSkeleton } from './banner-skeleton';
       // ... 11개 모두 export
       ```
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec vitest run components/__tests__/skeleton-variants.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/skeletons/index.ts contains "export" for all 11 variants
    - apps/web/components/skeletons/performance-card-skeleton.tsx contains "aspect-[2/3]"
    - apps/web/components/skeletons/banner-skeleton.tsx contains "aspect-[2.5/1]"
    - apps/web/components/skeletons/genre-grid-skeleton.tsx contains "grid-cols-4"
    - apps/web/components/skeletons/reservation-list-skeleton.tsx contains "h-[120px]"
    - All skeleton files contain 'aria-busy="true"'
    - All skeleton files contain '콘텐츠를 불러오는 중입니다'
    - apps/web/components/__tests__/skeleton-variants.test.tsx exits 0
  </acceptance_criteria>
  <done>11개 스켈레톤 variant가 생성되고, 모두 aria-busy + aria-label 접근성 속성을 갖고, 테스트 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 | 스켈레톤은 순수 UI 표시용으로 데이터를 처리하지 않음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Skeleton variants | accept | 정적 placeholder만 렌더링, 사용자 데이터 미포함 |
</threat_model>

<verification>
1. `pnpm --filter @grapit/web exec vitest run components/__tests__/skeleton-variants.test.tsx` — 통과
2. 11개 스켈레톤 파일이 모두 `apps/web/components/skeletons/` 디렉토리에 존재
3. barrel export가 11개 variant를 모두 포함
</verification>

<success_criteria>
- 11개 스켈레톤 variant 파일이 생성되고 barrel export에서 접근 가능
- 모든 스켈레톤이 aria-busy="true"와 aria-label 접근성 속성을 포함
- 기존 shadcn Skeleton의 animate-pulse 스타일 사용
- 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-launch/05-02-SUMMARY.md`
</output>
