---
phase: 05-polish-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/error-messages.ts
  - apps/web/lib/api-client.ts
  - apps/web/components/layout/network-banner.tsx
  - apps/web/app/not-found.tsx
  - apps/web/app/error.tsx
  - apps/web/app/layout.tsx
  - apps/web/lib/__tests__/api-client.test.ts
  - apps/web/components/layout/__tests__/network-banner.test.tsx
  - apps/web/app/__tests__/not-found.test.tsx
autonomous: true
requirements:
  - INFR-03

must_haves:
  truths:
    - "API 에러 시 한국어 에러 메시지와 에러 코드(ERR-{status})가 sonner 토스트로 표시된다"
    - "네트워크 오프라인 시 화면 상단에 '인터넷 연결을 확인해주세요' 배너가 표시된다"
    - "존재하지 않는 URL 접근 시 404 페이지가 표시된다"
    - "글로벌 에러 페이지에 에러 코드가 표시된다"
    - "401 에러는 토스트 없이 로그인 페이지로 리다이렉트된다"
  artifacts:
    - path: "apps/web/lib/error-messages.ts"
      provides: "HTTP 상태별 한국어 에러 메시지 매핑"
      exports: ["STATUS_MESSAGES", "DEFAULT_ERROR_MESSAGE"]
    - path: "apps/web/lib/api-client.ts"
      provides: "에러 인터셉터가 추가된 API 클라이언트"
      contains: "toast.error"
    - path: "apps/web/components/layout/network-banner.tsx"
      provides: "네트워크 오프라인 배너"
      exports: ["NetworkBanner"]
    - path: "apps/web/app/not-found.tsx"
      provides: "커스텀 404 페이지"
      contains: "페이지를 찾을 수 없습니다"
    - path: "apps/web/app/error.tsx"
      provides: "개선된 글로벌 에러 페이지"
      contains: "ERR-"
  key_links:
    - from: "apps/web/lib/api-client.ts"
      to: "apps/web/lib/error-messages.ts"
      via: "import STATUS_MESSAGES"
      pattern: "import.*STATUS_MESSAGES.*from.*error-messages"
    - from: "apps/web/lib/api-client.ts"
      to: "sonner"
      via: "toast.error()"
      pattern: "toast\\.error"
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/components/layout/network-banner.tsx"
      via: "import NetworkBanner"
      pattern: "import.*NetworkBanner"
---

<objective>
API 에러 인터셉터, 네트워크 오프라인 배너, 404 페이지, 에러 코드 시스템을 구현하여 사용자 친화적 에러 UX를 제공한다.

Purpose: API 에러나 네트워크 문제 발생 시 사용자에게 명확한 한국어 안내와 복구 옵션을 제공한다. CS 문의 시 에러 코드로 빠른 추적이 가능하다.
Output: 에러 메시지 매핑, 향상된 api-client, NetworkBanner, 404 페이지, 개선된 error.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-launch/05-CONTEXT.md
@.planning/phases/05-polish-launch/05-UI-SPEC.md
@.planning/phases/05-polish-launch/05-RESEARCH.md

<interfaces>
<!-- Current api-client.ts that will be modified -->

From apps/web/lib/api-client.ts:
```typescript
import { useAuthStore } from '@/stores/use-auth-store';

const API_URL = process.env.NEXT_PUBLIC_API_URL || '';

class ApiClientError extends Error {
  statusCode: number;
  constructor(message: string, statusCode: number) {
    super(message);
    this.name = 'ApiClientError';
    this.statusCode = statusCode;
  }
}

// request() function throws ApiClientError on !res.ok
// 401 handling: silent refresh + retry, then redirect to /auth
// Non-401 errors: throw ApiClientError with server message or default
```

From apps/web/app/error.tsx:
```typescript
'use client';
import { useEffect } from 'react';
import { Button } from '@/components/ui/button';
export default function GlobalError({
  error, reset,
}: { error: Error & { digest?: string }; reset: () => void; }) {
  // Logs error, shows "문제가 발생했습니다" + "다시 시도" button
}
```

From apps/web/app/layout.tsx:
```typescript
import { Toaster } from '@/components/ui/sonner';
import { LayoutShell } from './layout-shell';
// Toaster is at the root level alongside LayoutShell
```

From apps/web/components/ui/sonner.tsx:
```typescript
// sonner Toaster configured with position and richColors
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 에러 메시지 매핑 + API 클라이언트 에러 인터셉터 + 테스트</name>
  <files>
    apps/web/lib/error-messages.ts,
    apps/web/lib/api-client.ts,
    apps/web/lib/__tests__/api-client.test.ts
  </files>
  <read_first>
    apps/web/lib/api-client.ts,
    apps/web/components/ui/sonner.tsx,
    apps/web/stores/use-auth-store.ts,
    apps/web/components/booking/__tests__/seat-map-controls.test.tsx,
    .planning/phases/05-polish-launch/05-UI-SPEC.md,
    .planning/phases/05-polish-launch/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: 400 에러 시 toast.error가 "잘못된 요청입니다."와 description "오류 코드: ERR-400"으로 호출된다
    - Test 2: 403 에러 시 toast.error가 "접근 권한이 없습니다."로 호출된다
    - Test 3: 404 에러 시 toast.error가 "요청하신 정보를 찾을 수 없습니다."로 호출된다
    - Test 4: 500 에러 시 toast.error가 서버 에러 메시지로 호출된다
    - Test 5: 401 에러 시 toast.error가 호출되지 않는다 (리다이렉트만)
    - Test 6: 서버가 커스텀 message를 반환하면 기본 메시지 대신 서버 메시지 사용
    - Test 7: toast.error의 duration이 5000ms로 설정된다
    - Test 8: STATUS_MESSAGES에 400, 403, 404, 408, 429 키가 존재한다
  </behavior>
  <action>
    Per D-10, D-12: API 에러 인터셉터를 구현한다.

    1. `apps/web/lib/__tests__/api-client.test.ts` 테스트를 먼저 작성한다:
       - vi.mock('sonner', () => ({ toast: { error: vi.fn() } }))
       - vi.mock('@/stores/use-auth-store') 로 useAuthStore.getState() 모킹
       - global.fetch를 vi.fn()으로 모킹
       - 각 HTTP 상태별 에러 응답을 모킹하여 toast.error 호출 검증

    2. `apps/web/lib/error-messages.ts` 파일을 생성한다:
       ```typescript
       export const STATUS_MESSAGES: Record<number, string> = {
         400: '잘못된 요청입니다.',
         403: '접근 권한이 없습니다.',
         404: '요청하신 정보를 찾을 수 없습니다.',
         408: '서버 응답이 지연되고 있습니다. 잠시 후 다시 시도해주세요.',
         429: '요청이 너무 많습니다. 잠시 후 다시 시도해주세요.',
       };
       export const DEFAULT_ERROR_MESSAGE = '일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요.';
       ```

    3. `apps/web/lib/api-client.ts`를 수정한다:
       - `import { toast } from 'sonner';` 추가
       - `import { STATUS_MESSAGES, DEFAULT_ERROR_MESSAGE } from './error-messages';` 추가
       - request() 함수의 `if (!res.ok)` 블록을 수정:
         ```typescript
         if (!res.ok) {
           const status = res.status;
           let errorMessage = STATUS_MESSAGES[status] ?? DEFAULT_ERROR_MESSAGE;
           try {
             const errorData = (await res.json()) as ApiError;
             if (errorData.message) errorMessage = errorData.message;
           } catch { /* use default */ }

           // 401은 위에서 이미 처리 (리다이렉트). 여기 도달하면 refresh 실패 후이므로 토스트 불필요
           if (status !== 401) {
             toast.error(errorMessage, {
               description: `오류 코드: ERR-${status}`,
               duration: 5000,
             });
           }

           throw new ApiClientError(errorMessage, status);
         }
         ```
       - 기존 에러 처리 로직의 default 메시지를 DEFAULT_ERROR_MESSAGE로 교체
       - 주의: 에러 토스트는 api-client에서만 표시. 컴포넌트 레벨 onError에서 추가 토스트 표시하면 중복 발생 (Pitfall 5 방지)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec vitest run lib/__tests__/api-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/error-messages.ts contains "export const STATUS_MESSAGES"
    - apps/web/lib/error-messages.ts contains "export const DEFAULT_ERROR_MESSAGE"
    - apps/web/lib/error-messages.ts contains "400" and "403" and "404" and "408" and "429"
    - apps/web/lib/api-client.ts contains "import { toast } from 'sonner'"
    - apps/web/lib/api-client.ts contains "import { STATUS_MESSAGES, DEFAULT_ERROR_MESSAGE } from './error-messages'"
    - apps/web/lib/api-client.ts contains "toast.error(errorMessage"
    - apps/web/lib/api-client.ts contains "오류 코드: ERR-"
    - apps/web/lib/api-client.ts contains "duration: 5000"
    - apps/web/lib/api-client.ts contains "status !== 401" (401 토스트 제외)
    - apps/web/lib/__tests__/api-client.test.ts exits 0
  </acceptance_criteria>
  <done>API 에러 시 한국어 메시지 + ERR-{status} 코드가 sonner 토스트로 5000ms 표시됨. 401은 토스트 없이 리다이렉트. 테스트 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetworkBanner + NotFoundPage + 글로벌 에러 페이지 개선 + 테스트</name>
  <files>
    apps/web/components/layout/network-banner.tsx,
    apps/web/app/not-found.tsx,
    apps/web/app/error.tsx,
    apps/web/app/layout.tsx,
    apps/web/components/layout/__tests__/network-banner.test.tsx,
    apps/web/app/__tests__/not-found.test.tsx
  </files>
  <read_first>
    apps/web/app/error.tsx,
    apps/web/app/layout.tsx,
    apps/web/app/globals.css,
    apps/web/components/ui/button.tsx,
    apps/web/components/booking/__tests__/seat-map-controls.test.tsx,
    .planning/phases/05-polish-launch/05-UI-SPEC.md,
    .planning/phases/05-polish-launch/05-RESEARCH.md
  </read_first>
  <behavior>
    NetworkBanner:
    - Test 1: navigator.onLine=false일 때 "인터넷 연결을 확인해주세요" 텍스트 렌더링
    - Test 2: navigator.onLine=true일 때 렌더링되지 않음 (null 반환)
    - Test 3: "다시 시도" 버튼이 존재함
    - Test 4: role="alert" 속성이 존재함
    - Test 5: aria-live="assertive" 속성이 존재함

    NotFoundPage:
    - Test 6: "페이지를 찾을 수 없습니다" 텍스트 렌더링
    - Test 7: "( ._.)" 텍스트 페이스 렌더링
    - Test 8: "홈으로 돌아가기" 링크가 href="/"를 가짐
    - Test 9: "요청하신 페이지가 존재하지 않거나 이동되었습니다." 텍스트 존재
  </behavior>
  <action>
    Per D-11, D-13: NetworkBanner와 NotFoundPage를 생성하고, 기존 error.tsx를 개선한다.

    1. 테스트 파일 먼저 작성:
       - `apps/web/components/layout/__tests__/network-banner.test.tsx`: navigator.onLine을 모킹. offline 이벤트 dispatch로 상태 변경 테스트.
       - `apps/web/app/__tests__/not-found.test.tsx`: next/link를 모킹. 정적 텍스트 렌더링 검증.

    2. `apps/web/components/layout/network-banner.tsx` 생성:
       ```typescript
       'use client';
       import { useEffect, useState } from 'react';

       export function NetworkBanner() {
         const [isOffline, setIsOffline] = useState(false);

         useEffect(() => {
           function handleOffline() { setIsOffline(true); }
           function handleOnline() { setIsOffline(false); }

           if (!navigator.onLine) setIsOffline(true);

           window.addEventListener('offline', handleOffline);
           window.addEventListener('online', handleOnline);
           return () => {
             window.removeEventListener('offline', handleOffline);
             window.removeEventListener('online', handleOnline);
           };
         }, []);

         if (!isOffline) return null;

         return (
           <div
             role="alert"
             aria-live="assertive"
             className="fixed top-0 left-0 right-0 z-[60] flex h-[44px] items-center justify-center gap-3 bg-error text-white"
           >
             <span className="text-caption font-semibold">인터넷 연결을 확인해주세요</span>
             <button
               onClick={() => window.location.reload()}
               className="rounded-md border border-white bg-transparent px-3 h-8 text-caption text-white"
             >
               다시 시도
             </button>
           </div>
         );
       }
       ```

    3. `apps/web/app/not-found.tsx` 생성 (per D-13):
       ```typescript
       import Link from 'next/link';
       import { Button } from '@/components/ui/button';

       export default function NotFound() {
         return (
           <main className="flex min-h-screen flex-col items-center justify-center gap-2 px-4">
             <p className="text-[48px] text-gray-400 mb-6">( ._.)</p>
             <h1 className="text-heading font-semibold text-gray-900">
               페이지를 찾을 수 없습니다
             </h1>
             <p className="text-base text-gray-500 mb-6">
               요청하신 페이지가 존재하지 않거나 이동되었습니다.
             </p>
             <Button asChild>
               <Link href="/">홈으로 돌아가기</Link>
             </Button>
           </main>
         );
       }
       ```
       - NOT a 'use client' component (server component로 충분)
       - GNB/Footer 없는 독립 페이지 (layout-shell에서 hideShell 처리 불필요, not-found.tsx는 LayoutShell 안에서 렌더링되지만 전체 화면 중앙 정렬이므로 문제없음)

    4. `apps/web/app/error.tsx` 개선:
       - 기존 구조 유지
       - ApiClientError에서 statusCode를 추출하여 에러 코드 표시 추가:
         ```typescript
         {error instanceof ApiClientError && (
           <p className="text-caption text-gray-500">
             오류 코드: ERR-{error.statusCode}
           </p>
         )}
         ```
       - ApiClientError를 import: `import { ApiClientError } from '@/lib/api-client';`
       - 기존 error.tsx가 이미 '문제가 발생했습니다' 제목과 '다시 시도' 버튼을 가지고 있으므로 에러 코드 p 태그만 추가

    5. `apps/web/app/layout.tsx` 수정:
       - `import { NetworkBanner } from '@/components/layout/network-banner';` 추가
       - `<Providers>` 안, `<LayoutShell>` 위에 `<NetworkBanner />` 추가
       - NetworkBanner는 fixed position이므로 layout 흐름에 영향 없음
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec vitest run components/layout/__tests__/network-banner.test.tsx app/__tests__/not-found.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/layout/network-banner.tsx contains "export function NetworkBanner"
    - apps/web/components/layout/network-banner.tsx contains 'role="alert"'
    - apps/web/components/layout/network-banner.tsx contains 'aria-live="assertive"'
    - apps/web/components/layout/network-banner.tsx contains "인터넷 연결을 확인해주세요"
    - apps/web/components/layout/network-banner.tsx contains "다시 시도"
    - apps/web/app/not-found.tsx contains "페이지를 찾을 수 없습니다"
    - apps/web/app/not-found.tsx contains "( ._.)"
    - apps/web/app/not-found.tsx contains "홈으로 돌아가기"
    - apps/web/app/not-found.tsx contains 'href="/"'
    - apps/web/app/error.tsx contains "ERR-"
    - apps/web/app/layout.tsx contains "NetworkBanner"
    - apps/web/components/layout/__tests__/network-banner.test.tsx exits 0
    - apps/web/app/__tests__/not-found.test.tsx exits 0
  </acceptance_criteria>
  <done>NetworkBanner가 오프라인 시 표시되고 온라인 복구 시 사라짐. 404 페이지가 친근한 이모지 + 한국어 안내 + 홈 버튼으로 표시됨. error.tsx에 에러 코드가 추가됨. 테스트 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API 에러 응답 -> 클라이언트 표시 | 서버 에러 메시지가 사용자에게 노출되는 경계 |
| navigator.onLine -> UI 상태 | 브라우저 네트워크 상태 감지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | api-client error interceptor | mitigate | 서버 내부 정보(스택 트레이스, DB 쿼리)를 사용자에게 노출하지 않음. STATUS_MESSAGES에 정의된 사용자 친화적 메시지만 표시. 서버가 반환하는 message 필드만 사용하고, 그 외 필드(stack, query 등) 무시 |
| T-05-05 | Information Disclosure | error.tsx | mitigate | error.digest만 표시 가능. statusCode는 ERR-{status} 형태로만 노출. error.message는 이미 api-client에서 필터링됨 |
| T-05-06 | Denial of Service | NetworkBanner reload | accept | window.location.reload()는 사용자가 직접 클릭해야 실행됨. 자동 재시도 없음 |
</threat_model>

<verification>
1. `pnpm --filter @grapit/web exec vitest run lib/__tests__/api-client.test.ts` — 통과
2. `pnpm --filter @grapit/web exec vitest run components/layout/__tests__/network-banner.test.tsx` — 통과
3. `pnpm --filter @grapit/web exec vitest run app/__tests__/not-found.test.tsx` — 통과
4. api-client.ts에서 toast.error 호출 시 항상 description에 ERR-{status} 포함
5. 401 에러에서는 toast.error가 호출되지 않음
</verification>

<success_criteria>
- API 에러 시 한국어 메시지 + ERR-{status} 에러 코드가 5초간 토스트 표시
- 네트워크 오프라인 시 상단 배너 표시, 온라인 복구 시 자동 소멸
- 존재하지 않는 URL 접근 시 ( ._.) + "페이지를 찾을 수 없습니다" + "홈으로 돌아가기" 버튼
- error.tsx에 에러 코드 표시 추가
- 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-launch/05-03-SUMMARY.md`
</output>
