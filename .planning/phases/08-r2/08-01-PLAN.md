---
phase: 08-r2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/admin/upload.service.ts
  - apps/api/src/modules/admin/upload.service.spec.ts
autonomous: true
requirements: [R2-01, R2-03]
must_haves:
  truths:
    - "S3Client가 forcePathStyle: true로 생성되어 R2 path-style URL을 사용한다"
    - "R2 모드에서 presigned URL이 정상 생성된다"
    - "publicUrl이 R2_PUBLIC_URL 환경변수 기반으로 생성된다"
  artifacts:
    - path: "apps/api/src/modules/admin/upload.service.ts"
      provides: "R2 호환 S3Client with forcePathStyle"
      contains: "forcePathStyle: true"
    - path: "apps/api/src/modules/admin/upload.service.spec.ts"
      provides: "forcePathStyle 검증 테스트"
      contains: "forcePathStyle"
  key_links:
    - from: "apps/api/src/modules/admin/upload.service.ts"
      to: "R2 bucket"
      via: "S3Client with forcePathStyle + path-style endpoint"
      pattern: "forcePathStyle:\\s*true"
---

<objective>
UploadService의 S3Client에 `forcePathStyle: true`를 추가하여 R2 path-style URL 호환성을 확보하고, 이를 검증하는 유닛 테스트를 추가한다.

Purpose: R2는 virtual-hosted-style URL을 지원하지 않으므로 forcePathStyle 설정이 누락되면 DNS 오류 또는 403이 발생한다. 프로덕션 R2 연동의 전제 조건이다.
Output: R2 호환 S3Client 설정이 완료된 upload.service.ts + forcePathStyle 검증 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-r2/08-CONTEXT.md
@.planning/phases/08-r2/08-RESEARCH.md

<interfaces>
<!-- UploadService 현재 S3Client 생성 코드 (upload.service.ts lines 30-40) -->
```typescript
this.s3 = new S3Client({
  region: 'auto',
  endpoint: `https://${accountId}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: config.get<string>('R2_ACCESS_KEY_ID', '') as string,
    secretAccessKey: config.get<string>('R2_SECRET_ACCESS_KEY', '') as string,
  },
});
```

<!-- UploadService 테스트 S3Client mock (upload.service.spec.ts lines 9-11) -->
```typescript
vi.mock('@aws-sdk/client-s3', () => ({
  S3Client: vi.fn().mockImplementation(() => ({})),
  PutObjectCommand: vi.fn().mockImplementation((input: Record<string, unknown>) => ({ input })),
}));
```

<!-- generatePresignedUrl 반환 타입 -->
```typescript
Promise<{
  uploadUrl: string;
  publicUrl: string;
  key: string;
  mode: 'local' | 'r2';
}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: S3Client forcePathStyle 추가 + 검증 테스트</name>
  <files>apps/api/src/modules/admin/upload.service.ts, apps/api/src/modules/admin/upload.service.spec.ts</files>
  <read_first>
    - apps/api/src/modules/admin/upload.service.ts (현재 S3Client 생성 코드 확인)
    - apps/api/src/modules/admin/upload.service.spec.ts (기존 테스트 패턴 확인)
    - .planning/phases/08-r2/08-RESEARCH.md (forcePathStyle 리서치 확인)
  </read_first>
  <behavior>
    - Test 1: S3Client 생성 시 forcePathStyle: true가 전달되는지 검증
    - Test 2: 기존 R2 모드 테스트가 모두 통과 (회귀 없음)
  </behavior>
  <action>
1. upload.service.spec.ts에 forcePathStyle 검증 테스트 추가 (RED):
   - "R2 mode" describe 블록 안에 새 테스트 추가:
     ```typescript
     it('should create S3Client with forcePathStyle: true', () => {
       const { S3Client } = require('@aws-sdk/client-s3');
       // S3Client mock이 호출된 첫 번째 인자에서 forcePathStyle 확인
       expect(S3Client).toHaveBeenCalledWith(
         expect.objectContaining({
           forcePathStyle: true,
           region: 'auto',
           endpoint: 'https://test-account-id.r2.cloudflarestorage.com',
         }),
       );
     });
     ```
   - 주의: S3Client mock은 vi.mock으로 이미 설정되어 있고, beforeEach에서 new UploadService() 호출 시 constructor에서 S3Client가 호출됨. mock의 호출 인자를 검증하는 방식.

2. 테스트 실행 (RED 확인): `pnpm --filter @grapit/api test -- upload.service` — forcePathStyle 테스트 실패 확인

3. upload.service.ts의 S3Client 생성 코드 수정 (GREEN):
   - Line 30-39 (constructor 내부 `this.s3 = new S3Client({...})` 블록)에 `forcePathStyle: true` 추가:
     ```typescript
     this.s3 = new S3Client({
       region: 'auto',
       endpoint: `https://${accountId}.r2.cloudflarestorage.com`,
       forcePathStyle: true,
       credentials: {
         accessKeyId: config.get<string>('R2_ACCESS_KEY_ID', '') as string,
         secretAccessKey: config.get<string>('R2_SECRET_ACCESS_KEY', '') as string,
       },
     });
     ```
   - 변경 위치: constructor의 else 블록 내부 S3Client 생성자 호출부
   - 다른 코드는 변경하지 않음

4. 테스트 재실행 (GREEN 확인): `pnpm --filter @grapit/api test -- upload.service` — 전체 통과
  </action>
  <verify>
    <automated>pnpm --filter @grapit/api test -- upload.service</automated>
  </verify>
  <acceptance_criteria>
    - upload.service.ts에 `forcePathStyle: true` 문자열이 존재: `grep -c "forcePathStyle: true" apps/api/src/modules/admin/upload.service.ts` 출력이 1
    - upload.service.spec.ts에 forcePathStyle 검증 테스트가 존재: `grep -c "forcePathStyle" apps/api/src/modules/admin/upload.service.spec.ts` 출력이 1 이상
    - 전체 upload.service 테스트 스위트 통과: `pnpm --filter @grapit/api test -- upload.service` exit code 0
  </acceptance_criteria>
  <done>S3Client에 forcePathStyle: true가 설정되고, 이를 검증하는 테스트가 통과한다. 기존 R2 모드/로컬 모드 테스트에 회귀가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> API (presigned URL 요청) | 인증된 관리자만 presigned URL 발급 가능 (기존 AdminGuard) |
| Browser -> R2 (PUT 업로드) | Presigned URL의 서명 기반 접근 제어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | S3Client credentials | mitigate | R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY를 GCP Secret Manager에 저장, Cloud Run secrets로 주입 (평문 env_vars 사용 금지) |
| T-08-02 | Tampering | Presigned URL | mitigate | presigned URL 600초 만료 (expiresIn: 600), ContentType 서버 지정으로 MIME spoofing 방지 |
| T-08-03 | Information Disclosure | forcePathStyle 미설정 | mitigate | forcePathStyle: true 추가로 virtual-hosted-style fallback 방지. 테스트로 검증 |
</threat_model>

<verification>
- `pnpm --filter @grapit/api test -- upload.service` 전체 통과
- `grep "forcePathStyle: true" apps/api/src/modules/admin/upload.service.ts` 결과 있음
</verification>

<success_criteria>
- S3Client 생성자에 forcePathStyle: true가 포함되어 R2 path-style URL 사용
- forcePathStyle 설정을 검증하는 유닛 테스트 존재 및 통과
- 기존 upload.service 테스트 전체 통과 (회귀 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/08-r2/08-01-SUMMARY.md`
</output>
