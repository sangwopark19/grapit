---
phase: 08-r2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/next.config.ts
  - apps/web/Dockerfile
  - .github/workflows/deploy.yml
autonomous: true
requirements: [R2-03, R2-04]
must_haves:
  truths:
    - "Next.js Image가 R2 공개 URL의 이미지를 프로덕션에서 로딩할 수 있다"
    - "NEXT_PUBLIC_R2_HOSTNAME 환경변수 교체만으로 r2.dev에서 cdn.grapit.kr로 전환 가능하다"
    - "GitHub Actions 배포 시 R2 시크릿이 Cloud Run에 주입된다"
    - "Web 빌드 시 NEXT_PUBLIC_R2_HOSTNAME이 Docker build arg로 전달된다"
  artifacts:
    - path: "apps/web/next.config.ts"
      provides: "R2 hostname 기반 동적 remotePatterns"
      contains: "NEXT_PUBLIC_R2_HOSTNAME"
    - path: "apps/web/Dockerfile"
      provides: "NEXT_PUBLIC_R2_HOSTNAME build arg"
      contains: "ARG NEXT_PUBLIC_R2_HOSTNAME"
    - path: ".github/workflows/deploy.yml"
      provides: "R2 시크릿 Cloud Run 주입 + web build arg"
      contains: "R2_ACCOUNT_ID"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "R2 public URL"
      via: "NEXT_PUBLIC_R2_HOSTNAME env var in remotePatterns"
      pattern: "NEXT_PUBLIC_R2_HOSTNAME"
    - from: ".github/workflows/deploy.yml"
      to: "Cloud Run secrets"
      via: "GCP Secret Manager references"
      pattern: "r2-account-id:latest"
    - from: "apps/web/Dockerfile"
      to: "next.config.ts"
      via: "build arg -> ENV"
      pattern: "ARG NEXT_PUBLIC_R2_HOSTNAME"
---

<objective>
Next.js remotePatterns를 환경변수 기반으로 동적 설정하고, Dockerfile에 build arg를 추가하고, GitHub Actions deploy.yml에 R2 시크릿 주입을 설정한다.

Purpose: 프로덕션 환경에서 R2에 업로드된 이미지가 Next.js Image 컴포넌트로 로딩되려면 remotePatterns에 R2 호스트네임이 등록되어야 한다. D-03에 따라 환경변수 기반으로 설정하여 도메인 전환 시 코드 변경 불필요. D-12에 따라 CI/CD에서 R2 시크릿을 Cloud Run에 주입한다.
Output: 환경변수 기반 remotePatterns + Dockerfile build arg + deploy.yml R2 시크릿 설정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-r2/08-CONTEXT.md
@.planning/phases/08-r2/08-RESEARCH.md

<interfaces>
<!-- next.config.ts 현재 images 설정 (lines 47-53) -->
```typescript
images: {
  unoptimized: process.env.NODE_ENV !== 'production',
  remotePatterns: [
    // Production: R2 CDN domain (add when R2 is configured)
    // { protocol: 'https', hostname: 'cdn.grapit.kr' },
  ],
},
```

<!-- Dockerfile 현재 build args (lines 5-8) -->
```dockerfile
ARG NEXT_PUBLIC_API_URL
ENV NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL
ARG NEXT_PUBLIC_WS_URL
ENV NEXT_PUBLIC_WS_URL=$NEXT_PUBLIC_WS_URL
```

<!-- deploy.yml deploy-api secrets 섹션 (lines 90-100) -->
```yaml
secrets: |
  DATABASE_URL=database-url:latest
  JWT_SECRET=jwt-secret:latest
  JWT_REFRESH_SECRET=jwt-refresh-secret:latest
  KAKAO_CLIENT_ID=kakao-client-id:latest
  KAKAO_CLIENT_SECRET=kakao-client-secret:latest
  NAVER_CLIENT_ID=naver-client-id:latest
  NAVER_CLIENT_SECRET=naver-client-secret:latest
  GOOGLE_CLIENT_ID=google-client-id:latest
  GOOGLE_CLIENT_SECRET=google-client-secret:latest
  REDIS_URL=redis-url:latest
```

<!-- deploy.yml deploy-web build args (lines 125-127) -->
```yaml
docker build -f apps/web/Dockerfile \
  --build-arg NEXT_PUBLIC_API_URL=https://grapit-api-d3c6wrfdbq-du.a.run.app \
  --build-arg NEXT_PUBLIC_WS_URL=https://grapit-api-d3c6wrfdbq-du.a.run.app \
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js remotePatterns 환경변수 기반 동적 설정 + Dockerfile build arg</name>
  <files>apps/web/next.config.ts, apps/web/Dockerfile</files>
  <read_first>
    - apps/web/next.config.ts (현재 images 설정 및 전체 config 구조)
    - apps/web/Dockerfile (현재 build arg 패턴)
    - .planning/phases/08-r2/08-RESEARCH.md (Pattern 2: remotePatterns 동적 설정 예시)
  </read_first>
  <action>
1. apps/web/next.config.ts 수정:
   - 파일 상단 (line 26 부근, `const nextConfig` 선언 직전)에 R2 hostname 변수 추가:
     ```typescript
     const r2Hostname = process.env.NEXT_PUBLIC_R2_HOSTNAME;
     ```
   - images.remotePatterns 블록 (line 49-52)을 다음으로 교체:
     ```typescript
     remotePatterns: [
       ...(r2Hostname
         ? [{ protocol: 'https' as const, hostname: r2Hostname }]
         : []),
     ],
     ```
   - 기존 주석 처리된 `// { protocol: 'https', hostname: 'cdn.grapit.kr' }` 라인 제거
   - 다른 설정 (output, rewrites, transpilePackages 등)은 변경하지 않음

2. apps/web/Dockerfile 수정:
   - Stage 1 (builder) 에서 기존 build arg들 (NEXT_PUBLIC_API_URL, NEXT_PUBLIC_WS_URL) 다음에 추가:
     ```dockerfile
     ARG NEXT_PUBLIC_R2_HOSTNAME
     ENV NEXT_PUBLIC_R2_HOSTNAME=$NEXT_PUBLIC_R2_HOSTNAME
     ```
   - 위치: line 8 (`ENV NEXT_PUBLIC_WS_URL=...`) 다음
   - Stage 2 (runner)에는 추가 불필요 — 빌드 타임에만 사용되는 변수 (Next.js가 빌드 시 인라인)
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_R2_HOSTNAME" apps/web/next.config.ts apps/web/Dockerfile && pnpm --filter @grapit/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts에 `NEXT_PUBLIC_R2_HOSTNAME` 문자열 존재: `grep -c "NEXT_PUBLIC_R2_HOSTNAME" apps/web/next.config.ts` 출력 1 이상
    - next.config.ts에 `r2Hostname` 변수와 spread 조건부 패턴 존재: `grep "r2Hostname" apps/web/next.config.ts` 결과 2줄 이상
    - next.config.ts에 주석 처리된 cdn.grapit.kr 라인이 제거됨: `grep "cdn.grapit.kr" apps/web/next.config.ts` 결과 없음
    - Dockerfile에 `ARG NEXT_PUBLIC_R2_HOSTNAME` 존재: `grep -c "ARG NEXT_PUBLIC_R2_HOSTNAME" apps/web/Dockerfile` 출력 1
    - Dockerfile에 `ENV NEXT_PUBLIC_R2_HOSTNAME` 존재: `grep -c "ENV NEXT_PUBLIC_R2_HOSTNAME" apps/web/Dockerfile` 출력 1
    - typecheck 통과: `pnpm --filter @grapit/web typecheck` exit code 0
  </acceptance_criteria>
  <done>next.config.ts의 remotePatterns가 NEXT_PUBLIC_R2_HOSTNAME 환경변수 기반으로 동적 설정되고, Dockerfile에 해당 build arg가 추가되어 빌드 시 주입 가능하다. D-03 완전 구현.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions deploy.yml R2 시크릿 주입</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (현재 secrets 패턴 및 web build args 확인)
    - .planning/phases/08-r2/08-RESEARCH.md (Pattern 3: deploy.yml R2 시크릿 추가 예시)
  </read_first>
  <action>
1. deploy-api job의 secrets 섹션 (line 90-100) 끝에 R2 시크릿 5개 추가:
   - 기존 `REDIS_URL=redis-url:latest` 다음에:
     ```yaml
     R2_ACCOUNT_ID=r2-account-id:latest
     R2_BUCKET_NAME=r2-bucket-name:latest
     R2_PUBLIC_URL=r2-public-url:latest
     R2_ACCESS_KEY_ID=r2-access-key-id:latest
     R2_SECRET_ACCESS_KEY=r2-secret-access-key:latest
     ```
   - GCP Secret Manager 시크릿 이름은 kebab-case (현재 패턴: `database-url`, `jwt-secret` 등과 동일)
   - 시크릿은 `secrets:` 섹션에 배치 (평문 `env_vars:`가 아님 — T-08-01 위협 완화)

2. deploy-web job의 docker build 커맨드 (line 125-128)에 R2 hostname build arg 추가:
   - 기존 `--build-arg NEXT_PUBLIC_WS_URL=...` 다음에:
     ```yaml
     --build-arg NEXT_PUBLIC_R2_HOSTNAME=${{ secrets.R2_PUBLIC_HOSTNAME }} \
     ```
   - 주의: GitHub secret 이름은 `R2_PUBLIC_HOSTNAME`으로 r2.dev 서브도메인의 hostname 부분만 저장 (예: `pub-xxxx.r2.dev`)
   - 이 값은 Next.js remotePatterns hostname으로 사용됨 (protocol 제외한 hostname만 필요)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && grep -c "r2-account-id" .github/workflows/deploy.yml && grep -c "R2_PUBLIC_HOSTNAME" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - deploy.yml secrets에 R2 시크릿 5개 존재: `grep -c "r2-.*:latest" .github/workflows/deploy.yml` 출력 5
    - deploy.yml web build에 R2_PUBLIC_HOSTNAME arg 존재: `grep -c "R2_PUBLIC_HOSTNAME" .github/workflows/deploy.yml` 출력 1 이상
    - R2 credentials가 secrets 섹션에 위치 (env_vars 아님): `grep -A1 "secrets:" .github/workflows/deploy.yml | grep -c "R2_ACCESS_KEY_ID"` — 간접 확인
    - YAML 문법 유효: deploy.yml 파일이 파싱 가능한 YAML (들여쓰기 확인)
  </acceptance_criteria>
  <done>GitHub Actions deploy.yml에 R2 시크릿 5개가 Cloud Run secrets로 주입되고, Web 빌드 시 NEXT_PUBLIC_R2_HOSTNAME이 build arg로 전달된다. D-11, D-12 완전 구현.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> GCP Secret Manager | OIDC 기반 인증, 시크릿 참조 |
| Cloud Run env -> Application | 런타임 시크릿 주입 |
| Docker build arg -> Next.js bundle | 빌드 타임 환경변수 인라인 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | deploy.yml R2 credentials | mitigate | R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY를 `secrets:` 섹션에 배치 (평문 `env_vars:` 금지). GCP Secret Manager에 저장 |
| T-08-05 | Information Disclosure | NEXT_PUBLIC_R2_HOSTNAME | accept | 공개 URL의 hostname이므로 build arg로 노출되어도 무방. R2 공개 접근이 의도된 설계 |
| T-08-06 | Tampering | remotePatterns bypass | accept | remotePatterns는 Next.js Image 최적화 프록시의 허용 목록. 우회해도 원본 이미지에 직접 접근할 뿐 보안 위험 없음 |
</threat_model>

<verification>
- `grep "NEXT_PUBLIC_R2_HOSTNAME" apps/web/next.config.ts` 결과 존재
- `grep "ARG NEXT_PUBLIC_R2_HOSTNAME" apps/web/Dockerfile` 결과 존재
- `grep "r2-account-id:latest" .github/workflows/deploy.yml` 결과 존재
- `grep "R2_PUBLIC_HOSTNAME" .github/workflows/deploy.yml` 결과 존재
- `pnpm --filter @grapit/web typecheck` 통과
</verification>

<success_criteria>
- Next.js remotePatterns가 NEXT_PUBLIC_R2_HOSTNAME 환경변수 기반으로 동적 설정
- Dockerfile에 NEXT_PUBLIC_R2_HOSTNAME build arg 존재
- deploy.yml에 R2 시크릿 5개가 secrets 섹션에 추가
- deploy.yml web 빌드에 R2_PUBLIC_HOSTNAME build arg 전달
- 환경변수 교체만으로 r2.dev -> cdn.grapit.kr 전환 가능한 구조
</success_criteria>

<output>
After completion, create `.planning/phases/08-r2/08-02-SUMMARY.md`
</output>
