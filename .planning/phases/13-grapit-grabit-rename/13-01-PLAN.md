---
phase: 13-grapit-grabit-rename
plan: 01
type: execute
wave: 1
depends_on: []
revision: 2
revision_notes: "Revision 2: D-10 from codex REVIEWS — STEP 0 inventory + multi-pattern bulk rename + expanded files_modified + line-level allowlist (4 entries); D-16 rg pipe safety; LOW #11 active-phase exception"
files_modified:
  - package.json
  - apps/api/package.json
  - apps/web/package.json
  - packages/shared/package.json
  - pnpm-lock.yaml
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/vitest.config.ts
  - apps/api/vitest.integration.config.ts
  - apps/api/Dockerfile
  - apps/web/Dockerfile
  - docker-compose.yml
  - .github/workflows/ci.yml
  - scripts/provision-valkey.sh
  - apps/api/src/database/seed.mjs
  - apps/web/e2e/helpers/auth.ts
  - apps/web/e2e/signup-sms.spec.ts
  - apps/api/src/modules/auth/email/email.service.spec.ts
  - apps/api/src/modules/auth/auth.service.ts
  - apps/api/test/admin-dashboard.integration.spec.ts
  - apps/api/src/modules/admin/upload.service.spec.ts
  - apps/api/src/modules/auth/email/templates/password-reset.tsx
  - apps/web/e2e/admin-dashboard.spec.ts
  - CLAUDE.md
  - AGENTS.md
  - docs/03-ARCHITECTURE.md
  - docs/06-KAKAO-OAUTH-SETUP.md
  - docs/PLANNING-REVIEW.md
  - arch/06-VERCEL-RESEARCH.md
  - arch/10-INFRA-DECISION-PROPOSAL.md
  - arch/11-INFRA-RESEARCH-SUMMARY.md
autonomous: true
requirements:
  - SC-1
  - SC-4
user_setup: []

must_haves:
  truths:
    - "루트 + 3개 하위 workspace package의 name 필드가 모두 @grabit/*로 변경됨 (D-01 grapit_dev password 제외)"
    - "모든 TypeScript/JavaScript import 문이 @grapit/shared → @grabit/shared로 치환됨"
    - "pnpm install --frozen-lockfile이 exit 0으로 성공 (lockfile drift 없음)"
    - "pnpm lint, pnpm typecheck, pnpm test가 모두 green (SC-1 달성)"
    - "docker-compose.yml이 container_name=grabit-postgres, POSTGRES_DB=grabit, POSTGRES_USER=grabit으로 변경되되 POSTGRES_PASSWORD=grapit_dev는 D-01 예외로 유지"
    - "ci.yml의 POSTGRES_DB / DATABASE_URL / pnpm --filter / admin@ email이 grabit_test + admin@grabit.test로 변경 (D-06)"
    - "seed.mjs admin 이메일이 admin@grabit.test로 변경 + TEST_USER_EMAIL GitHub secret 갱신 체크리스트 존재"
    - "fixture/test 이메일이 @heygrabit.com (D-07) / @grabit.test (dev) / @e2e.grabit.dev / @social.grabit.com으로 변경"
    - "scripts/provision-valkey.sh 변수명(INSTANCE_NAME/POLICY_NAME)이 grabit-*로 변경되되 grapit-cloudrun@ SA는 D-05로 유지 + '재실행 금지' 주석 추가"
    - "CLAUDE.md/AGENTS.md/docs/*/arch/*의 @grapit/* 스코프 + Grapit 단어 경계 + grapit-{web,api} 서비스명이 grabit으로 치환되되 CLAUDE.md L232 /grapit/ 로컬 경로는 D-03으로 유지"
    - "D-10 (Revision 2) 누락 파일 4개 포함: admin-dashboard.integration.spec.ts (grapit_test 참조), upload.service.spec.ts (grapit-uploads bucket + cdn.grapit.kr → cdn.heygrabit.com), password-reset.tsx (Grapit comment), admin-dashboard.spec.ts (admin@grapit.test)"
    - "D-10 (Revision 2) STEP 0 full inventory (/tmp/grapit-inventory.txt)를 먼저 생성한 뒤 STEP 4 bulk rename이 inventory 전체를 대상으로 실행됨"
    - "D-10 (Revision 2) audit-brand-rename.sh allowlist가 line-level 4개: grapit_dev (D-01), /grapit/.env (D-03), grapit-cloudrun@ (D-05), @social.grabit.com (D-07 exception)"
    - "완료된 phase 폴더(.planning/milestones/, .planning/phases/0*, 09.1-*, 10.1-*, 11-*, 12-*) 및 .planning/quick/ 변경 0건 (SC-4)"
  artifacts:
    - path: "package.json"
      provides: "root workspace manifest with name=@grabit/root"
      contains: '"name": "@grabit/root"'
    - path: "apps/api/package.json"
      provides: "API package manifest with name=@grabit/api and dependencies[@grabit/shared]"
      contains: '"@grabit/shared": "workspace:*"'
    - path: "apps/web/package.json"
      provides: "Web package manifest with name=@grabit/web and dependencies[@grabit/shared]"
      contains: '"@grabit/shared": "workspace:*"'
    - path: "packages/shared/package.json"
      provides: "Shared package manifest with name=@grabit/shared"
      contains: '"name": "@grabit/shared"'
    - path: "pnpm-lock.yaml"
      provides: "Regenerated lockfile referencing @grabit/* (no stale @grapit/* entries)"
    - path: "apps/web/next.config.ts"
      provides: "transpilePackages ['@grabit/shared']"
    - path: "apps/web/tsconfig.json"
      provides: "paths {'@grabit/shared': ...}"
    - path: "docker-compose.yml"
      provides: "container_name=grabit-postgres, POSTGRES_DB=grabit, POSTGRES_USER=grabit, POSTGRES_PASSWORD=grapit_dev (D-01 retained)"
    - path: ".github/workflows/ci.yml"
      provides: "POSTGRES_DB=grabit_test + DATABASE_URL with grabit_test + @grabit/* filters + admin@grabit.test"
    - path: "scripts/provision-valkey.sh"
      provides: "INSTANCE_NAME=grabit-valkey, POLICY_NAME=grabit-valkey-policy + rerun-prohibited comment + grapit-cloudrun@ SA retained (D-05)"
    - path: "apps/api/src/database/seed.mjs"
      provides: "admin@grabit.test seed fixture"
    - path: "apps/api/src/modules/auth/auth.service.ts"
      provides: "social fallback email @social.grabit.com"
    - path: "apps/api/test/admin-dashboard.integration.spec.ts"
      provides: "grapit_test → grabit_test in integration test filter/DB references (D-10)"
    - path: "apps/api/src/modules/admin/upload.service.spec.ts"
      provides: "R2 fixture renamed (grapit-uploads → grabit-uploads, cdn.grapit.kr → cdn.heygrabit.com) (D-10)"
    - path: "apps/api/src/modules/auth/email/templates/password-reset.tsx"
      provides: "Doc comment 'Grapit' → 'Grabit' (D-10)"
    - path: "apps/web/e2e/admin-dashboard.spec.ts"
      provides: "admin@grapit.test → admin@grabit.test in e2e fixture (D-10)"
  key_links:
    - from: "apps/api/package.json"
      to: "packages/shared/package.json"
      via: "workspace:* protocol"
      pattern: '"@grabit/shared": "workspace:\\*"'
    - from: "apps/web/tsconfig.json paths"
      to: "packages/shared/src"
      via: "TS path alias"
      pattern: "@grabit/shared"
    - from: "apps/web/vitest.config.ts alias"
      to: "packages/shared/src"
      via: "vitest resolve.alias"
      pattern: "@grabit/shared"
    - from: "apps/api/Dockerfile"
      to: "pnpm workspace filter"
      via: "RUN pnpm --filter"
      pattern: "@grabit/(shared|api|web)"
    - from: ".github/workflows/ci.yml"
      to: "seed.mjs admin user"
      via: "seed step + login probe with admin@grabit.test"
      pattern: "admin@grabit\\.test"
    - from: "seed.mjs"
      to: "GitHub secret TEST_USER_EMAIL"
      via: "E2E login helper fallback"
      pattern: "admin@grabit\\.test"
---

<!-- Revision 2 (2026-04-22): D-10 (inventory + 4 new files + multi-pattern + line-level allowlist), D-16 (rg pipe safety), LOW #11 (active-phase exception) from codex REVIEWS. -->

<objective>
Phase 13 Plan 01 (P1) — 코드/설정 rename. 678건의 `grapit` 출현(.planning 제외)을 `grabit`으로 치환한다. 4개 workspace manifest + 92+ 애플리케이션 import + 6개 build/workspace config + 2개 Dockerfile + docker-compose.yml + ci.yml + scripts/provision-valkey.sh + seed/fixture/test 이메일 + CLAUDE.md/AGENTS.md/docs/*/arch/* + **Revision 2 (D-10) 누락 4개 파일 (admin-dashboard.integration.spec / upload.service.spec / password-reset.tsx / admin-dashboard.spec)** 을 단일 PR로 변경하고, `pnpm install`로 lockfile을 재생성하여 `pnpm install --frozen-lockfile`이 green인 상태로 main에 머지한다. 도메인 cutover(P3/P4)는 범위 외.

Purpose: 신규 코드/문서/설정이 `grabit`으로 통일되고 빌드·타입체크·린트가 통과하는 상태(SC-1)를 달성한다. 동시에 D-01(grapit_dev password), D-03(/grapit/ 로컬 경로), D-05(grapit-cloudrun@ SA), D-07 exception (@social.grabit.com 내부 합성 이메일) 예외를 정확히 보존하고, D-06(dev/CI DB rename), D-07(@heygrabit.com 이메일), **D-10 (inventory-driven 누락 파일 포함 + cdn.grapit.kr → cdn.heygrabit.com), D-16 (rg pipe safety)** 결정을 적용한다.

Output: 단일 PR, 약 120+ 파일 수정 (Revision 2 +4 파일), lockfile 재생성 포함. `pnpm install --frozen-lockfile && pnpm -r lint && pnpm -r typecheck && pnpm -r test && pnpm -r build` 전부 exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-grapit-grabit-rename/13-CONTEXT.md
@.planning/phases/13-grapit-grabit-rename/13-RESEARCH.md
@.planning/phases/13-grapit-grabit-rename/13-PATTERNS.md
@.planning/phases/13-grapit-grabit-rename/13-VALIDATION.md
@.planning/phases/13-grapit-grabit-rename/13-REVIEWS.md
@CLAUDE.md

<interfaces>
<!-- Key contracts extracted from the codebase for executor context. No repo exploration needed. -->

Current manifest surfaces (from package.json files):
```
package.json              → "name": "@grapit/root"
apps/api/package.json     → "name": "@grapit/api",  "@grapit/shared": "workspace:*"
apps/web/package.json     → "name": "@grapit/web",  "@grapit/shared": "workspace:*"
packages/shared/package.json → "name": "@grapit/shared"
```

Current docker-compose.yml (verbatim):
```yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: grapit-postgres
    environment:
      POSTGRES_DB: grapit
      POSTGRES_USER: grapit
      POSTGRES_PASSWORD: grapit_dev   # ← D-01: retain this ONLY
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U grapit"]
```

Current ci.yml env variables (from lines 22, 32):
```
POSTGRES_DB: grapit_test
DATABASE_URL: postgresql://postgres:postgres@localhost:5432/grapit_test
```
ci.yml also contains: `pnpm --filter @grapit/api`, `admin@grapit.test`, `pnpm --filter @grapit/web test:e2e`

Target TSConfig path (apps/web/tsconfig.json:14):
```json
"paths": { "@grapit/shared": ["../../packages/shared/src"] }
```

Seed.mjs admin user (lines 19-22, 49, 51 from PATTERNS §Cat 8):
- DELETE refresh_tokens/social_accounts/terms_agreements/users WHERE email='admin@grapit.test' (4 queries)
- INSERT INTO users VALUES (..., 'admin@grapit.test', ...)
- console.log('Inserted admin user: admin@grapit.test')

D-10 (Revision 2) 누락 파일 현재 상태:
- apps/api/test/admin-dashboard.integration.spec.ts §L31 `pnpm --filter @grapit/api`, §L47,L60 `grapit_test`
- apps/api/src/modules/admin/upload.service.spec.ts §L27-28 `grapit-uploads` bucket, §L102 `cdn.grapit.kr` R2 public URL
- apps/api/src/modules/auth/email/templates/password-reset.tsx §L8 `Grapit password reset flow` doc comment
- apps/web/e2e/admin-dashboard.spec.ts §L15,L20 `admin@grapit.test`
</interfaces>

<exclude_globs>
<!-- MANDATORY for ALL ripgrep/sed commands in this plan -->
EXCLUDES=(
  --glob '!.planning/**'
  --glob '!.playwright-mcp/**'
  --glob '!node_modules/**'
  --glob '!**/node_modules/**'
  --glob '!apps/*/dist/**'
  --glob '!apps/*/.next/**'
  --glob '!packages/*/dist/**'
  --glob '!pnpm-lock.yaml'
  --glob '!.claude/**'
)
</exclude_globs>

<decision_constraints>
<!-- Hard constraints enforced in acceptance_criteria -->
- D-01: `docker-compose.yml` POSTGRES_PASSWORD value stays literally `grapit_dev` (1 occurrence must remain)
- D-03: `CLAUDE.md` L232 `/grapit/.env` filesystem path expression stays (NOT an `@grapit/*` scope)
- D-05: `grapit-cloudrun@${PROJECT_ID}.iam.gserviceaccount.com` SA references retained in `.github/workflows/deploy.yml` (2 occurrences) and `scripts/provision-valkey.sh` (2 comment occurrences). These are NOT touched in Plan 01 (deploy.yml is touched in Plan 03).
- D-06: ALL dev/CI DB identifiers renamed — `grapit_test` → `grabit_test`, `container_name: grapit-postgres` → `grabit-postgres`, `POSTGRES_DB: grapit` → `grabit`, `POSTGRES_USER: grapit` → `grabit` (password excepted by D-01)
- D-07: ALL `@grapit.com` USER-FACING emails → `@heygrabit.com` (no-reply, support, privacy, admin legal contacts).
  - **D-07 exception (auth.service.ts `@social.grapit.com` fallback):** 소셜 로그인 provider 가 이메일을 제공하지 않을 때 서버가 내부 생성하는 **합성(synthetic) 이메일 주소**. 사용자에게 노출되지 않고(UI/이메일/SMS 어디에도 표기 안 됨), 이메일 송수신 대상이 아니라 mailbox 설정 불필요. 따라서 `@social.grapit.com` 은 `@heygrabit.com` (실 수신 가능 도메인) 이 아니라 `@social.grabit.com` (내부 도메인 rename만) 으로 전환한다. Task 2 STEP 4 의 auth.service.ts L418 변경은 이 예외에 근거한다.
  - 기타 test/internal fixture(`@grabit.test`, `@e2e.grabit.dev`) 도 동일 원칙(실제 mailbox 없음 → internal rename 만) 적용.
- **D-10 (Revision 2) — Full inventory + multi-pattern bulk rename:**
  - STEP 0 에서 `rg -l 'grapit|Grapit|@grapit/|@grapit\.'` 로 동적 inventory 생성 → `/tmp/grapit-inventory.txt`. Task 2/3 작업은 모두 이 inventory 기반.
  - STEP 4 bulk rename 은 다음 멀티 패턴으로 확장: `@grapit/`, `\bGrapit\b`, `grapit_test`, `grapit-uploads`, `cdn\.grapit\.kr`, `admin@grapit\.test`, `@e2e\.grapit\.dev`.
  - `cdn.grapit.kr` → `cdn.heygrabit.com` (test fixture 전용, 실 prod R2 설정 영향 없음).
  - audit-brand-rename.sh allowlist 는 **line-level** 4 entries:
    1. `grapit_dev` (D-01)
    2. `/grapit/.env` (D-03)
    3. `grapit-cloudrun@` (D-05)
    4. `@social.grabit.com` (D-07 exception)
- **D-16 (Revision 2) — rg pipe safety:** 모든 `<verify>` / `<acceptance_criteria>` 의 bash snippet 에서 `set -euo pipefail` 환경에서 no-match 시 pipe 중단 방지. 패턴: `rg -c 'pattern' path 2>/dev/null || echo 0` 또는 `rg -l 'pattern' path 2>/dev/null | wc -l` 또는 `rg -c 'pattern' path | awk -F: '{s+=$2} END {print s}'`.
- SC-4: Zero diff under `.planning/milestones/`, `.planning/phases/0[1-9]-*`, `.planning/phases/09.1-*`, `.planning/phases/10.1-*`, `.planning/phases/11-*`, `.planning/phases/12-*`, `.planning/quick/`
  - **예외 (LOW #11):** active phase folder (`.planning/phases/13-grapit-grabit-rename/`) 내부의 `13-0N-SUMMARY.md`, `13-HUMAN-UAT.md`, `13-04-SUMMARY.md` 등 phase-owned artifact 생성/수정은 허용.
</decision_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STEP 0 inventory + Package manifests + application code multi-pattern rename + lockfile regeneration (Revision 2 / D-10)</name>
  <files>
    package.json,
    apps/api/package.json,
    apps/web/package.json,
    packages/shared/package.json,
    pnpm-lock.yaml,
    apps/web/next.config.ts,
    apps/web/tsconfig.json,
    apps/web/vitest.config.ts,
    apps/api/vitest.integration.config.ts,
    apps/api/Dockerfile,
    apps/web/Dockerfile,
    apps/api/test/admin-dashboard.integration.spec.ts,
    apps/api/src/modules/admin/upload.service.spec.ts,
    apps/api/src/modules/auth/email/templates/password-reset.tsx,
    apps/web/e2e/admin-dashboard.spec.ts,
    (+ ~92 *.ts/*.tsx files with `from '@grapit/shared'` imports + files surfaced by STEP 0 inventory)
  </files>
  <read_first>
    - .planning/phases/13-grapit-grabit-rename/13-PATTERNS.md § Category 1 (Package manifests), § Category 2 (Workspace/build config), § Category 3 (Dockerfiles), § Category 6 (Application code import), § Shared Patterns A-D
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § Pattern 1: workspace:* 기반 스코프 rename (topo order), § Risks & Mitigations (pnpm-lock drift)
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-10 (Revision 2, inventory + multi-pattern)
    - .planning/phases/13-grapit-grabit-rename/13-REVIEWS.md § Codex HIGH #3 (P1 inventory 불완전)
    - All 4 package.json files (root, apps/api, apps/web, packages/shared)
    - apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/vitest.config.ts, apps/api/vitest.integration.config.ts
    - apps/api/Dockerfile, apps/web/Dockerfile
    - apps/api/test/admin-dashboard.integration.spec.ts (D-10 new)
    - apps/api/src/modules/admin/upload.service.spec.ts (D-10 new)
    - apps/api/src/modules/auth/email/templates/password-reset.tsx (D-10 new)
    - apps/web/e2e/admin-dashboard.spec.ts (D-10 new)
  </read_first>
  <action>
    **D-10 (Revision 2) — STEP 0 MUST precede all other steps:**

    STEP 0 — Full inventory 동적 생성 (D-10):
    ```bash
    rg -l 'grapit|Grapit|@grapit/|@grapit\.' \
      --glob '!.planning/**' \
      --glob '!pnpm-lock.yaml' \
      --glob '!node_modules/**' \
      --glob '!**/node_modules/**' \
      --glob '!apps/*/dist/**' \
      --glob '!apps/*/.next/**' \
      --glob '!packages/*/dist/**' \
      --glob '!.claude/**' \
      > /tmp/grapit-inventory.txt
    wc -l /tmp/grapit-inventory.txt   # sanity: 135~160 lines expected (original 120 + D-10 누락 4 + edges)
    ```
    이 inventory 는 Task 2/3 모두의 작업 대상 기준. STEP 4 bulk rename 은 **이 inventory 전체** 를 scope 로 한다.

    Topo order MANDATORY (per D-01/PATTERNS §Cat 1 "Why this order"):

    STEP 1 — Manifest name fields (4 files):
    - `packages/shared/package.json`: change `"name": "@grapit/shared"` → `"name": "@grabit/shared"`
    - `apps/api/package.json`: change `"name": "@grapit/api"` → `"name": "@grabit/api"` AND `"@grapit/shared": "workspace:*"` → `"@grabit/shared": "workspace:*"`
    - `apps/web/package.json`: change `"name": "@grapit/web"` → `"name": "@grabit/web"` AND `"@grapit/shared": "workspace:*"` → `"@grabit/shared": "workspace:*"`
    - `package.json` (root): change `"name": "@grapit/root"` → `"name": "@grabit/root"`

    STEP 2 — Build/workspace config (4 files):
    - `apps/web/next.config.ts` L43: `transpilePackages: ['@grapit/shared']` → `transpilePackages: ['@grabit/shared']`
    - `apps/web/tsconfig.json` L14: `"@grapit/shared": ["../../packages/shared/src"]` → `"@grabit/shared": ["../../packages/shared/src"]`
    - `apps/web/vitest.config.ts` L16: `'@grapit/shared': resolve(__dirname, '../../packages/shared/src')` → `'@grabit/shared': ...`
    - `apps/api/vitest.integration.config.ts` L24: same pattern (`@grapit/shared` → `@grabit/shared`)

    STEP 3 — Dockerfiles:
    - `apps/api/Dockerfile` L7-9: `pnpm --filter @grapit/shared build`, `pnpm --filter @grapit/api build`, `pnpm --filter @grapit/api deploy --prod --legacy /deploy` → replace `@grapit/` with `@grabit/` (3 replacements)
    - `apps/web/Dockerfile` L17-18: `pnpm --filter @grapit/shared build`, `pnpm --filter @grapit/web build` → replace `@grapit/` with `@grabit/` (2 replacements)

    STEP 4 — **Multi-pattern bulk rename (D-10 expanded from `@grapit/` single-pattern):**
    - Generate target list (inventory-based, git-tracked):
      ```bash
      # Use STEP 0 inventory filtered to git-tracked source files
      cat /tmp/grapit-inventory.txt | while read f; do git ls-files --error-unmatch "$f" 2>/dev/null && echo "$f"; done | sort -u > /tmp/grapit-rename-targets.txt
      wc -l /tmp/grapit-rename-targets.txt   # expected ~135-160
      ```
    - Apply **each** pattern separately (macOS sed BSD, `-i ''` required):
      ```bash
      # Pattern 1: @grapit/ scope (≈92 files)
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' 's|@grapit/|@grabit/|g'

      # Pattern 2: Grapit word boundary (BSD sed uses [[:<:]] and [[:>:]] for word boundaries)
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' 's/[[:<:]]Grapit[[:>:]]/Grabit/g'

      # Pattern 3-7: literal multi-patterns
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' -e 's/grapit_test/grabit_test/g'
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' -e 's/grapit-uploads/grabit-uploads/g'
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' -e 's/cdn\.grapit\.kr/cdn.heygrabit.com/g'
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' -e 's/admin@grapit\.test/admin@grabit.test/g'
      cat /tmp/grapit-rename-targets.txt | xargs sed -i '' -e 's/@e2e\.grapit\.dev/@e2e.grabit.dev/g'
      ```
    - **주의:** `\bGrapit\b` regex 는 GNU grep/rg 용. macOS BSD sed 는 `[[:<:]]`/`[[:>:]]` POSIX word boundary 를 사용한다. 위 스크립트 그대로 실행 가능.
    - **D-01/D-03/D-05/D-07 exception 파일은 Task 2 에서 수동 복원** (STEP 4 bulk 가 touch 한 결과를 복원): `docker-compose.yml` `grapit_dev`, `CLAUDE.md` `/grapit/.env`, `scripts/provision-valkey.sh`/`deploy.yml` `grapit-cloudrun@`, `auth.service.ts` `@social.grapit.com` → `@social.grabit.com` 은 Task 2 에서 처리.
    - **D-07 exception (`@social.grapit.com` → `@social.grabit.com`)** 은 Pattern 1-7 중 어느 것도 매칭하지 않으므로 bulk 에 의해 자동 변경되지 않음. Task 2 에서 수동 처리.
    - **D-17 참고:** `sms.service.spec.ts:137` `INFOBIP_SENDER: 'Grapit'` 는 Pattern 2 (`\bGrapit\b`) 에 의해 `'Grabit'` 으로 치환됨. 'Grabit' 도 alphanumeric 이라 KISA 반례로 여전히 유효. allowlist 불필요.

    STEP 5 — Regenerate lockfile:
    - `pnpm install` at repo root (NOT `--frozen-lockfile`). This produces an updated `pnpm-lock.yaml` with `@grabit/shared: link:../../packages/shared`.
    - Stage lockfile changes for this same commit.

    STEP 6 — Local verification (must pass before commit):
    - `pnpm install --frozen-lockfile` → exit 0
    - `pnpm -r lint` → exit 0
    - `pnpm -r typecheck` → exit 0
    - `pnpm -r test` → exit 0
    - `pnpm -r build` → exit 0

    CONSTRAINTS:
    - DO NOT manually edit `pnpm-lock.yaml` — only `pnpm install` regenerates it (PATTERNS §Known Exceptions E13).
    - DO NOT touch `pnpm-workspace.yaml` or `turbo.json` — per PATTERNS §Cat 2 they contain no scope names.
    - DO NOT run `sed` globally on the repo — use the `/tmp/grapit-rename-targets.txt` inventory-derived file list.
    - Commit all STEP 0-5 changes as a single atomic commit (or a small chain within the same PR). The lockfile MUST be in the same PR, same branch, same push. Anti-pattern per RESEARCH: "lockfile을 코드 rename과 다른 PR로 분리" causes CI failure.
    - **LOW #11 예외:** active phase folder (`.planning/phases/13-grapit-grabit-rename/`) 내부의 `13-0N-SUMMARY.md`, `13-HUMAN-UAT.md` 등 phase-owned artifact 생성/수정은 허용. SC-4 audit 은 완료된 phase 폴더만 대상.
  </action>
  <verify>
    <automated>
      set -euo pipefail
      # STEP 0 inventory sanity
      test -s /tmp/grapit-inventory.txt || { echo "FAIL inventory missing"; exit 1; }
      INV_COUNT=$(wc -l < /tmp/grapit-inventory.txt | tr -d ' ')
      [ "$INV_COUNT" -ge 100 ] || { echo "FAIL inventory too small: $INV_COUNT"; exit 1; }
      # A. Manifest name integrity
      NAMES=$(jq -r '.name' package.json apps/api/package.json apps/web/package.json packages/shared/package.json)
      GRAPIT_NAMES=$(echo "$NAMES" | grep -c grapit || echo 0)
      [ "$GRAPIT_NAMES" -eq 0 ] || { echo "FAIL manifest names still contain grapit: $GRAPIT_NAMES"; exit 1; }
      GRABIT_NAMES=$(echo "$NAMES" | grep -c grabit || echo 0)
      [ "$GRABIT_NAMES" -eq 4 ] || { echo "FAIL manifest grabit count: $GRABIT_NAMES"; exit 1; }
      # B. No stale @grapit/ references in source/config (D-16 safe pattern)
      REMAIN_FILES=$(rg -l '@grapit/' --glob '!.planning/**' --glob '!pnpm-lock.yaml' --glob '!node_modules/**' --glob '!**/node_modules/**' --glob '!apps/*/dist/**' --glob '!apps/*/.next/**' --glob '!packages/*/dist/**' 2>/dev/null | wc -l | tr -d ' ')
      [ "$REMAIN_FILES" = "0" ] || { echo "FAIL stale @grapit/ files: $REMAIN_FILES"; exit 1; }
      # B2. D-10 누락 4개 파일이 처리되었는지 확인
      rg -q 'grabit_test' apps/api/test/admin-dashboard.integration.spec.ts || { echo "FAIL D-10 admin-dashboard.integration.spec"; exit 1; }
      rg -q 'grabit-uploads' apps/api/src/modules/admin/upload.service.spec.ts || { echo "FAIL D-10 upload.service.spec uploads"; exit 1; }
      rg -q 'cdn\.heygrabit\.com' apps/api/src/modules/admin/upload.service.spec.ts || { echo "FAIL D-10 upload.service.spec cdn"; exit 1; }
      rg -q 'Grabit' apps/api/src/modules/auth/email/templates/password-reset.tsx || { echo "FAIL D-10 password-reset.tsx"; exit 1; }
      rg -q 'admin@grabit\.test' apps/web/e2e/admin-dashboard.spec.ts || { echo "FAIL D-10 admin-dashboard.spec"; exit 1; }
      # B3. D-10 누락 4개 파일의 구 문자열 완전 제거
      STALE_D10=$(rg -c 'grapit_test|grapit-uploads|cdn\.grapit\.kr|admin@grapit\.test|\bGrapit\b' \
        apps/api/test/admin-dashboard.integration.spec.ts \
        apps/api/src/modules/admin/upload.service.spec.ts \
        apps/api/src/modules/auth/email/templates/password-reset.tsx \
        apps/web/e2e/admin-dashboard.spec.ts 2>/dev/null | awk -F: '{s+=$2} END {print s+0}')
      [ "$STALE_D10" = "0" ] || { echo "FAIL D-10 stale strings in 4 new files: $STALE_D10"; exit 1; }
      # C. Lockfile integrity (CI parity)
      pnpm install --frozen-lockfile
      # D. Turbo pipeline green
      pnpm -r lint
      pnpm -r typecheck
      pnpm -r test
      pnpm -r build
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -s /tmp/grapit-inventory.txt` exit **0** (D-10 STEP 0 inventory 생성됨)
    - `wc -l < /tmp/grapit-inventory.txt` ≥ **100** (inventory 최소 파일 수)
    - `jq -r '.name' package.json apps/*/package.json packages/*/package.json | grep -c grapit || echo 0` equals **0**
    - `jq -r '.name' package.json apps/*/package.json packages/*/package.json | grep -c grabit || echo 0` equals **4**
    - `rg -l '@grapit/' --glob '!.planning/**' --glob '!pnpm-lock.yaml' --glob '!node_modules/**' --glob '!apps/*/dist/**' --glob '!apps/*/.next/**' --glob '!packages/*/dist/**' 2>/dev/null | wc -l` equals **0** (D-16 safe pattern)
    - `rg -q 'grabit_test' apps/api/test/admin-dashboard.integration.spec.ts` exit **0** (D-10)
    - `rg -q 'grabit-uploads' apps/api/src/modules/admin/upload.service.spec.ts` exit **0** (D-10)
    - `rg -q 'cdn\.heygrabit\.com' apps/api/src/modules/admin/upload.service.spec.ts` exit **0** (D-10: cdn fixture domain 전환)
    - `rg -q 'Grabit' apps/api/src/modules/auth/email/templates/password-reset.tsx` exit **0** (D-10)
    - `rg -q 'admin@grabit\.test' apps/web/e2e/admin-dashboard.spec.ts` exit **0** (D-10)
    - `rg -c 'grapit_test|grapit-uploads|cdn\.grapit\.kr|admin@grapit\.test' apps/api/test/admin-dashboard.integration.spec.ts apps/api/src/modules/admin/upload.service.spec.ts apps/web/e2e/admin-dashboard.spec.ts 2>/dev/null | awk -F: '{s+=$2} END {print s+0}'` equals **0** (D-10 구 문자열 제거, D-16 safe)
    - `pnpm install --frozen-lockfile` exit code **0**
    - `pnpm -r lint` exit code **0**
    - `pnpm -r typecheck` exit code **0**
    - `pnpm -r test` exit code **0**
    - `pnpm -r build` exit code **0**
    - `git diff --name-only HEAD~1 HEAD -- pnpm-lock.yaml | wc -l` equals **1** (lockfile changed in same commit as manifests)
    - `git diff --name-only main...HEAD -- .planning/milestones/ .planning/phases/0[1-9]-* .planning/phases/09.1-* .planning/phases/10.1-* .planning/phases/11-* .planning/phases/12-* .planning/quick/ | wc -l` equals **0** (SC-4, active phase 폴더 예외 per LOW #11)
  </acceptance_criteria>
  <done>
    STEP 0 inventory 생성 → 4개 manifest name + dependencies 가 @grabit/*로 변경, multi-pattern bulk rename (7 patterns including D-10 확장) 으로 ~92 @grapit/ import + 4개 D-10 누락 파일 + 기타 inventory 상 모든 파일 치환, 4개 build config, 2개 Dockerfile, lockfile 재생성이 단일 atomic PR 에 포함되고 `pnpm install --frozen-lockfile` + `pnpm -r {lint,typecheck,test,build}`가 모두 exit 0. 완료된 phase/milestone 폴더 변경 0건 (active phase 폴더 예외 per LOW #11).
  </done>
</task>

<task type="auto">
  <name>Task 2: Infrastructure files (docker-compose, ci.yml, provision-valkey.sh) + seed/fixture/test emails + D-07 exception (@social.grabit.com) 수동 처리</name>
  <files>
    docker-compose.yml,
    .github/workflows/ci.yml,
    scripts/provision-valkey.sh,
    apps/api/src/database/seed.mjs,
    apps/web/e2e/helpers/auth.ts,
    apps/web/e2e/signup-sms.spec.ts,
    apps/api/src/modules/auth/email/email.service.spec.ts,
    apps/api/src/modules/auth/auth.service.ts
  </files>
  <read_first>
    - .planning/phases/13-grapit-grabit-rename/13-PATTERNS.md § Category 4 (docker-compose), § Category 5a (ci.yml), § Category 7 (provision-valkey.sh), § Category 8 (seed/fixture/test email), § Known Exceptions table E1-E3
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-01 (prod DB rename 제외 / grapit_dev password 유지), § D-05 (SA 유지), § D-06 (dev/CI DB rename), § D-07 (@heygrabit.com + @social.grabit.com exception)
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § Open Question 2 해소 (D-06), § Open Question 4 해소 (no-reply@grabit.com fixture)
    - Current `docker-compose.yml`, `.github/workflows/ci.yml`, `scripts/provision-valkey.sh`, `apps/api/src/database/seed.mjs`
  </read_first>
  <action>
    **D-07 EXCEPTION PRE-FLIGHT NOTE (applies to STEP 4 auth.service.ts):**
    `@social.grapit.com` 은 소셜 로그인 provider 가 이메일을 주지 않을 때 서버가 내부 생성하는 **합성 이메일** 이다. 사용자에게 노출되지 않고(UI/이메일/SMS 어디에도 표기 안 됨), 실제 이메일 송수신 대상이 아니므로 mailbox 필요 없음. 따라서 D-07의 `@heygrabit.com` 일괄 치환 원칙의 **예외**로, `@social.grapit.com` → `@social.grabit.com` (내부 도메인 rename만) 으로 처리한다. 근거는 <decision_constraints> D-07 exception 참조.

    STEP 1 — `docker-compose.yml` (D-06 + D-01 exception):
    - `container_name: grapit-postgres` → `container_name: grabit-postgres`
    - `POSTGRES_DB: grapit` → `POSTGRES_DB: grabit`
    - `POSTGRES_USER: grapit` → `POSTGRES_USER: grabit`
    - `POSTGRES_PASSWORD: grapit_dev` → **UNCHANGED** (D-01 exception). Add inline comment: `# ← D-01: 비밀번호 유지 (prod DB rename 제외 원칙의 연장)`
    - healthcheck `pg_isready -U grapit` → `pg_isready -U grabit`

    STEP 2 — `.github/workflows/ci.yml` (D-06 — rename all grapit_test / admin@grapit.test / @grapit/* filters):
    - L22 (postgres service env): `POSTGRES_DB: grapit_test` → `POSTGRES_DB: grabit_test`
    - L32 (job env): `DATABASE_URL: postgresql://postgres:postgres@localhost:5432/grapit_test` → `...@localhost:5432/grabit_test`
    - All `pnpm --filter @grapit/api` → `pnpm --filter @grabit/api` (PATTERNS lists L56, L66, L96, L136, L148 — grep ALL occurrences and rename)
    - All `pnpm --filter @grapit/web` → `pnpm --filter @grabit/web`
    - `Seed test data (admin@grapit.test)` step name → `Seed test data (admin@grabit.test)`
    - `psql ... -d grapit_test` → `-d grabit_test`
    - `SELECT ... FROM users WHERE email='admin@grapit.test'` → `admin@grabit.test`
    - `'{"email":"admin@grapit.test", ...}'` (curl login probe) → `admin@grabit.test`

    STEP 3 — `scripts/provision-valkey.sh` (D-05 exception: grapit-cloudrun@ SA retained):
    - Header comment `# Grapit - Google Memorystore...` → `# Grabit - Google Memorystore...`
    - Insert ADDITIONAL comment block after shebang/set line:
      ```
      # NOTE (Phase 13 rename): 이미 provision된 `grapit-valkey` 인스턴스는 이름 immutable
      # 이므로 그대로 사용 중. 본 스크립트 변수는 `grabit-*`로 갱신했지만 **재실행 금지**
      # (Valkey 데이터 재생성 = 좌석 잠금 유실). 신규 프로젝트에서만 rerun.
      ```
    - `INSTANCE_NAME="grapit-valkey"` → `INSTANCE_NAME="grabit-valkey"`
    - `POLICY_NAME="grapit-valkey-policy"` → `POLICY_NAME="grabit-valkey-policy"`
    - `grapit-cloudrun@...` SA references (L26, L86 per PATTERNS) → **UNCHANGED** (D-05). Append inline comment `# D-05: SA 유지` on the SA lines to make the exception explicit.

    STEP 4 — seed/fixture/test emails (D-07 `@heygrabit.com` for user-facing, `@grabit.test`/`@e2e.grabit.dev`/`@social.grabit.com` for test/internal):
    - `apps/api/src/database/seed.mjs` L19-22, 49, 51: all `admin@grapit.test` → `admin@grabit.test` (5 SQL queries + 1 console.log)
    - `apps/web/e2e/helpers/auth.ts` L42: `|| 'admin@grapit.test'` → `|| 'admin@grabit.test'`
    - `apps/web/e2e/helpers/auth.ts` L73: error hint string `pnpm --filter @grapit/api seed` → `pnpm --filter @grabit/api seed` (already covered by Task 1 bulk rename Pattern 1, but verify this file is in targets — if missed, fix here)
    - `apps/web/e2e/signup-sms.spec.ts` L27: ``test${timestamp}@e2e.grapit.dev`` → ``test${timestamp}@e2e.grabit.dev``
    - `apps/api/src/modules/auth/email/email.service.spec.ts` L49, L60, L89: fixture values `'no-reply@grapit.com'` → `'no-reply@heygrabit.com'` (3 occurrences — D-07 user-facing 원칙 literal 준수: "@grapit.com 이메일 전부 @heygrabit.com으로 치환")
    - `apps/api/src/modules/auth/auth.service.ts` L418: ``@social.grapit.com`` fallback → ``@social.grabit.com`` — **D-07 EXCEPTION** (see pre-flight note above + <decision_constraints>). 내부 합성 이메일, 사용자 미노출, mailbox 불필요 → `@heygrabit.com` 이 아닌 `@social.grabit.com` (내부 rename) 으로 전환.

    STEP 5 — GitHub secret checklist (document in PR description, execute after merge):
    - `gh secret set TEST_USER_EMAIL --body 'admin@grabit.test'` (RESEARCH §Risks: seed ↔ secret 동기화. If unchanged, CI E2E login probe uses the fallback `admin@grabit.test` correctly but if secret is set to old value, it overrides. Verify current state and update.)
    - Include in commit message: `[ACTION REQUIRED POST-MERGE] gh secret set TEST_USER_EMAIL --body 'admin@grabit.test'`

    CONSTRAINTS:
    - D-01 `grapit_dev` password MUST remain (verify with grep).
    - D-05 `grapit-cloudrun@` SA MUST remain in provision-valkey.sh comments (2 occurrences retained).
    - **D-07 exception for auth.service.ts `@social.grapit.com`:** 위 pre-flight note + STEP 4 마지막 항목 참조. `@social.grabit.com` 결과값은 의도된 것이며 acceptance_criteria 의 `rg -q "@social.grabit.com" apps/api/src/modules/auth/auth.service.ts` 로 enforce 된다.
    - DO NOT delete or recreate the real `grapit-valkey` GCP instance. Script is edited only; `# 재실행 금지` comment enforced.
    - DO NOT modify deploy.yml in this plan (Plan 03's scope — P3).
  </action>
  <verify>
    <automated>
      set -euo pipefail
      # D-01 exception: grapit_dev MUST remain (D-16 safe)
      DEV_COUNT=$(rg -c 'grapit_dev' docker-compose.yml 2>/dev/null || echo 0)
      [ "$DEV_COUNT" = "1" ] || { echo "FAIL D-01 grapit_dev count: $DEV_COUNT"; exit 1; }
      # D-06: grabit_test + grabit-postgres + POSTGRES_DB=grabit present
      rg -q 'container_name: grabit-postgres' docker-compose.yml || { echo "FAIL container rename"; exit 1; }
      rg -q 'POSTGRES_DB: grabit_test' .github/workflows/ci.yml || { echo "FAIL ci POSTGRES_DB"; exit 1; }
      rg -q 'localhost:5432/grabit_test' .github/workflows/ci.yml || { echo "FAIL ci DATABASE_URL"; exit 1; }
      # D-05 exception: grapit-cloudrun@ retained in provision-valkey.sh (D-16 safe)
      SA_COUNT=$(rg -c 'grapit-cloudrun@' scripts/provision-valkey.sh 2>/dev/null || echo 0)
      [ "$SA_COUNT" -ge 2 ] || { echo "FAIL D-05 SA retained: $SA_COUNT"; exit 1; }
      # D-05: new grabit-valkey variables present
      rg -q 'INSTANCE_NAME="grabit-valkey"' scripts/provision-valkey.sh || { echo "FAIL valkey instance var"; exit 1; }
      rg -q '재실행 금지' scripts/provision-valkey.sh || { echo "FAIL rerun-prohibited comment"; exit 1; }
      # Seed/fixture emails renamed (D-16 safe)
      rg -q "admin@grabit.test" apps/api/src/database/seed.mjs || { echo "FAIL seed email"; exit 1; }
      STALE_EMAILS=$(rg -l 'admin@grapit\.test|no-reply@grapit\.com|@e2e\.grapit\.dev|@social\.grapit\.com' apps/ 2>/dev/null | wc -l | tr -d ' ')
      [ "$STALE_EMAILS" = "0" ] || { echo "FAIL stale grapit email files: $STALE_EMAILS"; exit 1; }
      # D-07 exception verification: auth.service.ts MUST contain @social.grabit.com (internal synthetic), NOT @social.heygrabit.com
      rg -q "@social\.grabit\.com" apps/api/src/modules/auth/auth.service.ts || { echo "FAIL D-07 exception @social.grabit.com"; exit 1; }
      HEYGRABIT_SOCIAL=$(rg -c '@social\.heygrabit\.com' apps/api/src/modules/auth/auth.service.ts 2>/dev/null || echo 0)
      [ "$HEYGRABIT_SOCIAL" = "0" ] || { echo "FAIL D-07 exception violated (heygrabit used for internal synthetic)"; exit 1; }
      # Unit tests green for email/auth/sms specs (sanity — Task 1 already ran full pnpm test)
      pnpm --filter @grabit/api test -- email.service.spec auth.service
      # SC-4 historical preservation (active phase 예외 per LOW #11)
      HIST=$(git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/ | wc -l | tr -d ' ')
      [ "$HIST" = "0" ] || { echo "FAIL SC-4"; exit 1; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'grapit_dev' docker-compose.yml 2>/dev/null || echo 0` equals **1** (D-01 exception enforced, D-16 safe)
    - `rg 'grapit' docker-compose.yml 2>/dev/null | grep -v 'grapit_dev' | wc -l` equals **0** (no other grapit in compose)
    - `rg -c 'grabit_test' .github/workflows/ci.yml 2>/dev/null || echo 0` ≥ **2** (POSTGRES_DB + DATABASE_URL)
    - `rg -c 'grapit_test|admin@grapit\.test' .github/workflows/ci.yml 2>/dev/null || echo 0` equals **0**
    - `rg -c 'grapit-cloudrun@' scripts/provision-valkey.sh 2>/dev/null || echo 0` ≥ **2** (D-05 exception)
    - `rg -q 'INSTANCE_NAME="grabit-valkey"' scripts/provision-valkey.sh` exit **0**
    - `rg -q '재실행 금지' scripts/provision-valkey.sh` exit **0**
    - `rg -l 'admin@grapit\.test|no-reply@grapit\.com|@e2e\.grapit\.dev|@social\.grapit\.com' apps/ 2>/dev/null | wc -l` equals **0** (D-16 safe)
    - `rg -q "admin@grabit.test" apps/api/src/database/seed.mjs` exit **0**
    - `rg -q "no-reply@heygrabit.com" apps/api/src/modules/auth/email/email.service.spec.ts` exit **0** (D-07 enforces @heygrabit.com for user-facing fixture)
    - `rg -q "@social.grabit.com" apps/api/src/modules/auth/auth.service.ts` exit **0** (D-07 exception — internal social fallback remains on `@social.grabit.com`, NOT heygrabit)
    - `rg -c '@social\.heygrabit\.com' apps/api/src/modules/auth/auth.service.ts 2>/dev/null || echo 0` equals **0** (D-07 exception enforcement: synthetic email must NOT use heygrabit, D-16 safe)
    - `pnpm --filter @grabit/api test -- email.service.spec auth.service` exit **0**
    - PR description contains the literal string `gh secret set TEST_USER_EMAIL --body 'admin@grabit.test'` (post-merge action)
    - `git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/ | wc -l` equals **0**
  </acceptance_criteria>
  <done>
    docker-compose.yml은 D-06 rename + D-01 password 유지 상태, ci.yml은 grabit_test + @grabit/* filter + admin@grabit.test로 통일, provision-valkey.sh는 변수명 rename + SA 유지 + 재실행 금지 주석 추가 완료, seed/fixture/test 이메일 전부 치환(D-07 user-facing → @heygrabit.com, D-07 exception `@social.grapit.com` → `@social.grabit.com` 내부 합성 이메일 경계 명시됨), post-merge GitHub secret 갱신이 PR description에 checklist로 포함.
  </done>
</task>

<task type="auto">
  <name>Task 3: Documentation rename (CLAUDE.md, AGENTS.md, docs/*, arch/*) with D-03 local path exception + final audit gate with line-level allowlist (Revision 2 / D-10)</name>
  <files>
    CLAUDE.md,
    AGENTS.md,
    docs/03-ARCHITECTURE.md,
    docs/06-KAKAO-OAUTH-SETUP.md,
    docs/PLANNING-REVIEW.md,
    arch/06-VERCEL-RESEARCH.md,
    arch/10-INFRA-DECISION-PROPOSAL.md,
    arch/11-INFRA-RESEARCH-SUMMARY.md,
    scripts/audit-brand-rename.sh (new file)
  </files>
  <read_first>
    - .planning/phases/13-grapit-grabit-rename/13-PATTERNS.md § Category 12 (Documentation), § Shared Patterns § Pattern C (Verification script), § Known Exceptions E6
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-03 (로컬 레포 디렉토리 rename 제외), § D-10 (line-level allowlist 4 entries)
    - CLAUDE.md (all 5 grapit occurrences at L4, L232, L237, L239, L251)
    - .planning/phases/13-grapit-grabit-rename/13-VALIDATION.md § Per-Task Verification Map rows 1-10 (SC-1 validation commands)
  </read_first>
  <action>
    STEP 1 — CLAUDE.md (D-03 exception at L232 `/grapit/.env` filesystem path):
    - L4 `**Grapit**` → `**Grabit**`
    - L232 `/grapit/.env` → **UNCHANGED** (D-03: local repo directory path). This is a filesystem path example, not an `@grapit/*` scope.
    - L237, L239 `pnpm --filter @grapit/api exec drizzle-kit ...` → `@grabit/api` (already handled in Task 1's bulk rename Pattern 1, but verify this file was included)
    - L251 `run: pnpm --filter @grapit/api exec drizzle-kit migrate` → `@grabit/api`

    STEP 2 — AGENTS.md + docs/* + arch/*:
    - Bulk `@grapit/` → `@grabit/` on all `.md` files via git-tracked list:
      `git ls-files 'docs/*.md' 'arch/*.md' CLAUDE.md AGENTS.md | xargs sed -i '' 's|@grapit/|@grabit/|g'`
    - Bulk `Grapit` (word boundary) → `Grabit`:
      `git ls-files 'docs/*.md' 'arch/*.md' CLAUDE.md AGENTS.md | xargs sed -i '' 's/[[:<:]]Grapit[[:>:]]/Grabit/g'`
    - Bulk `grapit-` (service/instance names) → `grabit-`:
      `git ls-files 'docs/*.md' 'arch/*.md' CLAUDE.md AGENTS.md | xargs sed -i '' 's/[[:<:]]grapit-\(web\|api\|postgres\|valkey\|valkey-policy\)/grabit-\1/g'`

    - After bulk rename, verify D-03: `rg '/grapit/\.env|/icons/grapit' CLAUDE.md` should still return the filesystem path reference (must be retained). If `Grapit` → `Grabit` word-boundary regex inadvertently affected it, restore manually.
    - Verify D-05: no `grapit-cloudrun` occurrences should have been renamed to `grabit-cloudrun` in docs. If any docs mention the service account, they must stay `grapit-cloudrun@` per D-05. If a doc references it in a now-incorrect context, add a `<!-- D-05: SA kept as grapit-cloudrun, see CONTEXT.md -->` comment.

    STEP 3 — Create `scripts/audit-brand-rename.sh` (Wave 0 requirement per VALIDATION.md, D-10 line-level allowlist):
    ```bash
    #!/usr/bin/env bash
    # scripts/audit-brand-rename.sh — Phase 13 rename audit gate.
    # Runs SC-1 + SC-4 checks at plan completion.
    # Revision 2 (D-10): line-level allowlist (4 entries) via grep -v regex.
    set -euo pipefail

    echo "=== SC-4: .planning/milestones/ 변경 금지 ==="
    CHANGED=$(git diff --name-only main...HEAD -- '.planning/milestones/' | wc -l | tr -d ' ')
    if [ "$CHANGED" -gt 0 ]; then echo "FAIL: milestones touched"; exit 1; fi

    echo "=== SC-4: 완료된 phase 폴더 변경 금지 (active 13-grapit-grabit-rename 예외) ==="
    CHANGED=$(git diff --name-only main...HEAD -- \
      '.planning/phases/0[1-9]-*' \
      '.planning/phases/1[012]-*' \
      '.planning/phases/09.1-*' \
      '.planning/phases/10.1-*' \
      '.planning/quick/' | wc -l | tr -d ' ')
    if [ "$CHANGED" -gt 0 ]; then echo "FAIL: completed phases touched"; exit 1; fi

    echo "=== SC-1: grapit residue (D-10 line-level allowlist: 4 entries) ==="
    # D-10 allowlist (line-level regex):
    #   1. grapit_dev         (D-01: docker-compose.yml password)
    #   2. /grapit/\.env      (D-03: CLAUDE.md local filesystem path)
    #   3. grapit-cloudrun@   (D-05: SA in deploy.yml + provision-valkey.sh)
    #   4. @social\.grabit\.com (D-07 exception — already grabit, but audit ensures NOT heygrabit)
    ALLOWLIST='grapit_dev|/grapit/\.env|grapit-cloudrun@|@social\.grabit\.com'

    # Find all lines containing 'grapit' (case-sensitive) across repo scope, excluding allowed
    RESIDUE=$(rg 'grapit' \
      --glob '!.planning/**' \
      --glob '!.playwright-mcp/**' \
      --glob '!node_modules/**' \
      --glob '!**/node_modules/**' \
      --glob '!apps/*/dist/**' \
      --glob '!apps/*/.next/**' \
      --glob '!packages/*/dist/**' \
      --glob '!pnpm-lock.yaml' \
      --glob '!.claude/**' \
      --glob '!scripts/audit-brand-rename.sh' \
      2>/dev/null | grep -vE "$ALLOWLIST" | wc -l | tr -d ' ')
    if [ "$RESIDUE" -gt 0 ]; then
      echo "FAIL: unexpected grapit residue (non-allowlisted lines):"
      rg 'grapit' \
        --glob '!.planning/**' --glob '!.playwright-mcp/**' --glob '!node_modules/**' \
        --glob '!**/node_modules/**' --glob '!apps/*/dist/**' --glob '!apps/*/.next/**' \
        --glob '!packages/*/dist/**' --glob '!pnpm-lock.yaml' --glob '!.claude/**' \
        --glob '!scripts/audit-brand-rename.sh' \
        2>/dev/null | grep -vE "$ALLOWLIST"
      exit 1
    fi

    echo "=== SC-1 Allowlist Sanity (each allowlisted pattern must exist — D-10 positive check) ==="
    rg -q 'grapit_dev' docker-compose.yml || { echo "FAIL: D-01 grapit_dev missing"; exit 1; }
    SA_DEPLOY=$(rg -c 'grapit-cloudrun@' .github/workflows/deploy.yml 2>/dev/null || echo 0)
    [ "$SA_DEPLOY" -ge 2 ] || { echo "FAIL: D-05 SA not retained in deploy.yml: $SA_DEPLOY"; exit 1; }
    SA_VALKEY=$(rg -c 'grapit-cloudrun@' scripts/provision-valkey.sh 2>/dev/null || echo 0)
    [ "$SA_VALKEY" -ge 2 ] || { echo "FAIL: D-05 SA not retained in provision-valkey.sh: $SA_VALKEY"; exit 1; }
    rg -q '/grapit/\.env' CLAUDE.md || { echo "FAIL: D-03 /grapit/.env path removed"; exit 1; }
    rg -q '@social\.grabit\.com' apps/api/src/modules/auth/auth.service.ts || { echo "FAIL: D-07 exception @social.grabit.com missing"; exit 1; }

    echo "=== ALL CHECKS PASSED ==="
    ```

    Make it executable: `chmod +x scripts/audit-brand-rename.sh`

    STEP 4 — Run audit script and fix any residue:
    - `./scripts/audit-brand-rename.sh` → must exit 0
    - If residue found, inspect and resolve manually (likely missed doc).

    CONSTRAINTS:
    - CLAUDE.md L232 `/grapit/.env` path MUST be retained (D-03).
    - DO NOT modify any `.planning/` file (audit enforces SC-4).
      - **예외 (LOW #11):** active phase folder (`.planning/phases/13-grapit-grabit-rename/`) 내부의 `13-0N-SUMMARY.md`, `13-HUMAN-UAT.md` 등 phase-owned artifact 생성/수정은 허용.
    - DO NOT modify `deploy.yml` in this plan (deferred to Plan 03).
  </action>
  <verify>
    <automated>
      set -euo pipefail
      # D-03 exception: CLAUDE.md filesystem path kept
      rg -q '/grapit/\.env' CLAUDE.md || { echo "FAIL D-03 /grapit/.env removed"; exit 1; }
      # Docs brand cleanup (D-16 safe)
      RESIDUE=$(rg 'Grapit\b|@grapit/|grapit-(web|api|postgres|valkey)' CLAUDE.md AGENTS.md docs/ arch/ 2>/dev/null | wc -l | tr -d ' ')
      [ "$RESIDUE" = "0" ] || { echo "FAIL doc brand residue: $RESIDUE"; exit 1; }
      # Audit script exists and passes
      test -x scripts/audit-brand-rename.sh || { echo "FAIL audit script missing or not executable"; exit 1; }
      ./scripts/audit-brand-rename.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '/grapit/\.env' CLAUDE.md` exit **0** (D-03 filesystem path retained)
    - `rg 'Grapit\b|@grapit/|grapit-(web|api|postgres|valkey)' CLAUDE.md AGENTS.md docs/ arch/ 2>/dev/null | wc -l` equals **0** (D-16 safe)
    - `test -x scripts/audit-brand-rename.sh` exit **0**
    - `./scripts/audit-brand-rename.sh` exit code **0**
    - Audit script contains the 4-entry D-10 line-level allowlist: `grep -q 'grapit_dev|/grapit/\\.env|grapit-cloudrun@|@social\\.grabit\\.com' scripts/audit-brand-rename.sh` exit **0**
    - `git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/ | wc -l` equals **0**
    - `rg -c 'grapit-cloudrun@' .github/workflows/deploy.yml 2>/dev/null || echo 0` ≥ **2** (D-05: retained until Plan 03 decision; Plan 01 MUST NOT touch deploy.yml)
    - `git diff --name-only main...HEAD -- .github/workflows/deploy.yml | wc -l` equals **0** (deploy.yml not modified in this plan)
  </acceptance_criteria>
  <done>
    CLAUDE.md/AGENTS.md/docs/*/arch/* 의 @grapit/* 스코프 + Grapit 단어 + grapit-* 서비스명이 모두 grabit으로 치환되고, CLAUDE.md L232 `/grapit/.env` 경로는 D-03으로 유지. scripts/audit-brand-rename.sh 작성 (D-10 line-level allowlist 4 entries) + SC-1/SC-4 audit green + deploy.yml은 Plan 03을 위해 touch하지 않음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repo → CI runner | CI installs dependencies via `pnpm install --frozen-lockfile`; lockfile drift could trigger unintended package install |
| Git repo → developer local `.env` | docker-compose.yml rename requires dev to re-create postgres container; `.env` DATABASE_URL also needs manual update |
| Git repo → GitHub secrets store | `TEST_USER_EMAIL` secret must stay in sync with `admin@grabit.test` seed email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `sed` bulk rename scope | mitigate | Use `git ls-files` + `--glob '!.planning/**'` exclude list on EVERY ripgrep/sed command. STEP 0 inventory-driven (D-10). Audit script (Task 3) fails if any `.planning/` file is modified. |
| T-13-02 | Denial of service (CI) | `pnpm install --frozen-lockfile` | mitigate | Task 1 regenerates `pnpm-lock.yaml` in the same commit as manifests. Acceptance criterion `git diff HEAD~1 HEAD -- pnpm-lock.yaml | wc -l` = 1 enforces single-commit atomicity. |
| T-13-03 | Information disclosure | `grapit_dev` password visible in docker-compose.yml | accept | D-01 decision; dev-only password, never used in prod. Retained in-place per locked decision. |
| T-13-04 | Elevation of privilege | `grapit-cloudrun@` service account references | accept (D-05) | SA retained in deploy.yml and provision-valkey.sh; rename would require IAM binding re-provisioning (14-16 bindings). Plan 01 does not touch deploy.yml; Plan 03 addresses deploy.yml AR_REPO/WEB_SERVICE/API_SERVICE only, not the SA. |
| T-13-05 | Repudiation | commit history of historical phases | mitigate | SC-4 audit enforces zero diff under `.planning/milestones/`, `.planning/phases/0*`, `09.1-*`, `10.1-*`, `11-*`, `12-*`, `.planning/quick/`. Audit script fails otherwise. active phase 예외는 LOW #11 에 명시. |
| T-13-06 | Spoofing | fixture test email `no-reply@grabit.com` | mitigate (D-07) | Use `no-reply@heygrabit.com` per D-07's literal scope. Test fixture has no real email delivery; value is symbolic. |
| T-13-25 | Tampering | D-10 누락 파일 (admin-dashboard.integration.spec, upload.service.spec, password-reset.tsx, admin-dashboard.spec) | mitigate | STEP 0 dynamic inventory + multi-pattern bulk rename covers all 4 files. Acceptance_criteria 에 각 파일별 post-rename presence 검증. |
</threat_model>

<verification>
Full phase-level verification gate (run at plan completion):

```bash
# 1. SC-1: CI parity
pnpm install --frozen-lockfile
pnpm -r lint && pnpm -r typecheck && pnpm -r test && pnpm -r build

# 2. SC-1 + SC-4 audit (D-10 line-level allowlist)
./scripts/audit-brand-rename.sh

# 3. Manifest integrity
jq -r '.name' package.json apps/*/package.json packages/*/package.json
# Expected: all four @grabit/*

# 4. Decision-scoped exceptions (D-01, D-03, D-05, D-07 exception) — D-16 safe patterns
rg -c 'grapit_dev' docker-compose.yml 2>/dev/null || echo 0        # expect 1
rg -c '/grapit/\.env' CLAUDE.md 2>/dev/null || echo 0              # expect >=1
rg -c 'grapit-cloudrun@' .github/workflows/deploy.yml scripts/provision-valkey.sh 2>/dev/null | awk -F: '{s+=$2} END {print s+0}'  # expect >=4
rg -c '@social\.grabit\.com' apps/api/src/modules/auth/auth.service.ts 2>/dev/null || echo 0  # expect >=1

# 5. D-10 누락 파일 후처리 확인
rg -l 'grabit_test|grabit-uploads|cdn\.heygrabit\.com|Grabit|admin@grabit\.test' \
  apps/api/test/admin-dashboard.integration.spec.ts \
  apps/api/src/modules/admin/upload.service.spec.ts \
  apps/api/src/modules/auth/email/templates/password-reset.tsx \
  apps/web/e2e/admin-dashboard.spec.ts 2>/dev/null | wc -l
# Expected: 4

# 6. Seed ↔ CI secret sync (post-merge action)
# gh secret set TEST_USER_EMAIL --body 'admin@grabit.test'
```
</verification>

<success_criteria>
- [x] **SC-1:** `pnpm install --frozen-lockfile && pnpm -r lint && pnpm -r typecheck && pnpm -r test && pnpm -r build` all exit 0
- [x] **SC-1:** Zero `@grapit/` references in source/config (excluding `.planning/`, `node_modules/`, `pnpm-lock.yaml` interim, `dist/`, `.next/`)
- [x] **SC-4:** Zero git diff under `.planning/milestones/`, completed phase dirs, `.planning/quick/` (active 13-* 예외 per LOW #11)
- [x] D-01 exception: `docker-compose.yml` POSTGRES_PASSWORD=`grapit_dev` retained (1 grep hit)
- [x] D-03 exception: `CLAUDE.md` `/grapit/.env` filesystem path retained
- [x] D-05 exception: `grapit-cloudrun@` SA references retained (4 occurrences: deploy.yml×2 + provision-valkey.sh×2) — deploy.yml untouched in Plan 01
- [x] D-06: all dev/CI DB identifiers (grapit_test, container_name, POSTGRES_DB, POSTGRES_USER) renamed
- [x] D-07: all `@grapit.com` user-facing emails replaced with `@heygrabit.com` in legal/email fixtures
- [x] D-07 exception: `@social.grapit.com` (internal synthetic email, user-invisible, no mailbox) → `@social.grabit.com` (NOT `@heygrabit.com`) — enforced in Task 2 acceptance_criteria
- [x] **D-10 (Revision 2):** 누락 4개 파일 처리 (admin-dashboard.integration.spec, upload.service.spec, password-reset.tsx, admin-dashboard.spec), STEP 0 inventory 생성, multi-pattern (7 patterns) bulk rename, cdn.grapit.kr → cdn.heygrabit.com
- [x] **D-10 (Revision 2):** audit-brand-rename.sh 가 line-level allowlist 4 entries 구현
- [x] **D-16 (Revision 2):** 모든 `<verify>` / `<acceptance_criteria>` bash snippet 이 no-match 시 pipe 중단 방지 패턴 사용
- [x] Audit script `scripts/audit-brand-rename.sh` exists, executable, passes
- [x] Post-merge action (GitHub secret update) documented in PR description
</success_criteria>

<output>
After completion, create `.planning/phases/13-grapit-grabit-rename/13-01-SUMMARY.md` documenting:
- Files modified count and categories (D-10 누락 4개 파일 포함)
- Lockfile regeneration proof (`pnpm install --frozen-lockfile` exit 0)
- SC-1 + SC-4 audit results (D-10 line-level allowlist 4 entries)
- Decision exceptions enforced (D-01 grapit_dev, D-03 /grapit/.env, D-05 grapit-cloudrun@, D-07 exception @social.grabit.com)
- Post-merge action checklist (TEST_USER_EMAIL GitHub secret update)
- Handoff note to Plan 02 (P2 — user-facing copy)
- Revision 2 adjustments applied: D-10 (inventory + 4 files + 7 patterns + cdn fixture domain), D-16 (rg pipe safety), LOW #11 (active-phase exception)
</output>
</output>
</output>
