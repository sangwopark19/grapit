---
phase: 13-grapit-grabit-rename
plan: 04
type: execute
wave: 4
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - scripts/rollback-cutover.sh
  - scripts/cleanup-old-grapit-resources.sh
  - .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md
autonomous: false
requirements:
  - SC-3
  - SC-4
user_setup:
  - service: oauth-kakao
    why: "heygrabit.com 도메인 cutover 이후 카카오 로그인 callback URL을 신규 도메인으로 재등록"
    env_vars: []
    dashboard_config:
      - task: "Kakao Developers 콘솔 → My Application → 플랫폼 → Web 사이트 도메인 & Redirect URI 추가"
        location: "https://developers.kakao.com/console/app/{APP_ID}/config/platform"
  - service: oauth-naver
    why: "heygrabit.com 도메인 cutover 이후 네이버 로그인 Callback URL 재등록"
    env_vars: []
    dashboard_config:
      - task: "Naver Developers 콘솔 → 내 애플리케이션 → API 설정 → 서비스 URL & Callback URL 수정"
        location: "https://developers.naver.com/apps/#/myapps/{APP_ID}/api"
  - service: oauth-google
    why: "heygrabit.com 도메인 cutover 이후 Google OAuth Authorized redirect URI 재등록"
    env_vars: []
    dashboard_config:
      - task: "GCP Console → APIs & Services → Credentials → OAuth 2.0 Client ID → Authorized redirect URIs 추가"
        location: "https://console.cloud.google.com/apis/credentials"
  - service: gcp-cloud-run-domain-mapping
    why: "heygrabit.com 도메인을 구 grapit-web → 새 grabit-web 으로 cutover (delete+create 순서, Google-managed cert 재발급 대기)"
    env_vars: []
    dashboard_config:
      - task: "gcloud beta run domain-mappings delete/create (상세 순서는 Task 3 참조)"
        location: "local shell"

must_haves:
  truths:
    - "scripts/rollback-cutover.sh 이 존재하고 executable + gcloud describe 사전 캡처(rollback.yaml) 로직 포함"
    - "heygrabit.com 도메인 매핑이 구 grapit-web 에서 새 grabit-web 으로 전환 완료됨"
    - "cutover 완료 후 curl https://heygrabit.com/ 200, curl https://heygrabit.com/api/v1/health 200"
    - "3개 OAuth provider(카카오/네이버/구글) 콘솔에서 heygrabit.com callback URL 등록 완료"
    - "실제 소셜 로그인 3종 E2E 성공 (HUMAN-UAT)"
    - "실제 이메일 발송 시 [Grabit] 비밀번호 재설정 subject 수신 (HUMAN-UAT)"
    - "실제 SMS 발송 시 [Grabit] 인증번호 ... body 수신 (HUMAN-UAT)"
    - "7일 유예 기간 이후 scripts/cleanup-old-grapit-resources.sh 로 구 Cloud Run 서비스 2개 + Artifact Registry grapit repo + Sentry grapit-* 프로젝트 2개 정리"
    - "`.planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md` 존재, 11개 체크리스트 항목 모두 통과 기록"
    - "완료된 phase/milestone/quick 폴더 diff 0건 (SC-4)"
  artifacts:
    - path: "scripts/rollback-cutover.sh"
      provides: "Executable rollback script for domain mapping + describe capture step (pre-cutover safety net)"
    - path: "scripts/cleanup-old-grapit-resources.sh"
      provides: "7-day grace cleanup for old grapit-* Cloud Run svc + AR repo + Sentry archive (D-02b)"
    - path: ".planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md"
      provides: "Cutover-day checklist (11 items: domain swap + OAuth register + email/SMS verify + old-service 7-day verify)"
    - path: "(GCP) Cloud Run domain mapping: heygrabit.com → grabit-web"
      provides: "New primary routing; managed cert provisioned; HTTPS 200"
  key_links:
    - from: "heygrabit.com DNS A/AAAA"
      to: "(GCP) Cloud Run load balancer IP"
      via: "DNS (unchanged)"
      pattern: "heygrabit\\.com"
    - from: "(GCP) domain-mapping heygrabit.com"
      to: "(Cloud Run) grabit-web"
      via: "spec.routeName"
      pattern: "grabit-web"
    - from: "OAuth provider Authorized Redirect URI"
      to: "(Cloud Run) grabit-api /api/v1/auth/social/{provider}/callback"
      via: "https://heygrabit.com/api/v1/auth/social/{kakao|naver|google}/callback"
      pattern: "heygrabit\\.com/api/v1/auth/social/"
---

<objective>
Phase 13 Plan 04 (P4) — 도메인 cutover + 7일 유예 후 정리. 블루-그린의 "그린" 쪽 신호등을 올린다:

1. 롤백 스크립트 사전 준비 (describe capture + rollback 경로)
2. `heygrabit.com` Cloud Run domain mapping 을 구 `grapit-web` → 새 `grabit-web` 으로 cutover (delete+create, Google-managed cert 재발급 대기)
3. OAuth 3개 provider(카카오/네이버/구글) 콘솔에서 callback URL 재등록
4. HUMAN-UAT: 실제 소셜 로그인 + 이메일 수신 + SMS 수신 확인
5. 7일 유예 후 구 `grapit-*` 리소스 일괄 삭제 (Cloud Run 2개 + AR repo + Sentry 2개 archive)

Purpose: SC-3 "prod Cloud Run 서비스가 grabit 식별자로 정상 동작" 의 나머지 절반 (사용자가 heygrabit.com 으로 접속하면 신 grabit-* 서비스가 응답) 을 달성한다.

Output: 2개 shell script 신규 생성 + HUMAN-UAT 체크리스트 문서 + 실제 domain mapping cutover 수행 + 구 리소스 7일 유예 후 정리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-grapit-grabit-rename/13-CONTEXT.md
@.planning/phases/13-grapit-grabit-rename/13-RESEARCH.md
@.planning/phases/13-grapit-grabit-rename/13-PATTERNS.md
@.planning/phases/13-grapit-grabit-rename/13-VALIDATION.md
@.planning/phases/13-grapit-grabit-rename/13-03-SUMMARY.md

<interfaces>
Cutover sequence (RESEARCH § Pattern 2):
```bash
# 0) 구 매핑 describe → rollback.yaml 저장
gcloud beta run domain-mappings describe \
  --domain=heygrabit.com --region=asia-northeast3 --format=yaml > rollback.yaml

# 1) 구 매핑 삭제 (이 시점부터 도메인 HTTPS는 잠시 503 가능)
gcloud beta run domain-mappings delete \
  --domain=heygrabit.com --region=asia-northeast3 --quiet

# 2) 새 서비스에 매핑 생성
gcloud beta run domain-mappings create \
  --service=grabit-web --domain=heygrabit.com --region=asia-northeast3

# 3) 매핑 status Ready까지 poll (SSL 인증서 자동 발급, 수 분 ~ 십여 분)
for i in $(seq 1 60); do
  STATUS=$(gcloud beta run domain-mappings describe --domain=heygrabit.com \
    --region=asia-northeast3 --format='value(status.conditions[0].status)')
  [ "$STATUS" = "True" ] && break
  sleep 15
done

# 4) 실제 도메인에서 HTTPS 200 확인
curl -sI https://heygrabit.com/ | head -1
```

OAuth callback URL patterns (from deploy.yml env_vars):
- 카카오: `https://heygrabit.com/api/v1/auth/social/kakao/callback`
- 네이버: `https://heygrabit.com/api/v1/auth/social/naver/callback`
- 구글: `https://heygrabit.com/api/v1/auth/social/google/callback`

7-day cleanup targets (D-02b):
- `gcloud run services delete grapit-web --region=asia-northeast3 --quiet`
- `gcloud run services delete grapit-api --region=asia-northeast3 --quiet`
- `gcloud artifacts repositories delete grapit --location=asia-northeast3 --quiet`
- Sentry UI: archive/delete `grapit-web`, `grapit-api` projects
</interfaces>

<decision_constraints>
- D-02: 블루-그린 cutover 방식 (delete+create, 공식 권장)
- D-02b: 7일 유예 후 구 리소스 정리
- D-08: OAuth callback URL 재등록은 P4 HUMAN-UAT 내부에 포함 (3개 provider)
- SC-4: `.planning/` 과거 폴더 diff 0건
- Off-peak 시간대 cutover 권장 (RESEARCH § Risks SSL 발급 대기 mitigation)
</decision_constraints>

<cutover_readiness_gate>
Plan 04 Task 3 (actual domain swap)를 **실행하기 전에** 반드시 확인:

1. Plan 03 Task 4 완료: 새 Cloud Run 서비스 status=True, URL health 200 (또는 503 허용)
2. `./scripts/rollback-cutover.sh` 존재 + executable
3. `rollback.yaml` 파일이 생성되어 있고 구 `spec.routeName: grapit-web` 참조 포함
4. Off-peak 시간대 확인 (KST 새벽 2-5시 권장)
5. 사용자 알림 준비 (옵션: 일시적 503 가능성 공지)
</cutover_readiness_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rollback 스크립트 + Cleanup 스크립트 + HUMAN-UAT 체크리스트 문서 작성</name>
  <files>
    scripts/rollback-cutover.sh,
    scripts/cleanup-old-grapit-resources.sh,
    .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md
  </files>
  <read_first>
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § Pattern 2 (Cloud Run 도메인 cutover), § P4 Cutover + 7일 후 정리
    - .planning/phases/13-grapit-grabit-rename/13-VALIDATION.md § Manual-Only Verifications (cutover + OAuth + email/SMS)
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-02b (7일 유예), § D-08 (OAuth callback 재등록)
  </read_first>
  <action>
    STEP 1 — `scripts/rollback-cutover.sh` 작성:
    ```bash
    #!/usr/bin/env bash
    # scripts/rollback-cutover.sh
    # Phase 13 Plan 04 — Rollback the heygrabit.com domain mapping cutover.
    # Usage:
    #   1. BEFORE cutover: ./scripts/rollback-cutover.sh capture       # captures rollback.yaml
    #   2. AFTER cutover (if needed): ./scripts/rollback-cutover.sh restore
    set -euo pipefail

    DOMAIN="heygrabit.com"
    REGION="asia-northeast3"
    OLD_SERVICE="grapit-web"
    ROLLBACK_FILE="rollback.yaml"

    case "${1:-}" in
      capture)
        echo "=== Capturing current domain mapping -> $ROLLBACK_FILE ==="
        gcloud beta run domain-mappings describe \
          --domain="$DOMAIN" \
          --region="$REGION" \
          --format=yaml > "$ROLLBACK_FILE"
        if grep -q "routeName: $OLD_SERVICE" "$ROLLBACK_FILE"; then
          echo "  Captured mapping for $OLD_SERVICE — rollback path ready"
        else
          echo "  WARNING: expected routeName=$OLD_SERVICE not found in rollback.yaml"
          echo "  Current mapping:"
          grep routeName "$ROLLBACK_FILE" || true
        fi
        ;;
      restore)
        echo "=== Restoring domain mapping to $OLD_SERVICE ==="
        [ -f "$ROLLBACK_FILE" ] || { echo "ERROR: $ROLLBACK_FILE not found"; exit 1; }
        # Delete current mapping (presumably pointing to grabit-web after failed cutover)
        gcloud beta run domain-mappings delete \
          --domain="$DOMAIN" \
          --region="$REGION" \
          --quiet || true
        # Recreate pointing to old service
        gcloud beta run domain-mappings create \
          --service="$OLD_SERVICE" \
          --domain="$DOMAIN" \
          --region="$REGION"
        # Poll until Ready
        for i in $(seq 1 60); do
          STATUS=$(gcloud beta run domain-mappings describe --domain="$DOMAIN" --region="$REGION" --format='value(status.conditions[0].status)' 2>/dev/null || echo "Unknown")
          echo "  [$i/60] status=$STATUS"
          [ "$STATUS" = "True" ] && { echo "  Rollback ready"; break; }
          sleep 15
        done
        curl -sI "https://$DOMAIN/" | head -1 || true
        echo "=== Rollback complete ==="
        ;;
      *)
        echo "Usage: $0 {capture|restore}"
        echo "  capture: save current mapping as rollback.yaml (run BEFORE cutover)"
        echo "  restore: revert domain mapping to $OLD_SERVICE (run AFTER failed cutover)"
        exit 1
        ;;
    esac
    ```
    Make executable: `chmod +x scripts/rollback-cutover.sh`

    STEP 2 — `scripts/cleanup-old-grapit-resources.sh` 작성 (D-02b 7일 유예 후 실행):
    ```bash
    #!/usr/bin/env bash
    # scripts/cleanup-old-grapit-resources.sh
    # Phase 13 Plan 04 — Delete old grapit-* resources after 7-day grace period.
    # Run: ./scripts/cleanup-old-grapit-resources.sh <GCP_PROJECT_ID>
    #
    # Preconditions:
    #   - heygrabit.com has been mapped to grabit-web for at least 7 days
    #   - No ongoing incident on new grabit-* services
    #   - Sentry grabit-* projects accumulating events normally
    set -euo pipefail

    PROJECT_ID="${1:?Usage: $0 <GCP_PROJECT_ID>}"
    REGION="asia-northeast3"

    echo "=== [1/3] Delete old Cloud Run services ==="
    for svc in grapit-web grapit-api; do
      if gcloud run services describe "$svc" --region="$REGION" --project="$PROJECT_ID" >/dev/null 2>&1; then
        echo "  Deleting $svc..."
        gcloud run services delete "$svc" --region="$REGION" --project="$PROJECT_ID" --quiet
      else
        echo "  $svc not found (already deleted?) — skip"
      fi
    done

    echo "=== [2/3] Delete old Artifact Registry repo 'grapit' ==="
    if gcloud artifacts repositories describe grapit --location="$REGION" --project="$PROJECT_ID" >/dev/null 2>&1; then
      echo "  Deleting AR repo grapit (includes all images)..."
      gcloud artifacts repositories delete grapit --location="$REGION" --project="$PROJECT_ID" --quiet
    else
      echo "  AR repo grapit not found — skip"
    fi

    echo "=== [3/3] Sentry old projects (manual, Sentry has no gcloud-equivalent CLI) ==="
    echo "  Open Sentry dashboard → grapit-web project → Settings → General → Remove Project"
    echo "  Open Sentry dashboard → grapit-api project → Settings → General → Remove Project"
    echo "  (or use Sentry CLI: sentry-cli projects remove {org} grapit-{web,api})"

    echo "=== Cleanup complete ==="
    echo "Verify:"
    echo "  gcloud run services list --region=$REGION --project=$PROJECT_ID | grep grapit-  # expected: empty"
    echo "  gcloud artifacts repositories list --location=$REGION --project=$PROJECT_ID | grep grapit  # expected: empty"
    ```
    Make executable: `chmod +x scripts/cleanup-old-grapit-resources.sh`

    STEP 3 — `.planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md` 작성:
    ```markdown
    # Phase 13 — HUMAN-UAT (Cutover-day checklist)

    **Planned:** 2026-04-22
    **Cutover target time:** _(TBD, off-peak KST 02:00-05:00 recommended)_
    **Executor:** _(developer name)_
    **Rollback script:** `scripts/rollback-cutover.sh`

    ## Pre-Cutover (T-30min)

    - [ ] Plan 03 완료 확인: 새 Cloud Run 서비스 `grabit-api` + `grabit-web` status=True (`gcloud run services describe ... --format='value(status.conditions[0].status)'` == True)
    - [ ] 새 서비스 health check green (직접 .run.app URL로 curl 200)
    - [ ] `./scripts/rollback-cutover.sh capture` 실행 → `rollback.yaml` 생성, `routeName: grapit-web` 확인
    - [ ] Sentry 새 프로젝트에 이벤트 수신 확인 (최근 24h)
    - [ ] off-peak 시간대 확인 + (옵션) 사용자 공지

    ## Cutover (T-0)

    - [ ] Step 1: 구 매핑 삭제 — `gcloud beta run domain-mappings delete --domain=heygrabit.com --region=asia-northeast3 --quiet`
    - [ ] Step 2: 새 매핑 생성 — `gcloud beta run domain-mappings create --service=grabit-web --domain=heygrabit.com --region=asia-northeast3`
    - [ ] Step 3: poll status Ready — 최대 15분 대기 (SSL 자동 재발급)
    - [ ] Step 4: `curl -sI https://heygrabit.com/ | head -1` → `HTTP/2 200`
    - [ ] Step 5: `curl -sI https://heygrabit.com/api/v1/health | head -1` → `HTTP/2 200`

    ## OAuth Callback Re-Registration (T+15min)

    - [ ] 카카오: https://developers.kakao.com/console/app/{APP_ID}/config/platform — Web 사이트 도메인 `https://heygrabit.com`, Redirect URI `https://heygrabit.com/api/v1/auth/social/kakao/callback` 추가
    - [ ] 네이버: https://developers.naver.com/apps/#/myapps/{APP_ID}/api — 서비스 URL `https://heygrabit.com`, Callback URL `https://heygrabit.com/api/v1/auth/social/naver/callback` 추가
    - [ ] 구글: https://console.cloud.google.com/apis/credentials — OAuth 2.0 Client ID의 Authorized redirect URIs 에 `https://heygrabit.com/api/v1/auth/social/google/callback` 추가

    ## User-Facing Verification (T+30min)

    - [ ] 카카오 로그인 E2E 성공 (heygrabit.com 에서 로그인 → 가입자 정보 확인)
    - [ ] 네이버 로그인 E2E 성공
    - [ ] 구글 로그인 E2E 성공
    - [ ] 비밀번호 재설정 요청 → 수신 이메일 subject `[Grabit] 비밀번호 재설정` 확인 (실제 mailbox)
    - [ ] 회원가입 SMS OTP 요청 → 수신 SMS body `[Grabit] 인증번호 XXXXXX (3분 이내 입력)` 확인 (실제 단말)
    - [ ] Sentry 새 프로젝트(grabit-api, grabit-web)에 prod 트래픽 이벤트 수신 확인

    ## Rollback Trigger Conditions (any failing → run rollback.sh)

    - domain mapping status가 15분 이상 Ready 가 아닌 경우
    - `https://heygrabit.com/` 이 지속적으로 5xx
    - OAuth provider 3종 중 callback 재등록 실패 + 로그인 완전 중단
    - 즉시 `./scripts/rollback-cutover.sh restore` 실행 → 구 grapit-web으로 복귀

    **Rollback 후 재시도:** Plan 03 재검증 → Plan 04 재실행 (새 cutover 시간 확정)

    ## 7-day Grace — Cleanup (T+7days)

    - [ ] 7일 유예 기간 동안 grabit-* 서비스 장애/롤백 요청 없음 확인
    - [ ] Sentry 구 grapit-* 프로젝트에 최근 24h 이벤트 0건 확인 (트래픽 단절)
    - [ ] `./scripts/cleanup-old-grapit-resources.sh $(gcloud config get-value project)` 실행
    - [ ] Sentry UI에서 grapit-* 프로젝트 2개 수동 archive/delete
    - [ ] 확인: `gcloud run services list --region=asia-northeast3 | grep grapit-` → empty
    - [ ] 확인: `gcloud artifacts repositories list --location=asia-northeast3 | grep grapit` → empty

    ## Sign-Off

    - **Cutover executor:** _________________ (date: ________)
    - **UAT pass:** _________________ (date: ________)
    - **7-day cleanup:** _________________ (date: ________)
    ```

    STEP 4 — 3개 파일이 production-safe:
    - rollback 스크립트 dry-run 불가 (실제 gcloud 호출) → CI에서는 syntax check만
    - cleanup 스크립트는 7일 후 수동 실행만, 직접 main에서 실행 금지

    CONSTRAINTS:
    - `.planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md` 는 이 phase의 **활성** 폴더에 위치하므로 SC-4 예외(현재 phase 폴더는 수정 허용)
    - rollback.yaml 파일은 커밋 대상이 아님 (`.gitignore`에 추가하거나 임시 파일로만 처리)
    - cleanup 스크립트는 7일 유예 이후에만 실행되어야 함 — 스크립트 상단 주석으로 명시됨
  </action>
  <verify>
    <automated>
      set -euo pipefail
      # Files exist + executable
      test -x scripts/rollback-cutover.sh || { echo "FAIL rollback script not executable"; exit 1; }
      test -x scripts/cleanup-old-grapit-resources.sh || { echo "FAIL cleanup script not executable"; exit 1; }
      test -f .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md || { echo "FAIL UAT checklist missing"; exit 1; }
      # Shebangs
      head -1 scripts/rollback-cutover.sh | grep -q '^#!/usr/bin/env bash' || { echo "FAIL rollback shebang"; exit 1; }
      head -1 scripts/cleanup-old-grapit-resources.sh | grep -q '^#!/usr/bin/env bash' || { echo "FAIL cleanup shebang"; exit 1; }
      # set -euo pipefail present
      rg -q 'set -euo pipefail' scripts/rollback-cutover.sh || { echo "FAIL rollback set -e"; exit 1; }
      rg -q 'set -euo pipefail' scripts/cleanup-old-grapit-resources.sh || { echo "FAIL cleanup set -e"; exit 1; }
      # rollback: capture + restore sub-commands
      rg -q 'capture\)' scripts/rollback-cutover.sh || { echo "FAIL rollback capture"; exit 1; }
      rg -q 'restore\)' scripts/rollback-cutover.sh || { echo "FAIL rollback restore"; exit 1; }
      # cleanup: 3 sections
      rg -q 'Delete old Cloud Run services' scripts/cleanup-old-grapit-resources.sh || { echo "FAIL cleanup cloud run"; exit 1; }
      rg -q "Delete old Artifact Registry repo 'grapit'" scripts/cleanup-old-grapit-resources.sh || { echo "FAIL cleanup AR"; exit 1; }
      rg -q 'Sentry old projects' scripts/cleanup-old-grapit-resources.sh || { echo "FAIL cleanup Sentry"; exit 1; }
      # HUMAN-UAT checklist has 11 tickable items minimum across all sections (3 OAuth + cutover 5 + UAT 6 + cleanup 6 minimum)
      CHECKS=$(rg -c '^- \[ \]' .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md)
      [ "$CHECKS" -ge 20 ] || { echo "FAIL UAT checklist too short: $CHECKS"; exit 1; }
      # SC-4 historical preservation (this plan writes to activephase dir — still verify completed phases untouched)
      HIST=$(git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/ | wc -l | tr -d ' ')
      [ "$HIST" = "0" ] || { echo "FAIL SC-4"; exit 1; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/rollback-cutover.sh` exit **0**
    - `test -x scripts/cleanup-old-grapit-resources.sh` exit **0**
    - `test -f .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md` exit **0**
    - `head -1 scripts/rollback-cutover.sh | grep -q '^#!'` exit **0**
    - `rg -q 'capture\)' scripts/rollback-cutover.sh && rg -q 'restore\)' scripts/rollback-cutover.sh` exit **0**
    - `rg -q "Delete old Cloud Run services" scripts/cleanup-old-grapit-resources.sh` exit **0**
    - `rg -c '^- \[ \]' .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md` ≥ **20** (ample checklist coverage)
    - HUMAN-UAT contains the three OAuth provider console URLs (kakao/naver/google)
    - `git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/ | wc -l` equals **0**
  </acceptance_criteria>
  <done>
    rollback-cutover.sh (capture/restore sub-commands), cleanup-old-grapit-resources.sh, 13-HUMAN-UAT.md (20+ 체크리스트 아이템 + OAuth 3개 provider 콘솔 링크) 모두 생성 완료, SC-4 historical 보존.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HUMAN-ACTION — Pre-cutover readiness (rollback capture + off-peak timing)</name>
  <what-built>없음. Cutover 직전 안전망 준비 단계.</what-built>
  <read_first>
    - Task 1에서 생성한 `scripts/rollback-cutover.sh` (capture sub-command)
    - `.planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md` § Pre-Cutover
  </read_first>
  <how-to-verify>
    개발자가 다음을 확인하고 `approved` 후 재개:

    1. **Plan 03 Task 4 완료 재확인:**
       ```bash
       for svc in grabit-api grabit-web; do
         STATUS=$(gcloud run services describe "$svc" --region=asia-northeast3 --format='value(status.conditions[0].status)')
         echo "$svc: $STATUS"
       done
       # Expected: True / True
       ```

    2. **Rollback capture:**
       ```bash
       ./scripts/rollback-cutover.sh capture
       cat rollback.yaml | grep routeName
       # Expected: routeName: grapit-web
       ```

    3. **Off-peak 확인:** KST 02:00-05:00 권장. 피크 시간대라면 대기.

    4. **사용자 공지 (옵션):** 블로그/Twitter/공지 페이지에 `heygrabit.com 도메인 인증서 재발급으로 수 분간 502/503 가능` 안내.

    5. **새 서비스 smoke:**
       ```bash
       curl -sI "$(gcloud run services describe grabit-web --region=asia-northeast3 --format='value(status.url)')/" | head -1
       curl -sI "$(gcloud run services describe grabit-api --region=asia-northeast3 --format='value(status.url)')/api/v1/health" | head -1
       # Expected: 200 / 200
       ```
  </how-to-verify>
  <acceptance_criteria>
    - 두 서비스 모두 status=True
    - rollback.yaml 존재, `routeName: grapit-web` 포함
    - Off-peak 시간 확인됨 (또는 user가 수용하는 위험으로 강행 결정)
    - 새 서비스 health 200
  </acceptance_criteria>
  <resume-signal>Type "approved" after rollback.yaml is captured and off-peak timing is confirmed.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: HUMAN-ACTION — Cutover execution (domain-mapping delete + create + poll + verify)</name>
  <what-built>
    `heygrabit.com` Cloud Run domain mapping 을 구 `grapit-web` → 새 `grabit-web` 으로 cutover. SSL 인증서 재발급 대기. Claude가 자동화할 수 없는 운영 작업 (gcloud CLI 직접 호출 + SSL 대기 시간 존재).
  </what-built>
  <read_first>
    - Task 1 rollback script
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § Pattern 2 정확한 순서
    - .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md § Cutover
  </read_first>
  <how-to-verify>
    개발자 명령 순서:

    1. **구 매핑 삭제:**
       ```bash
       gcloud beta run domain-mappings delete \
         --domain=heygrabit.com \
         --region=asia-northeast3 \
         --quiet
       ```
       이 시점부터 heygrabit.com은 수 분간 5xx 가능.

    2. **새 매핑 생성:**
       ```bash
       gcloud beta run domain-mappings create \
         --service=grabit-web \
         --domain=heygrabit.com \
         --region=asia-northeast3
       ```

    3. **Status Ready 까지 poll (수 분 ~ 15분):**
       ```bash
       for i in $(seq 1 60); do
         STATUS=$(gcloud beta run domain-mappings describe \
           --domain=heygrabit.com \
           --region=asia-northeast3 \
           --format='value(status.conditions[0].status)' 2>/dev/null || echo "Unknown")
         echo "[$i/60] status=$STATUS"
         [ "$STATUS" = "True" ] && break
         sleep 15
       done
       ```

    4. **실제 도메인 HTTPS 검증:**
       ```bash
       curl -sI https://heygrabit.com/ | head -1
       # Expected: HTTP/2 200 (또는 HTTP/1.1 200)
       curl -sI https://heygrabit.com/api/v1/health | head -1
       # Expected: HTTP/2 200 (api는 redirect 체크 포함 시 -L 추가)
       ```

    5. **현재 mapping 확인:**
       ```bash
       gcloud beta run domain-mappings describe \
         --domain=heygrabit.com \
         --region=asia-northeast3 \
         --format='value(spec.routeName)'
       # Expected: grabit-web
       ```

    **실패 경로:** Step 3에서 15분이 지나도 Ready가 아니거나 Step 4에서 지속적 5xx → 즉시 `./scripts/rollback-cutover.sh restore` 실행.
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud beta run domain-mappings describe --domain=heygrabit.com --region=asia-northeast3 --format='value(spec.routeName)'` returns **grabit-web**
    - `gcloud beta run domain-mappings describe --domain=heygrabit.com --region=asia-northeast3 --format='value(status.conditions[0].status)'` returns **True**
    - `curl -sI https://heygrabit.com/ | head -1 | grep -q '200'` exit **0**
    - `curl -sI https://heygrabit.com/api/v1/health | head -1 | grep -q '200'` exit **0**
    - 소요 시간 기록 (예상 5-15분)
  </acceptance_criteria>
  <resume-signal>Type "approved" after the domain mapping reports `routeName: grabit-web` with status True and `curl -sI https://heygrabit.com/` returns 200.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: HUMAN-ACTION — OAuth callback URL 재등록 (카카오/네이버/구글) + 실제 로그인/이메일/SMS UAT</name>
  <what-built>없음. 3개 외부 OAuth provider 콘솔 + 실제 user flow UAT.</what-built>
  <read_first>
    - Task 1에서 생성한 13-HUMAN-UAT.md § OAuth Callback Re-Registration + User-Facing Verification
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-08 (OAuth callback 재등록)
  </read_first>
  <how-to-verify>
    **OAuth Callback Re-Registration (Task 3 cutover 직후):**

    1. **카카오:** https://developers.kakao.com/console/app/{APP_ID}/config/platform
       - Web 사이트 도메인에 `https://heygrabit.com` 추가
       - Redirect URI에 `https://heygrabit.com/api/v1/auth/social/kakao/callback` 추가
       - 저장

    2. **네이버:** https://developers.naver.com/apps/#/myapps/{APP_ID}/api
       - 서비스 URL에 `https://heygrabit.com` 추가
       - Callback URL에 `https://heygrabit.com/api/v1/auth/social/naver/callback` 추가
       - 저장

    3. **구글:** https://console.cloud.google.com/apis/credentials
       - OAuth 2.0 Client ID 클릭 → Authorized redirect URIs 에 `https://heygrabit.com/api/v1/auth/social/google/callback` 추가
       - 저장

    **User-Facing UAT (등록 완료 후 15분 propagation 대기):**

    4. **카카오 로그인:**
       - https://heygrabit.com/auth/login 접속
       - "카카오로 로그인" 클릭 → Kakao login page → 승인 → heygrabit.com 로 리다이렉트
       - 사용자 프로필 페이지에서 닉네임 확인

    5. **네이버 로그인 동일**
    6. **구글 로그인 동일**

    7. **비밀번호 재설정 이메일:**
       - https://heygrabit.com/auth/password-reset 에 본인 이메일 입력 → 제출
       - 실제 mailbox에서 subject `[Grabit] 비밀번호 재설정` 이메일 수신 확인
       - From 필드: RESEND_FROM_EMAIL secret 값 (이 시점에서 mailbox는 이전 @grapit.com 또는 @heygrabit.com 로 미설정 상태일 수 있음 — **Deferred**: Plan 04 deferred follow-up 항목)

    8. **SMS OTP:**
       - https://heygrabit.com/auth/signup 접속 → 전화번호 입력 → OTP 발송
       - 실제 단말에서 body `[Grabit] 인증번호 XXXXXX (3분 이내 입력)` 수신 확인
       - 발신자 ID: INFOBIP_SENDER (KISA 제약으로 number-only 상태; 브랜드 텍스트는 body에만 표시됨)

    9. **Sentry prod 이벤트 확인:**
       - Sentry dashboard → grabit-web / grabit-api 프로젝트 → Issues
       - 실제 prod 트래픽 이벤트 수신 확인 (최근 15분 내)

    **실패 경로:**
    - OAuth 3종 중 하나 실패 → 해당 provider 콘솔의 callback URL 오타 확인, 또는 Plan 03의 GitHub vars CLOUD_RUN_API_URL 확인
    - 이메일/SMS 수신 실패 → Resend/Infobip 콘솔에서 로그 확인, sender ID/from 값 재설정 필요 가능
    - 심각한 실패 → Task 3 `scripts/rollback-cutover.sh restore` 로 롤백 + Plan 03 재검증
  </how-to-verify>
  <acceptance_criteria>
    - 3개 OAuth provider 모두 heygrabit.com 로그인 E2E 성공 (각 provider 사용자 정보 수신)
    - 비밀번호 재설정 이메일 subject `[Grabit] 비밀번호 재설정` 실제 수신
    - SMS 인증 body `[Grabit] 인증번호` 실제 수신
    - Sentry grabit-{web,api} 프로젝트에 prod 트래픽 이벤트 수신 (최근 15분)
    - Plan 01 Task 2의 TEST_USER_EMAIL GitHub secret 갱신 완료 후 CI 재실행 → E2E 12/12 green (또는 해당 시점에 실행 가능한 만큼)
  </acceptance_criteria>
  <resume-signal>Type "approved" after OAuth 3종 + email + SMS UAT all pass. Reply with Sentry event ID(s) + screenshots if feasible.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: HUMAN-ACTION — 7-day grace period monitoring + cleanup execution (D-02b)</name>
  <what-built>
    7일 유예 이후 구 `grapit-*` 리소스 삭제. D-02b 에 따라 cutover 후 7일간 구 서비스 유지 → 문제 없으면 `scripts/cleanup-old-grapit-resources.sh` 실행.
  </what-built>
  <read_first>
    - Task 1에서 생성한 `scripts/cleanup-old-grapit-resources.sh`
    - 13-HUMAN-UAT.md § 7-day Grace — Cleanup
    - Task 4의 UAT sign-off 기록 (날짜 기준)
  </read_first>
  <how-to-verify>
    **7일 유예 동안 모니터링:**

    - 매일 Sentry grabit-{web,api} 프로젝트 에러 비율 확인
    - heygrabit.com 사용자 report / 지원 문의 확인
    - 구 grapit-* 서비스 로그 확인 → 마지막 요청 timestamp 확인 (트래픽이 완전히 단절되었는지)

    **T+7일 실행 (날짜 기록):**

    1. **사전 점검:**
       ```bash
       # 구 서비스 최근 요청 확인 (트래픽 단절)
       gcloud run services logs read grapit-api --region=asia-northeast3 --limit=10
       gcloud run services logs read grapit-web --region=asia-northeast3 --limit=10
       # 마지막 요청이 T-7일 이전이면 안전
       ```

    2. **Cleanup 스크립트 실행:**
       ```bash
       ./scripts/cleanup-old-grapit-resources.sh $(gcloud config get-value project)
       ```

    3. **Sentry 수동 정리:**
       - Sentry dashboard → grapit-web → Settings → General → Remove Project
       - Sentry dashboard → grapit-api → Settings → General → Remove Project

    4. **검증:**
       ```bash
       gcloud run services list --region=asia-northeast3 | grep grapit-
       # Expected: empty
       gcloud artifacts repositories list --location=asia-northeast3 | grep grapit
       # Expected: empty
       ```

    5. **Post-cleanup sanity:** heygrabit.com HTTPS 200 유지 확인 (cleanup이 새 서비스에 영향 주지 않음).

    6. **13-HUMAN-UAT.md 의 7-day Grace 섹션 체크박스 모두 [x] 로 업데이트 후 commit.**
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud run services list --region=asia-northeast3 --format='value(metadata.name)' | grep -c grapit-` equals **0**
    - `gcloud artifacts repositories list --location=asia-northeast3 --format='value(name)' | grep -c grapit$` equals **0**
    - Sentry `grapit-web` / `grapit-api` 프로젝트 archive/delete 완료 (Sentry UI screenshot or API query)
    - `curl -sI https://heygrabit.com/ | head -1 | grep -q '200'` exit **0** (cleanup 이후 여전히 정상)
    - `13-HUMAN-UAT.md` § 7-day Grace 섹션 체크박스 모두 완료 + commit
  </acceptance_criteria>
  <resume-signal>Type "approved" after all old grapit-* resources are deleted and heygrabit.com remains operational. This closes Phase 13.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DNS resolver → Google load balancer | heygrabit.com A/AAAA 레코드는 그대로; load balancer가 domain-mapping 기반으로 라우팅 결정 |
| Cloud Run domain-mapping → service (routeName) | Cutover 는 이 매핑 하나를 교체하는 atomic operation |
| OAuth provider → heygrabit.com/api/v1/auth/social/{p}/callback | Authorized redirect URI 목록에 등록되어야 토큰 교환 성공 |
| Email 수신자 mailbox → Resend verified sender | 도메인 브랜드 변경 시 verified sender 재설정 가능성 (Assumption A11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-18 | Spoofing | SSL 인증서 재발급 대기 중 브라우저 경고 | mitigate | Off-peak 시간대 cutover + Google-managed cert 자동 재발급 (수 분~15분) + rollback.yaml 즉시 복구 |
| T-13-19 | Tampering | OAuth callback URL 등록 실수 → 악의적 리다이렉트 주입 | mitigate | 각 provider 콘솔에서 정확한 URL 입력 (Task 4 체크리스트), provider 측 validation이 fallback |
| T-13-20 | Denial of service | domain-mapping cutover 중 수 분 5xx | accept | User-acceptable; off-peak + 사용자 공지 준비 |
| T-13-21 | Denial of service | rollback 실패로 도메인 완전 다운 | mitigate | `rollback.yaml` 사전 capture + `scripts/rollback-cutover.sh restore` 테스트된 경로 |
| T-13-22 | Repudiation | 7일 유예 전 구 서비스 삭제 → rollback 불가 | mitigate | Task 5가 T+7일 이후에만 실행되도록 문서화; cleanup 스크립트가 7-day grace 가정 명시 |
| T-13-23 | Information disclosure | rollback.yaml 에 Cloud Run internal routing 정보 | accept | 로컬 파일, git commit 대상 아님 (.gitignore 권장) |
| T-13-24 | Elevation of privilege | OAuth 구 callback URL 가 provider 콘솔에 남아 피싱 위험 | mitigate | 7-day grace 후 구 callback URL (grapit-api 기반) 도 각 provider 콘솔에서 제거 — HUMAN-UAT checklist 추가 항목 |
</threat_model>

<verification>
Final phase-wide verification gate:

```bash
# 1. Domain mapping on new service
gcloud beta run domain-mappings describe --domain=heygrabit.com --region=asia-northeast3 --format='value(spec.routeName,status.conditions[0].status)'
# Expected: grabit-web    True

# 2. HTTPS 200 on prod domain
curl -sI https://heygrabit.com/ | head -1
curl -sI https://heygrabit.com/api/v1/health | head -1
# Both: 200

# 3. Rollback + cleanup scripts exist
test -x scripts/rollback-cutover.sh
test -x scripts/cleanup-old-grapit-resources.sh

# 4. HUMAN-UAT documented
test -f .planning/phases/13-grapit-grabit-rename/13-HUMAN-UAT.md

# 5. T+7: no grapit-* resources
gcloud run services list --region=asia-northeast3 --format='value(metadata.name)' | grep grapit- || echo "clean"
gcloud artifacts repositories list --location=asia-northeast3 --format='value(name)' | grep grapit$ || echo "clean"

# 6. SC-4 historical
git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/
# Expected: empty

# 7. Phase 13 audit script still green (regression check)
./scripts/audit-brand-rename.sh
```
</verification>

<success_criteria>
- [x] **SC-3 (full):** heygrabit.com 매핑이 grabit-web 을 가리키고 HTTPS 200
- [x] **SC-3:** api 도메인 경로(heygrabit.com/api/v1/health) 200
- [x] **SC-3:** 3개 OAuth provider 로그인 E2E 성공
- [x] **SC-3:** Sentry 새 프로젝트에 prod 이벤트 수신
- [x] D-02b: 7일 유예 후 구 grapit-* 리소스 삭제 완료 (Cloud Run 2개 + AR repo + Sentry 2개)
- [x] D-08: 3개 OAuth provider callback URL 재등록 완료
- [x] **SC-4:** 완료된 phase/milestone/quick 폴더 diff 0건
- [x] rollback 스크립트 executable + capture/restore 경로 문서화
- [x] HUMAN-UAT 체크리스트 20+ 항목 완료 표기
</success_criteria>

<output>
After completion, create `.planning/phases/13-grapit-grabit-rename/13-04-SUMMARY.md` documenting:
- Domain mapping cutover timing (start/end timestamps, SSL 재발급 소요 시간)
- 3개 OAuth provider callback URL 등록 스크린샷/메모
- UAT 결과 (11+ 체크리스트 항목 sign-off)
- 7-day grace 기간의 Sentry 에러율 / 사용자 지원 문의 수 기록
- Cleanup 스크립트 실행 결과 (삭제된 리소스 목록)
- Phase 13 완료 선언 + v1.1 milestone status 업데이트 핸드오프

Additional post-phase tasks to consider:
- PROJECT.md Concerns에 `legal mailbox 미개설 (@heygrabit.com)` deferred 항목 추가
- PROJECT.md Concerns에 `Resend verified sender domain (heygrabit.com) / Infobip sender ID 사업자등록 후 재설정` 추가
- ROADMAP.md Phase 13 entry 에 [x] 완료 표시 + completed date
</output>
