---
phase: 13-grapit-grabit-rename
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - .github/workflows/deploy.yml
  - scripts/provision-grabit-infra.sh
autonomous: false
requirements:
  - SC-3
  - SC-4
user_setup:
  - service: gcp-artifact-registry
    why: "새 Artifact Registry 저장소 `grabit` 생성 (기존 repo rename 불가, 이름 immutable)"
    env_vars: []
    dashboard_config:
      - task: "gcloud CLI로 새 AR repo 생성"
        location: "local shell: gcloud artifacts repositories create grabit --repository-format=docker --location=asia-northeast3"
  - service: sentry
    why: "새 Sentry 프로젝트 2개 생성 (grabit-web Next.js platform, grabit-api Node.js platform), 새 DSN 발급"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry Dashboard → grabit-api project → Client Keys (DSN)"
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Sentry Dashboard → grabit-web project → Client Keys (DSN)"
    dashboard_config:
      - task: "grabit-web / grabit-api 프로젝트 신규 생성 후 DSN 복사"
        location: "https://sentry.io/organizations/{ORG}/projects/new/"
  - service: gcp-secret-manager
    why: "기존 `sentry-dsn` Secret Manager 값 교체 (secret 이름은 유지, 값만 갱신)"
    env_vars: []
    dashboard_config:
      - task: "Secret Manager에서 sentry-dsn의 새 version 추가 (grabit-api DSN)"
        location: "gcloud secrets versions add sentry-dsn --data-file=-"
      - task: "GitHub Actions secret NEXT_PUBLIC_SENTRY_DSN 갱신 (grabit-web DSN)"
        location: "gh secret set NEXT_PUBLIC_SENTRY_DSN --body '{new_dsn}'"
  - service: gcp-cloud-run
    why: "deploy.yml 머지 시 새 Cloud Run 서비스 grabit-web/grabit-api가 최초 생성됨 (사용자 도메인 영향 0 — domain mapping은 Plan 04에서 cutover)"
    env_vars: []
    dashboard_config:
      - task: "main push 후 deploy workflow 완료 대기 → 새 서비스 health check"
        location: "gcloud run services describe grabit-{web,api} --region=asia-northeast3"

must_haves:
  truths:
    - "새 Artifact Registry 저장소 `grabit` (asia-northeast3)가 docker 포맷으로 생성되어 있고 image push 가능"
    - "새 Sentry 프로젝트 `grabit-web`, `grabit-api` 가 생성되고 DSN 2건 발급"
    - "GCP Secret Manager의 `sentry-dsn` secret에 grabit-api DSN 새 version이 추가됨 (secret 이름은 유지)"
    - "GitHub Actions secret `NEXT_PUBLIC_SENTRY_DSN` 값이 grabit-web DSN으로 갱신됨"
    - "`.github/workflows/deploy.yml` 의 `AR_REPO`, `WEB_SERVICE`, `API_SERVICE` env가 `grabit`, `grabit-web`, `grabit-api` 로 변경됨"
    - "`grapit-cloudrun@...` service account 참조는 D-05로 deploy.yml 2곳(L81, L168)에 그대로 유지됨 (`# D-05: SA 유지` 주석 포함)"
    - "main push 후 deploy workflow 완료 → `grabit-api`, `grabit-web` Cloud Run 서비스가 asia-northeast3에 신규 생성됨 (status.conditions[0].status == True)"
    - "새 Cloud Run URL 경로로 `curl -I {cloud_run_web_url}/` 200, `curl {cloud_run_api_url}/api/v1/health` 200 (도메인 mapping은 아직 구 서비스 — 사용자 영향 0)"
    - "새 Sentry 프로젝트에 의도적 테스트 이벤트 1건 이상 수신 확인"
    - "GitHub variables `CLOUD_RUN_WEB_URL`, `CLOUD_RUN_API_URL` 이 신규 grabit-* 서비스의 .run.app URL로 갱신됨"
    - "완료된 phase/milestone/quick 폴더 diff 0건 (SC-4)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Deploy workflow pointing at AR_REPO=grabit, WEB_SERVICE=grabit-web, API_SERVICE=grabit-api with grapit-cloudrun@ SA retained (D-05)"
    - path: "scripts/provision-grabit-infra.sh"
      provides: "Idempotent provisioning script: gcloud AR create + gcloud run describe + smoke tests (executable, new file)"
    - path: "(GCP) Artifact Registry repo: grabit"
      provides: "Docker image repo, asia-northeast3"
    - path: "(Sentry) project grabit-web"
      provides: "Next.js error tracking project"
    - path: "(Sentry) project grabit-api"
      provides: "NestJS error tracking project"
    - path: "(GCP Secret Manager) sentry-dsn latest version"
      provides: "New DSN value for grabit-api (secret name retained per D-02)"
    - path: "(Cloud Run) service grabit-api"
      provides: "NestJS API, asia-northeast3, immutable name"
    - path: "(Cloud Run) service grabit-web"
      provides: "Next.js web, asia-northeast3, immutable name"
  key_links:
    - from: ".github/workflows/deploy.yml env"
      to: "(GCP) Cloud Run services"
      via: "google-github-actions/deploy-cloudrun@v3 action"
      pattern: "service: \\${{ env\\.(WEB|API)_SERVICE }}"
    - from: ".github/workflows/deploy.yml env"
      to: "(GCP) Artifact Registry"
      via: "docker push / image URI"
      pattern: "\\${{ env\\.AR_REPO }}"
    - from: "Cloud Run grabit-api (running revision)"
      to: "Secret Manager sentry-dsn"
      via: "secrets: SENTRY_DSN=sentry-dsn:latest mapping"
      pattern: "SENTRY_DSN=sentry-dsn:latest"
    - from: "Cloud Run grabit-web build"
      to: "GitHub secret NEXT_PUBLIC_SENTRY_DSN"
      via: "docker build --build-arg"
      pattern: "NEXT_PUBLIC_SENTRY_DSN=\\${{ secrets\\."
---

<objective>
Phase 13 Plan 03 (P3) — 인프라 식별자 생성. 블루-그린 cutover의 "블루" 쪽을 프로비저닝한다:

1. 새 Artifact Registry 저장소 `grabit` 생성 (기존 `grapit` repo는 7일 유예 후 Plan 04에서 삭제)
2. 새 Sentry 프로젝트 `grabit-web` / `grabit-api` 생성 + DSN 발급
3. Secret Manager `sentry-dsn` 값 + GitHub secret `NEXT_PUBLIC_SENTRY_DSN` 값 교체
4. GitHub variables `CLOUD_RUN_WEB_URL` / `CLOUD_RUN_API_URL` 를 새 .run.app URL로 갱신
5. `.github/workflows/deploy.yml` 의 `AR_REPO`/`WEB_SERVICE`/`API_SERVICE` env 3줄 변경 → main 머지 → 새 Cloud Run 서비스 `grabit-web`/`grabit-api` 최초 생성
6. 새 서비스 health check + Sentry 새 프로젝트에 이벤트 1건 수신 확인

이 plan은 HUMAN-AUTH checkpoint를 포함한다 (gcloud + Sentry console). 완료 시점에서도 사용자는 여전히 구 `grapit-web` 도메인 매핑을 통해 접속하므로 사용자 영향 0. 도메인 cutover는 Plan 04.

Purpose: SC-3 "prod Cloud Run 서비스가 grabit 식별자로 정상 동작" 의 절반 (신 서비스 기동 + health check)을 달성한다. 나머지 절반 (도메인 cutover)은 Plan 04.

Output: deploy.yml 1개 파일 수정 + scripts/provision-grabit-infra.sh 신규 생성 + 5가지 GCP/Sentry/GitHub 콘솔 작업 완료 + 새 Cloud Run 서비스 health green + 새 Sentry 프로젝트 이벤트 수신 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-grapit-grabit-rename/13-CONTEXT.md
@.planning/phases/13-grapit-grabit-rename/13-RESEARCH.md
@.planning/phases/13-grapit-grabit-rename/13-PATTERNS.md
@.planning/phases/13-grapit-grabit-rename/13-VALIDATION.md
@.planning/phases/13-grapit-grabit-rename/13-01-SUMMARY.md
@.planning/phases/13-grapit-grabit-rename/13-02-SUMMARY.md

<interfaces>
Current deploy.yml env (lines 11-13):
```yaml
env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: asia-northeast3
  AR_REPO: grapit
  WEB_SERVICE: grapit-web
  API_SERVICE: grapit-api
```

Current deploy.yml SA references (L81 api, L168 web — D-05 retention):
```yaml
flags: >-
  --service-account=grapit-cloudrun@${{ env.GCP_PROJECT_ID }}.iam.gserviceaccount.com
```

Current deploy.yml secrets mapping (L106-L130):
```yaml
secrets: |
  DATABASE_URL=database-url:latest
  JWT_SECRET=jwt-secret:latest
  ...
  SENTRY_DSN=sentry-dsn:latest              # ← value replaced, NAME retained
  ...
```

Current deploy.yml web build args (L153):
```yaml
--build-arg NEXT_PUBLIC_SENTRY_DSN=${{ secrets.NEXT_PUBLIC_SENTRY_DSN }}
```

GitHub repository variables consumed (vars reference in deploy.yml):
- `vars.CLOUD_RUN_WEB_URL` → used as `FRONTEND_URL` in api deploy
- `vars.CLOUD_RUN_API_URL` → used as `KAKAO_CALLBACK_URL`, `NAVER_CALLBACK_URL`, `GOOGLE_CALLBACK_URL` base + web build args

GitHub secrets consumed:
- `GCP_PROJECT_ID`, `GCP_WIF_PROVIDER`, `GCP_SERVICE_ACCOUNT`, `CLOUD_SQL_CONNECTION_NAME`
- `R2_PUBLIC_HOSTNAME`, `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_TOSS_CLIENT_KEY`
</interfaces>

<decision_constraints>
- D-02: 새 Cloud Run 서비스 (grabit-web, grabit-api) 생성하되 도메인 매핑은 Plan 04에서 cutover (사용자 영향 0)
- D-02b: 구 grapit-* 리소스는 Plan 04의 7일 유예 이후 정리 (이 plan은 구 리소스 touch하지 않음)
- D-05: `grapit-cloudrun@...` SA 유지 (deploy.yml L81, L168 변경 금지; 주석 `# D-05: SA 유지` 추가)
- SC-4: `.planning/` 과거 폴더 touch 0
- **Resend / Infobip 재설정 가능성** (Assumption A11 from RESEARCH): Resend verified sender + Infobip sender ID가 도메인 변경 시 외부 콘솔에서 재설정 필요할 수 있음. 이 plan은 현재 sender 값을 유지(Resend `RESEND_FROM_EMAIL=resend-from-email:latest` secret 이름 유지). Plan 04의 HUMAN-UAT에서 실제 메일/SMS 수신 확인 시 이슈 발생하면 해당 시점 대응.
</decision_constraints>

<manual_steps_preflight>
deploy.yml 수정 PR을 머지하기 **전에** 반드시 완료해야 하는 gcloud + Sentry + GitHub 수동 작업 목록 (Task 1 + Task 2에서 수행):

1. `gcloud artifacts repositories create grabit --repository-format=docker --location=asia-northeast3 --description="Grabit brand rename target repo (Phase 13)"`
2. Sentry dashboard → New Project → `grabit-api` (platform: Node.js) → copy DSN
3. Sentry dashboard → New Project → `grabit-web` (platform: Next.js) → copy DSN
4. `echo "{grabit-api-dsn}" | gcloud secrets versions add sentry-dsn --data-file=-` (secret 이름 유지, 새 version 추가)
5. `gh secret set NEXT_PUBLIC_SENTRY_DSN --body "{grabit-web-dsn}"`
6. `gh variable set CLOUD_RUN_API_URL --body "{new_grabit_api_run_app_url}"` — **deploy 후에 확인 가능**, 첫 deploy 시 자동 생성되는 URL을 이후 rerun에 사용
7. `gh variable set CLOUD_RUN_WEB_URL --body "{new_grabit_web_run_app_url}"` — 동일

**순서 주의:** Step 6-7은 첫 deploy 이후에만 가능 (URL을 모름). 따라서:
- Pre-merge: Step 1-5 완료 + deploy.yml PR 리뷰 완료
- Merge: main push → deploy workflow 기동 → 첫 실패 가능 (CLOUD_RUN_*_URL이 구 URL 가리킴)
- Post-first-deploy: Step 6-7 실행 → workflow_dispatch로 재배포 → grabit-* URL로 정상 동작
</manual_steps_preflight>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN-AUTH — GCP Artifact Registry + Sentry 프로젝트 2개 생성</name>
  <what-built>없음 (human-action). 이 checkpoint는 Claude가 실행할 수 없는 외부 콘솔 작업.</what-built>
  <read_first>
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § Architecture Patterns § System Diagram + § P3 — 인프라 리소스
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-02 (Cloud Run 서비스명 + Sentry + AR 신규 생성)
  </read_first>
  <how-to-verify>
    사용자(개발자)가 다음 명령/작업을 순서대로 실행하고 결과를 확인한 뒤 `approved` 로 재개:

    1. **GCP Artifact Registry 생성 (로컬 shell):**
       ```bash
       gcloud artifacts repositories create grabit \
         --repository-format=docker \
         --location=asia-northeast3 \
         --description="Grabit brand rename target repo (Phase 13)"
       ```
       확인:
       ```bash
       gcloud artifacts repositories describe grabit --location=asia-northeast3 --format='value(name)'
       # Expected: projects/{PROJECT_ID}/locations/asia-northeast3/repositories/grabit
       ```

    2. **Sentry 프로젝트 `grabit-api` 생성:**
       - https://sentry.io/organizations/{ORG}/projects/new/ 로 이동
       - Platform: Node.js
       - Team: (기존 grapit-api와 동일 team)
       - Project name: `grabit-api`
       - Create Project 클릭 → 다음 화면에서 DSN 복사 (https://...@{org}.ingest.sentry.io/{project-id} 형태)
       - 로컬에 `/tmp/grabit-api-dsn.txt` 등으로 임시 저장

    3. **Sentry 프로젝트 `grabit-web` 생성:**
       - 동일 절차, Platform: Next.js, Project name: `grabit-web`
       - DSN 복사 → `/tmp/grabit-web-dsn.txt`

    4. **확인 쿼리:**
       ```bash
       # Sentry API로 프로젝트 존재 확인 (SENTRY_AUTH_TOKEN 필요)
       curl -s -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" \
         "https://sentry.io/api/0/organizations/{ORG}/projects/" | \
         jq -r '.[] | select(.slug | test("grabit-(web|api)")) | .slug'
       # Expected: grabit-api, grabit-web (both)
       ```
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud artifacts repositories describe grabit --location=asia-northeast3 --format='value(name)'` returns a non-empty path containing `/repositories/grabit`
    - Sentry API query returns both `grabit-web` and `grabit-api` project slugs (manual verification OK if `SENTRY_AUTH_TOKEN` unavailable — user confirms via dashboard screenshot)
    - 두 DSN 값이 로컬에 캡처됨 (Task 2 input으로 사용)
  </acceptance_criteria>
  <resume-signal>Type "approved" after both AR repo + both Sentry projects are created and DSNs captured. Reply with any issues to abort.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HUMAN-AUTH — Secret Manager + GitHub secret 값 교체</name>
  <what-built>없음 (human-action). GCP Secret Manager / GitHub secrets 업데이트.</what-built>
  <read_first>
    - Task 1 output: 2개 DSN 값
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § Sentry DSN 교체 경로, § Risks "Sentry DSN 교체 누락"
  </read_first>
  <how-to-verify>
    사용자가 아래 명령을 실행:

    1. **GCP Secret Manager `sentry-dsn` 새 version 추가 (secret 이름 유지 per D-02):**
       ```bash
       echo -n "$(cat /tmp/grabit-api-dsn.txt)" | \
         gcloud secrets versions add sentry-dsn --data-file=-
       ```
       확인:
       ```bash
       gcloud secrets versions access latest --secret=sentry-dsn | head -c 50
       # Expected: 새 grabit-api DSN의 첫 50자 (sentry.io subdomain 포함)
       ```

    2. **GitHub Actions secret `NEXT_PUBLIC_SENTRY_DSN` 갱신 (grabit-web DSN):**
       ```bash
       gh secret set NEXT_PUBLIC_SENTRY_DSN --body "$(cat /tmp/grabit-web-dsn.txt)"
       ```
       확인:
       ```bash
       gh secret list | grep NEXT_PUBLIC_SENTRY_DSN
       # Expected: NEXT_PUBLIC_SENTRY_DSN Updated YYYY-MM-DD
       ```

    3. **로컬 임시 DSN 파일 삭제 (보안):**
       ```bash
       rm /tmp/grabit-api-dsn.txt /tmp/grabit-web-dsn.txt
       ```

    4. **구 DSN 값은 Secret Manager의 이전 version에 그대로 남음 (7일 유예 대비 롤백용). 삭제하지 않는다.**
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud secrets versions list sentry-dsn --limit=2 --format='value(name)' | wc -l` ≥ **2** (기존 + 신규)
    - `gcloud secrets versions access latest --secret=sentry-dsn | wc -c` > **50** (DSN 최소 길이)
    - `gh secret list | grep -q NEXT_PUBLIC_SENTRY_DSN` exit **0** + Updated date ≥ today
    - `/tmp/grabit-*-dsn.txt` 파일 삭제 확인 (보안)
  </acceptance_criteria>
  <resume-signal>Type "approved" after Secret Manager + GitHub secret are updated and temporary DSN files are deleted.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

<task type="auto">
  <name>Task 3: deploy.yml env update (AR_REPO/WEB_SERVICE/API_SERVICE) + provision script 작성</name>
  <files>
    .github/workflows/deploy.yml,
    scripts/provision-grabit-infra.sh
  </files>
  <read_first>
    - .planning/phases/13-grapit-grabit-rename/13-PATTERNS.md § Category 5b (deploy.yml reference edits)
    - .planning/phases/13-grapit-grabit-rename/13-CONTEXT.md § D-05 (grapit-cloudrun@ SA 유지)
    - .planning/phases/13-grapit-grabit-rename/13-RESEARCH.md § P3 새 Cloud Run 서비스 설정
    - `.github/workflows/deploy.yml` (현재 파일 전체)
  </read_first>
  <action>
    STEP 1 — `.github/workflows/deploy.yml` env 변경 (L11-13):
    ```yaml
    env:
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_REGION: asia-northeast3
      AR_REPO: grabit          # was: grapit
      WEB_SERVICE: grabit-web  # was: grapit-web
      API_SERVICE: grabit-api  # was: grapit-api
    ```

    STEP 2 — `pnpm --filter` 갱신 (`L63` Run migrations step):
    - `pnpm --filter @grapit/api exec drizzle-kit migrate` → `pnpm --filter @grabit/api exec drizzle-kit migrate` (이미 Plan 01 Task 1의 `@grapit/` → `@grabit/` bulk rename에서 처리되었을 가능성 있음 — deploy.yml이 Task 1 target list에 포함되었는지 확인하고, 누락되었다면 여기서 수정)

    STEP 3 — D-05 SA 유지 주석 추가 (L81, L168):
    ```yaml
    flags: >-
      --service-account=grapit-cloudrun@${{ env.GCP_PROJECT_ID }}.iam.gserviceaccount.com
      # D-05: service account 이름은 유지 (IAM 바인딩 재생성 리스크 회피, Phase 13 decision)
    ```
    **주의:** YAML multiline `>-` flags block 안에서 주석 처리가 까다로울 수 있다. 대안은 flags 블록 **위에** `# D-05: --service-account 값은 grapit-cloudrun@ 그대로 유지 (Phase 13)` 를 한 줄 추가.

    STEP 4 — `scripts/provision-grabit-infra.sh` 신규 생성:
    ```bash
    #!/usr/bin/env bash
    # scripts/provision-grabit-infra.sh
    # Phase 13 Plan 03 — Grabit blue-green provisioning helper.
    # Idempotent: safe to re-run; creates AR repo only if missing, prints DSN hints.
    set -euo pipefail

    PROJECT_ID="${1:?Usage: $0 <GCP_PROJECT_ID>}"
    REGION="asia-northeast3"
    AR_REPO="grabit"

    echo "=== [1/3] Artifact Registry 'grabit' ==="
    if gcloud artifacts repositories describe "$AR_REPO" --location="$REGION" --project="$PROJECT_ID" >/dev/null 2>&1; then
      echo "  already exists — skip create"
    else
      gcloud artifacts repositories create "$AR_REPO" \
        --repository-format=docker \
        --location="$REGION" \
        --project="$PROJECT_ID" \
        --description="Grabit brand rename target repo (Phase 13)"
    fi

    echo "=== [2/3] Sentry projects (manual step, CLI not provided) ==="
    echo "  Open: https://sentry.io/organizations/{ORG}/projects/new/"
    echo "  Create: grabit-api (Node.js), grabit-web (Next.js)"
    echo "  Copy DSNs, run:"
    echo "    echo -n '<grabit-api-dsn>' | gcloud secrets versions add sentry-dsn --data-file=-"
    echo "    gh secret set NEXT_PUBLIC_SENTRY_DSN --body '<grabit-web-dsn>'"

    echo "=== [3/3] Smoke — new Cloud Run services (run AFTER deploy.yml PR merge) ==="
    for svc in grabit-api grabit-web; do
      if gcloud run services describe "$svc" --region="$REGION" --project="$PROJECT_ID" >/dev/null 2>&1; then
        STATUS=$(gcloud run services describe "$svc" --region="$REGION" --project="$PROJECT_ID" --format='value(status.conditions[0].status)')
        URL=$(gcloud run services describe "$svc" --region="$REGION" --project="$PROJECT_ID" --format='value(status.url)')
        echo "  $svc: status=$STATUS url=$URL"
      else
        echo "  $svc: NOT YET deployed (expected before first deploy.yml run)"
      fi
    done

    echo "=== DONE ==="
    ```
    Make executable: `chmod +x scripts/provision-grabit-infra.sh`

    STEP 5 — Sanity pre-merge:
    - actionlint (if installed): `actionlint .github/workflows/deploy.yml` → exit 0
    - `pnpm -r typecheck && pnpm -r lint` → still green (deploy.yml 변경은 code build에 무영향)

    CONSTRAINTS:
    - DO NOT delete or rename the `grapit-cloudrun@` SA lines (D-05)
    - DO NOT change any secrets mapping (`DATABASE_URL=database-url:latest` 등) — secret NAMES 유지, 값만 Task 2에서 교체
    - DO NOT change `RESEND_FROM_EMAIL` secret (envelope sender 값 교체는 Plan 04 HUMAN-UAT에서 Resend verified sender 설정 후 결정)
    - DO NOT touch Cloud Run 구 서비스 `grapit-web`/`grapit-api` in this plan (Plan 04 cleanup)
  </action>
  <verify>
    <automated>
      set -euo pipefail
      # deploy.yml env 변경 확인
      rg -q '^  AR_REPO: grabit$' .github/workflows/deploy.yml || { echo "FAIL AR_REPO"; exit 1; }
      rg -q '^  WEB_SERVICE: grabit-web$' .github/workflows/deploy.yml || { echo "FAIL WEB_SERVICE"; exit 1; }
      rg -q '^  API_SERVICE: grabit-api$' .github/workflows/deploy.yml || { echo "FAIL API_SERVICE"; exit 1; }
      # D-05: grapit-cloudrun@ 유지
      [ "$(rg -c 'grapit-cloudrun@' .github/workflows/deploy.yml)" -ge 2 ] || { echo "FAIL D-05 SA not retained"; exit 1; }
      rg -q 'D-05' .github/workflows/deploy.yml || { echo "FAIL D-05 annotation"; exit 1; }
      # pnpm --filter @grabit/api 확인 (drizzle migrate step)
      rg -q 'pnpm --filter @grabit/api exec drizzle-kit migrate' .github/workflows/deploy.yml || { echo "FAIL pnpm filter migrate"; exit 1; }
      # 구 grapit- 서비스명 잔여 0 (SA 제외)
      REMAIN=$(rg 'grapit' .github/workflows/deploy.yml | grep -v 'grapit-cloudrun@' | wc -l | tr -d ' ')
      [ "$REMAIN" = "0" ] || { echo "FAIL stale grapit in deploy.yml"; rg 'grapit' .github/workflows/deploy.yml | grep -v 'grapit-cloudrun@'; exit 1; }
      # provision script 존재 + 실행 가능
      test -x scripts/provision-grabit-infra.sh || { echo "FAIL provision script not executable"; exit 1; }
      head -1 scripts/provision-grabit-infra.sh | grep -q '^#!/' || { echo "FAIL provision script shebang"; exit 1; }
      # actionlint (optional)
      command -v actionlint >/dev/null && actionlint .github/workflows/deploy.yml || echo "actionlint not installed — skip"
      # code build 유지
      pnpm -r typecheck
      pnpm -r lint
      # SC-4
      HIST=$(git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/ | wc -l | tr -d ' ')
      [ "$HIST" = "0" ] || { echo "FAIL SC-4"; exit 1; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '^  AR_REPO: grabit$' .github/workflows/deploy.yml` exit **0**
    - `rg -q '^  WEB_SERVICE: grabit-web$' .github/workflows/deploy.yml` exit **0**
    - `rg -q '^  API_SERVICE: grabit-api$' .github/workflows/deploy.yml` exit **0**
    - `rg -c 'grapit-cloudrun@' .github/workflows/deploy.yml` ≥ **2** (D-05 exception)
    - `rg -q 'D-05' .github/workflows/deploy.yml` exit **0** (annotation present)
    - `rg 'grapit' .github/workflows/deploy.yml | grep -v 'grapit-cloudrun@' | wc -l` equals **0** (no stale service-name grapit)
    - `test -x scripts/provision-grabit-infra.sh` exit **0**
    - `head -1 scripts/provision-grabit-infra.sh | grep -q '^#!/'` exit **0**
    - `pnpm -r typecheck` exit **0**, `pnpm -r lint` exit **0**
    - `git diff --name-only main...HEAD -- .planning/milestones/ ...quick/` equals **0 lines** (SC-4)
  </acceptance_criteria>
  <done>
    deploy.yml의 AR_REPO/WEB_SERVICE/API_SERVICE 3줄이 grabit 값으로 변경됨, D-05 SA 유지 주석 추가, provision-grabit-infra.sh 신규 작성 및 실행 가능, 코드 빌드 grade, SC-4 historical 보존.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-VERIFY — deploy.yml PR 머지 → 새 Cloud Run 서비스 기동 + Sentry 이벤트 수신 확인</name>
  <what-built>
    Task 3의 deploy.yml PR을 머지 → main push 트리거 → GitHub Actions Deploy workflow 실행 → 새 Cloud Run 서비스 `grabit-api`, `grabit-web` 최초 생성. 이 Task는 Claude가 자동화할 수 없는 외부 시스템 실행 결과 확인.
  </what-built>
  <read_first>
    - Task 3 커밋이 main에 머지된 상태
    - .planning/phases/13-grapit-grabit-rename/13-VALIDATION.md § Per-Task Verification Map 행 "Cloud Run service 생성 완료" + "새 서비스에서 health 200"
    - scripts/provision-grabit-infra.sh (Task 3에서 생성됨)
  </read_first>
  <how-to-verify>
    1. **main push 후 GitHub Actions Deploy workflow 완료 대기:**
       ```bash
       gh run watch --exit-status
       # deploy-api + deploy-web 둘 다 success
       ```
       실패 시: logs에서 실패 원인 파악 후 Task 3으로 돌아가 수정.

    2. **새 Cloud Run 서비스 describe (provision 스크립트 활용):**
       ```bash
       ./scripts/provision-grabit-infra.sh $(gcloud config get-value project)
       ```
       기대 출력 마지막 섹션에서 두 서비스 모두 `status=True url=https://grabit-{api,web}-{hash}-du.a.run.app`:
       ```
       grabit-api: status=True url=https://grabit-api-...-du.a.run.app
       grabit-web: status=True url=https://grabit-web-...-du.a.run.app
       ```

    3. **개별 서비스 health check:**
       ```bash
       API_URL=$(gcloud run services describe grabit-api --region=asia-northeast3 --format='value(status.url)')
       WEB_URL=$(gcloud run services describe grabit-web --region=asia-northeast3 --format='value(status.url)')
       curl -sI "$API_URL/api/v1/health" | head -1
       # Expected: HTTP/2 200 OR HTTP/2 503 (Valkey fallback — 본 plan은 Valkey 신규 prov 안 함, 503 OK)
       curl -sI "$WEB_URL/" | head -1
       # Expected: HTTP/2 200
       ```

    4. **GitHub variables 갱신:**
       ```bash
       gh variable set CLOUD_RUN_API_URL --body "$API_URL"
       gh variable set CLOUD_RUN_WEB_URL --body "$WEB_URL"
       gh variable list | grep CLOUD_RUN
       # Both should show today's date
       ```
       그 후 workflow_dispatch로 deploy.yml 재실행 → 이번에는 FRONTEND_URL / OAuth callback URLs가 grabit URL로 올바르게 주입됨.

    5. **Sentry 새 프로젝트 이벤트 수신 확인 (의도적 에러 트리거):**
       ```bash
       # grabit-api 에 의도적으로 404 유도
       curl -sI "$API_URL/api/v1/_sentry-test-$(date +%s)"
       # 또는 admin endpoint 권한 없이 접근
       ```
       Sentry dashboard > grabit-api 프로젝트 > Issues 에서 최근 5분 이벤트 표시 확인. **404는 기본으로 Sentry 캡처 안 될 수 있음** — 확실한 방법은 auth token 없이 protected endpoint 호출하여 500 발생시키거나, Cloud Run 로그에서 임의 에러 확인:
       ```bash
       gcloud run services logs read grabit-api --region=asia-northeast3 --limit=20
       ```

       grabit-web 은 Sentry.captureException 이 자동 초기화되므로 존재 자체가 이벤트 트리거 조건 (prod 배포 시 1회 init 이벤트). Sentry UI에서 `grabit-web` project > Issues > "[release] initialized" 또는 "first transaction" 확인.

    6. **사용자 영향 0 확인 (도메인 매핑은 아직 구 서비스):**
       ```bash
       curl -sI https://heygrabit.com/ | head -1
       # Expected: HTTP/2 200 (구 grapit-web 으로 라우팅 — Plan 04 cutover 전까지 정상)
       curl -sI https://heygrabit.com/api/v1/health | head -1
       # Expected: HTTP/2 200 (구 grapit-api)
       ```

    7. **롤백 조건:** 새 서비스가 status=False 거나 이미지 build/push 실패 → deploy.yml PR revert + GitHub secret/variables 되돌리기. Secret Manager `sentry-dsn` 은 이전 version으로 롤백: `gcloud secrets versions access $((LATEST - 1)) --secret=sentry-dsn`.
  </how-to-verify>
  <acceptance_criteria>
    - `gcloud run services describe grabit-api --region=asia-northeast3 --format='value(status.conditions[0].status)'` equals **True**
    - `gcloud run services describe grabit-web --region=asia-northeast3 --format='value(status.conditions[0].status)'` equals **True**
    - `curl -sI {grabit_web_url}/ | head -1 | grep -q '200'` exit **0**
    - `curl -sI {grabit_api_url}/api/v1/health | head -1 | grep -qE '200|503'` exit **0** (503 허용 — Valkey VPC 등 연결되지 않은 신규 서비스 초기 상태)
    - `gh variable list | grep -E 'CLOUD_RUN_(API|WEB)_URL'` 에서 두 변수 모두 **today** date 업데이트 확인
    - 새 `grabit-api` / `grabit-web` Sentry 프로젝트 대시보드에서 최근 1시간 이내 이벤트 최소 1건 수신 확인 (UI screenshot or API query)
    - `curl -sI https://heygrabit.com/ | head -1 | grep -q '200'` exit **0** (구 서비스 그대로 동작, 사용자 영향 0)
    - `gcloud artifacts docker images list asia-northeast3-docker.pkg.dev/{PROJECT_ID}/grabit --limit=2 --format='value(package)' | wc -l` ≥ **2** (grabit-api + grabit-web image pushed)
  </acceptance_criteria>
  <resume-signal>Type "approved" after all acceptance criteria met. Reply with deployment log excerpts if any step failed.</resume-signal>
  <files>N/A (human-action — no repo file edits; work happens in external consoles/CLI tools as described in <how-to-verify>)</files>
  <action>
    This is a checkpoint:human-action task. Claude does NOT execute this task.
    Execution happens entirely in the user's shell / external consoles per the <how-to-verify> block above.
    Wait for the user's `approved` signal on <resume-signal> before proceeding to the next task.
    Concrete steps are listed verbatim in <how-to-verify>; the user follows them and reports back.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verified by user via <how-to-verify> + <acceptance_criteria>; no automated command run by agent"</automated>
  </verify>
  <done>User signals `approved` after the <acceptance_criteria> conditions above are all true. Approval is recorded in the summary; no file changes are made by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GCP IAM | Workload Identity Federation; deploy.yml 의 service account 참조가 변경되면 토큰 교환 실패 가능 |
| GCP Secret Manager → Cloud Run runtime | `sentry-dsn:latest` 참조. 버전 교체 시 Cloud Run 재배포로 반영 |
| Sentry org → 새 프로젝트 | 새 DSN이 올바른 project에 발급되어야 observability 연속성 확보 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Spoofing | 잘못된 Sentry DSN으로 이벤트 orphan | mitigate | Task 4 Step 5 — 새 프로젝트에 실제 이벤트 수신 확인 후 approved |
| T-13-12 | Tampering | deploy.yml 수정 중 SA 실수 rename | mitigate | Task 3 acceptance: `rg -c 'grapit-cloudrun@' deploy.yml >= 2` + annotation present |
| T-13-13 | Denial of service | 새 Cloud Run 서비스 기동 실패 → deploy workflow fail | mitigate | Task 4 Step 1 `gh run watch --exit-status` 실패 시 롤백 경로 제공 |
| T-13-14 | Information disclosure | 임시 `/tmp/grabit-*-dsn.txt` 파일 잔존 | mitigate | Task 2 Step 3에서 명시적 `rm` 지시 + acceptance 체크 |
| T-13-15 | Denial of service (observability) | 구 Sentry 프로젝트 DSN 참조가 여전히 active → 이벤트 유실 | mitigate | Secret Manager `sentry-dsn:latest` 가 grabit-api DSN을 가리킴 + Cloud Run 재배포로 반영 |
| T-13-16 | Elevation of privilege | SA rename 시 IAM 바인딩 재설정 필요 | accept (D-05) | rename 하지 않음 |
| T-13-17 | Repudiation | 구 리소스(Cloud Run grapit-*, Sentry grapit-*) 이벤트 히스토리 유실 | accept (D-02b) | 7일 유예 후 Plan 04에서 삭제. 히스토리는 구 프로젝트에 최종 snapshot으로 남음 |
</threat_model>

<verification>
End-of-plan comprehensive verification:

```bash
# 1. deploy.yml 변경 확인
rg 'AR_REPO|WEB_SERVICE|API_SERVICE' .github/workflows/deploy.yml
# Expected:
#   AR_REPO: grabit
#   WEB_SERVICE: grabit-web
#   API_SERVICE: grabit-api

# 2. D-05 SA 유지
rg -c 'grapit-cloudrun@' .github/workflows/deploy.yml
# Expected: >= 2

# 3. 새 Cloud Run 서비스 Ready
for svc in grabit-api grabit-web; do
  gcloud run services describe "$svc" --region=asia-northeast3 \
    --format='value(metadata.name,status.conditions[0].status,status.url)'
done

# 4. Artifact Registry 새 repo + image 2종 push 확인
gcloud artifacts docker images list asia-northeast3-docker.pkg.dev/$(gcloud config get-value project)/grabit --limit=10

# 5. Secret Manager sentry-dsn latest version (grabit-api DSN)
gcloud secrets versions list sentry-dsn --limit=3

# 6. GitHub secrets + variables
gh secret list | grep NEXT_PUBLIC_SENTRY_DSN
gh variable list | grep -E 'CLOUD_RUN_(API|WEB)_URL'

# 7. 사용자 영향 0 (구 서비스가 도메인 매핑 담당)
curl -sI https://heygrabit.com/ | head -1
# Expected: HTTP/2 200

# 8. SC-4
git diff --name-only main...HEAD -- .planning/milestones/ '.planning/phases/0[1-9]-*' '.planning/phases/09.1-*' '.planning/phases/10.1-*' '.planning/phases/11-*' '.planning/phases/12-*' .planning/quick/
# Expected: empty

# 9. Plan 01 audit 스크립트 재실행 (neg regression)
./scripts/audit-brand-rename.sh
```
</verification>

<success_criteria>
- [x] **SC-3 (half):** 새 Cloud Run 서비스 `grabit-api`, `grabit-web` 기동 + health check 200 (또는 API 503 Valkey 연결 전 상태 허용)
- [x] 새 Artifact Registry 저장소 `grabit` 존재 + 이미지 2종 push 완료
- [x] 새 Sentry 프로젝트 `grabit-api`, `grabit-web` 존재 + 이벤트 수신 확인
- [x] Secret Manager `sentry-dsn` 새 version (grabit-api DSN), GitHub secret `NEXT_PUBLIC_SENTRY_DSN` 갱신
- [x] GitHub variables `CLOUD_RUN_API_URL`, `CLOUD_RUN_WEB_URL` 이 grabit URL로 갱신
- [x] D-05: `grapit-cloudrun@` SA 유지 (deploy.yml 2곳)
- [x] 사용자 영향 0: heygrabit.com 도메인은 여전히 구 grapit-web 에 매핑된 상태
- [x] **SC-4:** 완료된 phase/milestone/quick 폴더 diff 0건
- [x] `scripts/provision-grabit-infra.sh` 신규 생성 (idempotent, 재실행 가능)
</success_criteria>

<output>
After completion, create `.planning/phases/13-grapit-grabit-rename/13-03-SUMMARY.md` documenting:
- deploy.yml diff summary (3 env 변경 + D-05 annotation)
- Provision script 경로 + idempotent 재실행 결과
- 새 Cloud Run 서비스 URL 2개 + health check 결과
- 새 Sentry 프로젝트 ID + 이벤트 수신 스크린샷/API 증거
- Secret Manager sentry-dsn version history
- Handoff note to Plan 04 (P4 — 도메인 cutover): 새 Cloud Run URL 정보 + Plan 04 pre-cutover 체크리스트 항목 준비
- **Resend/Infobip sender 재설정 가능성** 노트 (Plan 04 HUMAN-UAT에서 실제 발송 테스트 시 이슈 발견 가능)
</output>
