---
phase: 10.1
plan: 03
type: execute
wave: 2
depends_on: [10.1-02]
files_modified:
  - apps/api/src/modules/sms/infobip-client.ts
autonomous: true
requirements: [SMS-02]
tags: [infobip, sms-v3, green, tdd]
must_haves:
  truths:
    - "InfobipClient.sendSms(e164, text)가 POST {baseUrl}/sms/3/messages 호출"
    - "Authorization: App {apiKey} 헤더 + Content-Type: application/json"
    - "Request body는 { messages: [{ from: sender, destinations: [{ to: e164 without + }], text }] }"
    - "200 응답 → SendSmsResult { messageId, status (status.name), groupId } 반환"
    - "4xx/5xx 응답 시 InfobipApiError(status, body) throw"
    - "messages 배열 없거나 비어있으면 InfobipApiError(500, 'messages 누락') throw"
    - "AbortSignal.timeout(5000) 적용"
    - "sendPin, verifyPin 메서드가 파일에서 완전 제거됨"
    - "InfobipSendPinResponse, InfobipVerifyPinResponse 인터페이스가 파일에서 완전 제거됨"
    - "infobip-client.spec.ts 전부 GREEN"
  artifacts:
    - path: "apps/api/src/modules/sms/infobip-client.ts"
      provides: "v3 /sms/3/messages 호출 전용 fetch wrapper"
      exports: ["InfobipClient", "InfobipApiError", "SendSmsResult"]
      min_lines: 40
  key_links:
    - from: "infobip-client.ts"
      to: "Infobip SMS API v3"
      via: "global fetch + AbortSignal.timeout(5000)"
      pattern: "/sms/3/messages"
---

<objective>
Phase 10의 `InfobipClient` (sendPin/verifyPin) 구현을 전면 재작성하여 `/sms/3/messages` 단일 endpoint만 호출하는 최소 API로 단순화한다. Plan 02에서 작성한 `infobip-client.spec.ts` RED 테스트를 GREEN으로 전환한다.

주요 변경:
1. 생성자 시그니처: `(baseUrl, apiKey, applicationId, messageId)` → `(baseUrl, apiKey, sender)`
2. 메서드 교체: `sendPin(e164)` + `verifyPin(pinId, pin)` → `sendSms(e164, text)` 단일
3. 타입 교체: `InfobipSendPinResponse`, `InfobipVerifyPinResponse` → `SendSmsResult`
4. 에러 클래스 `InfobipApiError`는 동일 shape 유지 (status, body)

Purpose:
- v3 API는 Application/Message Template 사전 생성 없이도 동작. PIN 생성·저장·검증은 `SmsService`가 자체 구현(Plan 04).
- `@infobip-api/sdk` 미사용 유지 — Phase 10과 동일하게 axios 트랜지티브 의존 0 원칙 준수.
- `encodeURIComponent(pinId)` 같은 v3 불필요 로직 제거로 코드 단순화.

Output: `apps/api/src/modules/sms/infobip-client.ts` (~50 LOC) + spec GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md
@apps/api/src/modules/sms/infobip-client.ts
@apps/api/src/modules/sms/infobip-client.spec.ts
@apps/api/src/modules/sms/__fixtures__/infobip-send-response.json
</context>

<interfaces>
<!-- Plan 02에서 spec이 기대하는 계약 (GREEN 전환 대상): -->

export interface SendSmsResult {
  messageId: string;
  status: string;    // e.g. "MESSAGE_ACCEPTED"
  groupId: number;   // 1=PENDING (accepted), 2=UNDELIVERABLE, 3=DELIVERED, 4=EXPIRED, 5=REJECTED
}

export class InfobipApiError extends Error {
  readonly status: number;
  readonly body: string;
  constructor(status: number, body: string);
}

export class InfobipClient {
  constructor(
    private readonly baseUrl: string,
    private readonly apiKey: string,
    private readonly sender: string,
  ) {}

  async sendSms(e164: string, text: string): Promise<SendSmsResult>;
}

<!-- 하위 호환: sendPin/verifyPin, InfobipSendPinResponse, InfobipVerifyPinResponse는 제거된다. -->
<!-- SmsService(Plan 04)는 새 sendSms만 호출한다. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>10.1-03-T1</id>
  <name>Task 1: infobip-client.ts 전면 재작성 — sendSms 단일 메서드 + v3 body shape</name>
  <files>apps/api/src/modules/sms/infobip-client.ts</files>
  <behavior>
    Plan 02의 infobip-client.spec.ts 전 테스트 GREEN 전환:
    1. new InfobipClient('https://x.api.infobip.com', 'key', '0212345678') 생성 가능
    2. sendSms('+821012345678', 'hello') → POST https://x.api.infobip.com/sms/3/messages
    3. Authorization: "App key", Content-Type: "application/json"
    4. Body = { messages: [{ from: '0212345678', destinations: [{ to: '821012345678' }], text: 'hello' }] }
    5. 200 응답(fixture) → { messageId: fixture.messages[0].messageId, status: 'MESSAGE_ACCEPTED', groupId: 1 }
    6. 400/500 응답 → InfobipApiError(status, body) throw
    7. messages 배열 누락/비어있음 → InfobipApiError(500, ...) throw
    8. AbortSignal.timeout(5000) 설정
  </behavior>
  <read_first>
    - apps/api/src/modules/sms/infobip-client.spec.ts (Plan 02 결과 — RED 테스트 계약)
    - apps/api/src/modules/sms/__fixtures__/infobip-send-response.json (응답 shape)
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §2.1 "InfobipClient 최소 API"
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §4.1, §4.2 "Pitfalls"
  </read_first>
  <action>
    `apps/api/src/modules/sms/infobip-client.ts` 내용을 다음으로 **완전 교체** (기존 sendPin/verifyPin 전부 제거):

    ```typescript
    export interface SendSmsResult {
      messageId: string;
      status: string;
      groupId: number;
    }

    export class InfobipApiError extends Error {
      constructor(
        public readonly status: number,
        public readonly body: string,
      ) {
        super(`Infobip API ${status}: ${body}`);
        this.name = 'InfobipApiError';
      }
    }

    /**
     * Infobip SMS API v3 client.
     *
     * Uses POST /sms/3/messages (2024-12 release). No applicationId/messageId required.
     * PIN generation and verification are implemented locally in SmsService against Valkey.
     *
     * Sender ID semantics:
     * - Free Trial: provide any string; Infobip replaces with shared sender.
     * - Production (KR): must be KISA-registered numeric sender. Alphanumeric senders are
     *   silently rewritten by KR MNOs.
     */
    export class InfobipClient {
      constructor(
        private readonly baseUrl: string,
        private readonly apiKey: string,
        private readonly sender: string,
      ) {}

      async sendSms(e164: string, text: string): Promise<SendSmsResult> {
        const to = e164.replace(/^\+/, '');

        const res = await fetch(`${this.baseUrl}/sms/3/messages`, {
          method: 'POST',
          headers: {
            Authorization: `App ${this.apiKey}`,
            'Content-Type': 'application/json',
            Accept: 'application/json',
          },
          body: JSON.stringify({
            messages: [
              {
                from: this.sender,
                destinations: [{ to }],
                text,
              },
            ],
          }),
          signal: AbortSignal.timeout(5000),
        });

        if (!res.ok) {
          const body = await res.text().catch(() => '');
          throw new InfobipApiError(res.status, body);
        }

        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const data = (await res.json()) as { messages?: Array<{ messageId?: string; status?: { name?: string; groupId?: number } }>; bulkId?: string };
        const msg = data.messages?.[0];
        if (!msg || !msg.messageId) {
          throw new InfobipApiError(
            500,
            'Infobip response missing messages[0].messageId',
          );
        }

        return {
          messageId: msg.messageId,
          status: msg.status?.name ?? 'UNKNOWN',
          groupId: msg.status?.groupId ?? 0,
        };
      }
    }
    ```

    **제거 대상 (완전 삭제):**
    - `InfobipSendPinResponse` interface
    - `InfobipVerifyPinResponse` interface
    - `InfobipClient.sendPin` method
    - `InfobipClient.verifyPin` method
    - 생성자 `applicationId`, `messageId` 인자
    - `/2fa/2/pin` 경로 전부
    - `encodeURIComponent(pinId)` 로직

    **구현 주의사항 (RESEARCH §4 Pitfalls):**
    - [§4.2] `toE164.replace(/^\+/, '')` — Infobip은 `+` 없는 E.164 기대
    - [§4.5] `AbortSignal.timeout(5000)` — Node 22.22.x LTS 지원 확인됨
    - [§4.6] `global.fetch` 사용. `undici` 직접 import 금지
    - `any` 금지 — 응답 타입을 좁은 structural type으로 명시
    - `res.text().catch(() => '')` — body 파싱 실패 시 빈 문자열 fallback
    - `messages[0]` missing → 500 fallback (Plan 02 spec에서 커버)

    **작성 후 검증:**
    ```bash
    pnpm --filter @grapit/api test infobip-client -- --run
    ```
    전 테스트 케이스 GREEN이어야 함.

    **TypeScript/lint 주의:**
    - `@typescript-eslint/no-explicit-any` 규칙 통과. 응답 파싱 타입은 narrow structural type 사용 (위 코드 참조).
    - `pnpm --filter @grapit/api typecheck` 통과
    - `pnpm --filter @grapit/api lint apps/api/src/modules/sms/infobip-client.ts` 통과

    **다른 consumer 영향:**
    이 시점에서 `sms.service.ts`는 여전히 `sendPin/verifyPin`을 호출하므로 typecheck가 fail할 수 있다. 다음 조치로 단일 plan 내에서 해결:
    - Plan 03은 `infobip-client.ts` + `infobip-client.spec.ts` GREEN만 성공 판정
    - `sms.service.ts` typecheck 실패는 Plan 04에서 해결되는 것으로 간주
    - **대안 — Plan 03 실행 시 단기 호환 스텁 제공:**
      `sms.service.ts`가 import하는 `InfobipSendPinResponse`, `InfobipVerifyPinResponse` type을 **type alias로 deprecated re-export** 추가하고, `sendPin`/`verifyPin` method를 임시로 throw stub 추가. 이렇게 하면 이 plan 범위에서 typecheck가 GREEN이고, Plan 04에서 SmsService를 재작성하면서 stub을 제거.

      **단, 실제로 이 plan에서는 stub을 추가하지 않고 typecheck 검증은 Plan 04 종료 시점으로 미룬다** (wave 2는 단일 파일 단위, wave 3에서 통합 검증). acceptance_criteria에서 typecheck 체크 제외.

    **Done criterion:**
    - `apps/api/src/modules/sms/infobip-client.ts` 파일이 위 구조로 존재
    - `pnpm --filter @grapit/api test infobip-client -- --run` GREEN
  </action>
  <acceptance_criteria>
    - `grep -q "/sms/3/messages" apps/api/src/modules/sms/infobip-client.ts`
    - `grep -q "async sendSms" apps/api/src/modules/sms/infobip-client.ts`
    - `grep -q "export interface SendSmsResult" apps/api/src/modules/sms/infobip-client.ts`
    - `grep -q "export class InfobipClient" apps/api/src/modules/sms/infobip-client.ts`
    - `grep -q "export class InfobipApiError" apps/api/src/modules/sms/infobip-client.ts`
    - `grep -q "AbortSignal.timeout(5000)" apps/api/src/modules/sms/infobip-client.ts`
    - `! grep -q "sendPin\|verifyPin" apps/api/src/modules/sms/infobip-client.ts`
    - `! grep -q "/2fa/2/pin" apps/api/src/modules/sms/infobip-client.ts`
    - `! grep -q "applicationId\|messageId\|InfobipSendPinResponse\|InfobipVerifyPinResponse" apps/api/src/modules/sms/infobip-client.ts`
    - `! grep -q "encodeURIComponent" apps/api/src/modules/sms/infobip-client.ts`
    - `! grep -q "import axios\|@infobip-api/sdk" apps/api/src/modules/sms/infobip-client.ts`
    - `pnpm --filter @grapit/api test infobip-client -- --run` exits 0 (전 spec GREEN)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/api test infobip-client -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>infobip-client.ts 완전 재작성, sendSms만 존재, sendPin/verifyPin/applicationId/messageId 0건, spec 전 케이스 GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run → Infobip API | HTTPS 외부 호출, API key 서버 측 (메모리 내) 사용 |
| InfobipClient → fetch global | Node 22 fetch + AbortSignal.timeout, undici 직접 import 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-03-01 | Information Disclosure | Authorization 헤더 credential | mitigate | apiKey는 constructor에만 전달. 로깅/Sentry tag에 key 미포함. 에러 throw 시 body만 포함(header 미포함). |
| T-10.1-03-02 | Denial of Service | 느린 Infobip 응답으로 Cloud Run 요청 블로킹 | mitigate | AbortSignal.timeout(5000). 상위(SmsService)에서 5xx/timeout → cooldown rollback. |
| T-10.1-03-03 | Tampering | messages[0] 응답 누락 시 silent false green | mitigate | `!msg.messageId` 체크로 InfobipApiError(500) throw. Plan 02 spec에 테스트 포함. |
| T-10.1-03-04 | Tampering | sender ID injection (text 필드에 본문 조작 삽입) | accept | sender는 서버 측 env에서만 주입. text는 SmsService가 서버에서 구성한 본문(`[Grapit] 인증번호 …`)으로만 들어옴. 사용자 입력이 text에 직접 흘러가지 않음 (SmsService가 템플릿 리터럴로 합성). |
</threat_model>

<verification>
- `pnpm --filter @grapit/api test infobip-client -- --run` green
- `grep -r "sendPin\|verifyPin\|applicationId\|messageId" apps/api/src/modules/sms/infobip-client.ts` → 0건
- 파일 LOC < 80 (단순화 목표 달성 지표)
</verification>

<success_criteria>
- `/sms/3/messages` 호출 + SendSmsResult 반환 타입 계약 확립
- Plan 02 spec 전 케이스 GREEN
- Phase 10의 sendPin/verifyPin 인터페이스 흔적 0건
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-sms-api-v3-rewrite/10.1-03-SUMMARY.md`
</output>
