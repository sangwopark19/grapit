---
phase: "10.1"
plan: "02"
subsystem: sms
tags: [tdd, red, test-scaffolding, sms-v3, fixtures]
dependency_graph:
  requires: []
  provides: [infobip-client-spec-v3, sms-service-spec-v3, fixtures-v3]
  affects: [infobip-client.ts, sms.service.ts]
tech_stack:
  added: []
  patterns: [vitest-mock-global-fetch, crypto-randomInt-spy, valkey-lua-verify-and-increment]
key_files:
  created: []
  modified:
    - apps/api/src/modules/sms/__fixtures__/infobip-send-response.json
    - apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json
    - apps/api/src/modules/sms/infobip-client.spec.ts
    - apps/api/src/modules/sms/sms.service.spec.ts
decisions:
  - "infobip-verify-response.json을 삭제하지 않고 _deprecated 마커로 대체 (import 깨짐 방지)"
  - "INFOBIP_APPLICATION_ID/MESSAGE_ID 비참조를 negative assertion으로 증명"
  - "Lua VERIFY_AND_INCREMENT 결과 분기 4종을 별도 it 블록으로 분리"
metrics:
  duration_seconds: 365
  completed: "2026-04-17T00:50:02Z"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 4
---

# Phase 10.1 Plan 02: RED Test Scaffolding Summary

Infobip /sms/3/messages v3 전환을 위한 RED 테스트 4파일 재작성 -- fixture 2종 + spec 2종을 v3 shape으로 교체하고 2FA 레거시 어서션을 전부 제거

## What Was Done

### Task 1: Fixture 2종 재작성 (1adb203)
- `infobip-send-response.json`: pinId/to/ncStatus/smsStatus 구조에서 messages[].messageId/status(groupId/name)/destination + bulkId 구조로 교체
- `infobip-verify-response.json`: 4종 verify 응답(success/wrongPin/expired/noMoreAttempts)을 `_deprecated` 마커 단일 객체로 대체. Valkey Lua 자체 verify 전환으로 Infobip verify endpoint 미사용

### Task 2: infobip-client.spec.ts 전면 재작성 (784eebc)
- `describe('sendPin')` 10개 + `describe('verifyPin')` 9개 케이스를 `describe('sendSms')` 10개로 축소
- URL 검증: `/2fa/2/pin` -> `/sms/3/messages`
- Body 구조: `{ applicationId, messageId, from, to }` -> `{ messages: [{ from, destinations: [{ to }], text }] }`
- 응답 파싱: pinId/smsStatus -> messageId/status(name)/groupId
- Constructor: 4-arg (baseUrl, apiKey, appId, msgId) -> 3-arg (baseUrl, apiKey, sender)
- InfobipApiError describe 블록 2개 유지
- APP_ID, MSG_ID, applicationId, messageId, verifyPin 완전 제거

### Task 3: sms.service.spec.ts 전면 재작성 (0ad52e6)
- **constructor**: env 4종 -> 3종 (APPLICATION_ID/MESSAGE_ID 제거, SENDER 추가). Legacy env 비참조 증명 테스트 추가
- **sendVerificationCode**: sendPin mock -> sendSms mock. OTP 생성은 crypto.randomInt spy로 결정성 확보. Valkey 저장 키 sms:pin -> sms:otp, TTL 180000ms. 메시지 본문 `[Grapit] 인증번호 654321 (3분 이내 입력)` 포맷 검증
- **verifyCode**: Infobip verifyPin 호출 -> Valkey Lua VERIFY_AND_INCREMENT 결과 분기 4종 (VERIFIED/WRONG/EXPIRED/NO_MORE_ATTEMPTS)
- Rate limiting 테스트 유지: phone axis send 5/3600s, phone axis verify 10/900s, 30s resend cooldown
- dev mock 테스트 유지: 000000 성공, 잘못된 코드 실패

## Decisions Made

1. **infobip-verify-response.json 보존**: `git rm` 대신 `_deprecated` 마커로 대체. 전환 기간 동안 import 에러 방지하면서 git history 보존
2. **Legacy env negative assertion**: `INFOBIP_APPLICATION_ID`/`INFOBIP_MESSAGE_ID`가 ConfigService.get 호출 인자에 포함되지 않음을 `expect(requestedKeys).not.toContain()` 패턴으로 증명
3. **VERIFY_AND_INCREMENT eval 인자 검증**: Lua script 내용 대신 eval 호출 시 전달되는 키 패턴(sms:otp:/sms:attempts:/sms:verified:)으로 계약 검증

## Deviations from Plan

None - plan executed exactly as written.

## RED State Confirmation

모든 spec 파일은 현재 RED 상태:
- `infobip-client.spec.ts`: SendSmsResult/sendSms가 infobip-client.ts에 미존재 (Plan 03에서 GREEN 전환)
- `sms.service.spec.ts`: OTP 생성/Valkey 저장/Lua verify 로직이 sms.service.ts에 미존재 (Plan 04에서 GREEN 전환)

이 RED 상태는 의도적이며, Plan 03/04 실행 시 구현 코드가 이 테스트들의 계약을 충족하여 GREEN으로 전환된다.

## Task Summary

| Task | Name | Commit | Files |
|------|------|--------|-------|
| T1 | Fixture 2종 재작성 | 1adb203 | __fixtures__/infobip-send-response.json, __fixtures__/infobip-verify-response.json |
| T2 | infobip-client.spec.ts 재작성 | 784eebc | infobip-client.spec.ts |
| T3 | sms.service.spec.ts 재작성 | 0ad52e6 | sms.service.spec.ts |

## Self-Check: PASSED

- All 4 modified files exist on disk
- All 3 task commits verified in git log
- SUMMARY.md created at correct path
