---
phase: 10.1
plan: 05
type: execute
wave: 4
depends_on: [10.1-04]
files_modified:
  - apps/api/src/modules/sms/sms.controller.spec.ts
  - apps/api/test/sms-throttle.integration.spec.ts
autonomous: true
requirements: [SMS-01, SMS-02, SMS-03, SMS-04]
tags: [sms, integration, testcontainers, valkey, regression]
must_haves:
  truths:
    - "sms.controller.spec.ts가 env 3종(INFOBIP_API_KEY/BASE_URL/SENDER)으로 마이그레이션되고 APPLICATION_ID/MESSAGE_ID 참조 0건"
    - "sms.controller.spec.ts @Throttle IP axis 테스트(sendCode 20/3600000ms, verifyCode 10/900000ms) 전부 GREEN"
    - "sms.controller.spec.ts sendCodeSchema 국제 번호 zod 테스트(한국/E.164) 전부 GREEN"
    - "sms.controller.spec.ts HTTP 429 HttpException 어서션 3건(sendVerificationCode cooldown, send phone axis, verify phone axis) 전부 GREEN"
    - "sms-throttle.integration.spec.ts가 testcontainers Valkey에서 end-to-end 검증: send-code IP/phone axis + verify-code IP/phone axis + OTP 자체 verify 원자성"
    - "sms-throttle.integration.spec.ts에서 Infobip 외부 호출은 mock으로 차단 (real Infobip 미호출)"
    - "sms-throttle.integration.spec.ts에서 `VERIFY_AND_INCREMENT_LUA`가 실제 Valkey Lua interpreter에서 4분기 결과(VERIFIED/WRONG/EXPIRED/NO_MORE_ATTEMPTS)를 올바르게 반환하는 smoke 케이스 포함"
    - "pnpm --filter @grapit/api test 전체 실행 시 0 실패"
  artifacts:
    - path: "apps/api/src/modules/sms/sms.controller.spec.ts"
      provides: "@Throttle + zod 국제 번호 + 429 HttpException 단위 테스트 (env 3종 마이그레이션)"
    - path: "apps/api/test/sms-throttle.integration.spec.ts"
      provides: "testcontainers Valkey + NestJS TestingModule 통합 검증, VERIFY_AND_INCREMENT Lua 실행 smoke 포함"
  key_links:
    - from: "sms.controller.spec.ts"
      to: "sms.service.ts (Plan 04 결과)"
      via: "dynamic import + mock ConfigService"
      pattern: "INFOBIP_SENDER"
    - from: "sms-throttle.integration.spec.ts"
      to: "Valkey testcontainer"
      via: "pnpm test:integration (격리 vitest config)"
      pattern: "VERIFY_AND_INCREMENT|sms:otp:"
---

<objective>
Plan 04까지 완료된 SmsService 재작성이 Phase 10의 rate limiting / controller 계층 / integration 시나리오에서 회귀 없이 동작하는지 확인한다. 두 개의 테스트 파일을 수정:

1. **`sms.controller.spec.ts`**: 기존 env 4종 mock(APPLICATION_ID/MESSAGE_ID 포함)을 3종으로 축소하면서 HTTP 429 HttpException 검증을 유지
2. **`sms-throttle.integration.spec.ts`**: testcontainers Valkey에서 실 Lua script 동작 확인 — 특히 신규 `VERIFY_AND_INCREMENT_LUA`가 EVAL 기반 Lua 5.1 인터프리터에서 기대대로 4분기 결과를 반환하는지

Purpose:
- Phase 10 review에서 testcontainers 통합 테스트를 HIGH 사항으로 확보한 이유 승계 — Lua 스크립트가 실제 Valkey에서 돌아가는지 unit mock로는 검증 불가
- controller 레벨 @Throttle metadata 어서션, zod 국제 번호 검증은 Phase 10 그대로 유효하지만 mock env가 4종 기준이라 마이그레이션 필요
- `pnpm --filter @grapit/api test` 전체 0 실패를 Phase 10.1의 최종 자동화 관문으로 고정

Output: 2 파일 수정, 전체 api 테스트 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md
@apps/api/src/modules/sms/sms.controller.ts
@apps/api/src/modules/sms/sms.controller.spec.ts
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/infobip-client.ts
@apps/api/src/modules/sms/sms.module.ts
@apps/api/src/modules/booking/providers/redis.provider.ts
@apps/api/src/app.module.ts
</context>

<interfaces>
<!-- Plan 04에서 완성된 SmsService 계약이 controller/integration 테스트 대상. -->
<!-- Valkey 키 스키마 (integration test에서 EVAL 직접 호출): -->

// sms:otp:{e164}       - OTP string, TTL 180_000ms
// sms:attempts:{e164}  - verify attempts counter, TTL 900s
// sms:verified:{e164}  - verified flag '1', TTL 600s
// sms:phone:send:{e164}    - phone axis send counter (ATOMIC_INCR_LUA)
// sms:phone:verify:{e164}  - phone axis verify counter (ATOMIC_INCR_LUA)

<!-- VERIFY_AND_INCREMENT_LUA 기대 결과 분기 (integration에서 실 Valkey EVAL 실행): -->
// otp='123456' pre-set → eval with code='123456' → ['VERIFIED', 1]
// otp='123456' pre-set → eval with code='999999' → ['WRONG', 4]
// otp 미저장 → eval → ['EXPIRED', 0]
// attempts 5회 초과 → ['NO_MORE_ATTEMPTS', 0]
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <id>10.1-05-T1</id>
  <name>Task 1: sms.controller.spec.ts — env 3종 마이그레이션 + 회귀 검증</name>
  <files>apps/api/src/modules/sms/sms.controller.spec.ts</files>
  <read_first>
    - apps/api/src/modules/sms/sms.controller.spec.ts (현재 4종 env mock)
    - apps/api/src/modules/sms/sms.controller.ts (@Throttle + zod 현재 구조)
    - apps/api/src/modules/sms/sms.service.ts (Plan 04 결과 — constructor 3종 env)
  </read_first>
  <action>
    `apps/api/src/modules/sms/sms.controller.spec.ts` 편집. 아래 세 구역을 교체:

    **A. `mockConfigService.get` implementation 3구데 마이그레이션:**

    현재 상태(3곳에 중복 존재):
    ```typescript
    const env: Record<string, string> = {
      INFOBIP_API_KEY: 'test-key',
      INFOBIP_BASE_URL: 'https://test.api.infobip.com',
      INFOBIP_APPLICATION_ID: 'test-app-id',
      INFOBIP_MESSAGE_ID: 'test-msg-id',
    };
    ```

    교체 후:
    ```typescript
    const env: Record<string, string> = {
      INFOBIP_API_KEY: 'test-key',
      INFOBIP_BASE_URL: 'https://test.api.infobip.com',
      INFOBIP_SENDER: '0212345678',
    };
    ```

    3곳 모두 일괄 변경 (`sendVerificationCode phone axis`, `verifyCode phone axis`, `sendVerificationCode resend cooldown` 각각의 케이스).

    **B. 주석/설명 업데이트:**
    파일 상단 JSDoc의 "3. sms.service.ts 429 HttpException tests"는 유지. 이 블록의 각 `it` 설명도 그대로 유지.

    Plan 06 참조 주석("Plan 10-06 changes:")은 유지하되 파일 상단에 한 줄 추가:
    ```
    // Phase 10.1: Infobip env 3종(API_KEY/BASE_URL/SENDER)으로 mock 마이그레이션.
    // APPLICATION_ID/MESSAGE_ID는 더 이상 사용되지 않음.
    ```

    **C. 회귀 동작 유지 확인:**
    - `@Throttle` decorator metadata 어서션 2건(sendCode 20/3600000ms, verifyCode 10/900000ms) — 변경 없음
    - `sendCodeSchema` zod 국제 번호 테스트 6건 — 변경 없음
    - HTTP 429 HttpException 테스트 3건 — env 교체 후에도 동일하게 통과해야 함
      - Plan 04 sms.service는 mockRedis의 반환값을 보고 동일 로직으로 429 throw (HttpException status 429)
      - 단, mockRedis의 `eval` 반환값이 `Promise<number>` (phone axis counter)인 점은 동일 — VERIFY_AND_INCREMENT은 controller spec 범위 밖

    **D. 검증:**
    ```bash
    pnpm --filter @grapit/api test sms.controller -- --run
    ```
    전 케이스 GREEN.

    **편집 금지:**
    - `describe('SmsController @Throttle decorators')` 블록 내용
    - `describe('sendCodeSchema phone validation')` 블록 내용
    - 파일 상단 Reflect.getMetadata 패턴
  </action>
  <acceptance_criteria>
    - `grep -q "INFOBIP_SENDER" apps/api/src/modules/sms/sms.controller.spec.ts`
    - `! grep -q "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID" apps/api/src/modules/sms/sms.controller.spec.ts`
    - `grep -c "INFOBIP_SENDER" apps/api/src/modules/sms/sms.controller.spec.ts` >= 3 (3개 케이스에서 각각 env mock)
    - `pnpm --filter @grapit/api test sms.controller -- --run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/api test sms.controller -- --run 2>&1 | tail -15</automated>
  </verify>
  <done>sms.controller.spec.ts env 3종 마이그레이션, 전 케이스 GREEN, 레거시 env 참조 0건</done>
</task>

<task type="auto" tdd="false">
  <id>10.1-05-T2</id>
  <name>Task 2: sms-throttle.integration.spec.ts — env 3종 마이그레이션 + VERIFY_AND_INCREMENT Lua smoke 추가</name>
  <files>apps/api/test/sms-throttle.integration.spec.ts</files>
  <read_first>
    - apps/api/test/sms-throttle.integration.spec.ts (현재 구조 — testcontainers Valkey + NestJS TestingModule)
    - apps/api/src/modules/sms/sms.service.ts (Plan 04 결과 — VERIFY_AND_INCREMENT_LUA 소스)
    - .planning/phases/10-sms/10-09-PLAN.md (Phase 10 integration 원본 — 패턴 참조)
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §2.3 Lua script 구조
  </read_first>
  <action>
    `apps/api/test/sms-throttle.integration.spec.ts` 편집. 2단계 마이그레이션.

    **A. 기존 4종 env mock/secret injection을 3종으로 교체:**

    파일 내 모든 `INFOBIP_APPLICATION_ID`, `INFOBIP_MESSAGE_ID` 참조를 찾는다.
    - ConfigService mock이나 `process.env` 주입 블록 → 3종(API_KEY/BASE_URL/SENDER)로 교체
    - TestingModule providers에서 해당 값 사용 시 동일 교체

    예:
    ```typescript
    process.env.INFOBIP_API_KEY = 'test-key';
    process.env.INFOBIP_BASE_URL = 'https://test.api.infobip.com';
    // 기존 APPLICATION_ID, MESSAGE_ID 라인 제거
    process.env.INFOBIP_SENDER = '0212345678';
    ```

    **B. InfobipClient 외부 호출 mock 처리:**

    Integration test는 실 Valkey는 쓰되 실 Infobip은 호출하면 안 됨. 기존 Phase 10 spec이 이미 InfobipClient를 어떻게 차단하는지 확인 후 유지/수정:
    - `vi.spyOn(InfobipClient.prototype, 'sendPin')` 패턴이 있다면 → `sendSms`로 교체
    - spy 반환값: `{ messageId: 'test-mid', status: 'MESSAGE_ACCEPTED', groupId: 1 }`

    verifyPin mock은 완전 제거 (v3에서 사용 안 함).

    **C. 새 describe 블록 추가 — VERIFY_AND_INCREMENT_LUA smoke:**

    기존 describe 블록 뒤에 신규:

    ```typescript
    describe('VERIFY_AND_INCREMENT_LUA atomic script (Valkey EVAL)', () => {
      let redis: IORedis;
      // testcontainer 기반 setup은 기존 beforeAll/beforeEach 패턴 재사용

      const VERIFY_AND_INCREMENT_LUA = `
    local stored = redis.call('GET', KEYS[1])
    if stored == false then
      return {'EXPIRED', 0}
    end

    local attempts = redis.call('INCR', KEYS[2])
    if attempts == 1 then
      redis.call('EXPIRE', KEYS[2], 900)
    end

    local max = tonumber(ARGV[2])
    if attempts > max then
      redis.call('DEL', KEYS[1], KEYS[2])
      return {'NO_MORE_ATTEMPTS', 0}
    end

    if stored == ARGV[1] then
      redis.call('DEL', KEYS[1], KEYS[2])
      redis.call('SETEX', KEYS[3], tonumber(ARGV[3]), '1')
      return {'VERIFIED', attempts}
    end

    return {'WRONG', max - attempts}
    `;

      const keys = (phone: string) => [
        `sms:otp:${phone}`,
        `sms:attempts:${phone}`,
        `sms:verified:${phone}`,
      ];

      beforeEach(async () => {
        // 테스트 키 클린업
        await redis.del(...keys('+821099990001'));
      });

      it('정답 코드 → VERIFIED, verified 플래그 저장, otp/attempts DEL', async () => {
        const phone = '+821099990001';
        await redis.set(`sms:otp:${phone}`, '123456', 'PX', 180_000);

        const result = await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '123456', '5', '600',
        );
        expect(result).toEqual(['VERIFIED', 1]);
        expect(await redis.get(`sms:otp:${phone}`)).toBeNull();
        expect(await redis.get(`sms:attempts:${phone}`)).toBeNull();
        expect(await redis.get(`sms:verified:${phone}`)).toBe('1');
      });

      it('오답 코드 → WRONG, attempts INCR만', async () => {
        const phone = '+821099990001';
        await redis.set(`sms:otp:${phone}`, '123456', 'PX', 180_000);

        const result = await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '999999', '5', '600',
        );
        expect(result).toEqual(['WRONG', 4]);
        expect(await redis.get(`sms:otp:${phone}`)).toBe('123456');
        expect(await redis.get(`sms:attempts:${phone}`)).toBe('1');
      });

      it('otp 없음 → EXPIRED', async () => {
        const phone = '+821099990001';
        // otp 미저장
        const result = await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '123456', '5', '600',
        );
        expect(result).toEqual(['EXPIRED', 0]);
      });

      it('attempts 5회 초과 시 NO_MORE_ATTEMPTS + otp/attempts DEL', async () => {
        const phone = '+821099990001';
        await redis.set(`sms:otp:${phone}`, '123456', 'PX', 180_000);

        // 먼저 4번 틀리게 호출 (attempts=4)
        for (let i = 0; i < 4; i++) {
          await redis.eval(
            VERIFY_AND_INCREMENT_LUA, 3,
            ...keys(phone), '999999', '5', '600',
          );
        }
        // 5번째 틀리기 — attempts=5, max=5, 조건 attempts > max 불충족 → WRONG(0)
        const r5 = await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '999999', '5', '600',
        );
        expect(r5).toEqual(['WRONG', 0]);

        // 6번째 → attempts=6 > max=5 → NO_MORE_ATTEMPTS
        const r6 = await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '999999', '5', '600',
        );
        expect(r6).toEqual(['NO_MORE_ATTEMPTS', 0]);
        expect(await redis.get(`sms:otp:${phone}`)).toBeNull();
        expect(await redis.get(`sms:attempts:${phone}`)).toBeNull();
      });

      it('attempts EXPIRE 900s 설정 확인', async () => {
        const phone = '+821099990001';
        await redis.set(`sms:otp:${phone}`, '123456', 'PX', 180_000);

        await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '999999', '5', '600',
        );
        const ttl = await redis.ttl(`sms:attempts:${phone}`);
        expect(ttl).toBeGreaterThan(800);
        expect(ttl).toBeLessThanOrEqual(900);
      });

      it('verified 플래그 TTL 600s 설정 확인', async () => {
        const phone = '+821099990001';
        await redis.set(`sms:otp:${phone}`, '123456', 'PX', 180_000);

        await redis.eval(
          VERIFY_AND_INCREMENT_LUA, 3,
          ...keys(phone), '123456', '5', '600',
        );
        const ttl = await redis.ttl(`sms:verified:${phone}`);
        expect(ttl).toBeGreaterThan(550);
        expect(ttl).toBeLessThanOrEqual(600);
      });
    });
    ```

    **D. 기존 send-code/verify-code IP+phone axis 통합 테스트:**

    Plan 04는 SmsService 로직을 바꿨지만 IP axis @Throttle 상수(20/3600000ms, 10/900000ms), phone axis 상수, cooldown 30s는 전부 유지. 따라서 기존 integration 케이스는 env 3종 마이그레이션 외 수정 불필요.

    단, verify-code integration에서 `mockRedis.get('sms:pin:...')` 패턴이 있다면 → `redis.set('sms:otp:...', '000000', 'PX', 180000)` 선저장 후 verifyCode 호출하여 VERIFIED 시나리오로 마이그레이션. 이 때 code는 test에서 알고 있는 값(예 '000000')을 사용.

    **E. 검증:**
    ```bash
    pnpm --filter @grapit/api test:integration sms-throttle -- --run
    ```
    기존 케이스 + 신규 6개 describe 블록 전부 GREEN.

    test:integration 스크립트가 없다면 기존 Phase 10 vitest config 재사용. 별도 config 파일 수정이 필요한지는 기존 파일 읽기로 확인하여 판단.

    **F. 편집 금지:**
    - testcontainers beforeAll/afterAll 부트스트랩 블록 (unless env 주입 변경 필요)
    - ThrottlerStorageRedisService 주입 블록
    - Phase 10의 password-reset 3/900s 유지 테스트
  </action>
  <acceptance_criteria>
    - `grep -q "INFOBIP_SENDER" apps/api/test/sms-throttle.integration.spec.ts`
    - `! grep -q "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID" apps/api/test/sms-throttle.integration.spec.ts`
    - `grep -q "VERIFY_AND_INCREMENT\|sms:otp:" apps/api/test/sms-throttle.integration.spec.ts`
    - `grep -q "VERIFIED\|EXPIRED\|NO_MORE_ATTEMPTS\|WRONG" apps/api/test/sms-throttle.integration.spec.ts`
    - `! grep -q "sendPin\|verifyPin\|pinId\|attemptsRemaining" apps/api/test/sms-throttle.integration.spec.ts`
    - `pnpm --filter @grapit/api test:integration sms-throttle -- --run` exits 0 (or equivalent vitest command)
    - `pnpm --filter @grapit/api test -- --run` 전체 실행 시 0 실패
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/api test -- --run 2>&1 | tail -40</automated>
  </verify>
  <done>sms-throttle.integration.spec.ts env 3종 마이그레이션 + VERIFY_AND_INCREMENT Lua smoke 6개 추가, 전체 api 테스트 0 실패</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → testcontainer Valkey | 실제 Valkey 인스턴스에 테스트 데이터 투입 |
| Integration test → mocked Infobip | 실 외부 호출 차단 (비용/레이턴시 방어) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-05-01 | Tampering | Integration test가 실 Infobip 호출 | mitigate | `vi.spyOn(InfobipClient.prototype, 'sendSms')`로 차단. 실패 시 네트워크 에러로 test 즉시 fail → 실수 감지. |
| T-10.1-05-02 | Tampering | Lua script 문자열 integration vs production 불일치 | mitigate | Task 2 action의 script 블록이 Plan 04 sms.service.ts의 VERIFY_AND_INCREMENT_LUA와 **완전 동일**해야 함. Plan 실행 시 checker가 diff 확인. Phase 11에서 script를 shared const로 export하는 리팩터링은 선택. |
| T-10.1-05-03 | Denial of Service | testcontainer startup 시 타임아웃 | accept | Phase 10 기존 vitest config/타임아웃 승계. CI 환경에서 테스트 자동 재시도 1회. |
</threat_model>

<verification>
- `pnpm --filter @grapit/api test -- --run` → 0 실패 (Phase 10.1 success criteria #5)
- `pnpm --filter @grapit/api test:integration sms-throttle -- --run` → 신규 6 케이스 + 기존 케이스 GREEN
- `grep -r "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID\|sendPin\|verifyPin\|pinId\b" apps/api` → 0건
</verification>

<success_criteria>
- controller 단위 테스트 + integration 테스트에서 env 3종 체계 동작 확인
- 신규 VERIFY_AND_INCREMENT_LUA가 실 Valkey에서 4분기 결과를 올바르게 반환
- 전체 API 테스트 스위트 0 실패
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-sms-api-v3-rewrite/10.1-05-SUMMARY.md`
</output>
