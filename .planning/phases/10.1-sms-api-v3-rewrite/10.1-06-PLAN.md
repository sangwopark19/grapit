---
phase: 10.1
plan: 06
type: execute
wave: 5
depends_on: [10.1-01, 10.1-05]
files_modified: []
autonomous: false
requirements: [SMS-01, SMS-02, SMS-03, SMS-04]
tags: [human-verify, checkpoint, sms-v3, smoke]
must_haves:
  truths:
    - "`pnpm --filter @grapit/api typecheck`가 0 실패로 통과"
    - "`pnpm --filter @grapit/api lint`가 변경 파일(sms/*)에서 0 에러"
    - "`pnpm --filter @grapit/api test -- --run`이 0 실패로 통과 (Phase 10.1 Success Criteria #5)"
    - "grep 기반 레거시 참조 0건: INFOBIP_APPLICATION_ID, INFOBIP_MESSAGE_ID, sendPin, verifyPin, /2fa/2/pin, attemptsRemaining, PIN_MAPPING_TTL_MS, sms:pin:"
    - "사용자가 dev mock 모드에서 sendVerificationCode → '000000' → verified=true 흐름을 phone-verification UI에서 확인"
    - "사용자가 로컬 `.env`에 INFOBIP 3종을 채운 후 본인 번호로 실 SMS 발송 smoke 1회 수행"
    - "사용자가 받은 6자리 OTP를 입력하여 verified=true 수신 확인"
    - "사용자가 잘못된 코드 입력 시 '인증번호가 일치하지 않습니다' 메시지 확인"
    - "사용자가 30초 이내 재발송 시도 시 '잠시 후 다시 시도해주세요' (429) 메시지 확인"
    - "DEPLOY-CHECKLIST.md / INFOBIP-SETUP-GUIDE.md 재검토 완료: 2FA Application/Message Template 섹션 제거됐고 3종 env 체계로 정합"
  artifacts: []
  key_links: []
---

<objective>
Phase 10.1의 전체 결과를 자동 체크와 사용자 smoke 검증으로 종결한다.

- **자동 게이트** (Task 1): typecheck + lint + 전체 test + grep 레거시 부재 — 모두 통과해야 Task 2로 진행
- **사용자 smoke** (Task 2): dev mock 모드에서 회원가입 flow → 실 Infobip 3종 env 주입 후 본인 번호로 1회 smoke → 30s cooldown / 오답 케이스 수동 확인
- **문서 재검토** (Task 3): DEPLOY-CHECKLIST.md / INFOBIP-SETUP-GUIDE.md가 Plan 01 수정 이후 내부 일관성을 유지하는지 최종 확인

Purpose:
- CI 자동 테스트가 mock 기반이므로 "사용자에게 실제로 SMS가 도달하는가"는 human-verify 없이는 보장 불가
- Phase 10 UAT 패턴(10-HUMAN-UAT.md)을 승계하여 동일 톤의 수동 검증 절차 유지
- Plan 01에서 수정한 배포 문서는 실제 운영 시점에 사용되므로 최종 읽기 검증 필수

Output: 체크리스트 결과가 `10.1-06-SUMMARY.md`에 기록됨. 실패 항목 있으면 해당 plan 재실행.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-01-PLAN.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-04-PLAN.md
@.planning/phases/10-sms/DEPLOY-CHECKLIST.md
@.planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md
</context>

<tasks>

<task type="auto">
  <id>10.1-06-T1</id>
  <name>Task 1: 자동 게이트 — typecheck + lint + 전체 test + legacy grep</name>
  <files />
  <action>
    다음 명령을 순차 실행하고 모두 exit 0이어야 Task 2로 진행:

    ```bash
    # 1. Typecheck (Plan 03 → 04 통합 결과 필수 검증)
    pnpm --filter @grapit/api typecheck

    # 2. Lint (변경 범위)
    pnpm --filter @grapit/api lint apps/api/src/modules/sms/

    # 3. 전체 API 테스트 (Success Criteria #5)
    pnpm --filter @grapit/api test -- --run

    # 4. Integration 테스트 (별도 스크립트가 있다면)
    pnpm --filter @grapit/api test:integration -- --run 2>/dev/null || echo "integration script absent — skip"

    # 5. 레거시 참조 부재 검증
    echo "=== Legacy references check ==="
    grep -rn "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID" apps/api/src apps/api/test .env.example 2>/dev/null | grep -v "node_modules" || echo "OK: no INFOBIP_APPLICATION_ID/INFOBIP_MESSAGE_ID in src/test/.env.example"
    grep -rn "sendPin\|verifyPin" apps/api/src apps/api/test 2>/dev/null | grep -v "node_modules" || echo "OK: no sendPin/verifyPin"
    grep -rn "/2fa/2/pin" apps/api/src apps/api/test 2>/dev/null | grep -v "node_modules" || echo "OK: no /2fa/2/pin"
    grep -rn "attemptsRemaining\|PIN_MAPPING_TTL_MS\|sms:pin:\|InfobipSendPinResponse\|InfobipVerifyPinResponse" apps/api/src apps/api/test 2>/dev/null | grep -v "node_modules" || echo "OK: no legacy 2FA API types"

    # 6. 신규 계약 존재 검증
    echo "=== New contract presence check ==="
    grep -q "INFOBIP_SENDER" apps/api/src/modules/sms/sms.service.ts && echo "OK: INFOBIP_SENDER in sms.service.ts"
    grep -q "sms/3/messages" apps/api/src/modules/sms/infobip-client.ts && echo "OK: /sms/3/messages in infobip-client.ts"
    grep -q "VERIFY_AND_INCREMENT_LUA" apps/api/src/modules/sms/sms.service.ts && echo "OK: VERIFY_AND_INCREMENT_LUA in sms.service.ts"
    grep -q "randomInt(100000, 1000000)" apps/api/src/modules/sms/sms.service.ts && echo "OK: crypto.randomInt OTP generation"
    ```

    **실패 처리:**
    - typecheck 실패 → Plan 04 재실행 필요 (어떤 call site가 legacy API 여전히 사용)
    - lint 실패 → 해당 파일 수정 후 재검증 (가벼우면 이 task에서 인라인 fix)
    - test 실패 → 해당 plan(02/03/04/05 중)으로 돌아가 fix
    - grep "legacy present" → 해당 파일이 어느 plan에서 놓쳤는지 식별하여 fix
    - grep "new contract absent" → Plan 03/04 재실행

    **출력 기록:**
    모든 통과 시 다음 한 줄을 콘솔에 출력:
    ```
    === Phase 10.1 auto-gate PASSED ===
    ```
    이 문자열을 `10.1-06-SUMMARY.md`에 복붙.
  </action>
  <verify>
    <automated>pnpm --filter @grapit/api typecheck && pnpm --filter @grapit/api test -- --run && ! grep -rn "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID\|sendPin\|verifyPin\|/2fa/2/pin\|InfobipSendPinResponse\|InfobipVerifyPinResponse" apps/api/src apps/api/test --include="*.ts" | grep -v "node_modules" && grep -q "INFOBIP_SENDER" apps/api/src/modules/sms/sms.service.ts && grep -q "sms/3/messages" apps/api/src/modules/sms/infobip-client.ts</automated>
  </verify>
  <done>자동 게이트 전 체크 항목 통과, 레거시 0건 확정</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>10.1-06-T2</id>
  <name>Task 2: 사용자 smoke — dev mock flow + 실 SMS 1회 발송/검증</name>
  <what-built>
    Phase 10.1 완료 시점:
    - `InfobipClient`가 /sms/3/messages v3 단일 호출
    - `SmsService`가 crypto.randomInt 6자리 OTP → Valkey 저장 → 자체 Lua verify
    - 환경변수 3종 (INFOBIP_API_KEY, INFOBIP_BASE_URL, INFOBIP_SENDER)
    - Phase 10 rate limiting (IP + phone axis) + 30s cooldown + mock 모드 유지
    - 응답 shape 불변 (auth.service.ts 무영향)

    프론트엔드(`phone-verification.tsx`)와 `sms.controller.ts`는 변경 없음. API 계약 동일 (`POST /api/v1/sms/send-code`, `POST /api/v1/sms/verify-code`).
  </what-built>
  <how-to-verify>
    **Part A — Dev Mock 로컬 검증** (5분, 비용 0)

    1. `.env` 파일에서 INFOBIP 3종을 모두 비우거나 주석 처리:
       ```bash
       # INFOBIP_API_KEY=
       # INFOBIP_BASE_URL=
       # INFOBIP_SENDER=
       ```

    2. `pnpm dev` 실행 (web + api)
       - api 기동 로그에서 `{"event":"sms.credential_missing","mode":"dev_mock"}` 확인

    3. 브라우저에서 http://localhost:3000/signup 진입
       - 이메일/비밀번호 입력 → step 3(전화번호 인증) 진입
       - 휴대폰 번호 입력 (아무 010-xxxx-xxxx)
       - "인증번호 발송" 클릭
       - 아무 6자리 입력 테스트: `000000` → "인증 완료" 메시지 확인
       - `111111` 같은 다른 6자리 → "인증번호가 일치하지 않습니다" 확인

    **Part A 통과 조건:** dev mock 상태에서 000000만 verified=true, 나머지는 false.

    ---

    **Part B — 실 SMS 발송 smoke** (10분, 비용 약 35원)

    ⚠️ 사전 요구:
    - Infobip 포털에서 API Key 발급 완료 (INFOBIP-SETUP-GUIDE.md §1.5)
    - Free Trial 계정이면 본인 번호가 verified recipient로 등록됨 (§1.6.1)
    - Base URL 확인 완료 (§1.2)
    - Sender는 Free Trial이면 공백 또는 임의값(공유 sender로 자동 치환됨)

    1. `.env`에 3종 채우기:
       ```bash
       INFOBIP_API_KEY=<실제 key>
       INFOBIP_BASE_URL=https://xxxxx.api.infobip.com
       INFOBIP_SENDER=<Free Trial: 비워두기 or 'Grapit' / Paid: KISA 등록 번호>
       ```

    2. `pnpm dev` 재시작
       - api 기동 로그에서 `sms.credential_missing` **없음** 확인 (production 모드가 아니어도 3종이 다 있으면 isDevMock=false)

    3. 브라우저에서 http://localhost:3000/signup 진입 → 전화번호 단계까지 진행

    4. 본인 휴대폰 번호 입력 → "인증번호 발송" 클릭
       - 응답 200 + "인증번호가 발송되었습니다" 토스트/라벨
       - 수 초 내 본인 휴대폰으로 SMS 도달 기대

    5. **수신된 SMS 형식 확인:**
       - 본문: `[국제발신][Grapit] 인증번호 XXXXXX (3분 이내 입력)` (앞의 `[국제발신]`은 한국 통신사 자동 태그)
       - 6자리 숫자가 랜덤으로 다르게 생성되는지 (Math.random 아님 확인)

    6. 받은 6자리를 입력 → "인증 완료" 메시지 확인
       - DevTools Network 탭에서 `POST /api/v1/sms/verify-code` 응답: `{"verified":true}`

    7. **오답 시나리오:** 재발송 쿨다운(30초) 대기 → 재발송 → 일부러 다른 6자리 입력
       - 응답: `{"verified":false,"message":"인증번호가 일치하지 않습니다"}`

    8. **쿨다운 시나리오:** 5번 단계에서 받은 SMS 입력 후, 즉시 새로 "인증번호 발송" 재시도
       - 응답 429 + "잠시 후 다시 시도해주세요"
       - 30초 대기 후 재발송 가능해짐

    **Part B 통과 조건:** 본인 휴대폰에 `[Grapit]` 포함 SMS 도달, 정답 입력 시 verified=true, 오답 시 false, 30초 내 재발송 시 429.

    ---

    **Part C — Valkey 상태 점검** (2분, 선택)

    Redis CLI로 Valkey 키 상태 확인:
    ```bash
    # 로컬 Valkey (Docker testcontainer 또는 dev 인스턴스)
    redis-cli -h localhost -p <port>

    # OTP 저장 확인 (발송 직후, 180s 이내)
    KEYS "sms:*"
    GET "sms:otp:+82<본인 번호>"
    TTL "sms:otp:+82<본인 번호>"
    # expected: 6자리 숫자 문자열, TTL 180 이내

    # verify 성공 후
    GET "sms:verified:+82<본인 번호>"
    # expected: "1", TTL 600 이내

    # verify 성공 후 otp/attempts는 사라져야
    GET "sms:otp:+82<본인 번호>"
    # expected: (nil)
    ```

    ---

    **재확인 체크리스트 (구두 확인 후 resume-signal 'approved'):**

    - [ ] Part A: dev mock 000000 동작
    - [ ] Part A: dev mock 111111 실패 메시지
    - [ ] Part B: 실 SMS 본인 번호 수신 (본문 `[Grapit] 인증번호 XXXXXX (3분 이내 입력)`)
    - [ ] Part B: 정답 verified=true
    - [ ] Part B: 오답 "인증번호가 일치하지 않습니다"
    - [ ] Part B: 30초 내 재발송 429
    - [ ] Part C (선택): Valkey 키 스키마 확인

    **실패 시 대응:**
    - Infobip 401 → API Key 검증 (`App ` prefix 이중 적용 확인 — `.env`에는 raw key만)
    - Infobip 400 `to: must be a valid phone number` → `e164.replace(/^\+/, '')` 동작 확인 (Plan 03)
    - SMS 미수신 → Free Trial이면 verified recipient 여부 확인, 유료면 KISA sender 상태 확인
    - OTP 평문 노출 → 로그/Sentry scope에 OTP가 들어있으면 즉시 Plan 04로 되돌아가 제거
  </how-to-verify>
  <resume-signal>Type "approved" or list failing items (예: "Part A OK, Part B 실패 — SMS 미수신")</resume-signal>
</task>

<task type="auto">
  <id>10.1-06-T3</id>
  <name>Task 3: 문서 재검토 — DEPLOY-CHECKLIST.md / INFOBIP-SETUP-GUIDE.md 내부 정합성</name>
  <files />
  <action>
    Plan 01에서 수정한 두 문서를 **읽기 전용 검증**하고 결과를 `10.1-06-SUMMARY.md`에 체크리스트로 기록한다.

    **A. `.planning/phases/10-sms/DEPLOY-CHECKLIST.md` 검증:**

    1. 섹션 목차 확인 (§1~§10 번호가 연속):
       ```bash
       grep -E "^## [0-9]+\." .planning/phases/10-sms/DEPLOY-CHECKLIST.md
       ```
       기대: §1 Portal → §2 KISA → §3 Sender ID → §4 API Key → §5 GCP Secret Manager → §6 GitHub Actions → §7 Cloud Run → §8 Staging Smoke → §9 Pre-Deploy Mandatory → §10 Production 배포

    2. 레거시 참조 0건:
       ```bash
       grep -n "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID\|pinAttempts\|pinTimeToLive\|sendPinPerPhoneNumberLimit\|allowMultiplePinVerifications\|Message Template 생성\|2FA Application 생성" .planning/phases/10-sms/DEPLOY-CHECKLIST.md
       ```
       허용 예외: "기존 2FA 전용 시크릿 제거" 같은 `gcloud secrets delete` 명령 컨텍스트 안 1~2건만.

    3. 신규 키워드 최소 1회:
       ```bash
       grep -c "sms/3/messages\|SMS API v3\|INFOBIP_SENDER" .planning/phases/10-sms/DEPLOY-CHECKLIST.md
       ```
       ≥ 3 (각 키워드 최소 1회 등장).

    4. `gcloud run services update` 블록의 `--update-secrets` 라인이 3개 (API_KEY/BASE_URL/SENDER)이고 `--remove-env-vars`에 APPLICATION_ID/MESSAGE_ID 포함.

    **B. `.planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md` 검증:**

    1. §1.3/§1.4가 v3로 교체됐는지:
       ```bash
       grep -E "^### 1\.(3|4)" .planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md
       ```
       기대: §1.3 발신자 / §1.4 메시지 본문 규격 (또는 유사 v3 타이틀)

    2. cURL 예시 블록이 /sms/3/messages body shape 준수:
       ```bash
       grep -A 10 "curl.*sms/3/messages" .planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md
       ```
       기대: messages[].from/destinations/text 구조 포함.

    3. §1.7 환경변수 섹션이 3종:
       ```bash
       sed -n '/^### 1\.7/,/^## /p' .planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md | grep "^INFOBIP_"
       ```
       기대: INFOBIP_API_KEY, INFOBIP_BASE_URL, INFOBIP_SENDER 3줄만.

    4. 메타 노트 존재:
       ```bash
       grep -q "Phase 10\.1" .planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md
       ```

    **C. `.env.example` 검증:**

    ```bash
    grep "^INFOBIP_" .env.example
    ```
    기대: 3줄 (API_KEY, BASE_URL, SENDER) — 정확히 3줄.

    **D. ROADMAP.md 갱신:**

    Phase 10.1 항목 업데이트:
    ```bash
    # 현재
    grep -A 15 "### Phase 10\.1" .planning/ROADMAP.md
    ```
    Plan 목록(`- [ ] TBD`) 자리에 6개 plan 파일명 나열되도록 이 task에서 ROADMAP.md 편집:

    Plans:
    - [ ] 10.1-01-PLAN.md — env 3종 + DEPLOY/SETUP 문서 갱신
    - [ ] 10.1-02-PLAN.md — 테스트 스캐폴딩 RED (spec + fixtures)
    - [ ] 10.1-03-PLAN.md — InfobipClient /sms/3/messages 재작성
    - [ ] 10.1-04-PLAN.md — SmsService OTP 자체 생성 + Valkey Lua verify
    - [ ] 10.1-05-PLAN.md — controller + integration 테스트 회귀 검증
    - [ ] 10.1-06-PLAN.md — 자동 게이트 + 사용자 smoke + 문서 재검토

    Plans 줄도 "6 plans"로 갱신.

    **E. SUMMARY 작성:**

    `.planning/phases/10.1-sms-api-v3-rewrite/10.1-06-SUMMARY.md`에 다음 체크리스트 기록 (각 항목 [x] / [ ] + 참조 명령어 결과 1~2줄):
    - DEPLOY-CHECKLIST.md 섹션 연속 / 레거시 0 / 신규 키워드 / gcloud 블록
    - INFOBIP-SETUP-GUIDE.md §1.3/§1.4/§1.7 / cURL / 메타 노트
    - .env.example 3종
    - ROADMAP.md plan 목록
    - Task 1 auto-gate 결과
    - Task 2 human smoke 결과

    **실패 처리:**
    문서 중 하나라도 criteria 미달 → Plan 01 재실행 후 본 task 재수행.
  </action>
  <verify>
    <automated>grep -q "sms/3/messages\|SMS API v3" .planning/phases/10-sms/DEPLOY-CHECKLIST.md && grep -q "INFOBIP_SENDER" .planning/phases/10-sms/DEPLOY-CHECKLIST.md && grep -q "INFOBIP_SENDER" .planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md && grep -q "Phase 10\.1" .planning/phases/10-sms/INFOBIP-SETUP-GUIDE.md && [ "$(grep -c '^INFOBIP_' .env.example)" = "3" ] && grep -q "10\.1-01-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>문서 3종 정합 검증 완료, ROADMAP 갱신, SUMMARY 체크리스트 기록</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 본인 휴대폰 ↔ Infobip ↔ 로컬 API | 실 SMS 한 번 발송 (~35원) |
| 개발자 로컬 `.env` | 실 API Key 보관 — `.gitignore` 포함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-06-01 | Information Disclosure | .env에 저장된 실 API Key가 git commit에 포함 | mitigate | `.gitignore`에 `.env` 존재 (Phase 1부터). `.env.example`은 placeholder만. |
| T-10.1-06-02 | Tampering | 사용자가 smoke 실패를 approve 처리 | accept | Resume-signal에 구체적 실패 항목 기술 요구. checker agent가 summary 재검토. |
| T-10.1-06-03 | Repudiation | 실 SMS 비용 누적 추적 없음 | accept | Phase 10.1 smoke는 1회 ~35원. 문제가 되는 규모는 아님. Infobip portal에서 사후 확인 가능. |
</threat_model>

<verification>
- Task 1: 자동 게이트 6개 모두 통과 로그
- Task 2: 사용자 체크리스트 8개 중 필수 7개(Part C 제외) 모두 체크
- Task 3: DEPLOY-CHECKLIST / INFOBIP-SETUP-GUIDE / .env.example / ROADMAP 4개 문서 정합
</verification>

<success_criteria>
- Phase 10.1 Success Criteria 5건 전부 관측 가능한 증거로 확인됨
- 운영 문서가 v3 API 기준으로 갱신되어 다음 배포 시점에 2FA Application/Message Template 언급 0건
- ROADMAP.md가 6 plan 목록으로 갱신되고 phase 10.1 항목이 실행 가능한 상태로 고정
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-sms-api-v3-rewrite/10.1-06-SUMMARY.md`
</output>
