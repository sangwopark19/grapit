---
phase: 10.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/sms/infobip-client.spec.ts
  - apps/api/src/modules/sms/sms.service.spec.ts
  - apps/api/src/modules/sms/__fixtures__/infobip-send-response.json
  - apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json
autonomous: true
requirements: [SMS-01, SMS-02, SMS-03, SMS-04]
tags: [tdd, red, test-scaffolding, sms-v3, fixtures]
must_haves:
  truths:
    - "infobip-client.spec.ts가 /sms/3/messages endpoint + `messages[].from/destinations/text` body shape + SendSmsResult 반환을 RED 상태로 선언"
    - "sms.service.spec.ts가 6자리 OTP 생성, Valkey `sms:otp:{e164}` 저장, Lua VERIFY_AND_INCREMENT 결과 분기, verify 성공 시 `sms:verified:{e164}` 플래그 저장을 RED 상태로 선언"
    - "sms.service.spec.ts에서 env 3종(INFOBIP_API_KEY/BASE_URL/SENDER) 누락 시나리오가 테스트되고 legacy APPLICATION_ID/MESSAGE_ID 참조가 제거됨"
    - "fixture JSON 2종이 Infobip /sms/3/messages 응답 구조(messages[].messageId/status/destination, bulkId)로 재작성됨"
    - "기존 2FA /2fa/2/pin 테스트 케이스, verifyPin 블록, attemptsRemaining 어서션이 spec에서 제거됨"
    - "IP axis @Throttle, phone axis Lua counter, 30s resend cooldown, mock 모드(000000) 테스트는 그대로 유지됨"
    - "spec 파일은 import 대상이 현재 /sms/3/messages 시그니처를 제공하지 않으므로 RED (Plan 03/04 실행 전까지 fail 정상)"
  artifacts:
    - path: "apps/api/src/modules/sms/infobip-client.spec.ts"
      provides: "InfobipClient.sendSms 전용 RED 테스트 (verifyPin 블록 삭제)"
    - path: "apps/api/src/modules/sms/sms.service.spec.ts"
      provides: "OTP 생성/Valkey 저장/Lua verify/verified 플래그 RED 테스트, env 3종 체계"
    - path: "apps/api/src/modules/sms/__fixtures__/infobip-send-response.json"
      provides: "POST /sms/3/messages 성공 응답 fixture"
    - path: "apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json"
      provides: "삭제 또는 빈 객체로 대체 (Grapit 자체 verify 전환)"
  key_links:
    - from: "infobip-client.spec.ts"
      to: "infobip-client.ts"
      via: "Plan 03에서 GREEN 전환"
      pattern: "sms/3/messages"
    - from: "sms.service.spec.ts"
      to: "sms.service.ts"
      via: "Plan 04에서 GREEN 전환"
      pattern: "sms:otp:|VERIFY_AND_INCREMENT|randomInt"
---

<objective>
Phase 10.1의 GREEN 전환을 위한 RED 테스트 스캐폴딩을 작성한다. Nyquist 원칙에 따라 구현 전에 기대 동작을 테스트로 선언한다.

주요 작업:
1. `infobip-client.spec.ts` 전면 재작성 — `/sms/3/messages` 하나의 `sendSms` 메서드 대상으로 축소, `verifyPin` 관련 전 블록 제거
2. `sms.service.spec.ts` 전면 재작성 — 4종 env → 3종, PIN mapping → 직접 OTP 저장, Infobip verifyPin 호출 → Valkey Lua `VERIFY_AND_INCREMENT` 결과 분기로 시나리오 전환. Rate limiting / cooldown / mock 모드(000000) / CN reject 테스트는 유지
3. fixture JSON 2종 — send-response를 v3 shape으로 재작성, verify-response는 제거(또는 빈 `{}`로 대체)

Purpose: 동일 상황에서 Plan 03 실행 시 `infobip-client.spec.ts`가 GREEN으로 전환되고, Plan 04 실행 시 `sms.service.spec.ts`가 GREEN으로 전환된다. 동시에 Phase 10의 2FA 시대 테스트 잔여물(verifyPin 어서션, attemptsRemaining 단언 등)이 0건 남도록 정리.

Output: 4 파일(테스트 2 + fixture 2), 모두 RED 상태.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md
@apps/api/src/modules/sms/infobip-client.spec.ts
@apps/api/src/modules/sms/sms.service.spec.ts
@apps/api/src/modules/sms/__fixtures__/infobip-send-response.json
@apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/phone.util.ts
</context>

<interfaces>
<!-- Plan 03(InfobipClient), Plan 04(SmsService)가 구현해야 할 계약 -->

<!-- infobip-client.ts Plan 03 결과: -->
export interface SendSmsResult {
  messageId: string;
  status: string;   // e.g. "PENDING_ACCEPTED", "MESSAGE_ACCEPTED"
  groupId: number;  // 1=PENDING, 2=UNDELIVERABLE, 3=DELIVERED, 4=EXPIRED, 5=REJECTED
}
export class InfobipApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly body: string,
  ) { super(...); }
}
export class InfobipClient {
  constructor(baseUrl: string, apiKey: string, sender: string);
  async sendSms(e164: string, text: string): Promise<SendSmsResult>;
}

<!-- sms.service.ts Plan 04 결과 (기존 SendResult/VerifyResult shape 불변): -->
export interface SendResult { success: boolean; message: string }
export interface VerifyResult { verified: boolean; message?: string }
@Injectable()
export class SmsService {
  constructor(configService, @Inject(REDIS_CLIENT) redis);
  async sendVerificationCode(phone: string): Promise<SendResult>;
  async verifyCode(phone: string, code: string): Promise<VerifyResult>;
}
// - 6자리 OTP = String(crypto.randomInt(100000, 1000000))
// - 저장: redis.set(`sms:otp:${e164}`, otp, 'PX', 180_000)
// - verify: redis.eval(VERIFY_AND_INCREMENT_LUA, 3, otpKey, attemptsKey, verifiedKey, userCode, '5', '600')
//   → [string result, number data]
//     - ['VERIFIED', attempts]         — 정답, `sms:verified:{e164}` TTL 600s 저장
//     - ['WRONG', remainingAttempts]   — 오답
//     - ['EXPIRED', 0]                 — PIN 없음
//     - ['NO_MORE_ATTEMPTS', 0]        — 시도 초과(→ GoneException)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <id>10.1-02-T1</id>
  <name>Task 1: __fixtures__ 2종 재작성 — /sms/3/messages 응답 shape으로 교체</name>
  <files>
    apps/api/src/modules/sms/__fixtures__/infobip-send-response.json,
    apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json
  </files>
  <read_first>
    - apps/api/src/modules/sms/__fixtures__/infobip-send-response.json (기존 2FA send 응답)
    - apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json (기존 2FA verify 응답 4종)
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §1.3 "Response Schema"
  </read_first>
  <action>
    **A. `infobip-send-response.json` 재작성:**
    기존 `{ pinId, to, ncStatus, smsStatus }` 구조를 /sms/3/messages 응답으로 교체:
    ```json
    {
      "messages": [
        {
          "messageId": "2250be2d4219-3af1-78856-aabe-1362af1edfd2",
          "status": {
            "groupId": 1,
            "groupName": "PENDING",
            "id": 26,
            "name": "MESSAGE_ACCEPTED",
            "description": "Message sent to next instance"
          },
          "destination": "821012345678"
        }
      ],
      "bulkId": "2034072219640523072"
    }
    ```

    **B. `infobip-verify-response.json` 처리:**
    Grapit 자체 verify(Valkey Lua)로 전환하므로 이 fixture는 더 이상 프로덕션 경로에서 사용되지 않는다. 선택지:
    - 옵션 A (선호): 빈 객체로 대체 + 주석용 키 추가. git history 보존 + import 에러 방지.
      ```json
      {
        "_deprecated": "Phase 10.1: Infobip verify endpoint is no longer used. Verification happens locally via Valkey Lua VERIFY_AND_INCREMENT script. File retained to avoid broken imports during transition; remove after all spec imports are cleaned."
      }
      ```
    - 옵션 B: `git rm` 후 spec에서 import 제거. 단, 동일 plan에서 spec을 대대적으로 편집하므로 혼란 회피를 위해 옵션 A 채택.

    옵션 A 채택. 파일 자체를 유지하되 내용을 위와 같이 교체.

    **JSON 형식 주의:**
    - JSON parse 가능해야 함 (trailing comma 금지)
    - `node -e "JSON.parse(require('fs').readFileSync('apps/api/src/modules/sms/__fixtures__/infobip-send-response.json','utf8'))"` 통과

    파일 저장 후 이후 Task 2, 3에서 이 shape을 참조하여 spec을 작성한다.
  </action>
  <acceptance_criteria>
    - `grep -q "messageId" apps/api/src/modules/sms/__fixtures__/infobip-send-response.json`
    - `grep -q "groupId" apps/api/src/modules/sms/__fixtures__/infobip-send-response.json`
    - `grep -q "bulkId" apps/api/src/modules/sms/__fixtures__/infobip-send-response.json`
    - `! grep -q "pinId\|smsStatus\|ncStatus" apps/api/src/modules/sms/__fixtures__/infobip-send-response.json`
    - `grep -q "_deprecated\|Phase 10\.1" apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json`
    - `! grep -q "attemptsRemaining\|NO_MORE_PIN_ATTEMPTS\|WRONG_PIN" apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json`
    - JSON parse 성공: `node -e "['apps/api/src/modules/sms/__fixtures__/infobip-send-response.json','apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json'].forEach(p => JSON.parse(require('fs').readFileSync(p,'utf8')))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs = require('fs'); const s = JSON.parse(fs.readFileSync('apps/api/src/modules/sms/__fixtures__/infobip-send-response.json','utf8')); if (!s.messages?.[0]?.messageId) process.exit(1); const v = JSON.parse(fs.readFileSync('apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json','utf8')); if (!v._deprecated) process.exit(2); console.log('fixtures OK');"</automated>
  </verify>
  <done>send-response fixture는 /sms/3/messages shape, verify-response는 deprecated 마커로 대체</done>
</task>

<task type="auto" tdd="false">
  <id>10.1-02-T2</id>
  <name>Task 2: infobip-client.spec.ts 전면 재작성 — /sms/3/messages sendSms 전용 RED</name>
  <files>apps/api/src/modules/sms/infobip-client.spec.ts</files>
  <read_first>
    - apps/api/src/modules/sms/infobip-client.spec.ts (현재 — sendPin + verifyPin)
    - apps/api/src/modules/sms/__fixtures__/infobip-send-response.json (Task 1 결과)
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §1.1 "Endpoint", §1.2 "Request Body Schema", §2.1 "InfobipClient 최소 API"
  </read_first>
  <action>
    `apps/api/src/modules/sms/infobip-client.spec.ts` 전면 재작성. 기존 구조 유지 요소는 `vi.stubGlobal('fetch', fetchSpy)` 패턴뿐이며, 나머지는 전부 교체한다.

    **상단 import 블록:**
    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import {
      InfobipClient,
      InfobipApiError,
      type SendSmsResult,
    } from './infobip-client';
    import sendFixture from './__fixtures__/infobip-send-response.json';
    ```

    **상수 블록:**
    ```typescript
    const BASE_URL = 'https://x.api.infobip.com';
    const API_KEY  = 'test-api-key';
    const SENDER   = '0212345678';
    ```

    **describe('InfobipClient')** 구조 — 모두 RED (import된 SendSmsResult / sendSms는 Plan 03 실행 전까지 미존재):

    1. **describe('sendSms')** 블록 내부 `it` 7개:

       - `'POST /sms/3/messages 경로로 호출'`
         - fetchSpy.mockResolvedValueOnce(new Response(JSON.stringify(sendFixture), { status: 200 }))
         - `await client.sendSms('+821012345678', 'test text')`
         - `expect(url).toBe(\`${BASE_URL}/sms/3/messages\`)`

       - `'Authorization 헤더 "App {apiKey}"'`
         - 같은 호출, 헤더 검증

       - `'Content-Type application/json'`
         - 헤더 검증

       - `'body는 { messages: [{ from, destinations: [{ to }], text }] } 구조'`
         - body JSON.parse → `expect(body.messages[0].from).toBe(SENDER)`
         - `expect(body.messages[0].destinations[0].to).toBe('821012345678')` (leading + 제거)
         - `expect(body.messages[0].text).toBe('test text')`

       - `'200 응답 시 SendSmsResult 반환'`
         - `expect(result.messageId).toBe(sendFixture.messages[0].messageId)`
         - `expect(result.groupId).toBe(1)`
         - `expect(result.status).toBe('MESSAGE_ACCEPTED')` (fixture status.name)

       - `'400 응답 시 InfobipApiError throw'`
         - fetchSpy.mockResolvedValueOnce(new Response('{"requestError":{"serviceException":{"text":"bad to"}}}', { status: 400 }))
         - `await expect(client.sendSms(...)).rejects.toThrow(InfobipApiError)`
         - 에러의 status === 400, body 포함 확인

       - `'5xx 응답 시 InfobipApiError throw'`
         - 상동, status 500 fixture로 교체

       - `'AbortSignal.timeout 5초 설정'`
         - `expect(init.signal).toBeDefined()`

       - `'leading + 제거 (e164.replace 동작 확인)'`
         - `await client.sendSms('+861391234XXXX', 'hi')` → body.messages[0].destinations[0].to 가 `861391234XXXX`

       - `'응답에 messages 배열이 비어있을 때 InfobipApiError 500으로 fallback'`
         - fetchSpy → `new Response(JSON.stringify({ messages: [] }), { status: 200 })`
         - `await expect(...).rejects.toThrow(/messages/)`

    2. **describe('InfobipApiError')** 블록 내부 `it` 2개 (기존 유지):
       - `'status와 body를 캡처한다'`
       - `'Error의 instance이다'`

    **삭제 대상:**
    - `describe('verifyPin', ...)` 블록 전체
    - `InfobipVerifyPinResponse`, `InfobipSendPinResponse` type import
    - `APP_ID`, `MSG_ID` 상수 및 관련 assertion
    - `applicationId` / `messageId` body assertion
    - `verifyFixtures` import 및 사용처 전부
    - `verify`로 시작하는 URL 어서션 전부

    **beforeEach 수정:**
    ```typescript
    beforeEach(() => {
      client = new InfobipClient(BASE_URL, API_KEY, SENDER);  // ← 4th arg 제거, SENDER 추가
      fetchSpy = vi.fn();
      vi.stubGlobal('fetch', fetchSpy);
    });
    ```

    **RED 상태 확인:**
    - `SendSmsResult`, `sendSms`가 Plan 03 실행 전까지 infobip-client.ts에 존재하지 않음 → import 에러 또는 타입 에러 또는 런타임 fail. 정상.
  </action>
  <acceptance_criteria>
    - `grep -q "sms/3/messages" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `grep -q "SendSmsResult" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `grep -q "sendSms\b" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `! grep -q "sendPin\|verifyPin" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `! grep -q "APP_ID\|MSG_ID\|applicationId\|messageId" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `grep -q "destinations" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `grep -q "InfobipApiError" apps/api/src/modules/sms/infobip-client.spec.ts`
    - RED 확인: 현재 infobip-client.ts는 sendPin/verifyPin만 존재하므로 테스트 실행 시 fail. 수동 검증 불필요(Plan 03 완료 후 GREEN 전환으로 증명).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "sms/3/messages" apps/api/src/modules/sms/infobip-client.spec.ts && grep -q "sendSms" apps/api/src/modules/sms/infobip-client.spec.ts && ! grep -q "sendPin\|verifyPin\|applicationId\|messageId" apps/api/src/modules/sms/infobip-client.spec.ts</automated>
  </verify>
  <done>infobip-client.spec.ts가 /sms/3/messages + sendSms 전용, 2FA 관련 케이스 0건. RED 상태.</done>
</task>

<task type="auto" tdd="false">
  <id>10.1-02-T3</id>
  <name>Task 3: sms.service.spec.ts 전면 재작성 — OTP 자체 생성 + Valkey Lua verify + env 3종 체계 RED</name>
  <files>apps/api/src/modules/sms/sms.service.spec.ts</files>
  <read_first>
    - apps/api/src/modules/sms/sms.service.spec.ts (현재 — PIN mapping + Infobip verifyPin)
    - apps/api/src/modules/sms/sms.service.ts (현재 구현 참고용)
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md §"OTP 생성 로직", §"Verify 로직"
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §2.2 "OTP 생성 & 저장", §2.3 "Verify 로직(Lua atomic)"
  </read_first>
  <action>
    `apps/api/src/modules/sms/sms.service.spec.ts` 전면 재작성. 테스트 케이스 구조를 유지하되 아래 변경사항 반영.

    **A. 상단 import + mock 블록 변경:**
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { BadRequestException, GoneException, HttpException } from '@nestjs/common';
    import { ConfigService } from '@nestjs/config';
    import { SmsService } from './sms.service.js';
    import { InfobipClient, InfobipApiError } from './infobip-client.js';

    // crypto.randomInt 결정성을 위해 spy 확보
    import * as nodeCrypto from 'node:crypto';

    const mockRedis = {
      set: vi.fn(),
      get: vi.fn(),
      del: vi.fn(),
      pttl: vi.fn(),
      eval: vi.fn(),
    };
    ```

    **B. `createConfigService` 교체:**
    ```typescript
    function createConfigService(overrides: Record<string, string | undefined> = {}): ConfigService {
      const config: Record<string, string | undefined> = {
        INFOBIP_API_KEY: 'test-key',
        INFOBIP_BASE_URL: 'https://test.api.infobip.com',
        INFOBIP_SENDER: '0212345678',
        NODE_ENV: 'test',
        ...overrides,
      };
      return { get: vi.fn((k: string) => config[k]) } as unknown as ConfigService;
    }
    ```
    `INFOBIP_APPLICATION_ID`, `INFOBIP_MESSAGE_ID` 완전 제거.

    **C. describe 구조 (모두 RED — Plan 04 실행 전까지 실제 SmsService에는 sendSms/Valkey OTP 저장/Lua verify 로직이 없으므로 fail):**

    1. **`describe('constructor')`**
       - `'production에서 INFOBIP_API_KEY 미설정 → throw'`
       - `'production에서 INFOBIP_BASE_URL 빈 문자열 → throw'`
       - `'production에서 INFOBIP_SENDER 누락 → throw'` ← 신규
       - `'non-production에서 3종 전부 미설정 → isDevMock=true'` ← 4종 → 3종 변경
       - `'non-production이어도 legacy env(APPLICATION_ID, MESSAGE_ID)를 참조하지 않는다'` — spy로 ConfigService.get 호출 인자 확인. `'INFOBIP_APPLICATION_ID'`, `'INFOBIP_MESSAGE_ID'`로는 호출되지 않음 (legacy env 완전 제거 증명).

    2. **`describe('sendVerificationCode')`**
       - `'중국 본토(+86) → BadRequestException'` (기존 유지)
       - `'dev mock에서 성공 반환 (Infobip 호출 없음)'`
          - `expect(InfobipClient.prototype.sendSms).not.toBeCalled()`
       - `'resend cooldown SET NX fail 시 HttpException(429)'` (기존 유지, statusCode 확인)
       - `'Infobip sendSms 성공 시 Valkey에 OTP 6자리 저장 (sms:otp:{e164}, TTL 180s)'`
          - `vi.spyOn(nodeCrypto, 'randomInt').mockReturnValueOnce(654321)` — deterministic
          - InfobipClient.prototype.sendSms mock `{ messageId: 'mid', status: 'MESSAGE_ACCEPTED', groupId: 1 }`
          - assert `mockRedis.set` 호출:
            - 키 `sms:otp:+821012345678` 포함
            - 값 `'654321'`
            - `'PX'`, `180000`
       - `'메시지 본문이 [Grapit] 인증번호 XXXXXX (3분 이내 입력) 포맷으로 전송됨'`
          - `expect(InfobipClient.prototype.sendSms).toHaveBeenCalledWith('+821012345678', '[Grapit] 인증번호 654321 (3분 이내 입력)')`
       - `'OTP 생성 시 Math.random 대신 crypto.randomInt 사용'`
          - `expect(nodeCrypto.randomInt).toHaveBeenCalledWith(100000, 1000000)`
       - `'Infobip sendSms 5xx 시 cooldown key DEL rollback'` (기존 유지, `InfobipApiError(500, ...)` mock)
       - `'Infobip sendSms 4xx 시 cooldown key 유지'` (기존 유지)
       - `'phone axis send counter Lua INCR: 6번째 호출 429'` (기존 유지, `mockRedis.eval.mockResolvedValueOnce(6)`)
       - `'phone axis send counter Lua에 sms:phone:send: prefix 키 전달'` (기존 유지)

    3. **`describe('verifyCode')`**

       **핵심 변경:** Infobip verifyPin 호출 제거, 대신 Valkey Lua `VERIFY_AND_INCREMENT_LUA` `redis.eval` 결과 분기를 테스트한다.

       - `'dev mock에서 000000 성공'` (기존 유지)
       - `'dev mock에서 잘못된 코드 실패'` (기존 유지)
       - `'phone axis verify counter Lua: 11번째 호출 429 (D-07)'` (기존 유지 — 이 counter는 `sms:phone:verify:{e164}` 별도 키)
       - `'Lua VERIFY_AND_INCREMENT 결과 ["VERIFIED", attempts] → {verified: true}, sms:verified:{e164} TTL 600s 저장'`
          - `mockRedis.eval.mockResolvedValueOnce(1)` — phone axis counter
          - `mockRedis.eval.mockResolvedValueOnce(['VERIFIED', 1])` — verify script
          - assert `redis.eval`이 VERIFY_AND_INCREMENT script 내용(INCR sms:attempts, GET sms:otp, SETEX sms:verified 패턴)으로 호출
          - Lua script 안에서 SETEX가 처리되므로 별도 redis.set 호출을 기대하지 않는다 (또는 분리 구현 시 `sms:verified:` 키에 TTL 600s로 set 호출).
       - `'Lua 결과 ["WRONG", remaining] → {verified: false, message: ...}'`
          - `mockRedis.eval.mockResolvedValueOnce(['WRONG', 3])`
          - 반환값 `{ verified: false, message: '인증번호가 일치하지 않습니다' }`
       - `'Lua 결과 ["EXPIRED", 0] → GoneException'`
          - `mockRedis.eval.mockResolvedValueOnce(['EXPIRED', 0])`
          - `GoneException` throw 확인, 메시지 "인증번호가 만료되었습니다. 재발송해주세요"
       - `'Lua 결과 ["NO_MORE_ATTEMPTS", 0] → GoneException'`
          - 상동
       - `'Infobip verifyPin을 호출하지 않는다 (Valkey Lua 자체 검증)'`
          - `vi.spyOn(InfobipClient.prototype as any, 'verifyPin')` 이 존재하지 않아야 함. 또는 `expect(InfobipClient.prototype).not.toHaveProperty('verifyPin')`.

    **D. 삭제 대상 (기존 spec에서 제거):**
    - `mockRedis.get.mockResolvedValueOnce('test-pin-id')` 패턴 — pinId mapping 경로 폐기
    - `vi.spyOn(InfobipClient.prototype, 'verifyPin').mockResolvedValueOnce(...)` 전 블록
    - `attemptsRemaining`, `pinError: 'WRONG_PIN' | 'PIN_EXPIRED' | 'NO_MORE_PIN_ATTEMPTS'` 어서션
    - `INFOBIP_APPLICATION_ID`, `INFOBIP_MESSAGE_ID` 참조 전부

    **E. 주석으로 명시:**
    테스트 파일 상단 JSDoc:
    ```
    /**
     * Phase 10.1: Infobip /sms/3/messages v3 전환
     * - OTP 생성: crypto.randomInt (Math.random 금지, OWASP A02)
     * - 저장: Valkey `sms:otp:{e164}` TTL 180s
     * - Verify: Valkey Lua VERIFY_AND_INCREMENT_LUA atomic script
     * - Infobip verify API 사용 안 함 (자체 구현)
     */
    ```

    **RED 상태 검증:**
    - Plan 04 완료 전까지 sms.service.ts는 여전히 sendPin/verifyPin 경로 — 이 spec 실행 시 fail. 정상.
  </action>
  <acceptance_criteria>
    - `grep -q "sms:otp:" apps/api/src/modules/sms/sms.service.spec.ts`
    - `grep -q "VERIFY_AND_INCREMENT\|VERIFIED\|EXPIRED\|NO_MORE_ATTEMPTS\|WRONG" apps/api/src/modules/sms/sms.service.spec.ts`
    - `grep -q "crypto\\.randomInt\|randomInt" apps/api/src/modules/sms/sms.service.spec.ts`
    - `grep -q "INFOBIP_SENDER" apps/api/src/modules/sms/sms.service.spec.ts`
    - `grep -q "\[Grapit\] 인증번호\|인증번호.*3분" apps/api/src/modules/sms/sms.service.spec.ts`
    - `! grep -q "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID" apps/api/src/modules/sms/sms.service.spec.ts`
    - `! grep -q "verifyPin\|sendPin\|attemptsRemaining\|NO_MORE_PIN_ATTEMPTS\|WRONG_PIN\|PIN_EXPIRED" apps/api/src/modules/sms/sms.service.spec.ts`
    - `grep -q "sms:verified:\|sms:verified" apps/api/src/modules/sms/sms.service.spec.ts`
    - `grep -q "sms:phone:send\|sms:phone:verify" apps/api/src/modules/sms/sms.service.spec.ts` (기존 rate limit 테스트 유지 확인)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "sms:otp:" apps/api/src/modules/sms/sms.service.spec.ts && grep -q "randomInt" apps/api/src/modules/sms/sms.service.spec.ts && grep -q "INFOBIP_SENDER" apps/api/src/modules/sms/sms.service.spec.ts && ! grep -q "verifyPin\|sendPin\|attemptsRemaining\|INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID" apps/api/src/modules/sms/sms.service.spec.ts</automated>
  </verify>
  <done>sms.service.spec.ts가 OTP 자체 생성/저장/Lua verify 시나리오로 재작성, 2FA 레거시 어서션 0건, env 3종</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → 프로덕션 응답 shape | fixture가 실 API 응답과 다르면 GREEN test가 prod에서 fail |
| Test spec ↔ 구현 파일 | RED → GREEN 전환 시 spec이 구현의 계약이 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-02-01 | Tampering | fixture shape이 실 Infobip 응답과 불일치 | mitigate | RESEARCH §1.3 응답 shape을 그대로 따름. DEPLOY-CHECKLIST §8 staging smoke에서 실응답과 diff 검증(Phase 10 관례 승계). |
| T-10.1-02-02 | Information Disclosure | OTP가 테스트 로그에 평문 노출 | accept | 테스트 환경 한정, mockRedis 경유로 실제 Valkey 접근 없음. 운영 로그에는 pinId 대신 OTP 미노출 유지. |
| T-10.1-02-03 | Spoofing | `crypto.randomInt` spy가 결정성 제공하지만 실제 사용 여부 미검증 | mitigate | Test 2 T3에 명시적 `expect(crypto.randomInt).toHaveBeenCalledWith(100000, 1000000)` 어서션 포함. |
</threat_model>

<verification>
- `grep -c "sms/3/messages" apps/api/src/modules/sms/infobip-client.spec.ts` >= 1
- `grep -c "sms:otp:" apps/api/src/modules/sms/sms.service.spec.ts` >= 2
- `find apps/api/src/modules/sms -name "*.spec.ts" -exec grep -l "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID\|sendPin\|verifyPin" {} \;` → 0개 파일
- Plan 03/04 미완 상태이므로 `pnpm --filter @grapit/api test sms -- --run`은 RED. 이 plan 범위에서는 GREEN 전환 시도하지 않는다.
</verification>

<success_criteria>
- 4 파일 전부 Phase 10.1 v3 shape으로 교체
- 레거시 2FA 어서션 0건
- Plan 03, 04가 이 테스트들을 그대로 GREEN으로 전환하는 계약 성립
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-sms-api-v3-rewrite/10.1-02-SUMMARY.md`
</output>
