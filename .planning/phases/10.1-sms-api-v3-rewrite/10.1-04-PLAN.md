---
phase: 10.1
plan: 04
type: execute
wave: 3
depends_on: [10.1-01, 10.1-03]
files_modified:
  - apps/api/src/modules/sms/sms.service.ts
autonomous: true
requirements: [SMS-01, SMS-02, SMS-03, SMS-04]
tags: [sms, otp, valkey, lua, green, tdd]
must_haves:
  truths:
    - "SmsService 생성자가 INFOBIP_API_KEY/BASE_URL/SENDER 3종 env를 검증"
    - "production에서 3종 중 1개라도 누락 → throw (hard-fail)"
    - "NODE_ENV !== 'production' && 3종 중 1개 이상 누락 → isDevMock=true"
    - "dev mock에서 sendVerificationCode → Infobip 호출 skip, verifyCode '000000' → verified=true"
    - "sendVerificationCode: CN(+86) reject → BadRequestException"
    - "sendVerificationCode: 30s resend cooldown via Valkey SET NX 유지"
    - "sendVerificationCode: phone axis send counter Lua atomic INCR (sms:phone:send:{e164}) 유지, 6번째 호출 429"
    - "sendVerificationCode: 6자리 OTP = String(crypto.randomInt(100000, 1000000)), Math.random 사용 금지"
    - "sendVerificationCode: Valkey `sms:otp:{e164}` 저장 (TTL 180_000ms), InfobipClient.sendSms(e164, `[Grapit] 인증번호 ${otp} (3분 이내 입력)`) 호출"
    - "sendVerificationCode: Infobip 5xx/timeout → cooldown key DEL rollback, 4xx → 유지"
    - "verifyCode: phone axis verify counter Lua atomic INCR (sms:phone:verify:{e164}) 유지, 11번째 호출 429"
    - "verifyCode: Valkey Lua VERIFY_AND_INCREMENT_LUA 실행 (KEYS=otp/attempts/verified, ARGV=code/maxAttempts/verifiedTtl)"
    - "verifyCode: Lua 결과 ['VERIFIED', _] → {verified: true}, ['WRONG', _] → {verified: false, message}, ['EXPIRED', 0] → GoneException, ['NO_MORE_ATTEMPTS', 0] → GoneException"
    - "verifyCode는 Infobip API를 호출하지 않음 (자체 Valkey verify)"
    - "응답 shape 불변: SendResult {success, message} / VerifyResult {verified, message?}"
    - "Phase 10의 parseE164, isChinaMainland import 유지"
    - "sms.service.spec.ts 전 케이스 GREEN"
  artifacts:
    - path: "apps/api/src/modules/sms/sms.service.ts"
      provides: "v3 Infobip + 자체 OTP + Valkey Lua verify SmsService"
      min_lines: 160
  key_links:
    - from: "sms.service.ts"
      to: "infobip-client.ts (v3)"
      via: "new InfobipClient(baseUrl, apiKey, sender).sendSms(e164, text)"
      pattern: "client\\.sendSms\\("
    - from: "sms.service.ts"
      to: "node:crypto.randomInt"
      via: "OTP 생성"
      pattern: "randomInt\\(100000"
    - from: "sms.service.ts"
      to: "REDIS_CLIENT"
      via: "@Inject(REDIS_CLIENT) private readonly redis: IORedis + redis.eval + redis.set"
      pattern: "sms:otp:|VERIFY_AND_INCREMENT_LUA|ATOMIC_INCR_LUA"
---

<objective>
SmsService를 Phase 10.1의 새 설계(자체 OTP 생성 + Valkey 저장 + Lua atomic verify)로 재작성하고, Plan 01의 env 계약(3종) 및 Plan 03의 InfobipClient v3 API에 맞춘다. Plan 02의 RED `sms.service.spec.ts`를 GREEN으로 전환한다.

핵심 변경:
1. **env 검증 축소:** 4종 → 3종 (APPLICATION_ID/MESSAGE_ID 제거, SENDER 추가)
2. **InfobipClient 사용:** `client.sendPin(e164)` → `client.sendSms(e164, text)` + text는 서버에서 합성
3. **OTP 자체 생성:** `crypto.randomInt(100000, 1000000)` 6자리 숫자, Valkey `sms:otp:{e164}` TTL 180s
4. **Valkey Lua 자체 verify:** `VERIFY_AND_INCREMENT_LUA` 원자 script — `sms:otp`, `sms:attempts`, `sms:verified` 3 키 다룸. 결과 4종(`VERIFIED | WRONG | EXPIRED | NO_MORE_ATTEMPTS`) 분기
5. **Infobip verifyPin 호출 제거**

유지 (Phase 10에서 승계):
- IP axis @Throttle (controller 레벨 — 본 plan에서 수정 없음)
- phone axis send/verify Lua counter (`ATOMIC_INCR_LUA`) 유지
- 30s resend cooldown `SET NX`
- CN(+86) reject
- dev mock (000000 universal)
- Sentry tag 주입 (provider=infobip, country=KR/unknown, http_status=N)
- cooldown key rollback 정책 (5xx/timeout DEL, 4xx retain)

Purpose:
- v3 API는 PIN 라이프사이클 서비스를 제공하지 않으므로 OTP 생성·저장·매칭을 애플리케이션이 책임진다
- Valkey Lua `VERIFY_AND_INCREMENT_LUA`로 "GET otp + INCR attempts + 비교 + 성공 시 verified 플래그 저장 + 5회 초과 시 삭제"를 원자 실행 → race condition 방지
- 응답 shape 불변(SendResult/VerifyResult)으로 `auth.service.ts` 호출부 무영향

Output: `apps/api/src/modules/sms/sms.service.ts` (~180 LOC) + sms.service.spec.ts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md
@.planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/sms.service.spec.ts
@apps/api/src/modules/sms/infobip-client.ts
@apps/api/src/modules/sms/phone.util.ts
@apps/api/src/modules/sms/sms.module.ts
@apps/api/src/modules/booking/providers/redis.provider.ts
@apps/api/src/modules/auth/auth.service.ts
</context>

<interfaces>
<!-- Plan 01에서 확정된 env 계약: -->
// INFOBIP_API_KEY, INFOBIP_BASE_URL, INFOBIP_SENDER (3종)
// APPLICATION_ID, MESSAGE_ID 없음

<!-- Plan 03에서 구현된 InfobipClient 계약: -->
new InfobipClient(baseUrl, apiKey, sender);
await client.sendSms(e164, text): Promise<SendSmsResult>;
// SendSmsResult { messageId, status, groupId }

<!-- Plan 02 spec이 기대하는 SmsService 동작 (이 plan이 GREEN으로 전환): -->
class SmsService {
  constructor(configService, @Inject(REDIS_CLIENT) redis);
  async sendVerificationCode(phone: string): Promise<{success, message}>;
  async verifyCode(phone: string, code: string): Promise<{verified, message?}>;
}

<!-- Valkey 키 스키마: -->
// sms:otp:{e164}       — 저장된 6자리 OTP, TTL 180_000ms
// sms:attempts:{e164}  — verify 시도 카운터, TTL 900s (Lua 안에서 EXPIRE)
// sms:verified:{e164}  — verify 성공 플래그 ('1'), TTL 600s (회원가입 직후 비번 설정 재확인용)
// sms:resend:{e164}    — 30s cooldown, TTL 30_000ms
// sms:phone:send:{e164}   — phone axis send counter, TTL 3600s
// sms:phone:verify:{e164} — phone axis verify counter, TTL 900s

<!-- Lua script: ATOMIC_INCR_LUA (phone axis counter — Phase 10 재사용): -->
// KEYS[1] = counter key, ARGV[1] = window seconds
// 반환: count (number)

<!-- Lua script: VERIFY_AND_INCREMENT_LUA (신규 — v3 자체 verify): -->
// KEYS[1] = sms:otp:{e164}
// KEYS[2] = sms:attempts:{e164}
// KEYS[3] = sms:verified:{e164}
// ARGV[1] = user-provided code
// ARGV[2] = max attempts (e.g. '5')
// ARGV[3] = verified flag TTL seconds (e.g. '600')
// 반환: [result_string, data_number]
//   ['VERIFIED', attempts]         — 정답. attempts/otp DEL, verified SETEX.
//   ['WRONG', remaining_attempts]  — 오답. attempts만 INCR.
//   ['EXPIRED', 0]                 — otp 만료/없음.
//   ['NO_MORE_ATTEMPTS', 0]        — 시도 초과. attempts/otp DEL.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>10.1-04-T1</id>
  <name>Task 1: SmsService 전면 재작성 — env 3종 + OTP 자체 생성 + Lua atomic verify</name>
  <files>apps/api/src/modules/sms/sms.service.ts</files>
  <behavior>
    Plan 02 sms.service.spec.ts 전 케이스 GREEN 전환. 특히:
    1. constructor: 3종 env 검증, APPLICATION_ID/MESSAGE_ID 참조 0건
    2. sendVerificationCode: 중국 reject, dev mock, 30s cooldown, phone axis send counter, OTP randomInt, Valkey 저장, sendSms 호출 (본문 포맷), 5xx/4xx cooldown rollback 분기
    3. verifyCode: dev mock 000000, phone axis verify counter, Lua VERIFY_AND_INCREMENT 4분기
    4. Infobip verifyPin/sendPin 호출 0건
    5. 응답 shape SendResult/VerifyResult 불변
  </behavior>
  <read_first>
    - apps/api/src/modules/sms/sms.service.spec.ts (Plan 02 결과 — RED 테스트 계약)
    - apps/api/src/modules/sms/sms.service.ts (현재 구현 — 참고, 전면 교체 대상)
    - apps/api/src/modules/sms/infobip-client.ts (Plan 03 결과 — sendSms API)
    - apps/api/src/modules/sms/phone.util.ts (parseE164, isChinaMainland — 재사용)
    - apps/api/src/modules/booking/providers/redis.provider.ts (REDIS_CLIENT 심볼, ioredis 타입)
    - apps/api/src/modules/auth/auth.service.ts (sendVerificationCode / verifyCode 호출부 — shape 영향 확인)
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-RESEARCH.md §2.2 "OTP 생성 & 저장", §2.3 "Verify 로직 (Lua atomic)", §4 "Pitfalls"
    - .planning/phases/10.1-sms-api-v3-rewrite/10.1-CONTEXT.md §"Verify 로직", §"메시지 본문"
  </read_first>
  <action>
    `apps/api/src/modules/sms/sms.service.ts`를 아래 구조로 **완전 재작성**:

    ```typescript
    import {
      Inject, Injectable, BadRequestException, GoneException, HttpException,
      HttpStatus, Logger,
    } from '@nestjs/common';
    import { ConfigService } from '@nestjs/config';
    import * as Sentry from '@sentry/nestjs';
    import { randomInt } from 'node:crypto';
    import type IORedis from 'ioredis';
    import { REDIS_CLIENT } from '../booking/providers/redis.provider.js';
    import { InfobipClient, InfobipApiError } from './infobip-client.js';
    import { parseE164, isChinaMainland } from './phone.util.js';

    // 기존 상수 (유지)
    const RESEND_COOLDOWN_MS = 30_000;
    const SEND_PHONE_LIMIT = 5;
    const SEND_PHONE_WINDOW_SEC = 3600;
    const VERIFY_PHONE_LIMIT = 10;
    const VERIFY_PHONE_WINDOW_SEC = 900;

    // Phase 10.1 신규 상수
    const OTP_TTL_MS = 180_000;              // 3분 — 메시지 본문과 일치
    const OTP_MAX_ATTEMPTS = 5;              // Infobip pinAttempts 대체
    const OTP_ATTEMPTS_WINDOW_SEC = 900;     // attempts 카운터 보관 창 (15분)
    const VERIFIED_FLAG_TTL_SEC = 600;       // 회원가입 직후 재확인 10분

    /**
     * [Phase 10] Lua atomic INCR + conditional EXPIRE for phone axis rate-limit counters.
     * First INCR (result==1) sets TTL. Already-existing keys are incremented only.
     * Prevents zombie keys on process crash.
     * Returns: current count (number).
     */
    const ATOMIC_INCR_LUA = `
    local count = redis.call('INCR', KEYS[1])
    if count == 1 then
      redis.call('EXPIRE', KEYS[1], tonumber(ARGV[1]))
    end
    return count
    `;

    /**
     * [Phase 10.1 NEW] Atomic OTP verify + attempt counter + verified flag.
     * KEYS:
     *   [1] sms:otp:{e164}
     *   [2] sms:attempts:{e164}
     *   [3] sms:verified:{e164}
     * ARGV:
     *   [1] user-provided code (6 digits)
     *   [2] max attempts (e.g. '5')
     *   [3] verified flag TTL seconds (e.g. '600')
     * Returns: [result_string, number]
     *   {'VERIFIED', attempts}        — 정답. otp/attempts DEL, verified SETEX.
     *   {'WRONG', remaining}          — 오답. attempts INCR(+EXPIRE if first).
     *   {'EXPIRED', 0}                — otp 만료/없음.
     *   {'NO_MORE_ATTEMPTS', 0}       — 시도 초과. otp/attempts DEL.
     */
    const VERIFY_AND_INCREMENT_LUA = `
    local stored = redis.call('GET', KEYS[1])
    if stored == false then
      return {'EXPIRED', 0}
    end

    local attempts = redis.call('INCR', KEYS[2])
    if attempts == 1 then
      redis.call('EXPIRE', KEYS[2], 900)
    end

    local max = tonumber(ARGV[2])
    if attempts > max then
      redis.call('DEL', KEYS[1], KEYS[2])
      return {'NO_MORE_ATTEMPTS', 0}
    end

    if stored == ARGV[1] then
      redis.call('DEL', KEYS[1], KEYS[2])
      redis.call('SETEX', KEYS[3], tonumber(ARGV[3]), '1')
      return {'VERIFIED', attempts}
    end

    return {'WRONG', max - attempts}
    `;

    export interface SendResult { success: boolean; message: string }
    export interface VerifyResult { verified: boolean; message?: string }

    @Injectable()
    export class SmsService {
      private readonly logger = new Logger(SmsService.name);
      private readonly client: InfobipClient | null;
      private readonly isDevMock: boolean;

      constructor(
        configService: ConfigService,
        @Inject(REDIS_CLIENT) private readonly redis: IORedis,
      ) {
        // Phase 10.1: env 3종 (APPLICATION_ID/MESSAGE_ID 제거, SENDER 추가)
        const apiKey  = configService.get<string>('INFOBIP_API_KEY')?.trim()  ?? '';
        const baseUrl = configService.get<string>('INFOBIP_BASE_URL')?.trim() ?? '';
        const sender  = configService.get<string>('INFOBIP_SENDER')?.trim()   ?? '';
        const isProduction = process.env['NODE_ENV'] === 'production';

        const missing = [
          !apiKey  && 'INFOBIP_API_KEY',
          !baseUrl && 'INFOBIP_BASE_URL',
          !sender  && 'INFOBIP_SENDER',
        ].filter(Boolean) as string[];

        if (isProduction && missing.length > 0) {
          throw new Error(
            `[sms] ${missing.join(', ')} required in production. Silent dev mock disabled.`,
          );
        }

        this.isDevMock = !isProduction && missing.length > 0;
        this.client = this.isDevMock ? null : new InfobipClient(baseUrl, apiKey, sender);

        if (this.isDevMock) {
          this.logger.warn({ event: 'sms.credential_missing', mode: 'dev_mock' });
        }
      }

      /**
       * [Phase 10] Atomic INCR + conditional EXPIRE via Lua.
       * Returns current count.
       */
      private async atomicIncr(key: string, windowSec: number): Promise<number> {
        return (await this.redis.eval(
          ATOMIC_INCR_LUA, 1, key, windowSec,
        )) as number;
      }

      /**
       * [Phase 10.1] 6자리 숫자 OTP. node:crypto.randomInt 사용 (OWASP A02 — CSPRNG).
       */
      private generateOtp(): string {
        return String(randomInt(100000, 1000000));
      }

      async sendVerificationCode(phone: string): Promise<SendResult> {
        const e164 = parseE164(phone);

        // D-03: 중국 본토 reject
        if (isChinaMainland(e164)) {
          throw new BadRequestException(
            '현재 중국 본토 SMS 인증은 지원되지 않습니다. 다른 국가 번호로 가입해 주세요',
          );
        }

        // Dev mock — cooldown/counter/Infobip 모두 skip
        if (this.isDevMock) {
          this.logger.log({ event: 'sms.sent', mode: 'dev_mock', phone: e164 });
          return { success: true, message: '인증번호가 발송되었습니다' };
        }

        // D-11: 30s resend cooldown via Valkey SET NX
        const cooldownKey = `sms:resend:${e164}`;
        const acquired = await this.redis.set(cooldownKey, '1', 'PX', RESEND_COOLDOWN_MS, 'NX');
        if (acquired === null) {
          const ttl = await this.redis.pttl(cooldownKey);
          this.logger.warn({ event: 'sms.rate_limited', phone: e164, layer: 'resend_cooldown' });
          throw new HttpException(
            { statusCode: 429, message: '잠시 후 다시 시도해주세요', retryAfterMs: Math.max(ttl, 0) },
            HttpStatus.TOO_MANY_REQUESTS,
          );
        }

        // D-06: phone axis send 5/3600s — Lua atomic INCR+EXPIRE
        const sendCount = await this.atomicIncr(
          `sms:phone:send:${e164}`, SEND_PHONE_WINDOW_SEC,
        );
        if (sendCount > SEND_PHONE_LIMIT) {
          this.logger.warn({
            event: 'sms.rate_limited', phone: e164, layer: 'phone_axis_send', count: sendCount,
          });
          throw new HttpException(
            { statusCode: 429, message: '잠시 후 다시 시도해주세요', retryAfterMs: SEND_PHONE_WINDOW_SEC * 1000 },
            HttpStatus.TOO_MANY_REQUESTS,
          );
        }

        // [Phase 10.1] OTP 자체 생성 + Valkey 저장
        const otp = this.generateOtp();
        const text = `[Grapit] 인증번호 ${otp} (3분 이내 입력)`;

        try {
          // OTP 먼저 저장 — SMS 발송 성공 후에야 사용자가 입력 가능하므로 선저장 OK.
          // (실패 시 catch에서 DEL 하지 않음 — TTL 180s 내 자연 만료. 다음 발송에서 덮어쓰기.)
          await this.redis.set(`sms:otp:${e164}`, otp, 'PX', OTP_TTL_MS);

          const res = await this.client!.sendSms(e164, text);
          this.logger.log({
            event: 'sms.sent', phone: e164, messageId: res.messageId, groupId: res.groupId,
          });
          return { success: true, message: '인증번호가 발송되었습니다' };
        } catch (err) {
          // [Phase 10 review] Cooldown rollback 정책
          // 5xx/timeout/network → 사용자가 SMS 미수신인데 30s 차단 방지 → cooldown DEL
          // 4xx(Infobip bad request) → 사용자 실수/abuse 의심 → cooldown 유지
          const shouldRollbackCooldown =
            !(err instanceof InfobipApiError) || err.status >= 500;
          if (shouldRollbackCooldown) {
            await this.redis.del(cooldownKey).catch(() => { /* best effort */ });
          }

          const country = e164.startsWith('+82') ? 'KR' : 'unknown';
          Sentry.withScope((scope) => {
            scope.setTag('provider', 'infobip');
            scope.setTag('country', country);
            if (err instanceof InfobipApiError) {
              scope.setTag('http_status', String(err.status));
            }
            scope.setLevel('error');
            Sentry.captureException(err);
          });
          this.logger.error({ event: 'sms.send_failed', phone: e164, err: (err as Error).message });
          throw new BadRequestException('인증번호 발송에 실패했습니다. 잠시 후 다시 시도해주세요.');
        }
      }

      async verifyCode(phone: string, code: string): Promise<VerifyResult> {
        const e164 = parseE164(phone);

        // Dev mock: 000000 universal
        if (this.isDevMock) {
          if (code === '000000') {
            this.logger.log({ event: 'sms.verified', mode: 'dev_mock', phone: e164 });
            return { verified: true };
          }
          return { verified: false, message: '인증번호가 일치하지 않습니다' };
        }

        // D-07: phone axis verify 10/900s — Lua atomic INCR+EXPIRE
        const verifyCount = await this.atomicIncr(
          `sms:phone:verify:${e164}`, VERIFY_PHONE_WINDOW_SEC,
        );
        if (verifyCount > VERIFY_PHONE_LIMIT) {
          this.logger.warn({
            event: 'sms.rate_limited', phone: e164, layer: 'phone_axis_verify', count: verifyCount,
          });
          throw new HttpException(
            { statusCode: 429, message: '잠시 후 다시 시도해주세요', retryAfterMs: VERIFY_PHONE_WINDOW_SEC * 1000 },
            HttpStatus.TOO_MANY_REQUESTS,
          );
        }

        // [Phase 10.1] Valkey Lua atomic OTP verify
        try {
          const result = (await this.redis.eval(
            VERIFY_AND_INCREMENT_LUA,
            3,
            `sms:otp:${e164}`,
            `sms:attempts:${e164}`,
            `sms:verified:${e164}`,
            code,
            String(OTP_MAX_ATTEMPTS),
            String(VERIFIED_FLAG_TTL_SEC),
          )) as [string, number];

          const [status, data] = result;
          switch (status) {
            case 'VERIFIED':
              this.logger.log({ event: 'sms.verified', phone: e164, attempts: data });
              return { verified: true };
            case 'WRONG':
              this.logger.warn({ event: 'sms.verify_wrong', phone: e164, remaining: data });
              return { verified: false, message: '인증번호가 일치하지 않습니다' };
            case 'EXPIRED':
              throw new GoneException('인증번호가 만료되었습니다. 재발송해주세요');
            case 'NO_MORE_ATTEMPTS':
              this.logger.warn({ event: 'sms.verify_exhausted', phone: e164 });
              throw new GoneException('인증번호가 만료되었습니다. 재발송해주세요');
            default: {
              // Unreachable — Lua script returns one of the above.
              const exhaustive: never = status as never;
              throw new Error(`Unknown VERIFY_AND_INCREMENT result: ${exhaustive}`);
            }
          }
        } catch (err) {
          if (err instanceof GoneException) throw err;
          // Valkey eval 실패 등 unexpected — log + 500 범주로 전파, 사용자 카피는 uniform.
          Sentry.withScope((scope) => {
            scope.setTag('provider', 'valkey');
            scope.setLevel('error');
            Sentry.captureException(err);
          });
          this.logger.error({ event: 'sms.verify_failed', phone: e164, err: (err as Error).message });
          return { verified: false, message: '인증번호 확인에 실패했습니다. 잠시 후 다시 시도해주세요.' };
        }
      }
    }
    ```

    **제거 대상 (기존 코드에서 완전 삭제):**
    - `PIN_MAPPING_TTL_MS` 상수 (대체: `OTP_TTL_MS`)
    - `configService.get('INFOBIP_APPLICATION_ID')`, `...'INFOBIP_MESSAGE_ID')` 라인
    - `new InfobipClient(baseUrl, apiKey, applicationId, messageId)` → 3 arg 생성자
    - `this.client!.sendPin(e164)` / `this.client!.verifyPin(pinId, code)` 호출부
    - `res.pinId`, `res.attemptsRemaining`, `res.pinError` 참조
    - `await this.redis.set('sms:pin:${e164}', res.pinId, ...)` 패턴
    - `await this.redis.get('sms:pin:${e164}')` 패턴

    **설계 주의사항:**

    1. **OTP 선저장 vs 후저장**
       위 구현은 "redis.set(otp) → sendSms" 순서. sendSms 실패 시 OTP가 Valkey에 남지만 TTL 180s 내 자연 만료 + 사용자가 수신 못하면 사용 불가 → 보안 OK. 장점: Infobip 성공인데 race로 Valkey 쓰기 실패 시 사용자가 문자 받아도 verify 불가한 상황 회피.

    2. **`this.redis.set` NX 타입 시그니처**
       `ioredis`의 `set(key, value, 'PX', ms, 'NX')` 시그니처는 null | 'OK' 반환. 기존 Phase 10 코드와 동일 유지.

    3. **`this.redis.eval` 반환 타입**
       ioredis는 `unknown` 반환 → `as [string, number]` narrow cast. Lua는 항상 `{result_string, number}` 쌍을 반환하므로 안전.

    4. **dev mock verify에서 phone axis counter 미증가**
       기존 Phase 10과 동일. dev에서 테스트 피드백 루프 보호.

    5. **Lua script에서 attempts EXPIRE 900**
       ARGV로 받지 않고 Lua 상수로 고정. 15분 창 내 5회 시도 → 정책 간단화.

    6. **Infobip 5xx/timeout 분기에서 OTP 키 처리**
       cooldown은 rollback하지만 `sms:otp:{e164}`는 DEL 안 함. 이유:
         - TTL 180s 자연 만료
         - 사용자 재시도 시 새 OTP로 덮어쓰기 (`set` 동일 키에 overwrite)
         - DEL 시 race condition으로 유효 otp를 지울 위험

    7. **typecheck/lint**
       - `pnpm --filter @grapit/api typecheck` 통과 (controller/module 등 SmsService 의존부에 영향 없음: 응답 shape 불변)
       - `pnpm --filter @grapit/api lint apps/api/src/modules/sms/sms.service.ts` 통과
       - 사용하지 않는 import 제거 (PIN_MAPPING_TTL_MS 등)

    8. **sms.module.ts — 수정 없음**
       `BookingModule` import + SmsService provider 패턴은 Phase 10 그대로 유지.

    **작성 후 검증:**
    ```bash
    pnpm --filter @grapit/api test sms.service -- --run
    pnpm --filter @grapit/api typecheck
    ```
    전 테스트 GREEN + typecheck 통과.
  </action>
  <acceptance_criteria>
    - `grep -q "INFOBIP_SENDER" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "VERIFY_AND_INCREMENT_LUA" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "randomInt\\(100000, 1000000\\)" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "sms:otp:" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "sms:verified:" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "sms:attempts:" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "sendSms(" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "\\[Grapit\\] 인증번호" apps/api/src/modules/sms/sms.service.ts`
    - `grep -q "ATOMIC_INCR_LUA" apps/api/src/modules/sms/sms.service.ts` (Phase 10 phone axis counter 유지)
    - `! grep -q "INFOBIP_APPLICATION_ID\|INFOBIP_MESSAGE_ID\|applicationId\|messageId" apps/api/src/modules/sms/sms.service.ts`
    - `! grep -q "sendPin\|verifyPin\|pinId\|attemptsRemaining\|PIN_MAPPING_TTL_MS\|sms:pin:" apps/api/src/modules/sms/sms.service.ts`
    - `! grep -q "Math\\.random" apps/api/src/modules/sms/sms.service.ts`
    - `pnpm --filter @grapit/api test sms.service -- --run` exits 0
    - `pnpm --filter @grapit/api typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/api test sms.service -- --run 2>&1 | tail -30</automated>
  </verify>
  <done>SmsService v3 재작성 완료, env 3종/자체 OTP/Lua verify 동작, spec 전 케이스 GREEN, 2FA 레거시 0건</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → SmsService | untrusted phone/code 입력 |
| SmsService → InfobipClient → Infobip API | HTTPS 외부 호출, 본문에 OTP 노출 (SMS 발송 필요) |
| SmsService → Valkey | OTP, attempts, verified, cooldown, counter 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-04-01 | Tampering | OTP brute-force (verify) | mitigate | Valkey Lua `VERIFY_AND_INCREMENT_LUA` — attempts 5회 시 otp DEL + GoneException. phone axis 10/15min + IP axis 10/15min (controller @Throttle) 중첩. 총 진입점 3layer. |
| T-10.1-04-02 | Spoofing | 예측 가능한 OTP (Math.random) | mitigate | `node:crypto.randomInt(100000, 1000000)` CSPRNG 사용. Plan 02 spec에 명시적 `toHaveBeenCalledWith` 어서션. |
| T-10.1-04-03 | Information Disclosure | OTP가 로그/Sentry에 유출 | mitigate | 로그는 `messageId`, `groupId`만 기록. OTP 값은 로그에 미포함. Sentry scope에도 미추가. |
| T-10.1-04-04 | Denial of Service | Infobip 장애 시 cooldown deadlock | mitigate | 5xx/timeout → cooldown key DEL rollback (Phase 10 review #1 계승) |
| T-10.1-04-05 | Tampering | Valkey Lua race condition (WRONG/VERIFIED/EXPIRED 동시 진입) | mitigate | `VERIFY_AND_INCREMENT_LUA` 내에서 GET + INCR + 비교 + DEL/SETEX 전부 원자 실행. EVAL은 single-thread. |
| T-10.1-04-06 | Tampering | SMS pumping (동일 번호 반복 send) | mitigate | 30s cooldown NX + phone 5/1h Lua + IP 20/1h @Throttle 중첩. Infobip 계정 `sendPinPerPhoneNumberLimit` 제거(v3 불필요)에 따른 추가 보상으로 Valkey 카운터로 충분. |
| T-10.1-04-07 | Spoofing | production 진입 시 dev mock 실수 활성화 | mitigate | NODE_ENV=production + env 누락 → throw. test/dev 분기 로직 그대로 유지. |
| T-10.1-04-08 | Tampering | SmsService → BookingModule 순환 의존 | accept | Phase 10에서 이미 검증 완료 (SmsModule imports [BookingModule], BookingModule은 SmsModule 미참조). 본 plan은 module 구조 미변경. |
</threat_model>

<verification>
- `pnpm --filter @grapit/api test sms -- --run` green (sms.service + sms.controller + infobip-client + phone.util 전부)
- `pnpm --filter @grapit/api typecheck` green
- `pnpm --filter @grapit/api lint apps/api/src/modules/sms/sms.service.ts` green
- grep으로 레거시 잔존 확인: `INFOBIP_APPLICATION_ID|INFOBIP_MESSAGE_ID|sendPin|verifyPin|pinId|attemptsRemaining|sms:pin:` 0건
</verification>

<success_criteria>
- 6자리 OTP + Valkey TTL 180s + 자체 verify로 Phase 10 2FA API 의존 완전 제거
- Plan 02 spec 전 케이스 GREEN
- auth.service.ts 호출부 무영향 (응답 shape 불변: SendResult/VerifyResult)
- Sentry/logger 주입 포인트 Phase 10과 동등
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-sms-api-v3-rewrite/10.1-04-SUMMARY.md`
</output>
