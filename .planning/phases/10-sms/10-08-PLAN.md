---
phase: 10
plan: 08
type: execute
wave: 4
depends_on: [10-05, 10-06]
files_modified:
  - apps/web/components/auth/phone-verification.tsx
  - apps/web/lib/phone.ts
  - packages/shared/src/constants/index.ts
autonomous: true
requirements: [SMS-01, SMS-02, SMS-04]
must_haves:
  truths:
    - "phone-verification.tsx가 30s 재발송 쿨다운 독립 타이머 보유"
    - "재발송 버튼 4-상태 정확 전환 (initial/sending/cooldown/resend-ready)"
    - "에러 카피 HTTP 상태별 분기 (429/410/422/400/5xx)"
    - "space-y-3 → space-y-4, text-sm → text-caption 교정"
    - "국제 번호 raw passthrough + 국가 감지 안내"
    - "autoComplete='one-time-code' + role='alert' + aria-label"
    - "phone-verification.test.tsx GREEN 전환"
  artifacts:
    - path: "apps/web/components/auth/phone-verification.tsx"
      provides: "4-state 버튼 + 30s 쿨다운 + HTTP 에러 매핑 + 국제 번호"
      contains: "resendCooldown"
    - path: "apps/web/lib/phone.ts"
      provides: "detectPhoneLocale 유틸"
      exports: ["detectPhoneLocale"]
    - path: "packages/shared/src/constants/index.ts"
      provides: "SMS_RESEND_COOLDOWN_SECONDS=30"
      contains: "SMS_RESEND_COOLDOWN_SECONDS"
  key_links:
    - from: "phone-verification.tsx"
      to: "ApiClientError.statusCode"
      via: "HTTP status 기반 에러 매핑"
      pattern: "statusCode"
---

<objective>
`phone-verification.tsx`를 UI-SPEC.md 계약대로 재작성. D-18~D-20 실현. 변경 범위는 단일 컴포넌트 + 공유 상수 + 유틸 1개.

Output: phone-verification.tsx 재작성 + lib/phone.ts 신규 + 공유 상수 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sms/10-CONTEXT.md
@.planning/phases/10-sms/10-UI-SPEC.md
@apps/web/components/auth/phone-verification.tsx
@apps/web/lib/api-client.ts
@apps/web/components/auth/__tests__/phone-verification.test.tsx
@packages/shared/src/constants/index.ts
</context>

<tasks>

<task type="auto">
  <id>10-08-T1</id>
  <name>Task 1: 공유 상수 + detectPhoneLocale 유틸</name>
  <files>packages/shared/src/constants/index.ts, apps/web/lib/phone.ts</files>
  <read_first>
    - packages/shared/src/constants/index.ts (기존 SMS_CODE_EXPIRY_SECONDS)
    - .planning/phases/10-sms/10-UI-SPEC.md §"국가 감지 안내"
    - apps/web/package.json (libphonenumber-js 설치 확인)
  </read_first>
  <action>
    **A. `packages/shared/src/constants/index.ts`** — 기존 SMS 상수 옆에 추가:
    ```typescript
    export const SMS_RESEND_COOLDOWN_SECONDS = 30;  // D-11: 서버 sms:resend:{e164} PX 30000과 동일
    ```

    **B. `apps/web/lib/phone.ts`** (신규):
    ```typescript
    import { parsePhoneNumberFromString } from 'libphonenumber-js/min';

    export interface PhoneLocale {
      isKorean: boolean;
      country: string | null;
      countryName: string | null;
      e164: string | null;
    }

    const COUNTRY_NAME_KO: Record<string, string> = {
      KR: '한국', TH: '태국', JP: '일본', US: '미국', VN: '베트남',
      HK: '홍콩', TW: '대만', SG: '싱가포르', PH: '필리핀',
      ID: '인도네시아', MY: '말레이시아', CN: '중국',
    };

    export function detectPhoneLocale(input: string): PhoneLocale {
      const cleaned = input.replace(/\s+/g, '');
      if (/^01[016789]\d{7,8}$/.test(cleaned.replace(/\D/g, ''))) {
        return { isKorean: true, country: 'KR', countryName: '한국', e164: null };
      }
      const withPlus = cleaned.startsWith('+') ? cleaned : (cleaned.length > 0 ? `+${cleaned}` : cleaned);
      const parsed = parsePhoneNumberFromString(withPlus);
      if (parsed?.isValid()) {
        const country = parsed.country ?? null;
        return {
          isKorean: country === 'KR',
          country,
          countryName: country ? (COUNTRY_NAME_KO[country] ?? null) : null,
          e164: parsed.format('E.164'),
        };
      }
      return { isKorean: false, country: null, countryName: null, e164: null };
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -q "SMS_RESEND_COOLDOWN_SECONDS = 30" packages/shared/src/constants/index.ts`
    - `test -f apps/web/lib/phone.ts`
    - `grep -q "export function detectPhoneLocale" apps/web/lib/phone.ts`
    - `pnpm --filter @grapit/web typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SMS_RESEND_COOLDOWN_SECONDS" packages/shared/src/constants/index.ts && test -f apps/web/lib/phone.ts && pnpm --filter @grapit/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>공유 상수 + phone.ts 유틸 작성, typecheck green</done>
</task>

<task type="auto" tdd="true">
  <id>10-08-T2</id>
  <name>Task 2: phone-verification.tsx 재작성 — 4-state + 30s 쿨다운 + 에러 카피 + 국제 번호</name>
  <files>apps/web/components/auth/phone-verification.tsx</files>
  <behavior>
    - 30초 쿨다운 독립 타이머 (resendCooldown state)
    - 발송 성공 시 timeLeft=180 + resendCooldown=30 동시 시작
    - 버튼 4-state: initial/sending/cooldown/resend-ready
    - mapErrorToCopy: 429→"잠시 후 다시 시도해주세요", 410/422→"인증번호가 만료되었습니다. 재발송해주세요", 400 CN→서버 메시지 전달, 400 invalid→"인증번호가 일치하지 않습니다", 5xx→"일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요."
    - space-y-3 → space-y-4, text-sm → text-caption (만료 타이머)
    - 국제 번호 raw passthrough + 감지 안내
    - autoComplete="one-time-code", autoComplete="tel"
    - aria-label(쿨다운), role="alert"(에러), role="status"(완료)
    - size="lg" 버튼
    - props 인터페이스 불변
    - 시도 횟수 노출 없음 (D-19)
  </behavior>
  <read_first>
    - apps/web/components/auth/phone-verification.tsx (현재 227줄)
    - .planning/phases/10-sms/10-UI-SPEC.md §"Copywriting Contract" 전체
    - .planning/phases/10-sms/10-CONTEXT.md D-18, D-19, D-20
    - apps/web/lib/api-client.ts (ApiClientError.statusCode 필드)
    - apps/web/components/auth/__tests__/phone-verification.test.tsx (Plan 01 RED)
    - apps/web/lib/phone.ts (Task 1에서 생성)
    - packages/shared/src/constants/index.ts (SMS_RESEND_COOLDOWN_SECONDS)
  </read_first>
  <action>
    **전체 변경 요약:** 기존 컴포넌트에 (a) resendCooldown state + useEffect, (b) mapErrorToCopy 헬퍼, (c) 4-state 버튼 분기, (d) detectPhoneLocale 호출 국가 안내, (e) 토큰/속성 교정 추가. props 인터페이스 불변.

    **1. Imports 추가:**
    ```typescript
    import { SMS_CODE_EXPIRY_SECONDS, SMS_RESEND_COOLDOWN_SECONDS } from '@grapit/shared';
    import { ApiClientError } from '@/lib/api-client';
    import { detectPhoneLocale } from '@/lib/phone';
    ```

    **2. state/ref 추가:**
    ```typescript
    const [resendCooldown, setResendCooldown] = useState(0);
    const cooldownTimerRef = useRef<ReturnType<typeof setInterval> | null>(null);
    ```

    **3. 쿨다운 useEffect (독립):**
    ```typescript
    useEffect(() => {
      if (resendCooldown <= 0) {
        if (cooldownTimerRef.current) { clearInterval(cooldownTimerRef.current); cooldownTimerRef.current = null; }
        return;
      }
      cooldownTimerRef.current = setInterval(() => {
        setResendCooldown((prev) => (prev <= 1 ? 0 : prev - 1));
      }, 1000);
      return () => { if (cooldownTimerRef.current) clearInterval(cooldownTimerRef.current); };
    }, [resendCooldown]);
    ```

    **4. mapErrorToCopy 헬퍼:**
    ```typescript
    function mapErrorToCopy(err: unknown): string {
      if (err instanceof ApiClientError) {
        if (err.statusCode === 429) return '잠시 후 다시 시도해주세요';
        if (err.statusCode === 410 || err.statusCode === 422) return '인증번호가 만료되었습니다. 재발송해주세요';
        if (err.statusCode === 400) {
          if (err.message.includes('중국 본토')) return err.message;
          return '인증번호가 일치하지 않습니다';
        }
        if (err.statusCode >= 500) return '일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요.';
      }
      return '일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요.';
    }
    ```

    **5. handleSendCode 성공 시:**
    ```typescript
    setTimeLeft(SMS_CODE_EXPIRY_SECONDS);
    setResendCooldown(SMS_RESEND_COOLDOWN_SECONDS);
    ```

    **6. handleVerifyCode에서 410/422 응답이면 `setTimeLeft(0)` 강제 (expired state 진입).**

    **7. 버튼 4-state:**
    ```tsx
    <Button variant={codeSent ? 'outline' : 'default'} size="lg"
      disabled={isVerified || isSending || (codeSent && resendCooldown > 0) || (!codeSent && phone.length < 10 && !locale.country)}
      aria-label={codeSent && resendCooldown > 0 ? `재발송 대기 중, ${resendCooldown}초 남음` : undefined}>
      {isSending ? <><Loader2 className="h-4 w-4 animate-spin" /> 발송 중...</>
       : codeSent && resendCooldown > 0 ? `재발송 (${resendCooldown}s)`
       : codeSent ? '재발송' : '인증번호 발송'}
    </Button>
    ```

    **8. 국제 번호 passthrough:**
    ```typescript
    const locale = detectPhoneLocale(phone);
    // 한국: formatPhoneInput 마스킹, 국제: raw passthrough
    ```

    **9. 국가 감지 안내 (KR/CN 제외):**
    ```tsx
    {showCountryHint && locale.countryName && (
      <span className="text-caption text-gray-500" aria-live="polite">
        {locale.countryName} 번호로 SMS를 발송합니다
      </span>
    )}
    ```

    **10. 토큰 교정:** space-y-3→space-y-4, text-sm→text-caption(만료 타이머), size="lg" 양쪽 버튼.

    **11. a11y:** autoComplete="one-time-code"(인증번호), autoComplete="tel"(전화번호), role="alert"(에러), role="status"(완료).

    **금지:** 시도 횟수 노출(D-19), props 변경, 다른 컴포넌트 수정, 신규 애니메이션.
  </action>
  <acceptance_criteria>
    - `grep -q "resendCooldown" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "mapErrorToCopy" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "one-time-code" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "space-y-4" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "size=\"lg\"" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "잠시 후 다시 시도해주세요" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "인증번호가 만료되었습니다" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "role=\"alert\"" apps/web/components/auth/phone-verification.tsx`
    - `grep -q "재발송 대기 중" apps/web/components/auth/phone-verification.tsx`
    - `pnpm --filter @grapit/web test -- --run phone-verification` GREEN
    - `pnpm --filter @grapit/web typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/web test -- --run phone-verification 2>&1 | tail -15</automated>
  </verify>
  <done>UI-SPEC 계약 100% 구현, phone-verification.test.tsx GREEN, props 불변, D-18/D-19/D-20 반영</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | fetch credentials: 'include' |
| server error → UI copy | HTTP status 기반 매핑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Information Disclosure | 시도 횟수 노출 | mitigate | D-19: UI에 절대 미노출 |
| T-10-08-02 | Tampering | 30s 쿨다운 프론트 우회 | mitigate | 서버 Valkey SET NX가 최종 판정 |
</threat_model>

<verification>
- `pnpm --filter @grapit/web typecheck` green
- `pnpm --filter @grapit/web test -- --run phone-verification` green
</verification>

<success_criteria>
- phone-verification.tsx UI-SPEC 계약 100%
- 4-state 버튼 + 30s 쿨다운 + 에러 카피 5종 + 국제 번호
- props 불변, 호출부 무영향
</success_criteria>

<output>
Create `.planning/phases/10-sms/10-08-SUMMARY.md`
</output>
