---
phase: 10
plan: 03
type: execute
wave: 1
depends_on: [10-01, 10-02]
files_modified:
  - apps/api/src/modules/sms/phone.util.ts
autonomous: true
requirements: [SMS-02]
must_haves:
  truths:
    - "parseE164()가 한국 로컬 포맷과 E.164 국제 포맷을 모두 +82 E.164로 정규화"
    - "isChinaMainland()가 +86 번호만 true 반환(+852 홍콩, +853 마카오, +886 대만 false)"
    - "잘못된 번호는 '올바른 휴대폰 번호를 입력해주세요' 에러 throw"
    - "0086 prefix, full-width digit, 공백 포함 번호 등 CN edge case 정확 처리"
    - "phone.util.spec.ts 모든 테스트 green"
  artifacts:
    - path: "apps/api/src/modules/sms/phone.util.ts"
      provides: "parseE164, isChinaMainland — libphonenumber-js/min 기반"
      exports: ["parseE164", "isChinaMainland"]
      min_lines: 25
  key_links:
    - from: "apps/api/src/modules/sms/phone.util.ts"
      to: "libphonenumber-js/min"
      via: "import { parsePhoneNumberWithError } from 'libphonenumber-js/min'"
      pattern: "libphonenumber-js/min"
---

<objective>
백엔드 전화번호 정규화 + 중국 본토 감지 유틸을 작성한다. Plan 01의 `phone.util.spec.ts`를 RED에서 GREEN으로 전환.

[Review #8 반영] 0086 prefix, full-width +86, 공백 포함 번호, +852(홍콩), +853(마카오), +886(대만) 등 CN normalization edge case 테스트를 통과하도록 구현. `parseE164`에서 입력 정리 시 full-width 문자(U+FF0B, U+FF10~U+FF19)를 ASCII로 변환하는 전처리 추가.

Output: `apps/api/src/modules/sms/phone.util.ts` + phone.util.spec.ts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sms/10-CONTEXT.md
@.planning/phases/10-sms/10-RESEARCH.md
@apps/api/src/modules/sms/phone.util.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <id>10-03-T1</id>
  <name>Task 1: phone.util.ts 작성 (parseE164 + isChinaMainland)</name>
  <files>apps/api/src/modules/sms/phone.util.ts</files>
  <behavior>
    - parseE164('010-1234-5678') === '+821012345678'
    - parseE164('01012345678') === '+821012345678'
    - parseE164('+821012345678') === '+821012345678'
    - parseE164('+66812345678') === '+66812345678'
    - parseE164('+8613912345678') === '+8613912345678'
    - parseE164('invalid') throws Error with '올바른 휴대폰 번호를 입력해주세요'
    - isChinaMainland('+8613912345678') === true
    - isChinaMainland('+821012345678') === false
    - isChinaMainland('+85212345678') === false (홍콩)
    - isChinaMainland('+85312345678') === false (마카오)
    - isChinaMainland('+886912345678') === false (대만)
    - [Review #8] parseE164('008613912345678') → '+8613912345678' (0086 prefix)
    - [Review #8] full-width digit 입력 → ASCII로 변환 후 정상 파싱
    - [Review #8] '+ 86 139 1234 5678' (공백 포함) → '+8613912345678'
  </behavior>
  <read_first>
    - apps/api/src/modules/sms/phone.util.spec.ts (Plan 01 RED 테스트)
    - .planning/phases/10-sms/10-RESEARCH.md §"Code Examples > Phone util"
    - .planning/phases/10-sms/10-REVIEWS.md #8 (CN normalization edge cases)
  </read_first>
  <action>
    `apps/api/src/modules/sms/phone.util.ts` 생성:

    ```typescript
    import { parsePhoneNumberWithError, ParseError } from 'libphonenumber-js/min';

    /**
     * Full-width ASCII 변환 (U+FF0B→+, U+FF10~U+FF19→0~9)
     * [Review #8] 중국 번호 입력 시 full-width digit 사용 가능성 대응
     */
    function normalizeFullWidth(input: string): string {
      return input
        .replace(/\uFF0B/g, '+')
        .replace(/[\uFF10-\uFF19]/g, (ch) =>
          String.fromCharCode(ch.charCodeAt(0) - 0xFEE0),
        );
    }

    export function parseE164(input: string): string {
      // 1. full-width → ASCII 변환
      const normalized = normalizeFullWidth(input);
      // 2. 공백/하이픈/괄호 제거
      const cleaned = normalized.replace(/[\s\-()]/g, '');
      // 3. 0086 prefix → +86 변환 [Review #8]
      const withPlus = cleaned.startsWith('00')
        ? `+${cleaned.slice(2)}`
        : cleaned;

      // 4. 한국 로컬 포맷 fast path (010-1234-5678 or 01012345678)
      const digits = withPlus.replace(/[^+\d]/g, '');
      if (/^01[016789]\d{7,8}$/.test(digits)) {
        return `+82${digits.slice(1)}`;
      }

      // 5. E.164 파싱
      try {
        const toParse = digits.startsWith('+') ? digits : `+${digits}`;
        const parsed = parsePhoneNumberWithError(toParse);
        return parsed.number; // E.164 string
      } catch (err) {
        if (err instanceof ParseError) {
          throw new Error('올바른 휴대폰 번호를 입력해주세요');
        }
        throw err;
      }
    }

    export function isChinaMainland(e164: string): boolean {
      try {
        const parsed = parsePhoneNumberWithError(e164);
        return parsed.country === 'CN';
      } catch {
        return false;
      }
    }
    ```

    핵심:
    - `libphonenumber-js/min` sub-entry만 사용 (~80KB metadata)
    - 한국 로컬 포맷 early return으로 libphonenumber 호출 우회 (빈번 케이스 fast path)
    - `normalizeFullWidth`로 U+FF0B(+) + U+FF10~U+FF19(0~9) 변환 [Review #8]
    - `00` prefix → `+` 변환으로 `0086` 입력 커버 [Review #8]
    - TypeScript strict: `any` 금지, `parsed.number`는 `string` 추론

    작성 후 `pnpm --filter @grapit/api test phone.util -- --run` 실행해서 GREEN 확인.
  </action>
  <acceptance_criteria>
    - `apps/api/src/modules/sms/phone.util.ts` 파일 존재
    - `grep -q "from 'libphonenumber-js/min'" apps/api/src/modules/sms/phone.util.ts`
    - `grep -q "export function parseE164" apps/api/src/modules/sms/phone.util.ts`
    - `grep -q "export function isChinaMainland" apps/api/src/modules/sms/phone.util.ts`
    - `grep -q "normalizeFullWidth\|\\\\uFF0B" apps/api/src/modules/sms/phone.util.ts` (Review #8)
    - `grep -q "slice(2)\|00.*\\+" apps/api/src/modules/sms/phone.util.ts` (0086 처리)
    - `grep -q "'CN'" apps/api/src/modules/sms/phone.util.ts`
    - `pnpm --filter @grapit/api test phone.util -- --run` 전부 통과
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/api test phone.util -- --run 2>&1 | tail -15</automated>
  </verify>
  <done>phone.util.ts 2 export, phone.util.spec.ts GREEN, CN edge cases 전부 통과, strict TS 준수</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 phone 입력 → 백엔드 정규화 | 악의적 입력(+86, 0086, full-width)을 정확히 판정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Spoofing | phone country detection | mitigate | libphonenumber-js `parsed.country === 'CN'`로 prefix 매칭 취약점 회피. 0086/full-width/공백 모두 정규화 후 판정 [Review #8] |
| T-10-11 | Tampering | parseE164 에러 처리 | accept | ParseError catch 후 한국어 메시지 throw |
</threat_model>

<verification>
- `pnpm --filter @grapit/api test phone.util -- --run` 전부 green
- `pnpm --filter @grapit/api lint -- apps/api/src/modules/sms/phone.util.ts` exits 0
</verification>

<success_criteria>
- phone.util.ts 2 export(parseE164, isChinaMainland)
- phone.util.spec.ts GREEN
- Review #8 CN edge cases(0086, full-width, 공백, 홍콩/마카오/대만 비차단) 전부 통과
</success_criteria>

<output>
Create `.planning/phases/10-sms/10-03-SUMMARY.md`
</output>
