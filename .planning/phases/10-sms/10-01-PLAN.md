---
phase: 10
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/modules/sms/sms.service.spec.ts
  - apps/api/src/modules/sms/infobip-client.spec.ts
  - apps/api/src/modules/sms/phone.util.spec.ts
  - apps/api/test/sms-throttle.integration.spec.ts
  - apps/web/components/auth/__tests__/phone-verification.test.tsx
  - apps/web/e2e/signup-sms.spec.ts
  - apps/api/src/modules/sms/__fixtures__/infobip-send-response.json
  - apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json
  - .planning/phases/10-sms/10-VALIDATION.md
autonomous: true
requirements: [SMS-01, SMS-02, SMS-03, SMS-04]
must_haves:
  truths:
    - "Wave 0 테스트 스캐폴딩이 RED 상태로 작성되어 후속 wave가 GREEN으로 전환 가능"
    - "SMS 서비스·Infobip 클라이언트·phone util·throttle integration·프론트 컴포넌트·E2E 전 영역에 실패 테스트 존재"
    - "Infobip API 응답 fixture 2종이 zod schema lock의 근거로 저장"
    - "중국 번호 차단 테스트에 0086, +86, full-width digit, +852(홍콩 비차단) 엣지 케이스 포함"
    - "sms.service.spec.ts에 Infobip 발송 실패 시 cooldown key rollback 테스트 포함"
    - "sms.service.spec.ts에 phone axis counter Lua 원자성 테스트 포함"
  artifacts:
    - path: "apps/api/src/modules/sms/sms.service.spec.ts"
      provides: "SmsService Infobip 기반 재작성 테스트 (Twilio mock 제거)"
    - path: "apps/api/src/modules/sms/infobip-client.spec.ts"
      provides: "InfobipClient fetch mock 단위 테스트"
    - path: "apps/api/src/modules/sms/phone.util.spec.ts"
      provides: "parseE164 + isChinaMainland 단위 테스트"
    - path: "apps/api/test/sms-throttle.integration.spec.ts"
      provides: "testcontainers Valkey + throttler integration 테스트"
    - path: "apps/web/components/auth/__tests__/phone-verification.test.tsx"
      provides: "phone-verification 컴포넌트 4-state/쿨다운/에러 카피 테스트"
    - path: "apps/web/e2e/signup-sms.spec.ts"
      provides: "signup SMS mock 모드 E2E flow"
    - path: "apps/api/src/modules/sms/__fixtures__/infobip-send-response.json"
      provides: "sendPin 성공 응답 fixture"
    - path: "apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json"
      provides: "verifyPin 성공·실패 응답 fixture"
  key_links:
    - from: "sms.service.spec.ts"
      to: "sms.service.ts"
      via: "Plan 05에서 GREEN 전환"
      pattern: "InfobipClient|REDIS_CLIENT"
    - from: "infobip-client.spec.ts"
      to: "infobip-client.ts"
      via: "Plan 04에서 GREEN 전환"
      pattern: "App \\${apiKey}"
    - from: "sms-throttle.integration.spec.ts"
      to: "ThrottlerStorageRedisService"
      via: "Plan 07+09에서 GREEN 전환"
      pattern: "testcontainers"
---

<objective>
Phase 10의 모든 테스트를 RED 상태로 작성한다. 7개 테스트/fixture 파일 + VALIDATION.md 업데이트. 후속 wave(Plan 03~09)가 GREEN으로 전환하는 계약을 고정.

Purpose: Nyquist 검증 전략의 Wave 0 요구사항. 모든 behavior를 테스트로 선언한 뒤 구현을 시작한다. 리뷰 피드백(Concern #1 cooldown rollback, #3 Valkey atomicity, #8 CN edge cases) 반영 — 추가 테스트 케이스 포함.

Output: 7개 파일 + VALIDATION.md 업데이트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sms/10-CONTEXT.md
@.planning/phases/10-sms/10-RESEARCH.md
@.planning/phases/10-sms/10-UI-SPEC.md
@.planning/phases/10-sms/10-VALIDATION.md
@.planning/phases/10-sms/10-REVIEWS.md
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/sms.controller.ts
</context>

<tasks>

<task type="auto">
  <id>10-01-T1</id>
  <name>Task 1: 백엔드 테스트 스캐폴딩 6파일 + fixture 2종 (RED)</name>
  <files>
    apps/api/src/modules/sms/sms.service.spec.ts,
    apps/api/src/modules/sms/infobip-client.spec.ts,
    apps/api/src/modules/sms/phone.util.spec.ts,
    apps/api/test/sms-throttle.integration.spec.ts,
    apps/api/src/modules/sms/__fixtures__/infobip-send-response.json,
    apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json
  </files>
  <read_first>
    - apps/api/src/modules/sms/sms.service.ts (현재 Twilio 구현 — 교체 대상의 시그니처 이해)
    - apps/api/src/modules/sms/sms.controller.ts (zod schema 현재 한국 전용 — 국제 확장 예정)
    - .planning/phases/10-sms/10-RESEARCH.md §"Code Examples" (전체)
    - .planning/phases/10-sms/10-RESEARCH.md §"Pattern 3: Infobip fetch wrapper"
    - .planning/phases/10-sms/10-RESEARCH.md §"Pattern 4: Valkey Key Schema"
    - .planning/phases/10-sms/10-CONTEXT.md D-03, D-06, D-07, D-11, D-12, D-14, D-15, D-16
    - .planning/phases/10-sms/10-REVIEWS.md §"Concerns" #1(cooldown rollback), #3(Valkey atomicity), #8(CN edge cases), #2(max attempts)
  </read_first>
  <action>
    7개 파일 작성. 모두 RED 상태(import 대상 파일이 아직 없거나 미완성이므로 실행 시 fail).

    **A. `apps/api/src/modules/sms/__fixtures__/infobip-send-response.json`**
    ```json
    {
      "pinId": "9C817C6F8AF3D48F9FE553282AFA2B67",
      "to": "821012345678",
      "ncStatus": "NC_DESTINATION_REACHABLE",
      "smsStatus": "MESSAGE_SENT"
    }
    ```

    **B. `apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json`**
    ```json
    {
      "success": {
        "msisdn": "821012345678",
        "verified": true,
        "attemptsRemaining": 0,
        "pinError": "NO_ERROR"
      },
      "wrongPin": {
        "msisdn": "821012345678",
        "verified": false,
        "attemptsRemaining": 3,
        "pinError": "WRONG_PIN"
      },
      "expired": {
        "msisdn": "821012345678",
        "verified": false,
        "attemptsRemaining": 0,
        "pinError": "PIN_EXPIRED"
      },
      "noMoreAttempts": {
        "msisdn": "821012345678",
        "verified": false,
        "attemptsRemaining": 0,
        "pinError": "NO_MORE_PIN_ATTEMPTS"
      }
    }
    ```

    **C. `apps/api/src/modules/sms/phone.util.spec.ts`**
    describe 블록:
    - `parseE164` — 한국 로컬(010-1234-5678, 01012345678), E.164 passthrough(+821012345678, +66812345678), 잘못된 번호 throw
    - `isChinaMainland` — true: +8613912345678. false: +821012345678, +85212345678(홍콩), +85312345678(마카오), +886912345678(대만)
    - **[Review #8 CN edge cases]**: 0086 prefix, full-width +86 (`+86`→ U+FF0B + 86 처리), +86 (불완전 번호 → false), 공백 포함 `+ 86 139 1234 5678`

    **D. `apps/api/src/modules/sms/infobip-client.spec.ts`**
    describe 블록:
    - `sendPin` — URL, Authorization header "App key", body shape(applicationId, messageId, from, to), leading + 제거 확인
    - `sendPin` 4xx/5xx → InfobipApiError throw
    - `sendPin` timeout → AbortError 계열
    - `verifyPin` — URL에 encodeURIComponent(pinId) 포함, body `{ pin }`, 응답 반환
    - `verifyPin` 에러 응답 처리
    - `global.fetch` mock 사용

    **E. `apps/api/src/modules/sms/sms.service.spec.ts`**
    Twilio mock 완전 제거. describe 블록:
    1. `constructor` — production hard-fail (D-14, D-16): 4 env 중 1개 비면 throw
    2. `constructor` — dev mock (D-15): 4 env 전부 미설정 + non-production → isDevMock=true
    3. `sendVerificationCode` — CN(+86) reject → BadRequestException(D-03)
    4. `sendVerificationCode` — dev mock success(000000)
    5. `sendVerificationCode` — resend cooldown SET NX fail → 429
    6. `sendVerificationCode` — Infobip sendPin success → redis SET pinId
    7. **[Review #1 cooldown rollback]** `sendVerificationCode` — Infobip sendPin 5xx/timeout 시 cooldown key 삭제(DEL). 4xx 시 cooldown key 유지
    8. `sendVerificationCode` — phone axis counter Lua: 6번째 호출 429 (D-06)
    9. **[Review #3 Valkey atomicity]** phone axis counter — Lua script가 INCR + conditional EXPIRE를 원자적으로 실행
    10. `verifyCode` — dev mock 000000 success / wrong code fail
    11. `verifyCode` — pinId 없음(만료) → GoneException
    12. `verifyCode` — Infobip verified=true → redis.del
    13. `verifyCode` — attemptsRemaining=0 / NO_MORE_PIN_ATTEMPTS → GoneException
    14. `verifyCode` — WRONG_PIN → verified false
    15. `verifyCode` — phone axis verify counter Lua: 11번째 호출 429 (D-07)
    16. **[Review #2 OTP max attempts]** 명시 주석: "OTP max 5 attempts는 Infobip Application pinAttempts=5 위임. 앱 레벨 카운터 불필요 — Infobip가 서버 사이드에서 강제. DEPLOY-CHECKLIST.md §3 'pinAttempts: 5' 참조"

    **F. `apps/api/test/sms-throttle.integration.spec.ts`**
    testcontainers + NestJS TestingModule:
    - send-code IP axis 20/3600s 초과 → 429
    - send-code phone axis 5/3600s 초과 → 429
    - verify-code IP axis 10/900s 초과 → 429
    - verify-code phone axis 10/900s 초과 → 429
    - password-reset 3/900s 유지 확인 (D-09)
    - **[Review #6 TTL ms 검증]** TTL 단위가 ms임을 검증: Throttler가 1h=3600000ms를 정확히 적용하는지 확인

    **G. 프론트엔드 — Plan 01 Task 2에서 처리**

    모든 spec 파일은 `describe.skip` 또는 `test.todo`가 아닌 **실제 expect 문이 있는 RED 테스트**로 작성. import 경로가 미존재하면 파일 자체를 빈 export로 stub해두고 주석으로 "Plan XX에서 구현" 표기.
  </action>
  <acceptance_criteria>
    - 6개 백엔드 테스트/fixture 파일 모두 존재
    - `grep -q "InfobipClient" apps/api/src/modules/sms/infobip-client.spec.ts`
    - `grep -q "isChinaMainland" apps/api/src/modules/sms/phone.util.spec.ts`
    - `grep -q "0086" apps/api/src/modules/sms/phone.util.spec.ts` (Review #8 CN edge case)
    - `grep -q "cooldown.*del\|DEL.*cooldown\|rollback" apps/api/src/modules/sms/sms.service.spec.ts` (Review #1)
    - `grep -q "pinAttempts.*5\|Infobip Application" apps/api/src/modules/sms/sms.service.spec.ts` (Review #2 명시)
    - `grep -q "lua\|EVAL\|atomic" apps/api/src/modules/sms/sms.service.spec.ts` (Review #3)
    - `grep -q "testcontainers" apps/api/test/sms-throttle.integration.spec.ts`
    - `test -f apps/api/src/modules/sms/__fixtures__/infobip-send-response.json`
    - `test -f apps/api/src/modules/sms/__fixtures__/infobip-verify-response.json`
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/api/src/modules/sms/sms.service.spec.ts && test -f apps/api/src/modules/sms/infobip-client.spec.ts && test -f apps/api/src/modules/sms/phone.util.spec.ts && test -f apps/api/test/sms-throttle.integration.spec.ts && test -f apps/api/src/modules/sms/__fixtures__/infobip-send-response.json</automated>
  </verify>
  <done>백엔드 테스트 6파일 + fixture 2종 RED 작성, Review #1/#2/#3/#8 반영 테스트 케이스 포함</done>
</task>

<task type="auto">
  <id>10-01-T2</id>
  <name>Task 2: 프론트엔드 테스트 스캐폴딩 2파일 (RED) + VALIDATION.md 업데이트</name>
  <files>
    apps/web/components/auth/__tests__/phone-verification.test.tsx,
    apps/web/e2e/signup-sms.spec.ts,
    .planning/phases/10-sms/10-VALIDATION.md
  </files>
  <read_first>
    - apps/web/components/auth/phone-verification.tsx (현재 구현 — Plan 08이 재작성 예정)
    - .planning/phases/10-sms/10-UI-SPEC.md §"Copywriting Contract", §"Button Labels", §"에러 메시지 카피"
    - .planning/phases/10-sms/10-VALIDATION.md (Per-Task Verification Map 업데이트 대상)
    - .planning/phases/10-sms/10-CONTEXT.md D-18, D-19, D-20, D-24
  </read_first>
  <action>
    **A. `apps/web/components/auth/__tests__/phone-verification.test.tsx`**
    @testing-library/react 기반 단위 테스트:
    - 초기 상태: "인증번호 발송" 버튼 존재, 인증번호 입력 숨김
    - 발송 후: "인증번호 6자리" 입력 노출, 30초 쿨다운 라벨(`재발송 (Ns)`) 표시
    - 쿨다운 종료: "재발송" 버튼 활성(outline variant)
    - 에러 카피: 429→"잠시 후 다시 시도해주세요", 410→"인증번호가 만료되었습니다. 재발송해주세요", 400→"인증번호가 일치하지 않습니다"
    - 국가 감지 안내: +66 입력 → "태국 번호로 SMS를 발송합니다" 텍스트 노출
    - 시도 횟수 미노출: "남은 시도" 문구 없음 (D-19)
    - autoComplete="one-time-code" 존재
    - aria-label="재발송 대기 중" 존재 (쿨다운 상태)
    - role="alert" 에러 메시지에 존재

    **B. `apps/web/e2e/signup-sms.spec.ts`**
    Playwright E2E (mock 모드):
    - 회원가입 step3까지 진행 → 전화번호 입력 → 발송 → 000000 입력 → "인증 완료"
    - 잘못된 코드 → "인증번호가 일치하지 않습니다"
    - 30초 쿨다운 `재발송 (\d+s)` 라벨 존재 확인

    **C. VALIDATION.md**
    Per-Task Verification Map 테이블을 실 Plan/Task/Wave/Command 정보로 채움.

    모든 spec은 RED 상태로 작성.
  </action>
  <acceptance_criteria>
    - `test -f apps/web/components/auth/__tests__/phone-verification.test.tsx`
    - `test -f apps/web/e2e/signup-sms.spec.ts`
    - `grep -q "재발송" apps/web/components/auth/__tests__/phone-verification.test.tsx`
    - `grep -q "000000" apps/web/e2e/signup-sms.spec.ts`
    - `grep -q "one-time-code" apps/web/components/auth/__tests__/phone-verification.test.tsx`
    - `grep -q "role.*alert\|aria-live" apps/web/components/auth/__tests__/phone-verification.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/components/auth/__tests__/phone-verification.test.tsx && test -f apps/web/e2e/signup-sms.spec.ts && grep -q "000000" apps/web/e2e/signup-sms.spec.ts</automated>
  </verify>
  <done>프론트 테스트 2파일 RED 작성, VALIDATION.md Per-Task 테이블 채움</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 fixture → 프로덕션 응답 shape | fixture가 실 API 응답과 다르면 false green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | fixture 응답 shape 불일치 | mitigate | RESEARCH Assumption A1/A2 기반 + DEPLOY-CHECKLIST §9(staging smoke 후 fixture diff) |
| T-10-02 | Repudiation | Wave 0 누락 시 coverage gap | mitigate | VALIDATION.md Per-Task Map으로 전 task 자동 verify 보장 |
</threat_model>

<verification>
- 9개 파일 존재 (fixture 2 + spec 5 + e2e 1 + VALIDATION 1)
- 모든 spec RED 상태 (구현 파일 미존재로 import 실패 또는 assertion 실패)
</verification>

<success_criteria>
- Wave 0 테스트 스캐폴딩 전부 RED
- Review concern #1/#2/#3/#8 반영 테스트 케이스 명시적 포함
- VALIDATION.md Per-Task Map 완성
</success_criteria>

<output>
After completion, create `.planning/phases/10-sms/10-01-SUMMARY.md`
</output>
