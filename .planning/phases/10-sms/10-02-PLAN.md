---
phase: 10
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web/package.json
  - pnpm-lock.yaml
  - .env.example
  - .planning/phases/10-sms/DEPLOY-CHECKLIST.md
autonomous: true
requirements: [SMS-02, SMS-03]
must_haves:
  truths:
    - "twilio npm 의존성 제거됨"
    - "@nest-lab/throttler-storage-redis ^1.2.0, libphonenumber-js ^1.12.41 apps/api에 추가"
    - "libphonenumber-js ^1.12.41 apps/web에 추가(국가 감지 UI)"
    - "INFOBIP_* 4종 env가 .env.example에 문서화 — env var 이름은 INFOBIP_BASE_URL, INFOBIP_API_KEY, INFOBIP_APPLICATION_ID, INFOBIP_MESSAGE_ID"
    - "TWILIO_* 3종 env가 .env.example에서 제거"
    - "Infobip 콘솔 운영 체크리스트(Application/Message Template 사전생성) 문서 존재"
  artifacts:
    - path: "apps/api/package.json"
      provides: "twilio 제거, @nest-lab/throttler-storage-redis + libphonenumber-js 추가"
      contains: "@nest-lab/throttler-storage-redis"
    - path: "apps/web/package.json"
      provides: "libphonenumber-js 추가(프론트 국가 감지)"
      contains: "libphonenumber-js"
    - path: ".env.example"
      provides: "INFOBIP_* 환경변수 스켈레톤 + TWILIO_* 제거"
      contains: "INFOBIP_API_KEY"
    - path: ".planning/phases/10-sms/DEPLOY-CHECKLIST.md"
      provides: "Infobip 콘솔 운영자 수동 작업 체크리스트"
      contains: "pinAttempts=5"
  key_links:
    - from: "apps/api/package.json"
      to: "@nest-lab/throttler-storage-redis"
      via: "Plan 07 ThrottlerModule forRootAsync storage"
      pattern: "@nest-lab/throttler-storage-redis"
    - from: ".env.example"
      to: "apps/api/src/modules/sms/sms.service.ts"
      via: "Plan 05 hard-fail 생성자"
      pattern: "INFOBIP_API_KEY"
---

<objective>
Phase 10 코드 작성 이전에 의존성 교체와 환경변수 문서를 완료한다. CONTEXT D-02(twilio 제거), D-16(env 4종 추가), D-17(GitHub Actions secrets 로테이션), D-04(Infobip 콘솔 사전작업 운영 체크리스트)를 반영.

[Review #9 반영] .env.example에 Infobip env var contract를 명시적으로 문서화: `INFOBIP_BASE_URL`(계정별 포털 도메인 e.g. `xxxxx.api.infobip.com`), `INFOBIP_API_KEY`(API key), `INFOBIP_APPLICATION_ID`(2FA Application ID), `INFOBIP_MESSAGE_ID`(Message Template ID).

Purpose: Wave 1+ 코드가 import할 패키지가 존재해야 하고, `.env.example`이 최신이어야 로컬 dev 시작이 가능하다.

Output: package.json 2개 + pnpm-lock.yaml 갱신 + .env.example + DEPLOY-CHECKLIST.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sms/10-CONTEXT.md
@.planning/phases/10-sms/10-RESEARCH.md
@apps/api/package.json
</context>

<tasks>

<task type="auto">
  <id>10-02-T1</id>
  <name>Task 1: twilio 제거 + @nest-lab/throttler-storage-redis + libphonenumber-js 설치 (api + web)</name>
  <files>apps/api/package.json, apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json (현재 twilio ^5.13.1 존재 확인)
    - apps/web/package.json (libphonenumber-js 미존재 확인)
    - .planning/phases/10-sms/10-RESEARCH.md §"Installation"
  </read_first>
  <action>
    프로젝트 루트에서 실행:

    ```bash
    pnpm --filter @grapit/api remove twilio
    pnpm --filter @grapit/api add @nest-lab/throttler-storage-redis@^1.2.0 libphonenumber-js@^1.12.41
    pnpm --filter @grapit/web add libphonenumber-js@^1.12.41
    ```

    설치 후 `node_modules/@nest-lab/throttler-storage-redis/package.json`의 peerDependencies 확인 — `@nestjs/common ^11.0.0`, `@nestjs/throttler >=6.0.0`, `ioredis >=5.0.0` 명시되어야 함.

    twilio는 `dependencies`에서 완전 제거. 기존 sms.service.ts의 `import Twilio from 'twilio'`는 Plan 05에서 제거 — 이 시점에서는 typecheck 실패 예상(RED 의도).
  </action>
  <acceptance_criteria>
    - `! grep -q '"twilio"' apps/api/package.json` (twilio 완전 제거)
    - `grep -q '"@nest-lab/throttler-storage-redis"' apps/api/package.json`
    - `grep -q '"libphonenumber-js"' apps/api/package.json`
    - `grep -q '"libphonenumber-js"' apps/web/package.json`
    - `pnpm install --frozen-lockfile` 성공
  </acceptance_criteria>
  <verify>
    <automated>! grep -q '"twilio"' apps/api/package.json && grep -q '"@nest-lab/throttler-storage-redis"' apps/api/package.json && grep -q '"libphonenumber-js"' apps/api/package.json && grep -q '"libphonenumber-js"' apps/web/package.json</automated>
  </verify>
  <done>twilio 완전 제거, 3개 패키지 설치 완료, lockfile 갱신</done>
</task>

<task type="auto">
  <id>10-02-T2</id>
  <name>Task 2: .env.example TWILIO 제거 + INFOBIP 4종 추가 + DEPLOY-CHECKLIST 작성</name>
  <files>.env.example, .planning/phases/10-sms/DEPLOY-CHECKLIST.md</files>
  <read_first>
    - .env.example (현재 루트 — TWILIO_* 3종 존재)
    - .planning/phases/10-sms/10-CONTEXT.md D-14, D-15, D-16, D-17
    - .planning/phases/10-sms/10-RESEARCH.md §"Common Pitfalls > Pitfall 2, 4"
    - .planning/phases/10-sms/10-REVIEWS.md #9 (Infobip env var contract 명시)
    - CLAUDE.md §"환경변수" (루트 1개 규칙)
  </read_first>
  <action>
    **A. `.env.example`에서 다음 3개 라인 완전 삭제:**
    ```
    TWILIO_ACCOUNT_SID=
    TWILIO_AUTH_TOKEN=
    TWILIO_VERIFY_SERVICE_SID=
    ```

    **다음 블록을 적절한 SMS 섹션에 추가 [Review #9 반영 — env var contract 명시]:**
    ```
    # ---- SMS (Infobip 2FA) ----
    # D-14, D-16: production에서 4종 모두 필수. 하나라도 비면 SmsService 생성자 throw(hard-fail)
    # D-15: NODE_ENV !== 'production' + 아래 값 전부 미설정일 때만 dev mock 모드(000000 유니버설 코드)
    # Infobip 콘솔에서 Application + Message Template 사전 생성 필요 (DEPLOY-CHECKLIST.md 참조)
    INFOBIP_API_KEY=                    # Infobip Portal > Developers > API Keys
    INFOBIP_BASE_URL=                   # 계정별 포털 도메인 (e.g. xxxxx.api.infobip.com)
    INFOBIP_APPLICATION_ID=             # Portal > 2FA > Applications > Application ID
    INFOBIP_MESSAGE_ID=                 # Portal > 2FA > Messages > Message ID
    ```

    **B. `.planning/phases/10-sms/DEPLOY-CHECKLIST.md` 작성:**
    10개 섹션:
    1. Infobip Portal 계정 확보 (base URL 확인)
    2. 한국 발신번호 사전등록 KISA (D-04)
    3. 2FA Application 생성 — `pinAttempts=5`(D-12), `allowMultiplePinVerifications=true`, `pinTimeToLive=3m`(D-10), `sendPinPerPhoneNumberLimit=5/1h`(D-06)
    4. Message Template 생성 — `pinType=NUMERIC`(D-13), `pinLength=6`(D-13), messageText `"[Grapit] 인증번호 {{pin}} (3분 이내 입력)"`
    5. API Key 발급 → INFOBIP_API_KEY
    6. GCP Secret Manager 업데이트 (D-17) — gcloud 커맨드 포함
    7. GitHub Actions Secrets (D-17) — TWILIO_* 삭제 + INFOBIP_* 추가
    8. Cloud Run env binding — 4종 secret → env var
    9. Staging Smoke Test (D-25) — 본인 번호 발송/검증, fixture diff
    10. Production 배포 — cold-start 후 Sentry alert 5분 모니터링, 본인 번호 스모크 1회 (~35원)

    **[Review #2 반영]** §3에 명시적 주석 추가: "OTP max attempts 5회는 이 Application 설정(`pinAttempts=5`)이 서버 사이드에서 강제. 앱 레벨 attempt counter는 의도적으로 미구현 — Infobip이 5회 초과 시 PIN 즉시 무효화."
  </action>
  <acceptance_criteria>
    - `! grep -q "TWILIO_" .env.example`
    - `grep -q "^INFOBIP_API_KEY=" .env.example`
    - `grep -q "^INFOBIP_BASE_URL=" .env.example`
    - `grep -q "^INFOBIP_APPLICATION_ID=" .env.example`
    - `grep -q "^INFOBIP_MESSAGE_ID=" .env.example`
    - `grep -q "xxxxx.api.infobip.com" .env.example` (Review #9 contract 명시)
    - `test -f .planning/phases/10-sms/DEPLOY-CHECKLIST.md`
    - `grep -q "pinAttempts.*5" .planning/phases/10-sms/DEPLOY-CHECKLIST.md`
    - `grep -q "앱 레벨.*counter.*미구현\|의도적" .planning/phases/10-sms/DEPLOY-CHECKLIST.md` (Review #2)
    - `grep -q "gcloud secrets" .planning/phases/10-sms/DEPLOY-CHECKLIST.md`
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "TWILIO_" .env.example && grep -q "^INFOBIP_API_KEY=" .env.example && grep -q "^INFOBIP_BASE_URL=" .env.example && test -f .planning/phases/10-sms/DEPLOY-CHECKLIST.md && grep -q "pinAttempts" .planning/phases/10-sms/DEPLOY-CHECKLIST.md</automated>
  </verify>
  <done>TWILIO_* 3개 제거, INFOBIP_* 4개 추가(contract 명시), DEPLOY-CHECKLIST 10섹션 완성, Review #2/#9 반영</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example → production 배포자 | 누락된 env가 있으면 production hard-fail로 catch |
| Infobip 콘솔 설정값 → 코드 기대값 | Application ID/Message ID 불일치 시 첫 발송 실패 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | supply chain (twilio 제거) | accept | `pnpm install --frozen-lockfile` lockfile 검증. 제거는 보안상 positive |
| T-10-07 | Information Disclosure | .env.example 실 시크릿 포함 | mitigate | 값 빈 상태로 유지, 실 시크릿 불포함 |
| T-10-08 | Spoofing | Infobip Application 설정 탈루 | mitigate | DEPLOY-CHECKLIST 10단계 강제 |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` 성공
- `! grep -q twilio apps/api/package.json`
- `grep -q INFOBIP .env.example`
- `test -f .planning/phases/10-sms/DEPLOY-CHECKLIST.md`
</verification>

<success_criteria>
- twilio 의존성 제거 + 3개 패키지 설치
- .env.example INFOBIP_* 4종 문서화(contract 명시) + TWILIO_* 3종 제거
- DEPLOY-CHECKLIST.md 10단계 완성 + Review #2 OTP max attempts 근거 명시
</success_criteria>

<output>
After completion, create `.planning/phases/10-sms/10-02-SUMMARY.md`
</output>
