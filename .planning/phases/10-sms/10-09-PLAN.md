---
phase: 10
plan: 09
type: execute
wave: 5
depends_on: [10-01, 10-05, 10-06, 10-07, 10-08]
files_modified:
  - apps/web/e2e/signup-sms.spec.ts
  - apps/api/test/sms-throttle.integration.spec.ts
  - .planning/phases/10-sms/DEPLOY-CHECKLIST.md
autonomous: false
requirements: [SMS-01, SMS-02, SMS-03, SMS-04]
must_haves:
  truths:
    - "signup-sms.spec.ts가 mock 모드(000000)로 회원가입 전 플로우를 Playwright에서 완주"
    - "sms-throttle.integration.spec.ts가 testcontainers Valkey + 실 throttler로 limit + 1번째 429 검증"
    - "apps/api typecheck / lint / test 모두 green"
    - "apps/web typecheck / lint / test / test:e2e 모두 green"
    - "staging 수동 SMS smoke 결과가 DEPLOY-CHECKLIST.md에 기록됨 (D-25)"
    - "staging smoke는 체크리스트 기록이며 merge blocker는 아님"
  artifacts:
    - path: "apps/web/e2e/signup-sms.spec.ts"
      provides: "Playwright CI mock 회원가입 플로우"
      contains: "000000"
    - path: "apps/api/test/sms-throttle.integration.spec.ts"
      provides: "testcontainers Valkey throttler integration 테스트"
      contains: "testcontainers"
  key_links:
    - from: "signup-sms.spec.ts"
      to: "phone-verification.tsx"
      via: "label/placeholder 기반 인터랙션"
      pattern: "인증번호 발송"
---

<objective>
Phase 10 전 구현(Plan 02~08)을 연결 검증. Wave 0(Plan 01) RED 테스트를 GREEN으로 전환하고, staging 수동 smoke(D-25)를 human checkpoint로 확인.

[Review #10 MEDIUM] staging smoke의 성격을 명확히 정의: **체크리스트 기록**이며 **merge blocker가 아님**. 1인 개발 환경에서 staging 환경이 항상 가용하지 않을 수 있고, CI 자동 테스트가 mock 모드로 기능 검증을 완료하므로, 실 SMS smoke는 배포 전 체크리스트 항목으로 DEPLOY-CHECKLIST.md에 기록.

Output: E2E + integration GREEN + staging smoke 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sms/10-CONTEXT.md
@.planning/phases/10-sms/10-UI-SPEC.md
@.planning/phases/10-sms/10-VALIDATION.md
@.planning/phases/10-sms/10-REVIEWS.md
@.planning/phases/10-sms/DEPLOY-CHECKLIST.md
@apps/web/e2e/signup-sms.spec.ts
@apps/api/test/sms-throttle.integration.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <id>10-09-T1</id>
  <name>Task 1: signup-sms.spec.ts — mock 모드 회원가입 E2E GREEN 전환</name>
  <files>apps/web/e2e/signup-sms.spec.ts</files>
  <read_first>
    - apps/web/e2e/signup-sms.spec.ts (Plan 01 RED 스캐폴딩)
    - apps/web/e2e/social-login.spec.ts (Playwright 구문 참조)
    - apps/web/components/auth/phone-verification.tsx (Plan 08 구현 후 label/placeholder)
    - .planning/phases/10-sms/10-UI-SPEC.md §"Copywriting Contract"
    - .planning/phases/10-sms/10-CONTEXT.md D-24
  </read_first>
  <action>
    Plan 01 RED 스캐폴딩을 실 E2E 스펙으로 전환:

    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Signup SMS mock flow', () => {
      test('전화번호 인증 mock(000000)으로 회원가입 step3 완주', async ({ page }) => {
        await page.goto('/auth/signup');
        // step1/step2 필드는 실 구현 확인 후 채움
        // step3 phone-verification:
        const phoneInput = page.getByPlaceholder(/010-0000-0000/);
        await phoneInput.fill('010-1234-5678');
        await page.getByRole('button', { name: '인증번호 발송' }).click();
        // 쿨다운 라벨
        await expect(page.getByRole('button', { name: /재발송 \(\d+s\)/ })).toBeVisible();
        // 만료 타이머
        await expect(page.locator('text=/^0[0-2]:\\d{2}$/')).toBeVisible();
        // 인증번호 입력
        await page.getByPlaceholder(/인증번호 6자리/).fill('000000');
        await page.getByRole('button', { name: '확인' }).click();
        await expect(page.getByText('인증 완료')).toBeVisible();
      });

      test('잘못된 인증번호 → 에러 메시지', async ({ page }) => {
        // ... step3 진입
        await page.getByPlaceholder(/인증번호 6자리/).fill('111111');
        await page.getByRole('button', { name: '확인' }).click();
        await expect(page.getByText('인증번호가 일치하지 않습니다')).toBeVisible();
      });
    });
    ```

    주의:
    - CI env: `NODE_ENV=test`, `INFOBIP_API_KEY` 미주입 → mock 모드
    - `test.fixme` 사용 금지
    - Flaky 방지: `expect().toBeVisible({ timeout: 10_000 })`
  </action>
  <acceptance_criteria>
    - `grep -q "000000" apps/web/e2e/signup-sms.spec.ts`
    - `grep -q "인증 완료" apps/web/e2e/signup-sms.spec.ts`
    - `grep -qv "test.fixme\|test.skip" apps/web/e2e/signup-sms.spec.ts`
    - `pnpm --filter @grapit/web test:e2e -- signup-sms.spec.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/web test:e2e -- signup-sms.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Playwright mock 회원가입 완주, 에러 분기 확인, CI green</done>
</task>

<task type="auto" tdd="true">
  <id>10-09-T2</id>
  <name>Task 2: sms-throttle.integration.spec.ts — testcontainers Valkey GREEN 전환</name>
  <files>apps/api/test/sms-throttle.integration.spec.ts</files>
  <read_first>
    - apps/api/test/sms-throttle.integration.spec.ts (Plan 01 RED)
    - apps/api/src/app.module.ts (Plan 07 forRootAsync)
    - apps/api/src/modules/sms/sms.controller.ts (Plan 06 @Throttle)
    - .planning/phases/10-sms/10-REVIEWS.md #6 (TTL ms 검증)
  </read_first>
  <action>
    Plan 01 RED 스캐폴딩을 실 testcontainers 통합 테스트로 전환:

    - `valkey/valkey:8` 또는 `redis:7` 이미지 시작
    - NestJS TestingModule로 REDIS_URL override
    - send-code IP 20/h 초과 → 429
    - send-code phone 5/h 초과 → 429 (Lua atomic counter)
    - verify-code IP 10/15min 초과 → 429
    - verify-code phone 10/15min 초과 → 429
    - password-reset 3/15min 유지 (D-09)
    - [Review #6] TTL ms 검증: 1h window가 정확히 3_600_000ms로 적용되는지 Valkey TTL 명령으로 확인
    - Infobip API는 global.fetch mock
    - container cleanup
  </action>
  <acceptance_criteria>
    - `grep -q "testcontainers" apps/api/test/sms-throttle.integration.spec.ts`
    - `grep -q "429" apps/api/test/sms-throttle.integration.spec.ts`
    - `grep -q "password.*reset\|auth.*throttle" apps/api/test/sms-throttle.integration.spec.ts` (D-09 검증)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grapit/api test:integration sms-throttle -- --run 2>&1 | tail -15</automated>
  </verify>
  <done>testcontainers 통합 테스트 GREEN, IP/phone 양축 429 검증, TTL ms 확인</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <id>10-09-T3</id>
  <name>Task 3: Staging SMS smoke test (D-25) — 체크리스트 기록</name>
  <files>.planning/phases/10-sms/DEPLOY-CHECKLIST.md</files>
  <action>
    [Review #10: 이 smoke는 **체크리스트 기록**이며 **merge blocker가 아님**]

    staging 환경이 가용한 경우 수동 smoke 테스트 수행:
    1. staging에 INFOBIP_* 4종 env 주입
    2. 본인 번호로 send-code → SMS 수신
    3. verify-code → verified: true
    4. 실 응답 JSON과 fixture diff
    5. DEPLOY-CHECKLIST.md §9에 결과 기록

    staging 미가용 시 skip 허용 — production 배포 전 DEPLOY-CHECKLIST §9/§10에서 반드시 수행.
  </action>
  <acceptance_criteria>
    - DEPLOY-CHECKLIST.md §9에 smoke 결과 또는 "staging 미가용 — 배포 전 수행 예정" 기록
  </acceptance_criteria>
  <verify>
    <automated>grep -q "smoke\|Smoke\|staging" .planning/phases/10-sms/DEPLOY-CHECKLIST.md</automated>
  </verify>
  <done>staging smoke 결과 기록 완료 또는 skip 사유 기록</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → mock mode | INFOBIP_API_KEY 미주입으로 mock 보장 |
| staging → Infobip 실 API | 실 SMS 비용 발생 (~35원/건) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Information Disclosure | CI에서 실 SMS 발송 | mitigate | D-24: INFOBIP_API_KEY 미주입 → mock. CI workflow에 INFOBIP secrets 절대 미추가 |
| T-10-09-02 | Tampering | fixture와 실 응답 불일치 | mitigate | staging smoke diff로 검증 후 fixture 업데이트 |
</threat_model>

<verification>
- `pnpm --filter @grapit/api typecheck && pnpm --filter @grapit/api test --run` green
- `pnpm --filter @grapit/web typecheck && pnpm --filter @grapit/web test --run` green
- `pnpm --filter @grapit/web test:e2e -- signup-sms.spec.ts` green
- staging smoke 결과 DEPLOY-CHECKLIST.md에 기록 (또는 skip 사유)
</verification>

<success_criteria>
- E2E + integration GREEN
- Phase 10 success criteria 4개 충족:
  1. 회원가입 시 실 SMS OTP 수신 (staging smoke)
  2. rate limiting (integration test)
  3. OTP 재시도/만료 (unit test + E2E)
  4. dev mock 자동 전환 (E2E)
- [Review #10] staging smoke = 체크리스트 기록 (non-blocking)
</success_criteria>

<output>
Create `.planning/phases/10-sms/10-09-SUMMARY.md`
</output>
