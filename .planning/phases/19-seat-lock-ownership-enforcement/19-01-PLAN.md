---
phase: 19-seat-lock-ownership-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
  - apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
  - apps/api/src/modules/reservation/reservation.service.spec.ts
  - apps/web/hooks/__tests__/use-booking.test.tsx
  - apps/web/e2e/toss-payment.spec.ts
autonomous: true
requirements:
  - VALK-03
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
must_haves:
  truths:
    - "Backend tests prove missing, expired, released, and other-user locks cannot prepare or confirm reservations."
    - "Valkey script tests prove every requested seat lock owner must equal the authenticated userId."
    - "Web tests prove lock rejection messages are shown and Toss payment is not started after prepare rejection."
    - "Existing seat-map UX regression tests for stage, legend, animation, minimap, and mobile touch behavior remain in the validation command."
    - "Decision coverage: D-18, D-19, D-20, and D-21 are translated into Wave 0 backend, Valkey, web, and E2E regression tests."
  artifacts:
    - path: "apps/api/src/modules/booking/__tests__/booking.service.spec.ts"
      provides: "BookingService assert/consume ownership RED tests"
      contains: "assertOwnedSeatLocks"
    - path: "apps/api/src/modules/reservation/reservation.service.spec.ts"
      provides: "Reservation prepare/confirm ownership RED tests"
      contains: "Existing pending order cannot bypass active lock ownership"
    - path: "apps/web/hooks/__tests__/use-booking.test.tsx"
      provides: "usePrepareReservation/useConfirmPayment mutation contract tests"
      contains: "/api/v1/reservations/prepare"
    - path: "apps/web/e2e/toss-payment.spec.ts"
      provides: "Prepare/confirm lock rejection E2E regressions"
      contains: "lock ownership"
  key_links:
    - from: "apps/api/src/modules/reservation/reservation.service.spec.ts"
      to: "apps/api/src/modules/booking/booking.service.ts"
      via: "mock BookingService assertOwnedSeatLocks/consumeOwnedSeatLocks"
      pattern: "assertOwnedSeatLocks|consumeOwnedSeatLocks"
    - from: "apps/web/e2e/toss-payment.spec.ts"
      to: "/api/v1/reservations/prepare"
      via: "Playwright route interception"
      pattern: "reservations/prepare"
---

<objective>
Phase 19의 구현 전에 실패해야 할 backend/web 회귀 테스트를 먼저 고정한다.

Purpose: D-18, D-19, D-20과 19-VALIDATION.md Wave 0 요구를 실행 가능한 테스트 계약으로 만든다.
Output: 이후 계획이 GREEN으로 전환해야 할 API unit, Valkey integration, web unit, Playwright E2E 테스트.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-CONTEXT.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-RESEARCH.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-PATTERNS.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-VALIDATION.md
@.planning/phases/07-valkey/07-01-SUMMARY.md
@.planning/phases/07-valkey/07-05-SUMMARY.md
@.planning/phases/12-ux/12-00-SUMMARY.md
@.planning/phases/12-ux/12-03-SUMMARY.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-03-SUMMARY.md
</context>

<decision_coverage>
- D-18: `ReservationService` prepare/confirm ownership RED tests are created before implementation.
- D-19: `BookingService` Lua, InMemoryRedis, and real Valkey assert/consume coverage is created before implementation.
- D-20: API/E2E regressions cover disappeared or changed locks and prove no confirmed reservation is created on lock rejection.
- D-21: All planned tests stay in the existing booking, redis provider, reservation, and Toss payment spec files.
</decision_coverage>

<interfaces>
New backend helpers that later plans must implement against these tests:
```typescript
export const LOCK_EXPIRED_MESSAGE = '좌석 점유 시간이 만료되었습니다. 좌석을 다시 선택해주세요.';
export const LOCK_OTHER_OWNER_MESSAGE = '이미 다른 사용자가 선택한 좌석입니다.';

export type SeatLockOwnershipFailureReason = 'MISSING' | 'OTHER_OWNER';
export type SeatLockOwnershipResult =
  | { ok: true; seatIds: string[] }
  | { ok: false; reason: SeatLockOwnershipFailureReason; seatId: string; owner: string | null };

async assertOwnedSeatLocks(userId: string, showtimeId: string, seatIds: string[]): Promise<void>;
async consumeOwnedSeatLocks(
  userId: string,
  showtimeId: string,
  seatIds: string[],
): Promise<{ consumedSeatIds: string[] }>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Wave 0 ownership tests</name>
  <files>apps/api/src/modules/booking/__tests__/booking.service.spec.ts, apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts, apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts, apps/api/src/modules/reservation/reservation.service.spec.ts</files>
  <read_first>
    .planning/phases/19-seat-lock-ownership-enforcement/19-CONTEXT.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-RESEARCH.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-PATTERNS.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-VALIDATION.md
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/providers/redis.provider.ts
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
    apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
    apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
    apps/api/src/modules/reservation/reservation.service.spec.ts
  </read_first>
  <behavior>
    - BookingService `assertOwnedSeatLocks()` succeeds only when every `{showtimeId}:seat:{seatId}` value equals `userId`.
    - BookingService `consumeOwnedSeatLocks()` deletes only requested owned locks and preserves unrelated same-showtime locks.
    - ReservationService prepare rejects missing/expired/released/other-user locks, including existing pending `orderId`.
    - ReservationService confirm rejects invalid locks before Toss confirm and compensates if post-Toss consume fails.
  </behavior>
  <action>
    Add RED tests per D-18 and D-19.

    In `booking.service.spec.ts`, add `describe('lock ownership helpers')` with these exact cases:
    - `assertOwnedSeatLocks succeeds when all requested seat locks belong to the user`
    - `assertOwnedSeatLocks rejects missing or expired locks with lock-expired message`
    - `assertOwnedSeatLocks rejects other-user locks with other-user message`
    - `consumeOwnedSeatLocks uses KEYS [userSeatsKey, lockedSeatsKey, ...seatLockKeys] and ARGV [userId, ...seatIds]`
    - `consumeOwnedSeatLocks preserves unrelated same-showtime locks`

    In `redis.provider.spec.ts`, add InMemoryRedis parity tests using local marker script constants named `ASSERT_OWNED_SEAT_LOCKS_LUA` and `CONSUME_OWNED_SEAT_LOCKS_LUA`. The tests must call `redis.eval(script, numKeys, ...keys, ...args)` and assert the unified ownership tuple contract: success returns `[1, 'OK', '2', '']`, missing/expired failure returns `[0, 'MISSING', 'A-2', '']`, and other-owner failure returns `[0, 'OTHER_OWNER', 'A-2', 'other-user']`.

    In `booking.service.integration.spec.ts`, instantiate `new BookingService(redis as IORedis, mockDb, mockGateway)` against the existing real Valkey container and add cases for all-owned, missing, other-owner, stale-index cleanup, and unrelated lock preservation. Cast the service to the planned helper interface so the file typechecks before implementation.

    In `reservation.service.spec.ts`, extend `createMockBookingService()` with `assertOwnedSeatLocks` and `consumeOwnedSeatLocks`. Add cases with exact test names:
    - `prepareReservation rejects missing active lock before creating pending reservation`
    - `prepareReservation rejects other-user lock before creating pending reservation`
    - `Existing pending order cannot bypass active lock ownership`
    - `confirmAndCreateReservation rejects invalid locks before Toss confirm`
    - `confirmAndCreateReservation cancels Toss and skips DB sold transition when consumeOwnedSeatLocks fails after Toss confirm`
    - `existing payment idempotency returns detail without active lock ownership check`

    Keep all tests in the existing files named in D-21. Do not create a new backend test directory for this phase.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- reservation.service booking.service redis.provider --reporter=verbose</automated>
    <automated>pnpm --filter @grabit/api test:integration -- booking.service.integration --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "lock ownership helpers|assertOwnedSeatLocks succeeds when all requested seat locks belong to the user|consumeOwnedSeatLocks preserves unrelated same-showtime locks" apps/api/src/modules/booking/__tests__/booking.service.spec.ts` prints matches.
    - `rg -n "ASSERT_OWNED_SEAT_LOCKS_LUA|CONSUME_OWNED_SEAT_LOCKS_LUA|OTHER_OWNER|MISSING" apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` prints matches.
    - `rg -n "stale-index|unrelated lock preservation|assertOwnedSeatLocks|consumeOwnedSeatLocks" apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts` prints matches.
    - `rg -n "Existing pending order cannot bypass active lock ownership|rejects invalid locks before Toss confirm|cancels Toss and skips DB sold transition|existing payment idempotency returns detail" apps/api/src/modules/reservation/reservation.service.spec.ts` prints matches.
    - The automated commands collect the new tests. RED failures for missing helper implementation are expected in this plan; collection/type errors not tied to the planned missing helpers must be fixed inside this task.
  </acceptance_criteria>
  <done>Wave 0 backend tests exist, are discoverable by the validation commands, and encode D-01 through D-13 plus D-18, D-19, and D-21.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web Wave 0 lock rejection tests</name>
  <files>apps/web/hooks/__tests__/use-booking.test.tsx, apps/web/e2e/toss-payment.spec.ts</files>
  <read_first>
    .planning/phases/19-seat-lock-ownership-enforcement/19-CONTEXT.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-RESEARCH.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-PATTERNS.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-VALIDATION.md
    apps/web/hooks/use-booking.ts
    apps/web/lib/api-client.ts
    apps/web/app/booking/[performanceId]/confirm/page.tsx
    apps/web/app/booking/[performanceId]/complete/page.tsx
    apps/web/components/booking/__tests__/seat-map-viewer.test.tsx
    apps/web/components/booking/__tests__/seat-selection-panel.test.tsx
    apps/web/e2e/toss-payment.spec.ts
  </read_first>
  <behavior>
    - `/reservations/prepare` 409 message is surfaced and Toss `requestPayment()` is not started.
    - `/payments/confirm` 409 message renders a failed confirmation state, not booking success.
    - Existing seat-map UX tests remain part of the quick web command for UX-02 through UX-06.
  </behavior>
  <action>
    Create `apps/web/hooks/__tests__/use-booking.test.tsx` using React Testing Library `renderHook`, `QueryClient`, and `QueryClientProvider`. Mock `@/lib/api-client` so:
    - `usePrepareReservation()` calls `apiClient.post('/api/v1/reservations/prepare', payload)`.
    - `useConfirmPayment()` calls `apiClient.post('/api/v1/payments/confirm', payload)`.
    - a rejected `ApiClientError('좌석 점유 시간이 만료되었습니다. 좌석을 다시 선택해주세요.', 409)` remains the mutation error message.

    Extend `apps/web/e2e/toss-payment.spec.ts` with two tests inside the existing `Toss Payments E2E` describe:
    - `lock ownership: prepare 409 surfaces server message and does not request Toss payment`
    - `lock ownership: confirm 409 renders failed state instead of booking success`

    The prepare test must intercept `POST **/api/v1/reservations/prepare` with status `409` and body `{ "statusCode": 409, "message": "좌석 점유 시간이 만료되었습니다. 좌석을 다시 선택해주세요.", "error": "Conflict" }`. It must also register a `POST **/api/v1/payments/confirm` route that flips `confirmIntercepted = true`, then assert `confirmIntercepted` remains `false`.

    The confirm test must intercept `POST **/api/v1/payments/confirm` with status `409` and body `{ "statusCode": 409, "message": "이미 다른 사용자가 선택한 좌석입니다.", "error": "Conflict" }`, navigate to `/booking/e2e-test-performance/complete?paymentKey=test_payment_key_lock_failure&orderId=test_order_lock_failure&amount=50000`, assert `예매를 완료하지 못했습니다` and `이미 다른 사용자가 선택한 좌석입니다.` are visible, and assert success copy matching `/예매가 완료|완료되었습니다/` is not visible.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- seat-map-viewer seat-selection-panel use-booking --reporter=verbose</automated>
    <automated>pnpm --filter @grabit/web test:e2e -- toss-payment.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/hooks/__tests__/use-booking.test.tsx` succeeds.
    - `rg -n "usePrepareReservation\\(\\).*reservations/prepare|useConfirmPayment\\(\\).*payments/confirm|좌석 점유 시간이 만료되었습니다" apps/web/hooks/__tests__/use-booking.test.tsx` prints matches.
    - `rg -n "lock ownership: prepare 409 surfaces server message and does not request Toss payment|lock ownership: confirm 409 renders failed state instead of booking success|confirmIntercepted.*false|예매를 완료하지 못했습니다" apps/web/e2e/toss-payment.spec.ts` prints matches.
    - `pnpm --filter @grabit/web test -- seat-map-viewer seat-selection-panel use-booking --reporter=verbose` collects `seat-map-viewer`, `seat-selection-panel`, and `use-booking`.
    - RED failures caused by missing confirm/complete UI behavior are expected in this plan; collection/type errors must be fixed inside this task.
  </acceptance_criteria>
  <done>Wave 0 web tests exist and encode D-14 through D-16, D-20, and UX-02 through UX-06 validation coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> reservation API | Browser-selected seats and orderId cross into authenticated backend endpoints. |
| reservation API -> Valkey | Backend asks Valkey whether authenticated user still owns every requested seat lock. |
| reservation API -> Toss | Backend may confirm or cancel external payment depending on lock ownership state. |
| API -> web UI | Server conflict messages drive visible recovery behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering / Elevation of privilege | `prepareReservation`, `confirmAndCreateReservation` | mitigate | Task 1 adds RED tests proving client-submitted seats fail unless Valkey per-seat owner equals authenticated `userId`. |
| T-19-02 | Tampering | `consumeOwnedSeatLocks` cleanup | mitigate | Task 1 adds RED tests proving unrelated same-showtime locks survive targeted consume. |
| T-19-03 | Tampering / DoS | Valkey Lua scripts | mitigate | Task 1 adds unit/integration tests asserting ioredis flat `eval(script, numKeys, ...KEYS, ...ARGV)` and explicit hash-tagged key usage. |
| T-19-04 | Elevation of privilege | existing pending `orderId` path | mitigate | Task 1 adds RED test `Existing pending order cannot bypass active lock ownership`. |
| T-19-05 | Tampering / Repudiation | post-Toss sold transition | mitigate | Task 1 adds RED test proving post-confirm consume failure calls Toss cancel and skips DB sold transition. |
| T-19-06 | Repudiation | confirm/complete UI | mitigate | Task 2 adds RED web/E2E tests proving server lock rejection cannot render false success. |
</threat_model>

<verification>
Plan-level verification:
- `pnpm --filter @grabit/api test -- reservation.service booking.service redis.provider --reporter=verbose`
- `pnpm --filter @grabit/api test:integration -- booking.service.integration --reporter=verbose`
- `pnpm --filter @grabit/web test -- seat-map-viewer seat-selection-panel use-booking --reporter=verbose`
- `pnpm --filter @grabit/web test:e2e -- toss-payment.spec.ts`
</verification>

<success_criteria>
- Backend RED tests cover 19-VALIDATION.md task IDs 19-W0-01 through 19-W0-05.
- Web RED tests cover 19-VALIDATION.md task ID 19-W0-06.
- No runtime code is modified in this plan.
- Test collection succeeds; expected RED assertions identify missing Phase 19 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/19-seat-lock-ownership-enforcement/19-01-SUMMARY.md`.
</output>
