---
phase: 19-seat-lock-ownership-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - apps/api/src/modules/booking/booking.service.ts
  - apps/api/src/modules/booking/providers/redis.provider.ts
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
  - apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
autonomous: true
requirements:
  - VALK-03
must_haves:
  truths:
    - "Backend can assert active Valkey ownership for every requested seat before reservation prepare/confirm."
    - "Backend can atomically consume only requested owned locks before sold transition."
    - "Valkey Lua scripts are cluster-compatible because every accessed key is passed through KEYS with `{showtimeId}` hash tags."
    - "Decision coverage: D-01, D-02, D-03, D-04, D-11, D-12, and D-24 are translated into BookingService Valkey ownership helper and InMemoryRedis parity contracts."
  artifacts:
    - path: "apps/api/src/modules/booking/booking.service.ts"
      provides: "assertOwnedSeatLocks and consumeOwnedSeatLocks helpers"
      exports: ["LOCK_EXPIRED_MESSAGE", "LOCK_OTHER_OWNER_MESSAGE", "ASSERT_OWNED_SEAT_LOCKS_LUA", "CONSUME_OWNED_SEAT_LOCKS_LUA"]
    - path: "apps/api/src/modules/booking/providers/redis.provider.ts"
      provides: "InMemoryRedis eval emulation for ownership scripts"
      contains: "evalConsumeOwnedSeatLocks"
    - path: "apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts"
      provides: "real Valkey coverage for ownership scripts"
      contains: "consumeOwnedSeatLocks"
  key_links:
    - from: "apps/api/src/modules/booking/booking.service.ts"
      to: "REDIS_CLIENT"
      via: "ioredis eval(script, numKeys, ...keys, ...args)"
      pattern: "this.redis.eval\\(\\s*CONSUME_OWNED_SEAT_LOCKS_LUA"
    - from: "apps/api/src/modules/booking/providers/redis.provider.ts"
      to: "apps/api/src/modules/booking/booking.service.ts"
      via: "script marker dispatch"
      pattern: "ASSERT_OWNED_SEAT_LOCKS_LUA|CONSUME_OWNED_SEAT_LOCKS_LUA"
---

<objective>
BookingService에 Valkey lock ownership assert/consume helper를 구현한다.

Purpose: reservation/payment 계층이 client state 대신 active Valkey lock ownership을 검증할 수 있는 단일 backend primitive를 제공한다.
Output: cluster-compatible Lua scripts, public helper methods, local InMemoryRedis parity, unit/integration GREEN tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-CONTEXT.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-RESEARCH.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-PATTERNS.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-VALIDATION.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-01-SUMMARY.md
@.planning/phases/07-valkey/07-01-SUMMARY.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-01-SUMMARY.md
</context>

<decision_coverage>
- D-01: The Valkey per-seat lock value remains the ownership token and must equal authenticated `userId`.
- D-02: No reservation-level lock token, client payload field, or shared schema change is introduced.
- D-03: All multi-key validation and consume scripts pass every accessed key through `KEYS` with `{showtimeId}` hash tags.
- D-04: `locked-seats` remains an index only; per-seat lock keys are authoritative.
- D-11: `consumeOwnedSeatLocks(userId, showtimeId, seatIds)` is backed by Lua that validates all seat lock keys, deletes only owned locks, removes index members, and returns structured failure tuples.
- D-12: Confirmed-seat consume replaces broad `unlockAllSeats(userId, showtimeId)` cleanup for payment confirmation.
- D-24: Production must keep the existing no-`REDIS_URL` hard fail and must never silently use `InMemoryRedis`.
</decision_coverage>

<interfaces>
Implement these exact exports in `apps/api/src/modules/booking/booking.service.ts`:
```typescript
export const LOCK_EXPIRED_MESSAGE = '좌석 점유 시간이 만료되었습니다. 좌석을 다시 선택해주세요.';
export const LOCK_OTHER_OWNER_MESSAGE = '이미 다른 사용자가 선택한 좌석입니다.';
export const ASSERT_OWNED_SEAT_LOCKS_LUA = `...`;
export const CONSUME_OWNED_SEAT_LOCKS_LUA = `...`;
export type SeatLockOwnershipFailureReason = 'MISSING' | 'OTHER_OWNER';

async assertOwnedSeatLocks(userId: string, showtimeId: string, seatIds: string[]): Promise<void>;
async consumeOwnedSeatLocks(
  userId: string,
  showtimeId: string,
  seatIds: string[],
): Promise<{ consumedSeatIds: string[] }>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BookingService ownership Lua helpers</name>
  <files>apps/api/src/modules/booking/booking.service.ts, apps/api/src/modules/booking/__tests__/booking.service.spec.ts, apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts</files>
  <read_first>
    .planning/phases/19-seat-lock-ownership-enforcement/19-CONTEXT.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-RESEARCH.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-PATTERNS.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-VALIDATION.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-01-SUMMARY.md
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
    apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
  </read_first>
  <behavior>
    - `assertOwnedSeatLocks()` throws `ConflictException(LOCK_EXPIRED_MESSAGE)` for missing/expired/released locks.
    - `assertOwnedSeatLocks()` throws `ConflictException(LOCK_OTHER_OWNER_MESSAGE)` for other-user locks.
    - `consumeOwnedSeatLocks()` checks all requested locks first, then deletes only those locks and removes only matching `user-seats` and `locked-seats` members.
  </behavior>
  <action>
    Modify `booking.service.ts` per D-01, D-02, D-03, D-04, D-11, and D-12.

    Do not add a reservation-level lock token, a new client payload field, or a shared schema change for lock ownership per D-02. The only ownership source is `GET {showtimeId}:seat:{seatId}` equals authenticated `userId`.

    Add exported constants:
    - `LOCK_EXPIRED_MESSAGE = '좌석 점유 시간이 만료되었습니다. 좌석을 다시 선택해주세요.'`
    - `LOCK_OTHER_OWNER_MESSAGE = '이미 다른 사용자가 선택한 좌석입니다.'`
    - `ASSERT_OWNED_SEAT_LOCKS_LUA`
    - `CONSUME_OWNED_SEAT_LOCKS_LUA`

    `ASSERT_OWNED_SEAT_LOCKS_LUA` must contain a marker comment `-- ASSERT_OWNED_SEAT_LOCKS_LUA`, use only `KEYS[i]` for seat lock keys, use `ARGV[1]` as `userId`, `ARGV[2..]` as `seatIds`, and return:
    - `{1, 'OK', tostring(#ARGV - 1), ''}` when all owners match.
    - `{0, 'MISSING', seatId, ''}` when `GET KEYS[i]` returns false/nil.
    - `{0, 'OTHER_OWNER', seatId, owner}` when owner differs from `ARGV[1]`.

    `CONSUME_OWNED_SEAT_LOCKS_LUA` must contain a marker comment `-- CONSUME_OWNED_SEAT_LOCKS_LUA`, use `KEYS[1] = {showtimeId}:user-seats:{userId}`, `KEYS[2] = {showtimeId}:locked-seats`, `KEYS[3..] = {showtimeId}:seat:{seatId}`, `ARGV[1] = userId`, `ARGV[2..] = seatIds`, run a full validation loop before any delete, then `DEL KEYS[i]`, `SREM KEYS[1] ARGV[i - 1]`, and `SREM KEYS[2] ARGV[i - 1]` for each requested seat. It must return the same failure tuples as assert and `{1, 'OK', tostring(#ARGV - 1), ''}` on success.

    Add `private lockConflictFromResult(result: [number, string, string, string]): ConflictException | null` or equivalent local mapper. Map `MISSING` to `LOCK_EXPIRED_MESSAGE`; map `OTHER_OWNER` to `LOCK_OTHER_OWNER_MESSAGE`. Treat both Lua scripts and InMemoryRedis emulator results as the unified ownership tuple contract: `[status, reasonOrOK, seatIdOrCount, ownerOrEmpty]`.

    Add methods:
    ```typescript
    async assertOwnedSeatLocks(userId: string, showtimeId: string, seatIds: string[]): Promise<void>
    async consumeOwnedSeatLocks(userId: string, showtimeId: string, seatIds: string[]): Promise<{ consumedSeatIds: string[] }>
    ```
    `assertOwnedSeatLocks()` must call `this.redis.eval(ASSERT_OWNED_SEAT_LOCKS_LUA, seatIds.length, ...seatLockKeys, userId, ...seatIds)`.
    `consumeOwnedSeatLocks()` must call `this.redis.eval(CONSUME_OWNED_SEAT_LOCKS_LUA, 2 + seatIds.length, userSeatsKey, lockedSeatsKey, ...seatLockKeys, userId, ...seatIds)`.

    Use exact key builders inline:
    - `const userSeatsKey = \`{${showtimeId}}:user-seats:${userId}\`;`
    - `const lockedSeatsKey = \`{${showtimeId}}:locked-seats\`;`
    - `const seatLockKeys = seatIds.map((seatId) => \`{${showtimeId}}:seat:${seatId}\`);`

    Update Wave 0 tests only where needed to import the exported script/message constants rather than duplicating them.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- booking.service --reporter=verbose</automated>
    <automated>pnpm --filter @grabit/api test:integration -- booking.service.integration --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export const LOCK_EXPIRED_MESSAGE|export const LOCK_OTHER_OWNER_MESSAGE|export const ASSERT_OWNED_SEAT_LOCKS_LUA|export const CONSUME_OWNED_SEAT_LOCKS_LUA" apps/api/src/modules/booking/booking.service.ts` prints matches.
    - `rg -n "assertOwnedSeatLocks\\(userId: string, showtimeId: string, seatIds: string\\[\\]\\)|consumeOwnedSeatLocks\\(userId: string, showtimeId: string, seatIds: string\\[\\]\\)" apps/api/src/modules/booking/booking.service.ts` prints matches.
    - `rg -n "this.redis.eval\\(\\s*ASSERT_OWNED_SEAT_LOCKS_LUA|this.redis.eval\\(\\s*CONSUME_OWNED_SEAT_LOCKS_LUA|2 \\+ seatIds.length" apps/api/src/modules/booking/booking.service.ts` prints matches.
    - `rg -n "\\{\\$\\{showtimeId\\}\\}:seat:\\$\\{seatId\\}|\\{\\$\\{showtimeId\\}\\}:user-seats:\\$\\{userId\\}|\\{\\$\\{showtimeId\\}\\}:locked-seats" apps/api/src/modules/booking/booking.service.ts` prints matches.
    - The booking unit and integration commands pass for the ownership helper cases.
  </acceptance_criteria>
  <done>`BookingService` exposes fail-closed ownership helpers backed by explicit-key Valkey Lua and the Wave 0 BookingService/Valkey tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add InMemoryRedis ownership script parity</name>
  <files>apps/api/src/modules/booking/providers/redis.provider.ts, apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts</files>
  <read_first>
    .planning/phases/19-seat-lock-ownership-enforcement/19-CONTEXT.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-RESEARCH.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-PATTERNS.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-VALIDATION.md
    .planning/phases/19-seat-lock-ownership-enforcement/19-01-SUMMARY.md
    apps/api/src/modules/booking/providers/redis.provider.ts
    apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
    apps/api/src/modules/booking/booking.service.ts
  </read_first>
  <behavior>
    - Local/test InMemoryRedis mirrors real Valkey ownership tuple returns and side effects.
    - Production still hard-fails when `NODE_ENV=production` and `REDIS_URL` is empty.
  </behavior>
  <action>
    Extend `InMemoryRedis.eval()` dispatch in `redis.provider.ts` without changing production fallback behavior from D-24.

    Add dispatch branches before the existing arity-only lock/unlock branches:
    ```typescript
    if (script.includes('ASSERT_OWNED_SEAT_LOCKS_LUA')) {
      return this.evalAssertOwnedSeatLocks(keys, args);
    }
    if (script.includes('CONSUME_OWNED_SEAT_LOCKS_LUA')) {
      return this.evalConsumeOwnedSeatLocks(keys, args);
    }
    ```

    Add private methods:
    ```typescript
    private evalAssertOwnedSeatLocks(keys: string[], args: string[]): [number, string, string, string]
    private evalConsumeOwnedSeatLocks(keys: string[], args: string[]): [number, string, string, string]
    ```

    `evalAssertOwnedSeatLocks` must treat `args[0]` as userId, `args.slice(1)` as seatIds, and `keys` as seat lock keys. Return `[0, 'MISSING', seatId, '']` when `this.store.get(lockKey)` is undefined, `[0, 'OTHER_OWNER', seatId, owner]` when owner differs, and `[1, 'OK', String(seatIds.length), '']` on success.

    `evalConsumeOwnedSeatLocks` must treat `keys[0]` as userSeatsKey, `keys[1]` as lockedSeatsKey, `keys.slice(2)` as seat lock keys, `args[0]` as userId, and `args.slice(1)` as seatIds. It must use the same tuple shape as `evalAssertOwnedSeatLocks`: return `[0, 'MISSING', seatId, '']` when a requested lock is absent, `[0, 'OTHER_OWNER', seatId, owner]` when owner differs, and `[1, 'OK', String(seatIds.length), '']` on success. First perform the full validation. Only after validation succeeds, delete each requested lock key, clear its timer/expiry maps, `SREM` each requested seat from userSeatsKey and lockedSeatsKey, and leave unrelated same-showtime locks untouched.

    Update `redis.provider.spec.ts` tests from Plan 01 to import or use the exact marker scripts from `booking.service.ts` if that keeps the test stable. Keep the production hard-fail test unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- redis.provider booking.service --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "evalAssertOwnedSeatLocks|evalConsumeOwnedSeatLocks|ASSERT_OWNED_SEAT_LOCKS_LUA|CONSUME_OWNED_SEAT_LOCKS_LUA" apps/api/src/modules/booking/providers/redis.provider.ts` prints matches.
    - `rg -n "Silent InMemoryRedis fallback is disabled|REDIS_URL is required in production" apps/api/src/modules/booking/providers/redis.provider.ts apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` prints matches.
    - `rg -n "preserves unrelated same-showtime locks|OTHER_OWNER|MISSING" apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` prints matches.
    - `pnpm --filter @grabit/api test -- redis.provider booking.service --reporter=verbose` exits 0.
  </acceptance_criteria>
  <done>InMemoryRedis emulates the new ownership Lua scripts exactly enough for local/unit tests and does not weaken production Valkey safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reservation service -> BookingService | Reservation/payment code will depend on BookingService as the lock authorization primitive. |
| BookingService -> Valkey | Lua scripts read and delete Valkey lock keys atomically. |
| local tests -> InMemoryRedis | Local mock must not hide behavior that production Valkey rejects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering / Elevation of privilege | `assertOwnedSeatLocks` | mitigate | Task 1 compares every `{showtimeId}:seat:{seatId}` value to authenticated `userId` and throws `409` on mismatch/missing. |
| T-19-02 | Tampering | `consumeOwnedSeatLocks` | mitigate | Task 1 deletes only requested owned lock keys and SREM members; Task 2 preserves unrelated locks in InMemoryRedis tests. |
| T-19-03 | Tampering / DoS | Valkey Lua | mitigate | Task 1 uses explicit `KEYS` for all seat/index keys and `{showtimeId}` hash tags; integration tests run against real Valkey. |
| T-19-04 | Elevation of privilege | existing pending `orderId` | transfer | Plan 19-03 wires `assertOwnedSeatLocks` into existing pending reservation return path. |
| T-19-05 | Tampering / Repudiation | post-Toss sold transition | transfer | Plan 19-03 wires `consumeOwnedSeatLocks` before DB sold transition and compensation on failure. |
| T-19-06 | Repudiation | confirm/complete UI | transfer | Plan 19-04 renders server lock rejection messages and failed confirmation UI. |
</threat_model>

<verification>
Plan-level verification:
- `pnpm --filter @grabit/api test -- reservation.service booking.service redis.provider`
- `pnpm --filter @grabit/api test:integration -- booking.service.integration`
</verification>

<success_criteria>
- `assertOwnedSeatLocks()` and `consumeOwnedSeatLocks()` are implemented in `BookingService`.
- Missing/released locks and other-user locks map to the exact Korean conflict messages from D-14.
- Consume helper validates all seats before deleting any key.
- InMemoryRedis parity tests and real Valkey integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/19-seat-lock-ownership-enforcement/19-02-SUMMARY.md`.
</output>
