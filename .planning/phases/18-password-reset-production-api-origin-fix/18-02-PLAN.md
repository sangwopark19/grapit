---
phase: "18-password-reset-production-api-origin-fix"
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - ".planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md"
autonomous: false
requirements:
  - DEBT-01
  - CUTOVER-01
  - CUTOVER-02
  - CUTOVER-03
  - CUTOVER-04
  - CUTOVER-05
  - CUTOVER-06
user_setup:
  - service: "Production Grabit mailbox and browser session"
    why: "Password reset email -> confirm -> login smoke requires a known password-based production account, mailbox access, and browser Network evidence."
    env_vars: []
    dashboard_config:
      - task: "Use a known password-based production account. If none exists, create one through https://heygrabit.com/auth before UAT; do not use a social-only account because enumeration defense suppresses email sending for users without passwordHash."
  - service: "Cloud Run / Sentry operator access"
    why: "UAT evidence must pin web/api production revisions or image digests and record email-service Sentry status without leaking secrets."
    env_vars: []
    dashboard_config:
      - task: "Use gcloud access for Cloud Run revision/image evidence and Sentry dashboard access for component:email-service recent count when available."
must_haves:
  truths:
    - "A known password-based production user receives the password reset email from no-reply@heygrabit.com."
    - "The reset email link opens the production confirm page and confirm submit posts to https://api.heygrabit.com/api/v1/auth/password-reset/confirm."
    - "The changed password logs in successfully on https://heygrabit.com/auth."
    - "Production UAT evidence is tied to the deployed grabit-web and grabit-api Cloud Run revisions or image digests that served the smoke."
    - "Sentry/email observability evidence records either component:email-service recent zero-count after UAT or a specific captured event id without PII."
    - "Automated web/API regression commands pass after Plan 01."
    - "UAT evidence avoids full email addresses, reset tokens, and other PII."
  artifacts:
    - path: ".planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md"
      provides: "Production password reset smoke evidence and sign-off"
    - path: "apps/web/lib/__tests__/api-url.test.ts"
      provides: "API base URL contract regression evidence"
    - path: "apps/web/lib/__tests__/next-config.test.ts"
      provides: "production rewrite guard evidence"
    - path: "apps/api/src/modules/auth/email/email.service.spec.ts"
      provides: "CUTOVER-06 email regression evidence"
  key_links:
    - from: "https://heygrabit.com/auth/reset-password"
      to: "production mailbox"
      via: "password reset request"
      pattern: "no-reply@heygrabit.com"
    - from: "production confirm page"
      to: "https://api.heygrabit.com/api/v1/auth/password-reset/confirm"
      via: "browser Network POST"
      pattern: "api\\.heygrabit\\.com/api/v1/auth/password-reset/confirm"
    - from: "18-HUMAN-UAT.md"
      to: "Cloud Run / Sentry email observability"
      via: "revision-scoped log query and Sentry notes"
      pattern: "Resend send failed"
    - from: "18-HUMAN-UAT.md"
      to: "Cloud Run deployed revisions"
      via: "grabit-web and grabit-api revision/image digest fields"
      pattern: "latestReadyRevisionName|image digest"
---

<objective>
Phase 18의 production smoke evidence를 남긴다. Plan 01의 코드 계약이 실제 production password reset email -> confirm -> login happy path에서 동작하는지 자동 regression과 human UAT artifact로 검증한다.

Purpose: DEBT-01과 CUTOVER-01..06이 코드상 green일 뿐 아니라 production Cloud Run, public API origin, Resend sender, login flow에서 함께 닫혔음을 증명한다.
Output: `18-HUMAN-UAT.md` with automated gate results, production reset flow evidence, Cloud Run/Sentry notes, and sign-off.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/18-password-reset-production-api-origin-fix/18-RESEARCH.md
@.planning/phases/18-password-reset-production-api-origin-fix/18-VALIDATION.md
@.planning/phases/18-password-reset-production-api-origin-fix/18-PATTERNS.md
@.planning/phases/18-password-reset-production-api-origin-fix/18-REVIEWS.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-03-SUMMARY.md
@.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md

@.github/workflows/deploy.yml
@apps/web/lib/api-url.ts
@apps/web/app/auth/reset-password/page.tsx
@apps/web/next.config.ts
@apps/api/src/modules/auth/auth.service.ts
@apps/api/src/modules/auth/email/email.service.ts

<interfaces>
Deployment API origin build arg that must remain true:
```yaml
--build-arg NEXT_PUBLIC_API_URL=${{ vars.CLOUD_RUN_API_URL }}
```

Production final email state from Phase 15:
```text
RESEND_FROM_EMAIL = no-reply@heygrabit.com
Resend domain = heygrabit.com verified
grabit-api serving revision included real RESEND_API_KEY and EmailService Sentry capture branch
```

Manual smoke target:
```text
https://heygrabit.com/auth/reset-password
https://api.heygrabit.com/api/v1/auth/password-reset/confirm
https://heygrabit.com/auth
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 18 production UAT evidence artifact</name>
  <files>.planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md (create)
    - .planning/phases/18-password-reset-production-api-origin-fix/18-RESEARCH.md
    - .planning/phases/18-password-reset-production-api-origin-fix/18-VALIDATION.md
    - .planning/phases/18-password-reset-production-api-origin-fix/18-PATTERNS.md
    - .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md
    - .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-03-SUMMARY.md
    - .planning/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Create `.planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md` in the Phase 15 UAT style, but with Phase 18 checks. Include these exact top-level sections:
    `# Phase 18 HUMAN-UAT -- Password reset production API origin fix`,
    `## Pre-conditions`,
    `## Automated Gate`,
    `## Deployment Revision Evidence`,
    `## SC-1: Production password reset email request`,
    `## SC-2: Confirm submit public API origin evidence`,
    `## SC-3: Login success after password change`,
    `## SC-4: Cloud Run and Sentry observation`,
    `## PII and Token Redaction Rules`,
    `## Sign-off`.

    The artifact must state the exact environment contract: local dev may use relative `/api` and dev-only `localhost:8080` rewrites; preview and production must set `NEXT_PUBLIC_API_URL` to their public API origin; production expected value is `https://api.heygrabit.com`. It must state that UAT must use a known password-based production account and must not use a social-only account. Add password-based production account preparation guidance: prefer an existing password account, otherwise create one through `https://heygrabit.com/auth` before UAT; verify readiness by logging in once before reset; record only the email domain in the artifact.

    `## Deployment Revision Evidence` must include blank fields and exact commands for pinning production evidence:
    `gcloud run services describe grabit-web --region=asia-northeast3 --project=grapit-491806 --format='value(status.latestReadyRevisionName)'`,
    `gcloud run services describe grabit-api --region=asia-northeast3 --project=grapit-491806 --format='value(status.latestReadyRevisionName)'`,
    `gcloud run revisions describe <WEB_REVISION> --region=asia-northeast3 --project=grapit-491806 --format='value(status.imageDigest)'`,
    `gcloud run revisions describe <API_REVISION> --region=asia-northeast3 --project=grapit-491806 --format='value(status.imageDigest)'`.
    If `status.imageDigest` is unavailable, require recording the service image string from `spec.template.spec.containers[0].image` and the Git SHA tag instead.

    It must include checkboxes and blank fields for timestamp, account domain only, web revision, web image digest or Git SHA tag, api revision, api image digest or Git SHA tag, email From `no-reply@heygrabit.com`, subject `[Grabit] 비밀번호 재설정`, inbox-not-spam result, confirm POST URL `https://api.heygrabit.com/api/v1/auth/password-reset/confirm`, confirm POST status `200`, login success, revision-scoped Cloud Logging notes, and Sentry `component:email-service` recent 24h count. For Sentry, require one of these explicit statements: `Sentry component:email-service recent 24h count: 0 after UAT` or `Sentry component:email-service captured event id: <redacted-id>`.

    In the redaction section, include the exact sentence `Reset tokens MUST NOT be recorded.` and explicitly forbid recording full email addresses, reset tokens, reset-link or screenshot URLs containing token query parameters, raw Resend API keys, authorization headers, bearer token values, JWTs, cookies, raw passwords, or secret values. Avoid writing example token query strings and literal auth-header examples in the artifact so Task 3's negative grep remains meaningful.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md` exits 0.
    - `rg -n "Phase 18 HUMAN-UAT|NEXT_PUBLIC_API_URL|https://api\\.heygrabit\\.com|localhost:8080|password-based production account|create one through https://heygrabit\\.com/auth|Deployment Revision Evidence|latestReadyRevisionName|status\\.imageDigest|no-reply@heygrabit\\.com|\\[Grabit\\] 비밀번호 재설정|api\\.heygrabit\\.com/api/v1/auth/password-reset/confirm|Sentry component:email-service recent 24h count|PII and Token Redaction Rules|Reset tokens MUST NOT be recorded|authorization headers|bearer token values|raw passwords" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md` prints matches.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md && rg -n "Phase 18 HUMAN-UAT|NEXT_PUBLIC_API_URL|https://api\\.heygrabit\\.com|localhost:8080|password-based production account|create one through https://heygrabit\\.com/auth|Deployment Revision Evidence|latestReadyRevisionName|status\\.imageDigest|no-reply@heygrabit\\.com|\\[Grabit\\] 비밀번호 재설정|api\\.heygrabit\\.com/api/v1/auth/password-reset/confirm|Sentry component:email-service recent 24h count|PII and Token Redaction Rules|Reset tokens MUST NOT be recorded|authorization headers|bearer token values|raw passwords" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md</automated>
  </verify>
  <done>`18-HUMAN-UAT.md` exists with all required production smoke fields, environment contract, password-based account preparation, revision/image evidence fields, Sentry evidence fields, and redaction rules.</done>
</task>

<task type="auto">
  <name>Task 2: Run automated Phase 18 regression gate and record evidence</name>
  <files>.planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md
    - .planning/phases/18-password-reset-production-api-origin-fix/18-VALIDATION.md
    - apps/web/package.json
    - apps/api/package.json
    - .github/workflows/deploy.yml
    - apps/web/lib/api-url.ts
    - apps/web/lib/__tests__/api-url.test.ts
    - apps/web/lib/__tests__/next-config.test.ts
    - apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx
    - apps/api/src/modules/auth/auth.service.spec.ts
    - apps/api/src/modules/auth/email/email.service.spec.ts
  </read_first>
  <action>
    Run this fast focused smoke first and append the result to the `## Automated Gate` section of `18-HUMAN-UAT.md` with command, timestamp, and exit code:
    `pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx`.
    Record this first result with the label `Fast focused smoke`.
    This is the first feedback loop for Task 2; if it fails, stop before running the longer regression gate and fix/report the focused failure.

    After the fast smoke is green, run the full Phase 18 regression recording gate commands and append their results to the same section with command, timestamp, and exit code. Required full-gate commands:
    `pnpm --filter @grabit/web typecheck`,
    `pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts lib/__tests__/next-config.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx`,
    `pnpm --filter @grabit/web test`,
    `pnpm --filter @grabit/api test -- src/modules/auth/auth.service.spec.ts src/modules/auth/email/email.service.spec.ts`.

    Also inspect `.github/workflows/deploy.yml` and record whether it contains the exact build arg `--build-arg NEXT_PUBLIC_API_URL=${{ vars.CLOUD_RUN_API_URL }}` plus the `Validate production public API URL` fail-fast step added by Plan 01. Record only pass/fail and command summaries; do not paste large test output.
  </action>
  <acceptance_criteria>
    - `rg -n "Fast focused smoke|pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx|pnpm --filter @grabit/web typecheck|pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts lib/__tests__/next-config.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx|pnpm --filter @grabit/web test|pnpm --filter @grabit/api test -- src/modules/auth/auth.service.spec.ts src/modules/auth/email/email.service.spec.ts|--build-arg NEXT_PUBLIC_API_URL=\\$\\{\\{ vars\\.CLOUD_RUN_API_URL \\}\\}|Validate production public API URL|exit code: 0" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md` prints matches after commands pass.
    - `rg -n -- "--build-arg NEXT_PUBLIC_API_URL=\\$\\{\\{ vars\\.CLOUD_RUN_API_URL \\}\\}|Validate production public API URL|CLOUD_RUN_API_URL must be set for web production build" .github/workflows/deploy.yml` prints matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx</automated>
    <phase_gate>pnpm --filter @grabit/web typecheck && pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts lib/__tests__/next-config.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx && pnpm --filter @grabit/web test && pnpm --filter @grabit/api test -- src/modules/auth/auth.service.spec.ts src/modules/auth/email/email.service.spec.ts</phase_gate>
  </verify>
  <done>Focused and full web/API auth-email regressions are green and the deployment build-arg contract is recorded in `18-HUMAN-UAT.md`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify production email-to-confirm-to-login smoke</name>
  <files>.planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md
    - .planning/phases/18-password-reset-production-api-origin-fix/18-RESEARCH.md
    - .planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-HUMAN-UAT.md
    - apps/api/src/modules/auth/auth.service.ts
    - apps/api/src/modules/auth/email/email.service.ts
  </read_first>
  <action>
    After Plan 01 is deployed to production, first pin the serving Cloud Run evidence. Run these commands if gcloud auth is available, otherwise copy them into `18-HUMAN-UAT.md` as pending operator commands and do not mark SC-4 PASS:
    `gcloud run services describe grabit-web --region=asia-northeast3 --project=grapit-491806 --format='value(status.latestReadyRevisionName)'`,
    `gcloud run services describe grabit-api --region=asia-northeast3 --project=grapit-491806 --format='value(status.latestReadyRevisionName)'`,
    `gcloud run revisions describe <WEB_REVISION> --region=asia-northeast3 --project=grapit-491806 --format='value(status.imageDigest)'`,
    `gcloud run revisions describe <API_REVISION> --region=asia-northeast3 --project=grapit-491806 --format='value(status.imageDigest)'`.
    If `status.imageDigest` is unavailable, record the `spec.template.spec.containers[0].image` value and Git SHA tag for each service. This addresses the review concern that UAT evidence must be tied to the actual deployed revisions.

    Use a known password-based production account. If no such account exists, create one through `https://heygrabit.com/auth` before the reset smoke and verify it can log in once before requesting reset. Open `https://heygrabit.com/auth/reset-password`, request a reset email, verify the email is from `no-reply@heygrabit.com` with subject `[Grabit] 비밀번호 재설정` and lands in inbox, not spam. Open the reset link, submit a new password, and record browser Network evidence that the confirm POST URL is exactly `https://api.heygrabit.com/api/v1/auth/password-reset/confirm` with status `200`. Then log in at `https://heygrabit.com/auth` using the new password and record login success.

    Record evidence in `18-HUMAN-UAT.md`. Use account domain only, such as `gmail.com`; do not record full email address, reset token, reset-password URL containing a token query, cookies, JWT, raw screenshot URL containing a token query, authorization headers, bearer token values, raw passwords, or secret values. The sender address `no-reply@heygrabit.com` is allowed and should still be recorded for From verification.

    Run or record the revision-scoped Cloud Logging check for `Resend send failed` after the UAT timestamp and record the result as `Resend send failed: empty`. Add Sentry evidence in SC-4 using one of these exact forms: `Sentry component:email-service recent 24h count: 0 after UAT` or `Sentry component:email-service captured event id: <redacted-id>`. Use the zero-count form only when dashboard or API inspection actually confirms no email-service errors in the window; otherwise leave SC-4 pending rather than using negative-only evidence.
  </action>
  <acceptance_criteria>
    - `rg -n "SC-1.*PASS|SC-2.*PASS|SC-3.*PASS|SC-4.*PASS|grabit-web.*latestReadyRevisionName|grabit-api.*latestReadyRevisionName|image digest|Git SHA tag|https://api\\.heygrabit\\.com/api/v1/auth/password-reset/confirm|confirm POST status: 200|login success|no-reply@heygrabit\\.com|inbox.*not spam|Resend send failed: empty|Sentry component:email-service (recent 24h count: 0 after UAT|captured event id:)" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md` prints matches after UAT is filled.
    - `! rg -n "\\?\\s*token\\s*=|/auth/reset-password\\?[^[:space:]]*token\\s*=|token\\s*[:=]|eyJ[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+|Authorization\\s*:|Bearer\\s+|Cookie\\s*:|Set-Cookie\\s*:|accessToken|refreshToken|RESEND_API_KEY|resend-api-key|JWT_SECRET|JWT_REFRESH_SECRET|SECRET\\s*=|API_KEY\\s*=|password\\s*[:=]|secret\\s*[:=]" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md` exits 0.
    - `! (rg -n -o -i "[[:alnum:]._%+-]+@[[:alnum:].-]+\\.[[:alpha:]]{2,}" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md | rg -v ":no-reply@heygrabit\\.com$")` exits 0, allowing only the sender address even when another email appears on the same line.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "SC-1.*PASS|SC-2.*PASS|SC-3.*PASS|SC-4.*PASS|grabit-web.*latestReadyRevisionName|grabit-api.*latestReadyRevisionName|image digest|Git SHA tag|https://api\\.heygrabit\\.com/api/v1/auth/password-reset/confirm|confirm POST status: 200|login success|no-reply@heygrabit\\.com|inbox.*not spam|Resend send failed: empty|Sentry component:email-service (recent 24h count: 0 after UAT|captured event id:)" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md && ! rg -n "\\?\\s*token\\s*=|/auth/reset-password\\?[^[:space:]]*token\\s*=|token\\s*[:=]|eyJ[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+|Authorization\\s*:|Bearer\\s+|Cookie\\s*:|Set-Cookie\\s*:|accessToken|refreshToken|RESEND_API_KEY|resend-api-key|JWT_SECRET|JWT_REFRESH_SECRET|SECRET\\s*=|API_KEY\\s*=|password\\s*[:=]|secret\\s*[:=]" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md && ! (rg -n -o -i "[[:alnum:]._%+-]+@[[:alnum:].-]+\\.[[:alpha:]]{2,}" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md | rg -v ":no-reply@heygrabit\\.com$")</automated>
  </verify>
  <done>Production password reset email -> confirm -> login flow is recorded as PASS with public API origin evidence, Cloud Run revision/image evidence, Sentry email-service evidence, no full user email addresses except allowed sender `no-reply@heygrabit.com`, and no reset token, reset-link token query, JWT, cookie, auth header, bearer value, raw password, or secret leakage in the artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production user mailbox -> browser | The reset link carries reset authority and must not be copied into artifacts. |
| Browser -> `api.heygrabit.com` | Confirm submit must use the public API origin and prove CORS/origin contract. |
| Production account identity -> UAT artifact | Evidence must avoid account enumeration and PII disclosure. |
| Cloud Run logs / Sentry -> planning artifact | Observability evidence must record status without secret or recipient leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Information Disclosure | `18-HUMAN-UAT.md` | mitigate | Redaction rules forbid full email addresses, reset tokens, JWTs, cookies, and secret values; Task 3 grep blocks common token/cookie patterns. |
| T-18-07 | Elevation of Privilege | reset email link handling | mitigate | UAT records only status and origin evidence; no reset link URL or token value is persisted. |
| T-18-08 | Information Disclosure | password reset request UAT | mitigate | Use a known password-based account and record only domain, preserving enumeration-defense expectations. |
| T-18-09 | Denial of Service | production API origin / CORS drift | mitigate | Task 3 records browser Network host `api.heygrabit.com` and status `200`; Task 2 records deploy build arg contract. |
| T-18-10 | Information Disclosure | email telemetry | mitigate | UAT checks Cloud Logging/Sentry for failure status and records `toDomain`/component notes only; no recipient local part is written. |
| T-18-11 | Repudiation | production smoke evidence | mitigate | Task 3 records `grabit-web` and `grabit-api` latest ready revisions plus image digest or Git SHA tag before marking UAT PASS. |
| T-18-12 | Information Disclosure | redaction gates | mitigate | Task 3 blocks token spacing variants, auth headers, bearer values, same-line user email leakage, raw passwords, and secret assignment patterns. |
</threat_model>

<verification>
Overall phase gate:

```bash
pnpm --filter @grabit/web typecheck
pnpm --filter @grabit/web test -- lib/__tests__/api-url.test.ts lib/__tests__/next-config.test.ts app/auth/reset-password/__tests__/reset-password.test.tsx
pnpm --filter @grabit/web test
pnpm --filter @grabit/api test -- src/modules/auth/auth.service.spec.ts src/modules/auth/email/email.service.spec.ts
test -f .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md
rg -n "https://api\\.heygrabit\\.com/api/v1/auth/password-reset/confirm|login success|no-reply@heygrabit\\.com|Resend send failed.*empty" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md
rg -n "grabit-web.*latestReadyRevisionName|grabit-api.*latestReadyRevisionName|Sentry component:email-service (recent 24h count: 0 after UAT|captured event id:)" .planning/phases/18-password-reset-production-api-origin-fix/18-HUMAN-UAT.md
```
</verification>

<success_criteria>
- `18-HUMAN-UAT.md` contains the local/preview/production API base URL contract.
- `18-HUMAN-UAT.md` records all required automated commands with exit code 0.
- `18-HUMAN-UAT.md` records production password reset email receipt from `no-reply@heygrabit.com`, confirm POST to `https://api.heygrabit.com/api/v1/auth/password-reset/confirm`, and login success.
- `18-HUMAN-UAT.md` records `grabit-web` and `grabit-api` Cloud Run revision/image evidence for the production smoke.
- `18-HUMAN-UAT.md` records Sentry `component:email-service` recent zero-count or captured event id evidence.
- `18-HUMAN-UAT.md` contains no full reset token, token query URL, JWT, cookie header, auth header, bearer value, raw password, user email address, or raw secret value.
- All Phase 18 requirement IDs are covered by plan frontmatter and UAT evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/18-password-reset-production-api-origin-fix/18-02-SUMMARY.md`.
</output>
