---
phase: 16
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - apps/web/components/legal/terms-markdown.tsx
  - apps/web/env.d.ts
  - apps/web/next.config.ts
  - apps/web/app/legal/layout.tsx
  - apps/web/app/legal/terms/page.tsx
  - apps/web/app/legal/privacy/page.tsx
  - apps/web/app/legal/marketing/page.tsx
autonomous: true
requirements: [D-01, D-02, D-09, D-10, D-11, D-12, D-13, D-14]
must_haves:
  truths:
    - "TermsMarkdown 이 showH1?: boolean prop 을 받으며 default false (dialog 호환) (D-09)"
    - "showH1=true 시 H1 이 text-display 토큰으로 렌더 (UI-SPEC §Typography L93-103) (D-09)"
    - "TermsMarkdown 의 h2/h3/p/ul/ol/li/strong/a/hr 매핑 9개가 변경되지 않는다 (UI-SPEC L129)"
    - "/legal/{terms,privacy,marketing} 3개 page.tsx 가 server component 로 신규 생성된다 (D-01)"
    - "각 page.tsx 가 export const dynamic='force-static' 명시 + Metadata export 보유 (D-10/D-13)"
    - "각 page.tsx 가 raw-loader 로 MD import (`?raw` query) + <TermsMarkdown showH1>{md}</TermsMarkdown> 렌더만 (D-10)"
    - "apps/web/app/legal/layout.tsx 가 server component <main mx-auto max-w-[760px] ...> 컨테이너만 담당 (D-02)"
    - "각 page.tsx 의 Metadata 가 robots: { index: isProd, follow: isProd } (env-driven 분기) 와 alternates.canonical(절대 URL) 을 export 한다 (D-12/D-13 + codex HIGH-4 안전 보강)"
    - "isProd 분기 = process.env.GRABIT_ENV === 'production' — prod 빌드에서만 index 허용, preview/staging/dev 는 noindex 강제 (codex HIGH-4 처리)"
    - "각 page.tsx 본문 / 산출 HTML 어디에도 application/ld+json 스크립트가 등장하지 않는다 (D-14)"
    - "env.d.ts 에 `*.md?raw` 모듈 declaration 추가 — vitest 가 page.tsx 모듈 로드 가능"
    - "next.config.ts 의 turbopack rules 에 `*.md?raw` 패턴 추가 — Turbopack 빌드도 `?raw` query 인식"
    - "Wave 0 의 terms-markdown.test (5건) 와 metadata.test (6건) 이 GREEN 으로 전환된다"
  artifacts:
    - path: "apps/web/components/legal/terms-markdown.tsx"
      provides: "showH1?: boolean prop + buildComponents(showH1) 분기"
      contains: "showH1"
    - path: "apps/web/env.d.ts"
      provides: "`*.md?raw` 모듈 declaration (vitest jsdom 호환)"
      contains: "*.md?raw"
    - path: "apps/web/next.config.ts"
      provides: "Turbopack `*.md?raw` raw-loader rule 추가"
      contains: "*.md?raw"
    - path: "apps/web/app/legal/layout.tsx"
      provides: "3개 페이지 공통 <main> container (D-02)"
      contains: "max-w-[760px]"
    - path: "apps/web/app/legal/terms/page.tsx"
      provides: "이용약관 SSG 페이지 (force-static + Metadata)"
      contains: "force-static"
    - path: "apps/web/app/legal/privacy/page.tsx"
      provides: "개인정보처리방침 SSG 페이지"
      contains: "force-static"
    - path: "apps/web/app/legal/marketing/page.tsx"
      provides: "마케팅 수신 동의 SSG 페이지"
      contains: "force-static"
  key_links:
    - from: "apps/web/app/legal/{terms,privacy,marketing}/page.tsx"
      to: "@/content/legal/*.md?raw"
      via: "Vite native ?raw query (vitest) + Turbopack `*.md?raw` rule (Next 16 build)"
      pattern: "import \\w+Md from '@/content/legal/.+\\.md\\?raw'"
    - from: "apps/web/app/legal/{terms,privacy,marketing}/page.tsx"
      to: "@/components/legal/terms-markdown"
      via: "server component 가 'use client' 컴포넌트 import (React boundary)"
      pattern: "<TermsMarkdown showH1>"
    - from: "apps/web/app/legal/layout.tsx"
      to: "Next App Router file-system routing"
      via: "공유 layout — 3개 page.tsx 가 children 으로 들어감"
      pattern: "<main"
---

<objective>
Wave 1 — Phase 16 의 코어 4 파일 신규 (`legal/layout.tsx` + 3개 `page.tsx`) + 1 파일 수정 (`terms-markdown.tsx` showH1 prop 추가) + 2 파일 declaration 보강 (`env.d.ts` 의 `*.md?raw` + `next.config.ts` 의 Turbopack rule). 본 plan 완료 시 (1) `/legal/terms`, `/legal/privacy`, `/legal/marketing` 3개 라우트가 SSG 산출물로 빌드되며, (2) Wave 0 의 terms-markdown.test (5건) + metadata.test (6건) 가 GREEN 으로 전환된다.

Purpose: 본 phase 의 핵심 capability 인 "공개 URL 3개 게시" 를 실현. **`?raw` query 채택 이유 (revision 결정):** vitest 는 .md 의 raw-loader transformer 가 없어 `import termsMd from '@/content/legal/terms-of-service.md'` 만으로는 모듈 로드 실패 → metadata.test 의 dynamic import (`await import('@/app/legal/terms/page')`) 도 cascade 실패. Vite native `?raw` query 는 vitest 환경에서 자동 string import 처리, Turbopack 도 `*.md?raw` rule 추가 시 동일 동작 — 양쪽 환경 호환성 확보. dialog 호환은 showH1 default false 로 lock — signup-step2 의 `<TermsMarkdown>{md}</TermsMarkdown>` 호출은 prop 미지정 → H1 미렌더 (D-11 dialog UX 불변). signup-step2 의 기존 `.md` (no query) import 는 변경하지 않음 (D-11 lock — Plan 05 책임 영역도 아님).

Output: 4 신규 + 3 수정 = 7 파일. 신규 의존성 0건. 신규 디자인 토큰 0건.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-legal-pages-launch-url/16-CONTEXT.md
@.planning/phases/16-legal-pages-launch-url/16-RESEARCH.md
@.planning/phases/16-legal-pages-launch-url/16-PATTERNS.md
@.planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md
@.planning/phases/16-legal-pages-launch-url/16-VALIDATION.md
@.planning/phases/16-legal-pages-launch-url/16-01-SUMMARY.md

@apps/web/components/legal/terms-markdown.tsx
@apps/web/app/layout.tsx
@apps/web/app/booking/[performanceId]/layout.tsx
@apps/web/next.config.ts
@apps/web/env.d.ts
@apps/web/vitest.config.ts
@apps/web/content/legal/terms-of-service.md
@apps/web/content/legal/privacy-policy.md
@apps/web/content/legal/marketing-consent.md

<interfaces>
<!-- 본 plan 이 의존하는 contracts. PATTERNS.md + RESEARCH.md 에서 인용. -->

From apps/web/components/legal/terms-markdown.tsx (수정 대상 — 현재 시그니처):
```typescript
const components: Components = {
  h1: () => null,                                              // ← Phase 16 분기 대상
  h2: (props) => (<h2 className="mt-6 text-base font-semibold text-gray-900 first:mt-0" {...props} />),
  h3: (props) => (<h3 className="mt-4 text-caption font-semibold text-gray-800" {...props} />),
  p:  (props) => (<p className="mt-2 text-caption leading-relaxed text-gray-700" {...props} />),
  ul: (props) => (<ul className="mt-2 ml-5 list-disc text-caption leading-relaxed text-gray-700" {...props} />),
  ol: (props) => (<ol className="mt-2 ml-5 list-decimal text-caption leading-relaxed text-gray-700" {...props} />),
  li: (props) => <li className="mt-1" {...props} />,
  strong: (props) => <strong className="font-semibold text-gray-900" {...props} />,
  a:  (props) => (<a className="text-primary underline hover:text-primary/80" target="_blank" rel="noopener noreferrer" {...props} />),
  hr: () => <hr className="my-4 border-gray-200" />,
};
export function TermsMarkdown({ children }: { children: string }) {
  return <ReactMarkdown components={components} remarkPlugins={[remarkGfm]}>{children}</ReactMarkdown>;
}
```
**변경 범위 lock (UI-SPEC §Typography 변경 금지):** h2/h3/p/ul/ol/li/strong/a/hr 9개 매핑 변경 금지. h1 만 분기 처리 + showH1 prop 추가.

From apps/web/next.config.ts L44-49 (raw-loader rule — 본 plan 에서 `*.md?raw` 추가):
```typescript
turbopack: {
  root: resolve(__dirname, '../../'),
  rules: {
    '*.md': { as: '*.js', loaders: ['raw-loader'] },          // ← 기존 (signup-step2 의존)
    '*.md?raw': { as: '*.js', loaders: ['raw-loader'] },      // ← Plan 02 신규 (page.tsx 의존)
  },
},
```

From apps/web/env.d.ts (현재 — 본 plan 에서 `?raw` declaration 추가):
```typescript
// 기존 (signup-step2 의존):
declare module '*.md' { const content: string; export default content; }

// Plan 02 신규 (page.tsx + vitest dynamic import 호환):
declare module '*.md?raw' { const content: string; export default content; }
```

From apps/web/vitest.config.ts (변경 없음 — Vite native `?raw` 지원):
```typescript
// Vite (vitest 의존성) 는 `?raw` query 를 native 로 인식 — 별도 transformer / plugin 불필요.
// 따라서 page.tsx 가 `import x from '...md?raw'` 패턴이면 vitest jsdom 환경에서 모듈 로드 성공.
```

From apps/web/app/booking/[performanceId]/layout.tsx (전 파일, 9 LOC — analog for legal/layout.tsx):
```tsx
export default function BookingLayout({ children }: { children: React.ReactNode }) {
  return (<div className="flex min-h-dvh flex-col bg-white">{children}</div>);
}
```
- server component (no `'use client'`).
- 단일 wrapper + `{children}`.
- root layout 의 Header/Footer 자동 상속 → 중복 렌더 금지.

From apps/web/app/layout.tsx (Metadata export analog — codebase 유일):
```tsx
import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Grabit - 공연 티켓 예매',
  description: '공연, 전시, 스포츠 등 라이브 엔터테인먼트 티켓 예매 플랫폼',
};
```
**상속 동작 (RESEARCH §Tech Stack Verification L178):** root metadata 의 title 은 page metadata 의 title 로 덮어써짐 (Next 16 자동 병합).

From apps/web/components/auth/signup-step2.tsx L15-19, L191 (dialog 호환 검증 대상):
```tsx
import termsOfServiceMd from '@/content/legal/terms-of-service.md';     // ← 변경 금지 (D-11)
import privacyPolicyMd from '@/content/legal/privacy-policy.md';
import marketingConsentMd from '@/content/legal/marketing-consent.md';
// ...
<TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown>       // ← 변경 금지 (D-11)
```
- prop 미지정 → showH1 default false → h1 미렌더 (D-11 dialog UX 불변).
- 기존 `*.md` (no query) import 도 그대로 유지 — `*.md` rule 과 `*.md?raw` rule 양쪽 활성화로 양립.
- 본 plan 의 task 는 signup-step2 변경 금지 (Wave 4 의 plan 05 책임 영역과도 무관 — D-11 invariant).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TermsMarkdown showH1 prop 추가 (D-09) + env.d.ts/next.config.ts 의 `*.md?raw` 보강</name>
  <files>apps/web/components/legal/terms-markdown.tsx, apps/web/env.d.ts, apps/web/next.config.ts</files>
  <read_first>
    - apps/web/components/legal/terms-markdown.tsx (전 파일 49 LOC — 정확한 현재 매핑 확인)
    - apps/web/env.d.ts (전 파일 8 LOC)
    - apps/web/next.config.ts L44-49 (turbopack rules)
    - apps/web/vitest.config.ts (`?raw` Vite native 지원 — 별도 plugin 없음)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L160-231 (showH1 prop 패치 spec)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L341-395 (Critical Code Patterns §showH1)
    - .planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md L91-119 (H1 className 정확값)
    - apps/web/components/legal/__tests__/terms-markdown.test.tsx (Wave 0 산출물 — 본 task 가 GREEN 전환시킬 대상)
  </read_first>
  <action>
    **1-A. `apps/web/components/legal/terms-markdown.tsx` 전 파일 재작성** (RESEARCH §Critical Code Patterns L344-389 그대로):

    ```tsx
    'use client';

    import ReactMarkdown, { type Components } from 'react-markdown';
    import remarkGfm from 'remark-gfm';

    /**
     * TermsMarkdown
     * Phase 9 DEBT-02: 법률 MD 원문을 Dialog 안에서 prose-like 스타일로 렌더.
     * Phase 16 D-09: showH1 prop 추가 — default false 로 dialog 호환 유지.
     *   - dialog (signup-step2): showH1 미지정 → DialogTitle 이 제목 역할
     *   - 공개 페이지 (/legal/{terms,privacy,marketing}): showH1=true → text-display H1 렌더
     *
     * UI-SPEC §Typography L82-119 타이포 매핑 적용 (h2~hr 변경 금지).
     * Phase 16 review MED-4: table/thead/tbody/tr/th/td 매핑 추가 (privacy 개정 이력 GFM 표용).
     *   기존 9개 매핑 + 신규 6개 매핑 — codex 리뷰 옵션 A 채택 (REVIEWS adjudication accept).
     */
    const baseComponents: Components = {
      h2: (props) => (
        <h2 className="mt-6 text-base font-semibold text-gray-900 first:mt-0" {...props} />
      ),
      h3: (props) => (
        <h3 className="mt-4 text-caption font-semibold text-gray-800" {...props} />
      ),
      p: (props) => (
        <p className="mt-2 text-caption leading-relaxed text-gray-700" {...props} />
      ),
      ul: (props) => (
        <ul className="mt-2 ml-5 list-disc text-caption leading-relaxed text-gray-700" {...props} />
      ),
      ol: (props) => (
        <ol className="mt-2 ml-5 list-decimal text-caption leading-relaxed text-gray-700" {...props} />
      ),
      li: (props) => <li className="mt-1" {...props} />,
      strong: (props) => <strong className="font-semibold text-gray-900" {...props} />,
      a: (props) => (
        <a
          className="text-primary underline hover:text-primary/80"
          target="_blank"
          rel="noopener noreferrer"
          {...props}
        />
      ),
      hr: () => <hr className="my-4 border-gray-200" />,
      // Phase 16 review MED-4 (codex) — TermsMarkdown table 매핑 추가.
      // privacy-policy.md 의 `### 개정 이력` GFM 표 + 향후 약관 개정 이력에서 사용.
      // 모바일 overflow 가드: table 자체가 아닌 wrapper div 가 overflow-x-auto 처리.
      table: (props) => (
        <div className="mt-4 -mx-2 overflow-x-auto">
          <table className="min-w-full text-caption text-gray-700 border-collapse" {...props} />
        </div>
      ),
      thead: (props) => <thead className="bg-gray-50" {...props} />,
      tbody: (props) => <tbody className="divide-y divide-gray-200" {...props} />,
      tr: (props) => <tr {...props} />,
      th: (props) => (
        <th className="px-3 py-2 text-left font-semibold text-gray-900 border-b border-gray-300" {...props} />
      ),
      td: (props) => (
        <td className="px-3 py-2 align-top border-b border-gray-200" {...props} />
      ),
    };

    const buildComponents = (showH1: boolean): Components => ({
      ...baseComponents,
      h1: showH1
        ? (props) => (
            <h1
              className="text-display font-semibold leading-[1.2] text-gray-900"
              {...props}
            />
          )
        : () => null,
    });

    export function TermsMarkdown({
      children,
      showH1 = false,
    }: {
      children: string;
      showH1?: boolean;
    }) {
      return (
        <ReactMarkdown components={buildComponents(showH1)} remarkPlugins={[remarkGfm]}>
          {children}
        </ReactMarkdown>
      );
    }
    ```

    핵심 계약:
    - `'use client'` 유지.
    - `baseComponents` 에는 h1 미포함 — `buildComponents(showH1)` 가 분기 처리.
    - **default `false`** 시그니처 보장 — `showH1?: boolean` + `showH1 = false`.
    - **H1 className 정확값:** `text-display font-semibold leading-[1.2] text-gray-900` (UI-SPEC L93-103).
    - **9개 기존 매핑 변경 금지** — h2/h3/p/ul/ol/li/strong/a/hr (UI-SPEC §Typography L129 lock).
    - JSDoc 코멘트 업데이트 — Phase 16 D-09 명시.

    **1-B. `apps/web/env.d.ts` 에 `*.md?raw` declaration 추가**

    현재 파일 내용:

        // Type declarations for non-TS modules imported at build time.
        // Phase 9 DEBT-02: MD files imported as raw strings via Turbopack raw-loader rule.

        declare module '*.md' {
          const content: string;
          export default content;
        }

    다음으로 변경 (기존 `*.md` declaration 보존 + `*.md?raw` 추가):

        // Type declarations for non-TS modules imported at build time.
        // Phase 9 DEBT-02: MD files imported as raw strings via Turbopack raw-loader rule.
        // Phase 16 D-10: `*.md?raw` query — Vite native (vitest jsdom) + Turbopack rule 양립.

        declare module '*.md' {
          const content: string;
          export default content;
        }

        declare module '*.md?raw' {
          const content: string;
          export default content;
        }

    **금지:**
    - 기존 `declare module '*.md'` 제거 금지 — signup-step2.tsx 의 `import termsOfServiceMd from '@/content/legal/terms-of-service.md'` 가 의존 (D-11 lock).

    **1-C. `apps/web/next.config.ts` 의 turbopack rules 에 `*.md?raw` 추가**

    현재 L44-49:

        turbopack: {
          root: resolve(__dirname, '../../'),
          rules: {
            '*.md': { as: '*.js', loaders: ['raw-loader'] },
          },
        },

    다음으로 변경 (기존 `*.md` rule 보존 + `*.md?raw` 추가):

        turbopack: {
          root: resolve(__dirname, '../../'),
          rules: {
            '*.md': { as: '*.js', loaders: ['raw-loader'] },
            '*.md?raw': { as: '*.js', loaders: ['raw-loader'] },
          },
        },

    **금지:**
    - 기존 `'*.md'` rule 제거 금지 — signup-step2.tsx 의 `*.md` import 가 의존.
    - `next.config.ts` 의 다른 영역 (output, images, rewrites, withSentryConfig 등) 변경 금지.
    - `'use client'` / 신규 import 추가 금지.
    - `text-2xl` / `text-3xl` 등 신규 타이포 토큰 도입 금지 (UI-SPEC L128).
    - `font-medium` / `font-bold` 사용 금지 (UI-SPEC L131).
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run components/legal/__tests__/terms-markdown.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "showH1" apps/web/components/legal/terms-markdown.tsx` 매치 ≥ 4건 (시그니처 + JSDoc + buildComponents + H1 분기)
    - `grep -F "showH1 = false" apps/web/components/legal/terms-markdown.tsx` exit 0 (default false 보장)
    - `grep -F "text-display font-semibold leading-[1.2] text-gray-900" apps/web/components/legal/terms-markdown.tsx` exit 0 (정확한 className)
    - `grep -c "mt-6 text-base font-semibold text-gray-900 first:mt-0" apps/web/components/legal/terms-markdown.tsx` ≥ 1 (h2 매핑 변경 없음)
    - `grep -c "mt-2 text-caption leading-relaxed text-gray-700" apps/web/components/legal/terms-markdown.tsx` ≥ 1 (p 매핑 변경 없음)
    - `grep -F "table:" apps/web/components/legal/terms-markdown.tsx` exit 0 (MED-4 table 매핑 추가 — REVIEWS adjudication accept)
    - `grep -F "thead:" apps/web/components/legal/terms-markdown.tsx` exit 0
    - `grep -F "tbody:" apps/web/components/legal/terms-markdown.tsx` exit 0
    - `grep -F "th:" apps/web/components/legal/terms-markdown.tsx` exit 0
    - `grep -F "td:" apps/web/components/legal/terms-markdown.tsx` exit 0
    - `grep -F "overflow-x-auto" apps/web/components/legal/terms-markdown.tsx` exit 0 (모바일 overflow 가드 — codex MED-4)
    - `grep -F "declare module '*.md'" apps/web/env.d.ts` exit 0 (기존 declaration 보존)
    - `grep -F "declare module '*.md?raw'" apps/web/env.d.ts` exit 0 (신규 declaration)
    - `grep -F "'*.md':" apps/web/next.config.ts` exit 0 (기존 rule 보존)
    - `grep -F "'*.md?raw':" apps/web/next.config.ts` exit 0 (신규 rule)
    - vitest: terms-markdown.test 의 5개 it() 가 모두 PASS (Wave 0 RED → Wave 1 GREEN 전환 검증)
    - `! grep -E "text-2xl|text-3xl|font-medium|font-bold" apps/web/components/legal/terms-markdown.tsx` (신규 토큰 금지)
    - typecheck pass: `pnpm --filter @grabit/web typecheck` exit 0
  </acceptance_criteria>
  <done>terms-markdown.tsx 가 showH1 prop 시그니처 + buildComponents 분기 + 기존 9개 매핑 보존 상태로 수정됨. env.d.ts + next.config.ts 가 `*.md?raw` 양립 (기존 `*.md` rule 보존). terms-markdown.test 5개 it() 가 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: legal/layout.tsx 신규 + 3개 page.tsx 신규 (D-01, D-02, D-10, D-12, D-13) — `?raw` query 채택</name>
  <files>apps/web/app/legal/layout.tsx, apps/web/app/legal/terms/page.tsx, apps/web/app/legal/privacy/page.tsx, apps/web/app/legal/marketing/page.tsx</files>
  <read_first>
    - apps/web/app/booking/[performanceId]/layout.tsx (analog — 9 LOC server-component layout)
    - apps/web/app/layout.tsx (Metadata export analog)
    - apps/web/next.config.ts L44-50 (Task 1 에서 추가된 `*.md?raw` rule 활성 확인)
    - apps/web/env.d.ts (Task 1 에서 추가된 `*.md?raw` declaration 활성 확인)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L34-159 (layout + page 패턴 spec)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L289-339 (Critical Code Patterns §Layout, §Page)
    - .planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md L264-306 (Layout container + Page.tsx 패턴 정확값)
    - .planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md L201-210 (Copywriting Contract — 3개 title/description 정확값)
    - apps/web/app/legal/__tests__/metadata.test.ts (Wave 0 산출물 — 본 task 가 GREEN 전환시킬 대상; dynamic import 가 `?raw` 질의 page.tsx 도 정상 로드해야 함)
  </read_first>
  <action>
    **2-A. `apps/web/app/legal/layout.tsx` 신규 생성** (RESEARCH §Critical Code Patterns L293-303):

    ```tsx
    import type { ReactNode } from 'react';

    /**
     * Legal pages 공통 layout (D-02)
     * 3개 페이지 (/legal/{terms,privacy,marketing}) 의 공유 container.
     * - <main> landmark 책임 (root layout 에는 <main> 없음 — LayoutShell 이 GNB/Footer/MobileTabBar 만 렌더)
     * - reading-optimized max-w-[760px] (UI-SPEC §Spacing L52-65)
     * - GNB/Footer/MobileTabBar 는 LayoutShell 자동 상속 — 본 layout 에서 중복 렌더 금지
     */
    export default function LegalLayout({ children }: { children: ReactNode }) {
      return (
        <main className="mx-auto w-full max-w-[760px] px-4 pt-8 pb-16 md:px-6 md:pt-12 md:pb-24">
          {children}
        </main>
      );
    }
    ```

    **금지:**
    - `'use client'` 부착 금지.
    - `force-static` / `Metadata` export 금지 (page.tsx 책임 — Next 16 의 Route Segment Config 는 layout 에서 child 에 자동 상속되지 않음, RESEARCH L165).
    - breadcrumb / back-link / wrapper card 추가 금지 (UI-SPEC §Component Inventory L277).
    - GNB/Footer 중복 렌더 금지.
    - max-w 절대값 변경 금지 (UI-SPEC L63 — `760px` reading-optimized lock).

    **2-B. `apps/web/app/legal/terms/page.tsx` 신규 생성** (UI-SPEC L284-306 + RESEARCH L312-334; **import 에 `?raw` query 사용**):

    ```tsx
    import type { Metadata } from 'next';
    import termsMd from '@/content/legal/terms-of-service.md?raw';
    import { TermsMarkdown } from '@/components/legal/terms-markdown';

    export const dynamic = 'force-static';

    /**
     * Phase 16 review HIGH-4 (codex) — preview/staging 환경 noindex 강제.
     * D-12 의 "검색 엔진 색인 허용" lock 은 prod 기준. placeholder leak 위험 기간 동안
     * preview/non-prod 환경이 색인되지 않도록 환경 변수로 분기.
     * GRABIT_ENV: 'production' | 'preview' | 'development' | undefined
     * - production → index:true, follow:true (D-12)
     * - 그 외 (preview/staging/dev) → index:false, follow:false (HIGH-4 안전 보강)
     * 빌드 시점 평가 — Cloud Run / GitHub Actions 배포 단계에서 GRABIT_ENV 주입 필요.
     */
    const isProd = process.env.GRABIT_ENV === 'production';

    export const metadata: Metadata = {
      title: '이용약관 — Grabit',
      description: 'Grabit 서비스 이용 조건과 회원·회사의 권리·의무를 안내합니다.',
      alternates: {
        canonical: 'https://heygrabit.com/legal/terms',
      },
      robots: {
        index: isProd,
        follow: isProd,
      },
    };

    export default function TermsPage() {
      return <TermsMarkdown showH1>{termsMd}</TermsMarkdown>;
    }
    ```

    **2-C. `apps/web/app/legal/privacy/page.tsx` 신규 생성** (동일 패턴, import + metadata 만 다름):

    ```tsx
    import type { Metadata } from 'next';
    import privacyMd from '@/content/legal/privacy-policy.md?raw';
    import { TermsMarkdown } from '@/components/legal/terms-markdown';

    export const dynamic = 'force-static';

    // Phase 16 review HIGH-4: prod 만 index. preview/staging 은 noindex (placeholder 누출 차단)
    const isProd = process.env.GRABIT_ENV === 'production';

    export const metadata: Metadata = {
      title: '개인정보처리방침 — Grabit',
      description:
        'Grabit이 수집·이용하는 개인정보 항목과 처리 목적, 보유 기간 및 이용자의 권리를 안내합니다.',
      alternates: {
        canonical: 'https://heygrabit.com/legal/privacy',
      },
      robots: {
        index: isProd,
        follow: isProd,
      },
    };

    export default function PrivacyPage() {
      return <TermsMarkdown showH1>{privacyMd}</TermsMarkdown>;
    }
    ```

    **2-D. `apps/web/app/legal/marketing/page.tsx` 신규 생성**:

    ```tsx
    import type { Metadata } from 'next';
    import marketingMd from '@/content/legal/marketing-consent.md?raw';
    import { TermsMarkdown } from '@/components/legal/terms-markdown';

    export const dynamic = 'force-static';

    // Phase 16 review HIGH-4: prod 만 index. preview/staging 은 noindex
    const isProd = process.env.GRABIT_ENV === 'production';

    export const metadata: Metadata = {
      title: '마케팅 정보 수신 동의 — Grabit',
      description:
        'Grabit이 발송하는 마케팅 정보의 수신 항목, 전송 수단, 동의 거부 권리를 안내합니다.',
      alternates: {
        canonical: 'https://heygrabit.com/legal/marketing',
      },
      robots: {
        index: isProd,
        follow: isProd,
      },
    };

    export default function MarketingPage() {
      return <TermsMarkdown showH1>{marketingMd}</TermsMarkdown>;
    }
    ```

    **3개 page.tsx 공통 계약:**
    - `'use client'` 부착 금지 (server component 유지).
    - **MD import 는 `?raw` query 강제** — `from '@/content/legal/terms-of-service.md?raw'`. `?raw` 없는 형태(`...md`)는 vitest 환경에서 모듈 로드 실패 → metadata.test cascade 실패. signup-step2 의 `.md` import 와 의도적으로 분리(D-11 lock).
    - **`?raw` query 채택 결정 (codex MED-3 partial accept):** Codex 는 `*.md` / `*.md?raw` 두 loader contract 공존을 long-term 유지보수 위험으로 지적했으나, 두 rule 모두 **동일한 raw-loader** 를 사용하는 query 변형일 뿐 — 실제 loader contract 는 단일. signup-step2 의 기존 `*.md` import 는 D-11 dialog UX lock 으로 변경 금지이며, page.tsx 의 `?raw` 는 vitest 환경 호환을 위한 의도적 분기. REVIEWS adjudication: **partial accept** — Plan 본문 (이 주석) 으로 결정 기록, 코드 변경 없음. 추가 위험 (`*.md` 와 `*.md?raw` 가 다른 결과를 내는 케이스) 은 turbopack rule 정의 상 발생 불가.
    - 변수명: `termsMd` / `privacyMd` / `marketingMd` (camelCase + Md suffix).
    - title 의 dash 는 **em-dash `—`** (U+2014) — UI-SPEC L227 명시.
    - description 정확값 (UI-SPEC L208-210 의 글자수 lock).
    - canonical absolute URL (RESEARCH L176 — root layout 에 metadataBase 미설정이므로 absolute 필수).
    - **`robots: { index: isProd, follow: isProd }` (codex HIGH-4 처리, REVIEWS adjudication accept):** `const isProd = process.env.GRABIT_ENV === 'production';` 으로 분기. prod 만 index 허용 (D-12 spirit 유지), preview/staging/dev 는 noindex 강제로 placeholder leak 기간 색인 회피. `GRABIT_ENV` 환경 변수 주입은 Cloud Run 배포 단계 또는 GitHub Actions deploy.yml 책임 — 본 plan 에서는 코드 분기만 추가. `GRABIT_ENV` 미설정 시 (local dev) → `isProd === false` → noindex. 안전한 default.
    - `<TermsMarkdown showH1>{md}</TermsMarkdown>` 만 반환 — wrapper `<article>`/`<section>` 추가 금지 (UI-SPEC L308).
    - JSON-LD 추가 금지 (D-14).
    - 3 파일 모두 ~10 줄 이내의 default export function (isProd 분기 포함).

    **route group `(legal)` 사용 금지** (D-01) — `/legal/` 가 URL 에 노출되어야 함. 디렉토리 이름 그대로 `apps/web/app/legal/{terms,privacy,marketing}/page.tsx`.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run app/legal/__tests__/metadata.test.ts --reporter=verbose 2>&1 | tail -30 && pnpm --filter @grabit/web typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 4개 파일 신규 존재: `test -f apps/web/app/legal/layout.tsx && test -f apps/web/app/legal/terms/page.tsx && test -f apps/web/app/legal/privacy/page.tsx && test -f apps/web/app/legal/marketing/page.tsx`
    - layout.tsx 가 max-w-[760px] 보유: `grep -F 'max-w-[760px]' apps/web/app/legal/layout.tsx` exit 0
    - layout.tsx 가 `<main` element 사용: `grep -F '<main' apps/web/app/legal/layout.tsx` exit 0
    - layout.tsx 가 `'use client'` 미부착: `! grep -F "'use client'" apps/web/app/legal/layout.tsx`
    - 3개 page.tsx 모두 force-static: `grep -l "force-static" apps/web/app/legal/{terms,privacy,marketing}/page.tsx | wc -l` 결과 3
    - 3개 page.tsx 모두 `'use client'` 미부착: `! grep -l "'use client'" apps/web/app/legal/{terms,privacy,marketing}/page.tsx` exit 0
    - **3개 page.tsx 모두 `?raw` query 로 MD import:** `grep -c "from '@/content/legal/.*\\.md?raw'" apps/web/app/legal/{terms,privacy,marketing}/page.tsx | grep -c ":1"` ≥ 3 (각 1건씩)
    - **3개 page.tsx 가 query 없는 `.md` import 사용 안함:** `! grep -E "from '@/content/legal/[a-z-]+\\.md'" apps/web/app/legal/{terms,privacy,marketing}/page.tsx` exit 0
    - title 정확값 3건 (em-dash): `grep -F '이용약관 — Grabit' apps/web/app/legal/terms/page.tsx && grep -F '개인정보처리방침 — Grabit' apps/web/app/legal/privacy/page.tsx && grep -F '마케팅 정보 수신 동의 — Grabit' apps/web/app/legal/marketing/page.tsx`
    - canonical absolute URL: `grep -F 'https://heygrabit.com/legal/terms' apps/web/app/legal/terms/page.tsx && grep -F 'https://heygrabit.com/legal/privacy' apps/web/app/legal/privacy/page.tsx && grep -F 'https://heygrabit.com/legal/marketing' apps/web/app/legal/marketing/page.tsx`
    - robots env-driven 분기 명시 (codex HIGH-4): `grep -c "index: isProd" apps/web/app/legal/{terms,privacy,marketing}/page.tsx | grep -c ":1"` ≥ 3 (각 1건)
    - robots env-driven 분기 follow: `grep -c "follow: isProd" apps/web/app/legal/{terms,privacy,marketing}/page.tsx | grep -c ":1"` ≥ 3
    - isProd 정의: `grep -c "process.env.GRABIT_ENV === 'production'" apps/web/app/legal/{terms,privacy,marketing}/page.tsx | grep -c ":1"` ≥ 3 (각 page 가 자체 isProd 변수 정의)
    - **하드코딩된 `index: true` 부재 (HIGH-4 회귀 가드):** `! grep -E "index: true" apps/web/app/legal/{terms,privacy,marketing}/page.tsx` exit 0
    - showH1 prop 부착: `grep -c "<TermsMarkdown showH1>" apps/web/app/legal/{terms,privacy,marketing}/page.tsx` ≥ 3
    - wrapper element 0건: `! grep -E '<article|<section' apps/web/app/legal/{terms,privacy,marketing}/page.tsx`
    - JSON-LD 0건: `! grep -rE "application/ld\\+json" apps/web/app/legal/`
    - vitest: metadata.test 6개 it() 가 모두 PASS (Wave 0 RED → Wave 1 GREEN 전환 검증)
    - typecheck pass: `pnpm --filter @grabit/web typecheck` exit 0
  </acceptance_criteria>
  <done>4개 파일 신규 + typecheck PASS + metadata.test 6개 GREEN. 3개 page.tsx 가 server component 로 SSG prerender 가능 상태이며, `?raw` query 로 vitest jsdom 환경에서도 모듈 로드 성공.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 산출물 검증 — /legal/* 3개 라우트가 정적 prerender 됨</name>
  <files>(no file changes — build artifact verification)</files>
  <read_first>
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L483-488 (R2 SSG 산출물 누락 위험)
    - .planning/phases/16-legal-pages-launch-url/16-VALIDATION.md L45 (16-02-02 smoke 검증)
  </read_first>
  <action>
    Task 1, 2 완료 후 production build 를 실행하여 SSG 산출물이 정상 생성되는지 검증한다. **코드 변경 없음** — 검증 전용 task.

    실행 단계:

    1. **Clean build (codex MED-2 처리, REVIEWS adjudication accept)** — stale `.next` 가 placeholder/이전 build artifact 검증을 통과시키는 false positive 회피.
       ```bash
       rm -rf apps/web/.next
       GRABIT_ENV=production pnpm --filter @grabit/web build
       ```
       - **`GRABIT_ENV=production` 주입 필수** — Task 2 의 `isProd` 분기가 prod 빌드에서 `index:true` 로 평가되는지 검증. prod build 산출물 grep 으로 robots/canonical 도 검증 가능.
       - 출력 로그에서 다음 패턴 확인:
         - `○  /legal/terms` (또는 `Static`/`prerendered` 표기 — Next 16 build CLI 의 정확한 표기는 환경에 따라 다름)
         - `○  /legal/privacy`
         - `○  /legal/marketing`
       - **기대 동작**: 3 라우트 모두 Static (SSG prerender) 표기. Dynamic 으로 표시되면 force-static 이 작동 안한 것 → Task 2 의 page.tsx 검토 필요.
       - 빌드 실패 시 (e.g. raw-loader 깨짐, MD 파싱 에러, Metadata 타입 에러, `?raw` query 미인식) 즉시 Task 1, 2 로 회귀.

    1.5. **Preview/staging 환경 noindex 분기 검증 (codex HIGH-4 추가 검증):**
       ```bash
       rm -rf apps/web/.next
       pnpm --filter @grabit/web build  # GRABIT_ENV 미설정 → isProd === false
       ```
       - 빌드 산출물 HTML 에 `<meta name="robots" content="noindex,nofollow">` 또는 동등 표기 인젝트 확인:
         ```bash
         find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'noindex' {} \;
         ```
       - 결과 ≥ 1 매치 시 HIGH-4 안전 분기 동작 확인. 본 검증 후 다시 prod build 로 복귀 (위 단계 1 재실행).

    2. **빌드 산출물 파일 존재 확인:**
       ```bash
       find apps/web/.next -path '*legal*' \( -name '*.html' -o -name '*.rsc' -o -name '*.meta' \) 2>/dev/null
       ```
       - 최소 3개 이상의 파일 매치 기대.
       - Next 16 의 정확한 산출물 path 는 `.next/server/app/legal/{terms,privacy,marketing}/page.{html,rsc}` 또는 `.next/server/app/legal/{terms,privacy,marketing}.{html,rsc}` 형태일 수 있음 — wildcard 로 캐치.

    3. **HTML 산출물 본문 검증 (한글 텍스트 인라인 확인):**
       ```bash
       find apps/web/.next -path '*legal*terms*' -name '*.html' -exec grep -l '이용약관' {} \;
       find apps/web/.next -path '*legal*privacy*' -name '*.html' -exec grep -l '개인정보처리방침' {} \;
       find apps/web/.next -path '*legal*marketing*' -name '*.html' -exec grep -l '마케팅 수신 동의' {} \;
       ```
       - 각 검색이 최소 1 파일 hit 해야 함 — JS 미실행 환경에서도 본문 100% 노출 확인 (검색엔진 / 크롤러 대응).
       - `text-display` className 도 inline 됐는지 spot check: `find ... -name '*.html' -exec grep -l 'text-display' {} \;`.

    4. **Metadata 산출물 검증 (D-13 calculator):**
       ```bash
       find apps/web/.next -path '*legal*terms*' -name '*.html' -exec grep -l 'rel="canonical"' {} \;
       find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'name="robots"' {} \;
       ```
       - canonical 매치 — `https://heygrabit.com/legal/terms` 등이 link rel="canonical" 로 인젝트.
       - robots 매치 — `<meta name="robots" content="index, follow">` 인젝트.

    검증 결과를 SUMMARY.md 에 정리. **빌드 산출물의 정확한 파일 경로는 환경/Next 16 minor 버전마다 미묘하게 다를 수 있으므로 wildcard 검색 + 결과 path 를 SUMMARY 에 기록**.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web build 2>&1 | tail -50 && find apps/web/.next -path '*legal*' \( -name '*.html' -o -name '*.rsc' \) 2>/dev/null | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf apps/web/.next && GRABIT_ENV=production pnpm --filter @grabit/web build` exit 0 (clean prod 빌드 성공 — codex MED-2)
    - 빌드 로그에 `/legal/terms`, `/legal/privacy`, `/legal/marketing` 3개 라우트가 등장하며 Static (`○` 또는 동등 표기) 으로 분류됨
    - **prod 빌드 산출물에 `index, follow` 인젝트 (HIGH-4 prod 분기 검증):** `find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'index, follow' {} \;` ≥ 3 매치
    - **noindex 분기 검증** (clean build with no GRABIT_ENV — non-prod):
      `rm -rf apps/web/.next && pnpm --filter @grabit/web build && find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'noindex' {} \;` ≥ 1 매치 (HIGH-4 안전 분기 동작 확인)
    - 검증 완료 후 prod 빌드 복귀: `rm -rf apps/web/.next && GRABIT_ENV=production pnpm --filter @grabit/web build` 으로 다음 wave 진입 상태 확보
    - `find apps/web/.next -path '*legal*' \( -name '*.html' -o -name '*.rsc' \) 2>/dev/null | wc -l` ≥ 3
    - 한글 본문이 HTML 산출물에 인라인됨: `find apps/web/.next -path '*legal*terms*' -name '*.html' -exec grep -l '이용약관' {} \; | wc -l` ≥ 1 (각 라우트별 동일 검증)
    - canonical 인젝트: `find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'rel="canonical"' {} \;` ≥ 1 매치
    - robots 인젝트: `find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'name="robots"' {} \;` ≥ 1 매치
    - **JSON-LD 미인젝트 (D-14):** `! grep -rE "application/ld\\+json" apps/web/app/legal/`
    - typecheck + lint 회귀 없음: `pnpm --filter @grabit/web typecheck && pnpm --filter @grabit/web lint` exit 0
  </acceptance_criteria>
  <done>빌드 산출물에 3개 legal 라우트의 정적 HTML 이 생성되고, 한글 본문 + canonical + robots 메타가 인라인됨. typecheck/lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 빌드 시점 ↔ 런타임 | raw-loader 가 build-time 에 MD → string 인라인. 런타임 fs read 0회. |
| Server component ↔ Client component | page.tsx (server) 가 TermsMarkdown ('use client') import — React boundary 자동 처리, hydration JS 동봉 |
| Search engine crawler ↔ SSG HTML | JS 미실행 환경에서도 본문 100% 노출 — D-12 색인 허용 |
| vitest jsdom ↔ Turbopack build | 두 환경 모두 `*.md?raw` query 인식 — Vite native + Turbopack rule. signup-step2 의 `*.md` (no query) 는 별도 경로로 보존. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering (showH1 default 회귀) | apps/web/components/legal/terms-markdown.tsx | mitigate | Wave 0 의 terms-markdown.test 가 default false 강제 — Task 1 의 prop 변경이 dialog 회귀 시 RED. signup-step2 의 호출 형태(`<TermsMarkdown>{...}</TermsMarkdown>`)는 Plan 05 의 변경 금지 invariant 로 추가 보호. |
| T-16-05 | Information Disclosure (SSG 산출물 누락 → 런타임 dynamic fallback) | apps/web/app/legal/{terms,privacy,marketing}/page.tsx | mitigate | Task 3 의 build artifact grep 이 3 라우트의 HTML 산출물 + 한글 본문 + canonical 인젝트를 강제. dynamic fallback 시 build 단계에서 검출 (force-static + cookies/headers 사용 시 build error). |
| T-16-06 | Tampering (vitest 환경에서 .md 모듈 로드 실패 → metadata.test cascade fail → CI 무회귀 통과 위험) | apps/web/app/legal/{terms,privacy,marketing}/page.tsx | mitigate | `?raw` query 강제 (acceptance grep) + env.d.ts 의 `*.md?raw` declaration. Vite native 처리이므로 vitest 별도 plugin 불필요. Task 2 의 acceptance: `! grep -E "from '@/content/legal/[a-z-]+\\.md'" page.tsx` 로 query 누락 차단. |
</threat_model>

<verification>
- terms-markdown.tsx 의 showH1 prop default false: `grep -F "showH1 = false" apps/web/components/legal/terms-markdown.tsx`
- env.d.ts 의 `?raw` declaration: `grep -F "declare module '*.md?raw'" apps/web/env.d.ts`
- next.config.ts 의 `?raw` rule: `grep -F "'*.md?raw':" apps/web/next.config.ts`
- 4개 신규 파일 존재 (layout + 3 page) + 3 page 모두 `?raw` import
- 빌드 성공 + 3 정적 산출물 생성: Task 3 의 검증 명령
- vitest: terms-markdown.test (5건) + metadata.test (6건) 모두 GREEN — 총 11건의 RED → GREEN 전환
- VALIDATION.md §Per-Task Verification Map 의 16-02-01/02/03 status 가 ✅ 으로 갱신될 수 있음
</verification>

<success_criteria>
- [ ] terms-markdown.tsx 가 showH1?: boolean prop 보유 (default false)
- [ ] H1 className 정확값 적용: `text-display font-semibold leading-[1.2] text-gray-900`
- [ ] 9개 기존 매핑 (h2~hr) 변경 없음
- [ ] env.d.ts 에 `*.md?raw` declaration 추가 (기존 `*.md` 보존)
- [ ] next.config.ts 의 turbopack rules 에 `*.md?raw` 추가 (기존 `*.md` 보존)
- [ ] apps/web/app/legal/layout.tsx 존재 + max-w-[760px] + `<main>` landmark
- [ ] 3개 page.tsx 모두 server component + force-static + Metadata + `<TermsMarkdown showH1>` + **`?raw` query MD import**
- [ ] em-dash title 3건 정확값
- [ ] canonical absolute URL 3건 정확값
- [ ] robots env-driven 분기 3건 명시 (`index: isProd, follow: isProd` + `process.env.GRABIT_ENV === 'production'`) — codex HIGH-4 accept
- [ ] **Clean build** (rm -rf .next + GRABIT_ENV=production) 성공 + 3 정적 HTML 산출물 + 한글 본문 + canonical + `index, follow` 인젝트 (codex MED-2)
- [ ] noindex 분기 검증 — GRABIT_ENV 미설정 build 시 `noindex` 인젝트 (codex HIGH-4)
- [ ] typecheck/lint pass
- [ ] Wave 0 의 11개 RED 케이스가 GREEN 으로 전환됨
</success_criteria>

<output>
After completion, create `.planning/phases/16-legal-pages-launch-url/16-02-SUMMARY.md`:
- 7 파일 변경 목록 + 빌드 산출물 path (정확한 wildcard 결과 기록 — Plan 04/05 의 cutover gate 가 동일 path 를 grep 함)
- terms-markdown.test (5건) + metadata.test (6건) 의 GREEN 전환 status
- 빌드 시간 + 3 라우트의 prerender 분류 (Static / Dynamic)
- env.d.ts + next.config.ts 의 `*.md?raw` 양립 검증 (signup-step2 의 `*.md` import 회귀 없음)
- 다음 wave (Plan 03 — Footer 패치) 진입 가능 여부 확인
</output>
