---
phase: 16
plan: 05
type: execute
wave: 4
depends_on: [16-02]
files_modified:
  - apps/web/components/legal/legal-draft-banner.tsx
  - apps/web/components/auth/signup-step2.tsx
  - .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md
autonomous: true
requirements: [D-05, D-07, D-08, D-11, D-15]
must_haves:
  truths:
    - "apps/web/components/legal/legal-draft-banner.tsx 파일이 codebase 에서 삭제된다"
    - "apps/web/components/auth/signup-step2.tsx 의 LegalDraftBanner import (L18) 가 제거된다"
    - "apps/web/components/auth/signup-step2.tsx 의 <LegalDraftBanner /> JSX (L190) 가 제거된다"
    - "signup-step2 의 <TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown> 호출 형태가 변경되지 않는다 (D-11 dialog UX 불변)"
    - "signup-step2 의 LEGAL_CONTENT 객체, 동의 체크박스, Dialog 구조, 핸들러 모두 변경되지 않는다 (D-11)"
    - "typecheck + build 가 회귀 없이 통과한다"
    - ".planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md 가 생성되어 cutover 게이트 (D-15) 와 placeholder leak 방지 자동 grep (D-07/D-08) 을 명세한다"
    - "HUMAN-UAT 의 cutover prereq 체크리스트가 mailbox 수신 검증, 사업자등록번호/통신판매업 신고번호/대표자명/주소/보호책임자 실값 주입, 시행일 cutover 일자 결정을 모두 포함한다 (RESEARCH §Validation Architecture L583-597 의 UAT-1~UAT-13)"
    - "HUMAN-UAT 가 빌드 산출물 grep 차단 명령 (`! grep -rE '\\[(시행일|사업자등록번호|통신판매업|보호책임자 실명):' apps/web/.next/`) 을 cutover 자동 게이트로 명세한다"
  artifacts:
    - path: "apps/web/components/auth/signup-step2.tsx"
      provides: "LegalDraftBanner import + JSX 제거된 회원가입 step 2"
      contains: "LEGAL_CONTENT"
    - path: ".planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md"
      provides: "Phase 16 cutover 게이트 — UAT-1~UAT-13 체크리스트 + 자동 grep 게이트 + 시각 회귀 항목 (D-05, D-07, D-08, D-11, D-15)"
      contains: "cutover prereq"
  key_links:
    - from: "apps/web/components/auth/signup-step2.tsx (수정 후)"
      to: "apps/web/components/legal/terms-markdown.tsx"
      via: "default showH1=false 로 dialog 호환 (Plan 02 Task 1 산출)"
      pattern: "<TermsMarkdown>"
    - from: "lucide-react AlertTriangle import"
      to: "(소멸 — legal-draft-banner.tsx 가 유일 사용처였음)"
      via: "파일 삭제로 자동 정리"
      pattern: "AlertTriangle"
    - from: ".planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md"
      to: "Plan 04 의 placeholder bracket pattern + Plan 02 의 빌드 산출물"
      via: "automated cutover gate (grep) + 사용자 manual UAT (mailbox/ Footer click / dialog 회귀)"
      pattern: "\\[(시행일|사업자등록번호|통신판매업|보호책임자 실명):"
---

<objective>
Wave 4 — 두 가지 cutover 책임을 한 plan 에 묶는다: (1) D-05 의 LegalDraftBanner 삭제 작업 (Task 1), (2) D-15 mailbox 수신 검증 + D-07 placeholder 실값 주입 + D-08 시행일 cutover 결정을 cutover 게이트로 묶는 HUMAN-UAT.md 작성 (Task 2). Plan 02 (Wave 1) 와 의존: dialog 호환성 검증 시 Plan 02 의 `showH1` default false lock 이 필요하며, HUMAN-UAT 의 빌드 산출물 grep 게이트가 Plan 02 산출물 path 를 가리킨다.

Purpose: 런칭 직전 phase 라는 시간 축에서 'draft' 안내가 더 이상 의미 없음 (D-05). 동시에 placeholder leak / mailbox 미개통 / 시행일 미확정 같은 cutover 위험을 단일 체크리스트(`16-HUMAN-UAT.md`) 로 집결시켜 cutover D-day 작업을 자동·수동 게이트 양쪽으로 보호. dialog UX 불변(D-11) 은 grep + 회귀 테스트로 lock — `<TermsMarkdown>` 호출 형태 유지(showH1 prop 부착 금지) + LEGAL_CONTENT 객체 보존 + 동의 체크박스 / Dialog 핸들러 변경 금지.

Output: 1 파일 삭제 + 1 파일 수정 + 1 신규 문서 (HUMAN-UAT.md). typecheck + build 회귀 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-legal-pages-launch-url/16-CONTEXT.md
@.planning/phases/16-legal-pages-launch-url/16-RESEARCH.md
@.planning/phases/16-legal-pages-launch-url/16-PATTERNS.md
@.planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md
@.planning/phases/16-legal-pages-launch-url/16-VALIDATION.md
@.planning/phases/16-legal-pages-launch-url/16-02-SUMMARY.md
@.planning/phases/16-legal-pages-launch-url/16-04-SUMMARY.md

@apps/web/components/legal/legal-draft-banner.tsx
@apps/web/components/auth/signup-step2.tsx

<interfaces>
<!-- 본 plan 이 의존/검증하는 contracts. -->

From apps/web/components/legal/legal-draft-banner.tsx (삭제 대상 — 25 LOC 전체):
- `import { AlertTriangle } from 'lucide-react'`
- `export function LegalDraftBanner()` — `role="note" aria-label="초안 안내"` div + `bg-warning-surface` + `text-[#8B6306]`
- 유일 사용처: signup-step2.tsx L18, L190 (RESEARCH §Implementation Approach Axis 4 verified — `grep -rn "LegalDraftBanner|legal-draft-banner" apps/web` → 1 매치 / 단 import 1, 사용 1)

From apps/web/components/auth/signup-step2.tsx (수정 대상 — 196 LOC):
- L18: `import { LegalDraftBanner } from '@/components/legal/legal-draft-banner';` ← 제거 대상
- L19: `import { TermsMarkdown } from '@/components/legal/terms-markdown';` ← 유지
- L30-34 LEGAL_CONTENT 객체 (`as const satisfies` 패턴) ← 변경 금지 (D-11)
- L190: `<LegalDraftBanner />` ← 제거 대상
- L191: `<TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown>` ← 유지 (showH1 prop 부착 금지 — Plan 02 의 default false 시그니처와 정합)

From booking confirm terms-agreement (verified 변경 영향 없음):
- RESEARCH §Implementation Approach Axis 4 [VERIFIED]: `booking/terms-agreement.tsx` 는 `TermsMarkdown` / `LegalDraftBanner` 전부 미사용 (인라인 plain text + 자체 dialog) — D-11 dialog UX 회귀 위험은 signup-step2 한 곳에 한정.

From lucide-react AlertTriangle:
- `legal-draft-banner.tsx` 가 유일 사용처 — 파일 삭제 시 import 트리에서 1건 감소. 패키지 자체 의존성 변경 0건.

From RESEARCH.md §Validation Architecture L583-597 (HUMAN-UAT 원본 표 — Task 2 인용 대상):
| # | Item | Gate |
|---|------|------|
| UAT-1 | 3개 MD 의 placeholder 모두 실값 주입 완료 (사업자등록 후) | cutover prereq |
| UAT-2 | 시행일이 cutover 일자(예: 2026-04-29)로 통일 (D-08) | cutover prereq |
| UAT-3 | mailbox 수신 검증: 외부 메일 → `support@heygrabit.com` 발송 → inbox 도착 (D-15) | cutover prereq |
| UAT-4 | mailbox 수신 검증: 외부 메일 → `privacy@heygrabit.com` 발송 → inbox 도착 (D-15) | cutover prereq |
| UAT-5 | prod 환경에서 `https://heygrabit.com/legal/terms` HTTP 200 + 본문 렌더 | post-deploy |
| UAT-6 | prod `https://heygrabit.com/legal/privacy` HTTP 200 + 본문 렌더 | post-deploy |
| UAT-7 | prod `https://heygrabit.com/legal/marketing` HTTP 200 + 본문 렌더 | post-deploy |
| UAT-8 | prod Footer 클릭: 이용약관 → `/legal/terms` 이동 | post-deploy |
| UAT-9 | prod Footer 클릭: 개인정보처리방침 → `/legal/privacy` 이동 | post-deploy |
| UAT-10 | prod Footer 클릭: 고객센터 → OS 메일 클라이언트에 `support@heygrabit.com` prefilled | post-deploy |
| UAT-11 | 회원가입 step 2 → 약관 보기 dialog: 본문 정상 + draft banner 미표시 | post-deploy |
| UAT-12 | 회원가입 step 2 → 동의 체크박스 / 다음 버튼 동작 정상 (회귀 없음, D-11) | post-deploy |
| UAT-13 | (선택) Google Search Console 색인 요청 (D-12) | post-launch |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: legal-draft-banner.tsx 파일 삭제 + signup-step2.tsx import/JSX 정리 (D-05)</name>
  <files>apps/web/components/legal/legal-draft-banner.tsx, apps/web/components/auth/signup-step2.tsx</files>
  <read_first>
    - apps/web/components/legal/legal-draft-banner.tsx (전 파일 25 LOC — 삭제 전 최종 확인)
    - apps/web/components/auth/signup-step2.tsx (전 파일 196 LOC — L18 import + L190 JSX 위치 정확히)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L235-258 (signup-step2 patch spec)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L321-333 (legal-draft-banner 삭제 영향)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L401-413 (signup-step2 patch + D-11 lock)
  </read_first>
  <action>
    **1-A. 파일 삭제: `apps/web/components/legal/legal-draft-banner.tsx`**

    실행 명령:
    ```bash
    rm apps/web/components/legal/legal-draft-banner.tsx
    ```

    혹은 git-aware 삭제 (권장):
    ```bash
    git rm apps/web/components/legal/legal-draft-banner.tsx
    ```

    **1-B. signup-step2.tsx import 제거 (L18)**

    L18 의 현재 내용:

        import { LegalDraftBanner } from '@/components/legal/legal-draft-banner';

    L18 을 완전 삭제. 다음 줄(L19) 의 `import { TermsMarkdown } from '@/components/legal/terms-markdown';` 가 삭제된 자리로 올라옴.

    **1-C. signup-step2.tsx JSX 제거 (L190)**

    L189-191 의 현재 내용:

            </DialogHeader>
            <LegalDraftBanner />
            <TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown>

    L190 (`<LegalDraftBanner />`) 한 줄만 완전 삭제. 결과:

            </DialogHeader>
            <TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown>

    **변경 후 signup-step2.tsx 의 변경 영역 최종 형태 (L1-19 import 블록):**

        'use client';

        import { useState } from 'react';
        import type { RegisterStep2Input } from '@grabit/shared';
        import { Checkbox } from '@/components/ui/checkbox';
        import { Button } from '@/components/ui/button';
        import { Separator } from '@/components/ui/separator';
        import {
          Dialog,
          DialogContent,
          DialogHeader,
          DialogTitle,
          DialogDescription,
        } from '@/components/ui/dialog';
        import termsOfServiceMd from '@/content/legal/terms-of-service.md';
        import privacyPolicyMd from '@/content/legal/privacy-policy.md';
        import marketingConsentMd from '@/content/legal/marketing-consent.md';
        import { TermsMarkdown } from '@/components/legal/terms-markdown';

    **변경 후 signup-step2.tsx 의 dialog 영역 최종 형태 (Dialog 본문):**

              {/* Terms dialog */}
              <Dialog open={dialogOpen} onOpenChange={setDialogOpen}>
                <DialogContent className="max-h-[80vh] overflow-y-auto">
                  <DialogHeader>
                    {/* WR-07: dialogKey 가 LegalKey 로 좁혀져 있어 `?.` 가 불필요. */}
                    <DialogTitle>{LEGAL_CONTENT[dialogKey].title}</DialogTitle>
                    <DialogDescription className="sr-only">
                      {LEGAL_CONTENT[dialogKey].title} 상세 내용
                    </DialogDescription>
                  </DialogHeader>
                  <TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown>
                </DialogContent>
              </Dialog>

    **D-11 invariant — 변경 금지 영역 (UI-SPEC §Component Inventory + RESEARCH §Risks R1):**
    - L30-34 LEGAL_CONTENT 객체 (`as const satisfies` 패턴) — 변경 금지.
    - L36 type LegalKey — 변경 금지.
    - L38-72 컴포넌트 함수 시그니처 / state hooks / handleSelectAll / handleViewTerms / handleSubmit — 변경 금지.
    - L74-178 JSX (전체 동의 / 개별 약관 체크박스 3개 / 이전·다음 버튼) — 변경 금지.
    - L180-192 Dialog 구조 (DialogContent / DialogHeader / DialogTitle / DialogDescription) — 변경 금지.
    - **`<TermsMarkdown>` 호출에 `showH1` prop 부착 금지** — default false 로 dialog 호환 (Plan 02 Task 1 의 시그니처와 정합).
    - L19 `import { TermsMarkdown }` — 유지 (TermsMarkdown 은 dialog 본문 렌더에 여전히 필요).
    - L15-17 3개 MD raw-loader import (`*.md`, no query) — 유지 (LEGAL_CONTENT 가 의존; Plan 02 의 `?raw` 도입은 page.tsx 한정 — D-11 lock).

    **금지:**
    - LegalDraftBanner 가 다른 사용처에서 등장하지 않는지 사전 확인 — `grep -rn "LegalDraftBanner\|legal-draft-banner" apps/web` 결과가 0건이어야 정상 (signup-step2.tsx 정리 후).
    - `<TermsMarkdown>` 의 prop 변경 (`showH1` 부착) 금지 — 부착 시 dialog 에서 H1 두 번 렌더(DialogTitle + H1) 회귀.
    - LEGAL_CONTENT 객체 키/값 변경 금지.
    - 사용 안 되는 import 자동 제거 / lint --fix 로 의도하지 않은 변경 발생 금지 — 명시적 L18 한 줄만 삭제.
    - 새 import 추가 0건.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && test ! -f apps/web/components/legal/legal-draft-banner.tsx && ! grep -E 'LegalDraftBanner|legal-draft-banner' apps/web/components/auth/signup-step2.tsx && pnpm --filter @grabit/web typecheck 2>&1 | tail -5 && pnpm --filter @grabit/web build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f apps/web/components/legal/legal-draft-banner.tsx` exit 0 (파일 삭제 확인)
    - `! grep -rE 'LegalDraftBanner|legal-draft-banner' apps/web --include='*.ts' --include='*.tsx'` exit 0 (codebase 전체에서 0 매치)
    - `! grep -E 'LegalDraftBanner|legal-draft-banner' apps/web/components/auth/signup-step2.tsx` (D-05b)
    - `grep -c 'import { TermsMarkdown }' apps/web/components/auth/signup-step2.tsx` 결과 1 (TermsMarkdown import 보존)
    - `grep -c '<TermsMarkdown>' apps/web/components/auth/signup-step2.tsx` 결과 1 (호출 1건만, showH1 prop 부착 0건 — D-11a)
    - `! grep -E '<TermsMarkdown showH1' apps/web/components/auth/signup-step2.tsx` (D-11 dialog UX 불변)
    - `grep -F 'LEGAL_CONTENT' apps/web/components/auth/signup-step2.tsx` 매치 ≥ 4 (객체 정의 + 3 사용처 — DialogTitle, DialogDescription, TermsMarkdown)
    - `grep -F 'as const satisfies' apps/web/components/auth/signup-step2.tsx` exit 0 (LEGAL_CONTENT 타입 패턴 보존)
    - `grep -c '<Dialog ' apps/web/components/auth/signup-step2.tsx` 결과 1 (Dialog 사용 보존)
    - `grep -c 'Checkbox' apps/web/components/auth/signup-step2.tsx` ≥ 3 (3개 체크박스 보존 — 체크박스 회귀 0건)
    - `pnpm --filter @grabit/web typecheck` exit 0
    - `pnpm --filter @grabit/web build` exit 0 (회귀 없음)
    - `pnpm --filter @grabit/web lint` exit 0 (lucide-react AlertTriangle unused-imports 경고 등 회귀 0건)
    - **booking/terms-agreement.tsx 변경 0건:** `git diff apps/web/components/booking/terms-agreement.tsx` empty (D-11b)
    - **other auth/signup files 변경 0건:** `git diff apps/web/components/auth/ | grep -F '+++ b/' | grep -v signup-step2` 결과 비어 있음
  </acceptance_criteria>
  <done>legal-draft-banner.tsx 삭제 + signup-step2.tsx import/JSX 단 2 라인만 정리됨. typecheck/build/lint pass + dialog UX 불변 검증.</done>
</task>

<task type="auto">
  <name>Task 2: signup-step2 dialog 시각 회귀 검증 (D-11)</name>
  <files>(no file changes — 시각 회귀 / 수동 검증 안내)</files>
  <read_first>
    - apps/web/components/auth/signup-step2.tsx (수정 후 상태 확인)
    - .planning/phases/16-legal-pages-launch-url/16-VALIDATION.md L76 (dialog 시각 회귀 manual UAT spec)
  </read_first>
  <action>
    Task 1 완료 후 회원가입 step 2 의 dialog 가 LegalDraftBanner 제거에 의해 시각 회귀가 없는지 확인한다. **코드 변경 없음** — 검증 안내 task.

    실행 단계 (자동화 가능 부분):

    1. **dev 서버 기동 후 dialog 본문 렌더 검증:**
       ```bash
       pnpm --filter @grabit/web dev &
       # 30초 대기 (Next 16 컴파일)
       curl -s http://localhost:3000/auth/signup | grep -c 'signup-step1'
       ```
       - dev 서버가 정상 컴파일됨을 확인. step1 페이지가 200 응답.

    2. **HUMAN-UAT.md 의 UAT-11/UAT-12 항목을 Task 3 작성 시 그대로 옮겨 cutover 게이트로 사용** — Task 3 의 HUMAN-UAT.md 가 본 시각 회귀 검증을 책임짐.

    3. **자동 회귀 가드 (Wave 0 의 terms-markdown.test 가 본 task 의 정적 검증을 대신 함):**
       ```bash
       cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run components/legal/__tests__/terms-markdown.test.tsx --reporter=verbose 2>&1 | tail -10
       ```
       - default false (`<TermsMarkdown>...</TermsMarkdown>`) → H1 미렌더 GREEN 유지 — Plan 02 Task 1 적용 후에도 회귀 없음.

    **금지:**
    - 시각 회귀 자동 detection 도구 (Percy, Chromatic) 신규 도입 금지 — Phase 16 scope 외.
    - dialog 의 className / 구조 변경 금지 (D-11 lock).
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run components/legal/__tests__/terms-markdown.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - terms-markdown.test 5건 모두 GREEN (default false 케이스 회귀 없음 — Plan 02 Task 1 적용 후 본 plan 의 import/JSX 정리도 회귀 없음)
    - `! grep -E '<TermsMarkdown showH1' apps/web/components/auth/signup-step2.tsx` (D-11 lock 재확인)
    - `grep -c 'as const satisfies' apps/web/components/auth/signup-step2.tsx` ≥ 1 (LEGAL_CONTENT 패턴 보존)
  </acceptance_criteria>
  <done>terms-markdown.test 회귀 없음 + signup-step2 의 dialog lock 재확인. 시각 회귀 manual UAT 는 Task 3 의 HUMAN-UAT.md 로 위임.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 16 HUMAN-UAT 체크리스트 작성 (D-15 cutover gate + D-07/D-08 placeholder leak 방지)</name>
  <files>.planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L583-597 (UAT-1~UAT-13 원본 표)
    - .planning/phases/16-legal-pages-launch-url/16-CONTEXT.md L34, L50 (D-07 D-08 D-15)
    - .planning/phases/16-legal-pages-launch-url/16-VALIDATION.md §Manual-Only Verifications (L72-77)
    - .planning/phases/16-legal-pages-launch-url/16-04-PLAN.md (placeholder bracket 패턴)
    - .planning/phases/16-legal-pages-launch-url/16-02-PLAN.md (빌드 산출물 path)
  </read_first>
  <action>
    `.planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` 신규 생성. RESEARCH §Validation Architecture L583-597 의 UAT-1~UAT-13 표를 그대로 인용 + Plan 04 의 placeholder bracket pattern 자동 grep 게이트 + Plan 02 의 빌드 산출물 path 자동 grep 게이트 추가.

    **파일 내용 (정확값):**

    ```markdown
    ---
    phase: 16
    slug: legal-pages-launch-url
    type: human-uat
    status: open
    related_decisions: [D-05, D-07, D-08, D-11, D-15]
    created: 2026-04-28
    ---

    # Phase 16 — HUMAN-UAT 체크리스트

    > Cutover D-day 이전·이후 사용자(개발자/운영자)가 직접 수행해야 하는 검증 항목 모음.
    > 자동화 가능한 항목은 `automated` 표기 + 실제 명령 인용.

    ---

    ## Cutover prereqs (prod 배포 전 모두 ✅ 필요)

    ### MD placeholder 실값 주입 (D-07)

    Plan 04 가 만든 placeholder 자리(`[라벨: 임시값]`)에 사용자가 직접 실값을 채워 넣은 후, 다음 자동 게이트가 0건이어야 prod 배포 가능.

    **Automated cutover gate — 빌드 산출물 placeholder leak 차단:**

    ```bash
    pnpm --filter @grabit/web build
    ! grep -rE '\[(시행일|사업자등록번호|통신판매업|보호책임자 실명):' apps/web/.next/
    ```

    위 grep 이 매치 0건일 때만 cutover 진행. 매치 발생 시 해당 placeholder 가 빌드 산출물에 인라인된 상태이므로 prod 배포 금지.

    **체크 항목:**

    - [ ] **UAT-1a:** `apps/web/content/legal/terms-of-service.md` 의 `[사업자명: 000]` → 실제 사업자명 치환
    - [ ] **UAT-1b:** `[대표자명: 000]` → 실제 대표자명 치환
    - [ ] **UAT-1c:** `[사업자등록번호: 000-00-00000]` → 실제 사업자등록번호 치환 (사업자 등록 완료 후)
    - [ ] **UAT-1d:** `[통신판매업 신고번호: 제0000-서울강남-00000호]` → 실제 신고번호 치환 (통신판매업 신고 완료 후)
    - [ ] **UAT-1e:** `[주소: 서울특별시 ...]` → 실제 사업장 주소 치환
    - [ ] **UAT-1f:** `[전화번호: 02-0000-0000]` → 실제 고객센터 전화번호 치환
    - [ ] **UAT-1g:** `apps/web/content/legal/privacy-policy.md` 의 `[보호책임자 실명: 000]` → 실명 치환
    - [ ] **UAT-1h:** `[직책: 대표]` → 실 직책 치환 (또는 그대로 유지 가능 — 1인 개발자는 보통 "대표")
    - [ ] **UAT-1i:** `[전화번호: 000-0000-0000]` (privacy 내) → 보호책임자 직통 전화번호 치환
    - [ ] **UAT-1j:** 자동 gate 통과: `! grep -rE '\[(사업자명|대표자명|사업자등록번호|통신판매업|주소|보호책임자 실명):' apps/web/.next/` 결과 0건

    ### 시행일 cutover 일자 결정 (D-08)

    - [ ] **UAT-2a:** Phase 16 prod 배포 일자 확정 (예: `2026-04-29`)
    - [ ] **UAT-2b:** 3개 MD 파일의 `[시행일: YYYY-MM-DD]` 를 동일 일자로 일괄 치환 (terms-of-service.md, privacy-policy.md, marketing-consent.md)
    - [ ] **UAT-2c:** privacy-policy.md `### 개정 이력` 표의 v1.1 행 `[시행일: YYYY-MM-DD]` 도 동일 일자로 치환
    - [ ] **UAT-2d:** privacy-policy.md `### 개정 이력` 표의 v1.0 행 `[직전 시행일: 2026년 4월 14일]` → 직전 prod 배포 일자(또는 본 cutover 가 최초이면 v1.0 행 삭제)
    - [ ] **UAT-2e:** 자동 gate 통과: `! grep -rF '[시행일:' apps/web/.next/` 결과 0건

    ### Mailbox 수신 검증 (D-15)

    `privacy@heygrabit.com` / `support@heygrabit.com` 두 alias 가 실제 수신 가능해야 prod 배포 가능. DNS MX/alias 설정은 quick-task (Phase 16 외) 책임.

    - [ ] **UAT-3:** mailbox 수신 검증 — 외부 메일(개인 gmail 등)에서 `support@heygrabit.com` 으로 송신 → 운영자 inbox 도착 확인
    - [ ] **UAT-4:** mailbox 수신 검증 — 외부 메일에서 `privacy@heygrabit.com` 으로 송신 → 운영자 inbox 도착 확인

    ---

    ## Post-deploy UAT (prod 배포 후 1시간 이내 검증)

    ### 공개 URL 응답 (D-12)

    - [ ] **UAT-5:** `curl -sI https://heygrabit.com/legal/terms` → HTTP/2 200, `curl https://heygrabit.com/legal/terms | grep -c '이용약관'` ≥ 1
    - [ ] **UAT-6:** `curl -sI https://heygrabit.com/legal/privacy` → HTTP/2 200, 본문에 `개인정보처리방침` 포함
    - [ ] **UAT-7:** `curl -sI https://heygrabit.com/legal/marketing` → HTTP/2 200, 본문에 `마케팅 수신 동의` 포함

    ### Footer 골든 패스 (D-03)

    - [ ] **UAT-8:** prod 임의 페이지에서 Footer "이용약관" 클릭 → `/legal/terms` 이동 + H1 + 본문 노출
    - [ ] **UAT-9:** Footer "개인정보처리방침" 클릭 → `/legal/privacy` 이동 (font-semibold 강조 시각 확인)
    - [ ] **UAT-10:** Footer "고객센터" 클릭 → OS 기본 메일 클라이언트가 `support@heygrabit.com` prefilled 새 메일 창 표시

    ### signup-step2 dialog 시각 회귀 (D-05, D-11)

    - [ ] **UAT-11:** prod `/auth/signup` step 2 진입 → "이용약관 보기" 버튼 클릭 → dialog 본문 정상 렌더 + LegalDraftBanner 노란 배너 부재 + DialogTitle 1건만 노출 (H1 중복 0건)
    - [ ] **UAT-12:** signup-step2 의 동의 체크박스 (3개 약관 + 전체 동의) + 이전/다음 버튼 동작 정상 — D-11 회귀 없음

    ### Post-launch (배포 후 1주일 이내, 선택)

    - [ ] **UAT-13:** Google Search Console 에서 3개 URL 색인 요청 (D-12 — 즉시 효과는 아님)

    ---

    ## Verification Sign-Off

    - [ ] All cutover prereqs UAT-1~UAT-4 완료
    - [ ] Automated grep gate 4건 모두 0 매치 (placeholder leak 0건)
    - [ ] Post-deploy UAT-5~UAT-12 완료
    - [ ] Phase 16 prod cutover commit/PR 머지

    **Cutover Approval:** pending
    ```

    **핵심 계약:**
    - **자동 grep gate 명령 정확값 lock** — `! grep -rE '\[(시행일|사업자등록번호|통신판매업|보호책임자 실명):' apps/web/.next/` (Plan 04 의 bracket 형식과 정합).
    - UAT-1 은 9개 sub-item 으로 분해 (1a~1j) — 사용자 작업 항목을 명확히 추적 가능.
    - UAT-2 는 4개 sub-item 으로 분해 (2a~2e) — 시행일 cutover 일자 결정/주입 단계 분리.
    - UAT-3, UAT-4 는 mailbox 수신 검증 (D-15 prereq) — quick-task 와 cutover 게이트의 연결 고리.
    - UAT-5~UAT-7 는 prod URL 200 응답 + 본문 grep — automated 가능하지만 실제 prod URL 은 배포 후만 검증 가능.
    - UAT-8~UAT-10 은 Footer 골든 패스 (D-03 manual UX 검증).
    - UAT-11, UAT-12 는 signup-step2 dialog 시각 회귀 (D-11 + D-05).
    - UAT-13 은 색인 요청 (post-launch, 선택).

    **금지:**
    - UAT-1~UAT-13 의 항목 ID / 게이트 분류 (cutover prereq / post-deploy / post-launch) 변경 금지 — RESEARCH §Validation Architecture L583-597 과 정합 유지.
    - automated grep gate 명령에서 `-r` flag 누락 금지 (디렉토리 검색 필요).
    - HUMAN-UAT.md 외의 다른 파일 (코드, 다른 plan, RESEARCH/CONTEXT) 변경 0건.
    - 빌드 산출물 path 를 wildcard 없이 하드코딩 금지 — `apps/web/.next/` 디렉토리 전체에 `-r` flag 적용.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && test -f .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md && grep -c '^- \[ \] \*\*UAT-' .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md && grep -F "grep -rE '\\[(시행일|사업자등록번호|통신판매업|보호책임자 실명):'" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -c '^- \[ \] \*\*UAT-' .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` ≥ 22 (UAT-1 9개 + UAT-2 5개 + UAT-3, UAT-4 + UAT-5~7 + UAT-8~10 + UAT-11, UAT-12 + UAT-13 = 24개)
    - `grep -F "## Cutover prereqs" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "## Post-deploy UAT" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "Mailbox 수신 검증 (D-15)" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "support@heygrabit.com" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "privacy@heygrabit.com" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "Automated cutover gate" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "grep -rE '\\[(시행일|사업자등록번호|통신판매업|보호책임자 실명):'" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "apps/web/.next/" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "## Verification Sign-Off" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0
    - `grep -F "[직전 시행일: 2026년 4월 14일]" .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md` exit 0 (UAT-2d 명세)
    - frontmatter 필수 필드: `head -8 .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md | grep -F 'phase: 16'` exit 0 + `head -8 .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md | grep -F 'type: human-uat'` exit 0
  </acceptance_criteria>
  <done>16-HUMAN-UAT.md 생성됨. UAT-1~UAT-13 (sub-item 분해 포함 24개 체크박스) + automated grep gate (`apps/web/.next/` 디렉토리 placeholder leak 차단) + cutover prereq / post-deploy / post-launch 분류 모두 명세.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 컴포넌트 삭제 ↔ 사용처 정합성 | LegalDraftBanner 가 signup-step2.tsx 외 다른 사용처에서 호출되면 빌드 실패 |
| dialog UX ↔ default 시그니처 | TermsMarkdown 호출 형태(prop 미지정) 가 default false 와 정합해야 dialog 회귀 없음 |
| Cutover gate ↔ 빌드 산출물 | placeholder leak 시 prod HTML 에 `[사업자등록번호: 000-...]` 노출 — 법적 표시 의무 위반 |
| Mailbox prereq ↔ Phase 16 cutover | privacy@/support@ alias 가 미개통 시 사용자 정보주체 권리 행사 불가 — 정통망법/개보법 위반 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering (showH1 default 회귀 — dialog 에서 의도치 않은 H1 노출) | apps/web/components/auth/signup-step2.tsx L191 | mitigate | (1) `<TermsMarkdown>{LEGAL_CONTENT[dialogKey].content}</TermsMarkdown>` 호출 형태 lock — `showH1` prop 부착 금지를 acceptance_criteria 의 `! grep -E '<TermsMarkdown showH1'` 로 강제. (2) Wave 0 의 terms-markdown.test 가 default false → H1 미렌더 회귀 케이스 보유 — 본 plan 적용 후에도 GREEN 유지 확인 (Task 2). (3) HUMAN-UAT UAT-11 의 시각 확인 ("회원가입 step 2 → 약관 보기 dialog: 본문 정상 + draft banner 미표시"). |
| T-16-02 | Information Disclosure (placeholder leak — 사업자등록번호 placeholder 가 prod 빌드에 노출되면 법적 표시 의무 위반) | apps/web/.next/server/app/legal/ | mitigate | (1) Task 3 의 HUMAN-UAT.md 가 자동 grep gate (`! grep -rE '\[(시행일\|사업자등록번호\|통신판매업\|보호책임자 실명):' apps/web/.next/`) 를 cutover 전 hard prereq 로 명세. (2) UAT-1 의 9개 sub-item 이 사용자 작업 항목을 추적. (3) UAT-1j 가 grep 0 매치를 sign-off 조건으로 lock. |
| T-16-07 | Repudiation / Information Disclosure (mailbox 미개통 시 정보주체 권리 행사 무력화 → 법적 책임) | privacy@heygrabit.com / support@heygrabit.com mailbox | mitigate | (1) HUMAN-UAT UAT-3, UAT-4 가 cutover prereq 로 mailbox 수신 검증 강제. (2) D-15 quick-task 완료 전 Phase 16 prod 배포 차단. (3) Verification Sign-Off section 이 UAT-3/4 미완료 시 cutover approval 차단. |
</threat_model>

<verification>
- 파일 삭제: `test ! -f apps/web/components/legal/legal-draft-banner.tsx`
- import/JSX 정리: `! grep -rE 'LegalDraftBanner|legal-draft-banner' apps/web --include='*.ts' --include='*.tsx'`
- D-11 dialog UX lock: `grep -c '<TermsMarkdown>' apps/web/components/auth/signup-step2.tsx` 결과 1 + `! grep -E '<TermsMarkdown showH1' apps/web/components/auth/signup-step2.tsx`
- typecheck/build/lint pass
- Wave 0 terms-markdown.test 의 default false 케이스가 여전히 GREEN (회귀 없음 확인)
- booking/terms-agreement.tsx 변경 0건 (D-11b)
- HUMAN-UAT.md 신규 생성 + UAT-1~UAT-13 (24개 체크박스) + automated grep gate 4건 명세
</verification>

<success_criteria>
- [ ] legal-draft-banner.tsx 삭제됨
- [ ] signup-step2.tsx 의 LegalDraftBanner import 제거 (L18)
- [ ] signup-step2.tsx 의 LegalDraftBanner JSX 제거 (L190)
- [ ] LEGAL_CONTENT 객체 / 동의 체크박스 / Dialog 구조 / 핸들러 변경 0건
- [ ] `<TermsMarkdown>` 호출에 showH1 prop 부착 0건 (D-11)
- [ ] typecheck + build + lint pass
- [ ] booking/terms-agreement.tsx 변경 0건 (D-11b)
- [ ] codebase 전체에서 LegalDraftBanner 잔존 매치 0건
- [ ] 16-HUMAN-UAT.md 생성 + UAT-1~UAT-13 (sub-item 분해 24개 체크박스) 명세
- [ ] HUMAN-UAT 의 automated grep gate 가 빌드 산출물 placeholder leak 차단 명령 보유
- [ ] HUMAN-UAT 의 cutover prereq 가 mailbox 수신 검증 + 사업자등록번호 등 실값 주입 + 시행일 cutover 일자 결정 모두 포함
</success_criteria>

<output>
After completion, create `.planning/phases/16-legal-pages-launch-url/16-05-SUMMARY.md`:
- 삭제 + 수정 + 신규 파일 list (legal-draft-banner.tsx 삭제 + signup-step2.tsx 정확한 라인 변경 + 16-HUMAN-UAT.md 생성)
- D-11 dialog UX 불변 검증 결과 (grep 매치 status, terms-markdown.test 회귀 없음)
- AlertTriangle import 트리에서 1건 감소 확인
- HUMAN-UAT.md 의 24개 체크박스 + 4건 automated grep gate 명세 status
- cutover D-day 작업자가 직접 수행해야 하는 항목 요약 (UAT-1, UAT-2, UAT-3, UAT-4)
</output>
