---
phase: 16
plan: 06
type: execute
wave: 5
depends_on: [16-02, 16-03, 16-04, 16-05]
files_modified:
  - apps/web/content/legal/terms-of-service.md
  - apps/web/content/legal/privacy-policy.md
  - apps/web/content/legal/marketing-consent.md
autonomous: false
requirements: [D-06, D-07, D-08, D-12, D-15]
must_haves:
  truths:
    - "사용자가 직접 placeholder 실값을 주입한 후, generic bracket regex grep 이 빌드 산출물에서 0 매치를 보장한다 (D-07 + codex HIGH-3)"
    - "3개 MD 의 시행일이 단일 cutover D-day 일자로 통일된다 (D-08)"
    - "privacy-policy.md 의 개정 이력 표 v1.1 시행일도 동일 cutover 일자로 치환된다 (D-08)"
    - "Clean prod build (rm -rf .next + GRABIT_ENV=production) 가 회귀 없이 통과한다 (codex MED-2)"
    - "prod 빌드 산출물 HTML 에 robots index/follow 인젝트 (preview/staging 은 GRABIT_ENV=production 미설정 시 noindex 인젝트) — codex HIGH-4"
    - "Mailbox 수신 검증 완료 (privacy@/support@heygrabit.com 둘 다 inbox 도착) — D-15 prereq"
    - "Plan 16-05 가 삭제한 LegalDraftBanner 가 codebase 에 잔존하지 않으면서, 동시에 placeholder 도 0 매치 — atomic launch 보장 (codex HIGH-1)"
  artifacts:
    - path: "apps/web/content/legal/terms-of-service.md"
      provides: "실값 주입 완료된 이용약관 (사업자 식별정보 7건 + 시행일 1건)"
      contains: "사업자등록번호"
    - path: "apps/web/content/legal/privacy-policy.md"
      provides: "실값 주입 완료된 개인정보처리방침 (보호책임자 4건 + 시행일 + 개정 이력 표)"
      contains: "개인정보 보호책임자"
    - path: "apps/web/content/legal/marketing-consent.md"
      provides: "실값 주입 완료된 마케팅 수신 동의 (시행일)"
      contains: "본 동의서는"
  key_links:
    - from: "사용자 placeholder 실값 주입"
      to: "Plan 16-04 산출 placeholder bracket"
      via: "수동 텍스트 편집 (사업자 등록 + 법무 검토 후)"
      pattern: "\\[[^]\\n]+:[^]\\n]+\\]"
    - from: "Plan 16-06 의 generic regex gate"
      to: "apps/web/.next/server/app/legal/ 빌드 산출물"
      via: "rm -rf .next + GRABIT_ENV=production build + grep 0 매치"
      pattern: "\\[[^]\\n]+:[^]\\n]+\\]"
    - from: "GRABIT_ENV environment variable"
      to: "page.tsx 의 isProd 분기 (Plan 16-02 산출)"
      via: "Cloud Run / GitHub Actions deploy.yml 환경 변수 주입"
      pattern: "process.env.GRABIT_ENV === 'production'"
---

<objective>
Wave 5 — Phase 16 prod cutover. Plan 16-02~16-05 가 만든 코드/콘텐츠/UAT 인프라 위에서, 사용자(개발자/운영자) 가 직접 (1) 사업자 등록 완료 후 placeholder 실값 주입, (2) 시행일 cutover 일자 결정, (3) mailbox 수신 검증을 모두 마친 후, (4) clean prod build 로 generic bracket regex gate 0 매치 + robots/canonical 인젝트를 자동 검증해 prod 배포 가능 상태로 전환한다.

Purpose: codex HIGH-1 finding (REVIEWS adjudication accept) — "executable cutover wave 부재" 해소. 16-04 가 placeholder 자리만 마련했고, 16-05 가 LegalDraftBanner 를 삭제하면서 'draft' 시각 안전 신호도 사라졌으므로, placeholder 가 prod 에 누출되면 법적 표시 의무 위반 + 검색엔진 색인 위험이 동시에 발생. 본 plan 은 (a) human-in-the-loop 실값 주입 단계 (Task 1), (b) 자동 검증 게이트 단계 (Task 2), (c) prod 배포 직전 최종 smoke (Task 3) 를 명시적 wave 로 분리해 cutover 시점을 lock.

`autonomous: false` — Task 1 의 placeholder 실값 주입이 사용자 manual action 이므로, executor 가 본 plan 을 기동할 때 checkpoint:human-action 으로 일시정지. Task 2/3 는 사용자가 Task 1 완료 보고 후 자동 실행.

Output: 3 MD 파일 수정 (실값 주입). 신규 코드 0건. 신규 의존성 0건. Phase 16 prod 배포 준비 완료.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-legal-pages-launch-url/16-CONTEXT.md
@.planning/phases/16-legal-pages-launch-url/16-RESEARCH.md
@.planning/phases/16-legal-pages-launch-url/16-PATTERNS.md
@.planning/phases/16-legal-pages-launch-url/16-VALIDATION.md
@.planning/phases/16-legal-pages-launch-url/16-REVIEWS.md
@.planning/phases/16-legal-pages-launch-url/16-02-SUMMARY.md
@.planning/phases/16-legal-pages-launch-url/16-03-SUMMARY.md
@.planning/phases/16-legal-pages-launch-url/16-04-SUMMARY.md
@.planning/phases/16-legal-pages-launch-url/16-05-SUMMARY.md
@.planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md

@apps/web/content/legal/terms-of-service.md
@apps/web/content/legal/privacy-policy.md
@apps/web/content/legal/marketing-consent.md

<interfaces>
<!-- 본 plan 이 의존하는 contracts. -->

From Plan 16-04 산출 (placeholder bracket 형식):
- terms: 7개 placeholder — `[사업자명: 000]`, `[대표자명: 000]`, `[사업자등록번호: 000-00-00000]`, `[통신판매업 신고번호: 제0000-서울강남-00000호]`, `[주소: 서울특별시 ...]`, `[전화번호: 02-0000-0000]`, `[시행일: YYYY-MM-DD]`
- privacy: 5+ placeholder — `[보호책임자 실명: 000]`, `[직책: 대표]`, `[전화번호: 000-0000-0000]`, `[시행일: YYYY-MM-DD]` (2건 — 본문 + 개정 이력 v1.1), `[직전 시행일: 2026년 4월 14일]`
- marketing: 1 placeholder — `[시행일: YYYY-MM-DD]`

From Plan 16-02 산출 (env-driven robots — codex HIGH-4):
```typescript
const isProd = process.env.GRABIT_ENV === 'production';
robots: { index: isProd, follow: isProd }
```
- prod 빌드: `GRABIT_ENV=production pnpm build` → HTML 에 `<meta name="robots" content="index, follow">` 인젝트
- non-prod 빌드: `pnpm build` → HTML 에 `<meta name="robots" content="noindex, nofollow">` 인젝트

From Plan 16-05 산출 (HUMAN-UAT.md):
- UAT-1a~UAT-1j: placeholder 실값 주입 9개 sub-item + automated gate
- UAT-2a~UAT-2e: 시행일 cutover 일자 결정 + 일괄 치환
- UAT-3, UAT-4: mailbox 수신 검증
- UAT-5~UAT-7: prod URL 200 응답
- UAT-8~UAT-10: Footer 골든 패스
- UAT-11, UAT-12: signup-step2 dialog 회귀 검증

From codex HIGH-3 generic bracket regex (REVIEWS adjudication accept):
```bash
! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/.next/server/app/legal/
```
- `[라벨: 값]` 형태의 모든 placeholder catch.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: placeholder 실값 주입 + 시행일 cutover 일자 결정 + mailbox 수신 검증 (사용자 직접 작업)</name>
  <files>apps/web/content/legal/terms-of-service.md, apps/web/content/legal/privacy-policy.md, apps/web/content/legal/marketing-consent.md</files>
  <read_first>
    - .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md (UAT-1, UAT-2, UAT-3, UAT-4 cutover prereq)
    - .planning/phases/16-legal-pages-launch-url/16-04-SUMMARY.md (placeholder 위치 + 형식)
    - apps/web/content/legal/terms-of-service.md (Plan 16-04 산출 — 7 placeholder)
    - apps/web/content/legal/privacy-policy.md (Plan 16-04 산출 — 5+ placeholder + GFM 표)
    - apps/web/content/legal/marketing-consent.md (Plan 16-04 산출 — 1 placeholder)
  </read_first>
  <what-built>
    Plan 16-02~16-05 가 만든 인프라:
    - /legal/{terms,privacy,marketing} 3개 SSG 페이지 (16-02)
    - Footer 3 링크 실 경로 교체 (16-03)
    - 3개 MD 의 placeholder bracket (16-04)
    - LegalDraftBanner 삭제 + HUMAN-UAT 체크리스트 (16-05)
    - codex review 반영 — TermsMarkdown table 매핑 (MED-4), env-driven robots (HIGH-4), generic bracket regex gate (HIGH-3)
  </what-built>
  <how-to-verify>
    **사용자 작업 단계 (3 단계):**

    **1단계 — placeholder 실값 주입 (UAT-1):**
    - 사업자 등록 완료 후 다음 9개 항목을 직접 텍스트 편집:
      - `apps/web/content/legal/terms-of-service.md`:
        - `[사업자명: 000]` → 실제 사업자명
        - `[대표자명: 000]` → 실제 대표자명
        - `[사업자등록번호: 000-00-00000]` → 실제 사업자등록번호
        - `[통신판매업 신고번호: 제0000-서울강남-00000호]` → 실제 신고번호 (통신판매업 신고 완료 후)
        - `[주소: 서울특별시 ...]` → 실제 사업장 주소
        - `[전화번호: 02-0000-0000]` → 실제 고객센터 전화번호
      - `apps/web/content/legal/privacy-policy.md`:
        - `[보호책임자 실명: 000]` → 보호책임자 실명
        - `[직책: 대표]` → 실 직책 (그대로 유지 가능 — 1인 개발자는 통상 "대표")
        - `[전화번호: 000-0000-0000]` → 보호책임자 직통 전화번호

    **2단계 — 시행일 cutover 일자 결정 (UAT-2):**
    - Phase 16 prod 배포 D-day 확정 (예: `2026-04-29`).
    - 다음 4개 위치의 `[시행일: YYYY-MM-DD]` 를 동일 일자로 일괄 치환:
      - `apps/web/content/legal/terms-of-service.md` 부칙
      - `apps/web/content/legal/privacy-policy.md` L93 본문
      - `apps/web/content/legal/privacy-policy.md` 개정 이력 표 v1.1 행
      - `apps/web/content/legal/marketing-consent.md` L32 본문
    - `apps/web/content/legal/privacy-policy.md` 개정 이력 표의 v1.0 행 `[직전 시행일: 2026년 4월 14일]`:
      - 직전 prod 배포 일자가 있으면 해당 일자로 치환
      - 본 cutover 가 최초 prod 배포면 v1.0 행 자체 삭제

    **3단계 — Mailbox 수신 검증 (UAT-3, UAT-4):**
    - DNS MX/alias 설정 quick-task 가 완료된 후:
      - 외부 메일 (개인 gmail 등) → `support@heygrabit.com` 으로 송신 → 운영자 inbox 도착 확인
      - 외부 메일 → `privacy@heygrabit.com` 으로 송신 → 운영자 inbox 도착 확인
    - 둘 다 inbox 도착 시 본 task resume 가능.

    **resume signal:** 사용자가 위 3단계 완료 후 "cutover-ready" 또는 "approved" 응답.
  </how-to-verify>
  <resume-signal>위 3단계 완료 후 "cutover-ready" 또는 "approved" 입력. 미완료 항목 있으면 항목 list 응답.</resume-signal>
  <action>
    **사용자 응답 후 Task 2 자동 진행.** 본 task 는 코드 변경 없음 — Task 1 의 코드 변경 (MD 실값 주입) 은 사용자가 직접 수행.

    Executor (Claude) 측 작업:
    - 사용자 cutover-ready 응답 받으면 Task 2 진입.
    - 사용자가 일부 항목만 완료한 경우 (e.g. mailbox 미검증) 미완료 list 회신 받고 task 1 재대기.
  </action>
  <verify>
    <automated>! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/content/legal/*.md</automated>
  </verify>
  <acceptance_criteria>
    - 사용자 "cutover-ready" 응답 수신
    - **MD 파일 placeholder 0 매치 (codex HIGH-3 generic bracket regex):** `! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/content/legal/*.md` exit 0
    - terms-of-service.md 에 `2026년` 또는 cutover D-day 일자 매치 (시행일 실값 주입 확인)
    - privacy-policy.md 에 `2026년` 또는 cutover D-day 일자 매치
    - marketing-consent.md 에 `2026년` 또는 cutover D-day 일자 매치
    - privacy-policy.md L82-86 영역에 한국어 실명 매치 (`[보호책임자 실명: 000]` placeholder 미잔존)
    - **mailbox 수신 검증** (16-HUMAN-UAT.md UAT-3, UAT-4 사용자 ✅ 마킹) — 본 plan executor 가 manual UAT 결과를 SUMMARY.md 에 기록
  </acceptance_criteria>
  <done>3개 MD 의 placeholder 0건 + 시행일 통일 + mailbox 수신 검증 완료. Task 2 (자동 검증 게이트) 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 2: clean prod build + generic bracket regex gate + robots/canonical 인젝트 검증 (자동 cutover gate)</name>
  <files>(no file changes — automated build artifact verification)</files>
  <read_first>
    - apps/web/content/legal/*.md (Task 1 산출물 — placeholder 0 매치 상태)
    - apps/web/app/legal/{terms,privacy,marketing}/page.tsx (Plan 16-02 산출 — env-driven robots)
    - .planning/phases/16-legal-pages-launch-url/16-VALIDATION.md (16-04-02 placeholder bracket 검출 목표)
    - .planning/phases/16-legal-pages-launch-url/16-REVIEWS.md (codex HIGH-1, HIGH-3, HIGH-4, MED-2 처리 명세)
  </read_first>
  <action>
    Task 1 완료 후 prod build 산출물에서 (a) placeholder 0 매치, (b) robots index/follow 인젝트, (c) canonical 인젝트, (d) 한글 본문 인젝트, (e) JSON-LD 0 매치 (D-14) 를 모두 자동 검증한다.

    **2-A. Clean prod build (codex MED-2 stale .next 회피):**
    ```bash
    rm -rf apps/web/.next
    GRABIT_ENV=production pnpm --filter @grabit/web build
    ```

    **2-B. Generic bracket regex gate (codex HIGH-3 — REVIEWS adjudication accept):**
    ```bash
    if grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/.next/server/app/legal/; then
      echo "FAIL: placeholder leak in build output"
      exit 1
    fi
    ```
    - 매치 0건 시 통과. 매치 발생 시 prod 배포 차단 + Task 1 으로 회귀.

    **2-C. robots index/follow 인젝트 검증 (HIGH-4 prod 분기):**
    ```bash
    find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'index, follow' {} \;
    ```
    - 결과 ≥ 3 매치. `noindex` 매치 발생 시 GRABIT_ENV 주입 누락 → Task 2-A 재실행.

    **2-D. canonical 인젝트 검증 (D-13):**
    ```bash
    find apps/web/.next -path '*legal*terms*' -name '*.html' -exec grep -l 'rel="canonical".*heygrabit.com/legal/terms' {} \;
    find apps/web/.next -path '*legal*privacy*' -name '*.html' -exec grep -l 'rel="canonical".*heygrabit.com/legal/privacy' {} \;
    find apps/web/.next -path '*legal*marketing*' -name '*.html' -exec grep -l 'rel="canonical".*heygrabit.com/legal/marketing' {} \;
    ```
    - 각 ≥ 1 매치.

    **2-E. 한글 본문 인젝트 검증 (D-10b):**
    ```bash
    find apps/web/.next -path '*legal*terms*' -name '*.html' -exec grep -l '이용약관' {} \;
    find apps/web/.next -path '*legal*privacy*' -name '*.html' -exec grep -l '개인정보처리방침' {} \;
    find apps/web/.next -path '*legal*marketing*' -name '*.html' -exec grep -l '마케팅' {} \;
    ```

    **2-F. JSON-LD 미인젝트 (D-14):**
    ```bash
    ! grep -rE 'application/ld\+json' apps/web/.next/server/app/legal/
    ```

    **2-G. LegalDraftBanner 잔존 0 매치 (Plan 16-05 산출 회귀):**
    ```bash
    ! grep -rE 'LegalDraftBanner|legal-draft-banner' apps/web --include='*.ts' --include='*.tsx'
    ```

    **2-H. typecheck/lint pass:**
    ```bash
    pnpm --filter @grabit/web typecheck
    pnpm --filter @grabit/web lint
    ```

    **금지:**
    - GRABIT_ENV 주입 없이 build → 산출물에 noindex 인젝트되어 prod cutover 의도와 불일치. 자동 게이트가 명시적으로 차단.
    - Task 1 완료 전 본 task 진입 금지 — placeholder 매치 시 즉시 실패.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && rm -rf apps/web/.next && GRABIT_ENV=production pnpm --filter @grabit/web build 2>&1 | tail -20 && ! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/.next/server/app/legal/ && find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'index, follow' {} \; | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf apps/web/.next && GRABIT_ENV=production pnpm --filter @grabit/web build` exit 0
    - **codex HIGH-3 generic bracket regex gate**: `! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/.next/server/app/legal/` exit 0 (placeholder 0 매치)
    - **codex HIGH-4 prod robots 분기**: `find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'index, follow' {} \;` 결과 ≥ 3 매치
    - **codex HIGH-4 noindex 미인젝트 (prod build):** `! find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'noindex' {} \;` (prod 빌드에서 noindex 미매치)
    - canonical 인젝트 3 라우트: `find apps/web/.next -path '*legal*' -name '*.html' -exec grep -l 'rel="canonical"' {} \;` ≥ 3
    - 한글 본문 인젝트: `find apps/web/.next -path '*legal*terms*' -name '*.html' -exec grep -l '이용약관' {} \;` ≥ 1 (3 라우트 동일 검증)
    - JSON-LD 미인젝트 (D-14): `! grep -rE 'application/ld\+json' apps/web/.next/server/app/legal/`
    - LegalDraftBanner 잔존 0 (Plan 16-05 회귀 가드): `! grep -rE 'LegalDraftBanner|legal-draft-banner' apps/web --include='*.ts' --include='*.tsx'`
    - typecheck/lint pass: `pnpm --filter @grabit/web typecheck && pnpm --filter @grabit/web lint`
  </acceptance_criteria>
  <done>Clean prod build + generic regex gate 0 매치 + robots/canonical/한글본문/JSON-LD/banner 잔존 검증 모두 PASS. Phase 16 prod 배포 가능 상태.</done>
</task>

<task type="auto">
  <name>Task 3: prod cutover commit + post-deploy smoke (UAT-5~UAT-12 trigger)</name>
  <files>(commit only — no further file changes)</files>
  <read_first>
    - .planning/phases/16-legal-pages-launch-url/16-HUMAN-UAT.md (UAT-5~UAT-12)
    - Task 2 의 자동 검증 결과 SUMMARY
  </read_first>
  <action>
    Task 2 자동 게이트 PASS 후 cutover commit 생성 + prod 배포 (Cloud Run / GitHub Actions deploy.yml) + post-deploy smoke 트리거.

    **3-A. cutover commit:**
    ```bash
    git add apps/web/content/legal/terms-of-service.md \
            apps/web/content/legal/privacy-policy.md \
            apps/web/content/legal/marketing-consent.md
    git commit -m "feat(legal): cutover with real business identifiers + effective date

    - terms: inject real 사업자명/대표자명/사업자등록번호/통신판매업 신고번호/주소/전화번호
    - privacy: inject 개인정보 보호책임자 실명/직책/직통 전화 + 개정 이력 v1.1 시행일
    - marketing: inject 시행일
    - All placeholders ([라벨: 값]) cleared. Generic bracket regex gate 0 match.
    - GRABIT_ENV=production build verified — robots: index/follow injected.

    Refs: Phase 16 cutover (codex HIGH-1 atomic launch)"
    ```

    **3-B. prod 배포 + GRABIT_ENV=production 인프라 주입 확정:**
    - **(B-1) deploy.yml 사전 검증 (W2 — checker requirement):**
      ```bash
      grep -E 'GRABIT_ENV.*production' .github/workflows/deploy.yml
      ```
      - 매치 발생 시 OK. 미매치 발생 시 deploy.yml 의 web 배포 step env 섹션에 다음 패턴 중 하나를 추가:
        - `env:` 블록에 `GRABIT_ENV: production` 추가
        - `gcloud run deploy ...` step 에 `--set-env-vars=GRABIT_ENV=production` 인자 추가
      - 패치 후 동일 grep 재실행 → 매치 보장.
    - **(B-2) Cloud Run runtime 검증 (배포 후, manual — autonomous: false 로 manual 호출 가능):**
      ```bash
      gcloud run services describe grabit-web --region=asia-northeast3 --format='get(spec.template.spec.containers[0].env)' | grep 'GRABIT_ENV'
      ```
      - 출력에 `GRABIT_ENV=production` 또는 `name=GRABIT_ENV value=production` 매치. 미매치 시 deploy.yml 패치 후 재배포.
    - **(B-3) prod revision 띄우기:** GitHub Actions deploy.yml 또는 `gcloud run deploy grabit-web ...` 명령으로 신규 revision traffic=100% 전환.
    - **금지:** deploy.yml grep 미통과 상태로 prod 배포 진행 — 산출물 HTML 에 noindex 인젝트되어 D-12 (heygrabit.com 색인) 위반.

    **3-C. post-deploy smoke (UAT-5~UAT-12 자동 가능 부분):**
    ```bash
    curl -sI https://heygrabit.com/legal/terms | head -1   # HTTP/2 200
    curl -sI https://heygrabit.com/legal/privacy | head -1
    curl -sI https://heygrabit.com/legal/marketing | head -1

    # 본문 grep
    curl -s https://heygrabit.com/legal/terms | grep -c '이용약관'
    curl -s https://heygrabit.com/legal/privacy | grep -c '개인정보처리방침'
    curl -s https://heygrabit.com/legal/marketing | grep -c '마케팅'

    # robots prod 인젝트 검증 (HIGH-4 prod)
    curl -s https://heygrabit.com/legal/terms | grep -c 'name="robots".*index, follow'

    # placeholder leak 최종 검증 (live URL — codex HIGH-3 prod gate)
    curl -s https://heygrabit.com/legal/terms https://heygrabit.com/legal/privacy https://heygrabit.com/legal/marketing | grep -cE '\[[^]\n]+:[^]\n]+\]'
    ```
    - 마지막 명령 결과 0건이어야 cutover 완료. 매치 발생 시 즉시 rollback (Cloud Run 이전 revision 으로 traffic 100%).

    **3-D. UAT-8~UAT-12 (manual smoke):**
    - 사용자가 prod 임의 페이지에서 Footer "이용약관"/"개인정보처리방침"/"고객센터" 클릭 + signup-step2 dialog 동의 흐름 회귀 확인 (16-HUMAN-UAT.md UAT-8~UAT-12 체크박스).

    **3-E. SUMMARY.md 작성:**
    - `.planning/phases/16-legal-pages-launch-url/16-06-SUMMARY.md` 에 cutover 일자, commit hash, prod URL smoke 결과, UAT-1~UAT-13 체크 status 모두 기록.
  </action>
  <verify>
    <automated>curl -sI https://heygrabit.com/legal/terms 2>&1 | head -1 && curl -s https://heygrabit.com/legal/terms | grep -cE '\[[^]\n]+:[^]\n]+\]'</automated>
  </verify>
  <acceptance_criteria>
    - cutover commit 생성됨 (git log 매치)
    - **deploy.yml 에 GRABIT_ENV=production 주입 확인 (W2):** `grep -E 'GRABIT_ENV.*production' .github/workflows/deploy.yml` exit 0
    - prod 배포 완료 — Cloud Run revision URL 응답 200
    - prod URL 200 + 한글 본문 매치 (UAT-5/6/7)
    - prod HTML 에 `name="robots".*index, follow` 매치 (HIGH-4 prod 분기 검증)
    - **prod URL 에 placeholder 0 매치 (codex HIGH-3 final gate):** `curl -s https://heygrabit.com/legal/terms https://heygrabit.com/legal/privacy https://heygrabit.com/legal/marketing | grep -cE '\[[^]\n]+:[^]\n]+\]'` 결과 0
    - 16-HUMAN-UAT.md 의 UAT-1 ~ UAT-12 모두 ✅ 마킹
    - 16-06-SUMMARY.md 작성 (cutover 일자 + commit hash + smoke 결과 포함)
  </acceptance_criteria>
  <done>Phase 16 prod cutover 완료. 16-HUMAN-UAT.md UAT-1~UAT-12 ✅. ROADMAP.md 의 Phase 16 entry 가 [x] 마킹 가능 상태.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-cutover content ↔ Post-cutover prod HTML | placeholder 잔존 시 prod 노출 → 법적 표시 의무 위반 |
| Build-time env (GRABIT_ENV) ↔ Runtime metadata | env 미주입 시 noindex prod 배포 → SEO/PG verification 실패 |
| Mailbox prereq ↔ User trust | privacy@/support@ 미수신 시 사용자 권리 행사 무력 |
| Atomic launch ↔ partial revert | LegalDraftBanner 삭제 + placeholder 미주입 동시 발생 시 사용자 안전 신호 + 콘텐츠 정확성 모두 손실 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Information Disclosure (placeholder leak — prod 빌드 산출물에 `[사업자등록번호: ...]` 노출 시 법적 표시 의무 위반) | apps/web/.next/server/app/legal/ | mitigate | (1) Task 2-B 의 generic bracket regex gate (`! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/.next/server/app/legal/`) 가 build 산출물 0 매치 강제. (2) Task 3-C 의 prod URL 최종 검증 — live HTML 에 placeholder 0 매치. (3) Task 1 의 사용자 직접 실값 주입 + HUMAN-UAT UAT-1~UAT-2 체크리스트. |
| T-16-04 | Tampering / Information Disclosure (prod build 시 GRABIT_ENV 미주입 → noindex 인젝트 → PG/도메인 verification 실패) | apps/web/app/legal/{terms,privacy,marketing}/page.tsx | mitigate | (1) Task 2-A 의 build 명령에 `GRABIT_ENV=production` 명시. (2) Task 2-C 의 `index, follow` 매치 ≥ 3 검증. (3) Task 3-B 의 Cloud Run 환경 변수 주입 확인. (4) Task 3-C 의 prod URL `name="robots".*index, follow` 매치 검증. |
| T-16-07 | Repudiation (mailbox 미개통 시 정보주체 권리 행사 무력 → 정통망법/개보법 위반) | privacy@heygrabit.com / support@heygrabit.com | mitigate | (1) Task 1 의 3단계 mailbox 수신 검증 (UAT-3, UAT-4). (2) HUMAN-UAT.md 의 cutover prereq 체크박스. (3) DNS MX/alias quick-task 완료 전 본 plan 진입 금지. |
| T-16-08 | Repudiation (cutover 시점 partial — placeholder 일부만 주입 + 나머지 prod 노출) | apps/web/content/legal/*.md | mitigate | Task 1 의 cutover-ready 응답이 9개 sub-item + 4개 시행일 + mailbox 2개 모두 완료 후에만 가능. Task 2-B 의 build artifact gate 가 추가 보호. (codex HIGH-1 atomic launch) |
| T-16-09 | Information Disclosure (preview/staging 환경 색인 → placeholder 가 검색엔진에 색인) | apps/web/app/legal/{terms,privacy,marketing}/page.tsx (Plan 16-02 isProd 분기) | mitigate | Plan 16-02 의 env-driven robots 분기 (`isProd === false → noindex, nofollow`) 로 preview/staging 자동 차단. Cloud Run preview revision 의 GRABIT_ENV 미주입 시 자연스럽게 noindex. (codex HIGH-4) |
</threat_model>

<verification>
- Task 1: 3개 MD 파일에 placeholder bracket 0 매치 (`! grep -rE '\[[^]\n]+:[^]\n]+\]' apps/web/content/legal/*.md`)
- Task 2: clean prod build 성공 + generic regex gate 0 매치 + robots/canonical/한글본문/JSON-LD/banner 잔존 검증 모두 PASS
- Task 3: prod cutover commit + 배포 + URL smoke (200 + index, follow + placeholder 0 매치)
- 16-HUMAN-UAT.md UAT-1~UAT-12 모두 ✅
- 16-06-SUMMARY.md 작성 (cutover 일자, commit, smoke 결과 + REVIEWS adjudication summary)
</verification>

<success_criteria>
- [ ] Task 1 사용자 cutover-ready 응답 + mailbox 수신 검증 완료
- [ ] 3개 MD placeholder bracket 0 매치
- [ ] 시행일 cutover D-day 단일 일자로 통일
- [ ] Clean prod build (rm -rf .next + GRABIT_ENV=production) 성공 (codex MED-2)
- [ ] 빌드 산출물 generic bracket regex gate 0 매치 (codex HIGH-3)
- [ ] 빌드 산출물에 robots `index, follow` 인젝트 ≥ 3 (codex HIGH-4 prod 분기)
- [ ] canonical absolute URL 인젝트 ≥ 3 라우트
- [ ] 한글 본문 인젝트 ≥ 1 매치 / 라우트
- [ ] JSON-LD 미인젝트 (D-14)
- [ ] LegalDraftBanner 잔존 0 매치 (Plan 16-05 회귀 가드)
- [ ] typecheck/lint pass
- [ ] cutover commit + prod 배포 + URL smoke 모두 PASS
- [ ] deploy.yml 에 `GRABIT_ENV=production` 주입 확정 (Cloud Run runtime env 매치) — W2 mitigation
- [ ] 16-HUMAN-UAT.md UAT-1~UAT-12 ✅
- [ ] codex HIGH-1 atomic launch 보장 (placeholder 0 + banner 부재 + 실값 주입 동시)
</success_criteria>

<output>
After completion, create `.planning/phases/16-legal-pages-launch-url/16-06-SUMMARY.md`:
- Cutover D-day 일자 + cutover commit hash
- Task 2 자동 게이트 결과 (generic regex 0 매치, robots/canonical/한글본문/JSON-LD/banner 검증)
- Task 3 prod URL smoke 결과 (HTTP 200, robots index/follow, placeholder 0)
- 16-HUMAN-UAT.md UAT-1~UAT-12 체크 status (UAT-13 색인 요청은 post-launch 선택)
- codex REVIEWS adjudication 처리 status (HIGH-1/HIGH-2/HIGH-3/HIGH-4/MED-1/MED-2/MED-3/MED-4/MED-6/LOW-1) — accept/partial/reject/deferred 분류 + 어느 plan/task 에서 처리했는지 매핑
- 다음 phase (Phase 17 — Local dev health indicator fix) 진입 가능 확인
- ROADMAP.md Phase 16 entry [x] 마킹 안내
</output>
