---
phase: 16
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/components/legal/__tests__/terms-markdown.test.tsx
  - apps/web/app/legal/__tests__/metadata.test.ts
  - apps/web/components/layout/__tests__/footer.test.tsx
autonomous: true
requirements: [D-09, D-10, D-13, D-03, D-04]
must_haves:
  truths:
    - "showH1 prop 양방향 동작이 vitest로 사양화된 테스트 파일이 존재한다 (default false → H1 미렌더, showH1=true → text-display H1 렌더) (D-09)"
    - "/legal/{terms,privacy,marketing}/page 의 metadata title/description/canonical/robots 계약이 vitest로 사양화된다 (D-10/D-13)"
    - "Footer의 3개 링크 href 계약(/legal/terms, /legal/privacy, mailto:support@heygrabit.com)이 vitest로 사양화된다 (D-03)"
    - "3개 신규 테스트는 Wave 0 시점에는 RED 상태가 정상이다 — 후속 wave 의 구현이 GREEN 으로 전환시킨다"
  artifacts:
    - path: "apps/web/components/legal/__tests__/terms-markdown.test.tsx"
      provides: "showH1 prop 양방향 unit test 3건 + 기존 매핑 회귀 가드"
      contains: "showH1"
    - path: "apps/web/app/legal/__tests__/metadata.test.ts"
      provides: "3개 page.tsx 의 metadata export 검증 (title/canonical/robots)"
      contains: "alternates"
    - path: "apps/web/components/layout/__tests__/footer.test.tsx"
      provides: "Footer 3 링크 href 계약 검증 + /legal/marketing 미노출 검증 (D-04)"
      contains: "mailto:support@heygrabit.com"
  key_links:
    - from: "apps/web/components/legal/__tests__/terms-markdown.test.tsx"
      to: "apps/web/components/legal/terms-markdown.tsx"
      via: "RTL render + container.querySelector('h1')"
      pattern: "querySelector\\('h1'\\)"
    - from: "apps/web/app/legal/__tests__/metadata.test.ts"
      to: "apps/web/app/legal/{terms,privacy,marketing}/page"
      via: "import { metadata } from '@/app/legal/*/page'"
      pattern: "import \\{ metadata"
    - from: "apps/web/components/layout/__tests__/footer.test.tsx"
      to: "apps/web/components/layout/footer.tsx"
      via: "RTL render + getByText().closest('a').getAttribute('href')"
      pattern: "getAttribute\\('href'\\)"
---

<objective>
Wave 0 — 후속 4개 plan 의 구현이 GREEN 으로 전환시킬 RED 테스트 3개를 먼저 작성한다 (test-first sampling continuity 보장). VALIDATION.md §Per-Task Verification Map 의 16-01-01/02/03 task 가 본 plan 에서 산출되며, Wave 1~3 가 동일 테스트를 GREEN 상태로 전환시키는 의존 그래프 확립.

Purpose: TDD-mode 가 disabled 라도 본 phase 는 (1) D-09 의 showH1 prop default 가 dialog 회귀 위험을 안고 있고, (2) D-13 의 metadata 계약이 page.tsx 신규 작성 시 누락되기 쉬우며, (3) D-03/D-04 의 Footer href 교체가 단순 string 치환이라 회귀 가드가 필요하다. 세 영역 모두 grep 보다 강한 RTL/import 검증이 sampling latency 90초 이내로 수행된다. Wave 0 의 RED 상태는 정상이며, 후속 wave merge 시점에 GREEN 으로 전환됨을 검증해야 한다.

Output: 3개 vitest 파일 신규 + 기존 vitest infra(`apps/web/vitest.config.ts`) 그대로 유지. 신규 install 0건.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-legal-pages-launch-url/16-CONTEXT.md
@.planning/phases/16-legal-pages-launch-url/16-RESEARCH.md
@.planning/phases/16-legal-pages-launch-url/16-PATTERNS.md
@.planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md
@.planning/phases/16-legal-pages-launch-url/16-VALIDATION.md

@apps/web/components/legal/terms-markdown.tsx
@apps/web/components/layout/footer.tsx
@apps/web/components/performance/__tests__/status-badge.test.tsx
@apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx
@apps/web/vitest.config.ts

<interfaces>
<!-- Wave 0 테스트가 의존하는 미래 contracts. 본 plan 작성 시점에는 일부 미존재 — RED 정상. -->

From apps/web/components/legal/terms-markdown.tsx (Wave 1 에서 D-09 prop 추가됨):
```typescript
// 현재 (Wave 0 시점):
export function TermsMarkdown({ children }: { children: string }): JSX.Element

// Wave 1 이후 변경될 시그니처 (RESEARCH §Critical Code Patterns L341-390):
export function TermsMarkdown({
  children,
  showH1 = false,
}: { children: string; showH1?: boolean }): JSX.Element
// default false 보장 — 기존 dialog 호출 `<TermsMarkdown>{md}</TermsMarkdown>` 호환
// showH1=true → h1 className="text-display font-semibold leading-[1.2] text-gray-900"
```

From apps/web/app/legal/{terms,privacy,marketing}/page.tsx (Wave 1 에서 신규 생성):
```typescript
// 미존재 (Wave 0 RED), Wave 1 이후 (RESEARCH §Critical Code Patterns L312-334):
export const dynamic: 'force-static';
export const metadata: Metadata = {
  title: string;        // 정확값: '이용약관 — Grabit' / '개인정보처리방침 — Grabit' / '마케팅 정보 수신 동의 — Grabit'
  description: string;  // UI-SPEC §Copywriting L208-210 정확값
  alternates: { canonical: 'https://heygrabit.com/legal/{terms|privacy|marketing}' };
  robots: { index: true; follow: true };
};
export default function {Terms|Privacy|Marketing}Page(): JSX.Element;
```

From apps/web/components/layout/footer.tsx (Wave 2 에서 href 교체됨):
```typescript
// 현재 (Wave 0 시점) — 3개 모두 href="#":
<Link href="#" className="hover:underline">이용약관</Link>
<Link href="#" className="font-semibold hover:underline">개인정보처리방침</Link>
<Link href="#" className="hover:underline">고객센터</Link>

// Wave 2 이후 (RESEARCH §Critical Code Patterns §Footer Patch):
<Link href="/legal/terms" ...>이용약관</Link>
<Link href="/legal/privacy" className="font-semibold hover:underline">개인정보처리방침</Link>
<a href="mailto:support@heygrabit.com" ...>고객센터</a>  // ← Link → <a> 교체
```

From apps/web/vitest.config.ts (변경 없음):
```typescript
// alias: '@' → 'apps/web/' (이미 설정)
// environment: jsdom
// globals: true (vitest 글로벌 describe/it/expect 활성)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: terms-markdown.test.tsx 작성 (D-09 showH1 prop 양방향 + 기존 매핑 회귀 가드)</name>
  <files>apps/web/components/legal/__tests__/terms-markdown.test.tsx</files>
  <read_first>
    - apps/web/components/legal/terms-markdown.tsx (현재 시그니처 — `{ children }: { children: string }`, h1: () => null)
    - apps/web/components/performance/__tests__/status-badge.test.tsx (vitest+RTL skeleton analog — PATTERNS.md L378-391)
    - apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx L26-31 (className contain 검증 패턴)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L374-415 (테스트 케이스 spec)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L575-579 (Wave 0 Gap 정의)
  </read_first>
  <action>
    `apps/web/components/legal/__tests__/__tests__` 디렉토리는 이미 미존재 — vitest 가 `apps/web/components/**/__tests__/*.test.{ts,tsx}` 글롭으로 자동 픽업하므로 단순 신규 파일 추가로 충분.

    파일 내용 (PATTERNS.md L378-415 인용 + RESEARCH L575 인용):

    ```tsx
    import { describe, it, expect } from 'vitest';
    import { render } from '@testing-library/react';
    import { TermsMarkdown } from '../terms-markdown';

    describe('TermsMarkdown', () => {
      describe('showH1 prop (D-09)', () => {
        it('default (showH1 미지정) 시 H1 을 렌더하지 않는다 — dialog 호환', () => {
          const { container } = render(
            <TermsMarkdown>{'# 이용약관\n\n본문'}</TermsMarkdown>,
          );
          expect(container.querySelector('h1')).toBeNull();
        });

        it('showH1=false 명시 시 H1 을 렌더하지 않는다', () => {
          const { container } = render(
            <TermsMarkdown showH1={false}>{'# 이용약관\n\n본문'}</TermsMarkdown>,
          );
          expect(container.querySelector('h1')).toBeNull();
        });

        it('showH1=true 시 H1 을 text-display 토큰으로 렌더한다', () => {
          const { container } = render(
            <TermsMarkdown showH1>{'# 이용약관\n\n본문'}</TermsMarkdown>,
          );
          const h1 = container.querySelector('h1');
          expect(h1).not.toBeNull();
          expect(h1?.textContent).toBe('이용약관');
          expect(h1?.className).toContain('text-display');
          expect(h1?.className).toContain('font-semibold');
          expect(h1?.className).toContain('text-gray-900');
        });
      });

      describe('기존 매핑 회귀 가드 (UI-SPEC §Typography L82-87 변경 금지)', () => {
        it('h2 가 mt-6 text-base font-semibold 매핑을 유지한다', () => {
          const { container } = render(
            <TermsMarkdown showH1>{'# 제목\n\n## 제1조 (목적)\n\n본문'}</TermsMarkdown>,
          );
          const h2 = container.querySelector('h2');
          expect(h2?.className).toContain('text-base');
          expect(h2?.className).toContain('font-semibold');
          expect(h2?.className).toContain('text-gray-900');
        });

        it('p 가 text-caption leading-relaxed 매핑을 유지한다', () => {
          const { container } = render(
            <TermsMarkdown>{'본문 단락'}</TermsMarkdown>,
          );
          const p = container.querySelector('p');
          expect(p?.className).toContain('text-caption');
          expect(p?.className).toContain('leading-relaxed');
          expect(p?.className).toContain('text-gray-700');
        });
      });
    });
    ```

    핵심 계약:
    - 5개 it() — D-09a default 미렌더, D-09a explicit false 미렌더, D-09b showH1=true 렌더+className, h2 회귀, p 회귀.
    - `import { TermsMarkdown } from '../terms-markdown'` (relative import — codebase analog 와 일치).
    - `next/link` mock 불필요 (TermsMarkdown 은 next/link 미사용).
    - `'use client'` mock 불필요 — vitest jsdom 에서 client component 직접 render 가능 (status-badge.test 와 동일 패턴).

    Wave 0 시점에서는 첫 3개 it() 가 RED (showH1 prop 미존재로 TS 에러 또는 default false 가 적용 안되어 H1 렌더), 나머지 2개는 이미 GREEN. **TS 에러는 정상** — Wave 1 에서 prop 추가 시 자동 해소.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run components/legal/__tests__/terms-markdown.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/components/legal/__tests__/terms-markdown.test.tsx` exit 0
    - `grep -c "showH1" apps/web/components/legal/__tests__/terms-markdown.test.tsx` ≥ 4 (3 케이스 + skeleton)
    - `grep -F "text-display" apps/web/components/legal/__tests__/terms-markdown.test.tsx` exit 0
    - `grep -F "showH1=true" apps/web/components/legal/__tests__/terms-markdown.test.tsx` exit 0 또는 `grep -F "showH1>" apps/web/components/legal/__tests__/terms-markdown.test.tsx` exit 0
    - vitest 실행 시 5개 it() 가 모두 collect 되며 (TS 컴파일 에러 / RED 일부는 정상)
    - `! grep -E "vi\.mock\(" apps/web/components/legal/__tests__/terms-markdown.test.tsx` (mock 0건)
  </acceptance_criteria>
  <done>5개 it() 가 정의된 신규 파일 존재, vitest 가 파일을 collect 함 (RED/GREEN 비율은 Wave 0 단계에서 무관).</done>
</task>

<task type="auto">
  <name>Task 2: legal page metadata.test.ts 작성 (D-13 metadata 계약 + D-10 dynamic export)</name>
  <files>apps/web/app/legal/__tests__/metadata.test.ts</files>
  <read_first>
    - apps/web/app/layout.tsx L1, L10-13 (Metadata export 패턴 analog — codebase 유일 사례)
    - apps/web/components/performance/__tests__/status-badge.test.tsx (vitest skeleton)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L418-446 (metadata.test 정의)
    - .planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md L201-210 (Copywriting Contract — title/description/canonical 정확값)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L173-178 (Metadata API 검증 — Context7 cited)
  </read_first>
  <action>
    `apps/web/app/legal/__tests__` 디렉토리 신규. vitest config 의 includes 가 `**/*.{test,spec}.?(c|m)[jt]s?(x)` 패턴이라 app/ 하위도 자동 픽업.

    파일 내용 (PATTERNS.md L427-441 인용 + UI-SPEC L201-210 의 정확값 인용):

    ```ts
    import { describe, it, expect } from 'vitest';

    describe('Legal pages metadata (D-10, D-13)', () => {
      describe('/legal/terms', () => {
        it('metadata 객체가 D-13 계약을 만족한다', async () => {
          const mod = await import('@/app/legal/terms/page');
          expect(mod.metadata.title).toBe('이용약관 — Grabit');
          expect(mod.metadata.description).toBe(
            'Grabit 서비스 이용 조건과 회원·회사의 권리·의무를 안내합니다.',
          );
          expect(mod.metadata.alternates?.canonical).toBe(
            'https://heygrabit.com/legal/terms',
          );
          expect(mod.metadata.robots).toMatchObject({ index: true, follow: true });
        });

        it('dynamic export 가 force-static 으로 명시된다 (D-10)', async () => {
          const mod = await import('@/app/legal/terms/page');
          expect(mod.dynamic).toBe('force-static');
        });
      });

      describe('/legal/privacy', () => {
        it('metadata 객체가 D-13 계약을 만족한다', async () => {
          const mod = await import('@/app/legal/privacy/page');
          expect(mod.metadata.title).toBe('개인정보처리방침 — Grabit');
          expect(mod.metadata.description).toBe(
            'Grabit이 수집·이용하는 개인정보 항목과 처리 목적, 보유 기간 및 이용자의 권리를 안내합니다.',
          );
          expect(mod.metadata.alternates?.canonical).toBe(
            'https://heygrabit.com/legal/privacy',
          );
          expect(mod.metadata.robots).toMatchObject({ index: true, follow: true });
        });

        it('dynamic export 가 force-static 으로 명시된다', async () => {
          const mod = await import('@/app/legal/privacy/page');
          expect(mod.dynamic).toBe('force-static');
        });
      });

      describe('/legal/marketing', () => {
        it('metadata 객체가 D-13 계약을 만족한다', async () => {
          const mod = await import('@/app/legal/marketing/page');
          expect(mod.metadata.title).toBe('마케팅 정보 수신 동의 — Grabit');
          expect(mod.metadata.description).toBe(
            'Grabit이 발송하는 마케팅 정보의 수신 항목, 전송 수단, 동의 거부 권리를 안내합니다.',
          );
          expect(mod.metadata.alternates?.canonical).toBe(
            'https://heygrabit.com/legal/marketing',
          );
          expect(mod.metadata.robots).toMatchObject({ index: true, follow: true });
        });

        it('dynamic export 가 force-static 으로 명시된다', async () => {
          const mod = await import('@/app/legal/marketing/page');
          expect(mod.dynamic).toBe('force-static');
        });
      });
    });
    ```

    핵심 계약:
    - **dynamic import** 사용 (`await import(...)`) — Wave 0 시점 page.tsx 미존재 시 모듈 해석 실패는 RED 정상.
    - 6 개 it() (3 페이지 × 2 검증).
    - `metadata` / `dynamic` 만 import — `default` export (server component function) 는 호출 안 함 (RESEARCH §Wave 0 Gaps L576: "server-component default function 직접 render 회피").
    - title 마지막 dash 는 **em-dash `—`** (U+2014) — UI-SPEC L227 명시 ("한국어·법적 문서 톤에 더 적합 — Phase 16 단독 결정"). hyphen-minus `-` (U+002D) 가 아님.
    - description 정확값 글자수: terms ~80자, privacy ~120자, marketing ~85자 (UI-SPEC §Copywriting L208-210).

    Wave 0 시점: 모든 it() 가 RED ("Cannot find module '@/app/legal/terms/page'"). Wave 1 에서 page.tsx 신규 생성 시 GREEN 전환.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run app/legal/__tests__/metadata.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/app/legal/__tests__/metadata.test.ts` exit 0
    - `grep -c "describe(" apps/web/app/legal/__tests__/metadata.test.ts` ≥ 4 (top-level 1 + 3 페이지)
    - `grep -c "import('@/app/legal/" apps/web/app/legal/__tests__/metadata.test.ts` ≥ 6 (3 페이지 × 2 it)
    - `grep -F "이용약관 — Grabit" apps/web/app/legal/__tests__/metadata.test.ts` exit 0 (em-dash 정확값)
    - `grep -F "개인정보처리방침 — Grabit" apps/web/app/legal/__tests__/metadata.test.ts` exit 0
    - `grep -F "마케팅 정보 수신 동의 — Grabit" apps/web/app/legal/__tests__/metadata.test.ts` exit 0
    - `grep -F "https://heygrabit.com/legal/terms" apps/web/app/legal/__tests__/metadata.test.ts` exit 0
    - `grep -F "force-static" apps/web/app/legal/__tests__/metadata.test.ts` exit 0
    - `! grep -E "function Page|export default" apps/web/app/legal/__tests__/metadata.test.ts` (default export 호출 없음)
  </acceptance_criteria>
  <done>6개 it() 가 정의된 metadata.test.ts 가 존재하며, em-dash + 정확한 description 문자열이 인용됨. Wave 0 RED 정상.</done>
</task>

<task type="auto">
  <name>Task 3: footer.test.tsx 작성 (D-03 href 계약 + D-04 marketing 미노출 회귀 가드)</name>
  <files>apps/web/components/layout/__tests__/footer.test.tsx</files>
  <read_first>
    - apps/web/components/layout/footer.tsx (현재 — 3개 href="#")
    - apps/web/components/layout/__tests__/mobile-tab-bar.test.tsx L26-44 (RTL href 검증 analog — 동일 디렉토리, exact 패턴)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L450-474 (footer.test 정의)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L549-550 (D-03/D-04 grep target)
  </read_first>
  <action>
    파일 내용 (PATTERNS.md L455-471 + mobile-tab-bar.test 패턴 차용):

    ```tsx
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { Footer } from '../footer';

    describe('Footer (D-03, D-04)', () => {
      describe('링크 href 계약 (D-03)', () => {
        it('이용약관 링크가 /legal/terms 로 연결된다', () => {
          render(<Footer />);
          const link = screen.getByText('이용약관').closest('a');
          expect(link?.getAttribute('href')).toBe('/legal/terms');
        });

        it('개인정보처리방침 링크가 /legal/privacy 로 연결되며 font-semibold 강조를 유지한다 (정통망법)', () => {
          render(<Footer />);
          const link = screen.getByText('개인정보처리방침').closest('a');
          expect(link?.getAttribute('href')).toBe('/legal/privacy');
          expect(link?.className).toContain('font-semibold');
        });

        it('고객센터 링크가 mailto:support@heygrabit.com 으로 변경된다', () => {
          render(<Footer />);
          const link = screen.getByText('고객센터').closest('a');
          expect(link?.getAttribute('href')).toBe('mailto:support@heygrabit.com');
        });

        it('고객센터 링크에 target/rel 이 부착되지 않는다 (mailto 는 새 탭 의미 없음)', () => {
          render(<Footer />);
          const link = screen.getByText('고객센터').closest('a');
          expect(link?.getAttribute('target')).toBeNull();
          expect(link?.getAttribute('rel')).toBeNull();
        });
      });

      describe('마케팅 수신 동의 미노출 (D-04 회귀 가드)', () => {
        it('Footer 에 /legal/marketing 링크가 등장하지 않는다', () => {
          const { container } = render(<Footer />);
          expect(container.innerHTML).not.toContain('/legal/marketing');
        });

        it('Footer 에 "마케팅" 텍스트가 등장하지 않는다', () => {
          render(<Footer />);
          expect(screen.queryByText(/마케팅/)).toBeNull();
        });
      });

      describe('변경 금지 영역 (UI-SPEC §Layout Contract)', () => {
        it('Copyright 라인이 변경되지 않는다', () => {
          render(<Footer />);
          expect(screen.getByText(/© 2026 Grabit\. All rights reserved\./)).not.toBeNull();
        });
      });
    });
    ```

    핵심 계약:
    - **next/link mock 정책 (실용주의):** Footer 가 `usePathname` 을 사용하지 않아 router context 가 무관하므로, jsdom 호환을 위해 필요시 `vi.mock('next/link', () => ({ default: ({ children, href, className, ...rest }: any) => <a href={href} className={className} {...rest}>{children}</a> }))` 를 파일 상단에 추가 가능. mock 가 있어도 href/className 검증 계약은 동일하게 유지된다 — `getAttribute('href')` / `closest('a')` 모두 native `<a>` 로 렌더된 결과를 검증한다. 우선 mock 없이 작성하고, 실행 단계에서 next/link 관련 jsdom 에러 발생 시 fallback mock 추가.
    - **mailto 검증은 정확 매치 (`toBe(...)`)** — `toContain` 사용 금지. T-16-01 (오픈리디렉트 회피) 위협 완화: relative path / mailto 외 외부 host URL 차단.
    - **D-04 회귀 가드 2 케이스** — innerHTML 에 `/legal/marketing` 부재 + `마케팅` 텍스트 부재. 둘 다 검증 (개발자가 한 쪽만 추가하는 실수 방지).
    - **target/rel null 검증** — UI-SPEC L241 "target=\"_blank\" / rel 부착 금지" lock 강제.
    - 7개 it() 정의 — 모두 Wave 0 시점에는 RED ("이용약관" closest('a') href === "#" 이지 "/legal/terms" 아님). Wave 2 에서 footer.tsx 패치 시 GREEN 전환.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run components/layout/__tests__/footer.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/components/layout/__tests__/footer.test.tsx` exit 0
    - `grep -c "it(" apps/web/components/layout/__tests__/footer.test.tsx` ≥ 7
    - `grep -F "/legal/terms" apps/web/components/layout/__tests__/footer.test.tsx` exit 0
    - `grep -F "/legal/privacy" apps/web/components/layout/__tests__/footer.test.tsx` exit 0
    - `grep -F "mailto:support@heygrabit.com" apps/web/components/layout/__tests__/footer.test.tsx` exit 0
    - `grep -F "/legal/marketing" apps/web/components/layout/__tests__/footer.test.tsx` exit 0 (회귀 가드 string 자체는 존재)
    - `grep -F "마케팅" apps/web/components/layout/__tests__/footer.test.tsx` exit 0 (queryByText 회귀 케이스 string)
    - `grep -F "font-semibold" apps/web/components/layout/__tests__/footer.test.tsx` exit 0
  </acceptance_criteria>
  <done>7개 it() 가 정의된 footer.test.tsx 가 존재하며, mailto 정확매치/D-04 회귀가드/Copyright 변경금지 모두 사양화됨. Wave 0 RED 정상.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 ↔ 미래 구현 | Wave 0 의 테스트가 Wave 1~3 의 contract spec — 테스트가 약하면 구현 회귀 검출 실패 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering / Spoofing (Footer 외부 host URL 침투) | apps/web/components/layout/footer.tsx | mitigate | Wave 0 의 footer.test 가 `toBe('/legal/terms')` / `toBe('mailto:support@heygrabit.com')` 정확 매치 — 외부 host (e.g. `https://attacker.com/legal/terms`) 가 들어오면 RED. Wave 2 가 동일 테스트로 검증됨. |
| T-16-04 | Tampering (showH1 default 회귀) | apps/web/components/legal/terms-markdown.tsx | mitigate | terms-markdown.test 의 it() 2건이 `showH1` 미지정 + `showH1={false}` 양쪽에서 H1 미렌더를 강제 — Wave 1 PR 에서 default 값 변경 시 RED 알림. |
</threat_model>

<verification>
- 3개 신규 테스트 파일 존재 (`apps/web/components/legal/__tests__/terms-markdown.test.tsx`, `apps/web/app/legal/__tests__/metadata.test.ts`, `apps/web/components/layout/__tests__/footer.test.tsx`)
- vitest 가 모두 collect 함: `pnpm --filter @grabit/web exec vitest run terms-markdown metadata footer --reporter=verbose 2>&1 | grep -E "Test Files|Tests"`
- 신규 install 0건: `git diff -- apps/web/package.json` empty
- Wave 0 RED 비율은 무관 — Wave 1~3 가 GREEN 전환을 검증함 (sampling continuity)
</verification>

<success_criteria>
- [ ] 3개 vitest 파일이 신규 추가됨
- [ ] terms-markdown.test 가 5개 it() 보유 (D-09 양방향 + 회귀 2)
- [ ] metadata.test 가 6개 it() 보유 (3 페이지 × 2 검증)
- [ ] footer.test 가 7개 it() 보유 (D-03 href 4 + D-04 회귀 2 + 변경금지 1)
- [ ] em-dash (`—`) / mailto 정확매치 / className contain 검증 모두 적용
- [ ] vi.mock 사용 시 next/link 만 fallback 으로 허용 (Footer router context 무관 — 실행 단계 결정)
- [ ] vitest collect 시 Wave 0 의 RED 케이스 식별 가능 (Wave 1~3 가 전환할 contract 명시)
</success_criteria>

<output>
After completion, create `.planning/phases/16-legal-pages-launch-url/16-01-SUMMARY.md`:
- 3개 테스트 파일의 it() 개수와 pass/fail 분포 (Wave 0 시점)
- 후속 wave 가 GREEN 전환시킬 의존성 매핑 (terms-markdown.test → Wave 1 / metadata.test → Wave 1 / footer.test → Wave 2)
- **VALIDATION.md §Per-Task Verification Map 갱신 규칙 (codex MED-1 처리, REVIEWS adjudication accept):** Wave 0 단계에서는 16-01-01/02/03 status 를 `✅` 단일 마크로 갱신하지 않는다. 두 차원을 분리 표기:
  - **Artifact 생성** (테스트 파일 존재 + vitest collect 성공) → `✅ artifact`
  - **Behavioral GREEN** (모든 it() PASS) → Wave 0 시점에는 `⏳ behavior` (RED 정상). Wave 1 (Plan 02 Task 1, Task 2) 완료 시 terms-markdown.test 5건 + metadata.test 6건 → `✅ behavior`. Wave 2 (Plan 03) 완료 시 footer.test 7건 → `✅ behavior`.
  - SUMMARY.md 의 status 표 또는 본문에 위 형식으로 명시 — RED test 가 falsified GREEN 으로 dashboard 에 보이지 않게 보호.
</output>
