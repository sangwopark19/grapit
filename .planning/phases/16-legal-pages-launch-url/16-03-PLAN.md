---
phase: 16
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - apps/web/components/layout/footer.tsx
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "Footer 의 placeholder href=\"#\" 3건이 모두 실 경로로 교체된다 (D-03)"
    - "이용약관 링크가 next/link 의 <Link href=\"/legal/terms\"> 로 매핑된다 (D-03)"
    - "개인정보처리방침 링크가 <Link href=\"/legal/privacy\"> 로 매핑되며 font-semibold 강조 유지 (정통망법) (D-03)"
    - "고객센터 링크가 <a href=\"mailto:support@heygrabit.com\"> 로 component 교체된다 (Link → 네이티브 anchor) (D-03)"
    - "Footer 에 /legal/marketing 링크는 등장하지 않는다 (D-04)"
    - "Wave 0 의 footer.test 7건이 모두 GREEN 으로 전환된다"
  artifacts:
    - path: "apps/web/components/layout/footer.tsx"
      provides: "3개 실 경로 링크 + mailto + D-04 marketing 미노출"
      contains: "mailto:support@heygrabit.com"
  key_links:
    - from: "apps/web/components/layout/footer.tsx 이용약관/개인정보처리방침 링크"
      to: "apps/web/app/legal/{terms,privacy}/page.tsx (Plan 02 산출)"
      via: "next/link client navigation"
      pattern: 'href="/legal/(terms|privacy)"'
    - from: "apps/web/components/layout/footer.tsx 고객센터 링크"
      to: "support@heygrabit.com mailbox"
      via: "OS 기본 메일 클라이언트 prompt (mailto:)"
      pattern: "mailto:support@heygrabit.com"
---

<objective>
Wave 2 — Phase 13 P02 D-07 에서 확정된 도메인 컨벤션(`heygrabit.com`)과 Plan 02 가 신규 게시한 `/legal/{terms,privacy}` 라우트를 Footer 에 연결한다. 28 LOC server component 의 3개 placeholder `href="#"` 만 교체 — 시각 / 카피 / 토큰 변경 0건.

Purpose: D-03 의 정통망법상 강조 의무(`font-semibold` 유지) 와 D-04 의 마케팅 동의 페이지 Footer 미노출 정책을 모두 충족. Plan 02 가 `/legal/marketing` 을 라우팅 가능 상태로 만들었지만 Footer 에는 노출하지 않는 의도(회원가입 동의 모달과 향후 마이페이지 동의 철회 화면 전용) 를 회귀 가드로 lock.

Output: 1 파일 수정. Wave 0 의 footer.test 7건 RED → GREEN 전환.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-legal-pages-launch-url/16-CONTEXT.md
@.planning/phases/16-legal-pages-launch-url/16-RESEARCH.md
@.planning/phases/16-legal-pages-launch-url/16-PATTERNS.md
@.planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md
@.planning/phases/16-legal-pages-launch-url/16-VALIDATION.md
@.planning/phases/16-legal-pages-launch-url/16-01-SUMMARY.md
@.planning/phases/16-legal-pages-launch-url/16-02-SUMMARY.md

@apps/web/components/layout/footer.tsx

<interfaces>
<!-- Plan 02 에서 신규 게시된 라우트 — 본 plan 의 href 가 가리키는 대상. -->

From apps/web/app/legal/terms/page.tsx (Plan 02 산출):
```typescript
// SSG prerender — Footer Link 클릭 시 client-side soft navigation
export const dynamic = 'force-static';
export default function TermsPage(): JSX.Element;
```

From apps/web/app/legal/privacy/page.tsx (Plan 02 산출):
```typescript
export const dynamic = 'force-static';
export default function PrivacyPage(): JSX.Element;
```

From apps/web/components/layout/footer.tsx (현재 — 수정 대상):
```tsx
import Link from 'next/link';

export function Footer() {
  return (
    <footer className="mt-auto min-h-[120px] bg-gray-100">
      <div className="mx-auto max-w-[1200px] px-6 py-8">
        <div className="flex flex-wrap items-center gap-2 text-sm text-gray-900">
          <Link href="#" className="hover:underline">이용약관</Link>                       {/* L9 */}
          <span className="text-gray-400">|</span>
          <Link href="#" className="font-semibold hover:underline">개인정보처리방침</Link>  {/* L13 */}
          <span className="text-gray-400">|</span>
          <Link href="#" className="hover:underline">고객센터</Link>                       {/* L17 */}
        </div>
        <p className="mt-4 text-sm text-gray-500">&copy; 2026 Grabit. All rights reserved.</p>
      </div>
    </footer>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer href 3건 교체 (D-03 + D-04 invariant)</name>
  <files>apps/web/components/layout/footer.tsx</files>
  <read_first>
    - apps/web/components/layout/footer.tsx (전 파일 29 LOC — 정확한 현재 상태)
    - .planning/phases/16-legal-pages-launch-url/16-PATTERNS.md L262-318 (Footer patch spec)
    - .planning/phases/16-legal-pages-launch-url/16-RESEARCH.md L213-232 (Implementation Approach Axis 2)
    - .planning/phases/16-legal-pages-launch-url/16-UI-SPEC.md L313-322 (Component Inventory diff)
    - apps/web/components/layout/__tests__/footer.test.tsx (Wave 0 산출물 — 본 task 가 GREEN 전환시킬 대상)
  </read_first>
  <action>
    `apps/web/components/layout/footer.tsx` 의 3개 link 만 교체 (PATTERNS.md L296-306 정확한 diff):

    **Diff:**
    ```diff
    - <Link href="#" className="hover:underline">
    -   이용약관
    - </Link>
    + <Link href="/legal/terms" className="hover:underline">
    +   이용약관
    + </Link>

      <span className="text-gray-400">|</span>

    - <Link href="#" className="font-semibold hover:underline">
    -   개인정보처리방침
    - </Link>
    + <Link href="/legal/privacy" className="font-semibold hover:underline">
    +   개인정보처리방침
    + </Link>

      <span className="text-gray-400">|</span>

    - <Link href="#" className="hover:underline">
    -   고객센터
    - </Link>
    + <a href="mailto:support@heygrabit.com" className="hover:underline">
    +   고객센터
    + </a>
    ```

    **변경 후 전 파일 형태:**

    ```tsx
    import Link from 'next/link';

    export function Footer() {
      return (
        <footer className="mt-auto min-h-[120px] bg-gray-100">
          <div className="mx-auto max-w-[1200px] px-6 py-8">
            {/* Legal links */}
            <div className="flex flex-wrap items-center gap-2 text-sm text-gray-900">
              <Link href="/legal/terms" className="hover:underline">
                이용약관
              </Link>
              <span className="text-gray-400">|</span>
              <Link href="/legal/privacy" className="font-semibold hover:underline">
                개인정보처리방침
              </Link>
              <span className="text-gray-400">|</span>
              <a href="mailto:support@heygrabit.com" className="hover:underline">
                고객센터
              </a>
            </div>

            {/* Copyright */}
            <p className="mt-4 text-sm text-gray-500">
              &copy; 2026 Grabit. All rights reserved.
            </p>
          </div>
        </footer>
      );
    }
    ```

    **핵심 계약:**
    - 3번째 링크: `Link` (next/link) → 네이티브 `<a>` 로 **컴포넌트 교체** (mailto: 는 internal navigation 이 아님 — next/link 의 prefetch/soft-navigation 의미 없음).
    - `Link` import 유지 — `<Link>` 가 2건 잔존 (1, 2번째 링크에서 사용).
    - **font-semibold** 는 "개인정보처리방침" 1건에만 유지 — 정통망법 강조 의무 (UI-SPEC L313-322 lock).
    - **target="_blank"** / **rel** 부착 금지 — mailto: 는 새 탭 의미 없음 (UI-SPEC L241).
    - 시각 hint(메일 icon 등) 추가 금지 — 다른 두 링크와 시각 균형 유지 (UI-SPEC L242).
    - **/legal/marketing 추가 금지** (D-04) — Footer 에는 terms/privacy 만.

    **변경 금지:**
    - 외부 div className (`flex flex-wrap items-center gap-2 text-sm text-gray-900`) 변경 금지.
    - 구분자 `<span className="text-gray-400">|</span>` 2건 변경 금지.
    - Copyright 라인 (`<p className="mt-4 text-sm text-gray-500">&copy; 2026 Grabit. All rights reserved.</p>`) 변경 금지.
    - max-w-[1200px] / px-6 py-8 / min-h-[120px] / bg-gray-100 등 layout 토큰 변경 금지.
    - import 변경 (`Link` 유지, 추가 import 0건).

    **T-16-01 (medium) 위협 완화:**
    - href 값은 모두 **relative path** (`/legal/terms`, `/legal/privacy`) 또는 **mailto:** 스킴만 — 외부 host (e.g. `https://attacker.com/legal/terms`) URL 절대 금지.
    - Wave 0 의 footer.test 가 `toBe('/legal/terms')` 정확 매치로 차단.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grabit/web exec vitest run components/layout/__tests__/footer.test.tsx --reporter=verbose 2>&1 | tail -30 && pnpm --filter @grabit/web typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'href="/legal/terms"' apps/web/components/layout/footer.tsx` exit 0
    - `grep -F 'href="/legal/privacy"' apps/web/components/layout/footer.tsx` exit 0
    - `grep -F 'href="mailto:support@heygrabit.com"' apps/web/components/layout/footer.tsx` exit 0
    - `! grep -F 'href="#"' apps/web/components/layout/footer.tsx` (placeholder href 0건)
    - `! grep -F '/legal/marketing' apps/web/components/layout/footer.tsx` (D-04 회귀 가드 — Footer 에 marketing 링크 부재)
    - `! grep -F '마케팅' apps/web/components/layout/footer.tsx` (마케팅 텍스트 부재)
    - `grep -F 'font-semibold hover:underline' apps/web/components/layout/footer.tsx` exit 0 (정통망법 강조 보존)
    - `grep -c '<Link' apps/web/components/layout/footer.tsx` 결과 2 (3번째는 `<a>` 로 교체됨)
    - `grep -c '<a href="mailto:' apps/web/components/layout/footer.tsx` 결과 1
    - `! grep -E 'target="_blank"|rel="noopener' apps/web/components/layout/footer.tsx` (mailto 에 target/rel 부착 0건)
    - `grep -F '© 2026 Grabit. All rights reserved.' apps/web/components/layout/footer.tsx` exit 0 (Copyright 변경 없음 — &copy; entity 또는 © character 어느 쪽이든 OK)
    - `grep -F 'max-w-[1200px]' apps/web/components/layout/footer.tsx` exit 0 (layout 토큰 변경 없음)
    - vitest: footer.test 7건 모두 PASS (D-03 href 4 + D-04 회귀 2 + 변경금지 1)
    - typecheck pass: `pnpm --filter @grabit/web typecheck` exit 0
  </acceptance_criteria>
  <done>Footer 3 링크 교체 완료. mailto 정확매치 + relative path 강제 + D-04 회귀 가드 모두 PASS. Wave 0 footer.test 7건 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Footer 링크 ↔ 외부 URL | 사용자가 무심코 외부 host 를 href 로 입력할 위험 — relative path 만 허용 |
| Footer 링크 ↔ mailbox | mailto: 는 OS 기본 메일 클라이언트로 위임 — 봇 크롤링 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering / Spoofing (Footer 외부 host URL 침투 → phishing) | apps/web/components/layout/footer.tsx | mitigate | (1) 코드 리뷰: href 값이 모두 relative `/legal/...` 또는 `mailto:` 스킴 시작인지 확인. (2) Wave 0 footer.test 가 정확 매치(`toBe('/legal/terms')` 등) 로 외부 host 차단. (3) acceptance_criteria 의 grep 이 `href="/legal/terms"` 정확 string 보장. |
| T-16-03 | Information Disclosure (mailto: 봇 크롤링 → spam 폭증) | apps/web/components/layout/footer.tsx (mailto 링크) | accept | privacy@/support@ 는 dedicated alias — 봇 spam 은 메일 서버 단의 spam filter 로 처리. 별도 도메인 alias 분리는 Phase 16 scope 외 (deferred). 정통망법 정보주체 권리 보장 의무가 더 우선 — 메일 주소 노출은 의도된 결정. |
</threat_model>

<verification>
- footer.tsx 의 href 패턴 검증: `grep -E 'href="(/legal/(terms|privacy)|mailto:support@heygrabit.com)"' apps/web/components/layout/footer.tsx` ≥ 3건
- D-04 회귀 가드: `! grep -F '/legal/marketing' apps/web/components/layout/footer.tsx`
- vitest footer.test 7건 GREEN
- typecheck/lint pass
- Wave 1 (Plan 02) + Wave 0 (Plan 01) 산출물에 의존 — 단독 실행 시 footer.test 의 D-03 케이스만 GREEN 가능 (D-04 회귀 가드는 무관)
</verification>

<success_criteria>
- [ ] 3개 href 교체 완료 (`/legal/terms`, `/legal/privacy`, `mailto:support@heygrabit.com`)
- [ ] 3번째 링크가 `<Link>` → `<a>` 로 component 교체됨
- [ ] font-semibold 는 개인정보처리방침에만 유지
- [ ] target/rel 부착 없음
- [ ] /legal/marketing 미노출 (D-04 회귀 가드)
- [ ] Copyright + layout 토큰 변경 없음
- [ ] Wave 0 의 footer.test 7건 모두 GREEN
- [ ] typecheck/lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-legal-pages-launch-url/16-03-SUMMARY.md`:
- Footer.tsx 변경 diff (3 라인 정도의 patch 요약)
- footer.test 7건 GREEN 확인
- T-16-01 위협 완화 status (relative/mailto 만 사용)
- 다음 wave (Plan 04 — MD 콘텐츠 보강) 진입 가능 확인
</output>
</content>
</invoke>