---
phase: 09.1
plan: 03
type: execute
wave: 2
depends_on: ["09.1-02"]
files_modified:
  - apps/web/e2e/helpers/auth.ts
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "수정된 loginAsTestUser helper 가 CI 에서 POST /api/v1/auth/login 을 호출했을 때 HTTP 200 + accessToken 을 받는다 (S3 신호 green)"
    - "수정된 helper 응답의 Set-Cookie 가 page.context() 에 replay 되어 후속 navigation 이 /auth 로 redirect 되지 않는다"
    - "서버측 코드(main.ts 제외 — 진단용만)는 수정되지 않는다 — production 회귀 접점 0 (범주 A/B 경로) 또는 최소 침습 (범주 C 경로)"
    - "AUTH_LOGIN_DEBUG 에서 수정 후 Playwright 요청의 bodyEmail/bodyPasswordLen 이 curl baseline 과 동일하게 채워진다 (S1 정상화)"
  artifacts:
    - path: "apps/web/e2e/helpers/auth.ts"
      provides: "범주 A 확정 시: Node 22 native fetch 기반 loginAsTestUser. 범주 B: Playwright headers/encoding 교정. 범주 C: (fallback — 서버측 수정 동반, 별도 task 분기)"
      exports: ["loginAsTestUser"]
  key_links:
    - from: "apps/web/e2e/helpers/auth.ts"
      to: "http://localhost:8080/api/v1/auth/login"
      via: "전송 스택 선택 (A: native fetch via undici / B: Playwright APIRequestContext with corrected headers / C: unchanged client, server body-parser 변경)"
      pattern: "auth/login"
    - from: "apps/web/e2e/helpers/auth.ts"
      to: "page.context().addCookies"
      via: "refreshToken cookie replay with secure: false, domain: localhost"
      pattern: "addCookies"
---

<objective>
Wave 2: Plan 02 checkpoint 에서 확정된 원인 범주(A/B/C) 에 대응하는 **단일 fix 분기** 를 실행한다. planner 는 모든 분기를 사전 task 로 정의하되 executor 는 Plan 02 SUMMARY 의 승인된 범주에 해당하는 task 하나만 실행한다.

Purpose: "관찰 결과 기반 최소 침습 수정" (RESEARCH.md §Summary Primary recommendation). 여러 분기를 동시에 적용하면 원인 확정이 fuzzy 해지고 symptom masking 발생.

Output: `apps/web/e2e/helpers/auth.ts` 의 단일 버전 (선택된 분기만 반영). 서버측 변경은 범주 C 일 때만 허용.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-CONTEXT.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-02-SUMMARY.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/diff.md
@apps/web/e2e/helpers/auth.ts
@apps/api/src/modules/auth/auth.controller.ts

<interfaces>
<!-- 수정 후 helper 가 유지해야 하는 외부 계약 -->

```typescript
// Contract (수정 전/후 동일 유지): loginAsTestUser(page: Page) => Promise<void>
// 성공 후 조건 (모든 분기에서 동일):
//   1. POST /api/v1/auth/login 이 HTTP 200 + body.accessToken 반환
//   2. Set-Cookie refreshToken 이 page.context() 에 저장됨 (domain=localhost, path=/, secure=false)
//   3. 이후 page.goto() 시 AuthInitializer 가 refreshToken → accessToken exchange 성공
// 실패 시: throw Error with status + body + hint (현재 패턴 유지)
```
</interfaces>

<branch_selection>
**MANDATORY: Plan 02 SUMMARY 의 "확정된 원인 범주" 를 읽고 정확히 하나의 Task(3A/3B/3C)만 실행한다. 다른 분기는 skip.**

- 범주 A → Task 1 + Task 3A + Task 4
- 범주 B → Task 1 + Task 3B + Task 4
- 범주 C → Task 1 + Task 3C + Task 4 (+ 서버측 추가 주의)
</branch_selection>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 현재 helper 파일 백업 주석 + 분기 선택 결과를 helper 상단 주석에 기록</name>
  <files>apps/web/e2e/helpers/auth.ts</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/helpers/auth.ts (현재 loginAsTestUser 구조 + 주석 — Blocker B2 주석 맥락 보존)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-02-SUMMARY.md (확정된 원인 범주 A/B/C)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/diff.md
  </read_first>
  <action>
    `apps/web/e2e/helpers/auth.ts` 파일 상단 JSDoc 블록에 다음을 추가한다 (기존 구현은 이 task 에서는 수정하지 않음 — Task 3A/B/C 가 교체):

    ```typescript
    /**
     * Phase 09.1 fix — 원인 범주: <A | B | C> (per artifacts/diff.md + 09.1-02-SUMMARY.md).
     * CI evidence: <GitHub Actions run URL 또는 commit SHA>
     *
     * Previous 5 client-side workarounds (all 401) are documented in 09.1-CONTEXT.md §decisions.
     * DO NOT re-attempt those; observation confirmed the cause layer.
     */
    ```

    `<A | B | C>` 플레이스홀더는 02-SUMMARY 의 확정 범주 단일 값으로 실제 치환.

    기존 file body(현재 L1-100)는 변경 금지 — Task 3A/B/C 가 loginAsTestUser 본문을 교체.
  </action>
  <verify>
    <automated>
      grep -qE "Phase 09.1 fix — 원인 범주: (A|B|C)" apps/web/e2e/helpers/auth.ts && \
      grep -q "09.1-02-SUMMARY.md" apps/web/e2e/helpers/auth.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/e2e/helpers/auth.ts` 상단 JSDoc 에 `Phase 09.1 fix — 원인 범주: X` (X = A/B/C 하나) 기록.
    - 02-SUMMARY.md 참조 주석 1건 존재.
    - Task 1 단독으로는 기능적 변경 없음 — 기존 `loginAsTestUser` 구현 그대로 (Task 3 이 교체).
  </acceptance_criteria>
  <done>
    Helper 파일 상단에 원인 범주와 증거 경로가 기록되어, 향후 git blame 시 어느 분기로 수정했는지 명확.
  </done>
</task>

<task type="auto">
  <name>Task 3A (execute IFF cause-a): Node 22 native fetch 로 loginAsTestUser 본문 교체</name>
  <files>apps/web/e2e/helpers/auth.ts</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/helpers/auth.ts (현재 본문 + cookie replay 패턴)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Architecture Patterns §Pattern 3 Node native fetch helper, §Code Examples §3, §Pitfall 4 cookie secure)
    - /Users/sangwopark19/icons/grapit/apps/api/src/modules/auth/auth.controller.ts (setRefreshTokenCookie 의 실제 cookie 속성 — sameSite: 'none', secure: true)
  </read_first>
  <action>
    **Skip this task if 02-SUMMARY 의 원인 범주가 A 가 아니다.**

    `loginAsTestUser` 함수 본문을 Node 22 `globalThis.fetch` (undici) 기반으로 전체 교체. Playwright APIRequestContext 는 사용하지 않는다.

    구체 요건:
    - `import { request as playwrightRequest } from '@playwright/test';` 를 **제거** (더 이상 필요 없음). `import type { Page } from '@playwright/test';` 는 유지.
    - helper 본문은 RESEARCH.md §Code Examples §3 의 예시를 그대로 따르되 아래를 반드시 포함:
      - `const res = await fetch(\`${apiURL}/api/v1/auth/login\`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) });`
      - `if (!res.ok)` 일 때 `res.text()` 로 body 조회 후 `Error` throw — 현재 helper 의 에러 메시지 패턴 (URL, body, headers, hint) 유지.
      - `const setCookieValues = res.headers.getSetCookie();` — Node 20+ WHATWG fetch API.
      - `setCookieValues` 에서 `refreshToken=` 으로 시작하는 cookie 의 값만 추출 (`.split(';')[0].split('=')[1]`).
      - `refreshToken` 부재 시 throw.
      - `page.context().addCookies([{ name: 'refreshToken', value, domain: 'localhost', path: '/', httpOnly: true, secure: false, sameSite: 'Lax' }]);`
        - **중요:** `secure: false`, `sameSite: 'Lax'` 로 하드코딩 (CI 는 http://localhost — server 의 `secure: true`/`sameSite: 'none'` 를 그대로 replay 하면 Chromium 이 cookie 저장 거부. RESEARCH.md §Pitfall 4 근거).
    - strict typing: `any` 금지. `unknown` / type narrowing 로 해결.
    - `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` / `E2E_API_URL` 환경변수 기본값은 현재 helper 와 동일(`admin@grapit.test` / `TestAdmin2026!` / `http://localhost:8080`).
    - 기존 LoginResponse interface 는 유지.
    - JSDoc 에 다음 한 줄 추가: `// undici (Node 22 native fetch) bypasses Playwright Chromium network stack — cause A fix.`
  </action>
  <verify>
    <automated>
      # cause-a 경로: native fetch 사용, playwrightRequest 미사용
      grep -q "const res = await fetch(" apps/web/e2e/helpers/auth.ts && \
      ! grep -q "playwrightRequest" apps/web/e2e/helpers/auth.ts && \
      grep -q "getSetCookie" apps/web/e2e/helpers/auth.ts && \
      grep -q "secure: false" apps/web/e2e/helpers/auth.ts && \
      grep -q "sameSite: 'Lax'" apps/web/e2e/helpers/auth.ts && \
      ! grep -E "\bany\b" apps/web/e2e/helpers/auth.ts && \
      pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "globalThis.fetch\|await fetch("` 매치 ≥ 1.
    - `grep -c "playwrightRequest"` 결과 `0` (완전 제거).
    - `grep "getSetCookie"` 매치 = 1.
    - `grep "secure: false"` 매치 = 1 AND `grep "sameSite: 'Lax'"` 매치 = 1 (cookie replay 속성 하드코딩).
    - `grep -w "any"` 결과 0건.
    - `pnpm --filter @grapit/web typecheck` exit 0.
    - `loginAsTestUser` 시그니처 `(page: Page): Promise<void>` 유지 (grep 매치 1).
    - `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` fallback 여전히 `admin@grapit.test` / `TestAdmin2026!`.
  </acceptance_criteria>
  <done>
    범주 A 확정 시: helper 가 Node native fetch 로 /auth/login 을 호출하고 Set-Cookie 를 수동 파싱하여 page.context() 에 localhost 용 속성으로 replay. Playwright APIRequestContext 는 helper 에서 제거됨. typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 3B (execute IFF cause-b): Playwright headers / encoding 교정</name>
  <files>apps/web/e2e/helpers/auth.ts</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/helpers/auth.ts (현재 본문)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/diff.md (범주 B 근거 — 어떤 header 가 잘못되었는지)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Architecture Patterns §Decision Tree §원인 B)
  </read_first>
  <action>
    **Skip this task if 02-SUMMARY 의 원인 범주가 B 가 아니다.**

    diff.md 의 "근거" 섹션을 참조하여 문제가 된 header 필드만 최소 교정한다. 예시 시나리오:

    - Content-Type 이 Playwright 쪽에서 누락 / 잘못 설정됨 → `headers: { 'Content-Type': 'application/json; charset=utf-8' }` 로 명시 + charset 명시.
    - Content-Length 미전송으로 body 가 chunked 로 흐름 → `data` 대신 `form`/`multipart` 파라미터로 Content-Length 를 명시적으로 제공하는 형태로 변경 불가 — 이 경우 fallback 은 항상 Task 3A(native fetch) 임을 인지.
    - Playwright 의 `httpHeaders` vs `headers` 옵션 혼동 → 공식 옵션 `headers` 로 통일.

    구체 규칙:
    - 현재 `playwrightRequest.newContext()` + `.post(...)` 구조는 유지.
    - `headers` 객체에서 diff.md 가 지목한 문제 필드만 수정.
    - 수정 후 helper 주석에 "범주 B — <구체 필드 명> 교정" 한 줄 기록.
    - `secure: false` / `sameSite: 'Lax'` 쿠키 replay 속성은 Task 3A 와 동일하게 수정 (Pitfall 4 — 범주 B 경로에서도 동일 적용).

    **주의:** 범주 B 가 실제 근본 원인이 아닐 경우 Plan 04 Task 1 에서 fixme 해제 후 3건이 다시 실패한다. 이 경우 즉시 Plan 03 을 revise → Task 3A(native fetch) 로 전환 (revision mode).
  </action>
  <verify>
    <automated>
      # cause-b 경로: playwrightRequest 유지 + 특정 header 교정
      grep -q "playwrightRequest.newContext" apps/web/e2e/helpers/auth.ts && \
      grep -q "범주 B" apps/web/e2e/helpers/auth.ts && \
      grep -q "secure: false" apps/web/e2e/helpers/auth.ts && \
      ! grep -E "\bany\b" apps/web/e2e/helpers/auth.ts && \
      pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `playwrightRequest.newContext` 사용 유지 (범주 A 와 달리 제거하지 않음).
    - helper 주석에 "범주 B" 문자열 포함.
    - `secure: false` + `sameSite: 'Lax'` cookie replay 속성 유지 (Pitfall 4).
    - diff.md 가 지목한 header 필드가 helper 의 `headers: {...}` 에 정확히 반영.
    - `pnpm --filter @grapit/web typecheck` exit 0.
    - Task 3A 의 native fetch 패턴은 helper 에 존재하지 않음 (`grep "globalThis.fetch\|await fetch(" apps/web/e2e/helpers/auth.ts` 결과 0).
  </acceptance_criteria>
  <done>
    범주 B 확정 시: helper 의 headers 옵션이 diff.md 근거에 맞게 최소 교정되고 주석에 기록. typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 3C (execute IFF cause-c — last resort): 서버측 body-parser / passport-local 최소 조정</name>
  <files>
    apps/web/e2e/helpers/auth.ts
    apps/api/src/main.ts
  </files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/diff.md (원인 C 근거 — req.body 가 채워졌는데 validate 미호출인 특수 케이스)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Anti-Patterns "서버 측 body-parser 옵션 변경을 1차 선택으로 금지", §Open Questions §3 passport-local version)
    - /Users/sangwopark19/icons/grapit/apps/api/src/modules/auth/strategies/local.strategy.ts (usernameField 확인)
    - /Users/sangwopark19/icons/grapit/apps/api/src/main.ts (body-parser 등록 순서)
  </read_first>
  <action>
    **Skip this task if 02-SUMMARY 의 원인 범주가 C 가 아니다.**

    이 경로는 **최후의 수단** — 서버 코드 변경 시 production 회귀 위험 최대. diff.md 의 근거가 "req.body 는 {email, password} 로 정상인데 validate 가 호출되지 않는다" 일 때만 실행.

    구체 규칙:
    - `@nestjs/passport` / `passport-local` 의 lockfile version 확인 (`grep "passport-local" pnpm-lock.yaml` 으로 정확 버전). version bump 가 필요하면 patch version 만 허용 (major/minor 변경 금지).
    - 서버측 수정이 불가피하면 main.ts 의 body-parser 순서 변경 **없이**, auth.controller.ts 의 `@UseGuards(AuthGuard('local'))` 앞에 explicit `@Body()` 주입 또는 `LocalAuthGuard` override 추가 고려.
    - helper(auth.ts) 는 최소 수정 — 현재 구조 유지, 서버측만 수정.
    - 모든 서버측 변경은 `pnpm --filter @grapit/api test` (기존 auth unit 테스트 전체) 0 failure 유지 필수.
    - 서버측 변경은 production 에 자동 반영되므로 Plan 05 Wave 4 에서 제거되지 않음 — helper 주석에 "범주 C — server-side permanent change" 명기.

    **추천 대안:** 범주 C 로 확정되었더라도, 가능하면 Plan 02 checkpoint 단계로 back-step 하여 범주 A(native fetch) 로 전환 (client 전용 수정). 서버 변경은 회귀 테스트 비용이 크다.
  </action>
  <verify>
    <automated>
      grep -q "범주 C" apps/web/e2e/helpers/auth.ts && \
      pnpm --filter @grapit/api test && \
      pnpm --filter @grapit/api typecheck && \
      pnpm --filter @grapit/web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - helper 주석에 "범주 C — server-side permanent change" 포함.
    - `pnpm --filter @grapit/api test` 0 failure — 기존 auth.controller.spec.ts / auth.service.spec.ts 회귀 방지.
    - `pnpm --filter @grapit/api typecheck` exit 0.
    - `pnpm --filter @grapit/web typecheck` exit 0.
    - main.ts 의 helmet / cors / useGlobalFilters / useGlobalPipes / setGlobalPrefix / listen 순서 유지 (grep count 동일).
    - curl baseline 여전히 HTTP 200 (Plan 04 Task 2 에서 재확인).
  </acceptance_criteria>
  <done>
    범주 C 확정 시: 서버측 최소 수정 + helper 주석 기록 + 기존 unit 테스트 0 failure. typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 4: 선택된 분기에 대한 CI smoke 검증 (probe spec 재실행 → HTTP 200 확인)</name>
  <files>
    .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md
  </files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/diff.md
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/diagnostic/auth-login-probe.spec.ts (Plan 02 Task 1)
  </read_first>
  <action>
    Task 3A/B/C 중 하나가 완료되면 변경을 push → CI 재실행.

    1. CI 의 "E2E diagnostic probe" step 로그에서 `[PROBE] status=200` 확인 (이전: 401).
    2. "Smoke test login" step 은 여전히 `HTTP 200` (S5).
    3. api.log 에서 `grep AUTH_LOGIN_DEBUG api.log | wc -l` 가 `2` (curl + probe 각 1회).
    4. 두 번째 AUTH_LOGIN_DEBUG 의 `bodyEmail` 이 `"admin@grapit.test"` 로 채워짐 — 이전 diff.md 의 null/빈값과 비교하여 **명시적으로 정상화** 됨을 확인 (S1 정상화).

    5. `artifacts/fix-verification.md` 생성:
       ```markdown
       # Phase 09.1 — Fix verification (post cause-<A|B|C> apply)

       **CI run:** <URL>
       **Fix branch:** <git SHA>

       ## Before (Plan 02 diff.md 요약)
       - Playwright bodyEmail: null (또는 빈값)
       - Playwright status: 401

       ## After
       - Playwright bodyEmail: admin@grapit.test ✓
       - Playwright bodyPasswordLen: 17 ✓
       - Playwright status: 200 ✓
       - Set-Cookie refreshToken present: ✓
       - curl baseline: HTTP 200 (S5 regression 0) ✓

       ## 3 독립 신호 결과
       - S1 (서버측 req 정상): ✓
       - S3 (response body 정상): ✓
       - S5 (curl regression 0): ✓
       ```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md && \
      grep -q "Playwright status: 200" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md && \
      grep -q "S1 (서버측 req 정상): ✓" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md && \
      grep -q "S5 (curl regression 0): ✓" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `artifacts/fix-verification.md` 파일 존재.
    - "Playwright status: 200" 문자열 포함 (이전 401 에서 변화).
    - S1, S3, S5 3 독립 신호 모두 ✓ 로 기록.
    - CI 의 probe step stdout 에 `[PROBE] status=200` 이 실제로 출력됨 (Actions UI 에서 확인 가능).
    - api.log 의 2번째 AUTH_LOGIN_DEBUG JSON 에 `"bodyEmail":"admin@grapit.test"` 와 `"bodyPasswordLen":17` 이 포함됨 (이전 null/0 에서 변화).
    - `pnpm --filter @grapit/web typecheck` 및 `pnpm --filter @grapit/api typecheck` 모두 exit 0.
  </acceptance_criteria>
  <done>
    선택된 분기 수정이 CI 의 probe spec 을 200 으로 통과시키고, 서버 관찰 로그에서 Playwright body 가 정상적으로 파싱됨을 증명. S1 + S3 + S5 3 신호 green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper (E2E only) → production bundle | helper 는 apps/web/e2e/ 디렉터리 — 프로덕션 빌드에 포함되지 않음 |
| 서버측 수정 (범주 C 경로) → Cloud Run production | 이 phase 에서 범주 C 선택 시 production 에 영구 영향 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-08 | Tampering | apps/api/main.ts body-parser / passport-local 변경이 기존 auth 엔드포인트(login/register/refresh/reset) 에 regression 유발 | mitigate | 범주 C 경로에서만 적용 가능, Task 3C acceptance 에서 `pnpm --filter @grapit/api test` 전체 0 failure 강제. 범주 A/B 는 서버 변경 없음 → 위협 제거. |
| T-09.1-09 | Spoofing | helper 의 cookie replay 속성(`secure: false`, `sameSite: 'Lax'`) 이 실수로 production 코드에 복제되어 cookie hardening 우회 | mitigate | helper 는 apps/web/e2e/ 전용 — production bundle 미포함. 파일 경로 자체가 격리. 추가로 Task 3A/B 주석에 "CI http-only" 명기. |
| T-09.1-10 | Information Disclosure | Node native fetch 경로가 TLS 검증을 우회 | accept | CI 는 `http://localhost:8080` 만 호출 — TLS 검증 자체가 존재하지 않는 경로. production 으로 helper 가 실행되는 경우 없음. |
</threat_model>

<verification>
- 선택된 Task(3A/3B/3C) 의 automated verify exit 0.
- Task 4 의 fix-verification.md 가 S1+S3+S5 green 기록.
- `pnpm --filter @grapit/api test` 여전히 0 failure.
- `pnpm --filter @grapit/web typecheck` 및 `pnpm --filter @grapit/api typecheck` exit 0.
- `git diff --stat main -- apps/api/src/` 가 범주 A/B 경로에서는 `apps/api/src/main.ts` + `req-debug.middleware.ts` 외 변경 0 (Plan 01 변경만 존재).
</verification>

<success_criteria>
- `loginAsTestUser` 가 CI 의 probe spec 을 HTTP 200 으로 통과.
- 서버 AUTH_LOGIN_DEBUG 에서 Playwright 요청이 curl 과 구조적으로 동등한 bodyEmail/bodyPasswordLen 을 보임.
- 선택되지 않은 task(3A/3B/3C 중 나머지 2개) 는 수정 0건 — 단일 분기 원칙 준수.
- curl baseline (S5) 여전히 HTTP 200.
</success_criteria>

<output>
완료 후 `.planning/phases/09.1-.../09.1-03-SUMMARY.md` 생성. SUMMARY 에 최종 선택된 분기와 fix-verification.md 링크 기록.
</output>
