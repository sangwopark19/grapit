---
phase: 09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40
plan: 03
subsystem: testing
tags: [helper, e2e, github-actions-secrets, nullish-coalescing-fix]

requires:
  - phase: 09.1-02
    provides: Approved cause branch (A) + approved fix option (D-3)
provides:
  - apps/web/e2e/helpers/auth.ts hardened — `??` → `||` so empty-string env vars also fall back to seed defaults
  - GitHub repo secrets TEST_USER_EMAIL / TEST_USER_PASSWORD (defense in depth)
  - artifacts/fix-verification.md proving S1+S3+S5 green
affects: [phase 09.1-04, phase 09.1-05]

tech-stack:
  added: []
  patterns:
    - "Helper env fallback robustness: `||` over `??` for env var defaults (catches empty strings)"

key-files:
  created:
    - .planning/phases/09.1-.../artifacts/fix-verification.md
  modified:
    - apps/web/e2e/helpers/auth.ts
  github_secrets_set:
    - TEST_USER_EMAIL
    - TEST_USER_PASSWORD

key-decisions:
  - "Selected fix branch: D-3 (helper code hardening + GitHub secret 설정 둘 다)"
  - "Skipped Plan 03 의 사전 정의 task 3A/3B/3C — 모두 잘못된 가설(body 인코딩/Content-Type/server-side) 이었음"
  - "서버측 코드 0 변경 (production 회귀 위험 0)"

patterns-established:
  - "Env var fallback rule: `||` for env vars (catches both nullish and empty strings — GitHub Actions injects empty strings for unset secrets)"

requirements-completed: []

duration: ~15min (helper edit + secret set + CI 재실행 ~7min 포함)
completed: 2026-04-15
---

# Phase 09.1 Plan 03: D-3 분기 실행 Summary

**Helper 의 `??` → `||` 한 줄 변경 + GitHub repo secret TEST_USER_EMAIL/TEST_USER_PASSWORD 설정으로 Playwright login 401 을 200 으로 수정. 서버 측 코드 변경 0건.**

## Performance

- **Duration:** ~15min
- **Started:** 2026-04-15
- **Completed:** 2026-04-15
- **Tasks:** 2 (D-1 helper edit, D-2 secret set) + verification CI run
- **Files modified:** 1 (apps/web/e2e/helpers/auth.ts)
- **Repo state:** 2 GitHub secrets added

## Why D-3 (not the planner's A/B/C)

Plan 03 의 사전 정의 task 들(3A native fetch / 3B header 교정 / 3C 서버측 변경) 은 모두 "Playwright body 가 정상이지만 서버가 파싱/검증 못함" 가설에 기반했다. 그러나 Plan 02 evidence 가 **반대 사실** 을 입증:

- content-length=26 (Playwright) 은 정확히 `{"email":"","password":""}` 의 길이
- Playwright 가 **빈 credentials 를 정상적으로 직렬화해 전송** 중
- 서버는 정상 동작 — 빈 credentials 를 정확히 reject (401)

Root cause 는 client-side env var fallback bug (planner 가 hypothesized 하지 못한 경로). User 가 D-3 (Decision Tree 외 옵션) 을 명시적으로 승인.

## Tasks executed

### D-1 — helper hardening (single-file edit)

`apps/web/e2e/helpers/auth.ts` 변경:
1. JSDoc 상단에 Phase 09.1 fix context + root cause 설명 추가 (12 lines)
2. line 42-43: `?? 'admin@grapit.test'` → `|| 'admin@grapit.test'`
3. line 43-44: `?? 'TestAdmin2026!'` → `|| 'TestAdmin2026!'`
4. 인라인 주석 추가 — `||` vs `??` 차이 명시

ESM, strict types 유지. typecheck pass (0 issues).

### D-2 — GitHub secrets set

```bash
echo -n "admin@grapit.test" | gh secret set TEST_USER_EMAIL
echo -n "TestAdmin2026!" | gh secret set TEST_USER_PASSWORD
```

`gh secret list` 결과:
- TEST_USER_EMAIL  2026-04-15T06:40:00Z (신규)
- TEST_USER_PASSWORD 2026-04-15T06:40:01Z (신규)

값은 `apps/api/src/database/seed.mjs` 의 admin seed credential 과 동일 — test-only, production credential 미포함.

### Verification — CI run 24440280011

- Trigger: `gh workflow run ci.yml --ref gsd/phase-09.1-...`
- Conclusion: success
- Probe step: `[PROBE] status=200`, body 에 valid user object + Set-Cookie refreshToken
- E2E tests (Toss Payments): 10 passed (9 normal + 1 probe; 3 fixme 그대로 — Plan 04 대상)
- Smoke curl: HTTP 200 (S5 regression 0)
- Unit tests: 172 (api) + 95 (web) passed

## 3 독립 신호 결과 (S1+S3+S5)

| Signal | Status | Evidence |
|--------|--------|----------|
| S1 (server-side req 정상) | ✓ | probe response 의 valid user object = passport-local validate 성공 |
| S3 (response body 정상) | ✓ | accessToken + user object 정상 직렬화 |
| S5 (curl regression 0) | ✓ | smoke test 여전히 HTTP 200 |
| S2 (wire-level headers) | ✓ (간접) | content-length 가 26 → 정상 크기로 변경 |
| S4 (toss-payment 12/12 green) | pending | Plan 04 |

## Symptom-masking 검토

RESEARCH.md §Proof-of-fix 의 우려 ("S4 만 green 이고 S1/S2 가 설명 없이 green 이면 symptom masking 의심") 미해당:
- 모든 신호가 explicit 한 인과 chain 으로 설명됨
- 서버 코드 변경 0 → S5 항상 green (변경 자체가 없음)
- helper 1줄 변경의 효과가 직접 측정 가능 (probe 의 status 401 → 200)

## Threat Model — Mitigations Implemented

| Threat ID | Status | Note |
|-----------|--------|------|
| T-09.1-08 (서버측 변경 회귀) | mitigated | 서버 코드 변경 0 — D-3 분기는 helper + CI secret 만 |
| T-09.1-09 (cookie hardening 우회 leak) | mitigated | helper 는 apps/web/e2e/ 전용, production bundle 미포함 |
| (신규) GitHub repo public + secret 노출 | accepted | 설정한 secret 은 test seed credential 전용. seed.mjs 자체가 공개 코드에 동일 값 하드코딩. production credential 미포함 |

## Commits

- `9cee814` fix(09.1): use || instead of ?? for env fallback in loginAsTestUser

## Next: Plan 04 (Wave 3)

D-3 fix 가 검증되었으므로 Plan 04 가 toss-payment.spec.ts 의 3개 `test.fixme()` 를 `test()` 로 복구하고, CI 에서 12/12 green (10 + 3 복구 + 진단 probe 1 — 단 probe 는 Plan 05 에서 제거) 을 달성한다.

## Cleanup Reminder (Plan 05)

- helper 의 Phase 09.1 JSDoc 블록 제거 (`||` 자체는 유지 — 영구 hardening)
- 진단 미들웨어 + ci.yml 진단 step + probe spec 모두 제거
- GitHub secret 은 유지 (영구 — defense in depth)
