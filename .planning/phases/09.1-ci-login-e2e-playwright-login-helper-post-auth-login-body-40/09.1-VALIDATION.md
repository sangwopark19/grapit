---
phase: 09.1
slug: ci-login-e2e-playwright-login-helper-post-auth-login-body-40
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-15
---

# Phase 09.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> RESEARCH.md `## Validation Architecture` 의 5개 독립 신호(S1-S5)를 기준으로 한다.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Playwright 1.59.x (apps/web/e2e), Vitest (apps/api unit) |
| **Config file** | apps/web/playwright.config.ts, apps/api/vitest.config.ts |
| **Quick run command** | `pnpm --filter @grapit/web exec playwright test e2e/toss-payment.spec.ts -g "login|widget|toast|error" --reporter=list` |
| **Full suite command** | `pnpm --filter @grapit/web exec playwright test` |
| **Estimated runtime** | ~60초 (quick 3 테스트), ~5분 (full) |

---

## Sampling Rate

- **After every task commit:** `pnpm --filter @grapit/api test auth.service` (validateUser 단위 테스트) OR helper 교체 task 시 `playwright test toss-payment.spec.ts -g "happy path"`
- **After every plan wave:** CI 에서 해당 wave 관련 E2E full suite 확인 (로컬 재현 가능 시 로컬 병행)
- **Before `/gsd-verify-work`:** CI 에서 toss-payment.spec.ts 12/12 green + helper 재구성 unit 커버리지
- **Max feedback latency:** 90초 (단일 테스트), 5분 (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 09.1-01-01 | 01 | 0 | TBD | — | 진단 middleware 가 `DEBUG_AUTH_REQ=1` 일 때만 활성, 프로덕션 경로 영향 0 | integration | `pnpm --filter @grapit/api test auth.controller` (middleware 비활성 assert) | ❌ W0 | ⬜ pending |
| 09.1-01-02 | 01 | 0 | TBD | — | rawBody enable 이 기존 JSON parsing 비손상 | integration | `curl -s -X POST localhost:8080/api/v1/auth/login -H 'Content-Type: application/json' -d '{"email":"admin@grapit.test","password":"TestAdmin2026!"}' \| jq .accessToken` (not null) | ✅ | ⬜ pending |
| 09.1-02-01 | 02 | 1 | TBD | — | S1: CI 로그에 `[AUTH_REQ_DEBUG]` 2회 출력(curl + Playwright), `req.body` 구조 diff 관찰 | log inspection | CI job log grep `AUTH_REQ_DEBUG` → 2 hit, JSON diff non-trivial | ❌ W0 | ⬜ pending |
| 09.1-02-02 | 02 | 1 | TBD | — | S2: Content-Type / Content-Length / Transfer-Encoding wire-level diff 기록 | log inspection | CI log 에서 header 3개 값 curl vs Playwright 비교 문서화 | ❌ W0 | ⬜ pending |
| 09.1-03-01 | 03 | 2 | TBD | — | (A/B 분기) helper → Node 22 native fetch 교체 시, Playwright test worker 에서 직접 호출 성공 (200 + accessToken) | e2e | `playwright test toss-payment.spec.ts -g "happy path"` (로컬 재현 가능 시) | ❌ W0 | ⬜ pending |
| 09.1-03-02 | 03 | 2 | TBD | — | 쿠키 replay 가 browser context 로 정상 주입 → `/mypage` 접근 가능 | e2e | 위 spec 에 cookie presence assert 포함 | ❌ W0 | ⬜ pending |
| 09.1-04-01 | 04 | 3 | TBD | — | 3개 `test.fixme(...)` → `test(...)` 복구, 로컬 실행 시 green | e2e | `playwright test toss-payment.spec.ts -g "happy path\|cancel\|decline"` → 3 passed | ✅ | ⬜ pending |
| 09.1-04-02 | 04 | 3 | TBD | — | S4: CI full suite 12/12 green (Toss 9 + login 3) | CI | GitHub Actions workflow → all green | ✅ | ⬜ pending |
| 09.1-05-01 | 05 | 4 | TBD | — | 진단 middleware / rawBody debug flag / `DEBUG_AUTH_REQ` env 완전 제거 | grep | `grep -r "AUTH_REQ_DEBUG\|DEBUG_AUTH_REQ" apps/` → 0 matches | ✅ | ⬜ pending |
| 09.1-05-02 | 05 | 4 | TBD | — | 09-VERIFICATION.md 의 "CI-login-E2E deferred operational checklist" closed | doc inspection | file grep 확인 + commit diff | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] 진단 middleware 파일 (예: `apps/api/src/modules/auth/debug/req-debug.middleware.ts`) — **신규, 환경변수 gate 필수**
- [ ] `NestFactory.create(App, { rawBody: true })` 활성 확인 (main.ts)
- [ ] CI workflow 에 `DEBUG_AUTH_REQ=1` env 주입 step — **E2E job 한정**
- [ ] CI smoke 에 `curl` baseline 호출 유지 (이미 존재 — 진단 비교 대상)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CI 로그의 `req.body` 실측 vs curl baseline 비교 | 원인 확정 (S1) | GitHub Actions 로그 파싱은 자동 assert 가 과한 투자 | Actions UI 에서 E2E job 로그 열람 → `AUTH_REQ_DEBUG` entry 2개 확인 → raw JSON diff 를 VERIFICATION.md 에 첨부 |
| 3개 fixme 복구 후 CI green 확인 | S4 | CI 실행 결과는 push 후 GitHub 에서 확인 | `git push` 후 Actions tab 에서 workflow status green 확인 |

*나머지 모든 동작은 자동 검증됨 (단위/통합/E2E).*

---

## Validation Sign-Off

- [ ] 모든 task 가 `<automated>` verify 또는 Wave 0 의존성 명시
- [ ] Sampling continuity: 연속 3개 task automated verify 없음 방지
- [ ] Wave 0 가 MISSING references (진단 middleware, rawBody, CI env) 커버
- [ ] No watch-mode flags (CI 에서 --watch 금지)
- [ ] Feedback latency < 90초 (quick run), < 5분 (full)
- [ ] `nyquist_compliant: true` frontmatter set
- [ ] 최소 3개 독립 신호 (S1 서버 req + S3 response meta + S4 E2E 복구) 일치 시 원인 확정

**Approval:** pending
