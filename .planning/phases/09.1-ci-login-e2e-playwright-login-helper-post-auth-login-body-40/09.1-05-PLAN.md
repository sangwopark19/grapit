---
phase: 09.1
plan: 05
type: execute
wave: 4
depends_on: ["09.1-04"]
files_modified:
  - apps/api/src/main.ts
  - apps/api/src/modules/auth/debug/req-debug.middleware.ts
  - apps/web/e2e/diagnostic/auth-login-probe.spec.ts
  - .github/workflows/ci.yml
  - .planning/phases/09-tech-debt/09-VERIFICATION.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "진단 미들웨어 파일 apps/api/src/modules/auth/debug/req-debug.middleware.ts 가 완전히 삭제된다"
    - "apps/api/src/main.ts 에서 createAuthReqDebugMiddleware import 와 app.use 호출이 제거되고 NestFactory.create 가 원래 상태(rawBody 옵션 없음)로 복귀한다"
    - "apps/web/e2e/diagnostic/ 디렉터리가 완전히 삭제된다 (probe spec 제거)"
    - ".github/workflows/ci.yml 에서 DEBUG_AUTH_REQ env, Record runtime versions step, Dump AUTH_LOGIN_DEBUG step, E2E diagnostic probe step 이 모두 제거된다"
    - "grep -r 'AUTH_LOGIN_DEBUG\\|DEBUG_AUTH_REQ\\|createAuthReqDebugMiddleware' apps/ .github/ 결과가 0 건"
    - "Phase 09 VERIFICATION.md 의 deferred_to_backlog 항목 'CI-login-E2E' 가 closed 로 표시된다"
    - "CI 에서 toss-payment.spec.ts 가 여전히 12/12 green (S4 신호 유지 — 정리가 회귀를 일으키지 않음)"
    - "curl baseline 여전히 HTTP 200 (S5 유지)"
  artifacts:
    - path: "apps/api/src/main.ts"
      provides: "진단 코드 제거된 main.ts — Phase 09.1 이전 상태와 기능적 동등"
      contains: "NestFactory.create(AppModule)"
    - path: ".github/workflows/ci.yml"
      provides: "진단 step/env 제거된 CI workflow"
      contains: "Start API server"
    - path: ".planning/phases/09-tech-debt/09-VERIFICATION.md"
      provides: "deferred CI-login-E2E 항목이 closed 로 기록된 Phase 09 verification"
      contains: "closed"
  key_links:
    - from: "09-VERIFICATION.md closure entry"
      to: ".planning/phases/09.1-.../artifacts/ci-12-12-green.md"
      via: "inline markdown link to S4 evidence"
      pattern: "ci-12-12-green.md"
---

<objective>
Wave 4: 진단 목적으로 추가된 모든 코드/설정을 완전 제거하고 Phase 09 의 deferred operational checklist (CI-login-E2E) 를 close 한다. 이 plan 이 완료되면 repo 는 Phase 09.1 이전 상태 + helper 수정 + fixme 복구만 남은 최종 정상 상태.

Purpose: RESEARCH.md §Pitfall 3 "진단 middleware 의 상주화" 방지. CONTEXT.md "진단 middleware 는 일회성 — 문제 확정 후 즉시 제거 (프로덕션 로그 오염 방지)" 의 실행. Phase 09 의 open deferred 를 close 하여 v1.1 milestone 의 누적 부채를 0으로.

Output: 진단 코드/설정 완전 제거 + Phase 09 VERIFICATION.md 업데이트 + CI 재검증 12/12 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-CONTEXT.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-04-SUMMARY.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md
@.planning/phases/09-tech-debt/09-VERIFICATION.md
@apps/api/src/main.ts
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 서버측 진단 코드 제거 (main.ts + req-debug.middleware.ts)</name>
  <files>
    apps/api/src/main.ts
    apps/api/src/modules/auth/debug/req-debug.middleware.ts
  </files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/api/src/main.ts (Plan 01 Task 2 에서 추가한 import + rawBody + app.use 3 지점)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-.../09.1-01-SUMMARY.md (변경 지점 정확한 위치 참고)
  </read_first>
  <action>
    정확히 두 가지 변경:

    1. **파일 삭제:** `apps/api/src/modules/auth/debug/req-debug.middleware.ts` 를 `rm` 로 제거. 빈 디렉터리 `apps/api/src/modules/auth/debug/` 도 삭제 (git 은 빈 디렉터리를 추적하지 않으므로 자동 cleanup 되지만, 안전을 위해 `rmdir` 명시).

    2. **main.ts 복구:**
       - `import { createAuthReqDebugMiddleware } from './modules/auth/debug/req-debug.middleware.js';` 라인 **삭제**.
       - `const app = await NestFactory.create(AppModule, { rawBody: true });` 를 `const app = await NestFactory.create(AppModule);` 로 복구 (rawBody 옵션 제거).
         - Phase 09.1 diagnostic 주석도 함께 삭제.
       - `app.use(cookieParser());` 바로 다음 라인의 `app.use(createAuthReqDebugMiddleware());` 와 바로 위 주석 `// Phase 09.1 diagnostic — gated by DEBUG_AUTH_REQ=1, removed in plan 05.` **삭제**.

    **변경 금지:**
    - helmet, cookieParser, enableCors, useGlobalFilters, useGlobalPipes, setGlobalPrefix, listen 의 순서/호출 유지.
    - FRONTEND_URL 검증 블록 / redis IO adapter / bootstrap catch 유지.
  </action>
  <verify>
    <automated>
      # 파일 삭제 확인
      ! test -e apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      ! test -d apps/api/src/modules/auth/debug && \
      # main.ts 복구 확인
      ! grep -q "createAuthReqDebugMiddleware" apps/api/src/main.ts && \
      ! grep -q "rawBody: true" apps/api/src/main.ts && \
      ! grep -q "req-debug.middleware" apps/api/src/main.ts && \
      grep -q "NestFactory.create(AppModule)" apps/api/src/main.ts && \
      pnpm --filter @grapit/api typecheck && \
      pnpm --filter @grapit/api build && \
      pnpm --filter @grapit/api test
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/auth/debug/req-debug.middleware.ts` 및 부모 디렉터리 `debug/` 존재하지 않음.
    - `grep -c "createAuthReqDebugMiddleware" apps/api/src/main.ts` 결과 `0`.
    - `grep -c "rawBody" apps/api/src/main.ts` 결과 `0` (옵션 완전 제거).
    - `grep -c "req-debug" apps/api/src/main.ts` 결과 `0`.
    - `grep "NestFactory.create(AppModule)" apps/api/src/main.ts` 매치 1건 (rawBody 옵션 없는 원래 형태).
    - `pnpm --filter @grapit/api typecheck` exit 0.
    - `pnpm --filter @grapit/api build` exit 0.
    - `pnpm --filter @grapit/api test` 모든 기존 auth/booking/catalog/etc. unit 테스트 0 failure.
  </acceptance_criteria>
  <done>
    서버측 진단 코드 완전 제거 + Phase 09.1 이전 상태로 main.ts 기능 복귀 + 기존 unit 테스트 0 failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E 진단 probe spec 제거 (apps/web/e2e/diagnostic/ 디렉터리 삭제)</name>
  <files>
    apps/web/e2e/diagnostic/auth-login-probe.spec.ts
  </files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/diagnostic/auth-login-probe.spec.ts (Plan 02 Task 1 에서 생성)
  </read_first>
  <action>
    1. `rm apps/web/e2e/diagnostic/auth-login-probe.spec.ts`
    2. `rmdir apps/web/e2e/diagnostic` (빈 디렉터리 제거)

    변경 금지: `apps/web/e2e/helpers/` / `apps/web/e2e/fixtures/` / `apps/web/e2e/toss-payment.spec.ts` / `apps/web/e2e/social-login.spec.ts` 등 모든 영구 spec 은 유지.
  </action>
  <verify>
    <automated>
      ! test -e apps/web/e2e/diagnostic/auth-login-probe.spec.ts && \
      ! test -d apps/web/e2e/diagnostic && \
      test -f apps/web/e2e/toss-payment.spec.ts && \
      test -f apps/web/e2e/helpers/auth.ts && \
      pnpm --filter @grapit/web typecheck && \
      pnpm --filter @grapit/web lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/e2e/diagnostic/` 디렉터리 및 내부 파일 존재하지 않음.
    - `apps/web/e2e/toss-payment.spec.ts` 및 `apps/web/e2e/helpers/auth.ts` 는 존재 (영구 자산 보존).
    - `pnpm --filter @grapit/web typecheck` exit 0.
    - `pnpm --filter @grapit/web lint` exit 0.
    - `grep -rn "auth-login-probe" apps/web/` 결과 0 건 (참조하는 곳 없음).
  </acceptance_criteria>
  <done>
    Diagnostic probe spec 완전 제거 + 다른 E2E spec 영향 없음.
  </done>
</task>

<task type="auto">
  <name>Task 3: CI workflow 정리 (DEBUG_AUTH_REQ env + 진단 step 3개 제거)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.github/workflows/ci.yml (Plan 01 Task 3 + Plan 02 Task 1 에서 추가한 지점)
  </read_first>
  <action>
    정확히 다음 지점만 제거. 원래 Phase 09 상태로 복귀.

    1. **Start API server (background) step** 의 `env:` 블록 (`DEBUG_AUTH_REQ: "1"`) 및 관련 주석 제거. step 의 run 블록은 완전 유지.

    2. **Record runtime versions (Phase 09.1 diagnostic) step** 전체 제거 (Plan 01 Task 3 에서 추가).

    3. **Dump AUTH_LOGIN_DEBUG after curl baseline (Phase 09.1 diagnostic) step** 전체 제거 (Plan 01 Task 3 에서 추가).

    4. **E2E diagnostic probe (Phase 09.1 — observe server-side AUTH_LOGIN_DEBUG) step** 전체 제거 (Plan 02 Task 1 에서 추가).

    변경 금지:
    - `DATABASE_URL / JWT_SECRET / JWT_REFRESH_SECRET / FRONTEND_URL / NODE_ENV` job-level env 유지.
    - Verify Toss test secrets / Install Playwright browsers / Build API / Start API server 의 run 블록 / Smoke test login / E2E tests (Toss Payments) / Dump API log / Stop API server step 모두 유지.
    - `services.postgres`, `steps[0-6]` (checkout, pnpm setup, node setup, install, lint, typecheck, test) 유지.
  </action>
  <verify>
    <automated>
      # 완전 제거 확인
      ! grep -q "DEBUG_AUTH_REQ" .github/workflows/ci.yml && \
      ! grep -q "AUTH_LOGIN_DEBUG" .github/workflows/ci.yml && \
      ! grep -q "Record runtime versions" .github/workflows/ci.yml && \
      ! grep -q "E2E diagnostic probe" .github/workflows/ci.yml && \
      ! grep -q "Dump AUTH_LOGIN_DEBUG after curl baseline" .github/workflows/ci.yml && \
      # 보존 확인
      grep -q "Start API server (background)" .github/workflows/ci.yml && \
      grep -q "Smoke test login" .github/workflows/ci.yml && \
      grep -q "E2E tests (Toss Payments)" .github/workflows/ci.yml && \
      grep -q "Stop API server" .github/workflows/ci.yml && \
      # YAML 유효성
      python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEBUG_AUTH_REQ" .github/workflows/ci.yml` 결과 `0`.
    - `grep -c "AUTH_LOGIN_DEBUG" .github/workflows/ci.yml` 결과 `0`.
    - `grep -c "Record runtime versions" .github/workflows/ci.yml` 결과 `0`.
    - `grep -c "E2E diagnostic probe" .github/workflows/ci.yml` 결과 `0`.
    - `grep -c "Start API server (background)"` 결과 `1` (보존).
    - `grep -c "Smoke test login"` 결과 `1` (보존).
    - `grep -c "E2E tests (Toss Payments)"` 결과 `1` (보존).
    - YAML parse 성공.
    - `deploy.yml` 은 이 plan 에서 절대 수정하지 않음 (`git diff .github/workflows/deploy.yml` 결과 빈 것 확인).
  </acceptance_criteria>
  <done>
    CI workflow 가 Phase 09 말 상태 + Phase 09.1 helper/fixme 수정만 남은 최종 상태로 복귀. 진단 step/env 완전 제거. YAML 유효.
  </done>
</task>

<task type="auto">
  <name>Task 4: 전체 codebase grep 으로 진단 잔재 0 건 검증</name>
  <files></files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Validation Architecture §BH-05 grep check)
  </read_first>
  <action>
    Task 1-3 완료 후 최종 cleanup 검증을 실행. 결과를 `artifacts/cleanup-verification.md` 에 기록.

    ```bash
    # 각 검색이 모두 0 을 반환해야 함
    grep -rn "AUTH_LOGIN_DEBUG" apps/ .github/ .env.example 2>/dev/null | wc -l   # expect 0
    grep -rn "DEBUG_AUTH_REQ" apps/ .github/ .env.example 2>/dev/null | wc -l     # expect 0
    grep -rn "createAuthReqDebugMiddleware" apps/ 2>/dev/null | wc -l             # expect 0
    grep -rn "req-debug" apps/ 2>/dev/null | wc -l                                # expect 0
    find apps/api/src/modules/auth/debug -type f 2>/dev/null | wc -l              # expect 0
    find apps/web/e2e/diagnostic -type f 2>/dev/null | wc -l                      # expect 0
    ```

    결과를 `.planning/phases/09.1-.../artifacts/cleanup-verification.md` 에 표로 기록:

    ```markdown
    # Phase 09.1 — Cleanup verification

    **Run date:** <ISO>
    **Commit SHA:** <post-cleanup SHA>

    | Check | Expected | Actual | Status |
    |-------|----------|--------|--------|
    | grep AUTH_LOGIN_DEBUG in apps/ .github/ .env.example | 0 | 0 | ✓ |
    | grep DEBUG_AUTH_REQ | 0 | 0 | ✓ |
    | grep createAuthReqDebugMiddleware | 0 | 0 | ✓ |
    | grep req-debug | 0 | 0 | ✓ |
    | find apps/api/src/modules/auth/debug | 0 | 0 | ✓ |
    | find apps/web/e2e/diagnostic | 0 | 0 | ✓ |

    **Conclusion:** 진단 코드 완전 제거 확인.
    ```
  </action>
  <verify>
    <automated>
      # 모든 검증이 0 을 반환
      [ $(grep -rn "AUTH_LOGIN_DEBUG" apps/ .github/ .env.example 2>/dev/null | wc -l) -eq 0 ] && \
      [ $(grep -rn "DEBUG_AUTH_REQ" apps/ .github/ .env.example 2>/dev/null | wc -l) -eq 0 ] && \
      [ $(grep -rn "createAuthReqDebugMiddleware" apps/ 2>/dev/null | wc -l) -eq 0 ] && \
      [ $(grep -rn "req-debug" apps/ 2>/dev/null | wc -l) -eq 0 ] && \
      [ ! -d apps/api/src/modules/auth/debug ] && \
      [ ! -d apps/web/e2e/diagnostic ] && \
      test -f .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/cleanup-verification.md && \
      grep -q "진단 코드 완전 제거 확인" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/cleanup-verification.md
    </automated>
  </verify>
  <acceptance_criteria>
    - 6개 grep/find check 결과 모두 0 (BH-05 요건 만족).
    - `cleanup-verification.md` 파일 존재 + 표 안의 모든 Status 가 `✓`.
    - `.env.example` 에 `DEBUG_AUTH_REQ` 언급 0건 (`grep DEBUG_AUTH_REQ .env.example` 결과 0 또는 파일 미존재).
  </acceptance_criteria>
  <done>
    전체 codebase 에 Phase 09.1 진단 관련 문자열/디렉터리 0 건 — 프로덕션 로그 오염 리스크 완전 제거.
  </done>
</task>

<task type="auto">
  <name>Task 5: CI 재실행 → 12/12 여전히 green (정리 후 회귀 0 확인)</name>
  <files>
    .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/post-cleanup-ci.md
  </files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-.../artifacts/ci-12-12-green.md (Plan 04 Task 2 — cleanup 전 baseline)
  </read_first>
  <action>
    Task 1-4 커밋을 push → CI 트리거.

    1. Actions UI 에서 CI workflow 가 success 로 완료되는지 확인.
    2. "E2E tests (Toss Payments)" step 이 여전히 12/12 green 인지 확인.
    3. "Smoke test login" step 이 여전히 HTTP 200 인지 확인.
    4. `artifacts/post-cleanup-ci.md` 생성:

       ```markdown
       # Phase 09.1 — Post-cleanup CI verification

       **CI run URL:** <URL>
       **Commit SHA (post-cleanup):** <SHA>
       **Compare to:** artifacts/ci-12-12-green.md (pre-cleanup baseline)

       | Step | Pre-cleanup | Post-cleanup |
       |------|-------------|--------------|
       | lint | ✓ | ✓ |
       | typecheck | ✓ | ✓ |
       | test (unit) | ✓ | ✓ |
       | DB migrations | ✓ | ✓ |
       | Seed test data | ✓ | ✓ |
       | Verify Toss test secrets | ✓ | ✓ |
       | Build API | ✓ | ✓ |
       | Start API server | ✓ | ✓ |
       | Smoke test login (curl) | HTTP 200 | HTTP 200 |
       | E2E tests (Toss Payments) | 12 passed | 12 passed |
       | (removed) Record runtime versions | existed | removed ✓ |
       | (removed) Dump AUTH_LOGIN_DEBUG | existed | removed ✓ |
       | (removed) E2E diagnostic probe | existed | removed ✓ |

       **Conclusion:** Cleanup introduced 0 regressions. Phase 09.1 goal fully achieved with no diagnostic code remaining.
       ```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/post-cleanup-ci.md && \
      grep -q "E2E tests (Toss Payments) | 12 passed | 12 passed" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/post-cleanup-ci.md && \
      grep -q "Smoke test login (curl) | HTTP 200 | HTTP 200" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/post-cleanup-ci.md && \
      grep -q "0 regressions" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/post-cleanup-ci.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `post-cleanup-ci.md` 파일 존재.
    - 표 안의 모든 post-cleanup 열이 ✓ 또는 "HTTP 200" 또는 "12 passed" 또는 "removed ✓".
    - CI run URL 필드가 실제 URL (빈 플레이스홀더 금지).
    - GitHub Actions 해당 run 의 workflow status = `success`.
    - "E2E tests (Toss Payments)" step 의 Playwright 결과 `12 passed`.
    - "Smoke test login" step `HTTP 200`.
    - 제거된 3 step 이 CI 로그에 나타나지 않음 (step 개수가 cleanup 전보다 3개 적음).
  </acceptance_criteria>
  <done>
    정리 후에도 CI 12/12 green + curl 200 유지 — cleanup 이 regression 0 으로 적용됨. S1-S5 5 신호 중 S4 + S5 여전히 green (S1/S2/S3 는 진단 인프라 제거로 더 이상 관찰 불가하지만, Plan 04 에서 이미 증거 확보됨).
  </done>
</task>

<task type="auto">
  <name>Task 6: Phase 09 VERIFICATION.md 의 deferred_to_backlog 항목 close</name>
  <files>.planning/phases/09-tech-debt/09-VERIFICATION.md</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.planning/phases/09-tech-debt/09-VERIFICATION.md (L17-18 deferred_to_backlog 항목)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-.../artifacts/ci-12-12-green.md
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-.../artifacts/post-cleanup-ci.md
  </read_first>
  <action>
    `.planning/phases/09-tech-debt/09-VERIFICATION.md` 의 frontmatter `re_verification.deferred_to_backlog:` 항목 (현재 L17-18) 을 다음과 같이 업데이트한다.

    기존 (L17-18):
    ```yaml
      deferred_to_backlog:
        - "CI-login-E2E (`.planning/phases/999-backlog/CI-login-E2E.md`): Playwright 의 page.request/request.newContext 를 통한 `POST /api/v1/auth/login` 이 Passport generic 401 을 반환 (curl 로 동일 URL/body/headers 는 200). `toss-payment.spec.ts` 의 login-dependent 3 테스트는 test.fixme 처리 (commit 5d65cb9). 원인 후보: Playwright 내부 네트워킹 layer 의 body encoding 과 NestJS body-parser 호환성. Toss 자체 검증 (9 E2E) 은 영향 없음."
    ```

    교체 후:
    ```yaml
      deferred_to_backlog: []
      deferred_closed_in_follow_up:
        - "CI-login-E2E (originally tracked at `.planning/phases/999-backlog/CI-login-E2E.md`) — **CLOSED in Phase 09.1** (`.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/`). 진단 미들웨어로 서버 측 req 관찰 → 원인 범주 <A|B|C> 확정 → helper 수정 → 3 test.fixme 복구 → CI 12/12 green (see `09.1-04-SUMMARY.md` + `artifacts/ci-12-12-green.md`). 진단 코드는 같은 phase 에서 완전 제거 (see `artifacts/cleanup-verification.md`)."
    ```

    `<A|B|C>` 는 09.1-03-SUMMARY.md 의 실제 확정 범주로 치환.

    추가로 Phase 09 VERIFICATION.md body 의 "Truth 4" 행 (L43) evidence 설명에서 `test.fixme 처리 (commit 5d65cb9)` 부분 바로 뒤에 "**Phase 09.1 에서 closure** (09.1-04-SUMMARY)" 문구를 추가 (inline 한 줄 보강 — 기존 문장 수정 최소화).

    변경 금지:
    - Truth 1/2/3/5 evidence row 는 touch 하지 않음.
    - frontmatter 의 status / score / re_verification.previous_status 등 다른 필드 수정 금지.
  </action>
  <verify>
    <automated>
      # deferred_to_backlog 가 빈 배열로 바뀌고 closed 항목 추가
      grep -q "deferred_to_backlog: \[\]" .planning/phases/09-tech-debt/09-VERIFICATION.md && \
      grep -q "deferred_closed_in_follow_up:" .planning/phases/09-tech-debt/09-VERIFICATION.md && \
      grep -q "CLOSED in Phase 09.1" .planning/phases/09-tech-debt/09-VERIFICATION.md && \
      grep -q "09.1-04-SUMMARY" .planning/phases/09-tech-debt/09-VERIFICATION.md && \
      grep -q "artifacts/ci-12-12-green.md" .planning/phases/09-tech-debt/09-VERIFICATION.md && \
      # Truth 4 inline 보강 확인
      grep -q "Phase 09.1 에서 closure" .planning/phases/09-tech-debt/09-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `deferred_to_backlog: []` (빈 배열) 문자열 매치 1건.
    - `deferred_closed_in_follow_up:` 키 매치 1건.
    - `CLOSED in Phase 09.1` 매치 1건.
    - `09.1-04-SUMMARY` 참조 1건.
    - `artifacts/ci-12-12-green.md` 참조 1건.
    - `Phase 09.1 에서 closure` 인라인 문구 Truth 4 evidence 에 포함.
    - Truth 1/2/3/5 evidence 텍스트 수정 0 (diff 로 확인 가능).
    - frontmatter 의 `status: pass_with_deferred` 는 `status: pass` 로 업데이트 (더 이상 deferred 가 없으므로) — 이 변경만 추가 허용.
    - YAML frontmatter 유효성 확인 (`python3 -c "import frontmatter; frontmatter.load(...)"` 이 가능하면, 아니면 파일 상단 `---` fence 구조 수동 확인).
  </acceptance_criteria>
  <done>
    Phase 09 의 open deferred 가 Phase 09.1 closure 참조와 함께 빈 배열로 정리됨. v1.1 milestone 의 누적 부채 해소.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cleanup commit → production deploy | deploy.yml 이 ci.yml 을 참조하지 않으므로 production 에 영향 없음. 그러나 main.ts 는 production 에 실행되므로 복구가 정확해야 함 |
| Task 6 의 09-VERIFICATION.md 수정 → 과거 기록 보존 | status 변경이 감사 추적 훼손 가능성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-13 | Tampering | Task 1 의 main.ts 복구 시 rawBody 옵션만 제거하는 게 아니라 다른 NestFactory 옵션까지 삭제 | mitigate | acceptance 에서 `grep "NestFactory.create(AppModule)"` 정확한 문자열 매치 강제. build + test 0 failure 검증. |
| T-09.1-14 | Repudiation | Task 6 의 VERIFICATION.md 수정이 과거 "deferred" 사실 자체를 지워 audit trail 훼손 | mitigate | `deferred_to_backlog: []` 로 비우되 `deferred_closed_in_follow_up:` 신규 키로 closure 기록 유지. status 변경은 `pass_with_deferred → pass` 단일 transition 만 허용 — 과거 스냅샷은 git history 에 보존. |
| T-09.1-15 | Information Disclosure | artifacts/*.txt 디렉터리(test credentials 포함) 가 Plan 05 에서 함께 삭제되어야 하는지 여부 | accept | 사용자 재량. planner 는 기본적으로 artifacts/ 를 보존 (감사 추적용). test credential 은 seed 전용이므로 유출 리스크 acceptable. 필요시 user 가 별도 commit 에서 삭제. |
</threat_model>

<verification>
- Task 1-6 모든 automated verify exit 0.
- `pnpm --filter @grapit/api test` 전체 0 failure.
- `pnpm --filter @grapit/api typecheck && pnpm --filter @grapit/web typecheck` 모두 exit 0.
- `pnpm lint` 전체 0 errors.
- `git diff main -- apps/api/src/main.ts` 는 Phase 09.1 전체 사이클 후 변화 0 건이어야 함 (진단 인프라 완전 제거). 실제로는 import 라인/formatting 변화가 있을 수 있으나, 의미적 변경은 0.
- CI 의 post-cleanup run: workflow success + 12 passed + curl 200.
- Phase 09 VERIFICATION.md 의 deferred_to_backlog 배열이 비어있음.
</verification>

<success_criteria>
- 진단 관련 파일/디렉터리/env/step 0건 잔재 (grep + find 로 증명).
- main.ts 가 Phase 09.1 이전과 기능적 동등 (build + test 통과).
- CI 에서 여전히 12/12 green + curl 200 (cleanup regression 0).
- Phase 09 VERIFICATION.md 의 CI-login-E2E deferred 항목이 closed 로 기록됨.
- Phase 09.1 의 6 must_have truths 모두 ✓.
</success_criteria>

<output>
완료 후 `.planning/phases/09.1-.../09.1-05-SUMMARY.md` 생성. SUMMARY 에 cleanup 증거 (cleanup-verification.md + post-cleanup-ci.md) 링크와 Phase 09 VERIFICATION 업데이트 증거를 기록.
</output>
