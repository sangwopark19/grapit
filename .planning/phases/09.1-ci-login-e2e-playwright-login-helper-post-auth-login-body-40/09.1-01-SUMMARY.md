---
phase: 09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40
plan: 01
subsystem: testing
tags: [diagnostics, ci, nestjs, playwright, body-parser, rawBody]

requires:
  - phase: 09-tech-debt
    provides: ci.yml E2E pipeline (postgres service, drizzle migrate/seed, Toss secrets gate, Smoke test login step)
provides:
  - DEBUG_AUTH_REQ-gated AUTH_LOGIN_DEBUG diagnostic middleware exposing req.body / req.headers / req.rawBody
  - NestFactory rawBody:true wiring for raw payload observation
  - CI workflow steps that record runtime versions and dump AUTH_LOGIN_DEBUG entries after curl baseline
affects: [phase 09.1-02, phase 09.1-03, phase 09.1-05]

tech-stack:
  added: []
  patterns:
    - "Environment-gated diagnostic middleware (factory pattern, DEBUG_AUTH_REQ=1 default OFF)"
    - "Rawbody Buffer observation via NestFactory.create(AppModule, { rawBody: true })"
    - "Per-step env override in GitHub Actions workflow (DEBUG_AUTH_REQ scoped to API server step only)"

key-files:
  created:
    - apps/api/src/modules/auth/debug/req-debug.middleware.ts
  modified:
    - apps/api/src/main.ts
    - .github/workflows/ci.yml

key-decisions:
  - "Diagnostic middleware factory exports createAuthReqDebugMiddleware(); first line gate ensures zero overhead when DEBUG_AUTH_REQ != '1'"
  - "Password 원문 미로그 — bodyPasswordLen(길이) 만 기록해 T-09.1-01 mitigation 준수"
  - "ci.yml 의 DEBUG_AUTH_REQ 는 'Start API server (background)' step 의 env 블록에만 스코프 — deploy.yml 으로 전파 차단 (T-09.1-04)"

patterns-established:
  - "Diagnostic-first principle: 증거 없이 수정하지 않는다 — Plan 02 가 이 인프라로 evidence 를 수집"
  - "Env-gated middleware: 첫 줄 if (env !== '1') return next(); — production 회귀 위험 0"

requirements-completed: []

duration: ~25min
completed: 2026-04-15
---

# Phase 09.1 Plan 01: 진단 인프라 구축 Summary

**CI 환경에서만 발생하는 Playwright 401 의 원인을 규명하기 위한 server-side 관찰 인프라(DEBUG_AUTH_REQ 게이트 미들웨어 + rawBody 노출 + CI workflow env/dump steps)를 완성했다.**

## Performance

- **Duration:** ~25min
- **Started:** 2026-04-15
- **Completed:** 2026-04-15
- **Tasks:** 3 (모두 autonomous)
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

### Task 1 — Diagnostic middleware factory
- 신규 파일 `apps/api/src/modules/auth/debug/req-debug.middleware.ts` 작성 (41 lines)
- ESM, strict types, no `any` — `bodyRecord = body as Record<string, unknown>` 로 타입 좁힘
- DEBUG_AUTH_REQ !== '1' 이면 즉시 next() — 기본 OFF 보장
- POST + `/auth/login` URL 게이트 통과 시 단일 라인 JSON (tag, method, url, headers, bodyType, bodyKeys, bodyEmail, bodyPasswordLen, rawBodyLen, rawBodyUtf8) 출력
- password 값은 절대 로그하지 않고 길이만 기록 (T-09.1-01)

### Task 2 — main.ts 패치
- `NestFactory.create(AppModule)` → `NestFactory.create(AppModule, { rawBody: true })` 변경
- `cookieParser()` 직후 `app.use(createAuthReqDebugMiddleware())` 등록
- ESM `.js` 확장자 import 규약 준수
- typecheck + build 모두 통과 (`apps/api/dist/main.js` 생성 확인)

### Task 3 — ci.yml 패치
- "Record runtime versions (Phase 09.1 diagnostic)" step 신규 추가 (Start API server 앞)
- "Start API server (background)" step 에 `env: DEBUG_AUTH_REQ: "1"` 추가
- "Dump AUTH_LOGIN_DEBUG after curl baseline (Phase 09.1 diagnostic)" step 신규 추가 (Smoke test login 뒤)
- YAML parse 통과, 기존 step 내용은 모두 보존

## Verification

- `pnpm --filter @grapit/api typecheck` → 0 issues
- `pnpm --filter @grapit/api build` → SWC 116 files compiled, dist/main.js 생성
- `pnpm --filter @grapit/api test` → 172/172 passed (회귀 0)
- `.env.example` 에 DEBUG_AUTH_REQ 0건 (Pitfall 3 — 상주화 방지)
- ci.yml YAML safe_load 성공

## Threat Model — Mitigations Implemented

| Threat ID | Status | Note |
|-----------|--------|------|
| T-09.1-01 (password leak in middleware logs) | mitigated | bodyPasswordLen 만 로그, password 값 직접 접근 0건 (`grep -E "\.password[^?]"` 결과 0건) |
| T-09.1-03 (rawBody:true 회귀) | mitigated | typecheck + build + 172 tests 모두 통과 — body-parser JSON parsing 영향 없음 |
| T-09.1-04 (production 전파) | mitigated | env gate 첫 줄 + ci.yml 만 수정, deploy.yml 미터치 |
| T-09.1-02 (api.log test password 노출) | accepted | test seed credential 전용, repo private |

## Commits

- `60e31e2` feat(09.1): add gated AUTH_LOGIN_DEBUG diagnostic middleware
- `a6af204` feat(09.1): enable rawBody and register AUTH_LOGIN_DEBUG middleware
- `01aefc7` ci(09.1): wire DEBUG_AUTH_REQ env and AUTH_LOGIN_DEBUG dump steps

## Next: Plan 02 (Wave 1)

진단 인프라는 준비 완료. Plan 02 는:
1. probe spec 작성 (helper 와 동일한 호출 형태로 POST /auth/login 1회 호출)
2. CI 푸시 → AUTH_LOGIN_DEBUG 2건(curl + Playwright) 추출 → artifacts/diff.md 작성
3. **Checkpoint** — 원인 범주 A/B/C 중 하나 user 승인

## Cleanup Reminder

Plan 05 (Wave 4) 에서 다음 항목 완전 제거:
- `apps/api/src/modules/auth/debug/req-debug.middleware.ts` 파일 삭제
- main.ts 의 `rawBody: true` 옵션, 미들웨어 import + 호출 원복
- ci.yml 의 3개 step (Record runtime versions, DEBUG_AUTH_REQ env, Dump AUTH_LOGIN_DEBUG) 제거
