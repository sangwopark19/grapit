---
phase: 09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40
plan: 05
subsystem: testing
tags: [cleanup, deferred-closure, audit-trail, phase-09-close]

requires:
  - phase: 09.1-04
    provides: 12/12 green CI evidence + S1-S5 5 signals
provides:
  - 진단 인프라 0건 잔재 (server middleware + main.ts rawBody + ci.yml steps + probe spec 모두 제거)
  - artifacts/cleanup-verification.md — 6 grep/find check 모두 0
  - artifacts/post-cleanup-ci.md — pre/post cleanup CI 비교, regression 0 확인
  - .planning/phases/09-tech-debt/09-VERIFICATION.md updated — deferred CI-login-E2E CLOSED, status pass
affects: [phase 10+, milestone v1.1 closure]

tech-stack:
  added: []
  patterns:
    - "Diagnostic instrumentation lifecycle: 일회성 도입 → 증거 수집 → 원인 확정 → 같은 phase 에서 완전 제거 (RESEARCH.md §Pitfall 3 ‘진단 middleware 의 상주화 방지’ 충족)"
    - "Deferred closure pattern: deferred_to_backlog 비우고 deferred_closed_in_follow_up 으로 audit trail 보존"

key-files:
  created:
    - .planning/phases/09.1-.../artifacts/cleanup-verification.md
    - .planning/phases/09.1-.../artifacts/post-cleanup-ci.md
  modified:
    - apps/api/src/main.ts (rawBody:true 옵션 + middleware import/use 제거)
    - .github/workflows/ci.yml (3 진단 step + DEBUG_AUTH_REQ env 제거)
    - .planning/phases/09-tech-debt/09-VERIFICATION.md (deferred 항목 closure + status pass)
  deleted:
    - apps/api/src/modules/auth/debug/req-debug.middleware.ts
    - apps/api/src/modules/auth/debug/ (dir)
    - apps/web/e2e/diagnostic/auth-login-probe.spec.ts
    - apps/web/e2e/diagnostic/ (dir)

key-decisions:
  - "Phase 09 status pass_with_deferred → pass — deferred 항목 0건"
  - "deferred_to_backlog 를 단순 삭제하지 않고 deferred_closed_in_follow_up 키로 closure 보존 (T-09.1-14 audit trail mitigation)"
  - "artifacts/ 디렉터리 보존 (감사 추적용) — test credentials 노출은 seed-only 라 acceptable"

patterns-established:
  - "Diagnostic-then-cleanup cycle: 같은 phase 에서 add 와 remove 를 모두 수행 — 다음 phase 로 부채를 넘기지 않음"
  - "Closure trail: 과거 deferred 사실 자체는 git history + deferred_closed_in_follow_up 키로 영구 보존"

requirements-completed: []

duration: ~10min (cleanup edits + grep verify + CI 재실행 ~5min)
completed: 2026-04-15
---

# Phase 09.1 Plan 05: 진단 코드 제거 + Phase 09 closure Summary

**Phase 09.1 의 진단 인프라(middleware + rawBody + CI steps + probe spec)를 완전 제거하고 cleanup 후에도 12/12 green 유지를 검증. Phase 09 의 deferred CI-login-E2E 항목을 root cause 정정과 함께 closed 로 기록 — v1.1 milestone 의 누적 부채 0.**

## Performance

- **Duration:** ~10min (Task 1-3 edits ~3min + grep verify ~1min + CI 재실행 ~6min)
- **Started:** 2026-04-15
- **Completed:** 2026-04-15
- **Tasks:** 6 (서버측 제거, probe 제거, ci.yml 제거, grep verify, post-cleanup CI, Phase 09 closure)
- **Files modified:** 3 source + 1 docs / 2 source files deleted (+ 2 dirs)

## Tasks executed

### Task 1 — 서버측 진단 코드 제거

- `rm apps/api/src/modules/auth/debug/req-debug.middleware.ts` + `rmdir debug/`
- main.ts: import 1줄 제거, `NestFactory.create(AppModule, { rawBody: true })` → `NestFactory.create(AppModule)`, `app.use(createAuthReqDebugMiddleware())` 호출 + 주석 제거
- 검증: typecheck pass, build success (dist/main.js, 115 files, 1 file 줄어듦), api unit test 172/172 pass

### Task 2 — Probe spec 제거

- `rm apps/web/e2e/diagnostic/auth-login-probe.spec.ts` + `rmdir diagnostic/`
- 검증: web typecheck pass, helpers/auth.ts 와 toss-payment.spec.ts 모두 보존

### Task 3 — ci.yml 진단 step 제거

- `Record runtime versions (Phase 09.1 diagnostic)` step 제거 (Plan 01 추가)
- `Start API server` step 의 `env: DEBUG_AUTH_REQ: "1"` 블록 제거
- `Dump AUTH_LOGIN_DEBUG after curl baseline` step 제거 (Plan 01 추가)
- `E2E diagnostic probe` step 제거 (Plan 02 추가)
- 검증: YAML safe_load pass, 보존 step 4개 모두 1건 매치, deploy.yml 무변경

### Task 4 — Cleanup grep verification

`artifacts/cleanup-verification.md` — 6개 check 모두 0:
- AUTH_LOGIN_DEBUG in apps/ : 0
- DEBUG_AUTH_REQ in apps/ : 0
- AUTH_LOGIN_DEBUG\|DEBUG_AUTH_REQ in .github/ : 0
- createAuthReqDebugMiddleware in apps/ : 0
- req-debug in apps/ : 0
- dirs apps/api/src/modules/auth/debug, apps/web/e2e/diagnostic : not exist

### Task 5 — Post-cleanup CI 재실행 (24440970303)

- conclusion: success
- E2E (Toss Payments): **12 passed** (probe 제거 후 13 → 12 자연 감소)
- Smoke curl: HTTP 200
- Unit tests: 172 (api) + 95 (web) passed
- Job step 개수: 20 (이전 23 → 3 진단 step 제거 정확히 일치)
- `artifacts/post-cleanup-ci.md` 작성 — pre/post 비교표

### Task 6 — Phase 09 VERIFICATION.md closure

- frontmatter `status: pass_with_deferred` → `pass`
- `deferred_to_backlog: [...]` → `deferred_to_backlog: []`
- 신규 키 `deferred_closed_in_follow_up:` 추가 — Phase 09.1 closure 참조 + **root cause 정정**
  - 기존 가설: "Playwright/body-parser 호환성"
  - 실제: "GitHub TEST_USER_EMAIL/PASSWORD secret 미설정 + helper 의 `??` 가 빈 문자열 fallback 실패"
- Truth 4 row: `? HUMAN` → `✓ VERIFIED` + "Phase 09.1 에서 closure (09.1-04-SUMMARY) — CI run 24440970303 13 passed (cleanup 전), 12 passed (cleanup 후)"

## 6 must_have truths 최종 검증

| # | Truth | Status |
|---|-------|--------|
| 1 | 진단 미들웨어 파일 완전 삭제 | ✓ |
| 2 | main.ts createAuthReqDebugMiddleware import + app.use 제거 + NestFactory.create 원래 상태 | ✓ |
| 3 | apps/web/e2e/diagnostic/ 디렉터리 완전 삭제 | ✓ |
| 4 | ci.yml 의 DEBUG_AUTH_REQ env + Record runtime + Dump AUTH_LOGIN_DEBUG + E2E diagnostic probe step 모두 제거 | ✓ |
| 5 | grep 결과 0건 (AUTH_LOGIN_DEBUG / DEBUG_AUTH_REQ / createAuthReqDebugMiddleware) | ✓ |
| 6 | Phase 09 VERIFICATION.md deferred CI-login-E2E closed | ✓ |
| 7 | CI 12/12 green 유지 (S4) | ✓ |
| 8 | curl baseline HTTP 200 (S5) | ✓ |

## Threat Model — Mitigations Implemented

| Threat ID | Status | Note |
|-----------|--------|------|
| T-09.1-13 (main.ts 다른 옵션 삭제 위험) | mitigated | grep 으로 NestFactory.create(AppModule) 정확 매치 + build pass + 172 tests pass |
| T-09.1-14 (audit trail 훼손) | mitigated | deferred_closed_in_follow_up 키로 closure 사실 보존, status 변경 single transition (pass_with_deferred → pass), 과거 스냅샷은 git history 에 |
| T-09.1-15 (artifacts 보존 결정) | accepted | artifacts/ 디렉터리 보존 — 감사 추적용. test credential 은 seed 전용이라 risk acceptable |
| (Pitfall 3) 진단 middleware 의 상주화 | prevented | 같은 phase 에서 add(Plan 01) → use(Plan 02) → remove(Plan 05) 완수, .env.example 오염 0 |

## Commits

- `9035a42` chore(09.1): remove server-side AUTH_LOGIN_DEBUG diagnostic infrastructure
- `0c93557` chore(09.1): remove diagnostic probe spec
- `e6fb188` chore(09.1): remove diagnostic CI steps and DEBUG_AUTH_REQ env
- `c57421d` docs(09.1): close Phase 09 deferred CI-login-E2E + record cleanup verification

## Phase 09.1 최종 상태

- **Goal:** CI Playwright login 401 root cause 규명 + 3 fixme 복구 + 12/12 green ✓
- **Side effects:** 0 server-side change, helper hardening (`||`), product fix (StrictMode latch), GitHub secrets 설정
- **Permanent retentions:** helper `||`, GitHub secrets, fixme 복구, useRef latch
- **Removed:** 진단 middleware/spec/CI steps, rawBody:true 모두 제거
- **Phase 09 status:** pass (no deferred)
- **Milestone v1.1:** Phase 09 의 마지막 open 항목 close — 누적 부채 0
