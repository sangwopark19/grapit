---
phase: 09.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/main.ts
  - apps/api/src/modules/auth/debug/req-debug.middleware.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "CI 에서 DEBUG_AUTH_REQ=1 일 때 POST /auth/login 요청이 들어오면 AUTH_LOGIN_DEBUG JSON 로그가 api.log 에 찍힌다"
    - "CI 에서 DEBUG_AUTH_REQ 미설정 또는 != '1' 일 때는 진단 로그가 전혀 나오지 않는다 (프로덕션 로그 오염 0)"
    - "진단 로그에 password 원문은 포함되지 않고 bodyPasswordLen(길이)만 기록된다"
    - "rawBody Buffer 가 req.rawBody 로 노출되어 bytes 길이와 UTF-8 문자열을 관찰할 수 있다"
    - "기존 curl smoke test login 이 수정 후에도 여전히 HTTP 200 + accessToken 을 반환 (regression 0)"
  artifacts:
    - path: "apps/api/src/modules/auth/debug/req-debug.middleware.ts"
      provides: "환경변수 gate 된 POST /auth/login 진단 미들웨어 factory"
      exports: ["createAuthReqDebugMiddleware"]
    - path: "apps/api/src/main.ts"
      provides: "NestFactory.create rawBody: true + app.use(createAuthReqDebugMiddleware()) 등록"
      contains: "rawBody: true"
    - path: ".github/workflows/ci.yml"
      provides: "Start API server step 에 DEBUG_AUTH_REQ='1' env + node -v 출력 + AUTH_LOGIN_DEBUG 로그 덤프 step"
      contains: "DEBUG_AUTH_REQ"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/modules/auth/debug/req-debug.middleware.ts"
      via: "import { createAuthReqDebugMiddleware } + app.use(...)"
      pattern: "createAuthReqDebugMiddleware\\(\\)"
    - from: ".github/workflows/ci.yml"
      to: "apps/api/src/main.ts"
      via: "Start API server step 의 env.DEBUG_AUTH_REQ='1' 주입"
      pattern: "DEBUG_AUTH_REQ"
---

<objective>
Wave 0: 진단 인프라(diagnostic infrastructure) 를 구축한다. CI 에서 서버가 실제로 받은 req.body / req.headers / req.rawBody 를 관찰할 수 있도록 환경변수 게이트(DEBUG_AUTH_REQ=1) 진단 미들웨어, NestFactory rawBody 옵션, CI workflow env 주입을 완성한다.

Purpose: "증거 없이 수정하지 않는다" — CONTEXT.md 의 "진단 우선 원칙" 을 코드로 실현. Wave 1 에서 curl 과 Playwright 요청을 동일 파이프로 비교 관찰하기 위한 토대.

Output: req-debug.middleware.ts (신규), main.ts 패치, ci.yml 패치. 모두 같은 PR 에서 Wave 4 에 제거 예정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-CONTEXT.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-VALIDATION.md
@apps/api/src/main.ts
@.github/workflows/ci.yml

<interfaces>
<!-- 이 plan 이 추가하는 인터페이스 — Wave 1 executor 가 그대로 사용 -->

From apps/api/src/modules/auth/debug/req-debug.middleware.ts (신규):
```typescript
import type { NextFunction, Request, Response } from 'express';

/**
 * Factory: POST /auth/login 만 관찰하는 환경변수 게이트 진단 미들웨어.
 * DEBUG_AUTH_REQ !== '1' 이면 미들웨어를 등록하더라도 즉시 next() 로 흘려보낸다.
 * Wave 4 (plan 05) 에서 파일 전체 삭제 예정.
 */
export function createAuthReqDebugMiddleware(): (
  req: Request & { rawBody?: Buffer },
  res: Response,
  next: NextFunction,
) => void;
```

AUTH_LOGIN_DEBUG 로그 JSON 스키마 (단일 라인, CI grep/jq 용):
```json
{
  "tag": "AUTH_LOGIN_DEBUG",
  "method": "POST",
  "url": "/api/v1/auth/login",
  "headers": {...},
  "bodyType": "object",
  "bodyKeys": ["email", "password"] | null,
  "bodyEmail": "admin@grapit.test" | null,
  "bodyPasswordLen": 17 | null,
  "rawBodyLen": 62 | null,
  "rawBodyUtf8": "{\"email\":\"...\",\"password\":\"...\"}" | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 진단 미들웨어 factory 파일 생성 (DEBUG_AUTH_REQ 환경변수 gate)</name>
  <files>apps/api/src/modules/auth/debug/req-debug.middleware.ts</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Code Examples §1 진단 middleware, §Pitfall 3 상주화 방지, §Security Domain V7)
    - /Users/sangwopark19/icons/grapit/apps/api/src/main.ts (import/구조 확인 — ESM `.js` 확장자 import 규약 준수)
    - /Users/sangwopark19/icons/grapit/CLAUDE.md (strict typing, ES modules, `any` 금지)
  </read_first>
  <action>
    신규 파일 `apps/api/src/modules/auth/debug/req-debug.middleware.ts` 를 ESM 로 작성한다. 파일은 정확히 다음 책임만 가진다:

    1. `createAuthReqDebugMiddleware()` 를 export — 인자 없는 factory 가 Express `(req, res, next) => void` 미들웨어를 반환.
    2. 미들웨어 본체의 첫 줄은 반드시 `if (process.env['DEBUG_AUTH_REQ'] !== '1') return next();` — 기본 OFF.
    3. 그 다음에만 `req.method === 'POST' && req.url.includes('/auth/login')` 게이트.
    4. 통과 시 정확히 아래 키만 포함한 **단일 라인 JSON** 을 `console.log(JSON.stringify({...}))` 로 stdout 출력:
       - `tag: 'AUTH_LOGIN_DEBUG'`
       - `method: req.method`
       - `url: req.url`
       - `headers: req.headers` (원본 그대로)
       - `bodyType: typeof req.body`
       - `bodyKeys: req.body && typeof req.body === 'object' ? Object.keys(req.body) : null`
       - `bodyEmail: (req.body as { email?: string })?.email ?? null`
       - `bodyPasswordLen: (req.body as { password?: string })?.password?.length ?? null`  ← **값은 절대 로그하지 않음, 길이만**
       - `rawBodyLen: req.rawBody?.length ?? null`
       - `rawBodyUtf8: req.rawBody?.toString('utf8') ?? null`
    5. 마지막에 `next()` 호출.

    타입 규약:
    - `import type { NextFunction, Request, Response } from 'express';`
    - Request 는 `Request & { rawBody?: Buffer }` 로 확장 (NestFactory rawBody:true 가 제공).
    - `any` 금지 — body 접근 시 `as { email?: string }` / `as { password?: string }` 로 좁힘.
    - `unknown` 은 허용.

    파일 주석 맨 위에 다음을 명시:
    ```
    /**
     * PHASE 09.1 DIAGNOSTIC — REMOVE IN PLAN 05 (Wave 4).
     * Gated by DEBUG_AUTH_REQ=1. CI-only. MUST NOT ship to production logs.
     */
    ```
  </action>
  <verify>
    <automated>
      # 파일 존재 + gate 라인 + tag 문자열 + password 값 미로그(grep으로 password 값 자체 참조 0회) + any 금지 확인:
      test -f apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      grep -q "if (process.env\['DEBUG_AUTH_REQ'\] !== '1') return next();" apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      grep -q "tag: 'AUTH_LOGIN_DEBUG'" apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      grep -q "bodyPasswordLen" apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      ! grep -E "\.password[^?]" apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      ! grep -E "\bany\b" apps/api/src/modules/auth/debug/req-debug.middleware.ts && \
      pnpm --filter @grapit/api typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/auth/debug/req-debug.middleware.ts` 파일 존재.
    - `grep -c "export function createAuthReqDebugMiddleware" apps/api/src/modules/auth/debug/req-debug.middleware.ts` 가 `1`.
    - 파일이 `if (process.env['DEBUG_AUTH_REQ'] !== '1') return next();` 라인을 정확히 포함.
    - `grep "tag: 'AUTH_LOGIN_DEBUG'"` 매치 1건 이상.
    - `grep "bodyPasswordLen"` 매치 1건 이상 AND password 값 직접 로그 없음 (`grep -E "password[\"']?\s*:" ... | grep -v "passwordLen\|passwordField"` 결과 0건).
    - `grep -w "any"` 결과 0건 (strict typing 준수).
    - `pnpm --filter @grapit/api typecheck` exit 0.
  </acceptance_criteria>
  <done>
    진단 미들웨어 factory 가 ESM + strict types 로 존재하고, DEBUG_AUTH_REQ 환경변수로 게이트되며, password 값은 로그하지 않고 길이만 기록한다. typecheck 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: main.ts 에 rawBody 옵션 활성화 + 진단 미들웨어 등록</name>
  <files>apps/api/src/main.ts</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/api/src/main.ts (현재 bootstrap 구조 — cookieParser 위치, 등록 순서 확인)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Pattern 2 rawBody, §Code Examples §1 등록 위치)
    - /Users/sangwopark19/icons/grapit/apps/api/src/modules/auth/debug/req-debug.middleware.ts (Task 1 에서 생성한 export)
  </read_first>
  <action>
    `apps/api/src/main.ts` 에 정확히 두 가지 변경만 가한다. 다른 라인은 touch 금지.

    1. `NestFactory.create(AppModule)` 호출(현재 L46) 을 다음으로 교체:
       ```typescript
       const app = await NestFactory.create(AppModule, { rawBody: true });
       ```
       주석으로 명시: `// Phase 09.1 diagnostic: expose req.rawBody to AUTH_LOGIN_DEBUG middleware. Remove in plan 05.`

    2. `app.use(cookieParser());` (현재 L69) 바로 다음 라인에 다음을 추가:
       ```typescript
       // Phase 09.1 diagnostic — gated by DEBUG_AUTH_REQ=1, removed in plan 05.
       app.use(createAuthReqDebugMiddleware());
       ```
       파일 상단 import 구문에 추가:
       ```typescript
       import { createAuthReqDebugMiddleware } from './modules/auth/debug/req-debug.middleware.js';
       ```
       (ESM `.js` 확장자 import 규약 — CLAUDE.md 준수)

    변경 금지:
    - helmet, cors, global filters/pipes, setGlobalPrefix, listen 순서는 건드리지 않는다.
    - FRONTEND_URL hard-fail 블록(L14-44) 은 수정 금지.
  </action>
  <verify>
    <automated>
      grep -c "rawBody: true" apps/api/src/main.ts | grep -q "^1$" && \
      grep -c "createAuthReqDebugMiddleware" apps/api/src/main.ts | grep -q "^2$" && \
      grep -q "from './modules/auth/debug/req-debug.middleware.js'" apps/api/src/main.ts && \
      pnpm --filter @grapit/api typecheck && \
      pnpm --filter @grapit/api build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rawBody: true" apps/api/src/main.ts` 는 `1`.
    - `grep -c "createAuthReqDebugMiddleware" apps/api/src/main.ts` 는 `2` (import 1회 + 호출 1회).
    - import 경로가 `.js` 확장자로 끝남 (ESM 규약).
    - `pnpm --filter @grapit/api typecheck` exit 0.
    - `pnpm --filter @grapit/api build` exit 0 (dist/main.js 생성 확인).
    - `ls apps/api/dist/main.js` 존재.
    - helmet, cookieParser, enableCors, useGlobalFilters, useGlobalPipes, setGlobalPrefix 호출이 main.ts 에 모두 여전히 존재 (grep count 동일 유지).
  </acceptance_criteria>
  <done>
    main.ts 에서 `{ rawBody: true }` 가 활성화되고 진단 미들웨어가 cookieParser 직후에 등록되었다. typecheck + build 모두 통과.
  </done>
</task>

<task type="auto">
  <name>Task 3: CI workflow 에 DEBUG_AUTH_REQ env + node -v 출력 + AUTH_LOGIN_DEBUG 덤프 step 추가</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/.github/workflows/ci.yml (현재 steps 구조 — Start API server, Smoke test login, E2E tests 위치 확인)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-RESEARCH.md (§Code Examples §4 CI workflow 패치)
  </read_first>
  <action>
    `.github/workflows/ci.yml` 에 정확히 세 가지 변경만 가한다:

    1. `- name: Start API server (background)` step (현재 L105-129) 에 `env:` 블록을 추가. 해당 step 은 현재 env 가 없으므로 `run:` 앞에 다음 블록을 삽입:
       ```yaml
             env:
               # Phase 09.1 diagnostic — CI-only, removed in plan 05.
               DEBUG_AUTH_REQ: "1"
       ```

    2. "Start API server" step 바로 **앞에** 아래 step 을 신규 삽입 (runtime 정보 기록용):
       ```yaml
             - name: Record runtime versions (Phase 09.1 diagnostic)
               run: |
                 echo "node: $(node -v)"
                 echo "pnpm: $(pnpm -v)"
                 echo "os: $(uname -a)"
       ```

    3. "Smoke test login (debug aid)" step (현재 L131-137) **바로 다음에** 아래 step 을 신규 삽입 (진단 로그 조기 덤프 — E2E 전에 curl baseline 1건 확인 가능):
       ```yaml
             - name: Dump AUTH_LOGIN_DEBUG after curl baseline (Phase 09.1 diagnostic)
               if: always()
               run: |
                 echo "=== AUTH_LOGIN_DEBUG entries (expected: 1 after curl) ==="
                 grep AUTH_LOGIN_DEBUG api.log || echo "(none yet — expected at least 1 from curl)"
       ```

    변경 금지:
    - 기존 env block (DATABASE_URL / JWT_SECRET / JWT_REFRESH_SECRET / FRONTEND_URL / NODE_ENV) 수정 금지.
    - Verify Toss test secrets / Install Playwright / Build API / Smoke test login step 내용 수정 금지 (env 추가 외에는).
    - Dump API log (on failure) / Stop API server step 수정 금지.
  </action>
  <verify>
    <automated>
      grep -c "DEBUG_AUTH_REQ: \"1\"" .github/workflows/ci.yml | grep -q "^1$" && \
      grep -q "Record runtime versions" .github/workflows/ci.yml && \
      grep -q "Dump AUTH_LOGIN_DEBUG after curl baseline" .github/workflows/ci.yml && \
      grep -q "node -v" .github/workflows/ci.yml && \
      # YAML syntax 검증 — python yaml 파싱 (ubuntu-latest 에 기본 설치)
      python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DEBUG_AUTH_REQ: "1"' .github/workflows/ci.yml` 는 `1`.
    - `grep "Record runtime versions"` 매치 1건 (신규 step name).
    - `grep "Dump AUTH_LOGIN_DEBUG after curl baseline"` 매치 1건 (신규 step name).
    - `grep "node -v"` 매치 1건.
    - YAML parse 성공 (`python3 -c "import yaml; yaml.safe_load(...)"` exit 0).
    - 기존 step 개수 증가 정확히 `+2` (Record runtime versions, Dump AUTH_LOGIN_DEBUG). 다른 step name 변경 없음.
    - Start API server step 이 신규 env 블록 아래에 여전히 setsid nohup 기동 스크립트를 포함 (`grep "setsid nohup node apps/api/dist/main.js"` 매치 1건 유지).
  </acceptance_criteria>
  <done>
    CI workflow 가 API server 기동 시 DEBUG_AUTH_REQ=1 을 주입하고, node 버전을 기록하고, curl baseline 후 AUTH_LOGIN_DEBUG 로그를 조기 덤프한다. 기존 step 기능은 모두 유지.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → api.log (GitHub Actions artifact) | test credentials (admin@grapit.test) 가 AUTH_LOGIN_DEBUG 로그에 raw 로 기록됨 |
| CI env → api process | DEBUG_AUTH_REQ='1' 주입이 의도치 않게 production 에 전파되면 안 됨 |
| Diagnostic middleware → stdout | 기본 OFF 여야 하고, ON 시에도 password 값 미로그 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | Information Disclosure | apps/api/src/modules/auth/debug/req-debug.middleware.ts — password 값이 CI 로그에 노출될 위험 | mitigate | bodyPasswordLen(길이)만 로그. password 값 자체 접근은 `.password?.length` 로만. Task 1 의 grep acceptance criteria 로 강제. Plan 05 Wave 4 에서 파일 전체 삭제. |
| T-09.1-02 | Information Disclosure | api.log 의 rawBodyUtf8 필드에 `TestAdmin2026!` 테스트 비밀번호가 포함됨 | accept | 테스트 전용 seed credential (admin@grapit.test / TestAdmin2026!) — 프로덕션 사용자 비밀번호가 아님. CI artifact retention 90일 기본, repo 가 private. GitHub Actions 로그 열람 권한은 sole-dev(user) 만. |
| T-09.1-03 | Tampering | apps/api/src/main.ts — rawBody:true 옵션 활성화가 기존 body-parser JSON parsing 에 영향 | mitigate | Nest 11 공식 옵션으로 body-parser 는 계속 동작 + rawBody 를 Buffer 로 추가 노출. Task 2 의 acceptance 에 `pnpm --filter @grapit/api build` + Plan 02 의 curl 200 재확인(S5) 으로 regression 검출. |
| T-09.1-04 | Elevation of Privilege | 진단 미들웨어가 production 으로 릴리스됨 | mitigate | env gate `DEBUG_AUTH_REQ !== '1'` 이 첫 줄에서 즉시 return. Plan 05 Wave 4 에서 파일/import/호출/env/step 전체 제거. deploy.yml 에는 DEBUG_AUTH_REQ 를 절대 추가하지 않음 (Task 3 에서 ci.yml 만 수정). |
</threat_model>

<verification>
- Task 1~3 의 automated verify 모두 exit 0.
- `pnpm --filter @grapit/api test` (기존 auth unit 테스트) 0 failure 유지 — rawBody:true 가 기존 JSON parsing 을 깨지 않음.
- `grep -r "DEBUG_AUTH_REQ" apps/ .github/ .env.example 2>/dev/null` 결과가 정확히 main.ts 의 req-debug.middleware.ts 참조 1건 + ci.yml 1건 = 총 2건. `.env.example` 에는 0건 (Pitfall 3).
</verification>

<success_criteria>
- 진단 미들웨어 factory 존재 + 환경변수 게이트 + password 값 미로그.
- main.ts 에 rawBody:true + 미들웨어 등록 + build 성공.
- ci.yml 에 DEBUG_AUTH_REQ env + runtime 기록 step + AUTH_LOGIN_DEBUG 덤프 step 추가.
- 기존 API unit 테스트 회귀 0.
- `.env.example` 오염 0.
</success_criteria>

<output>
완료 후 `.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-01-SUMMARY.md` 생성.
</output>
