---
phase: 09.1
plan: 04
type: execute
wave: 3
depends_on: ["09.1-03"]
files_modified:
  - apps/web/e2e/toss-payment.spec.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "apps/web/e2e/toss-payment.spec.ts 의 3개 test.fixme 가 모두 test 로 복구된다"
    - "CI 에서 toss-payment.spec.ts 가 3 passed (login-dependent) + 9 passed (Toss 자체) = 12/12 green (S4 신호 최종 green)"
    - "3개 test 모두 loginAsTestUser 호출 후 /booking/.../confirm 또는 /booking/.../complete 접근 시 /auth 로 redirect 되지 않는다"
    - "curl baseline (S5) 여전히 HTTP 200 (서버측 regression 0)"
  artifacts:
    - path: "apps/web/e2e/toss-payment.spec.ts"
      provides: "3개 login-dependent test 가 test.fixme 없이 활성화된 E2E spec"
      contains: "test('happy path"
  key_links:
    - from: "apps/web/e2e/toss-payment.spec.ts"
      to: "apps/web/e2e/helpers/auth.ts"
      via: "loginAsTestUser(page) 호출 3회 (Scenario 1, 2, 3)"
      pattern: "loginAsTestUser\\(page\\)"
    - from: ".github/workflows/ci.yml E2E tests step"
      to: "apps/web/e2e/toss-payment.spec.ts"
      via: "pnpm --filter @grapit/web test:e2e (12/12 expected)"
      pattern: "test:e2e"
---

<objective>
Wave 3: Plan 03 의 helper 수정이 CI 에서 HTTP 200 을 받음을 확인한 후, `apps/web/e2e/toss-payment.spec.ts` 의 3개 `test.fixme()` 를 `test()` 로 복구하고 CI 에서 **12/12 green** 을 달성한다. 이것이 S4 신호의 최종 증거 — Phase 09.1 의 goal 이 실제 user-facing flow 로 달성되었음을 입증.

Purpose: CONTEXT.md "재활성화 확정 조건: test.fixme(...) → test(...) + CI 12/12 green" 의 이행. 이 plan 이 완료되어야 Phase 09 의 VERIFICATION.md "deferred operational checklist" 를 close 할 수 있다.

Output: toss-payment.spec.ts 의 단일 수정 (test.fixme → test 3회) + CI 12/12 green evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-CONTEXT.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-03-SUMMARY.md
@.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md
@apps/web/e2e/toss-payment.spec.ts

<interfaces>
<!-- 현재 toss-payment.spec.ts 의 fixme 3건 정확한 위치 -->

Line 44: `test.fixme('happy path: widget mounts AND confirm API intercepts on complete page', async ({`
Line 129: `test.fixme('UI regression: cancel error URL → toast renders', async ({ page }) => {`
Line 155: `test.fixme('UI regression: decline error URL → error message renders', async ({ page }) => {`

위 3건을 각각 `test.fixme` → `test` 로 변경 (나머지 9개 test 는 이미 `test(...)` 형태).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test.fixme → test 3건 복구 + FIXME 주석 갱신</name>
  <files>apps/web/e2e/toss-payment.spec.ts</files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/toss-payment.spec.ts (L37-43, L125-128, L151-154 FIXME 주석 블록 — 교체 대상)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/09.1-03-SUMMARY.md (Plan 03 에서 적용한 분기 A/B/C)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md (S1+S3+S5 green evidence)
  </read_first>
  <action>
    `apps/web/e2e/toss-payment.spec.ts` 에서 정확히 3건의 변경:

    1. L44 `test.fixme('happy path: widget mounts AND confirm API intercepts on complete page', async ({` → `test('happy path: widget mounts AND confirm API intercepts on complete page', async ({`

    2. L129 `test.fixme('UI regression: cancel error URL → toast renders', async ({ page }) => {` → `test('UI regression: cancel error URL → toast renders', async ({ page }) => {`

    3. L155 `test.fixme('UI regression: decline error URL → error message renders', async ({ page }) => {` → `test('UI regression: decline error URL → error message renders', async ({ page }) => {`

    추가로 각 test 바로 위의 FIXME 주석 블록 (현재 L37-43, L125-128, L151-154) 을 제거하고 대신 아래 단일 라인 주석으로 교체:

    ```typescript
    // Phase 09.1 closure: previous 401 from Playwright login helper resolved (9.1-03-SUMMARY).
    ```

    **변경 금지:**
    - test 본문 (L45-123, L130-149, L156-173) 은 완전히 유지 — fixture seed / intercept / page.goto / expect 모두 그대로.
    - `test.describe` block, `test.skip` (TOSS_CLIENT_KEY_TEST env gate, L29-32) 은 유지.
    - 나머지 9개 `test(...)` 는 touch 금지.
  </action>
  <verify>
    <automated>
      # test.fixme 가 정확히 0건인지 확인
      grep -c "test.fixme" apps/web/e2e/toss-payment.spec.ts | grep -q "^0$" && \
      # 3개 test title 이 모두 존재
      grep -q "test('happy path: widget mounts AND confirm API intercepts on complete page'" apps/web/e2e/toss-payment.spec.ts && \
      grep -q "test('UI regression: cancel error URL → toast renders'" apps/web/e2e/toss-payment.spec.ts && \
      grep -q "test('UI regression: decline error URL → error message renders'" apps/web/e2e/toss-payment.spec.ts && \
      # Phase 09.1 closure 주석이 3건
      grep -c "Phase 09.1 closure" apps/web/e2e/toss-payment.spec.ts | grep -q "^3$" && \
      pnpm --filter @grapit/web typecheck && \
      pnpm --filter @grapit/web lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.fixme" apps/web/e2e/toss-payment.spec.ts` 결과 `0` (완전 제거).
    - `grep -c "test(" apps/web/e2e/toss-payment.spec.ts` 가 이전(`grep -c "test(" old`) + `grep -c "test.fixme" old` 와 동일 (3건이 test.fixme → test 로 변환).
    - `grep -c "Phase 09.1 closure"` 결과 `3` (3개 test 각각에 closure 주석).
    - 이전 FIXME 주석 본문 (`grep "FIXME (Phase 10 / CI-login-E2E)"`) 결과 `0`.
    - 3개 test title 문자열 모두 파일에 존재 (title 변경 금지).
    - `pnpm --filter @grapit/web typecheck` exit 0.
    - `pnpm --filter @grapit/web lint` exit 0 (0 errors).
    - describe block / test.skip / loginAsTestUser / injectBookingFixture / page.route / expect 호출 개수 변화 없음 (본문 미변경 확인).
  </acceptance_criteria>
  <done>
    3개 login-dependent test 가 test.fixme 없이 활성화되고 closure 주석 기록. typecheck + lint green. 나머지 9개 test 미변경.
  </done>
</task>

<task type="auto">
  <name>Task 2: CI 실행 → 12/12 green 확인 및 evidence 기록</name>
  <files>
    .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md
  </files>
  <read_first>
    - /Users/sangwopark19/icons/grapit/apps/web/e2e/toss-payment.spec.ts (Task 1 수정본)
    - /Users/sangwopark19/icons/grapit/.planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/fix-verification.md (S1/S3/S5 이미 green — Task 2 는 S4 추가)
  </read_first>
  <action>
    Task 1 커밋을 push → CI 트리거.

    1. Actions UI 에서 "E2E tests (Toss Payments)" step 이 exit 0 으로 완료되는지 확인.
    2. Playwright HTML reporter 또는 stdout 에서 `12 passed` (또는 `12/12`) 확인.
    3. 다음 evidence 파일 생성:

       ```markdown
       # Phase 09.1 — CI 12/12 green evidence (S4 신호)

       **CI run URL:** <GitHub Actions run URL>
       **Commit SHA:** <git SHA of the fixme-restoration commit>
       **Date:** <ISO timestamp>
       **Cause branch applied:** <A | B | C> (per 09.1-03-SUMMARY.md)

       ## Before (Phase 09 VERIFICATION)
       - toss-payment.spec.ts: 9 passed, 3 test.fixme (login-dependent)

       ## After
       - toss-payment.spec.ts: **12 passed, 0 test.fixme, 0 failed**
         - happy path: widget mounts AND confirm API intercepts on complete page ✓
         - UI regression: cancel error URL → toast renders ✓
         - UI regression: decline error URL → error message renders ✓
         - (기존 9 Toss 자체 테스트) ✓

       ## Independent signals final
       | Signal | Status | Evidence |
       |--------|--------|----------|
       | S1 (server req 정상화) | ✓ | artifacts/fix-verification.md + api.log AUTH_LOGIN_DEBUG bodyEmail="admin@grapit.test" bodyPasswordLen=17 |
       | S2 (wire header 정합) | ✓ | artifacts/fix-verification.md header 테이블 |
       | S3 (response body 정상) | ✓ | probe spec status=200 + accessToken + Set-Cookie:refreshToken |
       | S4 (E2E 복구) | ✓ | **이 문서 — 12/12 green** |
       | S5 (curl regression 0) | ✓ | CI "Smoke test login" step HTTP 200 유지 |
       ```

    4. `git add artifacts/ci-12-12-green.md && git commit -m "docs(09.1): record 12/12 green CI evidence (S4 signal)"` — Plan 05 가 이를 참조.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md && \
      grep -q "12 passed, 0 test.fixme, 0 failed" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md && \
      grep -q "S1 (server req 정상화) | ✓" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md && \
      grep -q "S4 (E2E 복구) | ✓" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md && \
      grep -q "S5 (curl regression 0) | ✓" .planning/phases/09.1-ci-login-e2e-playwright-login-helper-post-auth-login-body-40/artifacts/ci-12-12-green.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `artifacts/ci-12-12-green.md` 파일 존재.
    - 정확한 문자열 `"12 passed, 0 test.fixme, 0 failed"` 포함.
    - CI run URL 필드에 실제 URL 기입 (빈 플레이스홀더 금지, `https://github.com/...` 형식 필수).
    - S1, S2, S3, S4, S5 5 신호 전부 `✓` 로 표시.
    - Cause branch applied 필드가 A/B/C 중 하나로 기입 (03-SUMMARY 와 일치).
    - GitHub Actions 해당 run 의 workflow status 가 실제로 `success` (CI 가 green 이어야 함).
    - CI "E2E tests (Toss Payments)" step exit 0.
    - `pnpm --filter @grapit/web test:e2e` 가 로컬에서도 (DB / API 기동 시) 12/12 green (검증 권장이지만 CI 결과가 최종 기준).
  </acceptance_criteria>
  <done>
    CI 에서 toss-payment.spec.ts 12/12 green 확인되고 evidence 파일이 커밋됨. S1+S2+S3+S4+S5 5 독립 신호 모두 green — Phase 09.1 의 핵심 목표 (S4) 달성.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toss-payment.spec.ts (CI only) → git main | 테스트 복구가 main 에 merge 되면 향후 모든 push 에서 S4 신호가 강제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-11 | Denial of Service | 3개 복구 test 가 flaky → CI 가 간헐적으로 red | mitigate | Plan 03 Task 4 fix-verification.md 에 S1+S3+S5 이미 green 확인. Task 2 의 CI run 이 stable 한 것으로 판정되면 merge. 만약 flaky 발생 시 Plan 05 는 보류하고 Plan 03 revision(fallback 분기 전환) 필요. |
| T-09.1-12 | Tampering | Task 1 이 test 본문을 실수로 수정하여 기존 assertion 약화 | mitigate | acceptance 에서 loginAsTestUser / injectBookingFixture / page.route / expect 호출 개수 변화 0 검증. Task 1 action 에서 "본문 미변경" 명시. |
</threat_model>

<verification>
- Task 1 automated verify: test.fixme 0건 + Phase 09.1 closure 주석 3건 + typecheck + lint.
- Task 2 automated verify: ci-12-12-green.md 존재 + "12 passed" 문자열 + S1-S5 ✓.
- GitHub Actions 의 해당 run 의 workflow status = success.
- `grep -c "test.fixme" apps/web/e2e/` 결과 전체 0 (다른 spec 포함 — Phase 09.1 범위는 toss-payment.spec.ts 만이지만 side-effect 확인).
</verification>

<success_criteria>
- 3개 login-dependent test 가 test 로 복구되고 CI 에서 12/12 green (S4).
- S1+S2+S3+S4+S5 5 독립 신호 모두 green → RESEARCH.md §Proof-of-fix 완전 만족.
- 다른 E2E spec (social-login.spec.ts 등) regression 0.
- 서버측 코드 (범주 A/B 경로에서는) 변경 0 (main.ts 의 진단 관련 수정만 존재).
</success_criteria>

<output>
완료 후 `.planning/phases/09.1-.../09.1-04-SUMMARY.md` 생성. SUMMARY 는 S1-S5 5 신호 최종 상태표와 CI run URL 을 포함.
</output>
