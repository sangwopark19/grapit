---
phase: 05-polish-launch
plan: 04
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - apps/web/instrumentation-client.ts
  - apps/web/instrumentation.ts
  - apps/web/sentry.server.config.ts
  - apps/web/sentry.edge.config.ts
  - apps/web/app/global-error.tsx
  - apps/web/next.config.ts
  - apps/api/src/instrument.ts
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/web/Dockerfile
  - apps/api/Dockerfile
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - .dockerignore
  - .env.example
autonomous: false
requirements:
  - INFR-03

user_setup:
  - service: sentry
    why: "에러 추적 및 성능 모니터링"
    env_vars:
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Sentry Dashboard -> Projects -> grapit-web -> Client Keys (DSN)"
      - name: SENTRY_DSN
        source: "Sentry Dashboard -> Projects -> grapit-api -> Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard -> Settings -> Auth Tokens -> Create New Token (org:read, project:releases, project:write)"
    dashboard_config:
      - task: "Create Next.js project (grapit-web)"
        location: "Sentry Dashboard -> Projects -> Create Project -> Next.js"
      - task: "Create Node.js project (grapit-api)"
        location: "Sentry Dashboard -> Projects -> Create Project -> Node.js"
  - service: gcp
    why: "Cloud Run 배포 + Artifact Registry + Secret Manager"
    env_vars:
      - name: GCP_PROJECT_ID
        source: "GCP Console -> Project Selector"
      - name: GCP_WIF_PROVIDER
        source: "GCP Console -> IAM -> Workload Identity Federation -> Provider full name"
      - name: GCP_SERVICE_ACCOUNT
        source: "GCP Console -> IAM -> Service Accounts -> email"
      - name: GCP_REGION
        source: "asia-northeast3 (서울)"
    dashboard_config:
      - task: "Create Artifact Registry repository"
        location: "GCP Console -> Artifact Registry -> Create Repository (Docker, asia-northeast3)"
      - task: "Create Workload Identity Pool + Provider for GitHub Actions"
        location: "GCP Console -> IAM -> Workload Identity Federation"
      - task: "Create Service Account with Cloud Run Admin + Artifact Registry Writer roles"
        location: "GCP Console -> IAM -> Service Accounts"
      - task: "Create secrets in Secret Manager"
        location: "GCP Console -> Secret Manager -> Create secrets for DATABASE_URL, JWT_SECRET, JWT_REFRESH_SECRET, etc."

must_haves:
  truths:
    - "Sentry가 프론트엔드와 백엔드 에러를 캡처한다"
    - "PR 생성 시 lint + typecheck + test가 자동 실행된다"
    - "main 머지 시 Docker 빌드 -> Cloud Run 배포가 자동 실행된다"
    - "Dockerfile 멀티스테이지 빌드로 프로덕션 이미지가 최적화된다"
    - "환경변수가 GCP Secret Manager에서 Cloud Run으로 주입된다"
  artifacts:
    - path: "apps/web/instrumentation-client.ts"
      provides: "Sentry 클라이언트 초기화"
      contains: "Sentry.init"
    - path: "apps/api/src/instrument.ts"
      provides: "Sentry 백엔드 초기화"
      contains: "Sentry.init"
    - path: "apps/web/Dockerfile"
      provides: "Next.js standalone 멀티스테이지 Docker 빌드"
      contains: "FROM node:22-alpine"
    - path: "apps/api/Dockerfile"
      provides: "NestJS 멀티스테이지 Docker 빌드"
      contains: "FROM node:22-alpine"
    - path: ".github/workflows/ci.yml"
      provides: "PR 검증 파이프라인"
      contains: "pnpm test"
    - path: ".github/workflows/deploy.yml"
      provides: "main 브랜치 배포 파이프라인"
      contains: "deploy-cloudrun"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/instrument.ts"
      via: "import './instrument.js' (첫 줄)"
      pattern: "import.*instrument"
    - from: "apps/web/next.config.ts"
      to: "@sentry/nextjs"
      via: "withSentryConfig wrapper"
      pattern: "withSentryConfig"
    - from: ".github/workflows/deploy.yml"
      to: "apps/web/Dockerfile"
      via: "docker build -f"
      pattern: "docker build"
---

<objective>
Sentry 에러 추적 설정, Docker 멀티스테이지 빌드, GitHub Actions CI/CD 파이프라인, Cloud Run 배포 설정을 구현하여 프로덕션 배포를 자동화한다.

Purpose: 에러를 실시간 추적하고, 코드 변경이 자동으로 검증/배포되어 안정적인 프로덕션 서비스를 운영한다.
Output: Sentry 설정 파일, Dockerfiles, GitHub Actions workflows, .dockerignore
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-launch/05-CONTEXT.md
@.planning/phases/05-polish-launch/05-RESEARCH.md

<interfaces>
<!-- Current next.config.ts that will be wrapped with Sentry -->

From apps/web/next.config.ts:
```typescript
import type { NextConfig } from 'next';
import { readFileSync } from 'fs';
import { resolve } from 'path';

// .env loading from monorepo root...

const nextConfig: NextConfig = {
  allowedDevOrigins: ['192.168.0.78', 'craig-paravail-yee.ngrok-free.dev'],
  output: 'standalone',
  transpilePackages: ['@grapit/shared'],
  turbopack: { root: resolve(__dirname, '../../') },
  async rewrites() { /* proxy to localhost:8080 */ },
  images: { unoptimized: process.env.NODE_ENV !== 'production', remotePatterns: [] },
};
export default nextConfig;
```

From apps/api/src/main.ts (first lines):
```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module.js';
// ... bootstrap function
```

From apps/api/src/app.module.ts:
```typescript
@Module({ imports: [/* existing modules */] })
export class AppModule {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sentry 프론트엔드 + 백엔드 설정</name>
  <files>
    apps/web/instrumentation-client.ts,
    apps/web/instrumentation.ts,
    apps/web/sentry.server.config.ts,
    apps/web/sentry.edge.config.ts,
    apps/web/app/global-error.tsx,
    apps/web/next.config.ts,
    apps/api/src/instrument.ts,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    .env.example
  </files>
  <read_first>
    apps/web/next.config.ts,
    apps/web/app/error.tsx,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    apps/web/package.json,
    apps/api/package.json,
    .env.example,
    .planning/phases/05-polish-launch/05-RESEARCH.md
  </read_first>
  <action>
    Per D-14: Sentry 프론트엔드(@sentry/nextjs)와 백엔드(@sentry/nestjs)를 설정한다.

    0. 패키지 설치:
       ```bash
       pnpm --filter @grapit/web add @sentry/nextjs@^10
       pnpm --filter @grapit/api add @sentry/nestjs@^10
       ```

    1. **프론트엔드 Sentry 설정 (4개 파일):**

       a. `apps/web/instrumentation-client.ts`:
       ```typescript
       import * as Sentry from '@sentry/nextjs';

       Sentry.init({
         dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
         tracesSampleRate: 0.1,
         replaysSessionSampleRate: 0,
         replaysOnErrorSampleRate: 1.0,
         environment: process.env.NODE_ENV,
       });
       ```

       b. `apps/web/instrumentation.ts`:
       ```typescript
       import * as Sentry from '@sentry/nextjs';

       export async function register() {
         if (process.env.NEXT_RUNTIME === 'nodejs') {
           await import('./sentry.server.config');
         }
         if (process.env.NEXT_RUNTIME === 'edge') {
           await import('./sentry.edge.config');
         }
       }

       export const onRequestError = Sentry.captureRequestError;
       ```

       c. `apps/web/sentry.server.config.ts`:
       ```typescript
       import * as Sentry from '@sentry/nextjs';

       Sentry.init({
         dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
         tracesSampleRate: 0.1,
         environment: process.env.NODE_ENV,
       });
       ```

       d. `apps/web/sentry.edge.config.ts`:
       ```typescript
       import * as Sentry from '@sentry/nextjs';

       Sentry.init({
         dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
         tracesSampleRate: 0.1,
         environment: process.env.NODE_ENV,
       });
       ```

    2. **global-error.tsx** 생성 (root layout 에러 캡처):
       ```typescript
       'use client';

       import * as Sentry from '@sentry/nextjs';
       import { useEffect } from 'react';
       import { Button } from '@/components/ui/button';

       export default function GlobalError({
         error,
         reset,
       }: {
         error: Error & { digest?: string };
         reset: () => void;
       }) {
         useEffect(() => {
           Sentry.captureException(error);
         }, [error]);

         return (
           <html lang="ko">
             <body className="flex min-h-screen flex-col items-center justify-center gap-4 px-4">
               <h2 className="text-xl font-semibold text-gray-900">문제가 발생했습니다</h2>
               <p className="text-base text-gray-500">
                 일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요.
               </p>
               <Button onClick={reset}>다시 시도</Button>
             </body>
           </html>
         );
       }
       ```

    3. **next.config.ts 수정:**
       - `import { withSentryConfig } from '@sentry/nextjs';` 추가
       - `outputFileTracingRoot: resolve(__dirname, '../../')` 추가 (monorepo root, per Pitfall 1)
       - `export default nextConfig`을 `export default withSentryConfig(nextConfig, { org: process.env.SENTRY_ORG, project: process.env.SENTRY_PROJECT, silent: !process.env.CI, widenClientFileUpload: true, disableLogger: true, automaticVercelMonitors: false })` 로 교체
       - 주의: Turbopack dev에서 Sentry warning이 발생할 수 있으나 프로덕션에 영향 없음 (Pitfall 2)

    4. **백엔드 Sentry 설정 (3개 파일):**

       a. `apps/api/src/instrument.ts`:
       ```typescript
       import * as Sentry from '@sentry/nestjs';

       Sentry.init({
         dsn: process.env.SENTRY_DSN,
         tracesSampleRate: 0.1,
         environment: process.env.NODE_ENV,
       });
       ```

       b. `apps/api/src/main.ts` 수정:
       - 파일 첫 줄에 `import './instrument.js';` 추가 (다른 모든 import보다 위)
       - 나머지 기존 코드 유지

       c. `apps/api/src/app.module.ts` 수정:
       - `import { SentryModule } from '@sentry/nestjs/setup';` 추가
       - `@Module({ imports: [SentryModule.forRoot(), /* ...기존 모듈 */] })` 로 SentryModule을 imports 배열 첫 번째에 추가

    5. **.env.example 업데이트:**
       ```
       # Sentry
       NEXT_PUBLIC_SENTRY_DSN=
       SENTRY_DSN=
       SENTRY_AUTH_TOKEN=
       SENTRY_ORG=
       SENTRY_PROJECT=
       ```
       기존 .env.example 내용을 유지하고 위 항목을 Sentry 섹션으로 추가
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web exec tsc --noEmit && pnpm --filter @grapit/api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/instrumentation-client.ts contains "Sentry.init"
    - apps/web/instrumentation-client.ts contains "NEXT_PUBLIC_SENTRY_DSN"
    - apps/web/instrumentation-client.ts contains "tracesSampleRate: 0.1"
    - apps/web/instrumentation.ts contains "onRequestError"
    - apps/web/sentry.server.config.ts contains "Sentry.init"
    - apps/web/sentry.edge.config.ts contains "Sentry.init"
    - apps/web/app/global-error.tsx contains "Sentry.captureException"
    - apps/web/next.config.ts contains "withSentryConfig"
    - apps/web/next.config.ts contains "outputFileTracingRoot"
    - apps/api/src/instrument.ts contains "Sentry.init"
    - apps/api/src/instrument.ts contains "SENTRY_DSN"
    - apps/api/src/main.ts first import is "./instrument.js"
    - apps/api/src/app.module.ts contains "SentryModule.forRoot()"
    - .env.example contains "NEXT_PUBLIC_SENTRY_DSN"
    - .env.example contains "SENTRY_AUTH_TOKEN"
    - tsc --noEmit exits 0 for both web and api
  </acceptance_criteria>
  <done>Sentry가 프론트엔드(클라이언트/서버/엣지)와 백엔드에 설정됨. DSN 환경변수 placeholder로 설정. TypeScript 빌드 통과.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfiles + GitHub Actions CI/CD + .dockerignore</name>
  <files>
    apps/web/Dockerfile,
    apps/api/Dockerfile,
    .github/workflows/ci.yml,
    .github/workflows/deploy.yml,
    .dockerignore
  </files>
  <read_first>
    apps/web/next.config.ts,
    apps/web/package.json,
    apps/api/package.json,
    packages/shared/package.json,
    pnpm-workspace.yaml,
    package.json,
    .planning/phases/05-polish-launch/05-RESEARCH.md,
    .planning/phases/05-polish-launch/05-CONTEXT.md
  </read_first>
  <action>
    Per D-15, D-16, D-17, D-18, D-19, D-20: Docker 멀티스테이지 빌드와 GitHub Actions CI/CD 파이프라인을 구축한다.

    1. **apps/web/Dockerfile** 생성 (per D-19, Next.js standalone):
       ```dockerfile
       # Stage 1: Dependencies
       FROM node:22-alpine AS deps
       RUN corepack enable && corepack prepare pnpm@latest --activate
       WORKDIR /app
       COPY pnpm-lock.yaml pnpm-workspace.yaml package.json ./
       COPY apps/web/package.json ./apps/web/
       COPY packages/shared/package.json ./packages/shared/
       RUN pnpm install --frozen-lockfile

       # Stage 2: Build
       FROM node:22-alpine AS builder
       RUN corepack enable && corepack prepare pnpm@latest --activate
       WORKDIR /app
       COPY --from=deps /app/ ./
       COPY . .
       RUN pnpm --filter @grapit/shared build
       RUN pnpm --filter @grapit/web build

       # Stage 3: Production
       FROM node:22-alpine AS runner
       WORKDIR /app
       ENV NODE_ENV=production
       RUN addgroup --system --gid 1001 nodejs && \
           adduser --system --uid 1001 nextjs
       COPY --from=builder /app/apps/web/.next/standalone ./
       COPY --from=builder /app/apps/web/.next/static ./apps/web/.next/static
       COPY --from=builder /app/apps/web/public ./apps/web/public
       USER nextjs
       EXPOSE 3000
       ENV PORT=3000
       ENV HOSTNAME="0.0.0.0"
       CMD ["node", "apps/web/server.js"]
       ```
       - Docker 빌드 컨텍스트는 monorepo root로 설정 (`docker build -f apps/web/Dockerfile .`)
       - outputFileTracingRoot가 next.config.ts에서 monorepo root로 설정되어 standalone이 shared 패키지를 포함함

    2. **apps/api/Dockerfile** 생성 (per D-19, NestJS):
       ```dockerfile
       # Stage 1: Dependencies
       FROM node:22-alpine AS deps
       RUN corepack enable && corepack prepare pnpm@latest --activate
       WORKDIR /app
       COPY pnpm-lock.yaml pnpm-workspace.yaml package.json ./
       COPY apps/api/package.json ./apps/api/
       COPY packages/shared/package.json ./packages/shared/
       RUN pnpm install --frozen-lockfile

       # Stage 2: Build
       FROM node:22-alpine AS builder
       RUN corepack enable && corepack prepare pnpm@latest --activate
       WORKDIR /app
       COPY --from=deps /app/ ./
       COPY . .
       RUN pnpm --filter @grapit/shared build
       RUN pnpm --filter @grapit/api build

       # Stage 3: Production
       FROM node:22-alpine AS runner
       WORKDIR /app
       ENV NODE_ENV=production
       RUN addgroup --system --gid 1001 nodejs && \
           adduser --system --uid 1001 nestjs
       COPY --from=builder /app/apps/api/dist ./dist
       COPY --from=builder /app/node_modules ./node_modules
       COPY --from=builder /app/apps/api/node_modules ./apps/api/node_modules
       COPY --from=builder /app/packages/shared ./packages/shared
       COPY --from=builder /app/apps/api/package.json ./package.json
       USER nestjs
       EXPOSE 8080
       ENV PORT=8080
       CMD ["node", "dist/main.js"]
       ```

    3. **.dockerignore** 생성:
       ```
       node_modules
       .next
       .git
       .env
       .env.*
       !.env.example
       *.md
       .planning
       .claude
       .agents
       dist
       coverage
       .turbo
       ```

    4. **.github/workflows/ci.yml** 생성 (per D-15, PR 검증):
       ```yaml
       name: CI
       on:
         pull_request:
           branches: [main]

       jobs:
         check:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: pnpm/action-setup@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: 22
                 cache: pnpm
             - run: pnpm install --frozen-lockfile
             - run: pnpm lint
             - run: pnpm typecheck
             - run: pnpm test
       ```

    5. **.github/workflows/deploy.yml** 생성 (per D-15, D-16, D-17, D-18, D-20):
       ```yaml
       name: Deploy
       on:
         push:
           branches: [main]

       env:
         GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
         GCP_REGION: asia-northeast3
         AR_REPO: grapit
         WEB_SERVICE: grapit-web
         API_SERVICE: grapit-api

       jobs:
         deploy-api:
           runs-on: ubuntu-latest
           permissions:
             contents: read
             id-token: write
           steps:
             - uses: actions/checkout@v4
             - uses: pnpm/action-setup@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: 22
                 cache: pnpm
             - run: pnpm install --frozen-lockfile
             # DB migration (per D-17)
             - name: Run migrations
               env:
                 DATABASE_URL: ${{ secrets.DATABASE_URL }}
               run: pnpm --filter @grapit/api exec drizzle-kit migrate
             # GCP Auth (OIDC, per D-20)
             - id: auth
               uses: google-github-actions/auth@v2
               with:
                 workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
                 service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
             - uses: google-github-actions/setup-gcloud@v2
             - run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev
             # Build & Push API image
             - name: Build API image
               run: |
                 docker build -f apps/api/Dockerfile \
                   -t ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.API_SERVICE }}:${{ github.sha }} \
                   .
             - name: Push API image
               run: docker push ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.API_SERVICE }}:${{ github.sha }}
             # Deploy to Cloud Run (per D-16, D-18)
             - uses: google-github-actions/deploy-cloudrun@v2
               with:
                 service: ${{ env.API_SERVICE }}
                 region: ${{ env.GCP_REGION }}
                 image: ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.API_SERVICE }}:${{ github.sha }}
                 flags: --min-instances=1 --port=8080

         deploy-web:
           runs-on: ubuntu-latest
           needs: deploy-api
           permissions:
             contents: read
             id-token: write
           steps:
             - uses: actions/checkout@v4
             # GCP Auth
             - id: auth
               uses: google-github-actions/auth@v2
               with:
                 workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
                 service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
             - uses: google-github-actions/setup-gcloud@v2
             - run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev
             # Build & Push Web image
             - name: Build Web image
               run: |
                 docker build -f apps/web/Dockerfile \
                   -t ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.WEB_SERVICE }}:${{ github.sha }} \
                   .
             - name: Push Web image
               run: docker push ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.WEB_SERVICE }}:${{ github.sha }}
             # Deploy to Cloud Run (per D-16, D-18)
             - uses: google-github-actions/deploy-cloudrun@v2
               with:
                 service: ${{ env.WEB_SERVICE }}
                 region: ${{ env.GCP_REGION }}
                 image: ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.WEB_SERVICE }}:${{ github.sha }}
                 flags: --min-instances=1 --port=3000
       ```
       - deploy-web은 deploy-api 완료 후 실행 (API가 먼저 올라가야 웹이 정상 동작)
       - min-instances=1 per D-18
       - Secret Manager 시크릿은 Cloud Run 서비스 설정에서 직접 마운트 (workflow에서는 처리하지 않음)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && test -f apps/web/Dockerfile && test -f apps/api/Dockerfile && test -f .github/workflows/ci.yml && test -f .github/workflows/deploy.yml && test -f .dockerignore && echo "All infra files exist"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/Dockerfile contains "FROM node:22-alpine"
    - apps/web/Dockerfile contains "server.js" in CMD
    - apps/web/Dockerfile contains three FROM stages (deps, builder, runner)
    - apps/api/Dockerfile contains "FROM node:22-alpine"
    - apps/api/Dockerfile contains three FROM stages
    - .github/workflows/ci.yml contains "pnpm lint"
    - .github/workflows/ci.yml contains "pnpm typecheck"
    - .github/workflows/ci.yml contains "pnpm test"
    - .github/workflows/deploy.yml contains "google-github-actions/auth@v2"
    - .github/workflows/deploy.yml contains "google-github-actions/deploy-cloudrun@v2"
    - .github/workflows/deploy.yml contains "min-instances=1"
    - .github/workflows/deploy.yml contains "drizzle-kit migrate"
    - .dockerignore contains ".env"
    - .dockerignore contains "node_modules"
  </acceptance_criteria>
  <done>Docker 멀티스테이지 빌드가 web/api 각각 설정됨. CI workflow가 PR에서 lint+typecheck+test 실행. Deploy workflow가 main 머지 시 Docker 빌드 + Cloud Run 배포 + DB 마이그레이션 자동화. min-instances=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 프로덕션 인프라 검증</name>
  <files>
    apps/web/Dockerfile,
    apps/api/Dockerfile,
    .github/workflows/ci.yml,
    .github/workflows/deploy.yml
  </files>
  <action>
    사용자가 프로덕션 인프라 설정을 검토한다. Docker 빌드 로컬 테스트, Sentry 설정 확인, GitHub Actions workflow 구조 검토.
  </action>
  <what-built>
    Sentry 에러 추적 (프론트엔드 + 백엔드), Docker 멀티스테이지 빌드 (web + api), GitHub Actions CI/CD (ci.yml + deploy.yml), .dockerignore
  </what-built>
  <how-to-verify>
    1. Sentry 설정 확인:
       - apps/web/instrumentation-client.ts, instrumentation.ts, sentry.server.config.ts, sentry.edge.config.ts 4개 파일 존재 확인
       - apps/api/src/instrument.ts 파일 존재 확인
       - apps/api/src/main.ts 첫 줄에 import './instrument.js' 확인

    2. Docker 빌드 로컬 테스트 (Docker Desktop 실행 필요):
       ```bash
       docker build -f apps/web/Dockerfile -t grapit-web .
       docker build -f apps/api/Dockerfile -t grapit-api .
       ```
       - 빌드 성공 여부 확인

    3. GitHub Actions workflow 검토:
       - .github/workflows/ci.yml: PR 시 lint + typecheck + test 실행 확인
       - .github/workflows/deploy.yml: OIDC 인증, Docker 빌드, Cloud Run 배포 단계 확인

    4. 실제 배포는 GCP 설정(Workload Identity Federation, Artifact Registry, Secret Manager)이 완료된 후에 가능
  </how-to-verify>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && test -f apps/web/instrumentation-client.ts && test -f apps/api/src/instrument.ts && test -f apps/web/Dockerfile && test -f .github/workflows/deploy.yml && echo "All infra artifacts present"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/instrumentation-client.ts exists
    - apps/api/src/instrument.ts exists
    - apps/web/Dockerfile exists
    - apps/api/Dockerfile exists
    - .github/workflows/ci.yml exists
    - .github/workflows/deploy.yml exists
  </acceptance_criteria>
  <done>사용자가 프로덕션 인프라 설정을 검토하고 승인함</done>
  <resume-signal>"approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> GCP | OIDC 토큰으로 GCP 서비스 접근 |
| Docker 이미지 -> 시크릿 | 빌드 시 시크릿이 이미지에 포함되지 않아야 함 |
| Sentry DSN -> 외부 서비스 | 에러 데이터가 Sentry 서버로 전송됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | Docker image | mitigate | 멀티스테이지 빌드로 빌드 환경과 프로덕션 환경 분리. .dockerignore에 .env 포함. node_modules에 dev dependencies 미포함 (--frozen-lockfile + NODE_ENV=production) |
| T-05-08 | Elevation of Privilege | GitHub Actions secrets | mitigate | Workload Identity Federation(OIDC)으로 keyless 인증. 서비스 키 JSON 파일 미사용. Service Account에 최소 권한(Cloud Run Admin + AR Writer)만 부여 |
| T-05-09 | Information Disclosure | Sentry error data | mitigate | tracesSampleRate 0.1로 제한. 에러 메시지에 PII 미포함 (api-client에서 STATUS_MESSAGES 사용). 소스맵은 CI에서만 업로드 (SENTRY_AUTH_TOKEN은 CI secret) |
| T-05-10 | Spoofing | Cloud Run public access | mitigate | Cloudflare WAF 뒤에 배치. Cloud Run invoker 권한으로 직접 접근 제한 |
| T-05-11 | Information Disclosure | .env in Docker | mitigate | .dockerignore에 .env, .env.* 포함. 프로덕션 환경변수는 GCP Secret Manager -> Cloud Run secret mount로만 주입 |
</threat_model>

<verification>
1. Sentry 설정 파일 4+3개 존재 확인
2. Dockerfile 2개 멀티스테이지 빌드 확인
3. GitHub Actions workflow 2개 존재 확인
4. .dockerignore에 .env 포함 확인
5. TypeScript 빌드 통과
</verification>

<success_criteria>
- Sentry가 프론트엔드(클라이언트/서버/엣지) + 백엔드에 설정됨
- Docker 멀티스테이지 빌드가 web/api 각각 3단계로 구성됨
- CI workflow가 PR에서 lint + typecheck + test 실행
- Deploy workflow가 main 머지 시 OIDC 인증 + Docker 빌드 + Cloud Run 배포
- DB 마이그레이션이 배포 전 자동 실행
- Cloud Run min-instances=1
- 시크릿이 Docker 이미지에 포함되지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-launch/05-04-SUMMARY.md`
</output>
