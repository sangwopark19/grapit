---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/components.json
  - apps/web/components/ui/button.tsx
  - apps/web/components/ui/input.tsx
  - apps/web/components/ui/label.tsx
  - apps/web/components/ui/checkbox.tsx
  - apps/web/components/ui/tabs.tsx
  - apps/web/components/ui/dialog.tsx
  - apps/web/components/ui/separator.tsx
  - apps/web/components/ui/sonner.tsx
  - apps/web/components/ui/form.tsx
  - apps/web/components/layout/gnb.tsx
  - apps/web/components/layout/footer.tsx
  - apps/web/components/layout/mobile-menu.tsx
  - apps/web/components/auth/social-login-button.tsx
  - apps/web/components/auth/step-indicator.tsx
  - apps/web/components/auth/password-input.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/public/icons/kakao.svg
  - apps/web/public/icons/naver.svg
  - apps/web/public/icons/google.svg
  - apps/web/lib/cn.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "GNB renders with Grapit logo, disabled genre tabs, disabled search bar, and login button"
    - "Footer renders with 이용약관, 개인정보처리방침, 고객센터 links and copyright"
    - "Home page shows empty state with brand heading, description, and CTA button"
    - "shadcn components installed and working (Button, Input, Tabs, etc.)"
    - "Pretendard font loads via next/font/local and applies to all text"
  artifacts:
    - path: "apps/web/components/layout/gnb.tsx"
      provides: "Global navigation bar per D-10 and UI-SPEC"
      contains: "Grapit"
    - path: "apps/web/components/layout/footer.tsx"
      provides: "Site footer per D-13 and UI-SPEC"
      contains: "개인정보처리방침"
    - path: "apps/web/app/page.tsx"
      provides: "Home empty state per D-11"
      contains: "곧 다양한 공연이 찾아옵니다"
    - path: "apps/web/components/auth/social-login-button.tsx"
      provides: "Branded social login buttons per UI-SPEC"
      exports: ["SocialLoginButton"]
    - path: "apps/web/components/auth/step-indicator.tsx"
      provides: "3-step registration progress per D-07"
      exports: ["StepIndicator"]
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/components/layout/gnb.tsx"
      via: "React component import"
      pattern: "import.*GNB"
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/components/layout/footer.tsx"
      via: "React component import"
      pattern: "import.*Footer"
---

<objective>
Set up shadcn/ui component library, create the app shell (GNB, Footer, root layout), build the home empty state page, and implement all reusable auth UI components (SocialLoginButton, StepIndicator, PasswordInput). This establishes the visual foundation with Grapit's brand palette.

Purpose: Deliver the app shell and component library so auth pages (Plan 05) can be built immediately against these components.
Output: Working app shell with GNB, Footer, Home page, and auth component primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui, install components, and set up Grapit design tokens</name>
  <files>
    apps/web/components.json
    apps/web/components/ui/button.tsx
    apps/web/components/ui/input.tsx
    apps/web/components/ui/label.tsx
    apps/web/components/ui/checkbox.tsx
    apps/web/components/ui/tabs.tsx
    apps/web/components/ui/dialog.tsx
    apps/web/components/ui/separator.tsx
    apps/web/components/ui/sonner.tsx
    apps/web/components/ui/form.tsx
    apps/web/app/globals.css
    apps/web/lib/cn.ts
  </files>
  <read_first>
    apps/web/package.json
    apps/web/app/layout.tsx
    apps/web/app/globals.css
    apps/web/app/fonts.ts
    apps/web/next.config.ts
    .planning/phases/01-foundation-auth/01-UI-SPEC.md
    .planning/phases/01-foundation-auth/01-CONTEXT.md
  </read_first>
  <action>
    **Step 1: Initialize shadcn/ui**
    Run from apps/web directory:
    ```bash
    cd apps/web && npx shadcn@latest init
    ```
    Configure: Style "New York", base color "Neutral", CSS variables ON. If interactive prompts are an issue, create components.json manually:
    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "new-york",
      "rsc": true,
      "tsx": true,
      "tailwind": {
        "config": "",
        "css": "app/globals.css",
        "baseColor": "neutral",
        "cssVariables": true
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
      },
      "iconLibrary": "lucide"
    }
    ```

    **Step 2: Install shadcn components**
    ```bash
    npx shadcn@latest add button input label checkbox tabs dialog separator form
    pnpm add sonner
    ```
    Per UI-SPEC component inventory: button, input, label, checkbox, tabs, dialog, separator, sonner, form.

    **Step 3: Create cn utility**
    `apps/web/lib/cn.ts`:
    ```typescript
    import { type ClassValue, clsx } from 'clsx';
    import { twMerge } from 'tailwind-merge';
    export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
    ```

    **Step 4: Configure Grapit design tokens in globals.css**
    Update apps/web/app/globals.css to include Grapit's brand palette as CSS custom properties (keeping whatever shadcn init generates for base tokens, but overriding/adding):

    Per D-14/D-15/D-16 locked decisions:
    ```css
    @import "tailwindcss";

    :root {
      /* Grapit Brand */
      --color-primary: #6C3CE0;
      --color-primary-foreground: #FFFFFF;

      /* Neutral per D-15 */
      --color-gray-900: #1A1A2E;
      --color-gray-500: #71717A;
      --color-gray-400: #A1A1AA;
      --color-gray-200: #E4E4E7;
      --color-gray-100: #F5F5F7;

      /* Semantic per D-16 locked decision */
      --color-error: #EF4444;
      --color-error-surface: #FEF2F2;
      --color-success: #22C55E;
      --color-success-surface: #F0FDF4;
      --color-warning: #FFB41B;
      --color-warning-surface: #FFFBEB;
      --color-info: #6C3CE0;
      --color-info-surface: #F3EFFF;

      /* Spacing scale (4px base, 8-point grid) */
      --spacing-xs: 4px;
      --spacing-sm: 8px;
      --spacing-md: 16px;
      --spacing-lg: 24px;
      --spacing-xl: 32px;
      --spacing-2xl: 48px;
      --spacing-3xl: 64px;
    }
    ```

    Also ensure shadcn's default CSS variable mappings for --primary, --destructive, etc. use Grapit values. Map shadcn's --primary to #6C3CE0, --destructive to #EF4444 (per D-16), etc.

    **Step 5: Configure Sonner**
    Create `apps/web/components/ui/sonner.tsx` wrapper that configures the Toaster with Grapit styling: position top-center, success/error colors matching brand, richColors: true, duration 3000 for success and 5000 for error (per UI-SPEC).

    Add `<Toaster />` to app/layout.tsx.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit/apps/web && ls components/ui/button.tsx components/ui/input.tsx components/ui/tabs.tsx components/ui/checkbox.tsx 2>&1 && cat components.json | head -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components.json exists and contains "style": "new-york"
    - apps/web/components/ui/button.tsx exists and contains "Button"
    - apps/web/components/ui/input.tsx exists and contains "Input"
    - apps/web/components/ui/tabs.tsx exists and contains "Tabs"
    - apps/web/components/ui/checkbox.tsx exists and contains "Checkbox"
    - apps/web/components/ui/dialog.tsx exists and contains "Dialog"
    - apps/web/components/ui/separator.tsx exists and contains "Separator"
    - apps/web/app/globals.css contains "--color-primary: #6C3CE0" and "--color-error: #EF4444" and "--color-gray-900: #1A1A2E"
    - apps/web/lib/cn.ts contains "twMerge" and "clsx"
    - apps/web/app/layout.tsx contains "Toaster"
  </acceptance_criteria>
  <done>shadcn/ui initialized with New York style. All 9 required components installed. Grapit brand palette configured as CSS custom properties. Sonner toast system wired into root layout.</done>
</task>

<task type="auto">
  <name>Task 2: Build GNB, Footer, Home page, and reusable auth components</name>
  <files>
    apps/web/components/layout/gnb.tsx
    apps/web/components/layout/footer.tsx
    apps/web/components/layout/mobile-menu.tsx
    apps/web/components/auth/social-login-button.tsx
    apps/web/components/auth/step-indicator.tsx
    apps/web/components/auth/password-input.tsx
    apps/web/app/layout.tsx
    apps/web/app/page.tsx
    apps/web/public/icons/kakao.svg
    apps/web/public/icons/naver.svg
    apps/web/public/icons/google.svg
  </files>
  <read_first>
    apps/web/components/ui/button.tsx
    apps/web/components/ui/input.tsx
    apps/web/components/ui/separator.tsx
    apps/web/app/layout.tsx
    apps/web/app/globals.css
    apps/web/app/fonts.ts
    apps/web/lib/cn.ts
    .planning/phases/01-foundation-auth/01-UI-SPEC.md
    .planning/phases/01-foundation-auth/01-CONTEXT.md
  </read_first>
  <action>
    **Social login icons (apps/web/public/icons/):**
    Create minimal SVG files for Kakao, Naver, Google brand icons. These are simple SVG paths for the logos (self-hosted per UI-SPEC, NOT from Lucide):
    - `kakao.svg`: Kakao speech bubble icon (yellow on transparent)
    - `naver.svg`: Naver "N" icon (white on transparent)
    - `google.svg`: Google "G" multicolor icon

    **GNB (apps/web/components/layout/gnb.tsx):**
    'use client' component (uses auth state). Per D-10 and UI-SPEC GNB spec:
    - Height: 64px. Background: #FFFFFF. Bottom border: 1px solid #E5E5E5. Sticky top.
    - Logo: Link to /. Text "Grapit", 20px semibold, color Primary (#6C3CE0).
    - Genre tabs: ["뮤지컬", "콘서트", "연극", "전시", "클래식"]. Visible but disabled: opacity-40, cursor-not-allowed. Tooltip on hover via title attribute: "곧 오픈 예정입니다".
    - Search bar: Placeholder input, disabled. Text: "공연명, 아티스트를 검색하세요". Gray background.
    - Auth button (not authenticated): "로그인" text button, links to /auth.
    - Auth button (authenticated): User initials in 32px circle + dropdown with "마이페이지" link and "로그아웃" button. (Use simple state for now, full auth wiring in Plan 05).
    - Props: isAuthenticated: boolean, userName?: string. Default to not authenticated for now.
    - Mobile (below 768px): Logo + hamburger menu. Genre tabs hidden. Search hidden. Auth button visible. Hamburger opens MobileMenu.

    **MobileMenu (apps/web/components/layout/mobile-menu.tsx):**
    'use client' component. Slide-in overlay with auth link for mobile.

    **Footer (apps/web/components/layout/footer.tsx):**
    Per D-13 and UI-SPEC Footer spec:
    - Background: #F5F5F7. Padding: 32px 24px. Min-height: 120px.
    - Links: "이용약관 | 개인정보처리방침 | 고객센터". 14px, regular, Gray-900. Separator "|" in Gray-400.
    - "개인정보처리방침" link is bold (600 weight) per Korean privacy law convention.
    - Copyright: "c 2026 Grapit. All rights reserved." 14px, Gray-500.
    - Links point to # for now (actual pages in later phases).

    **Home page (apps/web/app/page.tsx):**
    Per D-11 and UI-SPEC:
    - Full width, vertically centered content (min-h-screen with flex centering).
    - Grapit text logo (or brand mark placeholder): styled same as GNB logo but larger.
    - Heading: "곧 다양한 공연이 찾아옵니다" -- 28px, semibold, Gray-900.
    - Description: "지금 가입하고 가장 먼저\n새로운 공연 소식을 만나보세요" -- 16px, regular, Gray-500 (#71717A).
    - CTA: Button "로그인 / 회원가입" -- Primary Button 48px height, 200px width, links to /auth.
    - Spacing per UI-SPEC: 24px logo-to-heading, 12px heading-to-description, 32px description-to-CTA.

    **Update root layout (apps/web/app/layout.tsx):**
    - Include GNB at top and Footer at bottom.
    - Main content area with flex-grow.
    - Body uses Pretendard font class, min-h-screen flex flex-col.
    - Ensure Toaster from sonner is rendered.

    **SocialLoginButton (apps/web/components/auth/social-login-button.tsx):**
    Per UI-SPEC Social Login Button Specifications:
    - Props: provider: 'kakao' | 'naver' | 'google', onClick: () => void
    - Height 48px, full width, border-radius 8px, font 16px weight 500.
    - Kakao: bg #FEE500, text #191919, icon from /icons/kakao.svg, label "카카오로 시작하기"
    - Naver: bg #03C75A, text #FFFFFF, icon from /icons/naver.svg, label "네이버로 시작하기"
    - Google: bg #FFFFFF, border 1px solid #DADCE0, text #1F1F1F, icon from /icons/google.svg, label "Google로 시작하기"
    - Loading state: show spinner, disable button.

    **StepIndicator (apps/web/components/auth/step-indicator.tsx):**
    Per D-07 and UI-SPEC Step Indicator:
    - Props: currentStep: 1 | 2 | 3, labels?: string[]
    - 3 dots connected by lines.
    - Active dot: Primary (#6C3CE0) filled, 32px diameter.
    - Completed dot: Primary filled + checkmark (Lucide Check icon).
    - Future dot: Gray-200 (#E4E4E7) outline.
    - Lines between dots: completed = Primary, future = Gray-200.

    **PasswordInput (apps/web/components/auth/password-input.tsx):**
    Per UI-SPEC:
    - Extends shadcn Input with show/hide toggle.
    - Eye icon (Lucide Eye/EyeOff) button on right side of input.
    - Toggle between type="password" and type="text".
    - Same height (44px) and styling as regular Input.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/grapit && pnpm --filter @grapit/web build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/layout/gnb.tsx contains "Grapit" and "뮤지컬" and "곧 오픈 예정입니다" and "로그인" and "sticky"
    - apps/web/components/layout/footer.tsx contains "이용약관" and "개인정보처리방침" and "고객센터" and "2026 Grapit"
    - apps/web/components/layout/footer.tsx contains "font-semibold" applied to 개인정보처리방침 link
    - apps/web/app/page.tsx contains "곧 다양한 공연이 찾아옵니다" and "로그인 / 회원가입" and "/auth"
    - apps/web/components/auth/social-login-button.tsx contains "#FEE500" and "#03C75A" and "카카오로 시작하기" and "네이버로 시작하기" and "Google로 시작하기"
    - apps/web/components/auth/step-indicator.tsx contains "currentStep" and "#6C3CE0" or "primary"
    - apps/web/components/auth/password-input.tsx contains "Eye" and "EyeOff" and 'type="password"' or "type === 'password'"
    - apps/web/public/icons/kakao.svg exists
    - apps/web/public/icons/naver.svg exists
    - apps/web/public/icons/google.svg exists
    - apps/web/app/layout.tsx contains "GNB" and "Footer" and "Toaster"
    - pnpm --filter @grapit/web build exits with code 0
  </acceptance_criteria>
  <done>App shell complete: GNB with disabled genre tabs and search, Footer with legal links, Home empty state with CTA. Auth component primitives (SocialLoginButton, StepIndicator, PasswordInput) ready for use in auth pages.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @grapit/web build` succeeds
2. `pnpm --filter @grapit/web dev` starts, navigate to localhost:3000 -- see GNB with Grapit logo, genre tabs (disabled), home empty state, footer
3. Pretendard font loads (check Network tab for .woff2)
4. Click "로그인 / 회원가입" navigates to /auth (404 is expected, page created in Plan 05)
</verification>

<success_criteria>
- GNB renders per D-10 with all elements (logo, disabled genre tabs, disabled search, login button)
- Footer renders per D-13 with legal links (개인정보처리방침 bold)
- Home page matches D-11 empty state with CTA
- All shadcn components installed and usable
- SocialLoginButton renders 3 branded variants
- StepIndicator shows 3-step progress
- PasswordInput toggles visibility
- Next.js build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
