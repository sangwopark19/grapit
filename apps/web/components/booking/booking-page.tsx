'use client';

/**
 * BookingPage - Stub component for Task 1.
 * Full implementation in Task 3.
 */
export function BookingPage({ performanceId }: { performanceId: string }) {
  return (
    <div>
      <p>Booking page for: {performanceId}</p>
    </div>
  );
}
